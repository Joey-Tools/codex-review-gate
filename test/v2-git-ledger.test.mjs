import assert from "node:assert/strict";
import { spawnSync } from "node:child_process";
import { createHash } from "node:crypto";
import { constants as fsConstants } from "node:fs";
import {
  copyFile,
  mkdtemp,
  open,
  rename,
  rm,
  writeFile,
} from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import test from "node:test";
import { setTimeout as delay } from "node:timers/promises";
import { deflateRawSync, gunzipSync, inflateRawSync } from "node:zlib";

import {
  V2_GIT_LEDGER_BLOB_PATH,
  V2_GIT_LEDGER_CAPABILITY_SCHEMA,
  V2_GIT_LEDGER_CANDIDATE_INVENTORY_RECORD_SCHEMA,
  V2_GIT_LEDGER_ENVELOPE_SCHEMA,
  V2_GIT_LEDGER_CHECKPOINT_ADMISSION_INSPECTION_SCHEMA,
  V2_GIT_LEDGER_CHECKPOINT_FRONTIER_NODES_PATH,
  V2_GIT_LEDGER_CHECKPOINT_PREPARATION_DIAGNOSTIC_SCHEMA,
  V2_GIT_LEDGER_CHECKPOINT_NEXT_UNIT_ADMISSION_HANDLE_SCHEMA,
  V2_GIT_LEDGER_CHECKPOINT_REQUIRED_RESULT_SCHEMA,
  V2_GIT_LEDGER_CHECKPOINT_SOURCE_LOAD_HANDLE_SCHEMA,
  V2_GIT_LEDGER_CHECKPOINT_STATE_BLOB_PATH,
  V2_GIT_LEDGER_CHECKPOINT_STATE_PATH,
  V2_GIT_LEDGER_CONTROL_PLANE_AUTHORITY_SCHEMA,
  V2_GIT_LEDGER_ESTABLISHED_RUNNER_STATE_AUTHORITY_SCHEMA,
  V2_GIT_LEDGER_DISCOVERY_CONTINUITY_RECEIPT_SCHEMA,
  V2_GIT_LEDGER_BOOTSTRAP_INPUT_SCHEMA,
  V2_GIT_LEDGER_HTTP_LIMITS,
  MAX_V2_GIT_LEDGER_COMMITS,
  MAX_V2_GIT_LEDGER_BLOB_BYTES,
  MAX_V2_CANDIDATE_DISPATCH_PLAN_BYTES,
  MAX_V2_CURRENT_OPEN_CANDIDATE_DISPATCH_BINDING_BYTES,
  MAX_V2_CURRENT_OPEN_CANDIDATE_DISPATCH_PLAN_BYTES,
  MAX_V2_AUTOMATIC_RECOVERY_ARTIFACT_BINDING_ACTION_BYTES,
  MAX_V2_AUTOMATIC_RECOVERY_ARTIFACT_BINDING_RECEIPT_BYTES,
  MAX_V2_SCHEDULED_CANDIDATE_LEDGER_RECORDS,
  V2_GIT_LEDGER_OIDC_AUDIENCE,
  V2_GIT_LEDGER_OIDC_CLAIMS,
  V2_GIT_LEDGER_PROVENANCE_RECEIPT_SCHEMA,
  V2_GIT_LEDGER_PROVENANCE_REQUEST_SCHEMA,
  V2_GIT_LEDGER_PROVENANCE_VERIFIER_REQUEST_SCHEMA,
  V2_GIT_LEDGER_PROVENANCE_VERIFIER_RESULT_SCHEMA,
  V2_GIT_LEDGER_PUBLIC_ENVELOPE_SCHEMA,
  assertV2GitLedgerControlPlaneAuthorityHandle,
  assertV2GitLedgerEstablishedRunnerStateAuthorityHandle,
  assertV2GitLedgerInitialRunnerStateAuthorityHandle,
  assertV2GitLedgerAutomaticRequestIntentHandle,
  assertV2GitLedgerAutomaticReservationHandle,
  assertV2GitLedgerReservationStatusIntentHandle,
  assertV2GitLedgerStatusWriteIntentHandle,
  assertV2GitLedgerCandidateDispatchHandle,
  assertV2GitLedgerCandidateDispatchResultHandle,
  V2_GIT_LEDGER_CANDIDATE_INVENTORY_ATTACHMENT_PATH,
  V2_GIT_LEDGER_REF,
  createV2GitHubGitLedger,
  createV2GitHubGitLedgerBootstrap,
  createV2GitLedgerCheckpointDiagnosticAdapter,
  createV2GitLedgerCandidateInventoryRecord,
  createV2GitLedgerCandidateInventoryEvaluatedScopeReceipt,
  createV2GitLedgerCandidateDispatchEvaluatedScopeReceipt,
  createV2GitLedgerCandidateDispatchRecord,
  createV2GitLedgerDiscoveryContinuityReceipt,
  createV2GitLedgerEffectIntentRecord,
  createV2GitLedgerEffectResponseRecord,
  createV2GitLedgerEvaluatedScopeReceipt,
  createV2GitLedgerManualSelectionReceipt,
  createV2GitLedgerRecord,
  calculateV2GitLedgerCandidateInventoryCycleCommitBudget,
  calculateV2GitLedgerCandidateDispatchCommitBudget,
  calculateV2GitLedgerCandidateDispatchReservedCommitBudget,
  classifyV2GitLedgerCandidateDispatchRecoveryPrefix,
  deriveV2GitLedgerCandidateInventoryAuthority,
  deriveV2GitLedgerCandidateDispatchAuthority,
  deriveV2GitLedgerAuthority,
  deriveV2GitLedgerRunnerState,
  digestV2GitLedgerStableCapabilityAuthorization,
  digestV2GitLedgerPayload,
  projectV2GitLedgerAutomaticReservation,
  projectV2GitLedgerAutomaticReviewRequestTransport,
  projectV2GitLedgerReservationStatusTransport,
  projectV2GitLedgerStatusWriteTransport,
  projectV2GitLedgerCandidateDispatchBinding,
  projectV2GitLedgerCandidateDispatchPlan,
  validateV2GitLedgerControlPlaneAuthority,
  validateV2GitLedgerCandidateDispatchBinding,
  validateV2GitLedgerCandidateDispatchPlan,
  validateV2GitLedgerCandidateDispatchReservation,
  validateV2GitLedgerDiscoveryContinuityReceipt,
  validateV2GitLedgerEstablishedRunnerStateAuthority,
  validateV2GitLedgerInitialRunnerStateAuthority,
  validateV2GitLedgerRecord,
} from "../packages/action/src/v2/git-ledger.mjs";
import {
  assertV2ProductionRunnerAuthorityHandle,
  createV2ControlPlaneReceiptFromGitLedgerAuthority,
  createV2ProductionRunnerAuthority,
} from "../packages/action/src/v2/control-plane-receipt.mjs";
import {
  MAX_V2_CANDIDATES_PER_SHARD,
  V2_CANDIDATE_INVENTORY_SCHEMA,
  V2_CANDIDATE_SHARD_RECEIPT_SCHEMA,
  createV2GitHubCandidateInventory,
  createV2GitHubCurrentOpenCandidateInventory,
  consumeV2CurrentOpenProductionCandidateAuthorityHandle,
  finalizeV2CandidateInventoryCycle,
  validateV2CandidateInventory,
} from "../packages/action/src/v2/candidate-inventory.mjs";
import {
  V2_SCHEDULER_SCHEMA,
  V2_SCHEDULER_SCHEMA_VERSION,
  planV2Actions,
} from "../packages/action/src/v2/scheduler.mjs";
import {
  V2_RUNNER_SCHEMA,
  buildV2StatusPlan,
  getV2AutomaticRecoveryArtifactBindingCandidateHandle,
  getV2AutomaticRequestRecoveryHandle,
  runV2Operation,
} from "../packages/action/src/v2/runner.mjs";
import { reduceV2Snapshot } from "../packages/action/src/v2/reducer.mjs";
import {
  acquireV2LeaseThenLoadDiscovery,
  loadV2MinimalLiveScope,
} from "../packages/action/src/v2/workflow-controller.mjs";
import {
  createV2GitHubWorkflowPreflight,
} from "../packages/action/src/v2/workflow-preflight.mjs";
import {
  createV2PersistentMerkleMap,
} from "../packages/action/src/v2/persistent-frontier.mjs";
import {
  V2_PUBLIC_WAIT_POLICY,
  V2_SERVER_ENFORCEMENT_POLICY,
  prepareV2WorkflowCommand,
} from "../packages/action/src/v2/workflow-command.mjs";
import {
  createV2GitHubTransport,
  loadV2ProviderPreScopeArtifact,
  V2_TRANSPORT_DEFAULT_LIMITS,
} from "../packages/action/src/v2/transport.mjs";

const REPOSITORY = {
  owner: "owner",
  name: "repo",
  id: "42",
  node_id: "R_repo",
  owner_id: "88",
};
const HEAD = "a".repeat(40);
const BASE = "b".repeat(40);
const POTENTIAL = "c".repeat(40);
const TIME = "2026-08-13T12:00:00.000Z";
const PR = { number: 7, node_id: "PR_7" };
const OWNER = { run_id: "9001", run_attempt: 1, actor_id: "15368" };
const C65_BATCH0_GIT_FIXTURE_URL = new URL(
  "./fixtures/v2-git-ledger-c65-batch0.git-fixture.json.gz",
  import.meta.url,
);
const C65_BATCH0_GIT_FIXTURE_GZIP_BYTES = 723_650;
const C65_BATCH0_GIT_FIXTURE_GZIP_SHA256 =
  "sha256:62308a780a93d0b2399881b7556d745d2b6fbc9aac4ce779e8f110bf1b6c18a0";
const C65_BATCH0_GIT_FIXTURE_JSON_BYTES = 4_051_651;
const C65_BATCH0_GIT_FIXTURE_JSON_SHA256 =
  "sha256:b3baf61e1d92c840f3fd1775bd1b34123a0734c32798ea31755d4906cb708db5";
const C65_BATCH0_GIT_FIXTURE_MAX_GZIP_BYTES = 2 * 1024 * 1024;
const C65_BATCH0_GIT_FIXTURE_MAX_JSON_BYTES = 16 * 1024 * 1024;
const C65_BATCH0_GIT_FIXTURE_O_CLOEXEC = fsConstants.O_CLOEXEC ?? ({
  darwin: 0x01000000,
  linux: 0x00080000,
})[process.platform] ?? 0;
const C65_BATCH0_GIT_FIXTURE_O_NONBLOCK = fsConstants.O_NONBLOCK ?? 0;
const C65_BATCH0_GIT_FIXTURE_OPEN_FLAGS =
  fsConstants.O_RDONLY |
  fsConstants.O_NOFOLLOW |
  C65_BATCH0_GIT_FIXTURE_O_NONBLOCK |
  C65_BATCH0_GIT_FIXTURE_O_CLOEXEC;
const C65_BATCH0_GIT_FIXTURE_COUNTS = Object.freeze({
  blobs: 199,
  trees: 199,
  commits: 198,
});
const C65_BATCH0_GIT_FIXTURE_REF =
  "874a4280bd98032c47a2a5b73ab5e885b33edbc0";
const C65_BATCH0_GIT_FIXTURE_CLOCK_SECOND = 119;
const HISTORICAL_LEDGER_FIXTURES = new WeakMap();
const CONTROL = {
  comment_id: "71",
  comment_node_id: "IC_71",
  raw_body_sha256: `sha256:${"d".repeat(64)}`,
};
const ACTOR = {
  id: "15368",
  node_id: "U_actions",
  login: "github-actions[bot]",
  type: "Bot",
};
const HUMAN_ACTOR = {
  id: "42",
  node_id: "USER_reviewer",
  login: "reviewer",
  type: "User",
};
const GITHUB_ACTIONS_APP = {
  id: "15368",
  node_id: "A_github_actions",
  slug: "github-actions",
};
const CODEX_ACTOR = {
  id: "199175422",
  node_id: "BOT_kgDOC98s_g",
  login: "chatgpt-codex-connector[bot]",
  type: "Bot",
};
const CODEX_APP = {
  id: "1144995",
  node_id: "A_kwHOAOQ6Gs4AEXij",
  slug: "chatgpt-codex-connector",
};
const APP = {
  id: "15368",
  node_id: "A_actions",
  slug: "github-actions",
};
// review helper synthetic-token pool joey-private-v3: bearer-a
const SYNTHETIC_BEARER = "codex_synth_v1_bearer_a";

test("bootstrap proves one-winner canary and high-level lease append is exact-ref stable", async () => {
  const fixture = githubGitFixture();
  const ledger = makeLedger(fixture);

  const bootstrap = await ledger.bootstrapCapability();
  assert.equal(bootstrap.already_current, false);
  assert.equal(bootstrap.contenders.length, 2);
  assert.deepEqual(
    bootstrap.contenders.filter((entry) => entry.outcome === "winner").length,
    1,
  );
  assert.equal(
    bootstrap.contenders.filter((entry) => entry.outcome === "non-fast-forward").length,
    1,
  );
  assert.equal(bootstrap.stable, true);

  const initial = await ledger.load();
  assert.equal(initial.commit_count, 3);
  assert.equal(initial.capability.attestation_commit_sha, initial.tip_commit_sha);
  assert.equal(initial.pre_ref.target_commit_sha, initial.post_ref.target_commit_sha);

  const lease = (await acquireDefaultDiscovery(ledger)).lease_receipt;
  assert.equal(lease.acquire_commit_sha, fixture.refTarget);

  const loaded = await ledger.load();
  assert.equal(loaded.tip_commit_sha, lease.acquire_commit_sha);
  assert.equal(loaded.post_ref.target_commit_sha, lease.acquire_commit_sha);
  assert.equal(loaded.effect_intent_count, 0);
  assert.equal(loaded.active_lease.lease_id, lease.lease_id);
  assert.deepEqual(
    loaded.records.map((entry) => entry.envelope.record_type),
    ["genesis", "capability-canary", "capability-attestation", "lease-acquire"],
  );
  assert.equal(
    fixture.calls.some((call) =>
      call.method === "PATCH" && JSON.parse(call.body).force !== false),
    false,
  );
});

test("public raw effect append seams reject effect identities before writes",
  async (t) => {
    const snapshotWrites = (fixture) => ({
      write_calls: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
      patches: fixture.calls.filter(({ method }) => method === "PATCH").length,
      object_posts: fixture.calls.filter(({ method, path }) =>
        method === "POST" && new Set([
          "/git/blobs", "/git/trees", "/git/commits",
        ]).has(path)).length,
    });
    const assertRejectedBeforeWrites = async ({ fixture, ledger, append }) => {
      const loaded = await ledger.load();
      const before = snapshotWrites(fixture);
      await assert.rejects(
        append(),
        (error) => error.code === "UNTRUSTED_RAW_EFFECT_APPEND",
      );
      assert.deepEqual(snapshotWrites(fixture), before);
      assert.equal((await ledger.load()).tip_commit_sha, loaded.tip_commit_sha);
    };
    const rawIntentContext = async (suffix) => {
      const fixture = githubGitFixture();
      const ledger = makeLedger(fixture);
      await ledger.bootstrapCapability();
      const lease = (await acquireDefaultDiscovery(ledger)).lease_receipt;
      return {
        fixture,
        ledger,
        input: effectIntentInput({
          predecessor: lease.acquire_commit_sha,
          lease,
          effectId: `effect-raw-${suffix}`,
          idempotencyKey: `intent-raw-${suffix}`,
          at: TIME,
        }),
      };
    };

    await t.test("appendRecord", async () => {
      const context = await rawIntentContext("record");
      await assertRejectedBeforeWrites({
        ...context,
        append: () => context.ledger.appendRecord(
          createV2GitLedgerEffectIntentRecord(context.input),
        ),
      });
    });

    await t.test("append alias", async () => {
      const context = await rawIntentContext("alias");
      await assertRejectedBeforeWrites({
        ...context,
        append: () => context.ledger.append(
          createV2GitLedgerEffectIntentRecord(context.input),
        ),
      });
    });

    await t.test("appendEffectIntent", async () => {
      const context = await rawIntentContext("intent");
      await assertRejectedBeforeWrites({
        ...context,
        append: () => context.ledger.appendEffectIntent(context.input),
      });
    });

    await t.test("appendEffectResponse", async () => {
      const fixture = githubGitFixture();
      const context = await createProtectedInitialRunnerContext(fixture);
      const schedulerPlan = schedulerObservationRecord({
        predecessor: context.lease.acquire_commit_sha,
        lease: context.lease,
        priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
        initialAuthority: context.initialAuthority,
      }).payload.action;
      const schedulerAppend = await context.ledger
        .appendInitialSchedulerObservation({
          initial_runner_state_authority: context.initialAuthority,
          scheduler_evaluation: schedulerPlan.scheduler_evaluation,
          status_plan: schedulerPlan.status_plan,
        });
      const statusIntent = await context.ledger.appendStatusWriteIntent({
        scheduler_append: schedulerAppend,
        status_write_index: 0,
      });
      const intentRecord = recordFromFixtureEnvelope(fixtureEnvelopeAt(
        fixture,
        statusIntent.intent_append_receipt.commit_sha,
      ));
      const receipt = statusWriteResponseReceipt(
        statusIntent.transport,
        statusIntent.intent_append_receipt.ref_reread.server_time,
      );
      await assertRejectedBeforeWrites({
        fixture,
        ledger: context.ledger,
        append: () => context.ledger.appendEffectResponse({
          intent_record: intentRecord,
          intent_commit_sha: statusIntent.intent_append_receipt.commit_sha,
          server_observed_at: receipt.refetch_server_time,
          receipt,
        }),
      });
    });
  });

test("public append cannot replace one validated null-effect snapshot through later accessor values",
  async () => {
    const fixture = githubGitFixture();
    const ledger = makeLedger(fixture);
    await ledger.bootstrapCapability();
    const discovery = await acquireDefaultDiscovery(ledger);
    const lease = discovery.lease_receipt;
    const loaded = await ledger.load();
    const boundary = loaded.post_ref.server_time;
    const nullEffectRecord = createV2GitLedgerRecord({
      record_type: "lease-release",
      predecessor_commit_sha: loaded.tip_commit_sha,
      pull_request: structuredClone(lease.scope.pull_request),
      head_ref_oid: lease.scope.head_ref_oid,
      base_ref_oid: lease.scope.base_ref_oid,
      potential_merge_commit_oid:
        lease.scope.potential_merge_commit_oid,
      kind: null,
      effect_id: null,
      idempotency_key: null,
      server_observed_at: boundary,
      payload: {
        lease_id: lease.lease_id,
        owner: structuredClone(lease.owner),
        acquire_commit_sha: lease.acquire_commit_sha,
        released_at: boundary,
      },
      control_comment_binding: loaded.control_comment_binding,
      lease: leaseBinding(lease),
    });
    const effect = effectRecord({
      predecessor: loaded.tip_commit_sha,
      lease,
      effectId: "accessor-late-effect",
      idempotencyKey: "accessor-late-effect-key",
      at: boundary,
    });
    const keys = Object.keys(nullEffectRecord);
    assert.deepEqual(keys, Object.keys(effect));
    assert.equal(keys.at(-1), "record_digest");

    const accessorRecord = ({ onRecordDigestRead, selectRecord }) => {
      const record = {};
      for (const key of keys) {
        Object.defineProperty(record, key, {
          enumerable: true,
          configurable: false,
          get() {
            const selected = selectRecord();
            const value = selected[key];
            if (key === "record_digest") onRecordDigestRead();
            return value;
          },
        });
      }
      return record;
    };

    let validationRecordDigestReads = 0;
    const probe = accessorRecord({
      onRecordDigestRead() {
        validationRecordDigestReads += 1;
      },
      selectRecord: () => nullEffectRecord,
    });
    assert.deepEqual(validateV2GitLedgerRecord(probe), nullEffectRecord);
    assert.ok(validationRecordDigestReads > 0);

    let nullSnapshotReadsRemaining = validationRecordDigestReads;
    let effectIdentityVisible = false;
    const switchingRecord = accessorRecord({
      onRecordDigestRead() {
        if (!effectIdentityVisible) {
          nullSnapshotReadsRemaining -= 1;
          if (nullSnapshotReadsRemaining === 0) {
            effectIdentityVisible = true;
          }
        }
      },
      selectRecord: () => effectIdentityVisible ? effect : nullEffectRecord,
    });
    const before = {
      write_calls: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
      patches: fixture.calls.filter(({ method }) => method === "PATCH").length,
      object_posts: fixture.calls.filter(({ method, path }) =>
        method === "POST" && new Set([
          "/git/blobs", "/git/trees", "/git/commits",
        ]).has(path)).length,
    };
    await assert.rejects(
      ledger.appendRecord(switchingRecord, {
        evaluated_scope_receipt:
          discovery.effect_evaluated_scope_receipt,
      }),
      (error) => error.code === "lease-scope-authority-mismatch",
    );
    assert.equal(effectIdentityVisible, true);
    assert.deepEqual({
      write_calls: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
      patches: fixture.calls.filter(({ method }) => method === "PATCH").length,
      object_posts: fixture.calls.filter(({ method, path }) =>
        method === "POST" && new Set([
          "/git/blobs", "/git/trees", "/git/commits",
        ]).has(path)).length,
    }, before);
    const final = await ledger.load();
    assert.equal(final.tip_commit_sha, loaded.tip_commit_sha);
    assert.equal(final.active_lease.lease_id, lease.lease_id);
    assert.equal(final.effect_intent_count, 0);
  });

test("bootstrap reuses one stable capability across live receipt churn", async () => {
  const fixture = githubGitFixture();
  const originalCapability = capabilityReceipt();
  const ledger = makeLedger(fixture, originalCapability);
  await ledger.bootstrapCapability();
  const tipBefore = fixture.refTarget;
  const writesBefore = fixture.writeCalls;
  const objectsBefore = {
    blobs: fixture.blobs.size,
    trees: fixture.trees.size,
    commits: fixture.commits.size,
  };

  const changedBase = capabilityBase();
  changedBase.repository_endpoint_receipt.server_time =
    "2026-08-13T12:00:01.000Z";
  changedBase.repository_endpoint_receipt.raw_body_sha256 =
    `sha256:${"5".repeat(64)}`;
  changedBase.permissions.observation_receipt_digest =
    `sha256:${"6".repeat(64)}`;
  changedBase.observed_at = "2026-08-13T12:00:01.000Z";
  for (const release of [
    changedBase.controller_release,
    changedBase.protection.source_workflow_pin,
  ]) {
    release.caller_workflow_file_receipt_digest = `sha256:${"d".repeat(64)}`;
    release.job_workflow_file_receipt_digest = `sha256:${"e".repeat(64)}`;
    release.release_receipt_digest = `sha256:${"f".repeat(64)}`;
  }
  const changedCapability = sealCapability(changedBase);
  assert.equal(
    digestV2GitLedgerStableCapabilityAuthorization(originalCapability),
    digestV2GitLedgerStableCapabilityAuthorization(changedCapability),
  );
  const restricted = createV2GitHubGitLedgerBootstrap({
    fetch: fixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: REPOSITORY,
    ledgerRef: V2_GIT_LEDGER_REF,
    restBaseUrl: "https://api.github.test",
    bootstrapCapabilityInput: bootstrapInputForCapability(changedCapability),
    verifyWorkflowProvenance: provenanceVerifier(),
  });
  const restarted = await restricted.bootstrapCapability();
  assert.equal(restarted.bootstrap_receipt.already_current, true);
  assert.equal(fixture.refTarget, tipBefore);
  assert.equal(fixture.writeCalls, writesBefore);
  assert.deepEqual({
    blobs: fixture.blobs.size,
    trees: fixture.trees.size,
    commits: fixture.commits.size,
  }, objectsBefore);
});

test("two concurrent siblings advance the protected ref exactly once", async () => {
  const fixture = githubGitFixture();
  const ledger = makeLedger(fixture);
  await ledger.bootstrapCapability();
  const loaded = await ledger.load();
  const authority = {
    evaluated_scope_receipt: await ledger.defaultEvaluatedScopeReceipt(),
  };
  const left = {
    ...leaseInput(loaded.tip_commit_sha),
    lease_id: "lease-run-9001-left",
  };
  const right = {
    ...leaseInput(loaded.tip_commit_sha),
    lease_id: "lease-run-9001-right",
  };
  const outcomes = await Promise.allSettled([
    ledger.acquireLease(left, authority),
    ledger.acquireLease(right, authority),
  ]);
  assert.equal(outcomes.filter((entry) => entry.status === "fulfilled").length, 1);
  assert.equal(outcomes.filter((entry) => entry.status === "rejected").length, 1);
  const rejected = outcomes.find((entry) => entry.status === "rejected");
  assert.equal(rejected.reason.code, "non-fast-forward");
  assert.match(rejected.reason.message, /unreachable/u);

  const final = await ledger.load();
  assert.equal(final.effect_intent_count, 0);
  assert.equal(final.records.at(-1).envelope.record_type, "lease-acquire");
  assert.equal(final.records.at(-1).commit_sha, fixture.refTarget);
  assert.equal(fixture.commits.size > final.commit_count, true,
    "the losing sibling remains an unreachable Git object");
});

test("capability, exact ref, rules, and observed permission mismatches fail before writes", async () => {
  const fixture = githubGitFixture();
  assert.throws(
    () => createV2GitHubGitLedger({
      ...factoryInput(fixture, capabilityReceipt()),
      ledgerRef: "refs/heads/not-the-ledger",
    }),
    /must be exactly/u,
  );

  const badRulesBase = capabilityBase();
  badRulesBase.protection.non_fast_forward_blocked = false;
  assert.throws(
    () => makeLedger(fixture, sealCapability(badRulesBase)),
    /protection is incomplete/u,
  );

  const broadPermissionBase = capabilityBase();
  broadPermissionBase.permissions.contents_write_observed = false;
  assert.throws(
    () => makeLedger(fixture, sealCapability(broadPermissionBase)),
    /observed contents:write/u,
  );
  assert.throws(
    () => makeLedger(fixture, null),
    /capability authority/u,
  );
  assert.equal(fixture.calls.length, 0);
});

test("restricted bootstrap is the only absent-ref path and seals production capability", async () => {
  const fixture = githubGitFixture();
  const restricted = createV2GitHubGitLedgerBootstrap({
    fetch: fixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: REPOSITORY,
    ledgerRef: V2_GIT_LEDGER_REF,
    restBaseUrl: "https://api.github.test",
    bootstrapCapabilityInput: bootstrapInput(),
    verifyWorkflowProvenance: provenanceVerifier(),
  });
  assert.deepEqual(Object.keys(restricted), ["bootstrapCapability"]);
  const result = await restricted.bootstrapCapability();
  assert.equal(result.bootstrap_receipt.stable, true);
  assert.equal(
    result.bootstrap_receipt.capability_attestation_commit_sha,
    fixture.refTarget,
  );
  assert.equal(result.sealed_capability_receipt.feature_enabled, true);
  const loaded = await result.ledger.load();
  assert.equal(loaded.tip_commit_sha, fixture.refTarget);
});

test("restricted bootstrap rejects ineligible input, wrong ref, and bad OIDC", async (t) => {
  await t.test("ineligible input", () => {
    const fixture = githubGitFixture();
    const input = bootstrapInputBase();
    input.bootstrap_eligible = false;
    assert.throws(
      () => createV2GitHubGitLedgerBootstrap({
        fetch: fixture.fetch,
        token: SYNTHETIC_BEARER,
        repository: REPOSITORY,
        ledgerRef: V2_GIT_LEDGER_REF,
        restBaseUrl: "https://api.github.test",
        bootstrapCapabilityInput: sealBootstrapInput(input),
        verifyWorkflowProvenance: provenanceVerifier(),
      }),
      /not eligible/u,
    );
    assert.equal(fixture.writeCalls, 0);
  });

  await t.test("wrong ref", () => {
    const fixture = githubGitFixture();
    assert.throws(
      () => createV2GitHubGitLedgerBootstrap({
        fetch: fixture.fetch,
        token: SYNTHETIC_BEARER,
        repository: REPOSITORY,
        ledgerRef: "refs/heads/not-ledger",
        restBaseUrl: "https://api.github.test",
        bootstrapCapabilityInput: bootstrapInput(),
        verifyWorkflowProvenance: provenanceVerifier(),
      }),
      /must be exactly/u,
    );
    assert.equal(fixture.writeCalls, 0);
  });

  await t.test("bad OIDC", async () => {
    const fixture = githubGitFixture();
    const baseVerifier = provenanceVerifier();
    const restricted = createV2GitHubGitLedgerBootstrap({
      fetch: fixture.fetch,
      token: SYNTHETIC_BEARER,
      repository: REPOSITORY,
      ledgerRef: V2_GIT_LEDGER_REF,
      restBaseUrl: "https://api.github.test",
      bootstrapCapabilityInput: bootstrapInput(),
      verifyWorkflowProvenance: async (request) => {
        const result = structuredClone(await baseVerifier(request));
        const receipt = result.receipt;
        delete receipt.receipt_digest;
        receipt.signature_verified = false;
        receipt.receipt_digest = digestCanonical(
          "codex-review-gate-v2-git-ledger-provenance",
          receipt,
        );
        return result;
      },
    });
    await assert.rejects(
      restricted.bootstrapCapability(),
      /not live, verified, and signature-bound/u,
    );
    assert.equal(fixture.writeCalls, 0);
  });
});

test("reusable workflow source requires exact SHA, not a mutable ref", () => {
  const fixture = githubGitFixture();
  const base = capabilityBase();
  base.controller_release.job_workflow_ref =
    "Joey-Tools/codex-review-gate-action/.github/workflows/" +
    "codex-review-gate.yml@refs/tags/v2";
  base.protection.source_workflow_pin.job_workflow_ref =
    base.controller_release.job_workflow_ref;
  assert.throws(
    () => makeLedger(fixture, sealCapability(base)),
    /exact-SHA ref/u,
  );
  assert.equal(fixture.writeCalls, 0);
});

test("multi-parent, noncanonical bytes, and digest tampering fail closed", async (t) => {
  await t.test("multi-parent", async () => {
    const fixture = githubGitFixture();
    const ledger = makeLedger(fixture);
    await ledger.bootstrapCapability();
    fixture.commits.get(fixture.refTarget).parents.push({ sha: "f".repeat(40) });
    await assert.rejects(
      ledger.load(),
      (error) => error.code === "multi-parent",
    );
  });

  await t.test("tree identity", async () => {
    const fixture = githubGitFixture();
    const ledger = makeLedger(fixture);
    await ledger.bootstrapCapability();
    const commit = fixture.commits.get(fixture.refTarget);
    const tree = fixture.trees.get(commit.tree.sha);
    tree.tree[0].sha = "f".repeat(40);
    await assert.rejects(
      ledger.load(),
      (error) => error.code === "tree-identity",
    );
  });

  await t.test("commit identity", async () => {
    const fixture = githubGitFixture();
    const ledger = makeLedger(fixture);
    await ledger.bootstrapCapability();
    fixture.commits.get(fixture.refTarget).author.name = "Untrusted Writer";
    await assert.rejects(
      ledger.load(),
      (error) => error.code === "commit-identity",
    );
  });

  await t.test("noncanonical blob", async () => {
    const fixture = githubGitFixture();
    const ledger = makeLedger(fixture);
    await ledger.bootstrapCapability();
    fixture.rewriteTipText((text) => ` ${text}`);
    await assert.rejects(
      ledger.load(),
      (error) => error.code === "noncanonical-blob",
    );
  });

  await t.test("tampered envelope", async () => {
    const fixture = githubGitFixture();
    const ledger = makeLedger(fixture);
    await ledger.bootstrapCapability();
    fixture.rewriteTipEnvelope((envelope) => {
      envelope.payload.protection_receipt_digest = `sha256:${"0".repeat(64)}`;
    });
    await assert.rejects(
      ledger.load(),
      /digest is invalid/u,
    );
  });

  await t.test("tampered stored JWT", async () => {
    const fixture = githubGitFixture();
    const ledger = makeLedger(fixture);
    await ledger.bootstrapCapability();
    fixture.rewriteTipEnvelope((envelope) => {
      envelope.workflow_provenance_jwt =
        `${envelope.workflow_provenance_jwt}A`;
      resealEnvelope(envelope);
    });
    await assert.rejects(
      ledger.load(),
      /JWT digest differs/u,
    );
  });
});

test("reachable history rejects duplicate effect and idempotency identities", async (t) => {
  async function historyWithTwoIntents() {
    const fixture = githubGitFixture();
    const context = await createProtectedInitialRunnerContext(fixture);
    const firstLoaded = await context.ledger.load();
    const firstRecord = effectRecord({
      predecessor: firstLoaded.tip_commit_sha,
      lease: context.lease,
      effectId: "effect-first",
      idempotencyKey: "key-first",
      at: firstLoaded.post_ref.server_time,
    });
    await materializeHistoricalProductionRecord({
      ledger: context.ledger,
      loaded: firstLoaded,
      record: firstRecord,
      evaluatedScopeReceipt: context.evaluatedScopeReceipt,
    });
    const secondLoaded = await context.ledger.load();
    const secondRecord = effectRecord({
      predecessor: secondLoaded.tip_commit_sha,
      lease: context.lease,
      effectId: "effect-second",
      idempotencyKey: "key-second",
      at: secondLoaded.post_ref.server_time,
    });
    await materializeHistoricalProductionRecord({
      ledger: context.ledger,
      loaded: secondLoaded,
      record: secondRecord,
      evaluatedScopeReceipt: context.evaluatedScopeReceipt,
    });
    return {
      fixture,
      ledger: context.ledger,
      firstEffectId: firstRecord.effect_id,
      firstIdempotencyKey: firstRecord.idempotency_key,
    };
  }

  await t.test("duplicate effect", async () => {
    const { fixture, ledger, firstEffectId } = await historyWithTwoIntents();
    fixture.rewriteTipEnvelope((envelope) => {
      envelope.effect_id = firstEffectId;
      envelope.workflow_provenance.operation_binding.record_identity.effect_id =
        firstEffectId;
      resealProvenance(envelope.workflow_provenance);
      resealEnvelope(envelope);
    });
    await assert.rejects(
      ledger.load(),
      (error) => error.code === "duplicate-effect",
    );
  });

  await t.test("duplicate idempotency", async () => {
    const { fixture, ledger, firstIdempotencyKey } =
      await historyWithTwoIntents();
    fixture.rewriteTipEnvelope((envelope) => {
      envelope.idempotency_key = firstIdempotencyKey;
      envelope.workflow_provenance.operation_binding.record_identity
        .idempotency_key = firstIdempotencyKey;
      resealProvenance(envelope.workflow_provenance);
      resealEnvelope(envelope);
    });
    await assert.rejects(
      ledger.load(),
      (error) => error.code === "duplicate-idempotency",
    );
  });
});

test("lease expiry and release both prevent later effects without refund", async () => {
  const fixture = githubGitFixture();
  const context = await createProtectedInitialRunnerContext(fixture);
  const plan = schedulerObservationRecord({
    predecessor: context.lease.acquire_commit_sha,
    lease: context.lease,
    priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
    initialAuthority: context.initialAuthority,
  }).payload.action;
  fixture.advanceServerTime(1_000);

  const postsBeforeExpiry = fixture.writeCalls;
  await assert.rejects(
    context.ledger.appendInitialSchedulerObservation({
      initial_runner_state_authority: context.initialAuthority,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: plan.status_plan,
    }),
    (error) => new Set([
      "lease-required", "lease-expired", "lease-write-window",
      "initial-runner-authority-stale",
    ]).has(error.code),
  );
  assert.equal(fixture.writeCalls, postsBeforeExpiry,
    "expired lease rejection must occur before Git object writes");

  const fixture2 = githubGitFixture();
  const context2 = await createProtectedInitialRunnerContext(fixture2);
  const plan2 = schedulerObservationRecord({
    predecessor: context2.lease.acquire_commit_sha,
    lease: context2.lease,
    priorAuthorityDigest: context2.initialAuthority.prior_authority_digest,
    initialAuthority: context2.initialAuthority,
  }).payload.action;
  await releaseFixtureDiscoveryLease(context2.ledger, context2.discovery);
  const writesBeforeReleased = fixture2.writeCalls;
  await assert.rejects(
    context2.ledger.appendInitialSchedulerObservation({
      initial_runner_state_authority: context2.initialAuthority,
      scheduler_evaluation: plan2.scheduler_evaluation,
      status_plan: plan2.status_plan,
    }),
    (error) => new Set([
      "lease-required", "initial-runner-authority-stale", "stale-predecessor",
    ]).has(error.code),
  );
  assert.equal(fixture2.writeCalls, writesBeforeReleased,
    "released lease rejection must occur before Git object writes");
});

test("absent ref and exact ref reread mismatch fail closed", async () => {
  const absentFixture = githubGitFixture();
  await assert.rejects(
    makeLedger(absentFixture).load(),
    (error) => error.code === "ref-absent",
  );

  const fixture = githubGitFixture();
  const ledger = makeLedger(fixture);
  await ledger.bootstrapCapability();
  const initial = await ledger.load();
  fixture.mismatchNextRefReread();
  await assert.rejects(
    ledger.acquireLease(leaseInput(initial.tip_commit_sha)),
    (error) => error.code === "ref-reread-mismatch",
  );
});

test("an unattested canary tip is recovered by a new sealed race", async () => {
  const fixture = githubGitFixture();
  const ledger = makeLedger(fixture);
  fixture.failNextAttestationUpdate();
  await assert.rejects(
    ledger.bootstrapCapability(),
    (error) => error.code === "unexpected-http-status",
  );
  assert.equal(fixture.tipRecordType(), "capability-canary");
  const writesBeforeRecovery = fixture.writeCalls;
  await assert.rejects(
    ledger.load(),
    (error) => error.code === "capability-recovery-required",
  );
  assert.equal(fixture.writeCalls, writesBeforeRecovery,
    "production access cannot advance an unattested capability canary");

  const recovered = await ledger.bootstrapCapability();
  assert.equal(recovered.already_current, false);
  const loaded = await ledger.load();
  assert.deepEqual(
    loaded.records.map((entry) => entry.envelope.record_type),
    ["genesis", "capability-canary", "capability-canary", "capability-attestation"],
  );
  assert.deepEqual(
    loaded.records.at(-1).envelope.payload.recovered_unattested_canary_commits,
    [loaded.records[1].commit_sha],
  );
});

test("a new capability epoch revalidates old JWTs under their embedded authority", async () => {
  const fixture = githubGitFixture();
  const original = makeLedger(fixture);
  await original.bootstrapCapability();
  const originalLoad = await original.load();

  const nextBase = capabilityBase();
  const nextRulesetDigest = `sha256:${"a".repeat(64)}`;
  nextBase.permissions.observation_receipt_digest = `sha256:${"b".repeat(64)}`;
  nextBase.protection.live_ruleset_receipt_digest = nextRulesetDigest;
  nextBase.ruleset_receipt.receipt_digest = nextRulesetDigest;
  nextBase.ruleset_receipt.protection_digest = nextRulesetDigest;
  nextBase.protection_receipt.protection_digest = nextRulesetDigest;
  nextBase.protection_receipt.ruleset_receipt_digest = nextRulesetDigest;
  nextBase.protection_receipt.receipt_digest = `sha256:${"c".repeat(64)}`;
  const nextCapability = sealCapability(nextBase);
  const nextVerifier = provenanceVerifier({ jtiPrefix: "epoch-two-jti" });
  const restricted = createV2GitHubGitLedgerBootstrap({
    fetch: fixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: REPOSITORY,
    ledgerRef: V2_GIT_LEDGER_REF,
    restBaseUrl: "https://api.github.test",
    bootstrapCapabilityInput: bootstrapInputForCapability(nextCapability),
    verifyWorkflowProvenance: nextVerifier,
  });
  const epoch = await restricted.bootstrapCapability();
  const current = await epoch.ledger.load();
  assert.notEqual(
    current.capability.capability_input_digest,
    originalLoad.capability.capability_input_digest,
  );
  assert.ok(current.commit_count > originalLoad.commit_count);
  await assert.rejects(
    original.load(),
    (error) => error.code === "capability-attestation-required",
  );
});

test("capability attestation exact-binds its canary authority and race",
  async (t) => {
    const resealObservedCapability = (payload) => {
      const observation = payload.write_observation_receipt;
      delete observation.receipt_digest;
      observation.receipt_digest = digestCanonical(
        "codex-review-gate-v2-git-ledger-write-observation",
        observation,
      );
      const capability = payload.capability_receipt;
      capability.permissions.observation_receipt_digest =
        observation.receipt_digest;
      delete capability.receipt_digest;
      capability.receipt_digest = digestCanonical(
        "codex-review-gate-v2-git-ledger-capability",
        capability,
      );
      payload.capability_input_digest =
        fixtureCapabilityBindingDigest(capability);
      payload.capability_stable_digest =
        digestV2GitLedgerStableCapabilityAuthorization(capability);
    };

    const rejectsResealedTransition = async (mutatePayload) => {
      const fixture = githubGitFixture();
      const ledger = makeLedger(fixture);
      await ledger.bootstrapCapability();
      let loadCapability = null;
      fixture.rewriteTipEnvelope((envelope) => {
        loadCapability = mutatePayload(envelope.payload) ?? null;
        envelope.payload_digest = digestV2GitLedgerPayload(envelope.payload);
        resealEnvelope(envelope);
      });
      const before = {
        writes: fixture.writeCalls,
        patches: fixture.calls.filter((call) =>
          call.method === "PATCH").length,
        ref: fixture.refTarget,
      };
      await assert.rejects(
        loadCapability === null
          ? ledger.load()
          : makeLedger(
            fixture,
            loadCapability,
            provenanceVerifier({ jtiPrefix: "capability-pivot-load-jti" }),
          ).load(),
        (error) => error.code === "capability-transition-mismatch",
      );
      assert.deepEqual({
        writes: fixture.writeCalls,
        patches: fixture.calls.filter((call) =>
          call.method === "PATCH").length,
        ref: fixture.refTarget,
      }, before);
    };

    await t.test("stable capability authority", async () => {
      await rejectsResealedTransition((payload) => {
        const capability = payload.capability_receipt;
        capability.workflow_provenance_policy
          .subject_policy_receipt_digest = rawDigest(
            "resealed-capability-policy-pivot",
          );
        delete capability.receipt_digest;
        capability.receipt_digest = digestCanonical(
          "codex-review-gate-v2-git-ledger-capability",
          capability,
        );
        payload.capability_input_digest =
          fixtureCapabilityBindingDigest(capability);
        payload.capability_stable_digest =
          digestV2GitLedgerStableCapabilityAuthorization(capability);
        return structuredClone(capability);
      });
    });

    await t.test("inner and outer race evidence", async () => {
      await rejectsResealedTransition((payload) => {
        payload.contenders[0].raw_body_sha256 = rawDigest(
          "resealed-outer-race-drift",
        );
      });
    });

    await t.test("final ref observation boundary", async () => {
      await rejectsResealedTransition((payload) => {
        const finalRef = payload.race_final_ref_reread;
        const driftedTime = new Date(
          Date.parse(finalRef.server_time) + 60_000,
        ).toISOString();
        finalRef.server_time = driftedTime;
        payload.write_observation_receipt
          .race_final_ref_reread.server_time = driftedTime;
        resealObservedCapability(payload);
      });
    });

    await t.test("contender observations precede the final ref", async () => {
      await rejectsResealedTransition((payload) => {
        const finalTime = Date.parse(
          payload.race_final_ref_reread.server_time,
        );
        const contender = payload.contenders[0];
        const observationContender = payload.write_observation_receipt
          .race_results.find((item) => item.label === contender.label);
        const driftedTime = new Date(finalTime + 60_000).toISOString();
        contender.server_time = driftedTime;
        observationContender.server_time = driftedTime;
        resealObservedCapability(payload);
      });
    });
  });

test("historical control comment creation is intent-null then exact response-bound", async () => {
  const fixture = githubGitFixture();
  const context = await createProtectedInitialRunnerContext(fixture);
  const { ledger, lease, evaluatedScopeReceipt } = context;
  const source = await ledger.load();
  const intent = createV2GitLedgerRecord({
    record_type: "effect-intent",
    predecessor_commit_sha: lease.acquire_commit_sha,
    pull_request: PR,
    head_ref_oid: HEAD,
    base_ref_oid: BASE,
    potential_merge_commit_oid: POTENTIAL,
    kind: "control-comment-create",
    effect_id: "control-comment-1",
    idempotency_key: "control-comment-create-1",
    server_observed_at: source.post_ref.server_time,
    payload: effectPayload({
      phase: "intent",
      kind: "control-comment-create",
      predecessor: lease.acquire_commit_sha,
      action: {
        method: "POST",
        body_digest: `sha256:${"4".repeat(64)}`,
        pre_comment_inventory_digest: `sha256:${"5".repeat(64)}`,
      },
    }),
    control_comment_binding: null,
    lease: leaseBinding(lease),
  });
  await materializeHistoricalProductionRecord({
    ledger,
    loaded: source,
    record: intent,
    evaluatedScopeReceipt,
  });
  const intentCommitSha = fixture.refTarget;
  const afterIntent = await ledger.load();
  const responseRecord = createV2GitLedgerRecord({
    record_type: "effect-response",
    predecessor_commit_sha: intentCommitSha,
    pull_request: PR,
    head_ref_oid: HEAD,
    base_ref_oid: BASE,
    potential_merge_commit_oid: POTENTIAL,
    kind: "control-comment-create",
    effect_id: "control-comment-1",
    idempotency_key: "control-comment-create-1",
    server_observed_at: afterIntent.post_ref.server_time,
    payload: effectPayload({
      phase: "response",
      kind: "control-comment-create",
      predecessor: intentCommitSha,
      intentCommitSha,
      action: {
        method: "POST",
        body_digest: `sha256:${"4".repeat(64)}`,
        pre_comment_inventory_digest: `sha256:${"5".repeat(64)}`,
      },
      receipt: {
        http_status: 201,
        server_time: TIME,
        raw_body_sha256: `sha256:${"6".repeat(64)}`,
        comment: CONTROL,
        actor: ACTOR,
        app: APP,
        pre_comment_inventory_digest: `sha256:${"5".repeat(64)}`,
        post_comment_inventory_digest: `sha256:${"7".repeat(64)}`,
      },
    }),
    control_comment_binding: CONTROL,
    lease: leaseBinding(lease),
  });
  await materializeHistoricalProductionRecord({
    ledger,
    loaded: afterIntent,
    record: responseRecord,
    evaluatedScopeReceipt,
  });
  const loaded = await ledger.load();
  assert.deepEqual(loaded.control_comment_binding, CONTROL);
  assert.deepEqual(loaded.projection.control_comment_binding, CONTROL);

  const {
    schema: _schema,
    schema_version: _version,
    payload_digest: _payloadDigest,
    record_digest: _recordDigest,
    ...intentPlan
  } = intent;
  const createAgain = createV2GitLedgerRecord({
    ...structuredClone(intentPlan),
    predecessor_commit_sha: loaded.tip_commit_sha,
    effect_id: "control-comment-2",
    idempotency_key: "control-comment-create-2",
    payload: {
      ...structuredClone(intentPlan.payload),
      predecessor_commit_sha: loaded.tip_commit_sha,
    },
    server_observed_at: loaded.post_ref.server_time,
  });
  const before = fixture.snapshotHistoricalState();
  await assert.rejects(
    materializeHistoricalProductionRecord({
      ledger,
      loaded,
      record: createAgain,
      evaluatedScopeReceipt,
    }),
    (error) => error.code === "control-comment-binding",
  );
  assert.equal(fixture.refTarget, before.ref_target);
  assert.deepEqual(new Set(fixture.blobs.keys()), before.blob_keys);
  assert.deepEqual(new Set(fixture.trees.keys()), before.tree_keys);
  assert.deepEqual(new Set(fixture.commits.keys()), before.commit_keys);
});

test("GitHub ref Date, not caller clock, controls high-level lease time", async () => {
  const fixture = githubGitFixture();
  const ledger = makeLedger(fixture);
  await ledger.bootstrapCapability();
  const initial = await ledger.load();
  const lease = await ledger.acquireLease(leaseInput(initial.tip_commit_sha));
  assert.equal(Date.parse(lease.acquired_at) > Date.parse(TIME), true);
  assert.equal(
    Date.parse(lease.expires_at) - Date.parse(lease.acquired_at),
    600_000,
  );
  const loaded = await ledger.load();
  const actual = loaded.records.find((entry) =>
    entry.commit_sha === lease.acquire_commit_sha);
  assert.equal(Date.parse(actual.envelope.server_observed_at) > Date.parse(TIME), true);

  const futureFixture = githubGitFixture();
  const futureLedger = makeLedger(futureFixture);
  await futureLedger.bootstrapCapability();
  const futureLoaded = await futureLedger.load();
  const future = {
    ...leaseInput(futureLoaded.tip_commit_sha),
    observed_at: "2099-01-01T00:00:00.000Z",
  };
  const writes = futureFixture.writeCalls;
  await assert.rejects(
    futureLedger.acquireLease(future),
    (error) => error.code === "caller-clock-ahead",
  );
  assert.equal(futureFixture.writeCalls, writes);
});

test("final ref Date fence rejects a lease too short for the bounded append", async () => {
  const fixture = githubGitFixture();
  const context = await createProtectedInitialRunnerContext(fixture);
  const plan = schedulerObservationRecord({
    predecessor: context.lease.acquire_commit_sha,
    lease: context.lease,
    priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
    initialAuthority: context.initialAuthority,
  }).payload.action;
  fixture.advanceServerTime(780);
  const writes = fixture.writeCalls;
  await assert.rejects(
    context.ledger.appendInitialSchedulerObservation({
      initial_runner_state_authority: context.initialAuthority,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: plan.status_plan,
    }),
    (error) => error.code === "lease-write-window",
  );
  assert.equal(fixture.writeCalls, writes,
    "lease safety-window rejection must precede Git object writes");
});

test("two-pass load rejects a ref that moves during chain traversal", async () => {
  const fixture = githubGitFixture();
  const ledger = makeLedger(fixture);
  await ledger.bootstrapCapability();
  fixture.moveRefToParentOnNextCommitRead();
  await assert.rejects(
    ledger.load(),
    (error) => error.code === "unstable-ref",
  );
});

test("workflow provenance failures reject before Git object writes", async (t) => {
  await t.test("missing verifier", () => {
    const fixture = githubGitFixture();
    const input = factoryInput(fixture, capabilityReceipt());
    delete input.verifyWorkflowProvenance;
    assert.throws(
      () => createV2GitHubGitLedger(input),
      /requires verifyWorkflowProvenance/u,
    );
    assert.equal(fixture.writeCalls, 0);
  });

  for (const [name, mutate, pattern] of [
    ["missing required claim", (receipt) => {
      delete receipt.claims.job_workflow_sha;
    }, /closed required set/u],
    ["expired token", (receipt) => {
      receipt.claims.exp = 1;
    }, /server-time window/u],
    ["wrong pull request SHA", (receipt) => {
      receipt.claims.sha = HEAD;
    }, /trigger identity|exact effect scope/u],
  ]) {
    await t.test(name, async () => {
      const fixture = githubGitFixture();
      const baseVerifier = provenanceVerifier();
      let verifierOrdinal = 0;
      let failureEnabled = false;
      const failing = makeLedger(fixture, capabilityReceipt(), async (request) => {
        if (request.mode === "reverify-stored" || !failureEnabled) {
          return baseVerifier(request);
        }
        verifierOrdinal += 1;
        const result = structuredClone(await baseVerifier(request));
        const receipt = result.receipt;
        delete receipt.receipt_digest;
        receipt.claims.jti = `negative-${name}-${verifierOrdinal}`;
        mutate(receipt);
        receipt.receipt_digest = digestCanonical(
          "codex-review-gate-v2-git-ledger-provenance",
          receipt,
        );
        return result;
      });
      await failing.bootstrapCapability();
      const discovery = await acquireDefaultDiscovery(failing);
      const writes = fixture.writeCalls;
      failureEnabled = true;
      await assert.rejects(
        releaseFixtureDiscoveryLease(failing, discovery),
        pattern,
      );
      assert.equal(fixture.writeCalls, writes);
    });
  }
});

test("workflow provenance verifier has an abortable hard deadline", async () => {
  const fixture = githubGitFixture();
  const setup = makeLedger(fixture);
  await setup.bootstrapCapability();
  let observedSignal = null;
  const stalled = createV2GitHubGitLedger({
    ...factoryInput(fixture, capabilityReceipt()),
    provenanceTimeoutMs: 5,
    verifyWorkflowProvenance: async (_request, { signal, deadline_ms }) => {
      assert.equal(deadline_ms, 5);
      observedSignal = signal;
      return new Promise((resolve, reject) => {
        void resolve;
        signal.addEventListener("abort", () => reject(signal.reason), {
          once: true,
        });
      });
    },
  });
  const writes = fixture.writeCalls;
  await assert.rejects(
    stalled.load(),
    (error) => error.code === "provenance-timeout",
  );
  assert.equal(observedSignal?.aborted, true);
  assert.equal(fixture.writeCalls, writes);
});

test("workflow provenance verifier rejects a synchronous post-read overrun",
  async () => {
    const fixture = githubGitFixture();
    const setup = makeLedger(fixture);
    await setup.bootstrapCapability();
    const baseVerifier = provenanceVerifier();
    const overrun = createV2GitHubGitLedger({
      ...factoryInput(fixture, capabilityReceipt()),
      provenanceTimeoutMs: 5,
      verifyWorkflowProvenance: async (...args) => {
        const result = await baseVerifier(...args);
        const deadline = performance.now() + 20;
        while (performance.now() < deadline) {
          // Exercise the synchronous post-verifier deadline fence.
        }
        return result;
      },
    });
    const writes = fixture.writeCalls;
    await assert.rejects(
      overrun.load(),
      (error) => error.code === "provenance-timeout",
    );
    assert.equal(fixture.writeCalls, writes);
  });

test("replayed OIDC jti and open effect payloads fail closed", async (t) => {
  await t.test("jti replay", async () => {
    const fixture = githubGitFixture();
    const repeated = provenanceVerifier({ fixedJti: "replayed-oidc-jti" });
    const ledger = makeLedger(fixture, capabilityReceipt(), repeated);
    await assert.rejects(
      ledger.bootstrapCapability(),
      (error) => error.code === "duplicate-provenance-identity",
    );
    assert.equal(fixture.tipRecordType(), "genesis");
  });

  await t.test("unknown payload key", async () => {
    const predecessor = "1".repeat(40);
    const payload = effectPayload({
      phase: "intent",
      kind: "review-request",
      predecessor,
      generation: automaticGeneration(1),
      action: {
        method: "POST",
        request_body_sha256: `sha256:${"1".repeat(64)}`,
      },
    });
    payload.action.untrusted = true;
    assert.throws(
      () => createV2GitLedgerRecord({
        record_type: "effect-intent",
        predecessor_commit_sha: predecessor,
        pull_request: PR,
        head_ref_oid: HEAD,
        base_ref_oid: BASE,
        potential_merge_commit_oid: POTENTIAL,
        kind: "review-request",
        effect_id: "open-payload",
        idempotency_key: "open-payload-key",
        server_observed_at: TIME,
        payload,
        control_comment_binding: null,
        lease: {
          lease_id: "lease-open",
          owner: OWNER,
          acquire_commit_sha: "2".repeat(40),
          expires_at: "2026-08-13T12:10:00.000Z",
        },
      }),
      /closed key set/u,
    );
  });
});

test("OIDC jti is optional and exact JWT bytes become the replay identity", async () => {
  const fixture = githubGitFixture();
  const ledger = makeLedger(
    fixture,
    capabilityReceipt(),
    provenanceVerifier({ omitJti: true }),
  );
  await ledger.bootstrapCapability();
  const loaded = await ledger.load();
  assert.equal(
    loaded.record_manifest.every((entry) =>
      entry.workflow_provenance_jti.startsWith("jwt:sha256:")),
    true,
  );
});

test("reachable JWTs are live-reverified but never returned or leaked by errors", async (t) => {
  const fixture = githubGitFixture();
  const ledger = makeLedger(fixture);
  await ledger.bootstrapCapability();
  const storedTokens = [...fixture.blobs.values()].map((blob) =>
    JSON.parse(blob.content).workflow_provenance_jwt);
  assert.ok(storedTokens.length >= 3);

  const baseVerifier = provenanceVerifier({ fixedJti: "live-load-jti" });
  const liveVerifier = async (request) => {
    const result = structuredClone(await baseVerifier(request));
    if (request.mode === "reverify-stored") {
      assert.match(request.stored_receipt.receipt_digest, /^sha256:/u);
      assert.equal(request.stored_receipt.token_sha256,
        rawDigest(request.compact_jwt));
      result.receipt.discovery.server_time =
        "2026-08-13T13:00:00.000Z";
      result.receipt.discovery.raw_body_sha256 = `sha256:${"8".repeat(64)}`;
      result.receipt.jwks.server_time = "2026-08-13T13:00:01.000Z";
      result.receipt.jwks.raw_body_sha256 = `sha256:${"9".repeat(64)}`;
      delete result.receipt.receipt_digest;
      result.receipt.receipt_digest = digestCanonical(
        "codex-review-gate-v2-git-ledger-provenance",
        result.receipt,
      );
    }
    return result;
  };
  const reverified = createV2GitHubGitLedger({
    ...factoryInput(fixture, capabilityReceipt()),
    verifyWorkflowProvenance: liveVerifier,
  });
  const loaded = await reverified.load();
  assert.equal(loaded.records[0].envelope.schema,
    V2_GIT_LEDGER_PUBLIC_ENVELOPE_SCHEMA);
  assert.equal(loaded.provenance_reverification.length, loaded.commit_count);
  assert.equal(loaded.provenance_reverification[0].discovery.server_time,
    "2026-08-13T13:00:00.000Z");
  const serialized = JSON.stringify(loaded);
  assert.doesNotMatch(serialized, /workflow_provenance_jwt/u);
  for (const token of storedTokens) assert.equal(serialized.includes(token), false);

  await t.test("kid rotation is typed and token-redacted", async () => {
    const token = storedTokens[0];
    const failing = createV2GitHubGitLedger({
      ...factoryInput(fixture, capabilityReceipt()),
      verifyWorkflowProvenance: async (request) => {
        if (request.mode === "reverify-stored") {
          const error = new Error(request.compact_jwt);
          error.code = "oidc-kid-unavailable";
          throw error;
        }
        return baseVerifier(request);
      },
    });
    let caught;
    try {
      await failing.load();
    } catch (error) {
      caught = error;
    }
    assert.equal(caught.code, "oidc-kid-unavailable");
    assert.equal(caught.message.includes(token), false);
    assert.equal(caught.cause, undefined);
    assert.equal(JSON.stringify(caught).includes(token), false);
  });
});

test("jti-less OIDC replay and operation audience tampering fail before writes", async (t) => {
  await t.test("replayed exact JWT digest", async () => {
    const fixture = githubGitFixture();
    const baseVerifier = provenanceVerifier({ omitJti: true });
    let repeated = null;
    const repeatedToken = async (request) => {
      const result = structuredClone(await baseVerifier(request));
      repeated ??= request.mode === "mint-and-verify"
        ? result.compact_jwt
        : request.compact_jwt;
      if (request.mode === "mint-and-verify") {
        result.compact_jwt = repeated;
      }
      result.receipt.token_sha256 = rawDigest(repeated);
      delete result.receipt.receipt_digest;
      result.receipt.receipt_digest = digestCanonical(
        "codex-review-gate-v2-git-ledger-provenance",
        result.receipt,
      );
      return result;
    };
    const ledger = makeLedger(fixture, capabilityReceipt(), repeatedToken);
    await assert.rejects(
      ledger.bootstrapCapability(),
      (error) => error.code === "duplicate-provenance-identity",
    );
    assert.equal(fixture.tipRecordType(), "genesis");
  });

  await t.test("effect-bound audience", async () => {
    const fixture = githubGitFixture();
    const baseVerifier = provenanceVerifier();
    const badAudience = async (request) => {
      const result = structuredClone(await baseVerifier(request));
      const receipt = result.receipt;
      receipt.audience = `${V2_GIT_LEDGER_OIDC_AUDIENCE}:wrong`;
      receipt.claims.aud = receipt.audience;
      delete receipt.receipt_digest;
      receipt.receipt_digest = digestCanonical(
        "codex-review-gate-v2-git-ledger-provenance",
        receipt,
      );
      return result;
    };
    const restricted = createV2GitHubGitLedgerBootstrap({
      fetch: fixture.fetch,
      token: SYNTHETIC_BEARER,
      repository: REPOSITORY,
      ledgerRef: V2_GIT_LEDGER_REF,
      restBaseUrl: "https://api.github.test",
      bootstrapCapabilityInput: bootstrapInput(),
      verifyWorkflowProvenance: badAudience,
    });
    await assert.rejects(
      restricted.bootstrapCapability(),
      /exact operation and source/u,
    );
    assert.equal(fixture.writeCalls, 0);
  });
});

test("HTTP transport caps declared, streamed, aggregate, and stalled bodies", async (t) => {
  await t.test("declared response bytes", async () => {
    const ledger = createV2GitHubGitLedger({
      ...factoryInput({
        fetch: async () => new Response("{}", {
          status: 200,
          headers: {
            Date: new Date(TIME).toUTCString(),
            "Content-Length": "3",
          },
        }),
      }, capabilityReceipt()),
      httpLimits: { ...V2_GIT_LEDGER_HTTP_LIMITS, response_bytes: 2 },
    });
    await assert.rejects(
      ledger.load(),
      (error) => error.code === "http-response-cap",
    );
  });

  await t.test("streamed response bytes", async () => {
    const ledger = createV2GitHubGitLedger({
      ...factoryInput({
        fetch: async () => new Response("{}", {
          status: 200,
          headers: { Date: new Date(TIME).toUTCString() },
        }),
      }, capabilityReceipt()),
      httpLimits: { ...V2_GIT_LEDGER_HTTP_LIMITS, response_bytes: 1 },
    });
    await assert.rejects(
      ledger.load(),
      (error) => error.code === "http-response-cap",
    );
  });

  await t.test("aggregate response bytes", async () => {
    const ledger = createV2GitHubGitLedger({
      ...factoryInput({
        fetch: async () => new Response("{}", {
          status: 200,
          headers: { Date: new Date(TIME).toUTCString() },
        }),
      }, capabilityReceipt()),
      httpLimits: { ...V2_GIT_LEDGER_HTTP_LIMITS, total_response_bytes: 1 },
    });
    await assert.rejects(
      ledger.load(),
      (error) => error.code === "http-total-byte-cap",
    );
  });

  await t.test("request count", async () => {
    let calls = 0;
    const ledger = createV2GitHubGitLedger({
      ...factoryInput({
        fetch: async () => {
          calls += 1;
          return new Response(JSON.stringify({
            ref: V2_GIT_LEDGER_REF,
            node_id: "REF_ledger",
            object: { type: "commit", sha: "1".repeat(40) },
          }), {
            status: 200,
            headers: { Date: new Date(TIME).toUTCString() },
          });
        },
      }, capabilityReceipt()),
      httpLimits: { ...V2_GIT_LEDGER_HTTP_LIMITS, request_count: 1 },
    });
    await assert.rejects(
      ledger.load(),
      (error) => error.code === "http-request-cap",
    );
    assert.equal(calls, 1);
  });

  await t.test("stalled body timeout", async () => {
    const body = new ReadableStream({
      start() {},
    });
    const ledger = createV2GitHubGitLedger({
      ...factoryInput({
        fetch: async () => new Response(body, {
          status: 200,
          headers: { Date: new Date(TIME).toUTCString() },
        }),
      }, capabilityReceipt()),
      httpLimits: { ...V2_GIT_LEDGER_HTTP_LIMITS, timeout_ms: 5 },
    });
    await assert.rejects(
      ledger.load(),
      (error) => error.code === "http-timeout",
    );
  });

  await t.test("zero-length response chunks", async () => {
    const body = new ReadableStream({
      start(controller) {
        controller.enqueue(new Uint8Array(0));
        controller.close();
      },
    });
    const ledger = createV2GitHubGitLedger({
      ...factoryInput({
        fetch: async () => new Response(body, {
          status: 200,
          headers: { Date: new Date(TIME).toUTCString() },
        }),
      }, capabilityReceipt()),
    });
    await assert.rejects(
      ledger.load(),
      (error) => error.code === "http-response-chunk-cap",
    );
  });

  await t.test("response status is snapshotted once", async () => {
    let statusReads = 0;
    const bodyResponse = new Response("{}", {
      status: 200,
      headers: { Date: new Date(TIME).toUTCString() },
    });
    const ledger = createV2GitHubGitLedger({
      ...factoryInput({
        fetch: async () => ({
          get status() {
            statusReads += 1;
            return statusReads === 1 ? 500 : 200;
          },
          headers: bodyResponse.headers,
          body: bodyResponse.body,
        }),
      }, capabilityReceipt()),
    });
    await assert.rejects(
      ledger.load(),
      (error) => error.code === "unexpected-http-status",
    );
    assert.equal(statusReads, 1);
  });
});

test("status-write binds its exact target role and accepts clean success", () => {
  const predecessor = "1".repeat(40);
  const primaryIntent = createV2GitLedgerRecord({
    record_type: "effect-intent",
    predecessor_commit_sha: predecessor,
    pull_request: PR,
    head_ref_oid: HEAD,
    base_ref_oid: BASE,
    potential_merge_commit_oid: POTENTIAL,
    kind: "status-write",
    effect_id: "status-primary-success",
    idempotency_key: "status-primary-success-key",
    server_observed_at: TIME,
    payload: effectPayload({
      phase: "intent",
      kind: "status-write",
      predecessor,
      action: {
        mode: "test-merge-with-head-sentinel",
        target_sha: POTENTIAL,
        role: "primary-terminal",
        context: "codex/github-review-gate",
        state: "success",
        description_digest: `sha256:${"1".repeat(64)}`,
        scheduler_observation_record_oid: "3".repeat(40),
        scheduler_action_key: "publish-status:test",
        scheduler_plan_digest: `sha256:${"3".repeat(64)}`,
        status_plan_digest: `sha256:${"4".repeat(64)}`,
        status_write_index: 0,
        status_write_count: 1,
      },
    }),
    control_comment_binding: null,
    lease: {
      lease_id: "lease-status",
      owner: OWNER,
      acquire_commit_sha: "2".repeat(40),
      expires_at: "2026-08-13T12:10:00.000Z",
    },
  });
  assert.equal(primaryIntent.payload.action.state, "success");
  assert.equal(primaryIntent.payload.action.target_sha, POTENTIAL);

  assert.throws(
    () => createV2GitLedgerRecord({
      ...recordPlan(primaryIntent),
      effect_id: "status-wrong-target",
      idempotency_key: "status-wrong-target-key",
      payload: effectPayload({
        phase: "intent",
        kind: "status-write",
        predecessor,
        action: {
          ...primaryIntent.payload.action,
          target_sha: HEAD,
        },
      }),
    }),
    /target does not match/u,
  );
  for (const action of [
    {
      ...primaryIntent.payload.action,
      mode: "head",
      role: "head-sentinel",
      target_sha: HEAD,
    },
    {
      ...primaryIntent.payload.action,
      mode: "head",
      role: "primary-terminal",
      target_sha: HEAD,
      state: "failure",
    },
  ]) {
    assert.throws(
      () => createV2GitLedgerRecord({
        ...recordPlan(primaryIntent),
        effect_id: `status-forbidden-head-${action.role}`,
        idempotency_key: `status-forbidden-head-key-${action.role}`,
        payload: effectPayload({
          phase: "intent",
          kind: "status-write",
          predecessor,
          action,
        }),
      }),
      /target does not match/u,
    );
  }
});

test("status-only builder binds one protected scheduler write through exact refetch", async () => {
  const fixture = githubGitFixture();
  const context = await createProtectedInitialRunnerContext(fixture);
  const plan = schedulerObservationRecord({
    predecessor: context.lease.acquire_commit_sha,
    lease: context.lease,
    priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
    initialAuthority: context.initialAuthority,
  }).payload.action;
  const schedulerAppend = await context.ledger
    .appendInitialSchedulerObservation({
      initial_runner_state_authority: context.initialAuthority,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: plan.status_plan,
    });
  const writesBeforeForgery = fixture.writeCalls;
  await assert.rejects(
    context.ledger.appendStatusWriteIntent({
      scheduler_append: structuredClone(schedulerAppend),
      status_write_index: 0,
    }),
    (error) => error.code === "UNTRUSTED_SCHEDULER_APPEND_HANDLE",
  );
  await assert.rejects(
    context.ledger.appendStatusWriteIntent({
      scheduler_append: schedulerAppend,
      status_write_index: 1,
    }),
    (error) => error.code === "status-write-index-unsupported",
  );
  assert.equal(fixture.writeCalls, writesBeforeForgery);

  const intent = await context.ledger.appendStatusWriteIntent({
    scheduler_append: schedulerAppend,
    status_write_index: 0,
  });
  assert.equal(
    assertV2GitLedgerStatusWriteIntentHandle(intent.status_intent_handle),
    intent.status_intent_handle,
  );
  assert.deepEqual(
    projectV2GitLedgerStatusWriteTransport(intent.status_intent_handle),
    intent.transport,
  );
  assert.deepEqual(intent.transport, {
    method: "POST",
    target_sha: HEAD,
    role: "head-sentinel",
    context: "codex/github-review-gate",
    state: "pending",
    description: "awaiting-terminal-test-merge-decision",
    description_digest: digestCanonical(
      "codex-review-gate-v2-status-description",
      { description: "awaiting-terminal-test-merge-decision" },
    ),
  });
  const loadedIntent = await context.ledger.load();
  assert.equal(loadedIntent.tip_commit_sha,
    intent.intent_append_receipt.commit_sha);
  const intentControl = await context.ledger.loadControlPlaneAuthority(
    context.initialAuthority.scope,
  );
  assert.equal(intentControl.scoped_authority.runner_state
    .status_inventory[0].response_record_oid, null);

  const responseReceipt = statusWriteResponseReceipt(
    intent.transport,
    intent.intent_append_receipt.ref_reread.server_time,
  );
  const beforeResponseForgery = fixture.writeCalls;
  await assert.rejects(
    context.ledger.appendStatusWriteResponse({
      status_intent_handle: structuredClone(intent.status_intent_handle),
      intent_append_receipt: intent.intent_append_receipt,
      receipt: responseReceipt,
    }),
    (error) => error.code === "UNTRUSTED_STATUS_WRITE_INTENT_HANDLE",
  );
  await assert.rejects(
    context.ledger.appendStatusWriteResponse({
      status_intent_handle: intent.status_intent_handle,
      intent_append_receipt: structuredClone(intent.intent_append_receipt),
      receipt: responseReceipt,
    }),
    (error) => error.code === "STATUS_WRITE_INTENT_RECEIPT_MISMATCH",
  );
  await assert.rejects(
    context.ledger.appendStatusWriteResponse({
      status_intent_handle: intent.status_intent_handle,
      intent_append_receipt: intent.intent_append_receipt,
      receipt: { ...responseReceipt, refetch_match_count: 2 },
    }),
    /unique status/u,
  );
  assert.equal(fixture.writeCalls, beforeResponseForgery);

  const response = await context.ledger.appendStatusWriteResponse({
    status_intent_handle: intent.status_intent_handle,
    intent_append_receipt: intent.intent_append_receipt,
    receipt: responseReceipt,
  });
  assert.equal(response.authoritative_status.status_id, "801");
  assert.equal(
    response.authoritative_status.response_record_oid,
    response.response_append_receipt.commit_sha,
  );
  const after = await context.ledger.load();
  assert.equal(after.tip_commit_sha, response.response_append_receipt.commit_sha);
  const responseControl = await context.ledger.loadControlPlaneAuthority(
    context.initialAuthority.scope,
  );
  assert.equal(
    responseControl.scoped_authority.runner_state.status_inventory[0]
      .response_record_oid,
    response.response_append_receipt.commit_sha,
  );
  const writesAfterResponse = fixture.writeCalls;
  await assert.rejects(
    context.ledger.appendStatusWriteResponse({
      status_intent_handle: intent.status_intent_handle,
      intent_append_receipt: intent.intent_append_receipt,
      receipt: responseReceipt,
    }),
    (error) => error.code === "status-write-response-replayed",
  );
  assert.equal(fixture.writeCalls, writesAfterResponse);
});

test("status-only builder rejects multi-write and stale scheduler authorities", async () => {
  const fixture = githubGitFixture();
  const context = await createProtectedInitialRunnerContext(fixture);
  const plan = schedulerObservationRecord({
    predecessor: context.lease.acquire_commit_sha,
    lease: context.lease,
    priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
    initialAuthority: context.initialAuthority,
  }).payload.action;
  const schedulerAppend = await context.ledger
    .appendInitialSchedulerObservation({
      initial_runner_state_authority: context.initialAuthority,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: plan.status_plan,
    });
  const tampered = structuredClone(schedulerAppend);
  tampered.record.payload.action.status_plan.writes.push({
    ...structuredClone(tampered.record.payload.action.status_plan.writes[0]),
    idempotency_key: "status:forged-second-write",
  });
  await assert.rejects(
    context.ledger.appendStatusWriteIntent({
      scheduler_append: tampered,
      status_write_index: 0,
    }),
    (error) => error.code === "UNTRUSTED_SCHEDULER_APPEND_HANDLE",
  );
  await releaseFixtureDiscoveryLease(context.ledger, context.discovery);
  const writesBefore = fixture.writeCalls;
  await assert.rejects(
    context.ledger.appendStatusWriteIntent({
      scheduler_append: schedulerAppend,
      status_write_index: 0,
    }),
    (error) => error.code === "stale-scheduler-status-authority",
  );
  assert.equal(fixture.writeCalls, writesBefore);
});

test("status transaction appends two writes in canonical order", async () => {
  const fixture = githubGitFixture();
  const context = await createProtectedInitialRunnerContext(fixture);
  const plan = schedulerObservationRecord({
    predecessor: context.lease.acquire_commit_sha,
    lease: context.lease,
    priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
    initialAuthority: context.initialAuthority,
    decision: "findings",
  }).payload.action;
  const statusPlan = buildV2StatusPlan({
    decision: "findings",
    status_target_mode: "test-merge-with-head-sentinel",
    status_context: "codex/github-review-gate",
    target: {
      mode: "test-merge-with-head-sentinel",
      validated: true,
      terminal_sha: POTENTIAL,
      head_sentinel_sha: HEAD,
      head_sentinel_receipt: null,
    },
    snapshot_fingerprint: plan.scheduler_evaluation.snapshot_fingerprint,
  });
  const schedulerAppend = await context.ledger
    .appendInitialSchedulerObservation({
      initial_runner_state_authority: context.initialAuthority,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: statusPlan,
    });

  const first = await context.ledger.loadNextStatusWriteIntent({
    scheduler_append: schedulerAppend,
  });
  assert.equal(first.status_write_index, 0);
  assert.equal(first.status_write_count, 2);
  assert.equal(first.transport.role, "primary-terminal");
  await context.ledger.appendStatusWriteResponse({
    status_intent_handle: first.status_intent_handle,
    intent_append_receipt: first.intent_append_receipt,
    receipt: statusWriteResponseReceipt(
      first.transport,
      first.intent_append_receipt.ref_reread.server_time,
    ),
  });

  const second = await context.ledger.loadNextStatusWriteIntent({
    scheduler_append: schedulerAppend,
  });
  assert.equal(second.status_write_index, 1);
  assert.equal(second.status_write_count, 2);
  assert.equal(second.transport.role, "head-sentinel");
  await context.ledger.appendStatusWriteResponse({
    status_intent_handle: second.status_intent_handle,
    intent_append_receipt: second.intent_append_receipt,
    receipt: statusWriteResponseReceipt(
      second.transport,
      second.intent_append_receipt.ref_reread.server_time,
    ),
  });
  assert.equal(await context.ledger.loadNextStatusWriteIntent({
    scheduler_append: schedulerAppend,
  }), null);
});

test("status transaction rehydrates its bound prefix across scheduler observations",
  async (t) => {
    const prepare = async ({ boundCount, statusContext = "codex/github-review-gate" }) => {
      const fixture = githubGitFixture();
      const runIdentity = { ...OWNER };
      const context = await createProtectedInitialRunnerContext(fixture, {
        runIdentity,
      });
      const plan = schedulerObservationRecord({
        predecessor: context.lease.acquire_commit_sha,
        lease: context.lease,
        priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
        initialAuthority: context.initialAuthority,
        decision: "findings",
      }).payload.action;
      const statusPlan = buildV2StatusPlan({
        decision: "findings",
        status_target_mode: "test-merge-with-head-sentinel",
        status_context: "codex/github-review-gate",
        target: {
          mode: "test-merge-with-head-sentinel",
          validated: true,
          terminal_sha: POTENTIAL,
          head_sentinel_sha: HEAD,
          head_sentinel_receipt: null,
        },
        snapshot_fingerprint: plan.scheduler_evaluation.snapshot_fingerprint,
      });
      const initialAppend = await context.ledger
        .appendInitialSchedulerObservation({
          initial_runner_state_authority: context.initialAuthority,
          scheduler_evaluation: plan.scheduler_evaluation,
          status_plan: statusPlan,
        });
      const first = await context.ledger.loadNextStatusWriteIntent({
        scheduler_append: initialAppend,
      });
      if (boundCount >= 1) {
        await context.ledger.appendStatusWriteResponse({
          status_intent_handle: first.status_intent_handle,
          intent_append_receipt: first.intent_append_receipt,
          receipt: statusWriteResponseReceipt(
            first.transport,
            first.intent_append_receipt.ref_reread.server_time,
          ),
        });
      }
      if (boundCount >= 2) {
        const second = await context.ledger.loadNextStatusWriteIntent({
          scheduler_append: initialAppend,
        });
        await context.ledger.appendStatusWriteResponse({
          status_intent_handle: second.status_intent_handle,
          intent_append_receipt: second.intent_append_receipt,
          receipt: statusWriteResponseReceipt(
            second.transport,
            second.intent_append_receipt.ref_reread.server_time,
          ),
        });
      }
      await releaseFixtureDiscoveryLease(context.ledger, context.discovery);
      runIdentity.run_attempt = 2;
      const resumed = await acquireEstablishedRunnerContext({
        ledger: context.ledger,
        runIdentity,
        observationBoundary: "public-initial-wait-complete",
        transportStartSecond: 3600,
        minimalServerTime: "2026-08-13T14:00:00.000Z",
      });
      const established = await context.ledger
        .loadEstablishedRunnerStateAuthority({
          control_plane_authority: resumed.controlPlaneAuthority,
          evaluated_scope_receipt: resumed.evaluatedScopeReceipt,
          workflow_command_handle: resumed.workflowCommandHandle,
        });
      const inputs = schedulerInputsForEstablishedAuthority(established, {
        snapshotId: boundCount > 0
          ? "snapshot-status-bound-prefix"
          : "snapshot-status-ambiguous-prefix",
        decision: "findings",
      });
      const establishedStatusPlan = buildV2StatusPlan({
        decision: "findings",
        status_target_mode: "test-merge-with-head-sentinel",
        status_context: statusContext,
        target: {
          mode: "test-merge-with-head-sentinel",
          validated: true,
          terminal_sha: POTENTIAL,
          head_sentinel_sha: HEAD,
          head_sentinel_receipt: null,
        },
        snapshot_fingerprint: inputs.scheduler_evaluation.snapshot_fingerprint,
      });
      const schedulerAppend = await context.ledger
        .appendEstablishedSchedulerObservation({
          established_runner_state_authority: established,
          scheduler_evaluation: inputs.scheduler_evaluation,
          status_plan: establishedStatusPlan,
        });
      return { context, fixture, schedulerAppend };
    };

    await t.test("a response-bound first slot advances to the second slot", async () => {
      const { context, schedulerAppend } = await prepare({ boundCount: 1 });
      const next = await context.ledger.loadNextStatusWriteIntent({
        scheduler_append: schedulerAppend,
      });
      assert.equal(next.status_write_index, 1);
      assert.equal(next.status_write_count, 2);
      assert.equal(next.transport.role, "head-sentinel");
    });

    await t.test("an unanswered first intent is consumed and never reminted", async () => {
      const { context, fixture, schedulerAppend } = await prepare({
        boundCount: 0,
      });
      const writesBefore = fixture.writeCalls;
      await assert.rejects(
        context.ledger.loadNextStatusWriteIntent({
          scheduler_append: schedulerAppend,
        }),
        (error) => error.code === "status-write-intent-ambiguous-consumed",
      );
      assert.equal(fixture.writeCalls, writesBefore);
    });

  });

test("automatic reservation and its nonrequired status bind one protected scheduler action", async () => {
  const fixture = githubGitFixture();
  const context = await createProtectedInitialRunnerContext(fixture);
  const plan = schedulerObservationRecord({
    predecessor: context.lease.acquire_commit_sha,
    lease: context.lease,
    priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
    initialAuthority: context.initialAuthority,
  }).payload.action;
  const schedulerAppend = await context.ledger
    .appendInitialSchedulerObservation({
      initial_runner_state_authority: context.initialAuthority,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: plan.status_plan,
    });
  const requiredIntent = await context.ledger.appendStatusWriteIntent({
    scheduler_append: schedulerAppend,
    status_write_index: 0,
  });
  const writesBeforeUnboundStatus = fixture.writeCalls;
  await assert.rejects(
    context.ledger.appendAutomaticRequestReservation({
      scheduler_append: schedulerAppend,
    }),
    (error) => error.code === "automatic-request-required-status-unbound",
  );
  assert.equal(fixture.writeCalls, writesBeforeUnboundStatus);
  await context.ledger.appendStatusWriteResponse({
    status_intent_handle: requiredIntent.status_intent_handle,
    intent_append_receipt: requiredIntent.intent_append_receipt,
    receipt: statusWriteResponseReceipt(
      requiredIntent.transport,
      requiredIntent.intent_append_receipt.ref_reread.server_time,
    ),
  });
  const writesBeforeForgery = fixture.writeCalls;
  await assert.rejects(
    context.ledger.appendAutomaticRequestReservation({
      scheduler_append: structuredClone(schedulerAppend),
    }),
    (error) => error.code === "UNTRUSTED_SCHEDULER_APPEND_HANDLE",
  );
  assert.equal(fixture.writeCalls, writesBeforeForgery);

  const automatic = await context.ledger.appendAutomaticRequestReservation({
    scheduler_append: schedulerAppend,
  });
  assert.equal(
    assertV2GitLedgerAutomaticReservationHandle(
      automatic.automatic_reservation_handle,
    ),
    automatic.automatic_reservation_handle,
  );
  assert.deepEqual(
    projectV2GitLedgerAutomaticReservation(
      automatic.automatic_reservation_handle,
    ),
    automatic.reservation,
  );
  assert.equal(automatic.reservation.ordinal, 1);
  assert.equal(automatic.reservation.body, "@codex review");

  const writesBeforeStatusForgery = fixture.writeCalls;
  await assert.rejects(
    context.ledger.appendReservationStatusWriteIntent({
      automatic_reservation_handle: structuredClone(
        automatic.automatic_reservation_handle,
      ),
      reservation_append_receipt: automatic.reservation_append_receipt,
    }),
    (error) => error.code === "UNTRUSTED_AUTOMATIC_RESERVATION_HANDLE",
  );
  await assert.rejects(
    context.ledger.appendReservationStatusWriteIntent({
      automatic_reservation_handle: automatic.automatic_reservation_handle,
      reservation_append_receipt: structuredClone(
        automatic.reservation_append_receipt,
      ),
    }),
    (error) => error.code === "AUTOMATIC_RESERVATION_RECEIPT_MISMATCH",
  );
  assert.equal(fixture.writeCalls, writesBeforeStatusForgery);

  const intent = await context.ledger.appendReservationStatusWriteIntent({
    automatic_reservation_handle: automatic.automatic_reservation_handle,
    reservation_append_receipt: automatic.reservation_append_receipt,
  });
  assert.equal(
    assertV2GitLedgerReservationStatusIntentHandle(
      intent.reservation_status_intent_handle,
    ),
    intent.reservation_status_intent_handle,
  );
  assert.deepEqual(
    projectV2GitLedgerReservationStatusTransport(
      intent.reservation_status_intent_handle,
    ),
    intent.transport,
  );
  assert.deepEqual(intent.transport, {
    method: "POST",
    target_sha: HEAD,
    context: "codex/github-review-gate-reservation/1",
    state: "pending",
    description: automatic.reservation.reservation_digest,
    description_digest: automatic.reservation.reservation_digest,
  });

  const responseReceipt = reservationStatusWriteResponseReceipt(
    intent.transport,
    intent.intent_append_receipt.ref_reread.server_time,
  );
  const writesBeforeResponseForgery = fixture.writeCalls;
  await assert.rejects(
    context.ledger.appendReservationStatusWriteResponse({
      reservation_status_intent_handle: structuredClone(
        intent.reservation_status_intent_handle,
      ),
      intent_append_receipt: intent.intent_append_receipt,
      receipt: responseReceipt,
    }),
    (error) => error.code === "UNTRUSTED_RESERVATION_STATUS_INTENT_HANDLE",
  );
  await assert.rejects(
    context.ledger.appendReservationStatusWriteResponse({
      reservation_status_intent_handle:
        intent.reservation_status_intent_handle,
      intent_append_receipt: structuredClone(intent.intent_append_receipt),
      receipt: responseReceipt,
    }),
    (error) => error.code === "RESERVATION_STATUS_INTENT_RECEIPT_MISMATCH",
  );
  await assert.rejects(
    context.ledger.appendReservationStatusWriteResponse({
      reservation_status_intent_handle:
        intent.reservation_status_intent_handle,
      intent_append_receipt: intent.intent_append_receipt,
      receipt: { ...responseReceipt, refetch_match_count: 2 },
    }),
    /unique status/u,
  );
  assert.equal(fixture.writeCalls, writesBeforeResponseForgery);

  const response = await context.ledger.appendReservationStatusWriteResponse({
    reservation_status_intent_handle: intent.reservation_status_intent_handle,
    intent_append_receipt: intent.intent_append_receipt,
    receipt: responseReceipt,
  });
  assert.equal(response.authoritative_reservation.reservation_status_bound, true);
  assert.equal(
    response.authoritative_reservation.reservation_status_response_record_oid,
    response.response_append_receipt.commit_sha,
  );
  const final = await context.ledger.loadControlPlaneAuthority(
    context.initialAuthority.scope,
  );
  assert.equal(final.scoped_authority.runner_state.reservations[0]
    .reservation_status_bound, true);
  const writesAfterResponse = fixture.writeCalls;
  await assert.rejects(
    context.ledger.appendReservationStatusWriteResponse({
      reservation_status_intent_handle:
        intent.reservation_status_intent_handle,
      intent_append_receipt: intent.intent_append_receipt,
      receipt: responseReceipt,
    }),
    (error) => error.code === "reservation-status-response-replayed",
  );
  assert.equal(fixture.writeCalls, writesAfterResponse);
});

test("automatic reservation remains consumed when its status response is ambiguous", async () => {
  const fixture = githubGitFixture();
  const context = await createProtectedInitialRunnerContext(fixture);
  const plan = schedulerObservationRecord({
    predecessor: context.lease.acquire_commit_sha,
    lease: context.lease,
    priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
    initialAuthority: context.initialAuthority,
  }).payload.action;
  const schedulerAppend = await context.ledger
    .appendInitialSchedulerObservation({
      initial_runner_state_authority: context.initialAuthority,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: plan.status_plan,
    });
  await appendRequiredStatusForScheduler(context.ledger, schedulerAppend);
  const automatic = await context.ledger.appendAutomaticRequestReservation({
    scheduler_append: schedulerAppend,
  });
  const intent = await context.ledger.appendReservationStatusWriteIntent({
    automatic_reservation_handle: automatic.automatic_reservation_handle,
    reservation_append_receipt: automatic.reservation_append_receipt,
  });
  const before = fixture.writeCalls;
  await assert.rejects(
    context.ledger.appendReservationStatusWriteResponse({
      reservation_status_intent_handle:
        intent.reservation_status_intent_handle,
      intent_append_receipt: intent.intent_append_receipt,
      receipt: {
        ...reservationStatusWriteResponseReceipt(
          intent.transport,
          intent.intent_append_receipt.ref_reread.server_time,
        ),
        refetch_match_count: 0,
      },
    }),
    /unique status/u,
  );
  assert.equal(fixture.writeCalls, before);
  const loaded = await context.ledger.loadControlPlaneAuthority(
    context.initialAuthority.scope,
  );
  assert.equal(loaded.scoped_authority.runner_state
    .head_ledger.automatic_request_count, 1);
  assert.equal(loaded.scoped_authority.runner_state.reservations[0]
    .reservation_status_bound, false);
  assert.equal(loaded.scoped_authority.runner_state.reservations[0]
    .reservation_status_intent_record_oid,
  intent.intent_append_receipt.commit_sha);
  const writesBeforeReplay = fixture.writeCalls;
  await assert.rejects(
    context.ledger.appendAutomaticRequestReservation({
      scheduler_append: schedulerAppend,
    }),
    (error) => error.code === "automatic-reservation-scheduler-replayed",
  );
  assert.equal(fixture.writeCalls, writesBeforeReplay);
});

test("automatic request intent durably records one retry-zero attempt before POST", async (t) => {
  await t.test("seals one attempt and review intent behind exact response authority", async () => {
    const fixture = githubGitFixture();
    const context = await createProtectedInitialRunnerContext(fixture);
    const plan = schedulerObservationRecord({
      predecessor: context.lease.acquire_commit_sha,
      lease: context.lease,
      priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
      initialAuthority: context.initialAuthority,
    }).payload.action;
    const schedulerAppend = await context.ledger
      .appendInitialSchedulerObservation({
        initial_runner_state_authority: context.initialAuthority,
        scheduler_evaluation: plan.scheduler_evaluation,
        status_plan: plan.status_plan,
      });
    const { automatic, response } = await appendAutomaticReservationStatus(
      context.ledger,
      schedulerAppend,
    );
    const input = {
      automatic_reservation_handle: automatic.automatic_reservation_handle,
      reservation_append_receipt: automatic.reservation_append_receipt,
      reservation_status_response_append: response,
    };
    const writesBeforeForgery = fixture.writeCalls;
    await assert.rejects(
      context.ledger.appendAutomaticReviewRequestIntent({
        ...input,
        automatic_reservation_handle: structuredClone(
          automatic.automatic_reservation_handle,
        ),
      }),
      (error) => error.code === "UNTRUSTED_AUTOMATIC_RESERVATION_HANDLE",
    );
    await assert.rejects(
      context.ledger.appendAutomaticReviewRequestIntent({
        ...input,
        reservation_append_receipt: structuredClone(
          automatic.reservation_append_receipt,
        ),
      }),
      (error) => error.code === "AUTOMATIC_RESERVATION_RECEIPT_MISMATCH",
    );
    await assert.rejects(
      context.ledger.appendAutomaticReviewRequestIntent({
        ...input,
        reservation_status_response_append: structuredClone(response),
      }),
      (error) =>
        error.code === "UNTRUSTED_RESERVATION_STATUS_RESPONSE_APPEND",
    );
    assert.equal(fixture.writeCalls, writesBeforeForgery);

    const requestIntent = await context.ledger
      .appendAutomaticReviewRequestIntent(input);
    assert.equal(
      assertV2GitLedgerAutomaticRequestIntentHandle(
        requestIntent.automatic_request_intent_handle,
      ),
      requestIntent.automatic_request_intent_handle,
    );
    assert.deepEqual(
      projectV2GitLedgerAutomaticReviewRequestTransport(
        requestIntent.automatic_request_intent_handle,
      ),
      requestIntent.transport,
    );
    assert.deepEqual(requestIntent.transport, {
      method: "POST",
      path: "/repos/owner/repo/issues/7/comments",
      body: "@codex review",
      json: { body: "@codex review" },
      expected_status: 201,
      retry_limit: 0,
      record_attempt_before_effect: true,
      network_uncertainty_policy: "do-not-retry-or-reclaim",
      generation_id: "automatic:1",
      reservation_digest: automatic.reservation.reservation_digest,
      attempt_digest:
        requestIntent.automatic_request_intent_handle.attempt_digest,
    });
    assert.throws(
      () => assertV2GitLedgerAutomaticRequestIntentHandle(
        structuredClone(requestIntent.automatic_request_intent_handle),
      ),
      (error) => error.code === "UNTRUSTED_AUTOMATIC_REQUEST_INTENT_HANDLE",
    );

    const loaded = await context.ledger.load();
    const attempt = loaded.records.find((entry) =>
      entry.commit_sha === requestIntent.attempt_append_receipt.commit_sha);
    const reviewIntent = loaded.records.find((entry) =>
      entry.commit_sha === requestIntent.intent_append_receipt.commit_sha);
    assert.equal(attempt.envelope.kind, "effect-attempt");
    assert.equal(reviewIntent.envelope.kind, "review-request");
    assert.equal(
      reviewIntent.envelope.payload.action.attempt_record_oid,
      attempt.commit_sha,
    );
    assert.equal(
      reviewIntent.envelope.payload.action.request_body_sha256,
      rawDigest("@codex review"),
    );
    assert.equal(
      loaded.records.some((entry) =>
        entry.envelope.record_type === "effect-response" &&
        entry.envelope.kind === "review-request"),
      false,
    );
    const control = await context.ledger.loadControlPlaneAuthority(
      context.initialAuthority.scope,
    );
    assert.equal(control.scoped_authority.runner_state.effect_attempts.length, 1);
    assert.equal(
      control.scoped_authority.runner_state.effect_attempts[0].record_oid,
      attempt.commit_sha,
    );
    assert.equal(
      control.scoped_authority.runner_state.effect_attempts[0].bound,
      false,
    );
    const writesAfterIntent = fixture.writeCalls;
    await assert.rejects(
      context.ledger.appendAutomaticReviewRequestIntent(input),
      (error) => error.code === "automatic-review-request-attempt-replayed",
    );
    assert.equal(fixture.writeCalls, writesAfterIntent);
  });

  await t.test("a failed review-intent commit leaves the attempt permanently consumed", async () => {
    const fixture = githubGitFixture();
    const context = await createProtectedInitialRunnerContext(fixture);
    const plan = schedulerObservationRecord({
      predecessor: context.lease.acquire_commit_sha,
      lease: context.lease,
      priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
      initialAuthority: context.initialAuthority,
    }).payload.action;
    const schedulerAppend = await context.ledger
      .appendInitialSchedulerObservation({
        initial_runner_state_authority: context.initialAuthority,
        scheduler_evaluation: plan.scheduler_evaluation,
        status_plan: plan.status_plan,
      });
    const { automatic, response } = await appendAutomaticReservationStatus(
      context.ledger,
      schedulerAppend,
    );
    const input = {
      automatic_reservation_handle: automatic.automatic_reservation_handle,
      reservation_append_receipt: automatic.reservation_append_receipt,
      reservation_status_response_append: response,
    };
    fixture.failNextEffectUpdate("review-request");
    await assert.rejects(
      context.ledger.appendAutomaticReviewRequestIntent(input),
      (error) => error.code === "unexpected-http-status",
    );
    const afterFailure = await context.ledger.load();
    const scopedAttempts = afterFailure.records.filter((entry) =>
      entry.envelope.record_type === "effect-intent" &&
      entry.envelope.kind === "effect-attempt" &&
      entry.envelope.payload.action.reservation_record_oid ===
        automatic.reservation_append_receipt.commit_sha);
    const scopedReviewIntents = afterFailure.records.filter((entry) =>
      entry.envelope.record_type === "effect-intent" &&
      entry.envelope.kind === "review-request" &&
      entry.envelope.payload.action.reservation_record_oid ===
        automatic.reservation_append_receipt.commit_sha);
    assert.equal(scopedAttempts.length, 1);
    assert.equal(scopedReviewIntents.length, 0);
    const writesAfterFailure = fixture.writeCalls;
    await assert.rejects(
      context.ledger.appendAutomaticReviewRequestIntent(input),
      (error) => error.code === "automatic-review-request-attempt-replayed",
    );
    assert.equal(fixture.writeCalls, writesAfterFailure);
  });
});

test("automatic request restart resumes one durable reservation without minting another", async () => {
  const fixture = githubGitFixture();
  const runIdentity = { ...OWNER };
  const context = await createProtectedInitialRunnerContext(fixture, {
    runIdentity,
  });
  const plan = schedulerObservationRecord({
    predecessor: context.lease.acquire_commit_sha,
    lease: context.lease,
    priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
    initialAuthority: context.initialAuthority,
  }).payload.action;
  const initialAppend = await context.ledger
    .appendInitialSchedulerObservation({
      initial_runner_state_authority: context.initialAuthority,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: plan.status_plan,
    });
  const { automatic } = await appendAutomaticReservationStatus(
    context.ledger,
    initialAppend,
  );
  await releaseFixtureDiscoveryLease(context.ledger, context.discovery);

  runIdentity.run_attempt = 2;
  const resumed = await acquireEstablishedRunnerContext({
    ledger: context.ledger,
    runIdentity,
    observationBoundary: "public-initial-wait-complete",
    transportStartSecond: 3600,
    minimalServerTime: "2026-08-13T14:00:00.000Z",
  });
  const established = await context.ledger
    .loadEstablishedRunnerStateAuthority({
      control_plane_authority: resumed.controlPlaneAuthority,
      evaluated_scope_receipt: resumed.evaluatedScopeReceipt,
      workflow_command_handle: resumed.workflowCommandHandle,
    });
  const inputs = schedulerInputsForEstablishedAuthority(established, {
    snapshotId: "snapshot-resume-automatic-request",
  });
  const schedulerAppend = await context.ledger
    .appendEstablishedSchedulerObservation({
      established_runner_state_authority: established,
      ...inputs,
    });
  const recovered = await context.ledger
    .appendRecoveredAutomaticReviewRequestIntent({
      scheduler_append: schedulerAppend,
    });
  assert.equal(recovered.transport.generation_id, "automatic:1");
  assert.equal(
    recovered.transport.reservation_digest,
    automatic.reservation.reservation_digest,
  );
  const loaded = await context.ledger.load();
  assert.equal(
    loaded.records.filter((entry) =>
      entry.envelope.record_type === "effect-intent" &&
      entry.envelope.kind === "automatic-request-reservation").length,
    1,
  );
  assert.equal(
    loaded.records.filter((entry) =>
      entry.envelope.record_type === "effect-intent" &&
      entry.envelope.kind === "effect-attempt").length,
    1,
  );
});

test("automatic review request binding is exact, controller-owned, and resumable", async (t) => {
  const prepare = async (fixture) => {
    const context = await createProtectedInitialRunnerContext(fixture);
    const plan = schedulerObservationRecord({
      predecessor: context.lease.acquire_commit_sha,
      lease: context.lease,
      priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
      initialAuthority: context.initialAuthority,
    }).payload.action;
    const schedulerAppend = await context.ledger
      .appendInitialSchedulerObservation({
        initial_runner_state_authority: context.initialAuthority,
        scheduler_evaluation: plan.scheduler_evaluation,
        status_plan: plan.status_plan,
      });
    const { automatic, response } = await appendAutomaticReservationStatus(
      context.ledger,
      schedulerAppend,
    );
    const requestIntent = await context.ledger
      .appendAutomaticReviewRequestIntent({
        automatic_reservation_handle:
          automatic.automatic_reservation_handle,
        reservation_append_receipt: automatic.reservation_append_receipt,
        reservation_status_response_append: response,
      });
    const receipt = await automaticReviewRequestBindingReceipt({
      action: {
        method: "POST",
        request_body_sha256: rawDigest("@codex review"),
      },
      observedAt: requestIntent.intent_append_receipt.ref_reread.server_time,
    });
    return { context, automatic, requestIntent, receipt };
  };

  await t.test("rejects ambiguity before any response and returns bound authority", async () => {
    const fixture = githubGitFixture();
    const { context, requestIntent, receipt } = await prepare(fixture);
    const input = {
      automatic_request_intent_handle:
        requestIntent.automatic_request_intent_handle,
      intent_append_receipt: requestIntent.intent_append_receipt,
      receipt,
    };
    const writesBeforeAmbiguity = fixture.writeCalls;
    await assert.rejects(
      context.ledger.appendAutomaticReviewRequestBinding({
        ...input,
        receipt: {
          ...receipt,
          request_url:
            "https://github.com/owner/repo/issues/7#issuecomment-71",
        },
      }),
      (error) => error.code === "automatic-review-request-url-scope",
    );
    await assert.rejects(
      context.ledger.appendAutomaticReviewRequestBinding({
        ...input,
        receipt: { ...receipt, actor: CODEX_ACTOR, app: CODEX_APP },
      }),
      (error) =>
        error.code === "automatic-review-request-controller-principal-mismatch" ||
        /GitHub Actions app/u.test(error.message),
    );
    await assert.rejects(
      context.ledger.appendAutomaticReviewRequestBinding({
        ...input,
        automatic_request_intent_handle: structuredClone(
          requestIntent.automatic_request_intent_handle,
        ),
      }),
      (error) => error.code === "UNTRUSTED_AUTOMATIC_REQUEST_INTENT_HANDLE",
    );
    assert.equal(fixture.writeCalls, writesBeforeAmbiguity);
    const beforeBinding = await context.ledger.load();
    assert.equal(beforeBinding.records.filter((entry) =>
      entry.envelope.kind === "effect-attempt").length, 1);
    assert.equal(beforeBinding.records.filter((entry) =>
      entry.envelope.kind === "review-request" &&
      entry.envelope.record_type === "effect-intent").length, 1);
    assert.equal(beforeBinding.records.some((entry) =>
      entry.envelope.kind === "review-request" &&
      entry.envelope.record_type === "effect-response"), false);

    const binding = await context.ledger
      .appendAutomaticReviewRequestBinding(input);
    assert.equal(
      binding.authoritative_controlled_request.request_id,
      receipt.request_id,
    );
    assert.equal(binding.authoritative_attempt.bound, true);
    assert.equal(
      binding.authoritative_attempt.binding_record_oid,
      binding.request_binding_response_append_receipt.commit_sha,
    );
    const final = await context.ledger.load();
    assert.deepEqual(
      final.records.slice(-3).map((entry) => [
        entry.envelope.record_type,
        entry.envelope.kind,
      ]),
      [
        ["effect-response", "review-request"],
        ["effect-intent", "request-binding"],
        ["effect-response", "request-binding"],
      ],
    );
    assert.equal(
      final.records.at(-3).envelope.payload.receipt.request_url,
      "https://github.com/owner/repo/pull/7#issuecomment-71",
    );
    assert.deepEqual(final.records.at(-3).envelope.payload.receipt.actor, ACTOR);
    const writesAfterBinding = fixture.writeCalls;
    assert.equal(
      await context.ledger.appendAutomaticReviewRequestBinding(input),
      binding,
    );
    assert.equal(fixture.writeCalls, writesAfterBinding);
  });

  await t.test("resumes the same receipt after a partial internal append", async () => {
    const fixture = githubGitFixture();
    const { context, requestIntent, receipt } = await prepare(fixture);
    fixture.freezeServerTime();
    const input = {
      automatic_request_intent_handle:
        requestIntent.automatic_request_intent_handle,
      intent_append_receipt: requestIntent.intent_append_receipt,
      receipt,
    };
    fixture.failNextEffectUpdate("request-binding");
    await assert.rejects(
      context.ledger.appendAutomaticReviewRequestBinding(input),
      (error) => error.code === "unexpected-http-status",
    );
    const partial = await context.ledger.load();
    assert.equal(partial.records.filter((entry) =>
      entry.envelope.kind === "review-request" &&
      entry.envelope.record_type === "effect-response").length, 1);
    assert.equal(partial.records.filter((entry) =>
      entry.envelope.kind === "request-binding").length, 0);
    const binding = await context.ledger
      .appendAutomaticReviewRequestBinding(input);
    assert.equal(binding.authoritative_attempt.bound, true);
    const final = await context.ledger.load();
    assert.equal(final.records.filter((entry) =>
      entry.envelope.kind === "review-request" &&
      entry.envelope.record_type === "effect-response").length, 1);
    assert.equal(final.records.filter((entry) =>
      entry.envelope.kind === "request-binding").length, 2);
  });
});

test("historical reservation status binding is required before the retry-zero request attempt", async () => {
  const fixture = githubGitFixture();
  const context = await createProtectedInitialRunnerContext(fixture);
  const {
    ledger, lease, initialAuthority,
  } = context;
  const generation = automaticGeneration(1);
  const beforeObservation = await ledger.load();
  const observationPlan = schedulerObservationRecord({
    predecessor: lease.acquire_commit_sha,
    lease,
    priorAuthorityDigest: priorRunnerAuthorityDigest(beforeObservation.records),
    initialAuthority,
  });
  const initialAppend = await ledger.appendInitialSchedulerObservation({
    initial_runner_state_authority: initialAuthority,
    scheduler_evaluation:
      observationPlan.payload.action.scheduler_evaluation,
    status_plan: observationPlan.payload.action.status_plan,
  });
  const observationRecord = initialAppend.record;
  const observation = initialAppend.append_receipt;
  const requiredStatus = await appendGenericRequiredStatus(
    ledger,
    lease,
    observationRecord,
    observation,
  );
  const statusControl = await ledger.loadControlPlaneAuthority(
    initialAuthority.scope,
  );
  const reservationRecord = protectedAutomaticReservationRecord({
    predecessor: requiredStatus.commit_sha,
    lease,
    generation,
    observationRecordOid: observation.commit_sha,
    observation: observationRecord,
    headLedger: statusControl.scoped_authority.runner_state.head_ledger,
  });
  const reservation = await appendHistoricalEffectRecord(
    ledger,
    reservationRecord,
  );
  const postAction = observationRecord.payload.action.scheduler_plan.actions
    .find((action) => action.kind === "post_review_request");

  const beforeStatusAttempt = effectAttemptRecord({
    predecessor: reservation.commit_sha,
    lease,
    generation,
    observationRecordOid: observation.commit_sha,
    reservationRecordOid: reservation.commit_sha,
    reservation: reservationRecord.payload.action.reservation,
    schedulerActionKey: postAction.idempotency_key,
  });
  let writes = fixture.writeCalls;
  await assert.rejects(
    appendHistoricalEffectRecord(ledger, beforeStatusAttempt),
    (error) => error.code === "reservation-status-binding-required",
  );
  assert.equal(fixture.writeCalls, writes, "no Git object is written before status intent");

  const statusIntentRecord = reservationStatusIntentRecord({
    predecessor: reservation.commit_sha,
    lease,
    generation,
    reservationRecordOid: reservation.commit_sha,
    reservationDigest: reservationRecord.payload.action.reservation_digest,
  });
  const statusIntent = await appendHistoricalEffectRecord(
    ledger,
    statusIntentRecord,
  );
  const afterAmbiguousStatusAttempt = effectAttemptRecord({
    predecessor: statusIntent.commit_sha,
    lease,
    generation,
    observationRecordOid: observation.commit_sha,
    reservationRecordOid: reservation.commit_sha,
    reservation: reservationRecord.payload.action.reservation,
    schedulerActionKey: postAction.idempotency_key,
  });
  writes = fixture.writeCalls;
  await assert.rejects(
    appendHistoricalEffectRecord(ledger, afterAmbiguousStatusAttempt),
    (error) => error.code === "reservation-status-binding-required",
  );
  assert.equal(fixture.writeCalls, writes, "ambiguous status never permits request POST");

  const loaded = await ledger.load();
  const authority = deriveV2GitLedgerAuthority(
    loaded.records,
    {
      pull_request: PR,
      head_ref_oid: HEAD,
      base_ref_oid: BASE,
      potential_merge_commit_oid: POTENTIAL,
    },
    loaded.observed_at,
  );
  assert.equal(authority.runner_state.head_ledger.automatic_request_count, 1,
    "reservation intent permanently consumes budget without a status response");
  assert.equal(authority.runner_state.reservations[0].reservation_status_bound,
    false);
  assert.equal(
    authority.runner_state.reservations[0].reservation_status_intent_record_oid,
    statusIntent.commit_sha,
  );
  assert.equal(
    authority.runner_state.scheduling.epoch.automatic_request.state,
    "intent-persisted",
  );
  assert.deepEqual(authority.runner_state.effect_attempts, []);
});

test("automatic recovery transition persists one opaque generation advance", async () => {
  const setup = await createAutomaticRecoveryTransitionContext();
  const writesBefore = setup.fixture.writeCalls;
  const transition = await setup.ledger
    .appendAutomaticRequestRecoveryTransition({
      scheduler_append: setup.schedulerAppend,
      recovery_handle: setup.recoveryHandle,
    });
  assert.equal(transition.prior_generation_id, "automatic:1");
  assert.equal(transition.next_generation_id, "automatic:2");
  assert.match(transition.intent_recovery_authority_digest, /^sha256:/u);
  assert.match(transition.completion_recovery_authority_digest, /^sha256:/u);
  assert.notEqual(
    transition.intent_record_receipt.commit_sha,
    transition.response_record_receipt.commit_sha,
  );
  assert.ok(setup.fixture.writeCalls > writesBefore);
  const loaded = await setup.ledger.loadControlPlaneAuthority(
    setup.context.evaluatedScopeReceipt.scope,
  );
  assert.equal(
    loaded.scoped_authority.runner_state.scheduling.epoch.automatic_request
      .state,
    "available",
  );
  assert.equal(
    loaded.scoped_authority.runner_state.scheduling.epoch.automatic_request
      .generation_index,
    2,
  );
  const nextGeneration = await createFreshAutomaticRecoveryContinuation(
    setup,
    {
      expectRecoveryHandle: false,
      expectArtifactBindingCandidateHandle: false,
      requireArtifactBinding: true,
    },
  );
  assert.equal(nextGeneration.recovery.recoveryHandle, null);
  assert.equal(
    nextGeneration.recovery.artifactBindingCandidateHandle,
    null,
  );
});

test("automatic recovery findings mint an opaque artifact-binding candidate before recovery",
  async () => {
    const setup = await createAutomaticRecoveryTransitionContext({
      bindArtifact: false,
    });
    assert.equal(setup.recoveryHandle, null);
    assert.notEqual(setup.artifactBindingCandidateHandle, null);
    assert.deepEqual(
      Object.keys(setup.artifactBindingCandidateHandle).sort(),
      ["authority_digest", "schema", "schema_version"],
    );
    assert.equal(
      Object.hasOwn(setup.runnerResult, "artifact_binding_candidate_handle"),
      false,
    );
    assert.throws(
      () => getV2AutomaticRecoveryArtifactBindingCandidateHandle(
        structuredClone(setup.runnerResult),
      ),
      /exact runner result object/u,
    );
  });

test("automatic recovery artifact bindings commit one exact aggregate transaction",
  async () => {
    const setup = await createAutomaticRecoveryTransitionContext({
      bindArtifact: false,
      additionalFindingIds: ["203"],
    });
    const writesBeforeIntent = setup.fixture.writeCalls;
    await assert.rejects(
      setup.ledger.loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: setup.schedulerAppend,
        artifact_binding_candidate_handle:
          structuredClone(setup.artifactBindingCandidateHandle),
      }),
      /opaque candidate handle/u,
    );
    assert.equal(setup.fixture.writeCalls, writesBeforeIntent);

    const intent = await setup.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: setup.schedulerAppend,
        artifact_binding_candidate_handle:
          setup.artifactBindingCandidateHandle,
      });
    assert.equal(intent.transport.operation,
      "loadV2ProviderPreScopeArtifacts");
    assert.equal(intent.transport.expected_count, 2);
    assert.deepEqual(
      intent.transport.requests.map((request) => request.selector.id),
      ["202", "203"],
    );
    assert.match(intent.transport.candidate_set_digest, /^sha256:/u);
    assert.equal(
      intent.latest_append_receipt.commit_sha,
      intent.ready_confirmation_append_receipt.commit_sha,
    );
    const handles = await loadAutomaticRecoveryProviderArtifactHandles({
      ledger: setup.ledger,
      fixture: setup.fixture,
      transport: intent.transport,
      snapshots: setup.snapshots,
    });
    const writesBeforeResponse = setup.fixture.writeCalls;
    const editedSnapshots = structuredClone(setup.snapshots);
    editedSnapshots.providerArtifacts.find((artifact) =>
      String(artifact.id) === "202").body += "\nEdited after the durable intent.";
    const editedHandles =
      await loadAutomaticRecoveryProviderArtifactHandles({
        ledger: setup.ledger,
        fixture: setup.fixture,
        transport: intent.transport,
        snapshots: editedSnapshots,
      });
    await assert.rejects(
      setup.ledger.appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          intent.artifact_binding_intent_handle,
        intent_append_receipt: intent.intent_append_receipt,
        provider_artifact_handles: editedHandles,
      }),
      (error) => error.code ===
        "automatic-artifact-binding-point-read-mismatch",
    );
    assert.equal(setup.fixture.writeCalls, writesBeforeResponse);
    await assert.rejects(
      setup.ledger.appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          intent.artifact_binding_intent_handle,
        intent_append_receipt: intent.intent_append_receipt,
        provider_artifact_handles: [...handles].reverse(),
      }),
      (error) => error.code ===
        "automatic-artifact-binding-point-read-mismatch",
    );
    assert.equal(setup.fixture.writeCalls, writesBeforeResponse);

    const bound = await setup.ledger
      .appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          intent.artifact_binding_intent_handle,
        intent_append_receipt: intent.intent_append_receipt,
        provider_artifact_handles: handles,
      });
    assert.equal(bound.bindings.length, 2);
    assert.deepEqual(
      bound.bindings.map((binding) => binding.artifact_selector.id),
      ["202", "203"],
    );
    assert.equal(
      new Set(bound.bindings.map((binding) => binding.record_oid)).size,
      1,
    );
    assert.equal(
      new Set(bound.bindings.map((binding) => binding.receipt_digest)).size,
      2,
    );
    const writesAfterResponse = setup.fixture.writeCalls;
    const replay = await setup.ledger
      .appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          intent.artifact_binding_intent_handle,
        intent_append_receipt: intent.intent_append_receipt,
        provider_artifact_handles: handles,
      });
    assert.deepEqual(replay, bound);
    assert.equal(setup.fixture.writeCalls, writesAfterResponse);

    const control = createV2ControlPlaneReceiptFromGitLedgerAuthority(
      await setup.ledger.loadControlPlaneAuthority(
        setup.context.evaluatedScopeReceipt.scope,
      ),
    );
    assert.deepEqual(
      control.derived.artifact_bindings.map((binding) =>
        binding.artifact_selector.id),
      ["202", "203"],
    );

    setup.fixture.rewriteTipEnvelope((envelope) => {
      const payload = structuredClone(envelope.payload);
      const receipt = payload.receipt;
      const binding = receipt.bindings[0];
      binding.raw_body_sha256 = `sha256:${"0".repeat(64)}`;
      const { receipt_digest: _bindingDigest, ...bindingWithoutDigest } =
        binding;
      binding.receipt_digest = digestCanonical(
        "codex-review-gate-v2-artifact-binding-receipt",
        bindingWithoutDigest,
      );
      receipt.bindings_digest = digestCanonical(
        "codex-review-gate-v2-aggregate-artifact-binding-receipts",
        receipt.bindings,
      );
      resealFixtureProductionEnvelopePayload(envelope, payload);
    });
    const restarted = makeLedger(
      setup.fixture,
      setup.context.capability,
      setup.context.verifier,
      setup.context.preflight,
    );
    await assert.rejects(
      restarted.load(),
      /differs from its exact candidate authority/u,
    );
  });

test("automatic recovery aggregate closes the exact 255-item blob boundary",
  async () => {
    const additionalFindingIds = Array.from(
      { length: 254 },
      (_, index) => String(1000 + index),
    );
    const setup = await createAutomaticRecoveryTransitionContext({
      bindArtifact: false,
      additionalFindingIds,
      includeAddresses: false,
    });
    const intent = await setup.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: setup.schedulerAppend,
        artifact_binding_candidate_handle:
          setup.artifactBindingCandidateHandle,
      });
    assert.equal(intent.transport.expected_count, 255);
    let loaded = await setup.ledger.load();
    const intentEnvelope = loaded.records.find((entry) =>
      entry.commit_sha === intent.intent_append_receipt.commit_sha).envelope;
    assert.ok(Buffer.byteLength(
      canonicalJson(intentEnvelope.payload.action),
      "utf8",
    ) < MAX_V2_AUTOMATIC_RECOVERY_ARTIFACT_BINDING_ACTION_BYTES);
    assert.ok(Buffer.byteLength(
      `${canonicalJson(intentEnvelope)}\n`,
      "utf8",
    ) <= MAX_V2_GIT_LEDGER_BLOB_BYTES);
    const providerArtifactHandles =
      await loadAutomaticRecoveryProviderArtifactHandles({
        ledger: setup.ledger,
        fixture: setup.fixture,
        transport: intent.transport,
        snapshots: setup.snapshots,
      });
    assert.equal(providerArtifactHandles.length, 255);
    const response = await setup.ledger
      .appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          intent.artifact_binding_intent_handle,
        intent_append_receipt: intent.intent_append_receipt,
        provider_artifact_handles: providerArtifactHandles,
      });
    assert.equal(response.bindings.length, 255);
    loaded = await setup.ledger.load();
    const responseEnvelope = loaded.records.find((entry) =>
      entry.commit_sha === response.response_append_receipt.commit_sha).envelope;
    assert.ok(Buffer.byteLength(
      canonicalJson(responseEnvelope.payload.receipt),
      "utf8",
    ) < MAX_V2_AUTOMATIC_RECOVERY_ARTIFACT_BINDING_RECEIPT_BYTES);
    assert.ok(Buffer.byteLength(
      `${canonicalJson(responseEnvelope)}\n`,
      "utf8",
    ) <= MAX_V2_GIT_LEDGER_BLOB_BYTES);
    const control = createV2ControlPlaneReceiptFromGitLedgerAuthority(
      await setup.ledger.loadControlPlaneAuthority(
        setup.context.evaluatedScopeReceipt.scope,
      ),
    );
    assert.equal(control.derived.artifact_bindings.length, 255);
    assert.deepEqual(
      control.derived.artifact_bindings.map((binding) =>
        binding.artifact_selector.id),
      response.bindings.map((binding) => binding.artifact_selector.id),
    );
    assert.deepEqual(
      response.bindings.map((binding) => binding.artifact_selector.id),
      intent.transport.requests.map((request) => request.selector.id),
    );
  });

test("automatic recovery aggregate rejects the 256th item before observation writes",
  async () => {
    let boundary = null;
    await assert.rejects(
      createAutomaticRecoveryTransitionContext({
        bindArtifact: false,
        additionalFindingIds: Array.from(
          { length: 255 },
          (_, index) => String(2000 + index),
        ),
        includeAddresses: false,
        beforeRecoveryObservation(value) {
          boundary = {
            fixture: value.fixture,
            writes: value.fixture.writeCalls,
            objects: value.fixture.blobs.size + value.fixture.trees.size +
              value.fixture.commits.size,
            tip: value.fixture.refTarget,
          };
        },
      }),
      (error) => error.code === "SELECTOR_LIMIT_EXCEEDED",
    );
    assert.notEqual(boundary, null);
    assert.equal(boundary.fixture.writeCalls, boundary.writes);
    assert.equal(
      boundary.fixture.blobs.size + boundary.fixture.trees.size +
        boundary.fixture.commits.size,
      boundary.objects,
    );
    assert.equal(boundary.fixture.refTarget, boundary.tip);
  });

test("automatic recovery aggregate rejects a self-sealed 256-item authority before write and on restart",
  async () => {
    const setup = await createAutomaticRecoveryTransitionContext({
      bindArtifact: false,
    });
    const intent = await setup.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: setup.schedulerAppend,
        artifact_binding_candidate_handle:
          setup.artifactBindingCandidateHandle,
      });
    const loaded = await setup.ledger.load();
    const intentEntry = loaded.records.find((entry) =>
      entry.commit_sha === intent.intent_append_receipt.commit_sha);
    assert.notEqual(intentEntry, undefined);
    const forgeCandidateCount = (payloadValue) => {
      const payload = structuredClone(payloadValue);
      const authority = payload.action.candidate_authority;
      authority.candidates = Array.from(
        { length: 256 },
        () => structuredClone(authority.candidates[0]),
      );
      return payload;
    };

    const forgedRecord = structuredClone(
      recordFromFixtureEnvelope(intentEntry.envelope),
    );
    forgedRecord.payload = forgeCandidateCount(forgedRecord.payload);
    forgedRecord.payload_digest = digestV2GitLedgerPayload(
      forgedRecord.payload,
    );
    const { record_digest: _recordDigest, ...recordWithoutDigest } =
      forgedRecord;
    forgedRecord.record_digest = digestCanonical(
      "codex-review-gate-v2-git-ledger-record",
      recordWithoutDigest,
    );
    const writesBefore = setup.fixture.writeCalls;
    const tipBefore = setup.fixture.refTarget;
    await assert.rejects(
      setup.ledger.appendRecord(forgedRecord, {
        evaluated_scope_receipt: setup.context.evaluatedScopeReceipt,
      }),
      (error) => error.code ===
        "automatic-artifact-binding-candidate-count",
    );
    assert.equal(setup.fixture.writeCalls, writesBefore);
    assert.equal(setup.fixture.refTarget, tipBefore);

    const providerArtifactHandles =
      await loadAutomaticRecoveryProviderArtifactHandles({
        ledger: setup.ledger,
        fixture: setup.fixture,
        transport: intent.transport,
        snapshots: setup.snapshots,
      });
    await setup.ledger.appendAutomaticRecoveryArtifactBindingResponse({
      artifact_binding_intent_handle:
        intent.artifact_binding_intent_handle,
      intent_append_receipt: intent.intent_append_receipt,
      provider_artifact_handles: providerArtifactHandles,
    });

    setup.fixture.rewriteTipEnvelope((envelope) => {
      resealFixtureProductionEnvelopePayload(
        envelope,
        forgeCandidateCount(envelope.payload),
      );
    });
    const restarted = makeLedger(
      setup.fixture,
      setup.context.capability,
      setup.context.verifier,
      setup.context.preflight,
    );
    await assert.rejects(
      restarted.load(),
      (error) => error.code ===
        "automatic-artifact-binding-candidate-count",
    );
  });

test("automatic recovery aggregate intent resumes on one fresh ordinary lease",
  async () => {
    const setup = await createAutomaticRecoveryTransitionContext({
      bindArtifact: false,
      additionalFindingIds: ["203"],
    });
    const originalIntent = await setup.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: setup.schedulerAppend,
        artifact_binding_candidate_handle:
          setup.artifactBindingCandidateHandle,
      });
    await releaseFixtureDiscoveryLease(
      setup.ledger,
      setup.context.discovery,
    );
    setup.fixture.advanceServerTime(7200);
    setup.runIdentity.run_id = "9002";
    setup.runIdentity.run_attempt = 1;
    const restarted = makeLedger(
      setup.fixture,
      setup.context.capability,
      setup.context.verifier,
      setup.context.preflight,
    );
    const nextRun = await acquireEstablishedRunnerContext({
      ledger: restarted,
      runIdentity: setup.runIdentity,
      observationBoundary: "public-post-request-wait-complete",
      transportStartSecond: 9000,
      minimalServerTime: "2026-08-13T15:00:00.000Z",
    });
    const nextObservation = await appendAutomaticRecoveryObservation({
      ledger: restarted,
      preflight: setup.context.preflight,
      evaluatedScopeReceipt: nextRun.evaluatedScopeReceipt,
      workflowCommandHandle: nextRun.workflowCommandHandle,
      requestCreatedAt: setup.requestCreatedAt,
      findingCreatedAt: setup.findingCreatedAt,
      additionalFindingIds: ["203"],
      expectRecoveryHandle: false,
    });
    const writesBeforeRehydrate = setup.fixture.writeCalls;
    const recoveredIntent = await restarted
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: nextObservation.schedulerAppend,
        artifact_binding_candidate_handle:
          nextObservation.artifactBindingCandidateHandle,
      });
    assert.equal(
      recoveredIntent.intent_append_receipt.commit_sha,
      originalIntent.intent_append_receipt.commit_sha,
    );
    assert.equal(setup.fixture.writeCalls, writesBeforeRehydrate);
    const handles = await loadAutomaticRecoveryProviderArtifactHandles({
      ledger: restarted,
      fixture: setup.fixture,
      transport: recoveredIntent.transport,
      snapshots: nextObservation.snapshots,
    });
    const response = await restarted
      .appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          recoveredIntent.artifact_binding_intent_handle,
        intent_append_receipt: recoveredIntent.intent_append_receipt,
        provider_artifact_handles: handles,
      });
    assert.equal(response.bindings.length, 2);
    const loaded = await restarted.load();
    const intents = loaded.records.filter((entry) =>
      entry.envelope.record_type === "effect-intent" &&
      entry.envelope.kind === "artifact-binding");
    const responses = loaded.records.filter((entry) =>
      entry.envelope.record_type === "effect-response" &&
      entry.envelope.kind === "artifact-binding");
    assert.equal(intents.length, 1);
    assert.equal(responses.length, 1);
    assert.equal(
      responses[0].envelope.payload.intent_commit_sha,
      intents[0].commit_sha,
    );
    assert.notEqual(
      responses[0].envelope.predecessor_commit_sha,
      intents[0].commit_sha,
    );
  });

test("automatic recovery aggregate resumes each durable pre-GET marker",
  async (t) => {
    for (const failure of [
      {
        label: "intent",
        kind: "artifact-binding",
        occurrence: 1,
        readyIntents: 0,
        readyResponses: 0,
      },
      {
        label: "ready-intent",
        kind: "artifact-binding-ready",
        occurrence: 1,
        readyIntents: 1,
        readyResponses: 0,
      },
      {
        label: "ready-confirmation",
        kind: "artifact-binding-ready",
        occurrence: 2,
        readyIntents: 1,
        readyResponses: 1,
      },
    ]) {
      await t.test(failure.label, async () => {
        const setup = await createAutomaticRecoveryTransitionContext({
          bindArtifact: false,
        });
        setup.fixture.failRereadAfterEffectUpdate(
          failure.kind,
          failure.occurrence,
        );
        await assert.rejects(
          setup.ledger.loadOrAppendAutomaticRecoveryArtifactBindingIntent({
            scheduler_append: setup.schedulerAppend,
            artifact_binding_candidate_handle:
              setup.artifactBindingCandidateHandle,
          }),
          (error) => error.code === "unexpected-http-status",
        );
        const partial = await setup.ledger.load();
        assert.equal(partial.records.filter((entry) =>
          entry.envelope.record_type === "effect-intent" &&
          entry.envelope.kind === "artifact-binding").length, 1);
        assert.equal(partial.records.filter((entry) =>
          entry.envelope.record_type === "effect-intent" &&
          entry.envelope.kind === "artifact-binding-ready").length,
        failure.readyIntents);
        assert.equal(partial.records.filter((entry) =>
          entry.envelope.record_type === "effect-response" &&
          entry.envelope.kind === "artifact-binding-ready").length,
        failure.readyResponses);

        const continuation = await createFreshAutomaticRecoveryContinuation(
          setup,
          {
            additionalFindingIds: ["203"],
            expectRecoveryHandle: false,
            requireArtifactBinding: false,
          },
        );
        const expanded = continuation.recovery;
        const continuationLedger = continuation.ledger;
        const recovered = await continuationLedger
          .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
            scheduler_append: expanded.schedulerAppend,
            artifact_binding_candidate_handle:
              expanded.artifactBindingCandidateHandle,
          });
        assert.equal(recovered.transport.operation,
          "loadV2ProviderPreScopeArtifacts");
        assert.equal(recovered.transport.expected_count, 1);
        assert.deepEqual(
          recovered.transport.requests.map((request) => request.selector.id),
          ["202"],
        );
        const stable = await continuationLedger.load();
        const bindingIntents = stable.records.filter((entry) =>
          entry.envelope.record_type === "effect-intent" &&
          entry.envelope.kind === "artifact-binding");
        assert.equal(bindingIntents.length, 2);
        assert.deepEqual(
          bindingIntents.map((entry) =>
            entry.envelope.payload.action.candidate_authority.candidates
              .map((candidate) => candidate.selector.id)),
          [["202"], ["203"]],
        );
        assert.equal(stable.records.filter((entry) =>
          entry.envelope.record_type === "effect-intent" &&
          entry.envelope.kind === "artifact-binding-ready").length, 1);
        assert.equal(stable.records.filter((entry) =>
          entry.envelope.record_type === "effect-response" &&
          entry.envelope.kind === "artifact-binding-ready").length, 1);
        assert.equal(
          recovered.ready_append_receipt.commit_sha,
          stable.records.find((entry) =>
            entry.envelope.record_type === "effect-intent" &&
            entry.envelope.kind === "artifact-binding-ready").commit_sha,
        );
        assert.equal(
          recovered.ready_confirmation_append_receipt.commit_sha,
          stable.records.find((entry) =>
            entry.envelope.record_type === "effect-response" &&
            entry.envelope.kind === "artifact-binding-ready").commit_sha,
        );
      });
    }
  });

test("automatic recovery aggregate resumes its frozen durable prefix when current evidence appends candidates",
  async () => {
    const setup = await createAutomaticRecoveryTransitionContext({
      bindArtifact: false,
    });
    setup.fixture.failRereadAfterEffectUpdate(
      "artifact-binding-ready",
      2,
    );
    await assert.rejects(
      setup.ledger.loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: setup.schedulerAppend,
        artifact_binding_candidate_handle:
          setup.artifactBindingCandidateHandle,
      }),
      (error) => error.code === "unexpected-http-status",
    );

    setup.fixture.advanceServerTime(60);
    const expanded = await appendAutomaticRecoveryObservation({
      ledger: setup.ledger,
      preflight: setup.context.preflight,
      evaluatedScopeReceipt: setup.context.evaluatedScopeReceipt,
      workflowCommandHandle: setup.context.workflowCommandHandle,
      requestCreatedAt: setup.requestCreatedAt,
      findingCreatedAt: setup.findingCreatedAt,
      additionalFindingIds: ["203"],
      expectRecoveryHandle: false,
      requireArtifactBinding: false,
    });
    setup.fixture.failRereadAfterEffectUpdate("artifact-binding", 1);
    await assert.rejects(
      setup.ledger.loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: expanded.schedulerAppend,
        artifact_binding_candidate_handle:
          expanded.artifactBindingCandidateHandle,
      }),
      (error) => error.code === "unexpected-http-status",
    );
    const queuedAfterReturnLoss = await setup.ledger.load();
    assert.deepEqual(
      queuedAfterReturnLoss.records.filter((entry) =>
        entry.envelope.record_type === "effect-intent" &&
        entry.envelope.kind === "artifact-binding").map((entry) =>
        entry.envelope.payload.action.candidate_authority.candidates
          .map((candidate) => candidate.selector.id)),
      [["202"], ["203"]],
    );
    const recovered = await setup.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: expanded.schedulerAppend,
        artifact_binding_candidate_handle:
          expanded.artifactBindingCandidateHandle,
      });
    assert.equal(recovered.transport.expected_count, 1);
    assert.deepEqual(
      recovered.transport.requests.map((request) => request.selector.id),
      ["202"],
    );
    const durableQueuedIntent = queuedAfterReturnLoss.records.find((entry) =>
      entry.envelope.record_type === "effect-intent" &&
      entry.envelope.kind === "artifact-binding" &&
      entry.envelope.payload.action
        .queued_after_artifact_binding_intent_record_oid !== null);
    assert.notEqual(durableQueuedIntent, undefined);
    assert.equal(
      recovered.latest_append_receipt.commit_sha,
      durableQueuedIntent.commit_sha,
    );
    const firstHandles = await loadAutomaticRecoveryProviderArtifactHandles({
      ledger: setup.ledger,
      fixture: setup.fixture,
      transport: recovered.transport,
      snapshots: expanded.snapshots,
    });
    const firstResponse = await setup.ledger
      .appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          recovered.artifact_binding_intent_handle,
        intent_append_receipt: recovered.intent_append_receipt,
        provider_artifact_handles: firstHandles,
      });
    assert.deepEqual(
      firstResponse.bindings.map((binding) => binding.artifact_selector.id),
      ["202"],
    );

    setup.fixture.advanceServerTime(60);
    const temporarilyShrunk = await appendAutomaticRecoveryObservation({
      ledger: setup.ledger,
      preflight: setup.context.preflight,
      evaluatedScopeReceipt: setup.context.evaluatedScopeReceipt,
      workflowCommandHandle: setup.context.workflowCommandHandle,
      requestCreatedAt: setup.requestCreatedAt,
      findingCreatedAt: setup.findingCreatedAt,
      expectRecoveryHandle: true,
      requireArtifactBinding: true,
    });
    const writesBeforeShrunkTransition = setup.fixture.writeCalls;
    await assert.rejects(
      setup.ledger.appendAutomaticRequestRecoveryTransition({
        scheduler_append: temporarilyShrunk.schedulerAppend,
        recovery_handle: temporarilyShrunk.recoveryHandle,
      }),
      (error) => error.code ===
        "automatic-recovery-artifact-binding-incomplete",
    );
    assert.equal(setup.fixture.writeCalls, writesBeforeShrunkTransition);

    const remainingContinuation =
      await createFreshAutomaticRecoveryContinuation(setup, {
        additionalFindingIds: ["203"],
        expectRecoveryHandle: false,
        requireArtifactBinding: true,
      });
    const continuationLedger = remainingContinuation.ledger;
    const remaining = remainingContinuation.recovery;
    const second = await continuationLedger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: remaining.schedulerAppend,
        artifact_binding_candidate_handle:
          remaining.artifactBindingCandidateHandle,
      });
    assert.equal(second.transport.expected_count, 1);
    assert.deepEqual(
      second.transport.requests.map((request) => request.selector.id),
      ["203"],
    );
    const secondHandles = await loadAutomaticRecoveryProviderArtifactHandles({
      ledger: continuationLedger,
      fixture: setup.fixture,
      transport: second.transport,
      snapshots: remaining.snapshots,
    });
    const secondResponse = await continuationLedger
      .appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          second.artifact_binding_intent_handle,
        intent_append_receipt: second.intent_append_receipt,
        provider_artifact_handles: secondHandles,
      });
    assert.deepEqual(
      secondResponse.bindings.map((binding) => binding.artifact_selector.id),
      ["203"],
    );

    setup.fixture.advanceServerTime(60);
    const complete = await appendAutomaticRecoveryObservation({
      ledger: continuationLedger,
      preflight: setup.context.preflight,
      evaluatedScopeReceipt:
        remainingContinuation.nextRun.evaluatedScopeReceipt,
      workflowCommandHandle:
        remainingContinuation.nextRun.workflowCommandHandle,
      requestCreatedAt: setup.requestCreatedAt,
      findingCreatedAt: setup.findingCreatedAt,
      additionalFindingIds: ["203"],
      expectRecoveryHandle: true,
      requireArtifactBinding: true,
    });
    assert.equal(complete.artifactBindingCandidateHandle, null);
    const transition = await continuationLedger
      .appendAutomaticRequestRecoveryTransition({
        scheduler_append: complete.schedulerAppend,
        recovery_handle: complete.recoveryHandle,
      });
    assert.equal(transition.prior_generation_id, "automatic:1");
    assert.equal(transition.next_generation_id, "automatic:2");
  });

test("automatic recovery aggregate rejects deletion, drift, or insertion before its frozen prefix",
  async (t) => {
    const setup = await createAutomaticRecoveryTransitionContext({
      bindArtifact: false,
    });
    setup.fixture.failRereadAfterEffectUpdate(
      "artifact-binding-ready",
      2,
    );
    await assert.rejects(
      setup.ledger.loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: setup.schedulerAppend,
        artifact_binding_candidate_handle:
          setup.artifactBindingCandidateHandle,
      }),
      (error) => error.code === "unexpected-http-status",
    );

    for (const mutation of [
      {
        label: "delete-frozen-candidate",
        options: {
          includePrimaryFinding: false,
          additionalFindingIds: ["203"],
        },
      },
      {
        label: "change-frozen-identity",
        options: {
          findingLine: 2,
          additionalFindingIds: ["203"],
        },
      },
      {
        label: "insert-before-and-reorder-frozen-candidate",
        options: {
          precedingFindingIds: ["201"],
        },
      },
    ]) {
      await t.test(mutation.label, async () => {
        setup.fixture.advanceServerTime(60);
        const current = await appendAutomaticRecoveryObservation({
          ledger: setup.ledger,
          preflight: setup.context.preflight,
          evaluatedScopeReceipt: setup.context.evaluatedScopeReceipt,
          workflowCommandHandle: setup.context.workflowCommandHandle,
          requestCreatedAt: setup.requestCreatedAt,
          findingCreatedAt: setup.findingCreatedAt,
          expectRecoveryHandle: false,
          requireArtifactBinding: false,
          ...mutation.options,
        });
        const writesBefore = setup.fixture.writeCalls;
        await assert.rejects(
          setup.ledger.loadOrAppendAutomaticRecoveryArtifactBindingIntent({
            scheduler_append: current.schedulerAppend,
            artifact_binding_candidate_handle:
              current.artifactBindingCandidateHandle,
          }),
          (error) => error.code ===
            "automatic-artifact-binding-candidate-drift",
        );
        assert.equal(setup.fixture.writeCalls, writesBefore);
      });
    }

    const stable = await setup.ledger.load();
    assert.equal(stable.records.filter((entry) =>
      entry.envelope.record_type === "effect-intent" &&
      entry.envelope.kind === "artifact-binding").length, 1);
    assert.equal(stable.records.filter((entry) =>
      entry.envelope.record_type === "effect-intent" &&
      entry.envelope.kind === "artifact-binding-ready").length, 1);
    assert.equal(stable.records.filter((entry) =>
      entry.envelope.record_type === "effect-response" &&
      entry.envelope.kind === "artifact-binding-ready").length, 1);
  });

test("automatic recovery aggregate rejects a third durable candidate transaction",
  async () => {
    const { setup } =
      await createAutomaticRecoveryQueuedArtifactBindingContext();
    setup.fixture.advanceServerTime(60);
    const third = await appendAutomaticRecoveryObservation({
      ledger: setup.ledger,
      preflight: setup.context.preflight,
      evaluatedScopeReceipt: setup.context.evaluatedScopeReceipt,
      workflowCommandHandle: setup.context.workflowCommandHandle,
      requestCreatedAt: setup.requestCreatedAt,
      findingCreatedAt: setup.findingCreatedAt,
      additionalFindingIds: ["203", "204"],
      expectRecoveryHandle: false,
      requireArtifactBinding: false,
    });
    const writesBefore = setup.fixture.writeCalls;
    const tipBefore = setup.fixture.refTarget;
    await assert.rejects(
      setup.ledger.loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: third.schedulerAppend,
        artifact_binding_candidate_handle:
          third.artifactBindingCandidateHandle,
      }),
      (error) => error.code ===
        "automatic-artifact-binding-queue-capacity",
    );
    assert.equal(setup.fixture.writeCalls, writesBefore);
    assert.equal(setup.fixture.refTarget, tipBefore);
  });

test("automatic recovery aggregate history rejects self-sealed queue lineage drift",
  async (t) => {
    for (const mutation of [
      {
        label: "missing-parent",
        additionalFindingIds: ["203"],
        mutate(payload) {
          payload.action
            .queued_after_artifact_binding_intent_record_oid = null;
        },
      },
      {
        label: "wrong-parent",
        additionalFindingIds: ["203"],
        mutate(payload) {
          payload.action
            .queued_after_artifact_binding_intent_record_oid =
              "f".repeat(40);
        },
      },
      {
        label: "duplicate-parent-candidate",
        additionalFindingIds: ["203"],
        mutate(payload, parentEntry) {
          const parentCandidate = parentEntry.envelope.payload.action
            .candidate_authority.candidates[0];
          payload.action.candidate_authority =
            resealFixtureAutomaticRecoveryCandidateAuthority(
              payload.action.candidate_authority,
              [parentCandidate],
            );
          payload.action.candidate_set_digest =
            fixtureAutomaticRecoveryCandidateSetDigest(
              payload.action.candidate_authority,
            );
        },
      },
      {
        label: "reordered-queued-suffix",
        additionalFindingIds: ["203", "204"],
        mutate(payload) {
          payload.action.candidate_authority =
            resealFixtureAutomaticRecoveryCandidateAuthority(
              payload.action.candidate_authority,
              [...payload.action.candidate_authority.candidates].reverse(),
            );
          payload.action.candidate_set_digest =
            fixtureAutomaticRecoveryCandidateSetDigest(
              payload.action.candidate_authority,
            );
        },
      },
    ]) {
      await t.test(mutation.label, async () => {
        const { setup, parentEntry } =
          await createAutomaticRecoveryQueuedArtifactBindingContext({
            additionalFindingIds: mutation.additionalFindingIds,
          });
        setup.fixture.rewriteTipEnvelope((envelope) => {
          assert.equal(envelope.record_type, "effect-intent");
          assert.equal(envelope.kind, "artifact-binding");
          const payload = structuredClone(envelope.payload);
          mutation.mutate(payload, parentEntry);
          resealFixtureProductionEnvelopePayload(envelope, payload);
        });
        const writesBefore = setup.fixture.writeCalls;
        const tipBefore = setup.fixture.refTarget;
        const restarted = makeLedger(
          setup.fixture,
          setup.context.capability,
          setup.context.verifier,
          setup.context.preflight,
        );
        await assert.rejects(
          restarted.load(),
          (error) => error.code ===
            "automatic-artifact-binding-queue-lineage",
        );
        assert.equal(setup.fixture.writeCalls, writesBefore);
        assert.equal(setup.fixture.refTarget, tipBefore);
      });
    }
  });

test("automatic recovery aggregate accepts point reads only after its durable ready confirmation",
  async () => {
    const setup = await createAutomaticRecoveryTransitionContext({
      bindArtifact: false,
    });
    const beforeIntentBoundary = (await setup.ledger.load()).post_ref.server_time;
    const preIntentHandle = await loadAutomaticRecoveryProviderArtifactHandle({
      request: {
        owner: REPOSITORY.owner,
        repo: REPOSITORY.name,
        pullNumber: PR.number,
        headSha: HEAD,
        selector: { kind: "issue_comment", id: "202" },
        expectedActor: CODEX_ACTOR,
        expectedApp: CODEX_APP,
      },
      snapshots: setup.snapshots,
      server_time: beforeIntentBoundary,
    });
    const intent = await setup.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: setup.schedulerAppend,
        artifact_binding_candidate_handle:
          setup.artifactBindingCandidateHandle,
      });
    const loaded = await setup.ledger.load();
    const ready = loaded.records.find((entry) =>
      entry.commit_sha === intent.ready_append_receipt.commit_sha);
    const readyConfirmation = loaded.records.find((entry) =>
      entry.commit_sha ===
        intent.ready_confirmation_append_receipt.commit_sha);
    assert.notEqual(ready, undefined);
    assert.notEqual(readyConfirmation, undefined);
    const readyBoundary = readyConfirmation.envelope.payload.receipt
      .ready_reachability_boundary.ref_receipt.server_time;
    const equalTimeHandle = await loadAutomaticRecoveryProviderArtifactHandle({
      request: intent.transport.requests[0],
      snapshots: setup.snapshots,
      server_time: readyBoundary,
    });
    const writesBefore = setup.fixture.writeCalls;
    const tipBefore = setup.fixture.refTarget;
    for (const providerHandle of [preIntentHandle, equalTimeHandle]) {
      await assert.rejects(
        setup.ledger.appendAutomaticRecoveryArtifactBindingResponse({
          artifact_binding_intent_handle:
            intent.artifact_binding_intent_handle,
          intent_append_receipt: intent.intent_append_receipt,
          provider_artifact_handles: [providerHandle],
        }),
        (error) => error.code ===
          "automatic-artifact-binding-point-read-too-early",
      );
      assert.equal(setup.fixture.writeCalls, writesBefore);
      assert.equal(setup.fixture.refTarget, tipBefore);
    }

    const providerArtifactHandles =
      await loadAutomaticRecoveryProviderArtifactHandles({
        ledger: setup.ledger,
        fixture: setup.fixture,
        transport: intent.transport,
        snapshots: setup.snapshots,
      });
    const bound = await setup.ledger
      .appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          intent.artifact_binding_intent_handle,
        intent_append_receipt: intent.intent_append_receipt,
        provider_artifact_handles: providerArtifactHandles,
      });
    assert.equal(bound.bindings.length, 1);
  });

test("automatic recovery aggregate point reads follow the post-ready stable ref boundary",
  async () => {
    const setup = await createAutomaticRecoveryTransitionContext({
      bindArtifact: false,
    });
    setup.fixture.resumeServerTime();
    const intent = await setup.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: setup.schedulerAppend,
        artifact_binding_candidate_handle:
          setup.artifactBindingCandidateHandle,
      });
    assert.notEqual(intent.ready_confirmation_append_receipt, undefined);
    const loaded = await setup.ledger.load();
    const ready = loaded.records.find((entry) =>
      entry.commit_sha === intent.ready_append_receipt.commit_sha);
    const readyConfirmation = loaded.records.find((entry) =>
      entry.commit_sha ===
        intent.ready_confirmation_append_receipt.commit_sha);
    assert.notEqual(ready, undefined);
    assert.notEqual(readyConfirmation, undefined);
    const preReadyBoundary = ready.envelope.payload.action
      .intent_reachability_boundary.ref_receipt.server_time;
    const postReadyBoundary = readyConfirmation.envelope.payload.receipt
      .ready_reachability_boundary.ref_receipt.server_time;
    const betweenTime = new Date(
      Date.parse(preReadyBoundary) + 1000,
    ).toISOString();
    assert.ok(Date.parse(preReadyBoundary) < Date.parse(betweenTime));
    assert.ok(Date.parse(betweenTime) < Date.parse(postReadyBoundary));
    const pointRead = await loadAutomaticRecoveryProviderArtifactHandle({
      request: intent.transport.requests[0],
      snapshots: setup.snapshots,
      server_time: betweenTime,
    });
    const writesBefore = setup.fixture.writeCalls;
    const tipBefore = setup.fixture.refTarget;
    await assert.rejects(
      setup.ledger.appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          intent.artifact_binding_intent_handle,
        intent_append_receipt: intent.intent_append_receipt,
        provider_artifact_handles: [pointRead],
      }),
      (error) => error.code ===
        "automatic-artifact-binding-point-read-too-early",
    );
    assert.equal(setup.fixture.writeCalls, writesBefore);
    assert.equal(setup.fixture.refTarget, tipBefore);
  });

test("automatic recovery aggregate ready intent exact-binds its stable ref time on restart",
  async () => {
    const setup = await createAutomaticRecoveryTransitionContext({
      bindArtifact: false,
    });
    setup.fixture.resumeServerTime();
    setup.fixture.failRereadAfterEffectUpdate("artifact-binding-ready", 1);
    await assert.rejects(
      setup.ledger.loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: setup.schedulerAppend,
        artifact_binding_candidate_handle:
          setup.artifactBindingCandidateHandle,
      }),
      (error) => error.code === "unexpected-http-status",
    );
    const loaded = await setup.ledger.load();
    const intentEntry = loaded.records.find((entry) =>
      entry.envelope.record_type === "effect-intent" &&
      entry.envelope.kind === "artifact-binding");
    const readyEntry = loaded.records.find((entry) =>
      entry.envelope.record_type === "effect-intent" &&
      entry.envelope.kind === "artifact-binding-ready");
    assert.notEqual(intentEntry, undefined);
    assert.notEqual(readyEntry, undefined);
    assert.notEqual(
      readyEntry.envelope.payload.action.intent_reachability_boundary
        .ref_receipt.server_time,
      intentEntry.envelope.server_observed_at,
    );
    setup.fixture.rewriteTipEnvelope((envelope) => {
      const payload = structuredClone(envelope.payload);
      const boundary = payload.action.intent_reachability_boundary;
      boundary.ref_receipt.server_time =
        intentEntry.envelope.server_observed_at;
      const { boundary_digest: _digest, ...withoutDigest } = boundary;
      boundary.boundary_digest = digestCanonical(
        "codex-review-gate-v2-automatic-recovery-artifact-reachability",
        withoutDigest,
      );
      resealFixtureProductionEnvelopePayload(envelope, payload);
    });
    const restarted = makeLedger(
      setup.fixture,
      setup.context.capability,
      setup.context.verifier,
      setup.context.preflight,
    );
    await assert.rejects(
      restarted.load(),
      (error) => error.code === "automatic-artifact-binding-ready-lineage",
    );
  });

test("automatic recovery aggregate history rejects a skipped ready marker and downshifted point read",
  async (t) => {
    for (const mutation of [
      "resealed-ready-boundary",
      "downshift-ready-confirmation-boundary-time",
      "skip-ready",
      "equal-ready-time",
    ]) {
      await t.test(mutation, async () => {
        const setup = await createAutomaticRecoveryTransitionContext({
          bindArtifact: false,
        });
        if (mutation === "downshift-ready-confirmation-boundary-time") {
          setup.fixture.resumeServerTime();
        }
        const intent = await setup.ledger
          .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
            scheduler_append: setup.schedulerAppend,
            artifact_binding_candidate_handle:
              setup.artifactBindingCandidateHandle,
          });
        if (new Set([
          "resealed-ready-boundary",
          "downshift-ready-confirmation-boundary-time",
        ]).has(mutation)) {
          const beforeMutation = await setup.ledger.load();
          const readyEntry = beforeMutation.records.find((entry) =>
            entry.commit_sha === intent.ready_append_receipt.commit_sha);
          assert.notEqual(readyEntry, undefined);
          setup.fixture.rewriteTipEnvelope((envelope) => {
            const payload = structuredClone(envelope.payload);
            const boundary = payload.receipt.ready_reachability_boundary;
            if (mutation === "resealed-ready-boundary") {
              boundary.boundary_tip_record_oid =
                boundary.continuation_scheduler_observation_record_oid;
              boundary.ref_receipt.target_commit_sha =
                boundary.continuation_scheduler_observation_record_oid;
            } else {
              assert.notEqual(
                boundary.ref_receipt.server_time,
                readyEntry.envelope.server_observed_at,
              );
              boundary.ref_receipt.server_time =
                readyEntry.envelope.server_observed_at;
            }
            const { boundary_digest: _digest, ...withoutDigest } = boundary;
            boundary.boundary_digest = digestCanonical(
              "codex-review-gate-v2-automatic-recovery-artifact-ready-reachability",
              withoutDigest,
            );
            resealFixtureProductionEnvelopePayload(envelope, payload);
          });
          const restarted = makeLedger(
            setup.fixture,
            setup.context.capability,
            setup.context.verifier,
            setup.context.preflight,
          );
          await assert.rejects(
            restarted.load(),
            (error) => error.code ===
              "automatic-artifact-binding-ready-confirmation-lineage",
          );
          return;
        }
        const providerArtifactHandles =
          await loadAutomaticRecoveryProviderArtifactHandles({
            ledger: setup.ledger,
            fixture: setup.fixture,
            transport: intent.transport,
            snapshots: setup.snapshots,
          });
        await setup.ledger.appendAutomaticRecoveryArtifactBindingResponse({
          artifact_binding_intent_handle:
            intent.artifact_binding_intent_handle,
          intent_append_receipt: intent.intent_append_receipt,
          provider_artifact_handles: providerArtifactHandles,
        });
        const loaded = await setup.ledger.load();
        const intentEntry = loaded.records.find((entry) =>
          entry.commit_sha === intent.intent_append_receipt.commit_sha);
        const readyEntry = loaded.records.find((entry) =>
          entry.commit_sha === intent.ready_append_receipt.commit_sha);
        const readyConfirmationEntry = loaded.records.find((entry) =>
          entry.commit_sha ===
            intent.ready_confirmation_append_receipt.commit_sha);
        assert.notEqual(intentEntry, undefined);
        assert.notEqual(readyEntry, undefined);
        assert.notEqual(readyConfirmationEntry, undefined);
        setup.fixture.rewriteTipEnvelope((envelope) => {
          const payload = structuredClone(envelope.payload);
          if (mutation === "skip-ready") {
            payload.receipt
              .artifact_binding_ready_confirmation_record_oid =
                readyEntry.commit_sha;
            payload.receipt
              .artifact_binding_ready_confirmation_payload_digest =
                readyEntry.envelope.payload_digest;
          } else {
            const boundaryTime = readyConfirmationEntry.envelope.payload
              .receipt.ready_reachability_boundary.ref_receipt.server_time;
            const binding = payload.receipt.bindings[0];
            binding.server_time = boundaryTime;
            const { receipt_digest: _digest, ...withoutDigest } = binding;
            binding.receipt_digest = digestCanonical(
              "codex-review-gate-v2-artifact-binding-receipt",
              withoutDigest,
            );
            payload.receipt.bindings_digest = digestCanonical(
              "codex-review-gate-v2-aggregate-artifact-binding-receipts",
              payload.receipt.bindings,
            );
          }
          resealFixtureProductionEnvelopePayload(envelope, payload);
        });
        const restarted = makeLedger(
          setup.fixture,
          setup.context.capability,
          setup.context.verifier,
          setup.context.preflight,
        );
        await assert.rejects(
          restarted.load(),
          (error) => error.code ===
            "automatic-artifact-binding-reachability-lineage",
        );
      });
    }
  });

test("automatic recovery aggregate returns one durable response without another GET or write",
  async () => {
    const setup = await createAutomaticRecoveryTransitionContext({
      bindArtifact: false,
    });
    const intent = await setup.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: setup.schedulerAppend,
        artifact_binding_candidate_handle:
          setup.artifactBindingCandidateHandle,
      });
    let providerReads = 0;
    const providerArtifactHandles =
      await loadAutomaticRecoveryProviderArtifactHandles({
        ledger: setup.ledger,
        fixture: setup.fixture,
        transport: intent.transport,
        snapshots: setup.snapshots,
        on_read() { providerReads += 1; },
      });
    assert.equal(providerReads, intent.transport.expected_count);
    setup.fixture.failRereadAfterEffectUpdate("artifact-binding", 1);
    await assert.rejects(
      setup.ledger.appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          intent.artifact_binding_intent_handle,
        intent_append_receipt: intent.intent_append_receipt,
        provider_artifact_handles: providerArtifactHandles,
      }),
      (error) => error.code === "unexpected-http-status",
    );
    const durable = await setup.ledger.load();
    assert.equal(durable.records.filter((entry) =>
      entry.envelope.record_type === "effect-response" &&
      entry.envelope.kind === "artifact-binding").length, 1);
    const writesBefore = setup.fixture.writeCalls;
    const response = await setup.ledger
      .appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          intent.artifact_binding_intent_handle,
        intent_append_receipt: intent.intent_append_receipt,
        provider_artifact_handles: providerArtifactHandles,
      });
    assert.equal(response.bindings.length, intent.transport.expected_count);
    assert.equal(setup.fixture.writeCalls, writesBefore);
    assert.equal(providerReads, intent.transport.expected_count);
  });

test("automatic recovery aggregate intent rejects fresh same-ID body drift",
  async () => {
    const setup = await createAutomaticRecoveryTransitionContext({
      bindArtifact: false,
    });
    await setup.ledger.loadOrAppendAutomaticRecoveryArtifactBindingIntent({
      scheduler_append: setup.schedulerAppend,
      artifact_binding_candidate_handle:
        setup.artifactBindingCandidateHandle,
    });
    await releaseFixtureDiscoveryLease(
      setup.ledger,
      setup.context.discovery,
    );
    setup.fixture.advanceServerTime(7200);
    setup.runIdentity.run_id = "9002";
    setup.runIdentity.run_attempt = 1;
    const restarted = makeLedger(
      setup.fixture,
      setup.context.capability,
      setup.context.verifier,
      setup.context.preflight,
    );
    const nextRun = await acquireEstablishedRunnerContext({
      ledger: restarted,
      runIdentity: setup.runIdentity,
      observationBoundary: "public-post-request-wait-complete",
      transportStartSecond: 9000,
      minimalServerTime: "2026-08-13T15:00:00.000Z",
    });
    const changed = await appendAutomaticRecoveryObservation({
      ledger: restarted,
      preflight: setup.context.preflight,
      evaluatedScopeReceipt: nextRun.evaluatedScopeReceipt,
      workflowCommandHandle: nextRun.workflowCommandHandle,
      requestCreatedAt: setup.requestCreatedAt,
      findingCreatedAt: setup.findingCreatedAt,
      findingLine: 2,
      expectRecoveryHandle: false,
    });
    const writesBefore = setup.fixture.writeCalls;
    const tipBefore = setup.fixture.refTarget;
    await assert.rejects(
      restarted.loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: changed.schedulerAppend,
        artifact_binding_candidate_handle:
          changed.artifactBindingCandidateHandle,
      }),
      (error) => error.code ===
        "automatic-artifact-binding-candidate-drift",
    );
    assert.equal(setup.fixture.writeCalls, writesBefore);
    assert.equal(setup.fixture.refTarget, tipBefore);
  });

test("automatic recovery transition resumes durable partial writes without replay",
  async (t) => {
    await t.test("an intent ref update resumes with one response", async () => {
      const setup = await createAutomaticRecoveryTransitionContext();
      setup.fixture.failRereadAfterEffectUpdate("scheduler-state", 1);
      await assert.rejects(
        setup.ledger.appendAutomaticRequestRecoveryTransition({
          scheduler_append: setup.schedulerAppend,
          recovery_handle: setup.recoveryHandle,
        }),
        (error) => error.code === "unexpected-http-status",
      );
      let loaded = await setup.ledger.load();
      assert.equal(loaded.records.filter((entry) =>
        entry.envelope.kind === "scheduler-state" &&
        entry.envelope.record_type === "effect-intent").length, 1);
      assert.equal(loaded.records.filter((entry) =>
        entry.envelope.kind === "scheduler-state" &&
        entry.envelope.record_type === "effect-response").length, 0);
      const recovered = await setup.ledger
        .appendAutomaticRequestRecoveryTransition({
          scheduler_append: setup.schedulerAppend,
          recovery_handle: setup.recoveryHandle,
        });
      assert.equal(recovered.next_generation_id, "automatic:2");
      loaded = await setup.ledger.load();
      assert.equal(loaded.records.filter((entry) =>
        entry.envelope.kind === "scheduler-state" &&
        entry.envelope.record_type === "effect-intent").length, 1);
      assert.equal(loaded.records.filter((entry) =>
        entry.envelope.kind === "scheduler-state" &&
        entry.envelope.record_type === "effect-response").length, 1);
    });

    await t.test("a durable response returns by zero-write reread", async () => {
      const setup = await createAutomaticRecoveryTransitionContext();
      setup.fixture.failRereadAfterEffectUpdate("scheduler-state", 2);
      await assert.rejects(
        setup.ledger.appendAutomaticRequestRecoveryTransition({
          scheduler_append: setup.schedulerAppend,
          recovery_handle: setup.recoveryHandle,
        }),
        (error) => error.code === "unexpected-http-status",
      );
      const partial = await setup.ledger.load();
      assert.equal(partial.records.filter((entry) =>
        entry.envelope.kind === "scheduler-state" &&
        entry.envelope.record_type === "effect-response").length, 1);
      const writesBefore = setup.fixture.writeCalls;
      const recovered = await setup.ledger
        .appendAutomaticRequestRecoveryTransition({
          scheduler_append: setup.schedulerAppend,
          recovery_handle: setup.recoveryHandle,
        });
      assert.equal(recovered.next_generation_id, "automatic:2");
      assert.equal(setup.fixture.writeCalls, writesBefore);
    });

    await t.test(
      "a fresh factory and proof complete one durable intent without replay",
      async () => {
        const setup = await createAutomaticRecoveryTransitionContext();
        setup.fixture.failRereadAfterEffectUpdate("scheduler-state", 1);
        await assert.rejects(
          setup.ledger.appendAutomaticRequestRecoveryTransition({
            scheduler_append: setup.schedulerAppend,
            recovery_handle: setup.recoveryHandle,
          }),
          (error) => error.code === "unexpected-http-status",
        );
        const partial = await setup.ledger.load();
        const durableIntent = partial.records.find((entry) =>
          entry.envelope.kind === "scheduler-state" &&
          entry.envelope.record_type === "effect-intent");
        assert.notEqual(durableIntent, undefined);
        const intentAuthorityDigest = durableIntent.envelope.payload.action
          .recovery_authority.authority_digest;
        const { ledger: restarted, recovery: nextRecovery } =
          await createFreshAutomaticRecoveryContinuation(setup);
        const writesBefore = setup.fixture.writeCalls;
        const recovered = await restarted
          .appendAutomaticRequestRecoveryTransition({
            scheduler_append: nextRecovery.schedulerAppend,
            recovery_handle: nextRecovery.recoveryHandle,
          });
        assert.equal(recovered.next_generation_id, "automatic:2");
        assert.equal(
          recovered.intent_recovery_authority_digest,
          intentAuthorityDigest,
        );
        assert.notEqual(
          recovered.completion_recovery_authority_digest,
          intentAuthorityDigest,
          "the completion binds the independently verified fresh proof",
        );
        const loaded = await restarted.load();
        const intents = loaded.records.filter((entry) =>
          entry.envelope.kind === "scheduler-state" &&
          entry.envelope.record_type === "effect-intent");
        const responses = loaded.records.filter((entry) =>
          entry.envelope.kind === "scheduler-state" &&
          entry.envelope.record_type === "effect-response");
        assert.equal(intents.length, 1);
        assert.equal(responses.length, 1);
        const oldClosure = intents[0].envelope.payload.action
          .recovery_authority.closure_records[0];
        const freshClosure = responses[0].envelope.payload.receipt
          .completion_recovery_authority.closure_records[0];
        const semanticClosure = (record) => {
          const {
            closure_authority_digest: _authorityDigest,
            ...semantic
          } = record;
          return semantic;
        };
        assert.deepEqual(
          semanticClosure(freshClosure),
          semanticClosure(oldClosure),
        );
        assert.notEqual(
          freshClosure.closure_authority_digest,
          oldClosure.closure_authority_digest,
          "a fresh exact permission/snapshot proof may renew its audit digest",
        );
        assert.ok(setup.fixture.writeCalls > writesBefore);
      },
    );

    await t.test(
      "a fresh proof cannot delete or rewrite one durable semantic closure",
      async () => {
        const setup = await createAutomaticRecoveryTransitionContext();
        setup.fixture.failRereadAfterEffectUpdate("scheduler-state", 1);
        await assert.rejects(
          setup.ledger.appendAutomaticRequestRecoveryTransition({
            scheduler_append: setup.schedulerAppend,
            recovery_handle: setup.recoveryHandle,
          }),
          (error) => error.code === "unexpected-http-status",
        );
        const { ledger: restarted, recovery: changedRecovery } =
          await createFreshAutomaticRecoveryContinuation(setup, {
            addressId: "304",
          });
        const writesBefore = setup.fixture.writeCalls;
        await assert.rejects(
          restarted.appendAutomaticRequestRecoveryTransition({
            scheduler_append: changedRecovery.schedulerAppend,
            recovery_handle: changedRecovery.recoveryHandle,
          }),
          (error) =>
            error.code === "automatic-recovery-completion-proof-regressed",
        );
        assert.equal(setup.fixture.writeCalls, writesBefore);
        const loaded = await restarted.load();
        assert.equal(loaded.records.filter((entry) =>
          entry.envelope.kind === "scheduler-state" &&
          entry.envelope.record_type === "effect-intent").length, 1);
        assert.equal(loaded.records.filter((entry) =>
          entry.envelope.kind === "scheduler-state" &&
          entry.envelope.record_type === "effect-response").length, 0);
      },
    );

    await t.test("clone and consumed-handle replay are zero-write", async () => {
      const setup = await createAutomaticRecoveryTransitionContext();
      let writesBefore = setup.fixture.writeCalls;
      await assert.rejects(
        setup.ledger.appendAutomaticRequestRecoveryTransition({
          scheduler_append: setup.schedulerAppend,
          recovery_handle: structuredClone(setup.recoveryHandle),
        }),
        /opaque recovery handle/u,
      );
      assert.equal(setup.fixture.writeCalls, writesBefore);
      const resealed = Object.freeze({
        ...structuredClone(setup.recoveryHandle),
        authority_digest: `sha256:${"0".repeat(64)}`,
      });
      await assert.rejects(
        setup.ledger.appendAutomaticRequestRecoveryTransition({
          scheduler_append: setup.schedulerAppend,
          recovery_handle: resealed,
        }),
        /opaque recovery handle/u,
      );
      assert.equal(setup.fixture.writeCalls, writesBefore);
      const foreignFactory = createV2GitHubGitLedger({
        ...factoryInput(setup.fixture, setup.context.capability),
        preflightHandle: setup.context.preflight,
        verifyWorkflowProvenance: setup.context.verifier,
      });
      await assert.rejects(
        foreignFactory.appendAutomaticRequestRecoveryTransition({
          scheduler_append: setup.schedulerAppend,
          recovery_handle: setup.recoveryHandle,
        }),
        (error) => error.code === "UNTRUSTED_SCHEDULER_APPEND_HANDLE",
      );
      assert.equal(setup.fixture.writeCalls, writesBefore);
      await setup.ledger.appendAutomaticRequestRecoveryTransition({
        scheduler_append: setup.schedulerAppend,
        recovery_handle: setup.recoveryHandle,
      });
      writesBefore = setup.fixture.writeCalls;
      await assert.rejects(
        setup.ledger.appendAutomaticRequestRecoveryTransition({
          scheduler_append: setup.schedulerAppend,
          recovery_handle: setup.recoveryHandle,
        }),
        (error) => error.code === "automatic-recovery-handle-replayed",
      );
      assert.equal(setup.fixture.writeCalls, writesBefore);
    });
  });

test("initial runner authority rejects a pre-lease control boundary", async () => {
  const fixture = githubGitFixture();
  const preflight = await loadFixtureProviderPreflight();
  const capability = capabilityReceiptForTrigger(
    "pull_request_target",
    "refs/heads/main",
  );
  const verifier = provenanceVerifier({
    eventName: "pull_request_target",
    ref: "refs/heads/main",
    shaValue: BASE,
  });
  const ledger = makeLedger(fixture, capability, verifier, preflight);
  await ledger.bootstrapCapability();
  const minimalScopeHandle = await loadFixtureMinimalScope();
  const preScopeReceipt = await ledger
    .createPullRequestEventEvaluatedScopeReceipt({
      minimal_scope_handle: minimalScopeHandle,
      trigger_identity: {
        event_name: "pull_request_target",
        ref: "refs/heads/main",
        sha: BASE,
      },
    });
  const controlPlaneAuthority = await ledger.loadControlPlaneAuthority(
    preScopeReceipt.scope,
  );
  const writes = fixture.writeCalls;
  await assert.rejects(
    ledger.loadInitialRunnerStateAuthority({
      control_plane_authority: controlPlaneAuthority,
      evaluated_scope_receipt: preScopeReceipt,
      workflow_command_handle:
        await createPullRequestTargetWorkflowCommandHandle(),
    }),
    (error) => error.code === "full-discovery-receipt-required",
  );
  assert.equal(fixture.writeCalls, writes);
});

test("initial runner authority is branded, one-shot, and historically reverified", async () => {
  const fixture = githubGitFixture();
  const context = await createProtectedInitialRunnerContext(fixture);
  const {
    ledger,
    lease,
    initialAuthority,
    evaluatedScopeReceipt,
    workflowCommandHandle,
    capability,
    verifier,
    preflight,
    controlPlaneAuthority,
  } = context;
  assert.equal(initialAuthority.scheduling.trigger, "initial");
  assert.equal(initialAuthority.scheduling.public_wait_supported, false);
  assert.equal(
    initialAuthority.scheduling.epoch.started_at,
    lease.acquired_at,
  );
  assert.equal(
    initialAuthority.head_ledger.observed_at,
    initialAuthority.source_authority.post_ref_receipt.server_time,
  );
  assert.equal(
    initialAuthority.source_authority.tip_commit_sha,
    controlPlaneAuthority.load.tip_commit_sha,
  );
  assert.deepEqual(
    initialAuthority.source_authority.post_ref_receipt,
    controlPlaneAuthority.load.post_ref,
  );
  assert.equal(
    initialAuthority.source_authority.same_job_source_inventory_digest,
    controlPlaneAuthority.scoped_authority.source_inventory_digest,
  );
  assert.equal(
    initialAuthority.source_authority.same_job_scoped_authority_digest,
    controlPlaneAuthority.scoped_authority.authority_digest,
  );
  assert.equal(
    assertV2GitLedgerInitialRunnerStateAuthorityHandle(initialAuthority, {
      control_plane_authority: controlPlaneAuthority,
      evaluated_scope_receipt: evaluatedScopeReceipt,
      workflow_command_handle: workflowCommandHandle,
      preflight_handle: preflight,
    }),
    initialAuthority,
  );

  const controlPlaneReceipt =
    createV2ControlPlaneReceiptFromGitLedgerAuthority(
      controlPlaneAuthority,
    );
  assert.equal(
    initialAuthority.prior_authority_digest,
    controlPlaneAuthority.scoped_authority.runner_state
      .source_authority_digest,
    "initial prior authority must equal the same-load runner source",
  );
  const {
    observed_at: initialHeadObservedAt,
    ...initialHeadFields
  } = initialAuthority.head_ledger;
  const {
    observed_at: controlHeadObservedAt,
    ...controlHeadFields
  } = controlPlaneAuthority.scoped_authority.runner_state.head_ledger;
  assert.deepEqual(
    initialHeadFields,
    controlHeadFields,
    "initial head fields must equal the same-load empty runner head",
  );
  assert.equal(
    initialHeadObservedAt,
    controlPlaneAuthority.load.post_ref.server_time,
    "initial head boundary must be the same-load post-ref receipt",
  );
  assert.equal(
    controlHeadObservedAt,
    controlPlaneAuthority.load.observed_at,
    "control runner head boundary must remain its same-load observation",
  );
  assert.deepEqual(
    controlPlaneAuthority.load.active_lease.scope,
    initialAuthority.scope,
    "initial scope must equal the same-load active lease scope",
  );
  assert.equal(
    initialAuthority.lease_authority.evaluated_scope_receipt_digest,
    controlPlaneAuthority.load.active_lease.evaluated_scope_receipt
      .receipt_digest,
    "initial lease authority must equal the same-load acquire authority",
  );
  assert.equal(
    initialAuthority.preflight_authority.preflight_receipt_digest,
    preflight.receipt_digest,
    "initial authority must retain the exact live preflight receipt",
  );
  assert.equal(
    initialAuthority.preflight_authority.configuration_digest,
    preflight.configuration_digest,
    "initial authority must retain the exact live preflight configuration",
  );
  assert.throws(
    () => createV2ProductionRunnerAuthority({
      preflight_handle: preflight,
      control_plane_receipt: controlPlaneReceipt,
      expected_scope: initialAuthority.scope,
    }),
    (error) => error.code === "INITIAL_RUNNER_STATE_AUTHORITY_REQUIRED",
  );
  assert.throws(
    () => createV2ProductionRunnerAuthority({
      preflight_handle: preflight,
      control_plane_receipt: controlPlaneReceipt,
      established_runner_state_authority: {},
      expected_scope: initialAuthority.scope,
    }),
    (error) => error.code === "ESTABLISHED_RUNNER_STATE_AUTHORITY_UNEXPECTED",
  );
  const productionAuthority = createV2ProductionRunnerAuthority({
    preflight_handle: preflight,
    control_plane_receipt: controlPlaneReceipt,
    initial_runner_state_authority: initialAuthority,
    expected_scope: initialAuthority.scope,
  });
  assert.equal(
    assertV2ProductionRunnerAuthorityHandle(productionAuthority),
    productionAuthority,
  );
  assert.equal(
    productionAuthority.effect_barrier,
    "scheduler-observation-required",
  );
  assert.deepEqual(productionAuthority.scheduling, initialAuthority.scheduling);
  assert.deepEqual(productionAuthority.head_ledger, initialAuthority.head_ledger);
  assert.equal(productionAuthority.runner_state.scheduling, null);
  assert.equal(
    productionAuthority.runner_state.runner_state_digest,
    controlPlaneAuthority.scoped_authority.runner_state.runner_state_digest,
  );
  assert.equal(
    productionAuthority.control_plane_binding.source_authority_digest,
    controlPlaneAuthority.scoped_authority.authority_digest,
  );
  assert.equal(
    productionAuthority.control_plane_binding.source_binding_digest,
    controlPlaneAuthority.binding_digest,
  );
  assert.equal(
    productionAuthority.source_binding_digest.startsWith("sha256:"),
    true,
  );
  assert.throws(
    () => assertV2ProductionRunnerAuthorityHandle(
      structuredClone(productionAuthority),
    ),
    (error) =>
      error.code === "UNTRUSTED_PRODUCTION_RUNNER_AUTHORITY_HANDLE",
  );
  assert.throws(
    () => createV2ProductionRunnerAuthority({
      preflight_handle: preflight,
      control_plane_receipt: structuredClone(controlPlaneReceipt),
      initial_runner_state_authority: initialAuthority,
      expected_scope: initialAuthority.scope,
    }),
    (error) => error.code === "UNTRUSTED_CONTROL_PLANE_RECEIPT_HANDLE",
  );
  await assert.rejects(
    ledger.loadInitialRunnerStateAuthority({
      control_plane_authority: structuredClone(controlPlaneAuthority),
      evaluated_scope_receipt: evaluatedScopeReceipt,
      workflow_command_handle: workflowCommandHandle,
    }),
    (error) => error.code === "UNTRUSTED_CONTROL_PLANE_AUTHORITY_HANDLE",
  );
  const wrongScopeControl = await ledger.loadControlPlaneAuthority({
    ...structuredClone(initialAuthority.scope),
    head_ref_oid: "9".repeat(40),
  });
  await assert.rejects(
    ledger.loadInitialRunnerStateAuthority({
      control_plane_authority: wrongScopeControl,
      evaluated_scope_receipt: evaluatedScopeReceipt,
      workflow_command_handle: workflowCommandHandle,
    }),
    (error) => error.code === "CONTROL_PLANE_AUTHORITY_SCOPE_MISMATCH",
  );
  assert.throws(
    () => assertV2GitLedgerInitialRunnerStateAuthorityHandle(
      initialAuthority,
      { control_plane_authority: wrongScopeControl },
    ),
    (error) =>
      error.code === "INITIAL_RUNNER_STATE_AUTHORITY_BINDING_MISMATCH",
  );
  const wrongScopeControlReceipt =
    createV2ControlPlaneReceiptFromGitLedgerAuthority(wrongScopeControl);
  assert.throws(
    () => createV2ProductionRunnerAuthority({
      preflight_handle: preflight,
      control_plane_receipt: wrongScopeControlReceipt,
      initial_runner_state_authority: initialAuthority,
      expected_scope: wrongScopeControl.scope,
    }),
    (error) => error.code === "INITIAL_RUNNER_STATE_AUTHORITY_UNTRUSTED",
  );
  const foreignFactory = createV2GitHubGitLedger({
    ...factoryInput(fixture, capability),
    preflightHandle: preflight,
    verifyWorkflowProvenance: verifier,
  });
  await assert.rejects(
    foreignFactory.loadInitialRunnerStateAuthority({
      control_plane_authority: controlPlaneAuthority,
      evaluated_scope_receipt: evaluatedScopeReceipt,
      workflow_command_handle: workflowCommandHandle,
    }),
    (error) => error.code === "UNTRUSTED_CONTROL_PLANE_AUTHORITY_HANDLE",
  );
  const foreignControlPlaneAuthority =
    await foreignFactory.loadControlPlaneAuthority(initialAuthority.scope);
  const foreignControlPlaneReceipt =
    createV2ControlPlaneReceiptFromGitLedgerAuthority(
      foreignControlPlaneAuthority,
    );
  assert.throws(
    () => createV2ProductionRunnerAuthority({
      preflight_handle: preflight,
      control_plane_receipt: foreignControlPlaneReceipt,
      initial_runner_state_authority: initialAuthority,
      expected_scope: initialAuthority.scope,
    }),
    (error) => error.code === "INITIAL_RUNNER_STATE_AUTHORITY_UNTRUSTED",
  );

  const clone = structuredClone(initialAuthority);
  assert.throws(
    () => assertV2GitLedgerInitialRunnerStateAuthorityHandle(clone),
    (error) =>
      error.code === "UNTRUSTED_INITIAL_RUNNER_STATE_AUTHORITY_HANDLE",
  );
  const resealedWithoutDigest = structuredClone(initialAuthority);
  delete resealedWithoutDigest.authority_digest;
  resealedWithoutDigest.source_authority.same_job_source_inventory_digest =
    `sha256:${"1".repeat(64)}`;
  const resealed = {
    ...resealedWithoutDigest,
    authority_digest: digestCanonical(
      "codex-review-gate-v2-git-ledger-initial-runner-state-authority",
      resealedWithoutDigest,
    ),
  };
  assert.equal(
    validateV2GitLedgerInitialRunnerStateAuthority(resealed).authority_digest,
    resealed.authority_digest,
    "structural resealing alone is not append authority",
  );
  assert.throws(
    () => assertV2GitLedgerInitialRunnerStateAuthorityHandle(resealed),
    (error) =>
      error.code === "UNTRUSTED_INITIAL_RUNNER_STATE_AUTHORITY_HANDLE",
  );
  assert.throws(
    () => createV2ProductionRunnerAuthority({
      preflight_handle: preflight,
      control_plane_receipt: controlPlaneReceipt,
      initial_runner_state_authority: resealed,
      expected_scope: initialAuthority.scope,
    }),
    (error) => error.code === "INITIAL_RUNNER_STATE_AUTHORITY_UNTRUSTED",
  );

  const beforeObservation = await ledger.load();
  const plan = schedulerObservationRecord({
    predecessor: lease.acquire_commit_sha,
    lease,
    priorAuthorityDigest: priorRunnerAuthorityDigest(beforeObservation.records),
    initialAuthority,
  }).payload.action;
  const writesBeforeAdmission = fixture.writeCalls;
  await assert.rejects(
    ledger.appendInitialSchedulerObservation({
      initial_runner_state_authority: clone,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: plan.status_plan,
    }),
    (error) =>
      error.code === "UNTRUSTED_INITIAL_RUNNER_STATE_AUTHORITY_HANDLE",
  );
  assert.equal(fixture.writeCalls, writesBeforeAdmission);

  await assert.rejects(
    ledger.appendInitialSchedulerObservation({
      initial_runner_state_authority: initialAuthority,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: {
        ...structuredClone(plan.status_plan),
        decision: "clean",
      },
    }),
    /runner status plan is not exact closed output/u,
  );
  assert.equal(fixture.writeCalls, writesBeforeAdmission);

  const appended = await ledger.appendInitialSchedulerObservation({
    initial_runner_state_authority: initialAuthority,
    scheduler_evaluation: plan.scheduler_evaluation,
    status_plan: plan.status_plan,
  });
  assert.equal(appended.record.kind, "scheduler-observation");
  assert.deepEqual(
    appended.record.payload.action.initial_runner_state_authority,
    initialAuthority,
  );

  const writesAfterAppend = fixture.writeCalls;
  await assert.rejects(
    ledger.appendInitialSchedulerObservation({
      initial_runner_state_authority: initialAuthority,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: plan.status_plan,
    }),
    (error) => error.code === "initial-runner-authority-replayed",
  );
  assert.equal(fixture.writeCalls, writesAfterAppend);
  await assert.rejects(
    ledger.loadInitialRunnerStateAuthority({
      control_plane_authority: controlPlaneAuthority,
      evaluated_scope_receipt: evaluatedScopeReceipt,
      workflow_command_handle: workflowCommandHandle,
    }),
    (error) => error.code === "STALE_CONTROL_PLANE_AUTHORITY",
  );
  const postObservationControl = await ledger.loadControlPlaneAuthority(
    initialAuthority.scope,
  );
  const postObservationReceipt =
    createV2ControlPlaneReceiptFromGitLedgerAuthority(
      postObservationControl,
    );
  assert.throws(
    () => createV2ProductionRunnerAuthority({
      preflight_handle: preflight,
      control_plane_receipt: postObservationReceipt,
      initial_runner_state_authority: initialAuthority,
      expected_scope: initialAuthority.scope,
    }),
    (error) => error.code === "INITIAL_RUNNER_STATE_AUTHORITY_UNEXPECTED",
  );
  assert.throws(
    () => createV2ProductionRunnerAuthority({
      preflight_handle: preflight,
      control_plane_receipt: postObservationReceipt,
      expected_scope: initialAuthority.scope,
    }),
    (error) => error.code === "ESTABLISHED_RUNNER_STATE_AUTHORITY_REQUIRED",
  );
  const establishedAuthority =
    await ledger.loadEstablishedRunnerStateAuthority({
      control_plane_authority: postObservationControl,
      evaluated_scope_receipt: evaluatedScopeReceipt,
      workflow_command_handle: workflowCommandHandle,
    });
  const readyAuthority = createV2ProductionRunnerAuthority({
    preflight_handle: preflight,
    control_plane_receipt: postObservationReceipt,
    established_runner_state_authority: establishedAuthority,
    expected_scope: initialAuthority.scope,
  });
  assert.equal(
    readyAuthority.effect_barrier,
    "scheduler-observation-required",
  );
  assert.deepEqual(
    readyAuthority.scheduling,
    establishedAuthority.scheduling,
  );
  assert.deepEqual(
    readyAuthority.head_ledger,
    establishedAuthority.head_ledger,
  );
  assert.notEqual(readyAuthority.runner_state.scheduling, null);
  assert.equal(
    readyAuthority.runner_state.runner_state_digest,
    postObservationControl.scoped_authority.runner_state.runner_state_digest,
  );
  assert.throws(
    () => createV2ProductionRunnerAuthority({
      preflight_handle: preflight,
      control_plane_receipt: postObservationReceipt,
      initial_runner_state_authority: initialAuthority,
      established_runner_state_authority: establishedAuthority,
      expected_scope: initialAuthority.scope,
    }),
    (error) => error.code === "INITIAL_RUNNER_STATE_AUTHORITY_UNEXPECTED",
  );
  assert.throws(
    () => createV2ProductionRunnerAuthority({
      preflight_handle: preflight,
      control_plane_receipt: postObservationReceipt,
      established_runner_state_authority:
        structuredClone(establishedAuthority),
      expected_scope: initialAuthority.scope,
    }),
    (error) =>
      error.code === "ESTABLISHED_RUNNER_STATE_AUTHORITY_UNTRUSTED",
  );
  await assert.rejects(
    ledger.loadInitialRunnerStateAuthority({
      control_plane_authority: postObservationControl,
      evaluated_scope_receipt: evaluatedScopeReceipt,
      workflow_command_handle: workflowCommandHandle,
    }),
    (error) => error.code === "initial-runner-history-exists",
  );

  const restarted = createV2GitHubGitLedger({
    ...factoryInput(fixture, capability),
    preflightHandle: preflight,
    verifyWorkflowProvenance: verifier,
  });
  const historical = await restarted.load();
  const historicalAuthority = deriveV2GitLedgerAuthority(
    historical.records,
    initialAuthority.scope,
    historical.observed_at,
  );
  assert.equal(
    historicalAuthority.runner_state.scheduling.trigger,
    "initial",
  );
  assert.equal(
    historicalAuthority.runner_state.observation_history[0].record_oid,
    appended.append_receipt.commit_sha,
  );
});

test("established runner authority preserves history and binds each current run", async () => {
  const fixture = githubGitFixture();
  const runIdentity = {
    run_id: OWNER.run_id,
    run_attempt: OWNER.run_attempt,
    actor_id: OWNER.actor_id,
  };
  const context = await createProtectedInitialRunnerContext(fixture, {
    runIdentity,
  });
  const {
    ledger,
    initialAuthority,
    controlPlaneAuthority,
    evaluatedScopeReceipt,
    workflowCommandHandle,
    discovery,
    capability,
    preflight,
  } = context;
  await assert.rejects(
    ledger.loadEstablishedRunnerStateAuthority({
      control_plane_authority: controlPlaneAuthority,
      evaluated_scope_receipt: evaluatedScopeReceipt,
      workflow_command_handle: workflowCommandHandle,
    }),
    (error) => error.code === "established-runner-history-required",
  );

  const seed = schedulerObservationRecord({
    predecessor: context.lease.acquire_commit_sha,
    lease: context.lease,
    priorAuthorityDigest: initialAuthority.prior_authority_digest,
    initialAuthority,
  }).payload.action;
  const firstAppend = await ledger.appendInitialSchedulerObservation({
    initial_runner_state_authority: initialAuthority,
    scheduler_evaluation: seed.scheduler_evaluation,
    status_plan: seed.status_plan,
  });
  const controlled = await appendControlledRequestAfterObservation(
    ledger,
    context.lease,
    {
      observationRecord: firstAppend.record,
      observationReceipt: firstAppend.append_receipt,
    },
  );
  await releaseFixtureDiscoveryLease(ledger, discovery);

  runIdentity.run_attempt = 2;
  const sameRun = await acquireEstablishedRunnerContext({
    ledger,
    runIdentity,
    observationBoundary: "public-initial-wait-complete",
    transportStartSecond: 3600,
    minimalServerTime: "2026-08-13T14:00:00.000Z",
  });
  const established = await ledger.loadEstablishedRunnerStateAuthority({
    control_plane_authority: sameRun.controlPlaneAuthority,
    evaluated_scope_receipt: sameRun.evaluatedScopeReceipt,
    workflow_command_handle: sameRun.workflowCommandHandle,
  });
  assert.equal(
    established.schema,
    V2_GIT_LEDGER_ESTABLISHED_RUNNER_STATE_AUTHORITY_SCHEMA,
  );
  assert.deepEqual(established.scheduling.run_identity, {
    run_id: OWNER.run_id,
    run_attempt: 2,
  });
  assert.equal(established.scheduling.trigger, "timer");
  assert.equal(
    established.scheduling.epoch.started_at,
    initialAuthority.scheduling.epoch.started_at,
  );
  assert.equal(
    established.scheduling.epoch.controlled_request.bound_at,
    controlled.boundAt,
    "a new execution must not reset the request-bound six-hour origin",
  );
  assert.equal(established.scheduling.complete_snapshots.length, 1);
  assert.equal(
    established.prior_authority_digest,
    sameRun.controlPlaneAuthority.scoped_authority.runner_state
      .source_authority_digest,
  );
  assert.equal(
    established.head_ledger.observed_at,
    sameRun.controlPlaneAuthority.load.post_ref.server_time,
  );
  assert.deepEqual(
    validateV2GitLedgerEstablishedRunnerStateAuthority(established),
    established,
  );
  assert.equal(
    assertV2GitLedgerEstablishedRunnerStateAuthorityHandle(established, {
      control_plane_authority: sameRun.controlPlaneAuthority,
      evaluated_scope_receipt: sameRun.evaluatedScopeReceipt,
      workflow_command_handle: sameRun.workflowCommandHandle,
      preflight_handle: preflight,
    }),
    established,
  );
  const sameRunControlReceipt =
    createV2ControlPlaneReceiptFromGitLedgerAuthority(
      sameRun.controlPlaneAuthority,
    );
  assert.throws(
    () => createV2ProductionRunnerAuthority({
      preflight_handle: preflight,
      control_plane_receipt: sameRunControlReceipt,
      expected_scope: established.scope,
    }),
    (error) => error.code === "ESTABLISHED_RUNNER_STATE_AUTHORITY_REQUIRED",
  );
  const establishedProductionAuthority = createV2ProductionRunnerAuthority({
    preflight_handle: preflight,
    control_plane_receipt: sameRunControlReceipt,
    established_runner_state_authority: established,
    expected_scope: established.scope,
  });
  assert.equal(
    establishedProductionAuthority.effect_barrier,
    "scheduler-observation-required",
  );
  assert.deepEqual(
    establishedProductionAuthority.scheduling,
    established.scheduling,
  );
  assert.deepEqual(
    establishedProductionAuthority.head_ledger,
    established.head_ledger,
  );
  assert.equal(
    establishedProductionAuthority.runner_state.runner_state_digest,
    sameRun.controlPlaneAuthority.scoped_authority.runner_state
      .runner_state_digest,
  );
  assert.notDeepEqual(
    establishedProductionAuthority.scheduling.run_identity,
    establishedProductionAuthority.runner_state.scheduling.run_identity,
    "execution authority updates only the current run identity",
  );
  assert.equal(
    establishedProductionAuthority.scheduling.epoch.started_at,
    establishedProductionAuthority.runner_state.scheduling.epoch.started_at,
    "execution projection must preserve the protected epoch origin",
  );
  assert.equal(
    establishedProductionAuthority.scheduling.epoch.controlled_request.bound_at,
    establishedProductionAuthority.runner_state.scheduling.epoch
      .controlled_request.bound_at,
    "execution projection must preserve the protected request deadline origin",
  );
  assert.throws(
    () => createV2ProductionRunnerAuthority({
      preflight_handle: preflight,
      control_plane_receipt: sameRunControlReceipt,
      initial_runner_state_authority: initialAuthority,
      established_runner_state_authority: established,
      expected_scope: established.scope,
    }),
    (error) => error.code === "INITIAL_RUNNER_STATE_AUTHORITY_UNEXPECTED",
  );
  const clone = structuredClone(established);
  assert.throws(
    () => assertV2GitLedgerEstablishedRunnerStateAuthorityHandle(clone),
    (error) =>
      error.code === "UNTRUSTED_ESTABLISHED_RUNNER_STATE_AUTHORITY_HANDLE",
  );
  assert.throws(
    () => createV2ProductionRunnerAuthority({
      preflight_handle: preflight,
      control_plane_receipt: sameRunControlReceipt,
      established_runner_state_authority: clone,
      expected_scope: established.scope,
    }),
    (error) =>
      error.code === "ESTABLISHED_RUNNER_STATE_AUTHORITY_UNTRUSTED",
  );
  const inputs = schedulerInputsForEstablishedAuthority(established, {
    snapshotId: "snapshot-established-attempt-2",
  });
  const writesBeforeForgery = fixture.writeCalls;
  await assert.rejects(
    ledger.appendEstablishedSchedulerObservation({
      established_runner_state_authority: clone,
      ...inputs,
    }),
    (error) =>
      error.code === "UNTRUSTED_ESTABLISHED_RUNNER_STATE_AUTHORITY_HANDLE",
  );
  await assert.rejects(
    ledger.appendEstablishedSchedulerObservation({
      established_runner_state_authority: established,
      scheduler_evaluation: inputs.scheduler_evaluation,
      status_plan: { ...inputs.status_plan, decision: "clean" },
    }),
    /runner status plan is not exact closed output/u,
  );
  assert.equal(fixture.writeCalls, writesBeforeForgery);
  const secondAppend = await ledger.appendEstablishedSchedulerObservation({
    established_runner_state_authority: established,
    ...inputs,
  });
  assert.equal(secondAppend.record.kind, "scheduler-observation");
  assert.equal(
    secondAppend.record.payload.action.initial_runner_state_authority,
    null,
  );
  const writesAfterAppend = fixture.writeCalls;
  await assert.rejects(
    ledger.appendEstablishedSchedulerObservation({
      established_runner_state_authority: established,
      ...inputs,
    }),
    (error) => error.code === "established-runner-authority-replayed",
  );
  assert.equal(fixture.writeCalls, writesAfterAppend);
  await releaseFixtureDiscoveryLease(ledger, sameRun.discovery);

  runIdentity.run_id = "9002";
  runIdentity.run_attempt = 1;
  const nextRun = await acquireEstablishedRunnerContext({
    ledger,
    runIdentity,
    observationBoundary: "public-post-request-wait-complete",
    transportStartSecond: 7200,
    minimalServerTime: "2026-08-13T16:00:00.000Z",
  });
  const nextEstablished = await ledger.loadEstablishedRunnerStateAuthority({
    control_plane_authority: nextRun.controlPlaneAuthority,
    evaluated_scope_receipt: nextRun.evaluatedScopeReceipt,
    workflow_command_handle: nextRun.workflowCommandHandle,
  });
  const nextRunControlReceipt =
    createV2ControlPlaneReceiptFromGitLedgerAuthority(
      nextRun.controlPlaneAuthority,
    );
  assert.throws(
    () => createV2ProductionRunnerAuthority({
      preflight_handle: preflight,
      control_plane_receipt: nextRunControlReceipt,
      established_runner_state_authority: established,
      expected_scope: nextEstablished.scope,
    }),
    (error) =>
      error.code === "ESTABLISHED_RUNNER_STATE_AUTHORITY_UNTRUSTED",
    "an established handle cannot cross a same-factory control load",
  );
  const nextRunProductionAuthority = createV2ProductionRunnerAuthority({
    preflight_handle: preflight,
    control_plane_receipt: nextRunControlReceipt,
    established_runner_state_authority: nextEstablished,
    expected_scope: nextEstablished.scope,
  });
  assert.deepEqual(
    nextRunProductionAuthority.scheduling.run_identity,
    { run_id: "9002", run_attempt: 1 },
  );
  assert.deepEqual(nextEstablished.scheduling.run_identity, {
    run_id: "9002",
    run_attempt: 1,
  });
  assert.equal(
    nextEstablished.scheduling.epoch.started_at,
    initialAuthority.scheduling.epoch.started_at,
  );
  assert.equal(
    nextEstablished.scheduling.epoch.controlled_request.bound_at,
    controlled.boundAt,
  );
  assert.equal(nextEstablished.scheduling.complete_snapshots.length, 2);
  await assert.rejects(
    ledger.loadEstablishedRunnerStateAuthority({
      control_plane_authority: nextRun.controlPlaneAuthority,
      evaluated_scope_receipt: nextRun.preScopeReceipt,
      workflow_command_handle: nextRun.workflowCommandHandle,
    }),
    (error) => error.code === "full-discovery-receipt-required",
  );
  const foreignFactory = createV2GitHubGitLedger({
    ...factoryInput(fixture, capability),
    preflightHandle: preflight,
    verifyWorkflowProvenance: provenanceVerifier({
      eventName: "pull_request_target",
      ref: "refs/heads/main",
      shaValue: BASE,
      runIdentity,
    }),
  });
  await assert.rejects(
    foreignFactory.loadEstablishedRunnerStateAuthority({
      control_plane_authority: nextRun.controlPlaneAuthority,
      evaluated_scope_receipt: nextRun.evaluatedScopeReceipt,
      workflow_command_handle: nextRun.workflowCommandHandle,
    }),
    (error) => error.code === "UNTRUSTED_CONTROL_PLANE_AUTHORITY_HANDLE",
  );
  const nextInputs = schedulerInputsForEstablishedAuthority(nextEstablished, {
    snapshotId: "snapshot-established-run-9002",
  });
  await releaseFixtureDiscoveryLease(ledger, nextRun.discovery);
  const writesAfterStaleFence = fixture.writeCalls;
  await assert.rejects(
    ledger.appendEstablishedSchedulerObservation({
      established_runner_state_authority: nextEstablished,
      ...nextInputs,
    }),
    (error) => new Set(["stale-predecessor", "lease-required"])
      .has(error.code),
  );
  assert.equal(fixture.writeCalls, writesAfterStaleFence);
});

test("scheduler observation run identity must equal its protected run authority", async () => {
  const fixture = githubGitFixture();
  const context = await createProtectedInitialRunnerContext(fixture);
  const {
    ledger, lease, initialAuthority,
  } = context;
  const loaded = await ledger.load();
  const valid = schedulerObservationRecord({
    predecessor: lease.acquire_commit_sha,
    lease,
    priorAuthorityDigest: priorRunnerAuthorityDigest(loaded.records),
    initialAuthority,
  });
  const action = structuredClone(valid.payload.action);
  action.scheduler_evaluation.run_id = "9002";
  const writes = fixture.writeCalls;
  await assert.rejects(
    ledger.appendInitialSchedulerObservation({
      initial_runner_state_authority: initialAuthority,
      scheduler_evaluation: action.scheduler_evaluation,
      status_plan: action.status_plan,
    }),
    (error) => error.code === "initial-scheduler-evaluation-mismatch",
  );
  assert.equal(fixture.writeCalls, writes);
});

test("scheduler history rejects a canonical wait completion without a trusted producer", async () => {
  const fixture = githubGitFixture();
  const runIdentity = structuredClone(OWNER);
  const preflight = await loadFixtureProviderPreflight({
    visibility: "public",
  });
  const context = await createProtectedInitialRunnerContext(fixture, {
    runIdentity,
    preflight,
  });
  const firstPlan = schedulerObservationRecord({
    predecessor: context.lease.acquire_commit_sha,
    lease: context.lease,
    priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
    initialAuthority: context.initialAuthority,
  }).payload.action;
  const firstAppend = await context.ledger.appendInitialSchedulerObservation({
    initial_runner_state_authority: context.initialAuthority,
    scheduler_evaluation: firstPlan.scheduler_evaluation,
    status_plan: firstPlan.status_plan,
  });
  await releaseFixtureDiscoveryLease(context.ledger, context.discovery);

  fixture.advanceServerTime(3600);
  runIdentity.run_attempt = 2;
  const establishedContext = await acquireEstablishedRunnerContext({
    ledger: context.ledger,
    runIdentity,
    observationBoundary: "public-initial-wait-complete",
    transportStartSecond: 7200,
    minimalServerTime: "2026-08-13T16:00:00.000Z",
  });
  const established = await context.ledger.loadEstablishedRunnerStateAuthority({
    control_plane_authority: establishedContext.controlPlaneAuthority,
    evaluated_scope_receipt: establishedContext.evaluatedScopeReceipt,
    workflow_command_handle: establishedContext.workflowCommandHandle,
  });
  const loaded = await context.ledger.load();
  const runnerState = deriveV2GitLedgerRunnerState(
    loaded.records,
    established.scope,
    loaded.post_ref.server_time,
  );
  const priorScheduling = structuredClone(runnerState.scheduling);
  priorScheduling.trigger = established.scheduling.trigger;
  priorScheduling.run_identity = structuredClone(
    established.scheduling.run_identity,
  );
  const deadline = new Date(
    Date.parse(priorScheduling.epoch.started_at) + 15 * 60 * 1000,
  ).toISOString();
  const topology = established.preflight_authority.public_wait_topology
    .environments.find((entry) => entry.stage === "initial");
  assert.notEqual(topology, undefined);
  const completionWithoutDigest = {
    schema: "codex-review-gate-public-wait-completion-v2",
    schema_version: 1,
    repository: { owner: REPOSITORY.owner, name: REPOSITORY.name },
    pull_request: { number: PR.number },
    head_ref_oid: HEAD,
    epoch_id: priorScheduling.epoch.id,
    generation_index: 1,
    stage: "public-initial",
    deadline,
    completed_at: deadline,
    environment: {
      name: topology.name,
      id: topology.id,
      wait_timer_rule_id: topology.wait_timer_rule_id,
      wait_timer_minutes: topology.wait_timer_minutes,
    },
    trusted_workflow: {
      repository: "Joey-Tools/codex-review-gate-action",
      path: ".github/workflows/codex-review-gate.yml",
      sha: BASE,
    },
    run: {
      run_id: established.scheduling.run_identity.run_id,
      run_attempt: established.scheduling.run_identity.run_attempt,
      wait_job_id: "public_initial_wait",
      continuation_job_id: "controller",
    },
    source_observation_record_oid: firstAppend.append_receipt.commit_sha,
    stage_nonce: "d".repeat(64),
  };
  priorScheduling.wait_completions = [{
    ...completionWithoutDigest,
    receipt_digest: digestCanonical(
      "codex-review-gate-v2-public-wait-completion",
      completionWithoutDigest,
    ),
  }];
  const observedAt = established.source_authority.post_ref_receipt.server_time;
  const evaluation = {
    epoch_id: priorScheduling.epoch.id,
    decision: "pending",
    complete: true,
    snapshot_id: "snapshot-forged-wait-completion",
    snapshot_fingerprint: `sha256:${"4".repeat(64)}`,
    observed_at: observedAt,
    provider_activity_fingerprint: `sha256:${"5".repeat(64)}`,
    no_start_candidate: structuredClone(priorScheduling.no_start_candidate),
    run_id: priorScheduling.run_identity.run_id,
    run_attempt: priorScheduling.run_identity.run_attempt,
  };
  const schedulerPlan = planV2Actions({
    schema: V2_SCHEDULER_SCHEMA,
    schema_version: V2_SCHEDULER_SCHEMA_VERSION,
    trigger: priorScheduling.trigger,
    now: observedAt,
    public_wait_supported: priorScheduling.public_wait_supported,
    status_target_mode: priorScheduling.status_target_mode,
    epoch: priorScheduling.epoch,
    evaluation,
    complete_snapshots: priorScheduling.complete_snapshots,
    status: {
      exact_sha_context_count:
        priorScheduling.status.exact_sha_context_count,
      latest_idempotency_key: priorScheduling.status.latest_idempotency_key,
    },
    applied_action_keys: priorScheduling.applied_action_keys,
    wait_completions: priorScheduling.wait_completions,
  });
  const statusPlan = buildV2StatusPlan({
    decision: "pending",
    status_target_mode: priorScheduling.status_target_mode,
    status_context: "codex/github-review-gate",
    target: {
      mode: priorScheduling.status_target_mode,
      validated: true,
      terminal_sha: POTENTIAL,
      head_sentinel_sha: HEAD,
      head_sentinel_receipt: priorScheduling.status.head_sentinel_receipt,
    },
    snapshot_fingerprint: evaluation.snapshot_fingerprint,
  });
  const action = {
    schema: "codex-review-gate-git-ledger-scheduler-observation-v2",
    schema_version: 1,
    prior_authority_digest: established.prior_authority_digest,
    prior_scheduling: priorScheduling,
    prior_head_ledger: structuredClone(established.head_ledger),
    scheduler_evaluation: evaluation,
    scheduler_plan: schedulerPlan,
    scheduler_plan_digest: digestCanonical(
      "codex-review-gate-v2-scheduler-plan",
      schedulerPlan,
    ),
    status_plan: statusPlan,
    status_plan_digest: digestCanonical(
      "codex-review-gate-v2-status-plan",
      statusPlan,
    ),
    evaluated_scope_receipt_digest:
      established.evaluated_scope_authority
        .record_evaluated_scope_receipt_digest,
    snapshot_server_time: observedAt,
    initial_runner_state_authority: null,
  };
  const forged = createV2GitLedgerEffectIntentRecord({
    predecessor_commit_sha: established.source_authority.tip_commit_sha,
    scope: established.scope,
    kind: "scheduler-observation",
    effect_id: "scheduler-observation:forged-wait-completion",
    idempotency_key: "scheduler-observation:forged-wait-completion",
    server_observed_at: observedAt,
    action,
    control_comment_binding: null,
    lease_receipt: established.lease_authority,
  });
  const writesBefore = fixture.writeCalls;
  await assert.rejects(
    appendHistoricalEffectRecord(
      context.ledger,
      forged,
      establishedContext.evaluatedScopeReceipt,
    ),
    (error) =>
      error.code === "public-wait-completion-producer-unavailable",
  );
  assert.equal(fixture.writeCalls, writesBefore);
});

test("historical authority projection preserves ordered full records and request intent binding", async () => {
  const fixture = githubGitFixture();
  const context = await createProtectedInitialRunnerContext(fixture);
  const { ledger, lease } = context;
  const prefix = await appendProtectedRequestPrefix(ledger, lease, context);
  const { generation } = prefix;
  const intentRecord = reviewRequestRecord({
    predecessor: prefix.attemptReceipt.commit_sha,
    lease,
    effectId: "projection-request",
    idempotencyKey: "projection-request-key",
    at: TIME,
    generation,
    schedulerObservationRecordOid: prefix.observationReceipt.commit_sha,
    reservationRecordOid: prefix.reservationReceipt.commit_sha,
    attemptRecordOid: prefix.attemptReceipt.commit_sha,
    schedulerActionKey: prefix.schedulerActionKey,
  });
  const intent = await appendHistoricalEffectRecord(ledger, intentRecord);
  const reviewResponseReceipt =
    await automaticReviewRequestBindingReceipt({
      action: intentRecord.payload.action,
      observedAt: intent.ref_reread.server_time,
    });
  const responseRecord = createV2GitLedgerRecord({
    ...recordPlan(intentRecord),
    record_type: "effect-response",
    predecessor_commit_sha: intent.commit_sha,
    server_observed_at:
      reviewResponseReceipt.request_scope_receipt.post_scope.observed_at,
    payload: effectPayload({
      phase: "response",
      kind: "review-request",
      predecessor: intent.commit_sha,
      generation,
      intentCommitSha: intent.commit_sha,
      action: intentRecord.payload.action,
      receipt: reviewResponseReceipt,
    }),
  });
  const response = await appendHistoricalEffectRecord(ledger, responseRecord);
  const loaded = await ledger.load();
  const authority = deriveV2GitLedgerAuthority(loaded.records, {
    pull_request: PR,
    head_ref_oid: HEAD,
    base_ref_oid: BASE,
    potential_merge_commit_oid: POTENTIAL,
  });
  assert.equal(authority.ordered_records.length, loaded.commit_count);
  assert.equal(authority.ordered_records.at(-1).commit_sha, response.commit_sha);
  const responseFact = authority.authority_facts.find((entry) =>
    entry.record_oid === response.commit_sha);
  assert.equal(responseFact.payload.intent_commit_sha, intent.commit_sha);
  assert.equal(responseFact.payload.generation.generation_id, "automatic:1");
  assert.equal(authority.scope_counters[0].automatic_requests_on_head, 1);
  assert.equal(authority.runner_state.head_ledger.automatic_request_count, 1);
  assert.equal(authority.runner_state.reservations.length, 1);
  assert.equal(authority.runner_state.reservations[0].reservation_status_bound,
    true);
  assert.equal(authority.runner_state.effect_attempts.length, 1);
  assert.equal(authority.runner_state.effect_attempts[0].bound, false);
  assert.equal(
    authority.runner_state.scheduling.epoch.automatic_request.state,
    "effect-attempted",
  );
  assert.equal(authority.runner_state.observation_history[0].run_identity.run_id,
    OWNER.run_id);
  assert.match(authority.source_inventory_digest, /^sha256:/u);
  assert.match(authority.authority_digest, /^sha256:/u);
  const closureAuthority = await ledger.loadControlPlaneAuthority({
    pull_request: PR,
    head_ref_oid: HEAD,
    base_ref_oid: BASE,
    potential_merge_commit_oid: POTENTIAL,
  });
  assert.equal(closureAuthority.schema,
    V2_GIT_LEDGER_CONTROL_PLANE_AUTHORITY_SCHEMA);
  assert.equal(closureAuthority.load.stable, true);
  assert.equal(closureAuthority.scoped_authority.scope.head_ref_oid, HEAD);
  assert.match(closureAuthority.binding_digest, /^sha256:/u);
  assert.deepEqual(
    validateV2GitLedgerControlPlaneAuthority(closureAuthority, {
      pull_request: PR,
      head_ref_oid: HEAD,
      base_ref_oid: BASE,
      potential_merge_commit_oid: POTENTIAL,
    }),
    closureAuthority,
  );
  assert.equal(
    assertV2GitLedgerControlPlaneAuthorityHandle(closureAuthority, {
      pull_request: PR,
      head_ref_oid: HEAD,
      base_ref_oid: BASE,
      potential_merge_commit_oid: POTENTIAL,
    }),
    closureAuthority,
  );
  const reconstructed = structuredClone(closureAuthority);
  assert.throws(
    () => assertV2GitLedgerControlPlaneAuthorityHandle(reconstructed),
    (error) =>
      error.code === "UNTRUSTED_CONTROL_PLANE_AUTHORITY_HANDLE",
  );
  const forged = structuredClone(closureAuthority);
  forged.scoped_authority.scope.head_ref_oid = "f".repeat(40);
  assert.throws(
    () => validateV2GitLedgerControlPlaneAuthority(forged),
    /not derived/u,
  );
});

test("closure-bound control-plane receipt derives an empty effect authority", async () => {
  const fixture = githubGitFixture();
  const ledger = makeLedger(fixture);
  await ledger.bootstrapCapability();
  const receipt = await ledger.loadControlPlaneReceipt({
    pull_request: PR,
    head_ref_oid: HEAD,
    base_ref_oid: BASE,
    potential_merge_commit_oid: POTENTIAL,
  });
  assert.equal(receipt.derived.budget.automatic_reservations_on_head, 0);
  assert.equal(receipt.derived.budget.automatic_requests_on_head, 0);
  assert.deepEqual(receipt.derived.generations, []);
  assert.deepEqual(receipt.derived.request_bindings, []);
  assert.deepEqual(receipt.derived.artifact_bindings, []);
  assert.equal(receipt.record_count, 3);
  assert.match(receipt.source_binding_digest, /^sha256:/u);
  assert.match(receipt.receipt_digest, /^sha256:/u);
});

test("public raw artifact binding records require the protected high-level seam", async () => {
  const setup = await createAutomaticRecoveryTransitionContext({
    bindArtifact: false,
  });
  const intent = await setup.ledger
    .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
      scheduler_append: setup.schedulerAppend,
      artifact_binding_candidate_handle:
        setup.artifactBindingCandidateHandle,
    });
  const loaded = await setup.ledger.load();
  const intentEntry = loaded.records.find((entry) =>
    entry.commit_sha === intent.intent_append_receipt.commit_sha);
  const readyEntry = loaded.records.find((entry) =>
    entry.commit_sha === intent.ready_append_receipt.commit_sha);
  const readyConfirmationEntry = loaded.records.find((entry) =>
    entry.commit_sha === intent.ready_confirmation_append_receipt.commit_sha);
  assert.notEqual(intentEntry, undefined);
  assert.notEqual(readyEntry, undefined);
  assert.notEqual(readyConfirmationEntry, undefined);
  const readyRecord = recordFromFixtureEnvelope(readyEntry.envelope);
  const writesBeforeReady = setup.fixture.writeCalls;
  const tipBeforeReady = setup.fixture.refTarget;
  await assert.rejects(
    setup.ledger.appendRecord(readyRecord, {
      evaluated_scope_receipt: setup.context.evaluatedScopeReceipt,
    }),
    (error) => error.code === "UNTRUSTED_RAW_EFFECT_APPEND",
  );
  assert.equal(setup.fixture.writeCalls, writesBeforeReady);
  assert.equal(setup.fixture.refTarget, tipBeforeReady);
  const intentRecord = recordFromFixtureEnvelope(intentEntry.envelope);
  const authority = intentRecord.payload.action.candidate_authority;
  const bindings = authority.candidates.map((candidate, index) => {
    const withoutDigest = {
      index,
      count: authority.candidates.length,
      generation_id: authority.generation_id,
      request_binding_record_oid: authority.request_binding_record_oid,
      request_id: authority.request_id,
      request_node_id: authority.request_node_id,
      artifact_selector: structuredClone(candidate.selector),
      artifact_node_id: candidate.artifact_node_id,
      artifact_url: candidate.artifact_url,
      artifact_type: candidate.artifact_type,
      artifact_created_at: candidate.artifact_created_at,
      server_time: candidate.evidence_response_server_time,
      raw_body_sha256: candidate.evidence_raw_body_sha256,
      actor: structuredClone(authority.expected_actor),
      app: structuredClone(authority.expected_app),
    };
    return {
      ...withoutDigest,
      receipt_digest: digestCanonical(
        "codex-review-gate-v2-artifact-binding-receipt",
        withoutDigest,
      ),
    };
  });
  const receipt = {
    http_status: 200,
    candidate_set_digest: intentRecord.payload.action.candidate_set_digest,
    artifact_binding_ready_confirmation_record_oid:
      readyConfirmationEntry.commit_sha,
    artifact_binding_ready_confirmation_payload_digest:
      readyConfirmationEntry.envelope.payload_digest,
    continuation_scheduler_observation_record_oid:
      readyConfirmationEntry.envelope.payload.receipt
        .ready_reachability_boundary
        .continuation_scheduler_observation_record_oid,
    continuation_full_scope_receipt_digest:
      readyConfirmationEntry.envelope.payload.receipt
        .ready_reachability_boundary.continuation_full_scope_receipt_digest,
    continuation_lease_acquire_commit_sha:
      readyConfirmationEntry.envelope.payload.receipt
        .ready_reachability_boundary.continuation_lease_acquire_commit_sha,
    binding_count: bindings.length,
    bindings,
    bindings_digest: digestCanonical(
      "codex-review-gate-v2-aggregate-artifact-binding-receipts",
      bindings,
    ),
  };
  const forgedResponse = createV2GitLedgerEffectResponseRecord({
    intent_record: intentRecord,
    intent_commit_sha: intentEntry.commit_sha,
    server_observed_at: loaded.post_ref.server_time,
    receipt,
  });
  const writesBefore = setup.fixture.writeCalls;
  const tipBefore = setup.fixture.refTarget;
  await assert.rejects(
    setup.ledger.appendRecord(forgedResponse, {
      evaluated_scope_receipt: setup.context.evaluatedScopeReceipt,
    }),
    (error) => error.code === "UNTRUSTED_RAW_EFFECT_APPEND",
  );
  assert.equal(setup.fixture.writeCalls, writesBefore);
  assert.equal(setup.fixture.refTarget, tipBefore);
});
test("protected candidate inventory is a sharded durable repository superset", async () => {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const verifier = provenanceVerifier({
    eventName: "schedule",
    ref: "refs/heads/main",
    shaValue: BASE,
  });
  const ledger = makeLedger(fixture, capability, verifier);
  await ledger.bootstrapCapability();
  const transportFixture = candidateTransportFixture(512);
  const transport = createV2GitHubCandidateInventory({
    fetch: transportFixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: candidateRepository(),
    restBaseUrl: "https://github.example.test",
  });
  const initial = await transport.scan();
  assert.equal(initial.shards.length, 2);

  let loaded = await ledger.load();
  const start = await appendCandidateInventoryPhase({
    ledger,
    loaded,
    phase: "cycle-start",
    initial,
  });
  loaded = await ledger.load();
  assert.equal(start.record_type, "candidate-inventory-observation");
  assert.equal(loaded.active_lease, null, "repository scans never acquire a PR lease");
  assert.equal(
    loaded.authority_projection.candidate_inventory.open_pr_discovery
      .candidates.length,
    512,
  );
  assert.equal(
    loaded.authority_projection.candidate_inventory.open_pr_discovery
      .bootstrap_complete,
    false,
  );

  const shards = [];
  for (let index = 0; index < initial.shards.length; index += 1) {
    const shard = await transport.readShard({
      inventory: initial,
      shard_index: index,
    });
    shards.push(shard);
    await appendCandidateInventoryPhase({
      ledger,
      loaded,
      phase: "shard",
      initial,
      shard,
    });
    loaded = await ledger.load();
  }
  const finalInventory = await transport.scan({ prior_inventory: initial });
  const finalShards = await readCandidateShardRound(transport, finalInventory);
  const cycleReceipt = finalizeV2CandidateInventoryCycle({
    initial_inventory: initial,
    shard_receipts: shards,
    final_inventory: finalInventory,
    final_shard_receipts: finalShards,
  });
  await appendCandidateInventoryPhase({
    ledger,
    loaded,
    phase: "cycle-complete",
    initial,
    finalInventory,
    cycleReceipt,
  });
  loaded = await ledger.load();
  const authority = loaded.authority_projection.candidate_inventory;
  assert.equal(authority.open_pr_discovery.bootstrap_complete, true);
  assert.equal(authority.open_pr_discovery.candidates.length, 512);
  assert.equal(authority.open_pr_discovery.current_open_pull_requests.length, 512);
  assert.equal(authority.completed_cycle.shard_record_oids.length, 2);
  assert.equal(
    loaded.projection.selectors.candidate_inventory_observations.length,
    4,
  );
  assert.deepEqual(
    deriveV2GitLedgerCandidateInventoryAuthority(
      loaded.records,
      REPOSITORY,
    ),
    authority,
  );
  const completionBlob = fixture.blobs.get(loaded.records.at(-1).blob_sha);
  assert.notEqual(completionBlob, undefined);
  assert.ok(
    Buffer.byteLength(completionBlob.content, "utf8") <=
      MAX_V2_GIT_LEDGER_BLOB_BYTES,
    "the full 512-candidate cycle-complete envelope fits the ledger blob cap",
  );
});

test("generic candidate inventory APIs reject raw legacy and compact records before writes",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const ledger = makeLedger(fixture, capability, provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    }));
    await ledger.bootstrapCapability();
    const transportFixture = candidateTransportFixture(0);
    const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
    const cycle = await readCandidateInventoryCycle({ transportFixture });
    await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...cycle,
    });
    const loaded = await ledger.load();
    const compactEnvelope = loaded.records
      .map(({ envelope }) => envelope)
      .find((envelope) =>
        envelope.record_type === "candidate-inventory-observation" &&
        envelope.payload.schema_version === 2 &&
        envelope.payload.phase === "cycle-start");
    assert.notEqual(compactEnvelope, undefined);
    const payload = structuredClone(compactEnvelope.payload);
    const record = createV2GitLedgerCandidateInventoryRecord({
      predecessor_commit_sha: loaded.tip_commit_sha,
      owner: payload.owner,
      server_observed_at: loaded.post_ref.server_time,
      payload,
    });
    const writesBefore = fixture.writeCalls;
    const objectsBefore = {
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
    };
    const tipBefore = fixture.refTarget;
    const isTypedRejection = (error) =>
      error.code === "UNTRUSTED_CANDIDATE_INVENTORY_APPEND";
    const assertNoWrites = () => {
      assert.equal(fixture.writeCalls, writesBefore);
      assert.deepEqual({
        blobs: fixture.blobs.size,
        trees: fixture.trees.size,
        commits: fixture.commits.size,
      }, objectsBefore);
      assert.equal(fixture.refTarget, tipBefore);
    };

    await assert.rejects(ledger.appendRecord(record), isTypedRejection);
    assertNoWrites();
    await assert.rejects(
      ledger.appendCandidateInventory({
        predecessor_commit_sha: loaded.tip_commit_sha,
        owner: payload.owner,
        server_observed_at: loaded.post_ref.server_time,
        payload,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      }),
      isTypedRejection,
    );
    assertNoWrites();

    const legacyPayload = candidateInventoryPayload({
      phase: "cycle-start",
      initial: cycle.initial_inventory,
      priorAuthorityDigest:
        loaded.authority_projection.candidate_inventory.authority_digest,
    });
    const legacyRecord = createV2GitLedgerCandidateInventoryRecord({
      predecessor_commit_sha: loaded.tip_commit_sha,
      owner: legacyPayload.owner,
      server_observed_at: loaded.post_ref.server_time,
      payload: legacyPayload,
    });
    await assert.rejects(
      ledger.productionApiForTest().appendRecord(legacyRecord),
      isTypedRejection,
    );
    assertNoWrites();
  });

test("unchanged empty schedules persist without fresh full evidence authority", async () => {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const triggerIdentity = scheduleTriggerIdentity();
  const ledger = makeLedger(fixture, capability, provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
  }));
  await ledger.bootstrapCapability();
  const transportFixture = candidateTransportFixture(0);
  const workflowCommandHandle = await createScheduleWorkflowCommandHandle();

  const bootstrapCycle = await readCandidateInventoryCycle({
    transportFixture,
  });
  const bootstrapAppend = await ledger
    .appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...bootstrapCycle,
  });
  assert.equal(bootstrapAppend.state, "persisted");
  assert.equal(bootstrapAppend.published_record_commit_shas.length, 4);
  assert.equal(bootstrapAppend.append_receipts.length, 1);
  const bootstrapDispatch = await ledger.loadOrReserveCandidateDispatch({
    workflow_command_handle: workflowCommandHandle,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });
  assert.equal(bootstrapDispatch.state, "complete");
  assert.equal(bootstrapDispatch.plan, null);
  const first = await ledger.load();
  let priorInventory = first.authority_projection.candidate_inventory
    .completed_cycle.final_inventory;

  for (let round = 0; round < 3; round += 1) {
    fixture.advanceServerTime(120);
    transportFixture.advanceServerTime(60);
    const cycle = await readCandidateInventoryCycle({
      transportFixture,
      priorInventory,
    });
    const persisted = await ledger
      .appendCandidateInventoryCycleIfRequired({
        workflow_command_handle: workflowCommandHandle,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        ...cycle,
      });
    assert.equal(persisted.state, "persisted");
    assert.equal(
      persisted.projection.reason,
      "candidate-full-evidence-authority-required",
    );
    assert.equal(persisted.published_record_commit_shas.length, 4);
    assert.equal(persisted.append_receipts.length, 1);
    const repeated = await ledger.load();
    assert.equal(
      repeated.commit_count,
      first.commit_count + (round + 1) * 4,
      "an unchanged empty schedule must remain durably auditable",
    );
    assert.equal(repeated.tip_commit_sha, persisted.final_tip_commit_sha);
    priorInventory = repeated.authority_projection.candidate_inventory
      .completed_cycle.final_inventory;
  }
});

test("read-only refresh classification authorizes only sampled durable empty sets",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    });
    const evidence = currentOpenSuppressionAdapterFixture();
    const ledger = makeLedger(fixture, capability, verifier, null, {
      candidateSuppressionEvidenceAdapter: evidence.adapter,
    });
    await ledger.bootstrapCapability();
    const transportFixture = candidateTransportFixture(0);
    const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
    const cycle = await readCandidateInventoryCycle({ transportFixture });
    await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...cycle,
    });
    await ledger.loadOrReserveCandidateDispatch({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    const stable = await ledger.load();
    const writeSnapshot = () => ({
      write_calls: fixture.writeCalls,
      blob_count: fixture.blobs.size,
      tree_count: fixture.trees.size,
      commit_object_count: fixture.commits.size,
      ref_target: fixture.refTarget,
    });
    const writesBefore = writeSnapshot();
    const input = {
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    };

    const begun = await ledger
      .beginCandidateInventoryRefreshClassification(input);
    assert.equal(begun.state, "evidence-required");
    assert.equal(
      begun.reason,
      "candidate-empty-current-open-evidence-required",
    );
    const collected = await ledger.collectCandidateInventoryRefreshEvidence({
      source_handle: begun.source_handle,
    });
    assert.equal(collected.state, "classification-required");
    assert.equal(collected.evidence_handle.current_open_candidate_count, 0);
    assert.equal(evidence.calls.length, 4);
    const writesBeforeEvidenceClone = writeSnapshot();
    await assert.rejects(
      ledger.classifyCandidateInventoryRefresh({
        source_handle: begun.source_handle,
        evidence_handle: structuredClone(collected.evidence_handle),
      }),
      (error) =>
        error.code === "UNTRUSTED_CANDIDATE_REFRESH_EVIDENCE_HANDLE",
    );
    assert.deepEqual(writeSnapshot(), writesBeforeEvidenceClone);
    const classified = await ledger.classifyCandidateInventoryRefresh({
      source_handle: begun.source_handle,
      evidence_handle: collected.evidence_handle,
    });
    assert.equal(classified.state, "suppress-authorized");
    assert.equal(classified.reason, "semantic-unchanged-not-due");
    assert.equal(
      classified.suppression_classification_handle.assurance,
      "bounded-sampled-empty-at-observation-boundary",
    );
    assert.equal(
      classified.suppression_classification_handle.github_atomic_snapshot,
      false,
    );
    assert.equal(
      classified.suppression_classification_handle.github_ref_fence,
      null,
    );
    assert.deepEqual(writeSnapshot(), writesBefore);
    assert.equal((await ledger.load()).commit_count, stable.commit_count);

    const callsBeforeClone = evidence.calls.length;
    await assert.rejects(
      ledger.collectCandidateInventoryRefreshEvidence({
        source_handle: structuredClone(begun.source_handle),
      }),
      (error) =>
        error.code === "UNTRUSTED_CANDIDATE_REFRESH_SOURCE_HANDLE",
    );
    assert.equal(evidence.calls.length, callsBeforeClone);
    assert.deepEqual(writeSnapshot(), writesBefore);

    const foreign = makeLedger(
      fixture,
      capability,
      provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
        jtiPrefix: "candidate-refresh-foreign-jti",
      }),
      null,
      { candidateSuppressionEvidenceAdapter: evidence.adapter },
    );
    await assert.rejects(
      foreign.collectCandidateInventoryRefreshEvidence({
        source_handle: begun.source_handle,
      }),
      (error) =>
        error.code === "UNTRUSTED_CANDIDATE_REFRESH_SOURCE_HANDLE",
    );
    assert.equal(evidence.calls.length, callsBeforeClone);
    assert.deepEqual(writeSnapshot(), writesBefore);

    evidence.setAfterProjection(() => {
      evidence.setListedNumbers([PR.number]);
      evidence.setAfterProjection(null);
    });
    const sampledBegin = await ledger
      .beginCandidateInventoryRefreshClassification(input);
    const sampledCollect = await ledger
      .collectCandidateInventoryRefreshEvidence({
        source_handle: sampledBegin.source_handle,
      });
    assert.equal(sampledCollect.state, "classification-required");
    assert.ok(
      Date.parse(sampledCollect.observation_boundary
        .github_observation_window.completed_at) <=
        Date.parse(sampledCollect.observation_boundary
          .ledger_pre_ref.server_time),
    );
    const sampledClassify = await ledger.classifyCandidateInventoryRefresh({
      source_handle: sampledBegin.source_handle,
      evidence_handle: sampledCollect.evidence_handle,
    });
    assert.equal(sampledClassify.state, "suppress-authorized");
    assert.equal(
      sampledClassify.observation_boundary.assurance,
      "bounded-sampled-empty-at-observation-boundary",
      "a later provider event is not consumed or reclassified as an atomic snapshot",
    );
    assert.deepEqual(writeSnapshot(), writesBefore);
    const afterSampled = await ledger.load();
    assert.equal(
      afterSampled.authority_projection.candidate_inventory.authority_digest,
      stable.authority_projection.candidate_inventory.authority_digest,
      "sampled suppression does not advance a durable inventory cursor",
    );
    assert.equal(
      afterSampled.authority_projection.candidate_dispatch.authority_digest,
      stable.authority_projection.candidate_dispatch.authority_digest,
      "sampled suppression does not ACK or advance dispatch authority",
    );

    const changedBegin = await ledger
      .beginCandidateInventoryRefreshClassification(input);
    const currentOpenCallsBefore = evidence.calls.length;
    const changedCollect = await ledger
      .collectCandidateInventoryRefreshEvidence({
        source_handle: changedBegin.source_handle,
      });
    assert.equal(changedCollect.state, "persist-required");
    assert.equal(
      changedCollect.reason,
      "candidate-full-evidence-dirty-epoch-required",
    );
    assert.equal(evidence.calls.length - currentOpenCallsBefore, 4);
    assert.equal(changedCollect.evidence_handle, null);
    assert.deepEqual(writeSnapshot(), writesBefore);

    evidence.setListedNumbers(Array.from({ length: 512 }, (_, index) =>
      index + 1));
    const boundedBegin = await ledger
      .beginCandidateInventoryRefreshClassification(input);
    const callsBeforeBounded = evidence.calls.length;
    const boundedNonempty = await ledger
      .collectCandidateInventoryRefreshEvidence({
        source_handle: boundedBegin.source_handle,
      });
    assert.equal(boundedNonempty.state, "persist-required");
    assert.equal(
      boundedNonempty.reason,
      "candidate-full-evidence-dirty-epoch-required",
    );
    assert.equal(evidence.calls.length - callsBeforeBounded, 14);
    assert.equal(boundedNonempty.evidence_handle, null);
    assert.deepEqual(writeSnapshot(), writesBefore);

    evidence.setListedNumbers(Array.from({ length: 513 }, (_, index) =>
      index + 1));
    const overBudgetBegin = await ledger
      .beginCandidateInventoryRefreshClassification(input);
    const callsBeforeOverBudget = evidence.calls.length;
    await assert.rejects(
      ledger.collectCandidateInventoryRefreshEvidence({
        source_handle: overBudgetBegin.source_handle,
      }),
      (error) => error.code === "CANDIDATE_OPEN_SET_CAP",
    );
    assert.equal(evidence.calls.length - callsBeforeOverBudget, 6);
    assert.deepEqual(writeSnapshot(), writesBefore);

    const callsBeforeStale = evidence.calls.length;
    await assert.rejects(
      ledger.collectCandidateInventoryRefreshEvidence({
        source_handle: sampledBegin.source_handle,
      }),
      (error) => error.code === "STALE_CANDIDATE_REFRESH_SOURCE_HANDLE",
    );
    assert.equal(evidence.calls.length, callsBeforeStale);
    assert.deepEqual(writeSnapshot(), writesBefore);
  });

test("high-level sampled-empty suppression exposes no authority handles",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const evidence = currentOpenSuppressionAdapterFixture();
    const ledger = makeLedger(
      fixture,
      capability,
      provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
      }),
      null,
      { candidateSuppressionEvidenceAdapter: evidence.adapter },
    );
    await ledger.bootstrapCapability();
    const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
    const transportFixture = candidateTransportFixture(0);
    const cycle = await readCandidateInventoryCycle({ transportFixture });
    await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...cycle,
    });
    await ledger.loadOrReserveCandidateDispatch({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    const stable = await ledger.load();
    const input = {
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    };
    const writeSnapshot = () => ({
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    });
    const writesBefore = writeSnapshot();
    const assertNoAuthorityHandles = (result) => {
      for (const key of [
        "source_handle",
        "evidence_handle",
        "suppression_classification_handle",
      ]) {
        assert.equal(key in result, false);
      }
    };

    const firstCalls = evidence.calls.length;
    const first = await ledger.trySuppressCandidateInventoryRefresh(input);
    assert.equal(first.state, "suppressed");
    assert.equal(first.reason, "semantic-unchanged-not-due");
    assert.equal(
      first.assurance,
      "bounded-sampled-empty-at-observation-boundary",
    );
    assert.equal(first.github_atomic_snapshot, false);
    assert.equal(first.github_ref_fence, null);
    assertNoAuthorityHandles(first);
    assert.equal(evidence.calls.length - firstCalls, 4);
    assert.equal("runV2Operation" in evidence.adapter, false);
    assert.deepEqual(writeSnapshot(), writesBefore);

    evidence.setAfterProjection(() => {
      evidence.setListedNumbers([PR.number]);
      evidence.setAfterProjection(null);
    });
    const postScanCalls = evidence.calls.length;
    const postScan = await ledger.trySuppressCandidateInventoryRefresh(input);
    assert.equal(postScan.state, "suppressed");
    assertNoAuthorityHandles(postScan);
    assert.equal(evidence.calls.length - postScanCalls, 4);
    assert.deepEqual(writeSnapshot(), writesBefore);
    const afterPostScan = await ledger.load();
    assert.equal(
      afterPostScan.authority_projection.candidate_inventory.authority_digest,
      stable.authority_projection.candidate_inventory.authority_digest,
    );
    assert.equal(
      afterPostScan.authority_projection.candidate_dispatch.authority_digest,
      stable.authority_projection.candidate_dispatch.authority_digest,
    );

    const nonemptyCalls = evidence.calls.length;
    const nonempty = await ledger.trySuppressCandidateInventoryRefresh(input);
    assert.equal(nonempty.state, "persist-required");
    assert.equal(
      nonempty.reason,
      "candidate-full-evidence-dirty-epoch-required",
    );
    assertNoAuthorityHandles(nonempty);
    assert.equal(evidence.calls.length - nonemptyCalls, 4);
    assert.deepEqual(writeSnapshot(), writesBefore);

    evidence.setListedNumbers(Array.from({ length: 512 }, (_, index) =>
      index + 1));
    const boundedCalls = evidence.calls.length;
    const bounded = await ledger.trySuppressCandidateInventoryRefresh(input);
    assert.equal(bounded.state, "persist-required");
    assert.equal(
      bounded.reason,
      "candidate-full-evidence-dirty-epoch-required",
    );
    assertNoAuthorityHandles(bounded);
    assert.equal(evidence.calls.length - boundedCalls, 14);
    assert.deepEqual(writeSnapshot(), writesBefore);

    evidence.setListedNumbers(Array.from({ length: 513 }, (_, index) =>
      index + 1));
    const capCalls = evidence.calls.length;
    await assert.rejects(
      ledger.trySuppressCandidateInventoryRefresh(input),
      (error) => error.code === "CANDIDATE_OPEN_SET_CAP",
    );
    assert.equal(evidence.calls.length - capCalls, 6);
    assert.deepEqual(writeSnapshot(), writesBefore);

    evidence.setListedNumbers([]);
    evidence.driftNextFence();
    const driftCalls = evidence.calls.length;
    const drifted = await ledger.trySuppressCandidateInventoryRefresh(input);
    assert.equal(drifted.state, "persist-required");
    assert.equal(
      drifted.reason,
      "candidate-current-open-evidence-incomplete",
    );
    assertNoAuthorityHandles(drifted);
    assert.equal(evidence.calls.length - driftCalls, 2);
    assert.deepEqual(writeSnapshot(), writesBefore);

    evidence.replayNextRequestPair();
    const replayCalls = evidence.calls.length;
    const replayed = await ledger.trySuppressCandidateInventoryRefresh(input);
    assert.equal(replayed.state, "persist-required");
    assert.equal(
      replayed.reason,
      "candidate-current-open-evidence-incomplete",
    );
    assertNoAuthorityHandles(replayed);
    assert.equal(evidence.calls.length - replayCalls, 2);
    assert.deepEqual(writeSnapshot(), writesBefore);

    evidence.setListedNumbers([]);
    const fresh = await ledger.trySuppressCandidateInventoryRefresh(input);
    assert.equal(fresh.state, "suppressed");
    evidence.reuseLastProjection();
    const reuseCalls = evidence.calls.length;
    const reused = await ledger.trySuppressCandidateInventoryRefresh(input);
    assert.equal(reused.state, "persist-required");
    assert.equal(
      reused.reason,
      "candidate-current-open-evidence-reused",
    );
    assertNoAuthorityHandles(reused);
    assert.equal(evidence.calls.length, reuseCalls);
    assert.deepEqual(writeSnapshot(), writesBefore);

    const gitCallsBeforeRaw = fixture.calls.length;
    await assert.rejects(
      ledger.trySuppressCandidateInventoryRefresh({
        ...input,
        source_handle: {},
      }),
      TypeError,
    );
    assert.equal(fixture.calls.length, gitCallsBeforeRaw);
    assert.equal(evidence.calls.length, reuseCalls);
    await assert.rejects(
      ledger.trySuppressCandidateInventoryRefresh({
        ...input,
        workflow_command_handle: structuredClone(workflowCommandHandle),
      }),
      /protected descriptor-backed command/u,
    );
    assert.equal(fixture.calls.length, gitCallsBeforeRaw);
    assert.equal(evidence.calls.length, reuseCalls);
    assert.deepEqual(writeSnapshot(), writesBefore);
  });

test("atomic refresh reconciliation privately preserves a dirty persist decision",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const currentOpen = currentOpenSuppressionAdapterFixture({
      initialNumbers: [],
    });
    const fullTransport = candidateTransportFixture(0);
    const fullRefreshCalls = [];
    const refreshAdapter = Object.freeze({
      schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
      schema_version: 1,
      configuration_digest: rawDigest(
        "candidate-refresh-reconciliation-test-configuration-v1",
      ),
      collectCurrentOpenProjection:
        currentOpen.adapter.collectCurrentOpenProjection,
      async collectCandidateInventoryAttempt(request) {
        fullRefreshCalls.push(request);
        const attempt = await readCandidateInventoryAttempt({
          transportFixture: fullTransport,
          priorInventory: request.prior_inventory,
          initialInventory: request.initial_inventory,
        });
        return {
          schema:
            "codex-review-gate-git-ledger-candidate-full-refresh-result-v2",
          schema_version: 1,
          request_handle: request.request_handle,
          attempt_index: request.attempt_index,
          ...attempt,
        };
      },
    });
    const ledger = makeLedger(
      fixture,
      capability,
      provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
      }),
      null,
      { candidateInventoryRefreshAdapter: refreshAdapter },
    );
    await ledger.bootstrapCapability();
    const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
    const bootstrapCycle = await readCandidateInventoryCycle({
      transportFixture: fullTransport,
    });
    await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...bootstrapCycle,
    });
    await ledger.loadOrReserveCandidateDispatch({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    const stable = await ledger.load();
    const emptyWritesBefore = fixture.writeCalls;
    const emptyObjectsBefore = {
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    };
    const emptyCurrentOpenCalls = currentOpen.calls.length;
    const suppressed = await ledger
      .reconcileCandidateInventoryRefreshAtomically({
        workflow_command_handle: workflowCommandHandle,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      });
    assert.equal(suppressed.state, "suppressed");
    assert.equal(suppressed.publication_result, null);
    assert.equal(suppressed.persistence_mode, null);
    assertNoCandidateRefreshPrivateAuthority(suppressed);
    assert.equal(fullRefreshCalls.length, 0);
    assert.equal(currentOpen.calls.length - emptyCurrentOpenCalls, 4);
    assert.equal(fixture.writeCalls, emptyWritesBefore);
    assert.deepEqual({
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    }, emptyObjectsBefore);

    currentOpen.setListedNumbers([PR.number]);
    const patchCountBefore = fixture.calls.filter(({ method }) =>
      method === "PATCH").length;
    const fullRestCallsBefore = fullTransport.calls.length;

    const reconciled = await ledger
      .reconcileCandidateInventoryRefreshAtomically({
        workflow_command_handle: workflowCommandHandle,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      });

    assert.equal(
      reconciled.schema,
      "codex-review-gate-git-ledger-candidate-refresh-reconciliation-v2",
    );
    assert.equal(reconciled.schema_version, 1);
    assert.equal(reconciled.state, "persisted");
    assert.equal(
      reconciled.reason,
      "candidate-full-evidence-dirty-epoch-required",
    );
    assert.equal(reconciled.suppression_result.state, "persist-required");
    assert.equal(reconciled.publication_result.state, "persisted");
    assert.equal(
      reconciled.publication_result.projection.semantic_change,
      false,
      "the private must-persist path must survive an unchanged full scan",
    );
    assert.equal(fullRefreshCalls.length, 1);
    assert.equal(fullTransport.calls.length - fullRestCallsBefore, 4);
    assert.equal("runV2Operation" in refreshAdapter, false);
    assert.equal(
      fullRefreshCalls[0].prior_inventory.receipt_digest,
      stable.authority_projection.candidate_inventory.completed_cycle
        .final_inventory.receipt_digest,
    );
    assert.equal(
      fixture.calls.filter(({ method }) => method === "PATCH").length -
        patchCountBefore,
      1,
      "the full refresh must publish through one reachable ref CAS",
    );
    assertNoCandidateRefreshPrivateAuthority(reconciled);

    currentOpen.setListedNumbers(Array.from(
      { length: 513 },
      (_, index) => index + 1,
    ));
    const capWritesBefore = fixture.writeCalls;
    const capObjectsBefore = {
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    };
    const capCallsBefore = currentOpen.calls.length;
    await assert.rejects(
      ledger.reconcileCandidateInventoryRefreshAtomically({
        workflow_command_handle: workflowCommandHandle,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      }),
      (error) => error.code === "CANDIDATE_OPEN_SET_CAP",
    );
    assert.equal(currentOpen.calls.length - capCallsBefore, 6);
    assert.equal(fullRefreshCalls.length, 1);
    assert.equal(fixture.writeCalls, capWritesBefore);
    assert.deepEqual({
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    }, capObjectsBefore);
  });

function currentOpenDispatchMeasurementPlan(productionAuthority, {
  sourceCandidateIndex = 0,
  batchIndex = 0,
  batchCount = 1,
} = {}) {
  assert.ok(
    sourceCandidateIndex >= 0 &&
      sourceCandidateIndex < productionAuthority.candidate_count,
  );
  const sourceGenerationRecordOid = "1".repeat(40);
  const summary = productionAuthority.candidates[sourceCandidateIndex];
  const lifecycleGenerationId =
    `candidate-lifecycle:${digestCanonical(
      "codex-review-gate-v2-current-open-lifecycle-generation",
      {
        repository: candidateRepository(),
        identity_digest: summary.identity_digest,
      },
    ).slice("sha256:".length)}`;
  const candidateWithoutDigest = {
    schema: "codex-review-gate-git-ledger-candidate-dispatch-selection-v2",
    schema_version: 2,
    source_generation_record_oid: sourceGenerationRecordOid,
    identity: structuredClone(summary.identity),
    identity_digest: summary.identity_digest,
    lifecycle_seed: structuredClone(summary.lifecycle_seed),
    lifecycle_seed_digest: summary.lifecycle_seed_digest,
    lifecycle_generation_id: lifecycleGenerationId,
  };
  const candidate = {
    ...candidateWithoutDigest,
    selection_digest: digestCanonical(
      "codex-review-gate-v2-current-open-dispatch-selection",
      {
        source_generation_record_oid: sourceGenerationRecordOid,
        identity_digest: summary.identity_digest,
        lifecycle_seed_digest: summary.lifecycle_seed_digest,
        lifecycle_generation_id: lifecycleGenerationId,
      },
    ),
  };
  const candidateSource = {
    schema: "codex-review-gate-git-ledger-candidate-dispatch-source-v2",
    schema_version: 2,
    source_profile: "stable-graphql-current-open-v4",
    source_generation_id: `candidate-source:${"2".repeat(64)}`,
    source_generation_record_oid: sourceGenerationRecordOid,
    source_generation_digest: rawDigest("binding-size-source-generation"),
    production_candidate_authority_digest: productionAuthority.authority_digest,
    candidate_set_digest: productionAuthority.candidate_set_digest,
    source_current_open_semantic_digest:
      productionAuthority.source_current_open_semantic_digest,
    lifecycle_candidate_set_digest:
      rawDigest("binding-size-lifecycle-candidate-set"),
  };
  const withoutDigest = {
    schema: "codex-review-gate-git-ledger-candidate-dispatch-plan-v2",
    schema_version: 2,
    repository: structuredClone(REPOSITORY),
    generation_id: `candidate-dispatch:${"3".repeat(64)}`,
    cycle_id: `candidate-cycle:${"4".repeat(64)}`,
    candidate_source: candidateSource,
    candidate_inventory_authority_digest:
      rawDigest("binding-size-candidate-authority"),
    candidate_dispatch_authority_digest:
      rawDigest("binding-size-dispatch-authority"),
    reservation_record_oid: "5".repeat(40),
    reservation_digest: rawDigest("binding-size-reservation"),
    inventory_digest: rawDigest("binding-size-inventory"),
    batch_index: batchIndex,
    batch_count: batchCount,
    dispatch_digest: rawDigest("binding-size-dispatch"),
    items: [{ candidate_index: 0, candidate }],
    remaining_count: 1,
    dispatch_commit_budget_required: 1,
    candidate_execution_commit_budget_required: 1,
    total_commit_budget_required: 2,
    remaining_ledger_commit_capacity_after_dispatch: 4094,
  };
  return validateV2GitLedgerCandidateDispatchPlan({
    ...withoutDigest,
    plan_digest: digestCanonical(
      "codex-review-gate-v2-candidate-dispatch-plan",
      withoutDigest,
    ),
  });
}

function currentOpenDispatchMeasurementBinding(plan) {
  const item = plan.items[0];
  const withoutDigest = {
    schema: "codex-review-gate-git-ledger-candidate-dispatch-binding-v2",
    schema_version: 2,
    repository: structuredClone(plan.repository),
    generation_id: plan.generation_id,
    cycle_id: plan.cycle_id,
    candidate_source: structuredClone(plan.candidate_source),
    candidate_inventory_authority_digest:
      plan.candidate_inventory_authority_digest,
    candidate_dispatch_authority_digest:
      plan.candidate_dispatch_authority_digest,
    inventory_digest: plan.inventory_digest,
    reservation_record_oid: plan.reservation_record_oid,
    reservation_digest: plan.reservation_digest,
    dispatch_digest: plan.dispatch_digest,
    batch_index: plan.batch_index,
    batch_count: plan.batch_count,
    candidate_index: item.candidate_index,
    candidate: structuredClone(item.candidate),
  };
  return {
    ...withoutDigest,
    binding_digest: digestCanonical(
      "codex-review-gate-v2-current-open-candidate-dispatch-binding",
      withoutDigest,
    ),
  };
}

test("current-open v3 rejects one oversized exact binding before atomic reserve",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const currentOpen = currentOpenSuppressionAdapterFixture({
      initialNumbers: [PR.number],
    });
    currentOpen.setRefNames(PR.number, {
      head: `feature/${"h".repeat(247)}`,
      base: `release/${"b".repeat(247)}`,
    });
    currentOpen.setPullRequestNodeId(PR.number, "P".repeat(256));
    currentOpen.setHeadRepository(PR.number, {
      id: "R".repeat(256),
      databaseId: Number.MAX_SAFE_INTEGER,
      nameWithOwner: `${"o".repeat(100)}/${"r".repeat(100)}`,
    });
    let pair = null;
    currentOpen.setAfterProjection((value) => {
      pair = value;
      currentOpen.setAfterProjection(null);
    });
    let legacyFinishCalls = 0;
    const refreshAdapter = Object.freeze({
      schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
      schema_version: 2,
      configuration_digest: rawDigest(
        "candidate-current-open-v3-binding-size-configuration-v1",
      ),
      collectCurrentOpenCandidateAuthority:
        currentOpen.collectProductionCandidateAuthority,
      async finishLegacyCandidateInventoryAttempt() {
        legacyFinishCalls += 1;
        throw new Error("oversized current-open binding used legacy REST");
      },
    });
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    });
    const ledger = makeLedger(fixture, capability, verifier, null, {
      candidateInventoryRefreshAdapter: refreshAdapter,
    });
    await ledger.bootstrapCapability();
    const input = {
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    };
    const before = {
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
      patches: fixture.calls.filter(({ method }) => method === "PATCH").length,
      object_posts: fixture.calls.filter(({ method, path }) =>
        method === "POST" && new Set([
          "/git/blobs", "/git/trees", "/git/commits",
        ]).has(path)).length,
    };

    await assert.rejects(
      ledger.reconcileCandidateInventoryRefreshAtomically(input),
      (error) => error.code === "candidate-dispatch-binding-size",
    );

    assert.notEqual(pair, null);
    const measurementPlan = currentOpenDispatchMeasurementPlan(
      pair.production_candidate_authority,
    );
    assert.ok(Buffer.byteLength(canonicalJson(measurementPlan), "utf8") <
      MAX_V2_CURRENT_OPEN_CANDIDATE_DISPATCH_PLAN_BYTES);
    assert.ok(Buffer.byteLength(canonicalJson(
      currentOpenDispatchMeasurementBinding(measurementPlan),
    ), "utf8") > MAX_V2_CURRENT_OPEN_CANDIDATE_DISPATCH_BINDING_BYTES);
    assert.throws(
      () => projectV2GitLedgerCandidateDispatchBinding(measurementPlan, 0),
      (error) => error.code === "candidate-dispatch-binding-size",
    );
    assert.equal(currentOpen.calls.length, 4);
    assert.equal(legacyFinishCalls, 0);
    assert.deepEqual({
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
      patches: fixture.calls.filter(({ method }) => method === "PATCH").length,
      object_posts: fixture.calls.filter(({ method, path }) =>
        method === "POST" && new Set([
          "/git/blobs", "/git/trees", "/git/commits",
        ]).has(path)).length,
    }, before);
    const loaded = await ledger.load();
    assert.equal(
      loaded.authority_projection.candidate_inventory.completed_generation ??
        null,
      null,
    );
    assert.equal(
      loaded.authority_projection.candidate_dispatch.active_reservation,
      null,
    );
    assert.equal(
      consumeV2CurrentOpenProductionCandidateAuthorityHandle(
        pair.production_candidate_authority,
        pair.projection,
      ),
      pair.production_candidate_authority,
    );
    assert.throws(
      () => consumeV2CurrentOpenProductionCandidateAuthorityHandle(
        pair.production_candidate_authority,
        pair.projection,
      ),
      (error) =>
        error.code === "CONSUMED_CURRENT_OPEN_PRODUCTION_AUTHORITY_HANDLE",
    );
  });

test("C65 golden loader keeps one opened object across path replacement",
  async () => {
    const raceDirectory = await mkdtemp(join(
      tmpdir(),
      "codex-review-gate-c65-fixture-race-",
    ));
    try {
      const racePath = join(raceDirectory, "fixture.json.gz");
      const displacedPath = join(raceDirectory, "opened.json.gz");
      await copyFile(C65_BATCH0_GIT_FIXTURE_URL, racePath);
      const asset = await loadC65Batch0GitFixtureState({
        fixtureUrl: racePath,
        async afterFileIdentityCheck() {
          await rename(racePath, displacedPath);
          await writeFile(
            racePath,
            Buffer.alloc(C65_BATCH0_GIT_FIXTURE_MAX_GZIP_BYTES + 1),
          );
        },
      });
      assert.equal(asset.git.commits.length, 198);
      assert.equal(asset.git.ref_target, C65_BATCH0_GIT_FIXTURE_REF);
    } finally {
      await rm(raceDirectory, { recursive: true, force: true });
    }
  });

test("C65 golden loader rejects a writerless FIFO without blocking",
  { timeout: 2_000 }, async () => {
    const fifoDirectory = await mkdtemp(join(
      tmpdir(),
      "codex-review-gate-c65-fixture-fifo-",
    ));
    const fifoPath = join(fifoDirectory, "fixture.json.gz");
    let completed;
    try {
      const created = spawnSync("/usr/bin/mkfifo", [fifoPath], {
        encoding: "utf8",
        timeout: 1_000,
      });
      assert.equal(created.status, 0, created.stderr);
      const pending = loadC65Batch0GitFixtureState({
        fixtureUrl: fifoPath,
      });
      completed = pending.then(
        () => ({ status: "resolved", error: null }),
        (error) => ({ status: "rejected", error }),
      );
      const outcome = await Promise.race([
        completed,
        delay(250).then(() => ({ status: "blocked", error: null })),
      ]);
      if (outcome.status === "blocked") {
        await writeFile(fifoPath, Buffer.alloc(0));
        await completed;
      }
      assert.equal(outcome.status, "rejected");
      assert.match(outcome.error.message, /regular file/u);
    } finally {
      await rm(fifoDirectory, { recursive: true, force: true });
    }
  });

test("C65 golden load reverifies every historical provenance exactly once",
  async () => {
    const asset = await loadC65Batch0GitFixtureState();
    const fixture = githubGitFixture(asset.git);
    const triggerIdentity = scheduleTriggerIdentity();
    const historicalReverificationLog = [];
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
      historicalTokenAuthorities: asset.historical_token_authorities,
      historicalReverificationLog,
    });
    const ledger = makeLedger(
      fixture,
      scheduleCapabilityReceipt(),
      verifier,
      await loadFixtureProviderPreflight(),
    );
    assert.equal((await ledger.load()).commit_count, 198);
    assertC65HistoricalProvenanceReverification(
      historicalReverificationLog,
      asset.historical_token_authorities,
    );
  });

test("C65 fresh provenance factories share one monotonic mint authority",
  async () => {
    const asset = await loadC65Batch0GitFixtureState();
    const fixture = githubGitFixture(asset.git);
    const stored = fixtureEnvelopeAt(fixture, fixture.refTarget)
      .workflow_provenance;
    const provenanceRequest = {
      schema: V2_GIT_LEDGER_PROVENANCE_REQUEST_SCHEMA,
      schema_version: 1,
      ...structuredClone(stored.operation_binding),
    };
    const mintState = createFixtureProvenanceMintState(
      asset.historical_token_authorities,
    );
    const createVerifier = (runId, fixedJti = null) => provenanceVerifier({
      eventName: "schedule",
      ref: "refs/heads/main",
      shaValue: BASE,
      runIdentity: { ...OWNER, run_id: runId },
      fixedJti,
      historicalTokenAuthorities: asset.historical_token_authorities,
      fixtureProvenanceMintState: mintState,
    });
    const verifierOne = createVerifier("9401");
    const verifierTwo = createVerifier("9402");
    const request = (mode, compactJwt = null) => ({
      schema: V2_GIT_LEDGER_PROVENANCE_VERIFIER_REQUEST_SCHEMA,
      schema_version: 1,
      mode,
      provenance_request: provenanceRequest,
      compact_jwt: compactJwt,
      stored_receipt: mode === "mint-and-verify" ? null : stored,
    });
    const first = await verifierOne(request("mint-and-verify"));
    const second = await verifierTwo(request("mint-and-verify"));
    const firstPayload = JSON.parse(Buffer.from(
      first.compact_jwt.split(".")[1],
      "base64url",
    ).toString("utf8"));
    const secondPayload = JSON.parse(Buffer.from(
      second.compact_jwt.split(".")[1],
      "base64url",
    ).toString("utf8"));
    assert.deepEqual(
      [firstPayload.ordinal, secondPayload.ordinal],
      [719, 720],
    );
    assert.notEqual(firstPayload.jti, secondPayload.jti);
    assert.notEqual(first.compact_jwt, second.compact_jwt);
    assert.notEqual(
      first.receipt.token_sha256,
      second.receipt.token_sha256,
    );
    assert.equal(first.receipt.claims.run_id, "9401");
    assert.equal(second.receipt.claims.run_id, "9402");
    const replayedSecond = await verifierOne(
      request("reverify", second.compact_jwt),
    );
    assert.equal(replayedSecond.receipt.claims.run_id, "9402");
    assert.deepEqual(
      mintState.minted_authorities.map(({ ordinal }) => ordinal),
      [719, 720],
    );
    await assert.rejects(
      createVerifier("9403", firstPayload.jti)(request("mint-and-verify")),
      /duplicate fixture provenance jti/u,
    );
  });

test("current-open v3 reconciliation publishes one fresh generation without legacy REST",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const currentOpen = currentOpenSuppressionAdapterFixture({
      initialNumbers: [PR.number],
    });
    let legacyFinishCalls = 0;
    const refreshAdapter = Object.freeze({
      schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
      schema_version: 2,
      configuration_digest: rawDigest(
        "candidate-current-open-v3-test-configuration-v1",
      ),
      collectCurrentOpenCandidateAuthority:
        currentOpen.collectProductionCandidateAuthority,
      async finishLegacyCandidateInventoryAttempt() {
        legacyFinishCalls += 1;
        throw new Error("fresh current-open generation used legacy REST");
      },
    });
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    });
    const ledger = makeLedger(fixture, capability, verifier, null, {
      candidateInventoryRefreshAdapter: refreshAdapter,
    });
    await ledger.bootstrapCapability();
    const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
    const input = {
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    };
    const before = {
      patches: fixture.calls.filter(({ method }) => method === "PATCH").length,
      ref: fixture.refTarget,
      commits: fixture.commits.size,
    };

    const reconciled = await ledger
      .reconcileCandidateInventoryRefreshAtomically(input);

    assert.equal(reconciled.schema_version, 2);
    assert.equal(reconciled.state, "persisted");
    assert.equal(reconciled.persistence_mode, "current-open-generation-v3");
    assert.equal(reconciled.publication_result.state, "persisted");
    assert.equal(
      reconciled.publication_result.publication_outcome,
      "published",
    );
    assert.equal(
      reconciled.publication_result.published_record_commit_shas.length,
      2,
      "one generation and its first reservation must share one publication",
    );
    assert.equal(legacyFinishCalls, 0);
    assert.equal(currentOpen.calls.length, 4);
    assert.equal(
      fixture.calls.filter(({ method }) => method === "PATCH").length -
        before.patches,
      1,
    );
    assert.equal(fixture.commits.size - before.commits, 2);
    assert.notEqual(fixture.refTarget, before.ref);
    assertNoCandidateRefreshPrivateAuthority(reconciled);

    const restarted = makeLedger(fixture, capability, verifier, null, {
      candidateInventoryRefreshAdapter: refreshAdapter,
    });
    const loaded = await restarted.load();
    const generation = loaded.authority_projection.candidate_inventory
      .completed_generation;
    assert.equal(generation.source_profile, "stable-graphql-current-open-v4");
    assert.equal(generation.candidate_count, 1);
    assert.equal(generation.candidates[0].identity.number, PR.number);
    const stableLifecycleId = (repository, identityDigest) =>
      `candidate-lifecycle:${digestCanonical(
        "codex-review-gate-v2-current-open-lifecycle-generation",
        { repository, identity_digest: identityDigest },
      ).slice("sha256:".length)}`;
    const candidateRepository = {
      owner: REPOSITORY.owner,
      name: REPOSITORY.name,
      id: REPOSITORY.id,
      node_id: REPOSITORY.node_id,
    };
    assert.equal(
      generation.candidates[0].lifecycle_generation_id,
      stableLifecycleId(
        candidateRepository,
        generation.candidates[0].identity_digest,
      ),
    );
    assert.notEqual(
      stableLifecycleId(
        { ...candidateRepository, name: "other-repo" },
        generation.candidates[0].identity_digest,
      ),
      generation.candidates[0].lifecycle_generation_id,
    );
    assert.notEqual(
      stableLifecycleId(candidateRepository, `sha256:${"f".repeat(64)}`),
      generation.candidates[0].lifecycle_generation_id,
    );
    assert.equal(
      loaded.authority_projection.candidate_dispatch.active_reservation
        .reservation.candidates[0].identity.number,
      PR.number,
    );
    const restartedDispatch = await restarted.loadOrReserveCandidateDispatch({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    assert.equal(restartedDispatch.state, "dispatch");
    const projectedBinding = projectV2GitLedgerCandidateDispatchBinding(
      restartedDispatch.plan,
      0,
    );
    const active = loaded.authority_projection.candidate_dispatch
      .active_reservation;
    assert.deepEqual(Object.keys(projectedBinding).sort(), [
      "batch_count",
      "batch_index",
      "binding_digest",
      "candidate",
      "candidate_dispatch_authority_digest",
      "candidate_index",
      "candidate_inventory_authority_digest",
      "candidate_source",
      "cycle_id",
      "dispatch_digest",
      "generation_id",
      "inventory_digest",
      "repository",
      "reservation_digest",
      "reservation_record_oid",
      "schema",
      "schema_version",
    ]);
    assert.equal(projectedBinding.schema,
      "codex-review-gate-git-ledger-candidate-dispatch-binding-v2");
    assert.equal(projectedBinding.schema_version, 2);
    assert.deepEqual(projectedBinding.repository, active.reservation.repository);
    assert.equal(projectedBinding.generation_id,
      active.reservation.generation_id);
    assert.equal(projectedBinding.cycle_id, active.reservation.cycle_id);
    assert.deepEqual(projectedBinding.candidate_source,
      active.reservation.candidate_source);
    assert.equal(projectedBinding.candidate_inventory_authority_digest,
      active.reservation.candidate_inventory_authority_digest);
    assert.equal(projectedBinding.candidate_dispatch_authority_digest,
      loaded.authority_projection.candidate_dispatch.authority_digest);
    assert.equal(projectedBinding.inventory_digest,
      active.reservation.inventory_digest);
    assert.equal(projectedBinding.reservation_record_oid,
      active.reservation_record_oid);
    assert.equal(projectedBinding.reservation_digest,
      active.reservation.reservation_digest);
    assert.equal(projectedBinding.dispatch_digest,
      active.reservation.dispatch_digest);
    assert.equal(projectedBinding.batch_index,
      active.reservation.batch_index);
    assert.equal(projectedBinding.batch_count,
      active.reservation.batch_count);
    assert.equal(projectedBinding.candidate_index, 0);
    assert.deepEqual(projectedBinding.candidate,
      active.reservation.candidates[0]);
    assert.match(projectedBinding.binding_digest, /^sha256:/u);
    const hybridDowngrade = {
      generation_id: projectedBinding.generation_id,
      cycle_id: projectedBinding.cycle_id,
      inventory_digest: projectedBinding.inventory_digest,
      batch_index: projectedBinding.batch_index,
      batch_count: projectedBinding.batch_count,
      dispatch_digest: projectedBinding.dispatch_digest,
      candidate: structuredClone(projectedBinding.candidate),
    };
    assert.throws(
      () => validateV2GitLedgerCandidateDispatchBinding(hybridDowngrade),
      /source representation|downgrade/u,
    );
    const resealBinding = (value) => {
      const { binding_digest: _digest, ...withoutDigest } = value;
      return {
        ...withoutDigest,
        binding_digest: digestCanonical(
          "codex-review-gate-v2-current-open-candidate-dispatch-binding",
          withoutDigest,
        ),
      };
    };
    const resealSelection = (value) => ({
      ...value,
      selection_digest: digestCanonical(
        "codex-review-gate-v2-current-open-dispatch-selection",
        {
          source_generation_record_oid:
            value.source_generation_record_oid,
          identity_digest: value.identity_digest,
          lifecycle_seed_digest: value.lifecycle_seed_digest,
          lifecycle_generation_id: value.lifecycle_generation_id,
        },
      ),
    });
    assert.deepEqual(
      validateV2GitLedgerCandidateDispatchBinding(projectedBinding),
      projectedBinding,
    );
    const identityTimestampAlias = structuredClone(projectedBinding);
    identityTimestampAlias.candidate.identity.created_at =
      identityTimestampAlias.candidate.identity.created_at.replace(
        ".000Z",
        "Z",
      );
    assert.notEqual(
      identityTimestampAlias.candidate.identity.created_at,
      projectedBinding.candidate.identity.created_at,
    );
    assert.throws(
      () => validateV2GitLedgerCandidateDispatchBinding(
        identityTimestampAlias,
      ),
      /identity\.created_at.*canonical millisecond UTC representation/u,
    );
    const lifecycleTamper = structuredClone(projectedBinding);
    lifecycleTamper.candidate.lifecycle_generation_id =
      `candidate-lifecycle:${"f".repeat(64)}`;
    lifecycleTamper.candidate = resealSelection(lifecycleTamper.candidate);
    assert.throws(
      () => validateV2GitLedgerCandidateDispatchBinding(
        resealBinding(lifecycleTamper),
      ),
      /changes its lifecycle identity/u,
    );
    const planTamper = structuredClone(restartedDispatch.plan);
    planTamper.items[0].candidate.lifecycle_generation_id =
      `candidate-lifecycle:${"f".repeat(64)}`;
    planTamper.items[0].candidate = resealSelection(
      planTamper.items[0].candidate,
    );
    delete planTamper.plan_digest;
    planTamper.plan_digest = digestCanonical(
      "codex-review-gate-v2-candidate-dispatch-plan",
      planTamper,
    );
    assert.throws(
      () => validateV2GitLedgerCandidateDispatchPlan(planTamper),
      /changes its lifecycle identity/u,
    );
    const sourceTamper = structuredClone(projectedBinding);
    sourceTamper.candidate_source.source_generation_record_oid =
      "f".repeat(40);
    assert.throws(
      () => validateV2GitLedgerCandidateDispatchBinding(
        resealBinding(sourceTamper),
      ),
      /changes its source generation/u,
    );
    const generationEntry = loaded.records.find((entry) =>
      entry.envelope.record_type === "candidate-inventory-observation" &&
      entry.envelope.payload.schema_version === 3);
    assert.notEqual(generationEntry, undefined);
    const generationIndex = loaded.records.indexOf(generationEntry);
    const generationEvidence = durableCurrentOpenGenerationAttachmentValue(
      fixture,
      generationEntry.envelope.payload.attachment_manifest,
    );
    const hydratedGenerationPrefix = loaded.records
      .slice(0, generationIndex + 1)
      .map((entry, index) => index === generationIndex
        ? {
            ...structuredClone(entry),
            candidate_inventory_attachment_evidence:
              structuredClone(generationEvidence),
          }
        : structuredClone(entry));
    assert.equal(
      deriveV2GitLedgerCandidateInventoryAuthority(
        hydratedGenerationPrefix,
        REPOSITORY,
      ).completed_generation.generation_id,
      generationEntry.envelope.payload.generation_id,
    );
    const resealGenerationManifest = (manifestValue) => {
      const manifest = structuredClone(manifestValue);
      delete manifest.manifest_digest;
      return {
        ...manifest,
        manifest_digest: digestCanonical(
          "codex-review-gate-v2-current-open-generation-attachment-manifest",
          manifest,
        ),
      };
    };
    const sourceGenerationTamper = structuredClone(hydratedGenerationPrefix);
    const sourceGenerationEntry = sourceGenerationTamper[generationIndex];
    const sourceGenerationPayload = structuredClone(
      sourceGenerationEntry.envelope.payload,
    );
    sourceGenerationPayload.generation_id =
      `candidate-source:${"f".repeat(64)}`;
    sourceGenerationPayload.attachment_manifest = resealGenerationManifest({
      ...sourceGenerationPayload.attachment_manifest,
      generation_id: sourceGenerationPayload.generation_id,
    });
    resealFixtureCandidateInventoryEnvelopePayload(
      sourceGenerationEntry.envelope,
      sourceGenerationPayload,
    );
    assert.throws(
      () => deriveV2GitLedgerCandidateInventoryAuthority(
        sourceGenerationTamper,
        REPOSITORY,
      ),
      (error) => error.code === "candidate-inventory-source-generation",
    );

    const lifecycleGenerationTamper = structuredClone(
      hydratedGenerationPrefix,
    );
    const lifecycleGenerationEntry =
      lifecycleGenerationTamper[generationIndex];
    const lifecycleEvidence = structuredClone(
      lifecycleGenerationEntry.candidate_inventory_attachment_evidence,
    );
    lifecycleEvidence.lifecycle_candidates[0].lifecycle_generation_id =
      `candidate-lifecycle:${"f".repeat(64)}`;
    lifecycleEvidence.lifecycle_candidate_set_digest = digestCanonical(
      "codex-review-gate-v2-current-open-lifecycle-candidate-set",
      lifecycleEvidence.lifecycle_candidates.map((candidate) => ({
        identity_digest: candidate.identity_digest,
        lifecycle_seed_digest: candidate.lifecycle_seed_digest,
        lifecycle_generation_id: candidate.lifecycle_generation_id,
      })),
    );
    lifecycleEvidence.dispatch_batch_digests = [digestCanonical(
      "codex-review-gate-v2-current-open-dispatch-batch-binding",
      {
        batch_index: 0,
        candidate_offset: 0,
        candidate_count: 1,
        candidates: lifecycleEvidence.lifecycle_candidates.map(
          (candidate) => ({
            identity: structuredClone(candidate.identity),
            identity_digest: candidate.identity_digest,
            lifecycle_seed: structuredClone(candidate.lifecycle_seed),
            lifecycle_seed_digest: candidate.lifecycle_seed_digest,
            lifecycle_generation_id: candidate.lifecycle_generation_id,
          }),
        ),
      },
    )];
    delete lifecycleEvidence.evidence_digest;
    lifecycleEvidence.evidence_digest = digestCanonical(
      "codex-review-gate-v2-current-open-generation-evidence",
      lifecycleEvidence,
    );
    lifecycleGenerationEntry.candidate_inventory_attachment_evidence =
      lifecycleEvidence;
    const lifecycleGenerationPayload = structuredClone(
      lifecycleGenerationEntry.envelope.payload,
    );
    lifecycleGenerationPayload.lifecycle_candidate_set_digest =
      lifecycleEvidence.lifecycle_candidate_set_digest;
    lifecycleGenerationPayload.dispatch_batch_digests =
      structuredClone(lifecycleEvidence.dispatch_batch_digests);
    lifecycleGenerationPayload.attachment_manifest = resealGenerationManifest({
      ...lifecycleGenerationPayload.attachment_manifest,
      lifecycle_candidate_set_digest:
        lifecycleEvidence.lifecycle_candidate_set_digest,
      dispatch_batch_digests:
        structuredClone(lifecycleEvidence.dispatch_batch_digests),
      evidence_digest: lifecycleEvidence.evidence_digest,
    });
    resealFixtureCandidateInventoryEnvelopePayload(
      lifecycleGenerationEntry.envelope,
      lifecycleGenerationPayload,
    );
    assert.throws(
      () => deriveV2GitLedgerCandidateInventoryAuthority(
        lifecycleGenerationTamper,
        REPOSITORY,
      ),
      (error) => error.code === "candidate-inventory-lifecycle-generation",
    );
    const rawGenerationRecord = createV2GitLedgerCandidateInventoryRecord({
      predecessor_commit_sha: loaded.tip_commit_sha,
      owner: generationEntry.envelope.payload.owner,
      server_observed_at: loaded.post_ref.server_time,
      payload: generationEntry.envelope.payload,
    });
    const rawBefore = {
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    };
    await assert.rejects(
      restarted.productionApiForTest().appendRecord(rawGenerationRecord),
      (error) => error.code === "UNTRUSTED_CANDIDATE_INVENTORY_APPEND",
    );
    assert.deepEqual({
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    }, rawBefore);
  });

test("current-open v3 reconciliation suppresses only a durable repeated empty generation",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt({
      allowedEventNames: ["pull_request_target", "schedule"],
    });
    const triggerIdentity = scheduleTriggerIdentity();
    const currentOpen = currentOpenSuppressionAdapterFixture();
    let legacyFinishCalls = 0;
    const refreshAdapter = Object.freeze({
      schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
      schema_version: 2,
      configuration_digest: rawDigest(
        "candidate-current-open-v3-empty-suppression-configuration-v1",
      ),
      collectCurrentOpenCandidateAuthority:
        currentOpen.collectProductionCandidateAuthority,
      async finishLegacyCandidateInventoryAttempt() {
        legacyFinishCalls += 1;
        throw new Error("current-open empty suppression used legacy REST");
      },
    });
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
      deriveTriggerFromRequest: true,
    });
    const ledger = makeLedger(fixture, capability, verifier, null, {
      candidateInventoryRefreshAdapter: refreshAdapter,
    });
    await ledger.bootstrapCapability();
    const historicalDiscovery = await acquireDefaultDiscovery(ledger);
    const historicalIntent = effectRecord({
      predecessor: historicalDiscovery.lease_receipt.acquire_commit_sha,
      lease: historicalDiscovery.lease_receipt,
      effectId: "historical-empty-suppression-effect",
      idempotencyKey: "historical-empty-suppression-effect-key",
      at: TIME,
    });
    const historicalIntentAppend = await appendHistoricalEffectRecord(
      ledger,
      historicalIntent,
      historicalDiscovery.effect_evaluated_scope_receipt,
    );
    const historicalResponseTime =
      historicalIntentAppend.ref_reread.server_time;
    const historicalResponse = createV2GitLedgerEffectResponseRecord({
      intent_record: historicalIntent,
      intent_commit_sha: historicalIntentAppend.commit_sha,
      server_observed_at: historicalResponseTime,
      receipt: {
        is_resolved: true,
        response_server_time: historicalResponseTime,
        run_id: OWNER.run_id,
        raw_body_sha256: rawDigest(
          "historical-empty-suppression-effect-response-v1",
        ),
      },
    });
    await appendHistoricalEffectRecord(
      ledger,
      historicalResponse,
      historicalDiscovery.effect_evaluated_scope_receipt,
    );
    await releaseFixtureDiscoveryLease(ledger, historicalDiscovery);
    const historical = await ledger.load();
    assert.equal(historical.effect_intent_count, 1);
    assert.equal(historical.effect_response_count, 1);
    const input = () => ({
      workflow_command_handle: createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    const firstInput = input();
    firstInput.workflow_command_handle = await firstInput.workflow_command_handle;
    const first = await ledger.reconcileCandidateInventoryRefreshAtomically(
      firstInput,
    );
    assert.equal(first.state, "persisted");
    assert.equal(first.publication_result.published_record_commit_shas.length, 2);
    const before = {
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    };
    const callsBefore = currentOpen.calls.length;
    const secondInput = input();
    secondInput.workflow_command_handle =
      await secondInput.workflow_command_handle;

    const second = await ledger.reconcileCandidateInventoryRefreshAtomically(
      secondInput,
    );

    assert.equal(second.schema_version, 2);
    assert.equal(second.state, "suppressed");
    assert.equal(second.reason, "semantic-unchanged-not-due");
    assert.equal(second.persistence_mode, null);
    assert.equal(second.publication_result, null);
    assert.equal(second.suppression_result.state, "suppressed");
    assert.equal(
      second.suppression_result.assurance,
      "bounded-sampled-empty-at-observation-boundary",
    );
    assert.equal(second.suppression_result.github_atomic_snapshot, false);
    assert.equal(second.suppression_result.github_ref_fence, null);
    assert.equal(second.suppression_result.writes_performed, false);
    assert.equal(currentOpen.calls.length - callsBefore, 4);
    assert.equal(legacyFinishCalls, 0);
    assert.deepEqual({
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    }, before);
  });

test("current-open v3 reconciliation rejects untrusted live authority pairs before Git writes",
  async (t) => {
    const cases = [
      {
        name: "cloned pair",
        code: "UNTRUSTED_CURRENT_OPEN_PRODUCTION_AUTHORITY_HANDLE",
        collect: async (left) => {
          const exact = await left.collectProductionCandidateAuthority();
          return Object.freeze({
            projection: exact.projection,
            production_candidate_authority: structuredClone(
              exact.production_candidate_authority,
            ),
          });
        },
      },
      {
        name: "cross-factory same-set pair",
        code: "UNTRUSTED_CURRENT_OPEN_PRODUCTION_AUTHORITY_PAIR",
        collect: async (left, right) => {
          const leftPair = await left.collectProductionCandidateAuthority();
          const rightPair = await right.collectProductionCandidateAuthority();
          return Object.freeze({
            projection: leftPair.projection,
            production_candidate_authority:
              rightPair.production_candidate_authority,
          });
        },
      },
      {
        name: "stale same-factory pair",
        code: "STALE_CURRENT_OPEN_PRODUCTION_AUTHORITY_HANDLE",
        collect: async (left) => {
          const stale = await left.collectProductionCandidateAuthority();
          await left.collectProductionCandidateAuthority();
          return stale;
        },
      },
    ];
    for (const testCase of cases) {
      await t.test(testCase.name, async () => {
        const fixture = githubGitFixture();
        const capability = scheduleCapabilityReceipt();
        const triggerIdentity = scheduleTriggerIdentity();
        const left = currentOpenSuppressionAdapterFixture({
          initialNumbers: [PR.number],
        });
        const right = currentOpenSuppressionAdapterFixture({
          initialNumbers: [PR.number],
        });
        const refreshAdapter = Object.freeze({
          schema:
            "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
          schema_version: 2,
          configuration_digest: rawDigest(
            `candidate-current-open-v3-${testCase.name}-configuration-v1`,
          ),
          collectCurrentOpenCandidateAuthority: () =>
            testCase.collect(left, right),
          async finishLegacyCandidateInventoryAttempt() {
            throw new Error("untrusted current-open pair used legacy REST");
          },
        });
        const verifier = provenanceVerifier({
          eventName: "schedule",
          ref: triggerIdentity.ref,
          shaValue: triggerIdentity.sha,
        });
        const ledger = makeLedger(fixture, capability, verifier, null, {
          candidateInventoryRefreshAdapter: refreshAdapter,
        });
        await ledger.bootstrapCapability();
        const input = {
          workflow_command_handle:
            await createScheduleWorkflowCommandHandle(),
          trigger_identity: triggerIdentity,
          repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        };
        const before = {
          writes: fixture.writeCalls,
          blobs: fixture.blobs.size,
          trees: fixture.trees.size,
          commits: fixture.commits.size,
          ref: fixture.refTarget,
        };
        await assert.rejects(
          ledger.reconcileCandidateInventoryRefreshAtomically(input),
          (error) => error.code === testCase.code,
        );
        assert.deepEqual({
          writes: fixture.writeCalls,
          blobs: fixture.blobs.size,
          trees: fixture.trees.size,
          commits: fixture.commits.size,
          ref: fixture.refTarget,
        }, before);
      });
    }
  });

test("current-open v3 rejects observations older than durable authority",
  async (t) => {
    const refreshAdapter = (label, collect) => Object.freeze({
      schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
      schema_version: 2,
      configuration_digest: rawDigest(
        `candidate-current-open-v3-observation-${label}-configuration-v1`,
      ),
      collectCurrentOpenCandidateAuthority: collect,
      async finishLegacyCandidateInventoryAttempt() {
        throw new Error("stale current-open observation used legacy REST");
      },
    });
    const reconciliationInput = async (triggerIdentity) => ({
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    const snapshotWrites = (fixture) => ({
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    });
    const assertRejectedBeforeConsumption = async ({
      ledger,
      fixture,
      triggerIdentity,
      returnedPairs,
    }) => {
      const before = snapshotWrites(fixture);
      for (let attempt = 0; attempt < 2; attempt += 1) {
        const pairCountBefore = returnedPairs.length;
        await assert.rejects(
          ledger.reconcileCandidateInventoryRefreshAtomically(
            await reconciliationInput(triggerIdentity),
          ),
          (error) => error.code === "candidate-inventory-time",
        );
        assert.equal(returnedPairs.length, pairCountBefore + 1);
        const pair = returnedPairs.at(-1);
        assert.equal(
          consumeV2CurrentOpenProductionCandidateAuthorityHandle(
            pair.production_candidate_authority,
            pair.projection,
          ),
          pair.production_candidate_authority,
        );
        assert.deepEqual(snapshotWrites(fixture), before);
      }
    };

    await t.test("v3 predecessor", async () => {
      const fixture = githubGitFixture();
      const capability = scheduleCapabilityReceipt();
      const triggerIdentity = scheduleTriggerIdentity();
      const verifier = provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
      });
      const staleCurrentOpen = currentOpenSuppressionAdapterFixture({
        initialNumbers: [PR.number],
      });
      const stalePairs = [];
      const laterCurrentOpen = currentOpenSuppressionAdapterFixture();
      for (let scan = 0; scan < 3; scan += 1) {
        await laterCurrentOpen.collectProductionCandidateAuthority();
      }
      laterCurrentOpen.setListedNumbers([]);
      const publisher = makeLedger(fixture, capability, verifier, null, {
        candidateInventoryRefreshAdapter: refreshAdapter(
          "v3-predecessor-publisher",
          laterCurrentOpen.collectProductionCandidateAuthority,
        ),
      });
      await publisher.bootstrapCapability();
      await publisher.reconcileCandidateInventoryRefreshAtomically(
        await reconciliationInput(triggerIdentity),
      );
      const durable = await publisher.load();
      assert.ok(Date.parse(
        durable.authority_projection.candidate_inventory.completed_generation
          .observed_at,
      ) > Date.parse(TIME));
      const staleLedger = makeLedger(fixture, capability, verifier, null, {
        candidateInventoryRefreshAdapter: refreshAdapter(
          "v3-predecessor-stale",
          async () => {
            const pair = await staleCurrentOpen
              .collectProductionCandidateAuthority();
            stalePairs.push(pair);
            return pair;
          },
        ),
      });
      await assertRejectedBeforeConsumption({
        ledger: staleLedger,
        fixture,
        triggerIdentity,
        returnedPairs: stalePairs,
      });
    });

    await t.test("legacy predecessor", async () => {
      const fixture = githubGitFixture();
      const capability = scheduleCapabilityReceipt();
      const triggerIdentity = scheduleTriggerIdentity();
      const verifier = provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
      });
      const staleCurrentOpen = currentOpenSuppressionAdapterFixture({
        initialNumbers: [PR.number],
      });
      const stalePairs = [];
      const ledger = makeLedger(fixture, capability, verifier, null, {
        candidateInventoryRefreshAdapter: refreshAdapter(
          "legacy-predecessor-stale",
          async () => {
            const pair = await staleCurrentOpen
              .collectProductionCandidateAuthority();
            stalePairs.push(pair);
            return pair;
          },
        ),
      });
      await ledger.bootstrapCapability();
      const legacyTransport = candidateTransportFixture(0);
      legacyTransport.advanceServerTime(10);
      const completed = await appendCompletedCandidateInventory({
        ledger,
        transportFixture: legacyTransport,
      });
      assert.ok(Date.parse(
        completed.authority_projection.candidate_inventory.completed_cycle
          .cycle_receipt.observed_at,
      ) > Date.parse(TIME));
      await ledger.loadOrReserveCandidateDispatch(
        await reconciliationInput(triggerIdentity),
      );
      await assertRejectedBeforeConsumption({
        ledger,
        fixture,
        triggerIdentity,
        returnedPairs: stalePairs,
      });
    });
  });

test("current-open v3 generation rejects projection and timestamp aliases",
  async (t) => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const currentOpen = currentOpenSuppressionAdapterFixture({
      initialNumbers: [PR.number],
    });
    const refreshAdapter = Object.freeze({
      schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
      schema_version: 2,
      configuration_digest: rawDigest(
        "candidate-current-open-v3-generation-reseal-configuration-v1",
      ),
      collectCurrentOpenCandidateAuthority:
        currentOpen.collectProductionCandidateAuthority,
      async finishLegacyCandidateInventoryAttempt() {
        throw new Error("generation reseal used legacy REST");
      },
    });
    const ledger = makeLedger(
      fixture,
      capability,
      provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
      }),
      null,
      { candidateInventoryRefreshAdapter: refreshAdapter },
    );
    await ledger.bootstrapCapability();
    await ledger.reconcileCandidateInventoryRefreshAtomically({
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    const loaded = await ledger.load();
    const generationIndex = loaded.records.findIndex((entry) =>
      entry.envelope.record_type === "candidate-inventory-observation" &&
      entry.envelope.payload.schema_version === 3);
    assert.notEqual(generationIndex, -1);
    const generationEntry = loaded.records[generationIndex];
    const evidence = durableCurrentOpenGenerationAttachmentValue(
      fixture,
      generationEntry.envelope.payload.attachment_manifest,
    );
    assert.equal(
      evidence.current_open_projection_digest,
      generationEntry.envelope.payload.current_open_projection_digest,
    );
    assert.equal(
      generationEntry.envelope.payload.attachment_manifest
        .current_open_projection_digest,
      generationEntry.envelope.payload.current_open_projection_digest,
    );
    const prefix = loaded.records.slice(0, generationIndex + 1)
      .map((entry, index) => index === generationIndex
        ? {
            ...structuredClone(entry),
            candidate_inventory_attachment_evidence:
              structuredClone(evidence),
          }
        : structuredClone(entry));

    await t.test("projection digest", () => {
      const forged = structuredClone(prefix);
      const entry = forged[generationIndex];
      const forgedProjectionDigest = `sha256:${"f".repeat(64)}`;
      const forgedEvidence = structuredClone(
        entry.candidate_inventory_attachment_evidence,
      );
      forgedEvidence.current_open_projection_digest = forgedProjectionDigest;
      delete forgedEvidence.evidence_digest;
      forgedEvidence.evidence_digest = digestCanonical(
        "codex-review-gate-v2-current-open-generation-evidence",
        forgedEvidence,
      );
      entry.candidate_inventory_attachment_evidence = forgedEvidence;
      const payload = structuredClone(entry.envelope.payload);
      payload.current_open_projection_digest = forgedProjectionDigest;
      const manifest = structuredClone(payload.attachment_manifest);
      manifest.current_open_projection_digest = forgedProjectionDigest;
      manifest.evidence_digest = forgedEvidence.evidence_digest;
      delete manifest.manifest_digest;
      manifest.manifest_digest = digestCanonical(
        "codex-review-gate-v2-current-open-generation-attachment-manifest",
        manifest,
      );
      payload.attachment_manifest = manifest;
      resealFixtureCandidateInventoryEnvelopePayload(entry.envelope, payload);
      assert.throws(
        () => deriveV2GitLedgerCandidateInventoryAuthority(
          forged,
          REPOSITORY,
        ),
        (error) => error.code === "candidate-inventory-source-generation",
      );
    });

    for (const field of ["started_at", "completed_at", "observed_at"]) {
      await t.test(`${field} canonical timestamp`, () => {
        const forged = structuredClone(prefix);
        const entry = forged[generationIndex];
        const payload = structuredClone(entry.envelope.payload);
        if (field === "observed_at") {
          payload.observed_at = payload.observed_at.replace(".000Z", "Z");
        } else {
          payload.observation_window[field] =
            payload.observation_window[field].replace(".000Z", "Z");
        }
        assert.throws(
          () => {
            resealFixtureCandidateInventoryEnvelopePayload(
              entry.envelope,
              payload,
            );
            deriveV2GitLedgerCandidateInventoryAuthority(
              forged,
              REPOSITORY,
            );
          },
          /canonical/u,
        );
      });
    }
  });

test("current-open v3 dispatch binds whole-source and lifecycle identities",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const currentOpen = currentOpenSuppressionAdapterFixture({
      initialNumbers: [PR.number],
    });
    const refreshAdapter = Object.freeze({
      schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
      schema_version: 2,
      configuration_digest: rawDigest(
        "candidate-current-open-v3-source-binding-configuration-v1",
      ),
      collectCurrentOpenCandidateAuthority:
        currentOpen.collectProductionCandidateAuthority,
      async finishLegacyCandidateInventoryAttempt() {
        throw new Error("current-open source binding used legacy REST");
      },
    });
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    });
    const ledger = makeLedger(fixture, capability, verifier, null, {
      candidateInventoryRefreshAdapter: refreshAdapter,
    });
    await ledger.bootstrapCapability();
    await ledger.reconcileCandidateInventoryRefreshAtomically({
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    const loaded = await ledger.load();
    const generation = loaded.authority_projection.candidate_inventory
      .completed_generation;
    const reservation = loaded.authority_projection.candidate_dispatch
      .active_reservation.reservation;

    assert.deepEqual(reservation.candidate_source, {
      schema: "codex-review-gate-git-ledger-candidate-dispatch-source-v2",
      schema_version: 2,
      source_profile: generation.source_profile,
      source_generation_id: generation.generation_id,
      source_generation_record_oid: generation.generation_record_oid,
      source_generation_digest: generation.generation_digest,
      production_candidate_authority_digest:
        generation.source_authority_digest,
      candidate_set_digest: generation.candidate_set_digest,
      source_current_open_semantic_digest: generation.source_semantic_digest,
      lifecycle_candidate_set_digest:
        generation.lifecycle_candidate_set_digest,
    });
    assert.equal(
      validateV2GitLedgerCandidateDispatchReservation(reservation)
        .candidates[0].lifecycle_seed.updated_at,
      reservation.candidates[0].lifecycle_seed.updated_at,
    );

    const timestampTamper = structuredClone(reservation);
    timestampTamper.candidates[0].lifecycle_seed.updated_at =
      timestampTamper.candidates[0].lifecycle_seed.updated_at.replace(
        ".000Z",
        "Z",
      );
    timestampTamper.candidates[0].lifecycle_seed_digest = rawDigest(
      `codex-review-gate-v2-production-candidate-lifecycle-seed\n${canonicalJson({
        identity: timestampTamper.candidates[0].identity,
        lifecycle_seed: timestampTamper.candidates[0].lifecycle_seed,
      })}\n`,
    );
    timestampTamper.candidates[0].selection_digest = digestCanonical(
      "codex-review-gate-v2-current-open-dispatch-selection",
      {
        source_generation_record_oid:
          timestampTamper.candidates[0].source_generation_record_oid,
        identity_digest: timestampTamper.candidates[0].identity_digest,
        lifecycle_seed_digest:
          timestampTamper.candidates[0].lifecycle_seed_digest,
        lifecycle_generation_id:
          timestampTamper.candidates[0].lifecycle_generation_id,
      },
    );
    timestampTamper.dispatch_digest = digestCanonical(
      "codex-review-gate-v2-current-open-candidate-dispatch-selection",
      {
        repository: timestampTamper.repository,
        generation_id: timestampTamper.generation_id,
        cycle_id: timestampTamper.cycle_id,
        candidate_source: timestampTamper.candidate_source,
        candidate_inventory_authority_digest:
          timestampTamper.candidate_inventory_authority_digest,
        inventory_digest: timestampTamper.inventory_digest,
        batch_index: timestampTamper.batch_index,
        batch_count: timestampTamper.batch_count,
        candidate_offset: timestampTamper.candidate_offset,
        candidates: timestampTamper.candidates,
      },
    );
    delete timestampTamper.reservation_digest;
    timestampTamper.reservation_digest = digestCanonical(
      "codex-review-gate-v2-current-open-candidate-dispatch-reservation",
      timestampTamper,
    );
    assert.throws(
      () => validateV2GitLedgerCandidateDispatchReservation(timestampTamper),
      /lifecycle_seed\.updated_at.*canonical/u,
    );

    const lifecycleTamper = structuredClone(reservation);
    lifecycleTamper.candidates[0].lifecycle_generation_id =
      `candidate-lifecycle:${"f".repeat(64)}`;
    lifecycleTamper.candidates[0].selection_digest = digestCanonical(
      "codex-review-gate-v2-current-open-dispatch-selection",
      {
        source_generation_record_oid:
          lifecycleTamper.candidates[0].source_generation_record_oid,
        identity_digest: lifecycleTamper.candidates[0].identity_digest,
        lifecycle_seed_digest:
          lifecycleTamper.candidates[0].lifecycle_seed_digest,
        lifecycle_generation_id:
          lifecycleTamper.candidates[0].lifecycle_generation_id,
      },
    );
    lifecycleTamper.dispatch_digest = digestCanonical(
      "codex-review-gate-v2-current-open-candidate-dispatch-selection",
      {
        repository: lifecycleTamper.repository,
        generation_id: lifecycleTamper.generation_id,
        cycle_id: lifecycleTamper.cycle_id,
        candidate_source: lifecycleTamper.candidate_source,
        candidate_inventory_authority_digest:
          lifecycleTamper.candidate_inventory_authority_digest,
        inventory_digest: lifecycleTamper.inventory_digest,
        batch_index: lifecycleTamper.batch_index,
        batch_count: lifecycleTamper.batch_count,
        candidate_offset: lifecycleTamper.candidate_offset,
        candidates: lifecycleTamper.candidates,
      },
    );
    delete lifecycleTamper.reservation_digest;
    lifecycleTamper.reservation_digest = digestCanonical(
      "codex-review-gate-v2-current-open-candidate-dispatch-reservation",
      lifecycleTamper,
    );
    assert.throws(
      () => validateV2GitLedgerCandidateDispatchReservation(lifecycleTamper),
      /changes its lifecycle identity/u,
    );

    const sourceTamper = structuredClone(reservation);
    sourceTamper.candidate_source.source_generation_record_oid = "f".repeat(40);
    sourceTamper.candidate_source.production_candidate_authority_digest =
      `sha256:${"f".repeat(64)}`;
    assert.throws(
      () => validateV2GitLedgerCandidateDispatchReservation(sourceTamper),
      /candidate changes its source generation/u,
    );
  });

test("current-open v3 reservation bindings survive every durable acknowledgement",
  async (t) => {
    const createAttempt = async (
      candidateCount,
      label,
      { configureCurrentOpen = null } = {},
    ) => {
      const fixture = githubGitFixture();
      const capability = scheduleCapabilityReceipt();
      const triggerIdentity = scheduleTriggerIdentity();
      const runIdentity = { ...OWNER };
      const preflight = await loadFixtureProviderPreflight();
      const currentOpen = currentOpenSuppressionAdapterFixture({
        initialNumbers: Array.from(
          { length: candidateCount },
          (_, index) => PR.number + index,
        ),
        exactScopeCandidateNumber: PR.number,
      });
      configureCurrentOpen?.(currentOpen);
      let publicationPair = null;
      currentOpen.setAfterProjection((pair) => {
        publicationPair = pair;
        currentOpen.setAfterProjection(null);
      });
      let legacyFinishCalls = 0;
      const refreshAdapter = Object.freeze({
        schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
        schema_version: 2,
        configuration_digest: rawDigest(
          `candidate-current-open-v3-${label}-configuration-v1`,
        ),
        collectCurrentOpenCandidateAuthority:
          currentOpen.collectProductionCandidateAuthority,
        async finishLegacyCandidateInventoryAttempt() {
          legacyFinishCalls += 1;
          throw new Error("current-open sequential dispatch used legacy REST");
        },
      });
      const verifier = provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
        runIdentity,
      });
      const ledger = makeLedger(
        fixture,
        capability,
        verifier,
        preflight,
        { candidateInventoryRefreshAdapter: refreshAdapter },
      );
      await ledger.bootstrapCapability();
      const scanCommand = await createScheduleWorkflowCommandHandle({
        runIdentity,
      });
      const input = {
        workflow_command_handle: scanCommand,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      };
      await ledger.reconcileCandidateInventoryRefreshAtomically(input);
      const reserved = await ledger.loadOrReserveCandidateDispatch(input);
      const originalBindings = reserved.plan.items.map((_, index) =>
        projectV2GitLedgerCandidateDispatchBinding(reserved.plan, index));
      assert.equal(originalBindings.length, Math.min(candidateCount, 64));
      assert.equal(new Set(originalBindings.map((binding) =>
        binding.candidate_dispatch_authority_digest)).size, 1);
      if (candidateCount === 64) {
        const planBytes = Buffer.byteLength(
          canonicalJson(reserved.plan),
          "utf8",
        );
        const bindingBytes = originalBindings.map((binding) =>
          Buffer.byteLength(canonicalJson(binding), "utf8"));
        assert.equal(planBytes, 69_644);
        assert.ok(planBytes <=
          MAX_V2_CURRENT_OPEN_CANDIDATE_DISPATCH_PLAN_BYTES);
        assert.equal(Math.max(...bindingBytes), 2_996);
        assert.ok(bindingBytes.every((bytes) =>
          bytes <= MAX_V2_CURRENT_OPEN_CANDIDATE_DISPATCH_BINDING_BYTES));
      }
      return {
        fixture,
        capability,
        triggerIdentity,
        runIdentity,
        preflight,
        refreshAdapter,
        verifier,
        input,
        originalBindings,
        currentOpen,
        publicationPair,
        legacyFinishCalls: () => legacyFinishCalls,
      };
    };

    const makeRestartedLedger = (attempt) => makeLedger(
      attempt.fixture,
      attempt.capability,
      attempt.verifier,
      attempt.preflight,
      { candidateInventoryRefreshAdapter: attempt.refreshAdapter },
    );

    const loadWithProjectionBuildCounts = async (ledger) => {
      const originalStructuredClone = globalThis.structuredClone;
      const originalFlatMap = Array.prototype.flatMap;
      const counts = { inventory: 0, dispatch: 0, attempt_prefix_scans: 0 };
      globalThis.structuredClone = function instrumentedStructuredClone(
        value,
        ...options
      ) {
        if (
          Array.isArray(value) && value.length > 0 &&
          value.every((item) =>
            item !== null && typeof item === "object" &&
            Object.hasOwn(item, "record_oid") &&
            Object.hasOwn(item, "payload_digest"))
        ) {
          const frames = new Error().stack?.split("\n") ?? [];
          const buildIndex = frames.findIndex((frame) =>
            frame.includes("buildCandidateInventoryAuthority") ||
            frame.includes("buildCandidateDispatchAuthority"));
          const buildFrame = frames[buildIndex] ?? "";
          const callerFrame = frames[buildIndex + 1] ?? "";
          const directProjectionBuild =
            callerFrame.includes("projectChainState") ||
            callerFrame.includes("currentCandidateInventoryAuthority") ||
            callerFrame.includes("currentCandidateDispatchAuthority");
          if (directProjectionBuild) {
            if (buildFrame.includes("buildCandidateInventoryAuthority")) {
              counts.inventory += 1;
            } else if (buildFrame.includes("buildCandidateDispatchAuthority")) {
              counts.dispatch += 1;
            }
          }
        }
        return Reflect.apply(originalStructuredClone, this, [value, ...options]);
      };
      Array.prototype.flatMap = function instrumentedFlatMap(...args) {
        const frames = new Error().stack?.split("\n") ?? [];
        if (
          frames.some((frame) =>
            frame.includes("candidateDispatchScheduledAcquireAttempts")) &&
          frames.some((frame) => frame.includes("projectChainState"))
        ) {
          counts.attempt_prefix_scans += 1;
        }
        return Reflect.apply(originalFlatMap, this, args);
      };
      try {
        return { loaded: await ledger.load(), counts };
      } finally {
        Array.prototype.flatMap = originalFlatMap;
        globalThis.structuredClone = originalStructuredClone;
      }
    };

    const prepareCandidate = async (
      attempt,
      candidateIndex,
      runId,
      { full },
    ) => {
      const ledger = makeRestartedLedger(attempt);
      const binding = attempt.originalBindings[candidateIndex];
      const candidate = binding.candidate;
      const number = candidate.identity.number;
      const nodeId = candidate.identity.node_id;
      const headOid = candidate.lifecycle_seed.head.sha;
      const headRefName = candidate.lifecycle_seed.head.ref;
      attempt.runIdentity.run_id = runId;
      attempt.runIdentity.run_attempt = 1;
      const scheduledCommand = await createScheduleWorkflowCommandHandle({
        pullRequestNumber: number,
        dispatchBinding: binding,
        runIdentity: attempt.runIdentity,
      });
      const minimal = await loadFixtureMinimalScopeForCandidate(
        number,
        headOid,
        headRefName,
        nodeId,
        TIME,
      );
      const scheduled = await ledger
        .loadCandidateDispatchForScheduledPullRequest({
          workflow_command_handle: scheduledCommand,
          minimal_scope_handle: minimal,
          trigger_identity: attempt.triggerIdentity,
          expected_dispatch_binding: binding,
        });
      const preScopeReceipt = await ledger
        .loadScheduledPullRequestEvaluatedScopeReceipt({
          candidate_dispatch_handle: scheduled.candidate_dispatch_handle,
          minimal_scope_handle: minimal,
          trigger_identity: attempt.triggerIdentity,
        });
      const prepared = {
        ...attempt,
        ledger,
        binding,
        candidateIndex,
        scheduledCommand,
        scheduled,
        minimal,
        preScopeReceipt,
        schedulerScope: {
          pullRequest: { number, node_id: nodeId },
          headOid,
          baseOid: candidate.lifecycle_seed.base.sha,
          potentialMergeCommitOid: POTENTIAL,
          statusIdempotencyKey: `status:fixture-head-pending-${number}`,
        },
      };
      if (!full) {
        const leaseReceipt = await ledger.acquireLease({
          predecessor_commit_sha:
            scheduled.reservation_receipt.source_tip_commit_sha,
          pull_request: { number, node_id: nodeId },
          head_ref_oid: headOid,
          base_ref_oid: candidate.lifecycle_seed.base.sha,
          potential_merge_commit_oid: POTENTIAL,
          lease_id: `lease:${runId}:1:${minimal.receipt_digest.slice(-16)}`,
          owner: structuredClone(attempt.runIdentity),
          observed_at: minimal.observed_at,
          lease_ttl_seconds: 900,
          control_comment_binding: null,
        }, {
          evaluated_scope_receipt: preScopeReceipt,
        });
        await ledger.releaseLease({
          predecessor_commit_sha: leaseReceipt.acquire_commit_sha,
          lease_receipt: leaseReceipt,
          released_at: leaseReceipt.acquired_at,
          control_comment_binding: null,
        }, {
          evaluated_scope_receipt: preScopeReceipt,
        });
        return { ...prepared, discovery: null };
      }
      let discoveryServerTime = Date.parse(TIME);
      const nextDiscoveryServerTime = () => {
        discoveryServerTime = Math.max(
          discoveryServerTime + 1_000,
          Date.parse(attempt.fixture.peekServerTime()) + 1_000,
        );
        return new Date(discoveryServerTime).toISOString();
      };
      const discovery = await acquireFixtureLeasedDiscovery({
        ledger,
        command: scheduledCommand,
        preScopeReceipt,
        minimalPre: minimal,
        minimalFetch: (url, init) => minimalScopeFetchForCandidate({
          number,
          nodeId,
          headOid,
          headRefName,
          serverTime: nextDiscoveryServerTime(),
          urlValue: url,
          init,
        }),
        transport: createProviderTransport(providerTransportFixture({
          nextServerTime: nextDiscoveryServerTime,
          comment: null,
          headOid,
          headRefName,
          pullRequestNumber: number,
          pullRequestNodeId: nodeId,
        }).fetch),
      });
      return {
        ...prepared,
        discovery,
      };
    };

    const projectRemainingBindings = (plan) => plan === null
      ? null
      : plan.items.map((_, index) =>
        projectV2GitLedgerCandidateDispatchBinding(plan, index));

    const executeCandidate = async (attempt, candidateIndex, {
      full,
      expectCycleComplete,
      runId,
      decision = "pending",
    }) => {
      const prepared = await prepareCandidate(
        attempt,
        candidateIndex,
        runId,
        { full },
      );
      prepared.schedulerScope.decision = decision;
      let result;
      if (full) {
        const completed = await completeScheduledCandidateDispatchAttempt(
          prepared,
          { expectCycleComplete },
        );
        result = completed.ack;
      } else {
        result = await prepared.ledger.recoverCandidateDispatchFailure({
          workflow_command_handle: prepared.scheduledCommand,
          trigger_identity: prepared.triggerIdentity,
          repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
          expected_dispatch_binding: prepared.binding,
        });
        assert.equal(result.recovery_mode, "released");
        assert.equal(result.cycle_complete, expectCycleComplete);
      }
      return { prepared, result };
    };

    const assertTerminalCycle = (loaded, candidateCount, executions = null) => {
      const dispatchRecords = loaded.records.filter((entry) =>
        entry.envelope.record_type === "candidate-dispatch-observation");
      const acks = dispatchRecords.filter((entry) =>
        entry.envelope.payload.phase === "candidate-ack");
      assert.equal(acks.length, candidateCount);
      assert.deepEqual(
        [...acks.map((entry) =>
          entry.envelope.payload.candidate_ack.candidate_index)].sort(
          (left, right) => left - right,
        ),
        Array.from({ length: candidateCount }, (_, index) => index),
      );
      if (executions !== null) {
        assert.equal(executions.length, candidateCount);
        for (const [ordinal, ack] of acks.entries()) {
          const execution = executions[ordinal];
          const payload = ack.envelope.payload;
          const expectedCurrentDigest = execution.prepared.preScopeReceipt
            .inventory_receipt.candidate_dispatch_authority_digest;
          assert.equal(
            payload.candidate_ack.candidate_index,
            execution.prepared.candidateIndex,
          );
          assert.equal(
            payload.prior_candidate_dispatch_authority_digest,
            expectedCurrentDigest,
          );
          assert.equal(
            payload.command_authority.pull_request_number,
            execution.prepared.binding.candidate.identity.number,
          );
          assert.deepEqual(
            payload.trigger_identity,
            execution.prepared.triggerIdentity,
          );
          if (ordinal + 1 < executions.length) {
            assert.equal(
              execution.result.dispatch_authority_digest,
              executions[ordinal + 1].prepared.preScopeReceipt
                .inventory_receipt.candidate_dispatch_authority_digest,
            );
          }
        }
        assert.equal(
          executions.at(-1).result.dispatch_authority_digest,
          loaded.authority_projection.candidate_dispatch.authority_digest,
        );
      }
      const batches = dispatchRecords.filter((entry) =>
        entry.envelope.payload.phase === "batch-complete");
      assert.equal(batches.length, 1);
      assert.deepEqual(
        batches[0].envelope.payload.batch_completion.ack_record_oids,
        [...acks]
          .sort((left, right) =>
            left.envelope.payload.candidate_ack.candidate_index -
              right.envelope.payload.candidate_ack.candidate_index)
          .map((entry) => entry.commit_sha),
      );
      assert.equal(dispatchRecords.filter((entry) =>
        entry.envelope.payload.phase === "cycle-complete").length, 1);
      assert.equal(
        loaded.authority_projection.candidate_dispatch.current_cycle
          .cycle_complete,
        true,
      );
    };

    await t.test(
      "terminal nonempty generation suppresses an equivalent fresh scan",
      async () => {
        const attempt = await createAttempt(
          1,
          "terminal-semantic-equivalence",
        );
        const execution = await executeCandidate(attempt, 0, {
          full: true,
          expectCycleComplete: true,
          runId: "9090",
          decision: "clean",
        });
        const beforeLoaded = await makeRestartedLedger(attempt).load();
        assertTerminalCycle(beforeLoaded, 1, [execution]);
        const before = {
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
          patches: attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
        };
        const currentOpenCallsBefore = attempt.currentOpen.calls.length;
        attempt.runIdentity.run_id = "9091";
        attempt.runIdentity.run_attempt = 1;
        const input = {
          ...attempt.input,
          workflow_command_handle: await createScheduleWorkflowCommandHandle({
            runIdentity: attempt.runIdentity,
          }),
        };

        const result = await makeRestartedLedger(attempt)
          .reconcileCandidateInventoryRefreshAtomically(input);

        assert.equal(result.schema_version, 2);
        assert.equal(result.state, "suppressed");
        assert.equal(result.reason, "semantic-unchanged-not-due");
        assert.equal(result.persistence_mode, null);
        assert.equal(result.publication_result, null);
        assert.equal(result.suppression_result.state, "suppressed");
        assert.equal(
          result.suppression_result.assurance,
          "authenticated-current-open-terminal-not-due",
        );
        assert.equal(result.suppression_result.writes_performed, false);
        assert.equal(Object.isFrozen(result), true);
        assert.equal(Object.isFrozen(result.suppression_result), true);
        assertNoCandidateRefreshPrivateAuthority(result);
        assert.equal(
          attempt.currentOpen.calls.length - currentOpenCallsBefore,
          4,
        );
        assert.equal(attempt.legacyFinishCalls(), 0);
        assert.deepEqual({
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
          patches: attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
        }, before);
        const afterLoaded = await makeRestartedLedger(attempt).load();
        assert.equal(
          afterLoaded.authority_projection.candidate_inventory
            .authority_digest,
          beforeLoaded.authority_projection.candidate_inventory
            .authority_digest,
        );
        assert.equal(
          afterLoaded.authority_projection.candidate_dispatch
            .authority_digest,
          beforeLoaded.authority_projection.candidate_dispatch
            .authority_digest,
        );
        assert.deepEqual(
          afterLoaded.authority_projection.candidate_dispatch.current_cycle,
          beforeLoaded.authority_projection.candidate_dispatch.current_cycle,
        );
      },
    );

    await t.test(
      "a cached live pair cannot authorize semantic suppression",
      async () => {
        const attempt = await createAttempt(1, "terminal-cached-live-pair");
        await executeCandidate(attempt, 0, {
          full: true,
          expectCycleComplete: true,
          runId: "9091",
          decision: "clean",
        });
        const cachedPair = await attempt.currentOpen
          .collectProductionCandidateAuthority();
        attempt.currentOpen.setListedNumbers([PR.number, PR.number + 1]);
        attempt.currentOpen.reuseLastProductionPair();
        const before = {
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
          patches: attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
        };
        const currentOpenCallsBefore = attempt.currentOpen.calls.length;
        attempt.runIdentity.run_id = "9092";
        attempt.runIdentity.run_attempt = 1;

        await assert.rejects(
          makeRestartedLedger(attempt)
            .reconcileCandidateInventoryRefreshAtomically({
              ...attempt.input,
              workflow_command_handle:
                await createScheduleWorkflowCommandHandle({
                  runIdentity: attempt.runIdentity,
                }),
            }),
          (error) =>
            error.code === "STALE_CURRENT_OPEN_INVENTORY_PROJECTION_HANDLE",
        );

        assert.equal(
          attempt.currentOpen.calls.length - currentOpenCallsBefore,
          0,
        );
        assert.equal(attempt.legacyFinishCalls(), 0);
        assert.deepEqual({
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
          patches: attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
        }, before);
        assert.equal(
          consumeV2CurrentOpenProductionCandidateAuthorityHandle(
            cachedPair.production_candidate_authority,
            cachedPair.projection,
          ),
          cachedPair.production_candidate_authority,
        );
        attempt.currentOpen.reuseLastProductionPair(false);
        attempt.runIdentity.run_id = "9093";
        const fresh = await makeRestartedLedger(attempt)
          .reconcileCandidateInventoryRefreshAtomically({
            ...attempt.input,
            workflow_command_handle: await createScheduleWorkflowCommandHandle({
              runIdentity: attempt.runIdentity,
            }),
          });
        assert.equal(fresh.state, "persisted");
        assert.equal(fresh.publication_result.candidate_count, 2);
        assert.equal(
          fresh.publication_result.published_record_commit_shas.length,
          2,
        );
      },
    );

    await t.test(
      "candidate-set and lifecycle changes are never suppressed",
      async (changeTest) => {
        const cases = [{
          label: "candidate-set change",
          mutate(currentOpen) {
            currentOpen.setListedNumbers([PR.number, PR.number + 1]);
          },
          expectedCount: 2,
        }, {
          label: "lifecycle seed change",
          mutate(currentOpen) {
            currentOpen.setHeadRefOid(PR.number, "3".repeat(40));
          },
          expectedCount: 1,
        }];
        for (const testCase of cases) {
          await changeTest.test(testCase.label, async () => {
            const attempt = await createAttempt(
              1,
              `terminal-${testCase.label.replaceAll(" ", "-")}`,
            );
            await executeCandidate(attempt, 0, {
              full: true,
              expectCycleComplete: true,
              runId: "9092",
              decision: "clean",
            });
            const before = {
              writes: attempt.fixture.writeCalls,
              commits: attempt.fixture.commits.size,
              ref: attempt.fixture.refTarget,
              patches: attempt.fixture.calls.filter(({ method }) =>
                method === "PATCH").length,
            };
            const currentOpenCallsBefore = attempt.currentOpen.calls.length;
            testCase.mutate(attempt.currentOpen);
            attempt.runIdentity.run_id = "9093";
            attempt.runIdentity.run_attempt = 1;
            const input = {
              ...attempt.input,
              workflow_command_handle:
                await createScheduleWorkflowCommandHandle({
                  runIdentity: attempt.runIdentity,
                }),
            };

            const result = await makeRestartedLedger(attempt)
              .reconcileCandidateInventoryRefreshAtomically(input);

            assert.equal(result.state, "persisted");
            assert.equal(result.suppression_result, null);
            assert.equal(
              result.publication_result.candidate_count,
              testCase.expectedCount,
            );
            assert.equal(
              result.publication_result.published_record_commit_shas.length,
              2,
            );
            assert.equal(
              attempt.currentOpen.calls.length - currentOpenCallsBefore,
              4,
            );
            assert.equal(attempt.legacyFinishCalls(), 0);
            assert.equal(attempt.fixture.writeCalls > before.writes, true);
            assert.equal(attempt.fixture.commits.size - before.commits, 2);
            assert.notEqual(attempt.fixture.refTarget, before.ref);
            assert.equal(
              attempt.fixture.calls.filter(({ method }) =>
                method === "PATCH").length - before.patches,
              1,
            );
          });
        }
      },
    );

    await t.test(
      "a due boundary crossed after the live scan falls back to publication",
      async () => {
        const attempt = await createAttempt(1, "terminal-due-boundary");
        await executeCandidate(attempt, 0, {
          full: true,
          expectCycleComplete: true,
          runId: "9094",
          decision: "clean",
        });
        const terminal = await makeRestartedLedger(attempt).load();
        const schedulerRecord = terminal.records.find((entry) =>
          entry.envelope.record_type === "effect-intent" &&
          entry.envelope.kind === "scheduler-observation");
        assert.notEqual(schedulerRecord, undefined);
        const dueAt = schedulerRecord.envelope.payload.action.scheduler_plan
          .due_at;
        assert.notEqual(dueAt, null);
        assert.ok(Date.parse(dueAt) > Date.parse(terminal.post_ref.server_time));
        const before = {
          writes: attempt.fixture.writeCalls,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
          patches: attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
        };
        const currentOpenCallsBefore = attempt.currentOpen.calls.length;
        let crossed = false;
        attempt.currentOpen.setAfterProjection(() => {
          if (crossed) return;
          crossed = true;
          attempt.fixture.advanceServerTime(Math.max(
            1,
            Math.ceil(
              (Date.parse(dueAt) -
                Date.parse(attempt.fixture.peekServerTime())) / 1_000,
            ) + 1,
          ));
        });
        attempt.runIdentity.run_id = "9095";
        attempt.runIdentity.run_attempt = 1;
        const input = {
          ...attempt.input,
          workflow_command_handle: await createScheduleWorkflowCommandHandle({
            runIdentity: attempt.runIdentity,
          }),
        };

        const result = await makeRestartedLedger(attempt)
          .reconcileCandidateInventoryRefreshAtomically(input);

        assert.equal(crossed, true);
        assert.equal(result.state, "persisted");
        assert.equal(result.suppression_result, null);
        assert.equal(
          result.publication_result.published_record_commit_shas.length,
          2,
        );
        assert.equal(
          attempt.currentOpen.calls.length - currentOpenCallsBefore,
          4,
        );
        assert.equal(attempt.legacyFinishCalls(), 0);
        assert.equal(attempt.fixture.writeCalls > before.writes, true);
        assert.equal(attempt.fixture.commits.size - before.commits, 2);
        assert.notEqual(attempt.fixture.refTarget, before.ref);
        assert.equal(
          attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length - before.patches,
          1,
        );
      },
    );

    await t.test(
      "a regressed final ref Date cannot hide a crossed due boundary",
      async () => {
        const attempt = await createAttempt(1, "terminal-ref-date-regression");
        await executeCandidate(attempt, 0, {
          full: true,
          expectCycleComplete: true,
          runId: "9096",
          decision: "clean",
        });
        const terminal = await makeRestartedLedger(attempt).load();
        const schedulerRecord = terminal.records.find((entry) =>
          entry.envelope.record_type === "effect-intent" &&
          entry.envelope.kind === "scheduler-observation");
        assert.notEqual(schedulerRecord, undefined);
        const dueAt = schedulerRecord.envelope.payload.action.scheduler_plan
          .due_at;
        assert.notEqual(dueAt, null);
        assert.ok(Date.parse(dueAt) > Date.parse(terminal.post_ref.server_time));
        let returnedPair = null;
        attempt.currentOpen.setAfterProjection((pair) => {
          returnedPair = pair;
          attempt.currentOpen.setAfterProjection(null);
          attempt.fixture.advanceServerTime(Math.max(
            1,
            Math.ceil(
              (Date.parse(dueAt) -
                Date.parse(attempt.fixture.peekServerTime())) / 1_000,
            ) + 1,
          ));
          attempt.fixture.overrideRefReadServerTimeAfter(
            3,
            pair.projection.receipt.observation_window.completed_at,
          );
        });
        const before = {
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
          patches: attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
        };
        const currentOpenCallsBefore = attempt.currentOpen.calls.length;
        attempt.runIdentity.run_id = "9097";
        attempt.runIdentity.run_attempt = 1;

        await assert.rejects(
          makeRestartedLedger(attempt)
            .reconcileCandidateInventoryRefreshAtomically({
              ...attempt.input,
              workflow_command_handle:
                await createScheduleWorkflowCommandHandle({
                  runIdentity: attempt.runIdentity,
                }),
            }),
          (error) => error.code === "candidate-inventory-cycle-stale",
        );

        assert.notEqual(returnedPair, null);
        assert.equal(
          attempt.currentOpen.calls.length - currentOpenCallsBefore,
          4,
        );
        assert.equal(attempt.legacyFinishCalls(), 0);
        assert.deepEqual({
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
          patches: attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
        }, before);
        assert.equal(
          consumeV2CurrentOpenProductionCandidateAuthorityHandle(
            returnedPair.production_candidate_authority,
            returnedPair.projection,
          ),
          returnedPair.production_candidate_authority,
        );
      },
    );

    await t.test(
      "a regressed publication write fence rejects before object staging",
      async () => {
        const attempt = await createAttempt(
          1,
          "terminal-publication-ref-date-regression",
        );
        await executeCandidate(attempt, 0, {
          full: true,
          expectCycleComplete: true,
          runId: "9098",
          decision: "clean",
        });
        attempt.currentOpen.setListedNumbers([PR.number, PR.number + 1]);
        let returnedPair = null;
        attempt.currentOpen.setAfterProjection((pair) => {
          returnedPair = pair;
          attempt.currentOpen.setAfterProjection(null);
          attempt.fixture.overrideRefReadServerTimeAfter(
            3,
            pair.projection.receipt.observation_window.completed_at,
          );
        });
        const before = {
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
          patches: attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
        };
        const currentOpenCallsBefore = attempt.currentOpen.calls.length;
        attempt.runIdentity.run_id = "9099";
        attempt.runIdentity.run_attempt = 1;

        await assert.rejects(
          makeRestartedLedger(attempt)
            .reconcileCandidateInventoryRefreshAtomically({
              ...attempt.input,
              workflow_command_handle:
                await createScheduleWorkflowCommandHandle({
                  runIdentity: attempt.runIdentity,
                }),
            }),
          (error) => error.code === "candidate-inventory-cycle-stale",
        );

        assert.notEqual(returnedPair, null);
        assert.equal(
          attempt.currentOpen.calls.length - currentOpenCallsBefore,
          4,
        );
        assert.equal(attempt.legacyFinishCalls(), 0);
        assert.deepEqual({
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
          patches: attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
        }, before);
        assert.equal(
          consumeV2CurrentOpenProductionCandidateAuthorityHandle(
            returnedPair.production_candidate_authority,
            returnedPair.projection,
          ),
          returnedPair.production_candidate_authority,
        );
      },
    );

    await t.test(
      "an unsettled reservation blocks refresh before another live scan",
      async () => {
        const attempt = await createAttempt(1, "unsettled-reservation");
        const before = {
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        };
        const currentOpenCallsBefore = attempt.currentOpen.calls.length;
        attempt.runIdentity.run_id = "9096";
        attempt.runIdentity.run_attempt = 1;
        await assert.rejects(
          makeRestartedLedger(attempt)
            .reconcileCandidateInventoryRefreshAtomically({
              ...attempt.input,
              workflow_command_handle:
                await createScheduleWorkflowCommandHandle({
                  runIdentity: attempt.runIdentity,
                }),
            }),
          (error) =>
            error.code === "candidate-inventory-cycle-dispatch-unsettled",
        );
        assert.equal(
          attempt.currentOpen.calls.length - currentOpenCallsBefore,
          0,
        );
        assert.equal(attempt.legacyFinishCalls(), 0);
        assert.deepEqual({
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        }, before);
      },
    );

    await t.test(
      "an active lease blocks refresh before another live scan",
      async () => {
        const attempt = await createAttempt(1, "active-lease");
        await prepareCandidate(attempt, 0, "9098", { full: true });
        const before = {
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        };
        const currentOpenCallsBefore = attempt.currentOpen.calls.length;
        attempt.runIdentity.run_id = "9099";
        attempt.runIdentity.run_attempt = 1;
        await assert.rejects(
          makeRestartedLedger(attempt)
            .reconcileCandidateInventoryRefreshAtomically({
              ...attempt.input,
              workflow_command_handle:
                await createScheduleWorkflowCommandHandle({
                  runIdentity: attempt.runIdentity,
                }),
            }),
          (error) => error.code === "candidate-inventory-cycle-lease-active",
        );
        assert.equal(
          attempt.currentOpen.calls.length - currentOpenCallsBefore,
          0,
        );
        assert.equal(attempt.legacyFinishCalls(), 0);
        assert.deepEqual({
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        }, before);
      },
    );

    await t.test(
      "a recovered terminal generation requires a fresh publication",
      async () => {
        const attempt = await createAttempt(1, "recovered-terminal");
        await executeCandidate(attempt, 0, {
          full: false,
          expectCycleComplete: true,
          runId: "9100",
        });
        const terminal = await makeRestartedLedger(attempt).load();
        const ack = terminal.records.find((entry) =>
          entry.envelope.record_type === "candidate-dispatch-observation" &&
          entry.envelope.payload.phase === "candidate-ack");
        assert.notEqual(ack, undefined);
        assert.equal(
          ack.envelope.payload.candidate_ack.terminal_authority.kind,
          "durable-prefix-recovery",
        );
        const before = {
          writes: attempt.fixture.writeCalls,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
          patches: attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
        };
        const currentOpenCallsBefore = attempt.currentOpen.calls.length;
        attempt.runIdentity.run_id = "9101";
        attempt.runIdentity.run_attempt = 1;

        const result = await makeRestartedLedger(attempt)
          .reconcileCandidateInventoryRefreshAtomically({
            ...attempt.input,
            workflow_command_handle:
              await createScheduleWorkflowCommandHandle({
                runIdentity: attempt.runIdentity,
              }),
          });

        assert.equal(result.state, "persisted");
        assert.equal(result.suppression_result, null);
        assert.equal(
          result.publication_result.published_record_commit_shas.length,
          2,
        );
        assert.equal(
          attempt.currentOpen.calls.length - currentOpenCallsBefore,
          4,
        );
        assert.equal(attempt.legacyFinishCalls(), 0);
        assert.equal(attempt.fixture.writeCalls > before.writes, true);
        assert.equal(attempt.fixture.commits.size - before.commits, 2);
        assert.notEqual(attempt.fixture.refTarget, before.ref);
        assert.equal(
          attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length - before.patches,
          1,
        );
      },
    );

    for (const order of [[0, 1], [1, 0]]) {
      await t.test(`two candidates complete in order ${order.join("-")}`,
        async () => {
          const attempt = await createAttempt(2, `order-${order.join("-")}`);
          const processed = new Set();
          const executions = [];
          const matrixRunId = order[0] === 0 ? "9100" : "9110";
          for (const candidateIndex of order) {
            const executed = await executeCandidate(attempt, candidateIndex, {
              full: true,
              expectCycleComplete: processed.size === 1,
              runId: matrixRunId,
            });
            executions.push(executed);
            processed.add(candidateIndex);
            const expected = attempt.originalBindings.filter(
              (_, index) => !processed.has(index),
            );
            assert.deepEqual(
              projectRemainingBindings(executed.result.remaining_plan),
              expected.length === 0 ? null : expected,
            );
          }
          let terminal;
          if (order[0] === 0) {
            const measured = await loadWithProjectionBuildCounts(
              makeRestartedLedger(attempt),
            );
            terminal = measured.loaded;
            const inventoryChanges = terminal.records.filter((entry) =>
              entry.envelope.record_type ===
                "candidate-inventory-observation").length;
            const dispatchChanges = terminal.records.filter((entry) =>
              entry.envelope.record_type ===
                "candidate-dispatch-observation").length;
            assert.ok(
              measured.counts.inventory <= inventoryChanges + 1 &&
                measured.counts.dispatch <= dispatchChanges + 1,
              `inventory authority rebuilt ${measured.counts.inventory} times ` +
                `for ${inventoryChanges} state changes; dispatch authority ` +
                `rebuilt ${measured.counts.dispatch} times for ` +
                `${dispatchChanges} state changes`,
            );
            assert.ok(
              measured.counts.attempt_prefix_scans <= 1,
              `scheduled attempt history was rescanned ` +
                `${measured.counts.attempt_prefix_scans} times`,
            );
          } else {
            terminal = await makeRestartedLedger(attempt).load();
          }
          assertTerminalCycle(terminal, 2, executions);
          if (order[0] === 1) {
            const forged = structuredClone(terminal.records);
            const batchIndex = forged.findIndex((entry) =>
              entry.envelope.record_type ===
                "candidate-dispatch-observation" &&
              entry.envelope.payload.phase === "batch-complete");
            const indexSortedLastAck = forged.find((entry) =>
              entry.envelope.record_type ===
                "candidate-dispatch-observation" &&
              entry.envelope.payload.phase === "candidate-ack" &&
              entry.envelope.payload.candidate_ack.candidate_index === 1);
            assert.notEqual(batchIndex, -1);
            assert.notEqual(indexSortedLastAck, undefined);
            const payload = structuredClone(
              forged[batchIndex].envelope.payload,
            );
            payload.command_authority = structuredClone(
              indexSortedLastAck.envelope.payload.command_authority,
            );
            payload.trigger_identity = structuredClone(
              indexSortedLastAck.envelope.payload.trigger_identity,
            );
            delete forged[batchIndex].envelope.public_envelope_digest;
            resealFixtureEnvelopePayload(
              forged[batchIndex].envelope,
              payload,
            );
            forged[batchIndex].envelope.public_envelope_digest =
              digestCanonical(
                "codex-review-gate-v2-git-ledger-public-envelope",
                forged[batchIndex].envelope,
              );
            assert.throws(
              () => deriveV2GitLedgerCandidateDispatchAuthority(
                forged.slice(0, batchIndex + 1),
                REPOSITORY,
              ),
              /batch completion differs from reachable acks/u,
            );
          }
        });
    }

    await t.test("second acknowledgement response loss completes once after restart",
      async () => {
        const attempt = await createAttempt(2, "ack-response-loss");
        const first = await executeCandidate(attempt, 0, {
          full: true,
          expectCycleComplete: false,
          runId: "9200",
        });
        assert.deepEqual(
          projectRemainingBindings(first.result.remaining_plan),
          attempt.originalBindings.slice(1),
        );
        const second = await prepareCandidate(attempt, 1, "9200", {
          full: true,
        });
        await assert.rejects(
          completeScheduledCandidateDispatchAttempt(second, {
            expectCycleComplete: true,
            beforeAck() {
              attempt.fixture.failNextCandidateDispatchUpdateAfterApply(
                "candidate-ack",
              );
            },
          }),
          (error) => error.code === "unexpected-http-status",
        );
        const afterLoss = await makeRestartedLedger(attempt).load();
        assert.equal(afterLoss.records.filter((entry) =>
          entry.envelope.record_type === "candidate-dispatch-observation" &&
          entry.envelope.payload.phase === "candidate-ack").length, 2);
        assert.equal(afterLoss.records.filter((entry) =>
          entry.envelope.record_type === "candidate-dispatch-observation" &&
          new Set(["batch-complete", "cycle-complete"]).has(
            entry.envelope.payload.phase,
          )).length, 0);
        attempt.runIdentity.run_id = "9203";
        attempt.runIdentity.run_attempt = 1;
        const recoveryInput = {
          ...attempt.input,
          workflow_command_handle: await createScheduleWorkflowCommandHandle({
            runIdentity: attempt.runIdentity,
          }),
        };
        const restarted = makeRestartedLedger(attempt);
        const completed = await restarted.loadOrReserveCandidateDispatch(
          recoveryInput,
        );
        assert.equal(completed.state, "complete");
        const final = await restarted.load();
        assertTerminalCycle(final, 2);
        const writesBeforeReplay = attempt.fixture.writeCalls;
        attempt.runIdentity.run_id = "9204";
        const replayInput = {
          ...attempt.input,
          workflow_command_handle: await createScheduleWorkflowCommandHandle({
            runIdentity: attempt.runIdentity,
          }),
        };
        assert.equal(
          (await makeRestartedLedger(attempt)
            .loadOrReserveCandidateDispatch(replayInput)).state,
          "complete",
        );
        assert.equal(attempt.fixture.writeCalls, writesBeforeReplay);
      });

    await t.test("64 original bindings retain D1 across fresh endpoint execution",
      async () => {
        const attempt = await createAttempt(64, "sixty-four");
        attempt.fixture.freezeServerTime();
        const reservationAuthorityDigest = attempt.originalBindings[0]
          .candidate_dispatch_authority_digest;
        assert.equal(new Set(attempt.originalBindings.map((binding) =>
          binding.candidate.identity_digest)).size, 64);
        assert.equal(new Set(attempt.originalBindings.map((binding) =>
          binding.candidate.identity.number)).size, 64);
        assert.ok(attempt.originalBindings.every((binding) =>
          binding.candidate_dispatch_authority_digest ===
            reservationAuthorityDigest));
        const projectedBindingsJson = canonicalJson(attempt.originalBindings);
        assert.match(projectedBindingsJson, /^[\x00-\x7f]+$/u);
        assert.ok(Buffer.byteLength(projectedBindingsJson, "utf8") * 2 <=
          512 * 1024);

        const first = await executeCandidate(attempt, 0, {
          full: true,
          expectCycleComplete: false,
          runId: "9300",
        });
        assert.equal(
          first.prepared.preScopeReceipt.inventory_receipt
            .candidate_dispatch_authority_digest,
          reservationAuthorityDigest,
        );
        assert.deepEqual(
          projectRemainingBindings(first.result.remaining_plan),
          attempt.originalBindings.slice(1),
        );

        const last = await executeCandidate(attempt, 63, {
          full: true,
          expectCycleComplete: false,
          runId: "9300",
        });
        assert.equal(
          last.prepared.preScopeReceipt.inventory_receipt
            .candidate_dispatch_authority_digest,
          first.result.dispatch_authority_digest,
        );
        assert.notEqual(
          last.prepared.preScopeReceipt.inventory_receipt
            .candidate_dispatch_authority_digest,
          reservationAuthorityDigest,
        );
        const lastRemainingBindings = projectRemainingBindings(
          last.result.remaining_plan,
        );
        assert.deepEqual(
          lastRemainingBindings,
          attempt.originalBindings.slice(1, 63),
        );
        assert.ok(lastRemainingBindings.every((binding) =>
          binding.candidate_dispatch_authority_digest ===
            reservationAuthorityDigest));

        const restarted = await makeRestartedLedger(attempt).load();
        const acks = restarted.records.filter((entry) =>
          entry.envelope.record_type === "candidate-dispatch-observation" &&
          entry.envelope.payload.phase === "candidate-ack");
        assert.deepEqual(acks.map((entry) =>
          entry.envelope.payload.candidate_ack.candidate_index), [0, 63]);
        assert.equal(restarted.records.some((entry) =>
          entry.envelope.record_type === "candidate-dispatch-observation" &&
          new Set(["batch-complete", "cycle-complete"]).has(
            entry.envelope.payload.phase,
          )), false);
        assert.equal(
          restarted.authority_projection.candidate_dispatch.authority_digest,
          last.result.dispatch_authority_digest,
        );
      });

    await t.test(
      "the second batch rejects one oversized binding before reservation",
      async () => {
        const asset = await loadC65Batch0GitFixtureState();
        const fixture = githubGitFixture(asset.git);
        const capability = scheduleCapabilityReceipt();
        const triggerIdentity = scheduleTriggerIdentity();
        const preflight = await loadFixtureProviderPreflight();
        const provenanceMintState = createFixtureProvenanceMintState(
          asset.historical_token_authorities,
        );
        const historicalReverificationLogs = [];
        const historicalMaxOrdinal = Math.max(
          ...asset.historical_token_authorities.map(({ ordinal }) => ordinal),
        );
        const assertFreshFactoryMints = (priorCount, runId) => {
          const minted = provenanceMintState.minted_authorities;
          assert.ok(minted.length >= priorCount);
          assert.ok(minted.slice(priorCount).every(({ run_identity }) =>
            run_identity.run_id === runId));
          assert.deepEqual(
            minted.map(({ ordinal }) => ordinal),
            Array.from(
              { length: minted.length },
              (_, index) => historicalMaxOrdinal + index + 1,
            ),
          );
          assert.equal(
            new Set(minted.map(({ token_sha256 }) => token_sha256)).size,
            minted.length,
          );
          assert.equal(
            new Set(minted.map(({ jti }) => jti)).size,
            minted.length,
          );
        };
        const createFreshLedger = async (runId) => {
          const runIdentity = {
            ...OWNER,
            run_id: runId,
            run_attempt: 1,
          };
          const historicalReverificationLog = [];
          historicalReverificationLogs.push(historicalReverificationLog);
          const verifier = provenanceVerifier({
            eventName: "schedule",
            ref: triggerIdentity.ref,
            shaValue: triggerIdentity.sha,
            runIdentity,
            historicalTokenAuthorities:
              asset.historical_token_authorities,
            historicalReverificationLog,
            fixtureProvenanceMintState: provenanceMintState,
          });
          return {
            ledger: makeLedger(
              fixture,
              capability,
              verifier,
              preflight,
            ),
            input: {
              workflow_command_handle:
                await createScheduleWorkflowCommandHandle({ runIdentity }),
              trigger_identity: triggerIdentity,
              repository_endpoint_receipt:
                scheduleRepositoryEndpointReceipt(),
            },
            historicalReverificationLog,
          };
        };
        const initial = await createFreshLedger("9401");
        let priorMintCount = provenanceMintState.minted_authorities.length;
        const completedBatch = await initial.ledger.load();
        assertFreshFactoryMints(priorMintCount, "9401");
        assertC65HistoricalProvenanceReverification(
          initial.historicalReverificationLog,
          asset.historical_token_authorities,
        );
        assert.equal(completedBatch.commit_count, 198);
        assert.equal(completedBatch.tip_commit_sha, C65_BATCH0_GIT_FIXTURE_REF);
        const generation = completedBatch.authority_projection
          .candidate_inventory.completed_generation;
        assert.notEqual(generation, null);
        assert.equal(generation.candidate_count, 65);
        assert.equal(generation.candidates.length, 65);
        const cycle = completedBatch.authority_projection.candidate_dispatch
          .current_cycle;
        assert.equal(cycle.candidate_count, 65);
        assert.equal(cycle.batch_count, 2);
        assert.equal(cycle.completed_batches.length, 1);
        assert.equal(cycle.completed_batches[0].batch_index, 0);
        assert.equal(cycle.completed_batches[0].candidate_count, 64);
        assert.equal(cycle.completed_batches[0].ack_record_oids.length, 64);
        assert.equal(new Set(cycle.completed_batches[0].ack_record_oids).size, 64);
        assert.equal(cycle.active_reservation, null);
        assert.equal(cycle.cycle_complete, false);
        assert.equal(completedBatch.records.filter((entry) =>
          entry.envelope.record_type === "candidate-dispatch-observation" &&
          entry.envelope.payload.phase === "candidate-ack").length, 64);
        const reserves = completedBatch.records.filter((entry) =>
          entry.envelope.record_type === "candidate-dispatch-observation" &&
          entry.envelope.payload.phase === "reserve");
        assert.equal(reserves.length, 1);
        assert.equal(reserves[0].envelope.payload.reservation.batch_index, 0);

        const oversizedPlan = currentOpenDispatchMeasurementPlan(
          {
            candidate_count: generation.candidate_count,
            candidates: generation.candidates,
            authority_digest: generation.source_authority_digest,
            candidate_set_digest: generation.candidate_set_digest,
            source_current_open_semantic_digest:
              generation.source_semantic_digest,
          },
          { sourceCandidateIndex: 64, batchIndex: 1, batchCount: 2 },
        );
        assert.ok(Buffer.byteLength(canonicalJson(oversizedPlan), "utf8") <
          MAX_V2_CURRENT_OPEN_CANDIDATE_DISPATCH_PLAN_BYTES);
        assert.ok(Buffer.byteLength(canonicalJson(
          currentOpenDispatchMeasurementBinding(oversizedPlan),
        ), "utf8") > MAX_V2_CURRENT_OPEN_CANDIDATE_DISPATCH_BINDING_BYTES);
        assert.throws(
          () => projectV2GitLedgerCandidateDispatchBinding(oversizedPlan, 0),
          (error) => error.code === "candidate-dispatch-binding-size",
        );

        const snapshot = () => ({
          writes: fixture.writeCalls,
          blob_oids: [...fixture.blobs.keys()].sort(),
          tree_oids: [...fixture.trees.keys()].sort(),
          commit_oids: [...fixture.commits.keys()].sort(),
          ref: fixture.refTarget,
          patches: fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
          object_posts: fixture.calls.filter(({ method, path }) =>
            method === "POST" && new Set([
              "/git/blobs", "/git/trees", "/git/commits",
            ]).has(path)).length,
        });
        const before = snapshot();
        const rejectNextBatch = async (runId) => {
          const restarted = await createFreshLedger(runId);
          priorMintCount = provenanceMintState.minted_authorities.length;
          await assert.rejects(
            restarted.ledger.loadOrReserveCandidateDispatch(restarted.input),
            (error) => error.code === "candidate-dispatch-binding-size",
          );
          assertFreshFactoryMints(priorMintCount, runId);
          assertC65HistoricalProvenanceReverification(
            restarted.historicalReverificationLog,
            asset.historical_token_authorities,
          );
          assert.deepEqual(snapshot(), before);
        };

        await rejectNextBatch("9402");
        await rejectNextBatch("9403");
        assert.equal(
          new Set(historicalReverificationLogs).size,
          historicalReverificationLogs.length,
        );
        const final = await createFreshLedger("9404");
        priorMintCount = provenanceMintState.minted_authorities.length;
        const loaded = await final.ledger.load();
        assertFreshFactoryMints(priorMintCount, "9404");
        assertC65HistoricalProvenanceReverification(
          final.historicalReverificationLog,
          asset.historical_token_authorities,
        );
        assert.equal(
          loaded.authority_projection.candidate_dispatch.active_reservation,
          null,
        );
        assert.equal(
          loaded.authority_projection.candidate_dispatch.current_cycle
            .completed_batches.length,
          1,
        );
        assert.equal(loaded.records.filter((entry) =>
          entry.envelope.record_type === "candidate-dispatch-observation" &&
          entry.envelope.payload.phase === "reserve").length, 1);
        assert.deepEqual(snapshot(), before);
      },
    );
  });

test("mature checkpoint preserves the bootstrap-only capacity boundary",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const transportFixture = candidateTransportFixture(255);
    const cycle = await readIdentityDriftCandidateInventoryCycle({
      transportFixture,
      priorInventory: null,
      listedNumbersAfterInitial: Array.from(
        { length: 255 },
        (_, index) => index + 2,
      ),
      mutateAfterInitial: () => {
        transportFixture.setCandidateState(1, "closed");
      },
    });
    const currentOpen = currentOpenSuppressionAdapterFixture({
      initialNumbers: [PR.number],
    });
    const refreshAdapter = Object.freeze({
      schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
      schema_version: 1,
      configuration_digest: rawDigest(
        "mature-checkpoint-bootstrap-boundary-adapter-v1",
      ),
      collectCurrentOpenProjection:
        currentOpen.adapter.collectCurrentOpenProjection,
      async collectCandidateInventoryAttempt(request) {
        const evidence = request.attempt_index === 0
          ? cycle.drift_attempts[0]
          : {
              initial_inventory: cycle.initial_inventory,
              shard_receipts: cycle.shard_receipts,
              final_inventory: cycle.final_inventory,
              final_shard_receipts: cycle.final_shard_receipts,
            };
        return {
          schema: "codex-review-gate-git-ledger-candidate-full-refresh-result-v2",
          schema_version: 1,
          request_handle: request.request_handle,
          attempt_index: request.attempt_index,
          ...evidence,
        };
      },
    });
    const ledger = makeLedger(
      fixture,
      capability,
      provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
      }),
      null,
      { candidateInventoryRefreshAdapter: refreshAdapter },
    );
    await ledger.bootstrapCapability();
    const input = {
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    };
    const before = {
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    };
    await assert.rejects(
      ledger.reconcileCandidateInventoryRefreshAtomically(input),
      (error) => error.code === "CANDIDATE_INVENTORY_CHECKPOINT_REQUIRED",
    );
    assert.deepEqual({
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    }, before);
  });

test("mature quiescent checkpoint preserves public authority and continues its next unit",
  async () => {
    const source = await matureQuiescentCheckpointSourceFixture();
    const before = {
      writes: source.fixture.writeCalls,
      blobs: source.fixture.blobs.size,
      trees: source.fixture.trees.size,
      commits: source.fixture.commits.size,
      ref: source.fixture.refTarget,
      patches: source.fixture.calls.filter(({ method }) =>
        method === "PATCH").length,
    };
    source.fixture.failNextCandidateInventoryUpdateAfterApply();

    const reconciled = await makeLedger(
      source.fixture,
      source.capability,
      source.verifier,
      source.preflight,
      { candidateInventoryRefreshAdapter: source.refreshAdapter },
    ).reconcileCandidateInventoryRefreshAtomically(source.checkpointInput);

    assert.equal(reconciled.state, "persisted");
    assert.equal(reconciled.persistence_mode, "private-must-persist");
    assert.equal(reconciled.publication_result.state, "persisted");
    assert.equal(
      reconciled.publication_result.published_record_commit_shas.length,
      5,
    );
    assert.equal(reconciled.publication_result.append_receipts.length, 0);
    assert.equal(reconciled.publication_result.final_commit_count, 6);
    assert.equal(source.fixture.writeCalls > before.writes, true);
    assert.equal(source.fixture.commits.size > before.commits, true);
    assert.notEqual(source.fixture.refTarget, before.ref);
    assert.equal(
      source.fixture.calls.filter(({ method }) => method === "PATCH").length -
        before.patches,
      1,
    );

    const restart = makeLedger(
      source.fixture,
      source.capability,
      source.verifier,
      source.preflight,
      { candidateInventoryRefreshAdapter: source.refreshAdapter },
    );
    const loaded = await restart.load();
    assert.equal(loaded.checkpoint.profile, "mature-quiescent-v1");
    assert.equal(loaded.commit_count, 6);
    assert.equal(loaded.epoch_commit_count, 6);
    assert.equal(
      loaded.logical_record_count,
      source.terminal.commit_count + 6,
    );
    assert.equal(
      loaded.provenance_reverification.length,
      source.terminal.records.length + 6,
    );
    const reverifiedRecordOids = loaded.provenance_reverification.map(
      ({ record_oid: recordOid }) => recordOid,
    );
    assert.equal(new Set(reverifiedRecordOids).size, reverifiedRecordOids.length);
    assert.deepEqual(
      reverifiedRecordOids.slice(0, source.terminal.records.length),
      source.terminal.records.map(({ commit_sha: commitSha }) => commitSha),
    );
    const layout = matureCheckpointFixtureLayout(source.fixture);
    assert.equal(layout.suffix.length, 5);
    assert.equal(layout.seed.records.some((entry) =>
      entry.candidate_inventory_attachment_evidence !== null), true);
    const readOnlySnapshot = () => ({
      writes: source.fixture.writeCalls,
      blobs: source.fixture.blobs.size,
      trees: source.fixture.trees.size,
      commits: source.fixture.commits.size,
      ref: source.fixture.refTarget,
      patches: source.fixture.calls.filter(({ method }) =>
        method === "PATCH").length,
    });
    const freshLoad = (verifier = source.verifier) => makeLedger(
      source.fixture,
      source.capability,
      verifier,
      source.preflight,
      { candidateInventoryRefreshAdapter: source.refreshAdapter },
    ).load();
    const rejectReadOnly = async (expectedCode, action) => {
      const snapshot = readOnlySnapshot();
      await assert.rejects(action, (error) => error.code === expectedCode);
      assert.deepEqual(readOnlySnapshot(), snapshot);
    };

    const classificationMutations = [
      ["unknown key", (value) => { value.unknown_key = true; }],
      ["state", (value) => { value.state = "unknown"; }],
      ["reason", (value) => { value.reason = "unknown"; }],
      ["source tip", (value) => { value.source_tip_commit_sha = 7; }],
      ["source count", (value) => { value.source_commit_count = "4096"; }],
      ["semantic digest", (value) => {
        value.semantic_inventory_digest = false;
      }],
      ["semantic change", (value) => { value.semantic_change = "false"; }],
      ["drift count", (value) => { value.drift_attempt_count = "0"; }],
      ["drift codes", (value) => { value.drift_codes = {}; }],
      ["authoritative timestamp alias", (value) => {
        value.authoritative_observed_at =
          value.authoritative_observed_at.replace(".000Z", "Z");
      }],
      ["candidate count", (value) => { value.candidate_count = "255"; }],
      ["due count", (value) => { value.due_candidate_count = -1; }],
      ["nullable due boundary", (value) => {
        value.due_candidate_count = 0;
        value.earliest_due_at = value.authoritative_observed_at;
      }],
      ["inventory budget", (value) => {
        value.inventory_commit_budget_required = -1;
      }],
      ["dispatch budget", (value) => {
        value.dispatch_commit_budget_required = "1";
      }],
      ["execution budget", (value) => {
        value.candidate_execution_commit_budget_required = null;
      }],
      ["total budget", (value) => {
        value.total_commit_budget_required = false;
      }],
      ["remaining capacity", (value) => {
        value.remaining_ledger_commit_capacity_after_dispatch = "0";
      }],
      ["projection digest", (value) => {
        value.projection_digest = `sha256:${"f".repeat(64)}`;
      }, false],
    ];
    for (const [label, mutateClassification, resealProjectionDigest = true]
      of classificationMutations) {
      const original = source.fixture.snapshotHistoricalState();
      try {
        resealMatureCheckpointClassificationForFixture({
          fixture: source.fixture,
          mutateClassification,
          resealProjectionDigest,
        });
        await rejectReadOnly(
          "CANDIDATE_INVENTORY_CLASSIFICATION_PROJECTION",
          freshLoad(),
        );
      } catch (error) {
        error.message = `${label}: ${error.message}`;
        throw error;
      } finally {
        source.fixture.restoreHistoricalState(original);
      }
    }

    const stateBlobOid = layout.state_blob_entry.sha;
    const originalStateBlob = source.fixture.blobs.get(stateBlobOid);
    const tamperedStateBytes = Buffer.from(originalStateBlob.bytes);
    tamperedStateBytes[0] ^= 1;
    source.fixture.blobs.set(stateBlobOid, {
      ...originalStateBlob,
      content: tamperedStateBytes.toString("utf8"),
      bytes: tamperedStateBytes,
    });
    try {
      await rejectReadOnly("CHECKPOINT_BLOB_IDENTITY", freshLoad());
    } finally {
      source.fixture.blobs.set(stateBlobOid, originalStateBlob);
    }

    const sourceChunkOid = layout.manifest.chunks[0].blob_sha;
    const originalSourceChunk = source.fixture.blobs.get(sourceChunkOid);
    const tamperedSourceChunk = Buffer.from(originalSourceChunk.bytes);
    tamperedSourceChunk[0] ^= 1;
    source.fixture.blobs.set(sourceChunkOid, {
      ...originalSourceChunk,
      content: tamperedSourceChunk.toString("utf8"),
      bytes: tamperedSourceChunk,
    });
    try {
      await rejectReadOnly("CHECKPOINT_BLOB_IDENTITY", freshLoad());
    } finally {
      source.fixture.blobs.set(sourceChunkOid, originalSourceChunk);
    }

    const sourceJwt = layout.seed.records[0].envelope.workflow_provenance_jwt;
    let rejectedSourceJwt = false;
    const rejectingVerifier = async (request, options) => {
      if (
        request.mode === "reverify-stored" &&
        request.compact_jwt === sourceJwt
      ) {
        rejectedSourceJwt = true;
        const error = new Error("fixture rejects the changed stored JWT");
        error.code = "fixture-stored-jwt-tamper";
        throw error;
      }
      return source.verifier(request, options);
    };
    await rejectReadOnly(
      "workflow-provenance-verification-failed",
      freshLoad(rejectingVerifier),
    );
    assert.equal(rejectedSourceJwt, true);

    const firstSuffix = layout.suffix[0];
    const originalFirstSuffixCommit = source.fixture.commits.get(
      firstSuffix.sha,
    );
    source.fixture.commits.set(firstSuffix.sha, {
      ...structuredClone(originalFirstSuffixCommit),
      parents: layout.checkpoint_commit.parents.map(({ sha }) => ({ sha })),
    });
    try {
      await rejectReadOnly("commit-object-identity", freshLoad());
    } finally {
      source.fixture.commits.set(firstSuffix.sha, originalFirstSuffixCommit);
    }
    assert.equal(
      loaded.records.length,
      source.terminal.records.length + 5,
    );
    assert.deepEqual(
      loaded.records.slice(0, source.terminal.records.length).map((entry) => ({
        commit_sha: entry.commit_sha,
        envelope_digest: entry.envelope.envelope_digest,
      })),
      source.terminal.records.map((entry) => ({
        commit_sha: entry.commit_sha,
        envelope_digest: entry.envelope.envelope_digest,
      })),
    );
    assert.equal(loaded.active_lease, null);
    assert.equal(loaded.effect_intent_count, source.terminal.effect_intent_count);
    assert.equal(
      loaded.effect_response_count,
      source.terminal.effect_response_count,
    );
    assert.deepEqual(
      loaded.control_comment_binding,
      source.terminal.control_comment_binding,
    );
    assert.deepEqual(
      loaded.authority_projection.runner_state,
      source.terminal.authority_projection.runner_state,
    );
    assert.equal(
      loaded.authority_projection.candidate_inventory.completed_cycle
        .cycle_receipt.open_pull_requests.length,
      255,
    );
    const active = loaded.authority_projection.candidate_dispatch
      .active_reservation;
    assert.notEqual(active, null);
    assert.equal(
      active.reservation.remaining_ledger_commit_capacity_after_dispatch,
      2,
    );
    const reserved = await restart.loadOrReserveCandidateDispatch(
      source.checkpointInput,
    );
    assert.equal(reserved.state, "dispatch");
    assert.equal(reserved.restarted, true);
    assert.equal(reserved.plan.items.length, 64);
    const checkpointBindings = reserved.plan.items.map((_, index) =>
      projectV2GitLedgerCandidateDispatchBinding(reserved.plan, index));

    source.runIdentity.run_id = "9605";
    source.runIdentity.run_attempt = 1;
    const restartInput = {
      ...source.checkpointInput,
      workflow_command_handle: await createScheduleWorkflowCommandHandle({
        runIdentity: source.runIdentity,
      }),
    };
    const freshReservation = await makeLedger(
      source.fixture,
      source.capability,
      source.verifier,
      source.preflight,
      { candidateInventoryRefreshAdapter: source.refreshAdapter },
    ).loadOrReserveCandidateDispatch(restartInput);
    assert.equal(
      freshReservation.reservation_receipt.reservation_record_oid,
      reserved.reservation_receipt.reservation_record_oid,
    );
    assert.deepEqual(
      freshReservation.plan.items.map((_, index) =>
        projectV2GitLedgerCandidateDispatchBinding(
          freshReservation.plan,
          index,
        )),
      checkpointBindings,
    );

    const first = await prepareCompactCandidateDispatchItem({
      ...source,
      binding: checkpointBindings[0],
      runId: "9606",
    });
    const immutableReservationAuthority = first.preScopeReceipt
      .inventory_receipt.candidate_dispatch_authority_digest;
    const completed = await completeScheduledCandidateDispatchAttempt(first, {
      expectCycleComplete: false,
    });
    assert.notEqual(
      completed.ack.dispatch_authority_digest,
      immutableReservationAuthority,
    );
    const afterAck = await makeLedger(
      source.fixture,
      source.capability,
      source.verifier,
      source.preflight,
      { candidateInventoryRefreshAdapter: source.refreshAdapter },
    ).load();
    assert.equal(
      afterAck.authority_projection.candidate_dispatch.authority_digest,
      completed.ack.dispatch_authority_digest,
    );
    assert.equal(afterAck.next_sequence, afterAck.commit_count);
    assert.equal(
      afterAck.logical_record_count,
      source.terminal.commit_count + afterAck.commit_count,
    );
    assert.equal(
      afterAck.records.length,
      source.terminal.records.length + afterAck.commit_count - 1,
    );
    assert.equal(afterAck.active_lease, null);
    assert.equal(afterAck.records.filter((entry) =>
      entry.envelope.record_type === "candidate-dispatch-observation" &&
      entry.envelope.payload.phase === "candidate-ack" &&
      entry.envelope.payload.candidate_ack.generation_id ===
        active.reservation.generation_id).length, 1);
    const remainingBindings = completed.ack.remaining_plan.items.map(
      (_, index) => projectV2GitLedgerCandidateDispatchBinding(
        completed.ack.remaining_plan,
        index,
      ),
    );
    assert.deepEqual(remainingBindings, checkpointBindings.slice(1));
  });

test("mature checkpoint refuses nonquiescent source boundaries without writes",
  async (t) => {
    const snapshot = (fixture) => ({
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
      patches: fixture.calls.filter(({ method }) => method === "PATCH").length,
    });

    await t.test("active reservation", async () => {
      const attempt = await stableEmptyCandidateRefreshReconciliationFixture({
        currentOpenNumbers: [PR.number],
        fullTransportOptions: { exactScopeCandidateNumber: PR.number },
      });
      attempt.fullTransport.setListedNumbers([PR.number]);
      await attempt.ledger.reconcileCandidateInventoryRefreshAtomically(
        attempt.input,
      );
      const reserved = await attempt.ledger.loadOrReserveCandidateDispatch(
        attempt.input,
      );
      assert.equal(reserved.state, "dispatch");
      const before = snapshot(attempt.fixture);
      await assert.rejects(
        attempt.ledger.reconcileCandidateInventoryRefreshAtomically(
          attempt.input,
        ),
        (error) => error.code ===
          "candidate-inventory-cycle-dispatch-unsettled",
      );
      assert.deepEqual(snapshot(attempt.fixture), before);
    });

    await t.test("active lease", async () => {
      const attempt = await createCompactScheduledCandidateDispatchAttempt();
      const before = snapshot(attempt.fixture);
      await assert.rejects(
        attempt.ledger.reconcileCandidateInventoryRefreshAtomically(
          attempt.input,
        ),
        (error) => error.code === "candidate-inventory-cycle-lease-active",
      );
      assert.deepEqual(snapshot(attempt.fixture), before);
    });

    await t.test("incomplete inventory", async () => {
      const historical = await legacyIncompleteCandidateRefreshFixture({
        candidateCount: 1,
      });
      const currentOpen = currentOpenSuppressionAdapterFixture({
        initialNumbers: Array.from({ length: 255 }, (_, index) => index + 1),
      });
      const checkpointNumbers = Array.from(
        { length: 255 },
        (_, index) => index + 1,
      );
      const refreshAdapter = Object.freeze({
        schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
        schema_version: 1,
        configuration_digest: rawDigest(
          "mature-checkpoint-incomplete-source-adapter-v1",
        ),
        collectCurrentOpenProjection:
          currentOpen.adapter.collectCurrentOpenProjection,
        async collectCandidateInventoryAttempt(request) {
          const attempt = request.attempt_index === 0
            ? await readDriftingCandidateInventoryAttempt({
                request,
                transportFixture: historical.transportFixture,
                finalNumbers: checkpointNumbers,
              })
            : await readCandidateInventoryAttempt({
                transportFixture: historical.transportFixture,
                priorInventory: request.prior_inventory,
                initialInventory: request.initial_inventory,
              });
          return {
            schema:
              "codex-review-gate-git-ledger-candidate-full-refresh-result-v2",
            schema_version: 1,
            request_handle: request.request_handle,
            attempt_index: request.attempt_index,
            ...attempt,
          };
        },
      });
      const ledger = makeLedger(
        historical.fixture,
        historical.capability,
        historical.verifier,
        null,
        { candidateInventoryRefreshAdapter: refreshAdapter },
      );
      const before = snapshot(historical.fixture);
      await assert.rejects(
        ledger.reconcileCandidateInventoryRefreshAtomically({
          workflow_command_handle:
            await createScheduleWorkflowCommandHandle(),
          trigger_identity: historical.triggerIdentity,
          repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        }),
        (error) => error.code === "CHECKPOINT_SOURCE_BOUNDARY_UNSUPPORTED",
      );
      assert.deepEqual(snapshot(historical.fixture), before);
    });
  });

test("current-open v3 lifecycle ids remain stable across seed and presence changes",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const runIdentity = { ...OWNER };
    const preflight = await loadFixtureProviderPreflight();
    const currentOpen = currentOpenSuppressionAdapterFixture({
      initialNumbers: [PR.number],
      exactScopeCandidateNumber: PR.number,
    });
    const refreshAdapter = Object.freeze({
      schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
      schema_version: 2,
      configuration_digest: rawDigest(
        "candidate-current-open-v3-lifecycle-lineage-configuration-v1",
      ),
      collectCurrentOpenCandidateAuthority:
        currentOpen.collectProductionCandidateAuthority,
      async finishLegacyCandidateInventoryAttempt() {
        throw new Error("current-open lifecycle lineage used legacy REST");
      },
    });
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
      runIdentity,
    });
    const ledger = makeLedger(
      fixture,
      capability,
      verifier,
      preflight,
      { candidateInventoryRefreshAdapter: refreshAdapter },
    );
    await ledger.bootstrapCapability();
    const scanInput = async (runId) => {
      runIdentity.run_id = runId;
      runIdentity.run_attempt = 1;
      return {
        workflow_command_handle: await createScheduleWorkflowCommandHandle({
          runIdentity,
        }),
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      };
    };
    const completeGeneration = async ({ input, headOid, runId }) => {
      const reserved = await ledger.loadOrReserveCandidateDispatch(input);
      const binding = projectV2GitLedgerCandidateDispatchBinding(
        reserved.plan,
        0,
      );
      runIdentity.run_id = runId;
      const scheduledCommand = await createScheduleWorkflowCommandHandle({
        pullRequestNumber: PR.number,
        dispatchBinding: binding,
        runIdentity,
      });
      const minimal = await loadFixtureMinimalScopeForCandidate(
        PR.number,
        headOid,
        "feature",
      );
      const scheduled = await ledger
        .loadCandidateDispatchForScheduledPullRequest({
          workflow_command_handle: scheduledCommand,
          minimal_scope_handle: minimal,
          trigger_identity: triggerIdentity,
          expected_dispatch_binding: binding,
        });
      const preScopeReceipt = await ledger
        .loadScheduledPullRequestEvaluatedScopeReceipt({
          candidate_dispatch_handle: scheduled.candidate_dispatch_handle,
          minimal_scope_handle: minimal,
          trigger_identity: triggerIdentity,
        });
      const discovery = await acquireFixtureLeasedDiscovery({
        ledger,
        command: scheduledCommand,
        preScopeReceipt,
        minimalPre: minimal,
        minimalFetch: (url, init) => minimalScopeFetchForCandidate({
          number: PR.number,
          nodeId: PR.node_id,
          headOid,
          headRefName: "feature",
          serverTime: "2026-08-13T14:00:00.000Z",
          urlValue: url,
          init,
        }),
        transport: createProviderTransport(providerTransportFixture({
          startSecond: 3600,
          headOid,
          headRefName: "feature",
        }).fetch),
      });
      await releaseFixtureDiscoveryLease(ledger, discovery);
      const recovered = await ledger.recoverCandidateDispatchFailure({
        workflow_command_handle: scheduledCommand,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        expected_dispatch_binding: binding,
      });
      assert.equal(recovered.recovery_mode, "released");
      assert.equal(recovered.cycle_complete, true);
    };

    const firstInput = await scanInput("9101");
    await ledger.reconcileCandidateInventoryRefreshAtomically(firstInput);
    const firstGeneration = (await ledger.load()).authority_projection
      .candidate_inventory.completed_generation;
    const firstLifecycleId =
      firstGeneration.candidates[0].lifecycle_generation_id;
    await completeGeneration({
      input: firstInput,
      headOid: HEAD,
      runId: "9102",
    });

    const changedHead = "d".repeat(40);
    currentOpen.setHeadRefOid(PR.number, changedHead);
    const secondInput = await scanInput("9103");
    await ledger.reconcileCandidateInventoryRefreshAtomically(secondInput);
    const secondGeneration = (await ledger.load()).authority_projection
      .candidate_inventory.completed_generation;
    assert.notEqual(secondGeneration.generation_id, firstGeneration.generation_id);
    assert.equal(
      secondGeneration.candidates[0].lifecycle_generation_id,
      firstLifecycleId,
    );
    assert.notEqual(
      secondGeneration.candidates[0].lifecycle_seed_digest,
      firstGeneration.candidates[0].lifecycle_seed_digest,
    );
    await completeGeneration({
      input: secondInput,
      headOid: changedHead,
      runId: "9104",
    });

    currentOpen.setListedNumbers([]);
    const absentInput = await scanInput("9105");
    await ledger.reconcileCandidateInventoryRefreshAtomically(absentInput);
    assert.deepEqual(
      (await ledger.load()).authority_projection.candidate_inventory
        .completed_generation.candidates,
      [],
    );

    currentOpen.setListedNumbers([PR.number]);
    const reappearedInput = await scanInput("9106");
    await ledger.reconcileCandidateInventoryRefreshAtomically(reappearedInput);
    const restarted = makeLedger(
      fixture,
      capability,
      verifier,
      preflight,
      { candidateInventoryRefreshAdapter: refreshAdapter },
    );
    const reappearedGeneration = (await restarted.load()).authority_projection
      .candidate_inventory.completed_generation;
    const reappeared = reappearedGeneration.candidates[0];
    const expectedReappearedLifecycleId =
      `candidate-lifecycle:${digestCanonical(
        "codex-review-gate-v2-current-open-lifecycle-generation",
        {
          repository: {
            owner: REPOSITORY.owner,
            name: REPOSITORY.name,
            id: REPOSITORY.id,
            node_id: REPOSITORY.node_id,
          },
          identity_digest: reappeared.identity_digest,
        },
      ).slice("sha256:".length)}`;
    assert.equal(reappeared.lifecycle_generation_id, firstLifecycleId);
    assert.equal(
      reappeared.lifecycle_generation_id,
      expectedReappearedLifecycleId,
    );
  });

test("current-open v3 reconciliation rejects a consumed live pair before a second write",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const currentOpen = currentOpenSuppressionAdapterFixture();
    let pair = null;
    const refreshAdapter = Object.freeze({
      schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
      schema_version: 2,
      configuration_digest: rawDigest(
        "candidate-current-open-v3-consumed-pair-configuration-v1",
      ),
      async collectCurrentOpenCandidateAuthority() {
        const fresh = await currentOpen.collectProductionCandidateAuthority();
        if (pair !== null) {
          consumeV2CurrentOpenProductionCandidateAuthorityHandle(
            fresh.production_candidate_authority,
            fresh.projection,
          );
        }
        pair = fresh;
        return pair;
      },
      async finishLegacyCandidateInventoryAttempt() {
        throw new Error("current-open consumed pair used legacy REST");
      },
    });
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    });
    const ledger = makeLedger(fixture, capability, verifier, null, {
      candidateInventoryRefreshAdapter: refreshAdapter,
    });
    await ledger.bootstrapCapability();
    const input = {
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    };
    const first = await ledger.reconcileCandidateInventoryRefreshAtomically(
      input,
    );
    assert.equal(first.state, "persisted");
    const before = {
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    };
    await assert.rejects(
      ledger.reconcileCandidateInventoryRefreshAtomically(input),
      (error) =>
        error.code === "CONSUMED_CURRENT_OPEN_PRODUCTION_AUTHORITY_HANDLE",
    );
    assert.deepEqual({
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    }, before);
  });

test("current-open v3 reconciliation recovers one applied atomic publication",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const currentOpen = currentOpenSuppressionAdapterFixture({
      initialNumbers: [PR.number],
    });
    let legacyFinishCalls = 0;
    const refreshAdapter = Object.freeze({
      schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
      schema_version: 2,
      configuration_digest: rawDigest(
        "candidate-current-open-v3-response-loss-test-configuration-v1",
      ),
      collectCurrentOpenCandidateAuthority:
        currentOpen.collectProductionCandidateAuthority,
      async finishLegacyCandidateInventoryAttempt() {
        legacyFinishCalls += 1;
        throw new Error("fresh current-open generation used legacy REST");
      },
    });
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    });
    const ledger = makeLedger(fixture, capability, verifier, null, {
      candidateInventoryRefreshAdapter: refreshAdapter,
    });
    await ledger.bootstrapCapability();
    const input = {
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    };
    const patchesBefore = fixture.calls.filter(({ method }) =>
      method === "PATCH").length;
    fixture.failNextCandidateInventoryUpdateAfterApply();

    const reconciled = await ledger
      .reconcileCandidateInventoryRefreshAtomically(input);

    assert.equal(reconciled.state, "persisted");
    assert.equal(
      reconciled.publication_result.publication_outcome,
      "recovered-after-apply",
    );
    assert.equal(reconciled.publication_result.append_receipts.length, 0);
    assert.equal(
      reconciled.publication_result.published_record_commit_shas.length,
      2,
    );
    assert.equal(legacyFinishCalls, 0);
    assert.equal(currentOpen.calls.length, 4);
    assert.equal(
      fixture.calls.filter(({ method }) => method === "PATCH").length -
        patchesBefore,
      1,
    );
    const loaded = await makeLedger(fixture, capability, verifier, null, {
      candidateInventoryRefreshAdapter: refreshAdapter,
    }).load();
    assert.equal(
      loaded.authority_projection.candidate_inventory.completed_generation
        .candidate_count,
      1,
    );
    assert.equal(
      loaded.authority_projection.candidate_dispatch.active_reservation
        .reservation.candidates[0].identity.number,
      PR.number,
    );
  });

test("current-open v3 reconciliation recognizes its publication below a later generation",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    });
    const firstCurrentOpen = currentOpenSuppressionAdapterFixture();
    const secondCurrentOpen = currentOpenSuppressionAdapterFixture();
    const refreshAdapter = (name, currentOpen) => Object.freeze({
      schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
      schema_version: 2,
      configuration_digest: rawDigest(
        `candidate-current-open-v3-${name}-configuration-v1`,
      ),
      collectCurrentOpenCandidateAuthority:
        currentOpen.collectProductionCandidateAuthority,
      async finishLegacyCandidateInventoryAttempt() {
        throw new Error("current-open suffix reconciliation used legacy REST");
      },
    });
    const first = makeLedger(fixture, capability, verifier, null, {
      candidateInventoryRefreshAdapter:
        refreshAdapter("prefix", firstCurrentOpen),
    });
    await first.bootstrapCapability();
    const second = makeLedger(fixture, capability, verifier, null, {
      candidateInventoryRefreshAdapter:
        refreshAdapter("suffix", secondCurrentOpen),
    });
    secondCurrentOpen.setListedNumbers([PR.number]);
    const firstInput = {
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    };
    const secondInput = {
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    };
    let suffixResult = null;
    fixture.afterNextCandidateInventoryUpdateApplied(async () => {
      suffixResult = await second.reconcileCandidateInventoryRefreshAtomically(
        secondInput,
      );
    });
    const patchesBefore = fixture.calls.filter(({ method }) =>
      method === "PATCH").length;

    const prefixResult = await first
      .reconcileCandidateInventoryRefreshAtomically(firstInput);

    assert.equal(prefixResult.state, "persisted");
    assert.equal(
      prefixResult.publication_result.publication_outcome,
      "recovered-after-apply",
    );
    assert.equal(
      prefixResult.publication_result.published_record_commit_shas.length,
      2,
    );
    assert.equal(suffixResult.state, "persisted");
    assert.equal(
      suffixResult.publication_result.publication_outcome,
      "published",
    );
    assert.equal(
      fixture.calls.filter(({ method }) => method === "PATCH").length -
        patchesBefore,
      2,
    );
    const loaded = await first.load();
    const prefixStart = loaded.records.findIndex(({ commit_sha: commitSha }) =>
      commitSha ===
        prefixResult.publication_result.published_record_commit_shas[0]);
    const prefixAuthority = deriveV2GitLedgerCandidateInventoryAuthority(
      loaded.records.slice(0, prefixStart + 2),
      REPOSITORY,
    );
    assert.equal(
      prefixAuthority.completed_generation.generation_record_oid,
      prefixResult.publication_result.published_record_commit_shas[0],
    );
    assert.equal(prefixAuthority.completed_generation.candidate_count, 0);
    assert.equal(
      loaded.authority_projection.candidate_inventory.completed_generation
        .candidate_count,
      1,
    );
    assert.deepEqual(
      loaded.records.slice(prefixStart, prefixStart + 2)
        .map(({ commit_sha: commitSha }) => commitSha),
      prefixResult.publication_result.published_record_commit_shas,
    );
    assert.equal(
      loaded.tip_commit_sha,
      suffixResult.publication_result.published_record_commit_shas.at(-1),
    );
  });

test("current-open v3 mature checkpoint recovers one two-record publication",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const runIdentity = { ...OWNER };
    const preflight = await loadFixtureProviderPreflight();
    let provenanceVerificationCalls = 0;
    const exactVerifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
      runIdentity,
    });
    const verifier = async (...args) => {
      provenanceVerificationCalls += 1;
      return exactVerifier(...args);
    };
    const currentOpen = currentOpenSuppressionAdapterFixture({
      initialNumbers: Array.from(
        { length: 255 },
        (_, index) => PR.number + index,
      ),
      exactScopeCandidateNumber: PR.number,
    });
    const refreshAdapter = Object.freeze({
      schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
      schema_version: 2,
      configuration_digest: rawDigest(
        "candidate-current-open-v3-mature-response-loss-configuration-v1",
      ),
      collectCurrentOpenCandidateAuthority:
        currentOpen.collectProductionCandidateAuthority,
      async finishLegacyCandidateInventoryAttempt() {
        throw new Error("current-open mature checkpoint used legacy REST");
      },
    });
    const ledger = makeLedger(fixture, capability, verifier, preflight, {
      candidateInventoryRefreshAdapter: refreshAdapter,
    });
    await ledger.bootstrapCapability();
    await padOrdinaryLedgerWithStandaloneAttestations({
      fixture,
      verifier,
      targetCommitCount: 7,
    });
    assert.equal((await ledger.load()).commit_count, 7);
    const callsBefore = currentOpen.calls.length;
    const patchesBefore = fixture.calls.filter(({ method }) =>
      method === "PATCH").length;
    let provenanceVerificationCallsAtPublicationApply = null;
    fixture.afterNextCandidateInventoryUpdateApplied(() => {
      provenanceVerificationCallsAtPublicationApply =
        provenanceVerificationCalls;
    });
    fixture.failNextCandidateInventoryUpdateAfterApply();

    const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
    const repositoryEndpointReceipt = scheduleRepositoryEndpointReceipt();
    const result = await ledger.reconcileCandidateInventoryRefreshAtomically({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: repositoryEndpointReceipt,
    });

    assert.equal(result.state, "persisted");
    assert.equal(result.persistence_mode, "current-open-generation-v3");
    assert.equal(
      result.publication_result.publication_outcome,
      "recovered-after-apply",
    );
    assert.equal(result.publication_result.final_commit_count, 3);
    assert.equal(result.publication_result.append_receipts.length, 0);
    assert.equal(
      result.publication_result.published_record_commit_shas.length,
      2,
    );
    assert.equal(
      fixture.calls.filter(({ method }) => method === "PATCH").length -
        patchesBefore,
      1,
    );
    assert.equal(currentOpen.calls.length > callsBefore, true);
    assertNoCandidateRefreshPrivateAuthority(result);
    assert.notEqual(provenanceVerificationCallsAtPublicationApply, null);
    assert.equal(
      provenanceVerificationCalls -
        provenanceVerificationCallsAtPublicationApply,
      11,
      "response-loss publication must retain its post-CAS full replay",
    );
    const publicationPatchIndex = fixture.calls.map(({ method }) => method)
      .lastIndexOf("PATCH");
    assert.notEqual(publicationPatchIndex, -1);
    assert.equal(
      fixture.calls.slice(publicationPatchIndex + 1).some(({ method, path }) =>
        method === "GET" && path.startsWith("/git/commits/")),
      true,
      "response-loss reconciliation must fully reload after the applied CAS",
    );
    const readCountsAfter = (callIndex) => {
      const calls = fixture.calls.slice(callIndex);
      return {
        refs: calls.filter(({ method, path }) =>
          method === "GET" &&
          path === "/git/ref/heads/codex-review-gate-ledger-v2").length,
        objects: calls.filter(({ method, path }) =>
          method === "GET" && (
            path.startsWith("/git/commits/") ||
            path.startsWith("/git/trees/") ||
            path.startsWith("/git/blobs/")
          )).length,
      };
    };
    const reserveWithContinuation = async ({
      targetLedger,
      reconciliationResult,
    }) => {
      const callIndex = fixture.calls.length;
      const verificationIndex = provenanceVerificationCalls;
      const writeIndex = fixture.writeCalls;
      const dispatch = await targetLedger.loadOrReserveCandidateDispatch({
        workflow_command_handle: workflowCommandHandle,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: repositoryEndpointReceipt,
        candidate_refresh_reconciliation_result: reconciliationResult,
      });
      assert.equal(dispatch.state, "dispatch");
      assert.equal(dispatch.restarted, true);
      return {
        dispatch,
        reads: readCountsAfter(callIndex),
        provenance_reverification_count:
          provenanceVerificationCalls - verificationIndex,
        write_count: fixture.writeCalls - writeIndex,
      };
    };
    const assertFullReplay = (attempt, label) => {
      assert.equal(attempt.reads.refs, 2, `${label} must use a full ref fence`);
      assert.ok(
        attempt.reads.objects > 0,
        `${label} must replay stored Git objects`,
      );
      assert.equal(
        attempt.provenance_reverification_count,
        11,
        `${label} must reverify the complete mature checkpoint query`,
      );
      assert.equal(attempt.write_count, 0, `${label} must remain read-only`);
    };
    const malformed = await reserveWithContinuation({
      targetLedger: ledger,
      reconciliationResult: {},
    });
    assertFullReplay(malformed, "malformed continuation");
    const cloned = await reserveWithContinuation({
      targetLedger: ledger,
      reconciliationResult: structuredClone(result),
    });
    assertFullReplay(cloned, "cloned continuation");
    const restarted = makeLedger(
      fixture,
      capability,
      verifier,
      preflight,
      { candidateInventoryRefreshAdapter: refreshAdapter },
    );
    const crossFactory = await reserveWithContinuation({
      targetLedger: restarted,
      reconciliationResult: result,
    });
    assertFullReplay(crossFactory, "cross-factory continuation");
    const exact = await reserveWithContinuation({
      targetLedger: ledger,
      reconciliationResult: result,
    });
    assert.deepEqual(exact.reads, { refs: 1, objects: 0 });
    assert.equal(exact.provenance_reverification_count, 0);
    assert.equal(exact.write_count, 0);
    const consumed = await reserveWithContinuation({
      targetLedger: ledger,
      reconciliationResult: result,
    });
    assertFullReplay(consumed, "consumed continuation");
    for (const attempt of [malformed, cloned, crossFactory, consumed]) {
      assert.equal(
        attempt.dispatch.plan.plan_digest,
        exact.dispatch.plan.plan_digest,
      );
    }
    const restartedDispatch = crossFactory.dispatch;
    const callsAfterPublication = currentOpen.calls.length;
    const beforeRestart = {
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    };
    const restartCallIndex = fixture.calls.length;
    const restartVerificationIndex = provenanceVerificationCalls;
    const loaded = await restarted.load();
    const restartReads = readCountsAfter(restartCallIndex);
    assert.equal(restartReads.refs, 2);
    assert.ok(restartReads.objects > 0);
    assert.equal(
      provenanceVerificationCalls - restartVerificationIndex,
      11,
      "restart must not inherit another factory's continuation",
    );
    assert.equal(currentOpen.calls.length, callsAfterPublication);
    assert.deepEqual({
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    }, beforeRestart);
    assert.equal(loaded.checkpoint.profile, "mature-quiescent-v1");
    assert.equal(loaded.commit_count, 3);
    assert.equal(loaded.epoch_commit_count, 3);
    assert.equal(loaded.logical_record_count, 10);
    assert.equal(
      loaded.authority_projection.candidate_inventory.completed_generation
        .candidate_count,
      255,
    );
    assert.equal(
      loaded.authority_projection.candidate_dispatch.active_reservation
        .reservation.candidates.length,
      64,
    );
    assert.equal(
      loaded.checkpoint.candidate_inventory_authority_digest,
      loaded.authority_projection.candidate_inventory.authority_digest,
    );
    assert.equal(
      loaded.checkpoint.candidate_dispatch_authority_digest,
      loaded.authority_projection.candidate_dispatch.authority_digest,
    );
    const checkpointLayout = matureCheckpointFixtureLayout(fixture);
    const checkpointState = JSON.parse(
      fixture.blobs.get(checkpointLayout.state_blob_entry.sha).content,
    );
    assert.equal(checkpointState.schema_version, 3);
    assert.equal(checkpointState.next_unit.candidate_count, 255);
    assert.equal(checkpointState.next_unit.semantic_record_count, 2);
    assert.equal(
      checkpointLayout.suffix.length,
      checkpointState.next_unit.semantic_record_count,
    );
    assert.equal(
      checkpointState.next_unit.total_commit_budget_required,
      4090,
    );
    assert.equal(
      checkpointState.next_unit.current_epoch_remaining_commit_capacity,
      -1,
    );
    assert.equal(
      checkpointState.next_unit.fresh_epoch_remaining_commit_capacity,
      5,
    );
    const readOnlySnapshot = () => ({
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
      patches: fixture.calls.filter(({ method }) => method === "PATCH").length,
    });
    const resealedStateMutations = [
      ["attachment manifest", (nextUnit) => {
        nextUnit.attachment_manifest_digest = rawDigest(
          "tampered-current-open-checkpoint-attachment-manifest",
        );
      }],
      ["production authority", (nextUnit) => {
        nextUnit.production_candidate_authority_digest = rawDigest(
          "tampered-current-open-checkpoint-production-authority",
        );
      }],
      ["current-open projection", (nextUnit) => {
        nextUnit.current_open_projection_digest = rawDigest(
          "tampered-current-open-checkpoint-projection",
        );
      }],
      ["stable command", (nextUnit) => {
        nextUnit.stable_command_authority_digest = rawDigest(
          "tampered-current-open-checkpoint-command",
        );
      }],
      ["trigger identity", (nextUnit) => {
        nextUnit.trigger_identity_digest = rawDigest(
          "tampered-current-open-checkpoint-trigger",
        );
      }],
      ["repository endpoint", (nextUnit) => {
        nextUnit.repository_endpoint_receipt_digest = rawDigest(
          "tampered-current-open-checkpoint-endpoint",
        );
      }],
      ["total budget", (nextUnit) => {
        nextUnit.total_commit_budget_required -= 1;
      }],
      ["current capacity", (nextUnit) => {
        nextUnit.current_epoch_remaining_commit_capacity -= 1;
      }],
      ["fresh capacity", (nextUnit) => {
        nextUnit.fresh_epoch_remaining_commit_capacity -= 1;
      }],
      ["admission digest", (nextUnit) => {
        nextUnit.admission_digest = rawDigest(
          "tampered-current-open-checkpoint-admission",
        );
      }, false, "CHECKPOINT_NEXT_UNIT_ADMISSION"],
    ];
    for (const [
      label,
      mutateNextUnit,
      resealAdmissionDigest = true,
      expectedCode = "CHECKPOINT_SUFFIX_AUTHORITY",
    ] of resealedStateMutations) {
      const original = fixture.snapshotHistoricalState();
      try {
        resealMatureCurrentOpenCheckpointStateForFixture({
          fixture,
          mutateNextUnit,
          resealAdmissionDigest,
        });
        const beforeTamperedLoad = readOnlySnapshot();
        await assert.rejects(
          makeLedger(
            fixture,
            capability,
            verifier,
            preflight,
            { candidateInventoryRefreshAdapter: refreshAdapter },
          ).load(),
          (error) => error.code === expectedCode,
        );
        assert.deepEqual(readOnlySnapshot(), beforeTamperedLoad);
      } catch (error) {
        error.message = `${label}: ${error.message}`;
        throw error;
      } finally {
        fixture.restoreHistoricalState(original);
      }
    }
    const generationEntry = loaded.records.find(({ commit_sha: commitSha }) =>
      commitSha ===
        result.publication_result.published_record_commit_shas[0]);
    assert.notEqual(generationEntry, undefined);
    const generationCommit = fixture.commits.get(generationEntry.commit_sha);
    assert.notEqual(generationCommit, undefined);
    const generationTree = fixture.trees.get(generationCommit.tree.sha);
    assert.notEqual(generationTree, undefined);
    const attachmentTreeEntry = generationTree.tree.find((entry) =>
      entry.path === V2_GIT_LEDGER_CANDIDATE_INVENTORY_ATTACHMENT_PATH &&
      entry.type === "tree");
    assert.notEqual(attachmentTreeEntry, undefined);
    const attachmentTreePath = `/git/trees/${attachmentTreeEntry.sha}`;
    assert.equal(restartedDispatch.state, "dispatch");
    assert.equal(restartedDispatch.restarted, true);
    assert.equal(
      restartedDispatch.plan.items[0].candidate.identity.number,
      PR.number,
    );
    const dispatchBinding = projectV2GitLedgerCandidateDispatchBinding(
      restartedDispatch.plan,
      0,
    );
    runIdentity.run_id = "9002";
    runIdentity.run_attempt = 1;
    const scheduledCommand = await createScheduleWorkflowCommandHandle({
      pullRequestNumber: PR.number,
      dispatchBinding,
      runIdentity,
    });
    const minimal = await loadFixtureMinimalScope();
    const scheduled = await restarted
      .loadCandidateDispatchForScheduledPullRequest({
      workflow_command_handle: scheduledCommand,
      minimal_scope_handle: minimal,
      trigger_identity: triggerIdentity,
      expected_dispatch_binding: dispatchBinding,
    });
    const preScopeReceipt =
      await restarted.loadScheduledPullRequestEvaluatedScopeReceipt({
        candidate_dispatch_handle: scheduled.candidate_dispatch_handle,
        minimal_scope_handle: minimal,
        trigger_identity: triggerIdentity,
      });
    const discovery = await acquireFixtureLeasedDiscovery({
      ledger: restarted,
      command: scheduledCommand,
      preScopeReceipt,
      minimalPre: minimal,
    });
    const completed = await completeScheduledCandidateDispatchAttempt({
      ledger: restarted,
      preflight,
      scheduled,
      scheduledCommand,
      discovery,
    }, { expectCycleComplete: false });
    assert.notEqual(completed.ack.ack_append_receipt, null);
    assert.equal(completed.ack.remaining_plan.items.length, 63);
    const attachmentTreeReadsBefore = fixture.calls.filter(({ method, path }) =>
      method === "GET" && path === attachmentTreePath).length;
    const beforeNonInventoryRestart = {
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    };
    const extended = await makeLedger(
      fixture,
      capability,
      verifier,
      preflight,
      { candidateInventoryRefreshAdapter: refreshAdapter },
    ).load();
    assert.equal(extended.commit_count, 9);
    assert.ok(
      matureCheckpointFixtureLayout(fixture).suffix.length >
        checkpointState.next_unit.semantic_record_count,
    );
    assert.notEqual(
      extended.checkpoint.candidate_dispatch_authority_digest,
      extended.authority_projection.candidate_dispatch.authority_digest,
      "a later suffix must project beyond the authenticated transaction state",
    );
    assert.equal(
      fixture.calls.filter(({ method, path }) =>
        method === "GET" && path === attachmentTreePath).length -
        attachmentTreeReadsBefore,
      1,
      "a later scheduled-dispatch suffix must not reload the admitted attachment",
    );
    assert.deepEqual({
      writes: fixture.writeCalls,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    }, beforeNonInventoryRestart);
    assert.equal(currentOpen.calls.length, callsAfterPublication);
    const admittedIndex = extended.records.findIndex(
      ({ commit_sha: commitSha }) => commitSha === generationEntry.commit_sha,
    );
    assert.notEqual(admittedIndex, -1);
    const admittedEntry = extended.records[admittedIndex];
    const admittedManifest = admittedEntry.envelope.payload
      .attachment_manifest;
    const admittedEvidence = durableCurrentOpenGenerationAttachmentValue(
      fixture,
      admittedManifest,
    );
    const admittedGeneration = deriveV2GitLedgerCandidateInventoryAuthority(
      extended.records.slice(0, admittedIndex + 1),
      REPOSITORY,
    ).completed_generation;
    assert.notEqual(admittedGeneration.candidates, null);
    assert.deepEqual(
      admittedGeneration.attachment_manifest,
      admittedManifest,
    );
    assert.equal(
      admittedEvidence.evidence_digest,
      admittedManifest.evidence_digest,
    );
    assert.equal(
      admittedEvidence.current_open_projection_digest,
      admittedManifest.current_open_projection_digest,
    );
    assert.equal(
      admittedEvidence.production_candidate_authority.authority_digest,
      admittedManifest.production_candidate_authority_digest,
    );
    assert.equal(
      admittedEvidence.production_candidate_authority.candidate_set_digest,
      admittedManifest.candidate_set_digest,
    );
    assert.equal(
      admittedEvidence.lifecycle_candidate_set_digest,
      admittedManifest.lifecycle_candidate_set_digest,
    );
    assert.deepEqual(
      admittedEvidence.dispatch_batch_digests,
      admittedManifest.dispatch_batch_digests,
    );
    assert.deepEqual(
      admittedGeneration.candidates,
      admittedEvidence.lifecycle_candidates,
    );
    assert.deepEqual(
      admittedGeneration.candidates.map(({ identity }) => identity.number),
      Array.from({ length: 255 }, (_, index) => PR.number + index),
    );
  });

async function matureCurrentOpenContinuationFixture(configurationLabel) {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const triggerIdentity = scheduleTriggerIdentity();
  const preflight = await loadFixtureProviderPreflight();
  const verification = { calls: 0 };
  const exactVerifier = provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
  });
  const verifier = async (...args) => {
    verification.calls += 1;
    return exactVerifier(...args);
  };
  const currentOpen = currentOpenSuppressionAdapterFixture({
    initialNumbers: Array.from(
      { length: 255 },
      (_, index) => PR.number + index,
    ),
    exactScopeCandidateNumber: PR.number,
  });
  const refreshAdapter = Object.freeze({
    schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
    schema_version: 2,
    configuration_digest: rawDigest(configurationLabel),
    collectCurrentOpenCandidateAuthority:
      currentOpen.collectProductionCandidateAuthority,
    async finishLegacyCandidateInventoryAttempt() {
      throw new Error("current-open mature checkpoint used legacy REST");
    },
  });
  const ledger = makeLedger(fixture, capability, verifier, preflight, {
    candidateInventoryRefreshAdapter: refreshAdapter,
  });
  await ledger.bootstrapCapability();
  await padOrdinaryLedgerWithStandaloneAttestations({
    fixture,
    verifier,
    targetCommitCount: 7,
  });
  const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
  const repositoryEndpointReceipt = scheduleRepositoryEndpointReceipt();
  const result = await ledger.reconcileCandidateInventoryRefreshAtomically({
    workflow_command_handle: workflowCommandHandle,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: repositoryEndpointReceipt,
  });
  assert.equal(result.persistence_mode, "current-open-generation-v3");
  return {
    fixture,
    ledger,
    result,
    trigger_identity: triggerIdentity,
    workflow_command_handle: workflowCommandHandle,
    repository_endpoint_receipt: repositoryEndpointReceipt,
    verification,
  };
}

test("current-open v3 mature continuation ref drift falls back to a full load",
  async () => {
    const context = await matureCurrentOpenContinuationFixture(
      "candidate-current-open-v3-mature-ref-drift-configuration-v1",
    );
    const {
      fixture,
      ledger,
      result,
      trigger_identity: triggerIdentity,
      workflow_command_handle: workflowCommandHandle,
      repository_endpoint_receipt: repositoryEndpointReceipt,
      verification,
    } = context;
    fixture.overrideNextRefReadTarget(
      result.publication_result.published_record_commit_shas[0],
    );
    const callIndex = fixture.calls.length;
    const verificationIndex = verification.calls;
    const writeIndex = fixture.writeCalls;
    const durableTip = fixture.refTarget;

    const dispatch = await ledger.loadOrReserveCandidateDispatch({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: repositoryEndpointReceipt,
      candidate_refresh_reconciliation_result: result,
    });

    const reads = fixture.calls.slice(callIndex).filter(({ method }) =>
      method === "GET");
    assert.equal(dispatch.state, "dispatch");
    assert.equal(dispatch.restarted, true);
    assert.equal(
      reads.filter(({ path }) =>
        path === "/git/ref/heads/codex-review-gate-ledger-v2").length,
      3,
    );
    assert.equal(
      reads.some(({ path }) => path.startsWith("/git/commits/")),
      true,
    );
    assert.equal(verification.calls - verificationIndex, 11);
    assert.equal(fixture.writeCalls - writeIndex, 0);
    assert.equal(fixture.refTarget, durableTip);
  });

test("current-open v3 mature continuation time regression falls back to a full load",
  async () => {
    const context = await matureCurrentOpenContinuationFixture(
      "candidate-current-open-v3-mature-time-regression-configuration-v1",
    );
    const {
      fixture,
      ledger,
      result,
      trigger_identity: triggerIdentity,
      workflow_command_handle: workflowCommandHandle,
      repository_endpoint_receipt: repositoryEndpointReceipt,
      verification,
    } = context;
    const durableTip = fixture.refTarget;
    assert.equal(result.publication_result.final_tip_commit_sha, durableTip);
    fixture.overrideRefReadServerTimeAfter(
      1,
      "2020-01-01T00:00:00.000Z",
    );
    const callIndex = fixture.calls.length;
    const verificationIndex = verification.calls;
    const writeIndex = fixture.writeCalls;

    const dispatch = await ledger.loadOrReserveCandidateDispatch({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: repositoryEndpointReceipt,
      candidate_refresh_reconciliation_result: result,
    });

    const reads = fixture.calls.slice(callIndex).filter(({ method }) =>
      method === "GET");
    assert.equal(dispatch.state, "dispatch");
    assert.equal(dispatch.restarted, true);
    assert.equal(
      reads.filter(({ path }) =>
        path === "/git/ref/heads/codex-review-gate-ledger-v2").length,
      3,
    );
    assert.equal(
      reads.some(({ path }) => path.startsWith("/git/commits/")),
      true,
    );
    assert.equal(verification.calls - verificationIndex, 11);
    assert.equal(fixture.writeCalls - writeIndex, 0);
    assert.equal(fixture.refTarget, durableTip);
  });

test("current-open v3 mature continuation binds command trigger and endpoint",
  async (t) => {
    const cases = [
      {
        label: "stable command",
        async input(context) {
          return {
            workflow_command_handle:
              await createScheduleWorkflowCommandHandle({
                selectionPolicy: "disabled",
              }),
            trigger_identity: context.trigger_identity,
            repository_endpoint_receipt:
              context.repository_endpoint_receipt,
          };
        },
        expected_error_code: "candidate-dispatch-restart-authority-mismatch",
      },
      {
        label: "trigger identity",
        async input(context) {
          return {
            workflow_command_handle: context.workflow_command_handle,
            trigger_identity: {
              ...context.trigger_identity,
              sha: "3".repeat(40),
            },
            repository_endpoint_receipt:
              context.repository_endpoint_receipt,
          };
        },
        expected_error_code: "candidate-dispatch-restart-authority-mismatch",
      },
      {
        label: "repository endpoint receipt",
        async input(context) {
          return {
            workflow_command_handle: context.workflow_command_handle,
            trigger_identity: context.trigger_identity,
            repository_endpoint_receipt: {
              ...context.repository_endpoint_receipt,
              raw_body_sha256: `sha256:${"b".repeat(64)}`,
            },
          };
        },
        expected_error_code: null,
      },
    ];
    for (const testCase of cases) {
      await t.test(testCase.label, async () => {
        const context = await matureCurrentOpenContinuationFixture(
          `candidate-current-open-v3-mature-${testCase.label}-binding-v1`,
        );
        const input = await testCase.input(context);
        const callIndex = context.fixture.calls.length;
        const verificationIndex = context.verification.calls;
        const writeIndex = context.fixture.writeCalls;
        let firstDispatch = null;
        let firstError = null;
        try {
          firstDispatch = await context.ledger
            .loadOrReserveCandidateDispatch({
              ...input,
              candidate_refresh_reconciliation_result: context.result,
            });
        } catch (error) {
          firstError = error;
        }
        const reads = context.fixture.calls.slice(callIndex)
          .filter(({ method }) => method === "GET");
        assert.equal(
          reads.filter(({ path }) =>
            path === "/git/ref/heads/codex-review-gate-ledger-v2").length,
          2,
        );
        assert.equal(
          reads.some(({ path }) => path.startsWith("/git/commits/")),
          true,
        );
        assert.equal(context.verification.calls - verificationIndex, 11);
        assert.equal(context.fixture.writeCalls - writeIndex, 0);
        if (testCase.expected_error_code === null) {
          assert.equal(firstError, null);
          assert.equal(firstDispatch.state, "dispatch");
        } else {
          assert.equal(firstDispatch, null);
          assert.equal(firstError?.code, testCase.expected_error_code);
        }

        const consumedCallIndex = context.fixture.calls.length;
        const consumedVerificationIndex = context.verification.calls;
        const consumed = await context.ledger.loadOrReserveCandidateDispatch({
          workflow_command_handle: context.workflow_command_handle,
          trigger_identity: context.trigger_identity,
          repository_endpoint_receipt:
            context.repository_endpoint_receipt,
          candidate_refresh_reconciliation_result: context.result,
        });
        const consumedReads = context.fixture.calls.slice(consumedCallIndex)
          .filter(({ method }) => method === "GET");
        assert.equal(consumed.state, "dispatch");
        assert.equal(
          consumedReads.filter(({ path }) =>
            path === "/git/ref/heads/codex-review-gate-ledger-v2").length,
          2,
        );
        assert.equal(
          context.verification.calls - consumedVerificationIndex,
          11,
        );
        if (firstDispatch !== null) {
          assert.equal(
            firstDispatch.plan.plan_digest,
            consumed.plan.plan_digest,
          );
        }
      });
    }
  });

test("current-open v3 reconciliation rejects 512 capacity and 513 scan cap without Git writes",
  async (t) => {
    for (const [candidateCount, expectedCode] of [
      [512, "candidate-inventory-cycle-commit-capacity"],
      [513, "CANDIDATE_OPEN_SET_CAP"],
    ]) {
      await t.test(`${candidateCount} candidates`, async () => {
        const fixture = githubGitFixture();
        const capability = scheduleCapabilityReceipt();
        const triggerIdentity = scheduleTriggerIdentity();
        const currentOpen = currentOpenSuppressionAdapterFixture({
          initialNumbers: Array.from(
            { length: candidateCount },
            (_, index) => index + 1,
          ),
        });
        let legacyFinishCalls = 0;
        const refreshAdapter = Object.freeze({
          schema:
            "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
          schema_version: 2,
          configuration_digest: rawDigest(
            `candidate-current-open-v3-cap-${candidateCount}-configuration-v1`,
          ),
          collectCurrentOpenCandidateAuthority:
            currentOpen.collectProductionCandidateAuthority,
          async finishLegacyCandidateInventoryAttempt() {
            legacyFinishCalls += 1;
            throw new Error("fresh current-open generation used legacy REST");
          },
        });
        const verifier = provenanceVerifier({
          eventName: "schedule",
          ref: triggerIdentity.ref,
          shaValue: triggerIdentity.sha,
        });
        const ledger = makeLedger(fixture, capability, verifier, null, {
          candidateInventoryRefreshAdapter: refreshAdapter,
        });
        await ledger.bootstrapCapability();
        const before = {
          writes: fixture.writeCalls,
          blobs: fixture.blobs.size,
          trees: fixture.trees.size,
          commits: fixture.commits.size,
          ref: fixture.refTarget,
        };

        await assert.rejects(
          ledger.reconcileCandidateInventoryRefreshAtomically({
            workflow_command_handle:
              await createScheduleWorkflowCommandHandle(),
            trigger_identity: triggerIdentity,
            repository_endpoint_receipt:
              scheduleRepositoryEndpointReceipt(),
          }),
          (error) => error.code === expectedCode,
        );

        assert.equal(currentOpen.calls.length, candidateCount === 512 ? 14 : 6);
        assert.equal(legacyFinishCalls, 0);
        assert.deepEqual({
          writes: fixture.writeCalls,
          blobs: fixture.blobs.size,
          trees: fixture.trees.size,
          commits: fixture.commits.size,
          ref: fixture.refTarget,
        }, before);
      });
    }
  });

test("atomic refresh reconciliation rejects forged attempts and concurrent reentry",
  async (t) => {
    for (const accessorKey of [
      "configuration_digest",
      "collectCurrentOpenProjection",
      "collectCandidateInventoryAttempt",
    ]) {
      await t.test(`constructor accessor ${accessorKey}`, () => {
        const fixture = githubGitFixture();
        let getterReads = 0;
        let callbackCalls = 0;
        const firstCallback = () => {
          callbackCalls += 1;
        };
        const secondCallback = () => {
          callbackCalls += 1;
        };
        const refreshAdapter = {
          schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
          schema_version: 1,
          configuration_digest: rawDigest(
            "candidate-refresh-accessor-first-configuration-v1",
          ),
          collectCurrentOpenProjection: firstCallback,
          collectCandidateInventoryAttempt: firstCallback,
        };
        Object.defineProperty(refreshAdapter, accessorKey, {
          enumerable: true,
          configurable: true,
          get() {
            getterReads += 1;
            if (accessorKey === "configuration_digest") {
              return rawDigest(
                getterReads === 1
                  ? "candidate-refresh-accessor-first-configuration-v1"
                  : "candidate-refresh-accessor-second-configuration-v1",
              );
            }
            return getterReads === 1 ? firstCallback : secondCallback;
          },
        });
        const before = {
          calls: fixture.calls.length,
          writes: fixture.writeCalls,
          blobs: fixture.blobs.size,
          trees: fixture.trees.size,
          commits: fixture.commits.size,
          ref: fixture.refTarget,
        };
        assert.throws(
          () => makeLedger(
            fixture,
            capabilityReceipt(),
            provenanceVerifier(),
            null,
            { candidateInventoryRefreshAdapter: refreshAdapter },
          ),
          /adapter requires own data properties/u,
        );
        assert.equal(getterReads, 0);
        assert.equal(callbackCalls, 0);
        assert.deepEqual({
          calls: fixture.calls.length,
          writes: fixture.writeCalls,
          blobs: fixture.blobs.size,
          trees: fixture.trees.size,
          commits: fixture.commits.size,
          ref: fixture.refTarget,
        }, before);
      });
    }

    for (const mode of ["extra-authority", "accessor-input"]) {
      await t.test(mode, async () => {
        const attempt = await stableEmptyCandidateRefreshReconciliationFixture({
          currentOpenNumbers: [PR.number],
        });
        const input = mode === "extra-authority"
          ? { ...attempt.input, must_persist_handle: {} }
          : { ...attempt.input };
        if (mode === "accessor-input") {
          delete input.trigger_identity;
          Object.defineProperty(input, "trigger_identity", {
            enumerable: true,
            configurable: true,
            get: () => attempt.input.trigger_identity,
          });
        }
        const before = {
          git: attempt.fixture.calls.length,
          current_open: attempt.currentOpen.calls.length,
          full: attempt.fullRefreshCalls.length,
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        };
        await assert.rejects(
          attempt.ledger.reconcileCandidateInventoryRefreshAtomically(input),
          mode === "extra-authority"
            ? /input keys are invalid/u
            : /input requires own data properties/u,
        );
        assert.deepEqual({
          git: attempt.fixture.calls.length,
          current_open: attempt.currentOpen.calls.length,
          full: attempt.fullRefreshCalls.length,
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        }, before);
      });
    }

    for (const mode of ["cloned-handle", "accessor", "attempt-index"]) {
      await t.test(mode, async () => {
        const attempt = await stableEmptyCandidateRefreshReconciliationFixture({
          currentOpenNumbers: [PR.number],
          wrapAttemptResult({ request, result }) {
            if (mode === "cloned-handle") {
              return {
                ...result,
                request_handle: structuredClone(request.request_handle),
              };
            }
            if (mode === "attempt-index") {
              return {
                ...result,
                attempt_index: request.attempt_index + 1,
              };
            }
            const forged = { ...result };
            Object.defineProperty(forged, "request_handle", {
              enumerable: true,
              configurable: true,
              get: () => request.request_handle,
            });
            return forged;
          },
        });
        const before = {
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        };
        await assert.rejects(
          attempt.ledger.reconcileCandidateInventoryRefreshAtomically(
            attempt.input,
          ),
          mode === "cloned-handle"
            ? (error) =>
                error.code === "UNTRUSTED_CANDIDATE_FULL_REFRESH_RESULT"
            : mode === "accessor"
              ? /own data properties/u
              : (error) =>
                  error.code === "UNTRUSTED_CANDIDATE_FULL_REFRESH_RESULT",
        );
        assert.deepEqual({
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        }, before);
      });
    }

    await t.test("prior request handle reuse", async () => {
      let priorRequestHandle = null;
      const attempt = await stableEmptyCandidateRefreshReconciliationFixture({
        currentOpenNumbers: [PR.number],
        collectAttempt({ request, fullTransport, defaultCollectAttempt }) {
          return request.attempt_index === 0
            ? readDriftingCandidateInventoryAttempt({
                request,
                transportFixture: fullTransport,
                finalNumbers: [PR.number],
              })
            : defaultCollectAttempt(request);
        },
        wrapAttemptResult({ request, result }) {
          if (request.attempt_index === 0) {
            priorRequestHandle = request.request_handle;
            return result;
          }
          return {
            ...result,
            request_handle: priorRequestHandle,
          };
        },
      });
      const before = {
        writes: attempt.fixture.writeCalls,
        blobs: attempt.fixture.blobs.size,
        trees: attempt.fixture.trees.size,
        commits: attempt.fixture.commits.size,
        ref: attempt.fixture.refTarget,
      };
      await assert.rejects(
        attempt.ledger.reconcileCandidateInventoryRefreshAtomically(
          attempt.input,
        ),
        (error) =>
          error.code === "UNTRUSTED_CANDIDATE_FULL_REFRESH_RESULT",
      );
      assert.equal(attempt.fullRefreshCalls.length, 2);
      assert.deepEqual({
        writes: attempt.fixture.writeCalls,
        blobs: attempt.fixture.blobs.size,
        trees: attempt.fixture.trees.size,
        commits: attempt.fixture.commits.size,
        ref: attempt.fixture.refTarget,
      }, before);
    });

    let releaseAttempt;
    let markAttemptStarted;
    const attemptStarted = new Promise((resolve) => {
      markAttemptStarted = resolve;
    });
    const attemptGate = new Promise((resolve) => {
      releaseAttempt = resolve;
    });
    const active = await stableEmptyCandidateRefreshReconciliationFixture({
      currentOpenNumbers: [PR.number],
      async collectAttempt({ request, defaultCollectAttempt }) {
        markAttemptStarted();
        await attemptGate;
        return defaultCollectAttempt(request);
      },
    });
    const first = active.ledger.reconcileCandidateInventoryRefreshAtomically(
      active.input,
    );
    await attemptStarted;
    const callsBeforeSecond = {
      git: active.fixture.calls.length,
      current_open: active.currentOpen.calls.length,
      full: active.fullRefreshCalls.length,
      writes: active.fixture.writeCalls,
    };
    await assert.rejects(
      active.ledger.reconcileCandidateInventoryRefreshAtomically(active.input),
      (error) => error.code === "CANDIDATE_REFRESH_RECONCILIATION_ACTIVE",
    );
    assert.deepEqual({
      git: active.fixture.calls.length,
      current_open: active.currentOpen.calls.length,
      full: active.fullRefreshCalls.length,
      writes: active.fixture.writeCalls,
    }, callsBeforeSecond);
    releaseAttempt();
    assert.equal((await first).state, "persisted");
  });

test("restricted bootstrap validates candidate adapters before external I/O",
  async (t) => {
    for (const mode of ["accessor", "double-adapter"]) {
      await t.test(mode, async () => {
        const fixture = githubGitFixture();
        const capability = scheduleCapabilityReceipt();
        const triggerIdentity = scheduleTriggerIdentity();
        let callbackCalls = 0;
        let getterReads = 0;
        const collect = async () => {
          callbackCalls += 1;
          return null;
        };
        const refreshAdapter = {
          schema:
            "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
          schema_version: 1,
          configuration_digest: rawDigest(
            "candidate-refresh-bootstrap-preflight-v1",
          ),
          collectCurrentOpenProjection: collect,
          collectCandidateInventoryAttempt: collect,
        };
        if (mode === "accessor") {
          delete refreshAdapter.collectCurrentOpenProjection;
          Object.defineProperty(
            refreshAdapter,
            "collectCurrentOpenProjection",
            {
              enumerable: true,
              configurable: true,
              get() {
                getterReads += 1;
                return collect;
              },
            },
          );
        }
        const legacy = currentOpenSuppressionAdapterFixture();
        let verifierCalls = 0;
        const baseVerifier = provenanceVerifier({
          eventName: "schedule",
          ref: triggerIdentity.ref,
          shaValue: triggerIdentity.sha,
        });
        const verifier = async (request) => {
          verifierCalls += 1;
          return baseVerifier(request);
        };
        const before = {
          calls: fixture.calls.length,
          writes: fixture.writeCalls,
          blobs: fixture.blobs.size,
          trees: fixture.trees.size,
          commits: fixture.commits.size,
          ref: fixture.refTarget,
        };
        await assert.rejects(
          Promise.resolve().then(async () => {
            const restricted = createV2GitHubGitLedgerBootstrap({
              fetch: fixture.fetch,
              token: SYNTHETIC_BEARER,
              repository: REPOSITORY,
              ledgerRef: V2_GIT_LEDGER_REF,
              restBaseUrl: "https://api.github.test",
              bootstrapCapabilityInput:
                bootstrapInputForCapability(capability),
              verifyWorkflowProvenance: verifier,
              candidateSuppressionEvidenceAdapter:
                mode === "double-adapter" ? legacy.adapter : null,
              candidateInventoryRefreshAdapter: refreshAdapter,
            });
            await restricted.bootstrapCapability();
          }),
          mode === "accessor"
            ? /adapter requires own data properties/u
            : /accepts one candidate refresh evidence adapter/u,
        );
        assert.equal(getterReads, 0);
        assert.equal(callbackCalls, 0);
        assert.equal(verifierCalls, 0);
        assert.deepEqual({
          calls: fixture.calls.length,
          writes: fixture.writeCalls,
          blobs: fixture.blobs.size,
          trees: fixture.trees.size,
          commits: fixture.commits.size,
          ref: fixture.refTarget,
        }, before);
      });
    }
  });

test("atomic refresh reconciliation retains drift in memory and bounds exhaustion",
  async (t) => {
    await t.test("drift then stable", async () => {
      const attempt = await stableEmptyCandidateRefreshReconciliationFixture({
        currentOpenNumbers: [PR.number],
        collectAttempt({ request, fullTransport, defaultCollectAttempt }) {
          return request.attempt_index === 0
            ? readDriftingCandidateInventoryAttempt({
                request,
                transportFixture: fullTransport,
                finalNumbers: [PR.number],
              })
            : defaultCollectAttempt(request);
        },
      });
      const patchCountBefore = attempt.fixture.calls.filter(({ method }) =>
        method === "PATCH").length;
      const reconciled = await attempt.ledger
        .reconcileCandidateInventoryRefreshAtomically(attempt.input);
      assert.equal(reconciled.state, "persisted");
      assert.equal(attempt.fullRefreshCalls.length, 2);
      assert.equal(
        reconciled.publication_result.projection.drift_attempt_count,
        1,
      );
      assert.deepEqual(
        reconciled.publication_result.projection.drift_codes,
        ["CANDIDATE_INVENTORY_DRIFT"],
      );
      assert.equal(
        reconciled.publication_result.published_record_commit_shas.length,
        5,
      );
      assert.equal(
        attempt.fixture.calls.filter(({ method }) => method === "PATCH")
          .length - patchCountBefore,
        1,
      );
    });

    await t.test("six drifting attempts", async () => {
      const attempt = await stableEmptyCandidateRefreshReconciliationFixture({
        currentOpenNumbers: [PR.number],
        collectAttempt({ request, fullTransport }) {
          return readDriftingCandidateInventoryAttempt({
            request,
            transportFixture: fullTransport,
            finalNumbers: [request.attempt_index + 1],
            mutateBeforeFinal: request.attempt_index === 0
              ? null
              : () => {
                  fullTransport.setCandidateState(
                    request.attempt_index,
                    "closed",
                  );
                },
          });
        },
      });
      const before = {
        writes: attempt.fixture.writeCalls,
        blobs: attempt.fixture.blobs.size,
        trees: attempt.fixture.trees.size,
        commits: attempt.fixture.commits.size,
        ref: attempt.fixture.refTarget,
      };
      await assert.rejects(
        attempt.ledger.reconcileCandidateInventoryRefreshAtomically(
          attempt.input,
        ),
        (error) =>
          error.code === "CANDIDATE_INVENTORY_STABILITY_EXHAUSTED",
      );
      assert.equal(attempt.fullRefreshCalls.length, 6);
      assert.deepEqual({
        writes: attempt.fixture.writeCalls,
        blobs: attempt.fixture.blobs.size,
        trees: attempt.fixture.trees.size,
        commits: attempt.fixture.commits.size,
        ref: attempt.fixture.refTarget,
      }, before);
    });

    await t.test("adapter-thrown drift is not a retry signal", async () => {
      const attempt = await stableEmptyCandidateRefreshReconciliationFixture({
        currentOpenNumbers: [PR.number],
        collectAttempt() {
          const error = new Error("adapter-originated drift");
          error.code = "CANDIDATE_INVENTORY_DRIFT";
          throw error;
        },
      });
      const before = {
        writes: attempt.fixture.writeCalls,
        blobs: attempt.fixture.blobs.size,
        trees: attempt.fixture.trees.size,
        commits: attempt.fixture.commits.size,
        ref: attempt.fixture.refTarget,
      };
      await assert.rejects(
        attempt.ledger.reconcileCandidateInventoryRefreshAtomically(
          attempt.input,
        ),
        (error) =>
          error.code === "CANDIDATE_INVENTORY_DRIFT" &&
          error.message === "adapter-originated drift",
      );
      assert.equal(attempt.fullRefreshCalls.length, 1);
      assert.deepEqual({
        writes: attempt.fixture.writeCalls,
        blobs: attempt.fixture.blobs.size,
        trees: attempt.fixture.trees.size,
        commits: attempt.fixture.commits.size,
        ref: attempt.fixture.refTarget,
      }, before);
    });
  });

test("atomic refresh reconciliation persists one candidate, recovers response loss, and rejects stale source",
  async (t) => {
    await t.test("one candidate", async () => {
      const attempt = await stableEmptyCandidateRefreshReconciliationFixture({
        currentOpenNumbers: [PR.number],
      });
      attempt.fullTransport.setListedNumbers([PR.number]);
      const currentOpenCallsBefore = attempt.currentOpen.calls.length;
      const fullRestCallsBefore = attempt.fullTransport.calls.length;
      const patchCountBefore = attempt.fixture.calls.filter(({ method }) =>
        method === "PATCH").length;
      const reconciled = await attempt.ledger
        .reconcileCandidateInventoryRefreshAtomically(attempt.input);
      assert.equal(reconciled.state, "persisted");
      assert.equal(attempt.fullRefreshCalls.length, 1);
      assert.equal(
        attempt.currentOpen.calls.length - currentOpenCallsBefore,
        4,
      );
      assert.equal(
        attempt.fullTransport.calls.length - fullRestCallsBefore,
        6,
      );
      assert.equal("runV2Operation" in attempt.refreshAdapter, false);
      assert.equal(
        attempt.fixture.calls.filter(({ method }) => method === "PATCH")
          .length - patchCountBefore,
        1,
      );
      const dispatch = await attempt.ledger.loadOrReserveCandidateDispatch(
        attempt.input,
      );
      assert.equal(dispatch.state, "dispatch");
      assert.equal(dispatch.restarted, true);
      assert.equal(dispatch.plan.items.length, 1);
      assert.equal(dispatch.plan.items[0].candidate.number, PR.number);
    });

    await t.test("applied PATCH response loss", async () => {
      const attempt = await stableEmptyCandidateRefreshReconciliationFixture({
        currentOpenNumbers: [PR.number],
      });
      const currentOpenCallsBefore = attempt.currentOpen.calls.length;
      const fullRestCallsBefore = attempt.fullTransport.calls.length;
      const patchCountBefore = attempt.fixture.calls.filter(({ method }) =>
        method === "PATCH").length;
      attempt.fixture.failNextCandidateInventoryUpdateAfterApply();
      const reconciled = await attempt.ledger
        .reconcileCandidateInventoryRefreshAtomically(attempt.input);
      assert.equal(reconciled.state, "persisted");
      assert.equal(attempt.fullRefreshCalls.length, 1);
      assert.equal(
        attempt.currentOpen.calls.length - currentOpenCallsBefore,
        4,
      );
      assert.equal(
        attempt.fullTransport.calls.length - fullRestCallsBefore,
        4,
      );
      assert.equal(reconciled.publication_result.append_receipts.length, 0);
      assert.equal(
        reconciled.publication_result.published_record_commit_shas.length,
        4,
      );
      assert.equal(
        attempt.fixture.calls.filter(({ method }) => method === "PATCH")
          .length - patchCountBefore,
        1,
      );
      const loaded = await attempt.ledger.load();
      assert.equal(
        reconciled.publication_result.final_tip_commit_sha,
        loaded.tip_commit_sha,
      );
    });

    await t.test("source changes while the trusted callback is pending",
      async () => {
        let releaseAttempt;
        let markAttemptStarted;
        const attemptStarted = new Promise((resolve) => {
          markAttemptStarted = resolve;
        });
        const attemptGate = new Promise((resolve) => {
          releaseAttempt = resolve;
        });
        const attempt = await stableEmptyCandidateRefreshReconciliationFixture({
          currentOpenNumbers: [PR.number],
          async collectAttempt({ request, defaultCollectAttempt }) {
            markAttemptStarted();
            await attemptGate;
            return defaultCollectAttempt(request);
          },
        });
        const stale = attempt.ledger
          .reconcileCandidateInventoryRefreshAtomically(attempt.input);
        await attemptStarted;
        const loaded = await attempt.ledger.load();
        const siblingTransport = candidateTransportFixture(1);
        const siblingCycle = await readCandidateInventoryCycle({
          transportFixture: siblingTransport,
          priorInventory: loaded.authority_projection.candidate_inventory
            .completed_cycle.final_inventory,
        });
        await attempt.ledger.appendCandidateInventoryCycleIfRequired({
          ...attempt.input,
          ...siblingCycle,
        });
        const afterSibling = {
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        };
        releaseAttempt();
        await assert.rejects(
          stale,
          (error) =>
            error.code === "STALE_CANDIDATE_INVENTORY_MUST_PERSIST_CLAIM",
        );
        assert.equal(attempt.fullRefreshCalls.length, 1);
        assert.deepEqual({
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        }, afterSibling);
      });
  });

test("current-open v3 reconciliation finishes one durable legacy cycle in place",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const transportFixture = candidateTransportFixture(1);
    const cycle = await readCandidateInventoryCycle({ transportFixture });
    const finishCalls = [];
    let currentOpenCalls = 0;
    const refreshAdapter = Object.freeze({
      schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
      schema_version: 2,
      configuration_digest: rawDigest(
        "candidate-refresh-legacy-finish-test-configuration-v1",
      ),
      async collectCurrentOpenCandidateAuthority() {
        currentOpenCalls += 1;
        throw new Error("legacy finish must not collect current-open evidence");
      },
      async finishLegacyCandidateInventoryAttempt(request) {
        finishCalls.push(request);
        return {
          schema:
            "codex-review-gate-git-ledger-candidate-inventory-legacy-finish-result-v2",
          schema_version: 1,
          request_handle: request.request_handle,
          cycle_id: request.cycle_id,
          start_record_oid: request.start_record_oid,
          next_shard_index: request.next_shard_index,
          initial_inventory: cycle.initial_inventory,
          shard_receipts: cycle.shard_receipts,
          final_inventory: cycle.final_inventory,
          final_shard_receipts: cycle.final_shard_receipts,
        };
      },
    });
    const ledger = makeLedger(
      fixture,
      capability,
      provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
      }),
      null,
      { candidateInventoryRefreshAdapter: refreshAdapter },
    );
    await ledger.bootstrapCapability();
    const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
    let loaded = await ledger.load();
    await appendCandidateInventoryPhase({
      ledger,
      loaded,
      phase: "cycle-start",
      initial: cycle.initial_inventory,
    });
    loaded = await ledger.load();
    const incomplete = loaded.authority_projection.candidate_inventory
      .incomplete_cycle;
    assert.notEqual(incomplete, null);
    const startRecordOid = incomplete.start_record_oid;
    const patchCountBefore = fixture.calls.filter(({ method }) =>
      method === "PATCH").length;
    const result = await ledger.reconcileCandidateInventoryRefreshAtomically({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    assert.equal(result.state, "persisted");
    assert.equal(result.persistence_mode, "legacy-finish-v1");
    assert.equal(result.publication_result.publication_outcome, "published");
    assert.equal(currentOpenCalls, 0);
    assert.equal(finishCalls.length, 1);
    assert.equal(finishCalls[0].mode, "finish-existing");
    assert.equal(finishCalls[0].query_state, "all");
    assert.equal(finishCalls[0].start_record_oid, startRecordOid);
    assert.equal(
      fixture.calls.filter(({ method }) => method === "PATCH").length -
        patchCountBefore,
      1,
    );
    const final = await ledger.load();
    const authority = final.authority_projection.candidate_inventory;
    assert.equal(authority.incomplete_cycle, null);
    assert.equal(authority.completed_cycle.start_record_oid, startRecordOid);
    assert.equal(
      final.records.filter((entry) =>
        entry.envelope.record_type === "candidate-inventory-observation" &&
        entry.envelope.payload.schema_version === 1 &&
        entry.envelope.payload.phase === "cycle-start").length,
      1,
    );
    assert.notEqual(
      final.authority_projection.candidate_dispatch.active_reservation,
      null,
    );
  });

test("current-open v3 legacy finish is one-shot, recoverable, and bounded",
  async (t) => {
    await t.test("continues one exact durable shard prefix", async () => {
      const cycle = directClosedCandidateInventoryCycle(257);
      const attempt = await legacyIncompleteCandidateRefreshFixture({
        candidateCount: 0,
        cycleOverride: cycle,
        seedShardCount: 1,
      });
      const before = await attempt.ledger.load();
      const result = await attempt.ledger
        .reconcileCandidateInventoryRefreshAtomically(attempt.input);
      assert.equal(result.state, "persisted");
      assert.equal(attempt.finishCalls.length, 1);
      assert.equal(attempt.finishCalls[0].next_shard_index, 1);
      assert.deepEqual(
        attempt.finishCalls[0].completed_shard_receipts,
        cycle.shard_receipts.slice(0, 1),
      );
      assert.equal(
        result.publication_result.published_record_commit_shas.length,
        3,
        "remaining shard, same-cycle completion, and empty dispatch share one CAS",
      );
      const final = await attempt.ledger.load();
      assert.equal(final.commit_count - before.commit_count, 3);
      assert.equal(
        final.authority_projection.candidate_inventory.completed_cycle
          .start_record_oid,
        before.authority_projection.candidate_inventory.incomplete_cycle
          .start_record_oid,
      );
    });

    await t.test(
      "rejects future nested shard evidence before publication planning",
      async () => {
        const cycle = directClosedCandidateInventoryCycle(2);
        let malicious = true;
        let futureEvidenceAt = null;
        const attempt = await legacyIncompleteCandidateRefreshFixture({
          candidateCount: 2,
          cycleOverride: cycle,
          wrapFinishResult({ result }) {
            if (!malicious) return result;
            const writeFenceAt = new Date(
              Date.parse(attempt.fixture.peekServerTime()) + 30_000,
            ).toISOString();
            futureEvidenceAt = new Date(
              Date.parse(writeFenceAt) + 30_000,
            ).toISOString();
            attempt.fixture.overrideRefReadServerTimeAfter(
              3,
              writeFenceAt,
            );
            return {
              ...result,
              shard_receipts: [directCandidateShardReceiptWithEndpointTimes(
                result.shard_receipts[0],
                [futureEvidenceAt, result.shard_receipts[0].observed_at],
              )],
            };
          },
        });
        const before = {
          writes: attempt.fixture.writeCalls,
          patches: attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        };

        await assert.rejects(
          attempt.ledger.reconcileCandidateInventoryRefreshAtomically(
            attempt.input,
          ),
          (error) => error.code === "candidate-inventory-cycle-stale",
        );

        assert.notEqual(futureEvidenceAt, null);
        assert.equal(attempt.finishCalls.length, 1);
        assert.equal(attempt.currentOpenCalls, 0);
        assert.deepEqual({
          writes: attempt.fixture.writeCalls,
          patches: attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        }, before);

        malicious = false;
        const retry = await attempt.ledger
          .reconcileCandidateInventoryRefreshAtomically(attempt.input);
        assert.equal(retry.state, "persisted");
        assert.equal(retry.persistence_mode, "legacy-finish-v1");
        assert.equal(attempt.finishCalls.length, 2);
      },
    );

    await t.test(
      "authority replay rejects resealed future nested shard evidence",
      async () => {
        const attempt = await legacyIncompleteCandidateRefreshFixture({
          candidateCount: 2,
          cycleOverride: directClosedCandidateInventoryCycle(2),
          seedShardCount: 1,
        });
        const loaded = await attempt.ledger.load();
        const forged = structuredClone(loaded.records);
        const shardEntry = forged.findLast((entry) =>
          entry.envelope.record_type === "candidate-inventory-observation" &&
          entry.envelope.payload.schema_version === 1 &&
          entry.envelope.payload.phase === "shard");
        assert.notEqual(shardEntry, undefined);
        const futureEvidenceAt = new Date(
          Date.parse(shardEntry.envelope.server_observed_at) + 1_000,
        ).toISOString();
        const payload = structuredClone(shardEntry.envelope.payload);
        payload.shard_receipt = directCandidateShardReceiptWithEndpointTimes(
          payload.shard_receipt,
          [futureEvidenceAt, payload.shard_receipt.observed_at],
        );
        resealFixtureCandidateInventoryEnvelopePayload(
          shardEntry.envelope,
          payload,
        );

        assert.throws(
          () => deriveV2GitLedgerCandidateInventoryAuthority(
            forged,
            REPOSITORY,
          ),
          (error) => error.code === "candidate-inventory-time",
        );
      },
    );

    await t.test(
      "authority replay rejects transport regression hidden by predecessor top-level time",
      async () => {
        const attempt = await legacyIncompleteCandidateRefreshFixture({
          candidateCount: 257,
          cycleOverride: directClosedCandidateInventoryCycle(257),
          seedShardCount: 2,
        });
        const loaded = await attempt.ledger.load();
        const forged = structuredClone(loaded.records);
        const shardEntries = forged.filter((entry) =>
          entry.envelope.record_type === "candidate-inventory-observation" &&
          entry.envelope.payload.schema_version === 1 &&
          entry.envelope.payload.phase === "shard");
        assert.equal(shardEntries.length, 2);
        const priorRecordTime = Date.parse(
          shardEntries[0].envelope.server_observed_at,
        );
        const initialTime = Date.parse(
          forged.find((entry) =>
            entry.envelope.record_type ===
              "candidate-inventory-observation" &&
            entry.envelope.payload.schema_version === 1 &&
            entry.envelope.payload.phase === "cycle-start")
            .envelope.payload.initial_inventory.observed_at,
        );
        assert.ok(priorRecordTime - initialTime >= 3);
        const priorTopLevelTime = new Date(priorRecordTime - 2).toISOString();
        const nextTime = new Date(priorRecordTime - 1).toISOString();
        const priorNestedTime = new Date(priorRecordTime).toISOString();

        const priorPayload = structuredClone(
          shardEntries[0].envelope.payload,
        );
        priorPayload.shard_receipt =
          directCandidateShardReceiptWithEndpointTimes(
            priorPayload.shard_receipt,
            priorPayload.shard_receipt.observations.map((_, index) =>
              index === 0 ? priorNestedTime : priorTopLevelTime),
          );
        resealFixtureCandidateInventoryEnvelopePayload(
          shardEntries[0].envelope,
          priorPayload,
        );

        const nextEntryIndex = forged.indexOf(shardEntries[1]);
        const nextPayload = structuredClone(
          shardEntries[1].envelope.payload,
        );
        nextPayload.prior_candidate_authority_digest =
          deriveV2GitLedgerCandidateInventoryAuthority(
            forged.slice(0, nextEntryIndex),
            REPOSITORY,
          ).authority_digest;
        nextPayload.shard_receipt =
          directCandidateShardReceiptWithEndpointTimes(
            nextPayload.shard_receipt,
            nextPayload.shard_receipt.observations.map(() => nextTime),
          );
        resealFixtureCandidateInventoryEnvelopePayload(
          shardEntries[1].envelope,
          nextPayload,
        );

        assert.throws(
          () => deriveV2GitLedgerCandidateInventoryAuthority(
            forged,
            REPOSITORY,
          ),
          (error) => error.code === "candidate-inventory-time",
        );
      },
    );

    await t.test("rejects a valid but different durable shard prefix",
      async () => {
        const cycle = directClosedCandidateInventoryCycle(257);
        const alternate = {
          ...cycle,
          shard_receipts: directClosedCandidateShardReceipts(
            cycle.initial_inventory,
            "-alternate",
          ),
          final_shard_receipts: directClosedCandidateShardReceipts(
            cycle.final_inventory,
            "-alternate",
          ),
        };
        const attempt = await legacyIncompleteCandidateRefreshFixture({
          candidateCount: 0,
          cycleOverride: cycle,
          seedShardCount: 1,
          wrapFinishResult({ result }) {
            return {
              ...result,
              shard_receipts: alternate.shard_receipts,
              final_shard_receipts: alternate.final_shard_receipts,
            };
          },
        });
        const before = {
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        };
        await assert.rejects(
          attempt.ledger.reconcileCandidateInventoryRefreshAtomically(
            attempt.input,
          ),
          (error) => error.code ===
            "CANDIDATE_INVENTORY_LEGACY_FINISH_MISMATCH",
        );
        assert.deepEqual({
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        }, before);
      });

    await t.test("applied PATCH response loss", async () => {
      const attempt = await legacyIncompleteCandidateRefreshFixture();
      const loadedBefore = await attempt.ledger.load();
      const patchCountBefore = attempt.fixture.calls.filter(({ method }) =>
        method === "PATCH").length;
      attempt.fixture.failNextCandidateInventoryUpdateAfterApply();
      const result = await attempt.ledger
        .reconcileCandidateInventoryRefreshAtomically(attempt.input);
      assert.equal(result.state, "persisted");
      assert.equal(result.publication_result.publication_outcome,
        "recovered-after-apply");
      assert.equal(result.publication_result.append_receipts.length, 0);
      assert.equal(
        result.publication_result.published_record_commit_shas.length,
        attempt.cycle.shard_receipts.length + 2,
      );
      assert.equal(
        attempt.fixture.calls.filter(({ method }) => method === "PATCH")
          .length - patchCountBefore,
        1,
      );
      const final = await attempt.ledger.load();
      assert.equal(
        final.commit_count - loadedBefore.commit_count,
        attempt.cycle.shard_receipts.length + 2,
      );
      assert.equal(attempt.currentOpenCalls, 0);
    });

    await t.test("cloned and reused request handles", async () => {
      let firstHandle = null;
      let callbackIndex = 0;
      const attempt = await legacyIncompleteCandidateRefreshFixture({
        wrapFinishResult({ request, result }) {
          callbackIndex += 1;
          if (callbackIndex === 1) {
            firstHandle = request.request_handle;
            return {
              ...result,
              request_handle: structuredClone(request.request_handle),
            };
          }
          return { ...result, request_handle: firstHandle };
        },
      });
      const before = {
        writes: attempt.fixture.writeCalls,
        blobs: attempt.fixture.blobs.size,
        trees: attempt.fixture.trees.size,
        commits: attempt.fixture.commits.size,
        ref: attempt.fixture.refTarget,
      };
      for (let index = 0; index < 2; index += 1) {
        await assert.rejects(
          attempt.ledger.reconcileCandidateInventoryRefreshAtomically(
            attempt.input,
          ),
          (error) => error.code ===
            "UNTRUSTED_CANDIDATE_INVENTORY_LEGACY_FINISH_RESULT",
        );
        assert.deepEqual({
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        }, before);
      }
      assert.equal(attempt.finishCalls.length, 2);
      assert.equal(attempt.currentOpenCalls, 0);
    });

    await t.test("adapter evidence drift", async () => {
      const attempt = await legacyIncompleteCandidateRefreshFixture({
        wrapFinishResult({ result }) {
          return {
            ...result,
            final_inventory: result.initial_inventory,
            final_shard_receipts: result.shard_receipts,
          };
        },
      });
      const before = {
        writes: attempt.fixture.writeCalls,
        blobs: attempt.fixture.blobs.size,
        trees: attempt.fixture.trees.size,
        commits: attempt.fixture.commits.size,
        ref: attempt.fixture.refTarget,
      };
      await assert.rejects(
        attempt.ledger.reconcileCandidateInventoryRefreshAtomically(
          attempt.input,
        ),
        (error) => new Set([
          "CANDIDATE_INVENTORY_DRIFT",
          "CANDIDATE_LIFECYCLE_DRIFT",
        ]).has(error.code),
      );
      assert.deepEqual({
        writes: attempt.fixture.writeCalls,
        blobs: attempt.fixture.blobs.size,
        trees: attempt.fixture.trees.size,
        commits: attempt.fixture.commits.size,
        ref: attempt.fixture.refTarget,
      }, before);
      assert.equal(attempt.currentOpenCalls, 0);
    });

    await t.test("durable source changes during the finish callback",
      async () => {
        let releaseFinish;
        let markFinishStarted;
        const finishStarted = new Promise((resolve) => {
          markFinishStarted = resolve;
        });
        const finishGate = new Promise((resolve) => {
          releaseFinish = resolve;
        });
        const attempt = await legacyIncompleteCandidateRefreshFixture({
          async wrapFinishResult({ result }) {
            markFinishStarted();
            await finishGate;
            return result;
          },
        });
        const reconciliation = attempt.ledger
          .reconcileCandidateInventoryRefreshAtomically(attempt.input);
        await finishStarted;
        const loaded = await attempt.ledger.load();
        await appendCandidateInventoryPhase({
          ledger: attempt.ledger,
          loaded,
          phase: "shard",
          initial: attempt.cycle.initial_inventory,
          shard: attempt.cycle.shard_receipts[0],
        });
        const afterSibling = {
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        };
        releaseFinish();
        await assert.rejects(
          reconciliation,
          (error) => error.code ===
            "STALE_CANDIDATE_INVENTORY_LEGACY_FINISH_CLAIM",
        );
        assert.deepEqual({
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        }, afterSibling);
        assert.equal(attempt.currentOpenCalls, 0);
      });

    await t.test("512-candidate tail capacity", async () => {
      const attempt = await legacyIncompleteCandidateRefreshFixture({
        candidateCount: 512,
      });
      const before = {
        writes: attempt.fixture.writeCalls,
        blobs: attempt.fixture.blobs.size,
        trees: attempt.fixture.trees.size,
        commits: attempt.fixture.commits.size,
        ref: attempt.fixture.refTarget,
      };
      await assert.rejects(
        attempt.ledger.reconcileCandidateInventoryRefreshAtomically(
          attempt.input,
        ),
        (error) => error.code ===
          "CANDIDATE_INVENTORY_LEGACY_FINISH_CAPACITY",
      );
      assert.deepEqual({
        writes: attempt.fixture.writeCalls,
        blobs: attempt.fixture.blobs.size,
        trees: attempt.fixture.trees.size,
        commits: attempt.fixture.commits.size,
        ref: attempt.fixture.refTarget,
      }, before);
      assert.equal(attempt.finishCalls.length, 1);
      assert.equal(attempt.currentOpenCalls, 0);
    });
  });

test("compact hydrated replay rejects transport regression hidden by artifact top-level time",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const ledger = makeLedger(
      fixture,
      capability,
      provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
      }),
    );
    await ledger.bootstrapCapability();
    const cycle = await readIdentityDriftCandidateInventoryCycle({
      transportFixture: candidateTransportFixture(2),
      priorInventory: null,
      listedNumbersAfterInitial: [3, 4],
    });
    await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...cycle,
    });
    const loaded = await ledger.load();
    const startIndex = loaded.records.findIndex((entry) =>
      entry.envelope.record_type === "candidate-inventory-observation" &&
      entry.envelope.payload.schema_version === 2 &&
      entry.envelope.payload.phase === "cycle-start");
    const attemptIndex = loaded.records.findIndex((entry) =>
      entry.envelope.record_type === "candidate-inventory-observation" &&
      entry.envelope.payload.schema_version === 2 &&
      entry.envelope.payload.phase === "attempt");
    assert.ok(startIndex >= 0 && attemptIndex > startIndex);
    const forged = loaded.records.slice(0, attemptIndex + 1)
      .map((entry) => structuredClone(entry));
    const startEntry = forged[startIndex];
    const attemptEntry = forged[attemptIndex];
    assert.equal(attemptEntry.envelope.payload.attempt_count, 2);

    const evidence = structuredClone(cycle.drift_attempts[0]);
    assert.equal(evidence.shard_receipts.length, 1);
    assert.equal(evidence.shard_receipts[0].observations.length, 2);
    const priorTopLevelTime = evidence.shard_receipts[0].observed_at;
    const priorNestedTime = attemptEntry.envelope.server_observed_at;
    assert.ok(
      Date.parse(priorNestedTime) - Date.parse(priorTopLevelTime) >= 2,
    );
    evidence.shard_receipts[0] =
      directCandidateShardReceiptWithEndpointTimes(
        evidence.shard_receipts[0],
        evidence.shard_receipts[0].observations.map((_, index) =>
          index === 0 ? priorNestedTime : priorTopLevelTime),
      );
    const manifest = resealFixtureCandidateInventoryAttemptManifest(
      attemptEntry.envelope.payload.attachment_manifest,
      evidence,
    );

    const startPayload = structuredClone(startEntry.envelope.payload);
    startPayload.expected_attempt_evidence_digests[0] =
      manifest.attempt_evidence_digest;
    resealFixtureCandidateInventoryEnvelopePayload(
      startEntry.envelope,
      startPayload,
    );
    const attemptPayload = structuredClone(attemptEntry.envelope.payload);
    attemptPayload.prior_candidate_authority_digest =
      deriveV2GitLedgerCandidateInventoryAuthority(
        forged.slice(0, attemptIndex),
        REPOSITORY,
      ).authority_digest;
    attemptPayload.expected_attempt_evidence_digests[0] =
      manifest.attempt_evidence_digest;
    attemptPayload.attempt_evidence_digest =
      manifest.attempt_evidence_digest;
    attemptPayload.attachment_manifest = manifest;
    resealFixtureCandidateInventoryEnvelopePayload(
      attemptEntry.envelope,
      attemptPayload,
    );
    attemptEntry.candidate_inventory_attachment_evidence = evidence;

    assert.throws(
      () => deriveV2GitLedgerCandidateInventoryAuthority(forged, REPOSITORY),
      (error) => error.code === "candidate-inventory-time",
    );
  });

test("compact atomic restart preserves scheduled scope through terminal ACK",
  async () => {
    const runIdentity = { ...OWNER };
    const preflight = await loadFixtureProviderPreflight();
    const attempt = await stableEmptyCandidateRefreshReconciliationFixture({
      currentOpenNumbers: [PR.number],
      fullTransportOptions: {
        exactScopeCandidateNumber: PR.number,
      },
      preflightHandle: preflight,
      runIdentity,
    });
    attempt.fullTransport.setListedNumbers([PR.number]);
    const reconciled = await attempt.ledger
      .reconcileCandidateInventoryRefreshAtomically(attempt.input);
    assert.equal(reconciled.state, "persisted");
    const reserved = await attempt.ledger.loadOrReserveCandidateDispatch(
      attempt.input,
    );
    assert.equal(reserved.state, "dispatch");
    const loaded = await attempt.ledger.load();
    assert.equal(
      loaded.authority_projection.candidate_inventory.completed_cycle
        .final_inventory.candidates.length,
      1,
    );
    assert.equal(
      loaded.authority_projection.candidate_inventory.open_pr_discovery
        .candidates.length,
      1,
    );
    const compactAttempt = loaded.records.find((entry) =>
      entry.envelope.record_type === "candidate-inventory-observation" &&
      entry.envelope.payload.schema_version === 2 &&
      entry.envelope.payload.phase === "attempt");
    assert.notEqual(compactAttempt, undefined);
    assert.equal(
      Object.hasOwn(
        compactAttempt,
        "candidate_inventory_attachment_evidence",
      ),
      false,
    );
    assert.equal(
      Object.hasOwn(
        compactAttempt,
        "candidate_inventory_attachment_tree_sha",
      ),
      false,
    );

    runIdentity.run_id = "9002";
    runIdentity.run_attempt = 1;
    const restarted = makeLedger(
      attempt.fixture,
      attempt.capability,
      attempt.verifier,
      preflight,
      { candidateInventoryRefreshAdapter: attempt.refreshAdapter },
    );
    const scheduledCommand = await createScheduleWorkflowCommandHandle({
      pullRequestNumber: PR.number,
      dispatchBinding: reserved.plan.items[0],
      runIdentity,
    });
    const minimal = await loadFixtureMinimalScope();
    const scheduled = await restarted
      .loadCandidateDispatchForScheduledPullRequest({
        workflow_command_handle: scheduledCommand,
        minimal_scope_handle: minimal,
        trigger_identity: attempt.triggerIdentity,
        expected_dispatch_binding: reserved.plan.items[0],
      });
    const preScopeReceipt = await restarted
      .loadScheduledPullRequestEvaluatedScopeReceipt({
        candidate_dispatch_handle: scheduled.candidate_dispatch_handle,
        minimal_scope_handle: minimal,
        trigger_identity: attempt.triggerIdentity,
      });
    const discovery = await acquireFixtureLeasedDiscovery({
      ledger: restarted,
      command: scheduledCommand,
      preScopeReceipt,
      minimalPre: minimal,
      leaseTtlSeconds: 900,
    });
    const completed = await completeScheduledCandidateDispatchAttempt({
      ...attempt,
      ledger: restarted,
      preflight,
      reserved,
      scheduled,
      scheduledCommand,
      discovery,
    });
    assert.notEqual(completed.ack.ack_append_receipt, null);
    assert.notEqual(completed.ack.batch_completion_append_receipt, null);
    assert.notEqual(completed.ack.cycle_completion_append_receipt, null);
    const final = await restarted.load();
    assert.equal(final.active_lease, null);
    assert.equal(
      final.authority_projection.candidate_dispatch.current_cycle
        .cycle_complete,
      true,
    );

    const priorInventory = final.authority_projection.candidate_inventory
      .completed_cycle.final_inventory;
    const omittedPrior = directCandidateInventory({
      candidates: [{
        id: String(1000 + PR.number + 1),
        node_id: `PR_candidate_${PR.number + 1}`,
        number: PR.number + 1,
        created_at: new Date(
          Date.parse("2026-08-01T00:00:00.000Z") +
            (PR.number + 1) * 1000,
        ).toISOString(),
      }],
      priorInventoryDigest: priorInventory.receipt_digest,
    });
    const beforeShrink = {
      writes: attempt.fixture.writeCalls,
      blobs: attempt.fixture.blobs.size,
      trees: attempt.fixture.trees.size,
      commits: attempt.fixture.commits.size,
      ref: attempt.fixture.refTarget,
    };
    runIdentity.run_id = OWNER.run_id;
    runIdentity.run_attempt = OWNER.run_attempt;
    await assert.rejects(
      appendCandidateInventoryPhase({
        ledger: restarted,
        loaded: final,
        phase: "cycle-start",
        initial: omittedPrior,
      }),
      (error) => error.code === "candidate-inventory-shrink",
    );
    assert.deepEqual({
      writes: attempt.fixture.writeCalls,
      blobs: attempt.fixture.blobs.size,
      trees: attempt.fixture.trees.size,
      commits: attempt.fixture.commits.size,
      ref: attempt.fixture.refTarget,
    }, beforeShrink);

    runIdentity.run_id = "9003";
    runIdentity.run_attempt = 1;
    const c2Ledger = makeLedger(
      attempt.fixture,
      attempt.capability,
      attempt.verifier,
      preflight,
      { candidateInventoryRefreshAdapter: attempt.refreshAdapter },
    );
    const c2Before = await c2Ledger.load();
    attempt.fullTransport.setListedNumbers([PR.number + 1]);
    attempt.fullTransport.setCandidateState(PR.number, "closed");
    const c2Cycle = await readCandidateInventoryCycle({
      transportFixture: attempt.fullTransport,
      priorInventory: c2Before.authority_projection.candidate_inventory
        .completed_cycle.final_inventory,
    });
    const c2Command = await createScheduleWorkflowCommandHandle({
      runIdentity,
    });
    attempt.fixture.failNextCandidateInventoryUpdateAfterApply();
    const c2 = await c2Ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: c2Command,
      trigger_identity: attempt.triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...c2Cycle,
    });
    assert.equal(c2.state, "persisted");
    assert.equal(c2.reason, "semantic-change");
    assert.equal(c2.append_receipts.length, 0);
    assert.equal(c2.published_record_commit_shas.length, 4);
    const c2Restarted = makeLedger(
      attempt.fixture,
      attempt.capability,
      attempt.verifier,
      preflight,
      { candidateInventoryRefreshAdapter: attempt.refreshAdapter },
    );
    const c2Loaded = await c2Restarted.load();
    assert.deepEqual(
      c2Loaded.authority_projection.candidate_inventory.completed_cycle
        .final_inventory.candidates.map(({ number }) => number),
      [PR.number, PR.number + 1],
    );
    const publicCompactAttempts = c2Loaded.records.filter((entry) =>
      entry.envelope.record_type === "candidate-inventory-observation" &&
      entry.envelope.payload.schema_version === 2 &&
      entry.envelope.payload.phase === "attempt");
    assert.equal(publicCompactAttempts.length, 3);
    for (const entry of publicCompactAttempts) {
      assert.equal(
        Object.hasOwn(entry, "candidate_inventory_attachment_evidence"),
        false,
      );
      assert.equal(
        Object.hasOwn(entry, "candidate_inventory_attachment_tree_sha"),
        false,
      );
    }
  });

test("compact atomic restart recovers released and expired scheduled attempts",
  async (t) => {
    await t.test("applied publication response loss then released recovery",
      async () => {
        const attempt = await createCompactScheduledCandidateDispatchAttempt({
          failPublicationAfterApply: true,
        });
        assert.equal(
          attempt.publication.publication_result.append_receipts.length,
          0,
        );
        assert.equal(
          attempt.publication.publication_result
            .published_record_commit_shas.length,
          4,
        );
        const releaseReceipt = await releaseFixtureDiscoveryLease(
          attempt.ledger,
          attempt.discovery,
        );
        const restarted = makeLedger(
          attempt.fixture,
          attempt.capability,
          attempt.verifier,
          attempt.preflight,
          { candidateInventoryRefreshAdapter: attempt.refreshAdapter },
        );
        const recovered = await restarted.recoverCandidateDispatchFailure({
          workflow_command_handle: attempt.scheduledCommand,
          trigger_identity: attempt.triggerIdentity,
          repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
          expected_dispatch_binding: attempt.reserved.plan.items[0],
        });
        assert.equal(recovered.recovery_mode, "released");
        assert.equal(recovered.result.outcome, "failed");
        assert.equal(recovered.result.public_effects_performed, 0);
        assert.notEqual(recovered.ack_append_receipt, null);
        assert.notEqual(recovered.batch_completion_append_receipt, null);
        assert.notEqual(recovered.cycle_completion_append_receipt, null);
        assert.equal(recovered.cycle_complete, true);
        const loaded = await restarted.load();
        assert.equal(loaded.active_lease, null);
        assert.equal(
          loaded.authority_projection.candidate_dispatch.current_cycle
            .cycle_complete,
          true,
        );
        const ack = loaded.records.find((entry) =>
          entry.envelope.record_type === "candidate-dispatch-observation" &&
          entry.envelope.payload.phase === "candidate-ack");
        assert.equal(
          ack.envelope.payload.candidate_ack.terminal_authority
            .lease_release_record_oid,
          releaseReceipt.commit_sha,
        );
      });

    await t.test("fresh factory expired recovery", async () => {
      const attempt = await createCompactScheduledCandidateDispatchAttempt({
        leaseTtlSeconds: 60,
      });
      attempt.runIdentity.run_id = "9003";
      attempt.runIdentity.run_attempt = 1;
      const recoveryCommand = await createScheduleWorkflowCommandHandle({
        runIdentity: attempt.runIdentity,
      });
      attempt.fixture.advanceServerTime(1_000);
      const restarted = makeLedger(
        attempt.fixture,
        attempt.capability,
        attempt.verifier,
        attempt.preflight,
        { candidateInventoryRefreshAdapter: attempt.refreshAdapter },
      );
      const ready = await restarted.loadOrReserveCandidateDispatch({
        workflow_command_handle: recoveryCommand,
        trigger_identity: attempt.triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      });
      assert.equal(ready.state, "recovery-required");
      assert.equal(ready.recovery_required.ready, true);
      assert.equal(ready.recovery_required.mode, "expired");
      const recovered = await restarted.recoverCandidateDispatchFailure({
        workflow_command_handle: recoveryCommand,
        trigger_identity: attempt.triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        expected_dispatch_binding:
          ready.recovery_required.expected_dispatch_binding,
      });
      assert.equal(recovered.recovery_mode, "expired");
      assert.equal(recovered.result.outcome, "failed");
      assert.equal(recovered.result.public_effects_performed, 0);
      assert.equal(recovered.cycle_complete, true);
      const loaded = await restarted.load();
      assert.equal(loaded.active_lease, null);
      const ack = loaded.records.find((entry) =>
        entry.envelope.record_type === "candidate-dispatch-observation" &&
        entry.envelope.payload.phase === "candidate-ack");
      assert.equal(
        ack.envelope.payload.candidate_ack.terminal_authority
          .lease_release_record_oid,
        null,
      );
    });
  });

test("atomic refresh reconciliation exposes typed capacity boundaries without admissions",
  async (t) => {
    await t.test("bootstrap checkpoint is required without leaking handles",
      async () => {
        const fixture = githubGitFixture();
        const capability = scheduleCapabilityReceipt();
        const triggerIdentity = scheduleTriggerIdentity();
        const transportFixture = candidateTransportFixture(255);
        const cycle = await readIdentityDriftCandidateInventoryCycle({
          transportFixture,
          priorInventory: null,
          listedNumbersAfterInitial: Array.from(
            { length: 255 },
            (_, index) => index + 2,
          ),
          mutateAfterInitial: () => {
            transportFixture.setCandidateState(1, "closed");
          },
        });
        const currentOpen = currentOpenSuppressionAdapterFixture({
          initialNumbers: [PR.number],
        });
        const fullRefreshCalls = [];
        const refreshAdapter = Object.freeze({
          schema:
            "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
          schema_version: 1,
          configuration_digest: rawDigest(
            "bootstrap-checkpoint-refresh-adapter-configuration-v1",
          ),
          collectCurrentOpenProjection:
            currentOpen.adapter.collectCurrentOpenProjection,
          async collectCandidateInventoryAttempt(request) {
            fullRefreshCalls.push(request);
            const evidence = request.attempt_index === 0
              ? cycle.drift_attempts[0]
              : {
                  initial_inventory: cycle.initial_inventory,
                  shard_receipts: cycle.shard_receipts,
                  final_inventory: cycle.final_inventory,
                  final_shard_receipts: cycle.final_shard_receipts,
                };
            return {
              schema:
                "codex-review-gate-git-ledger-candidate-full-refresh-result-v2",
              schema_version: 1,
              request_handle: request.request_handle,
              attempt_index: request.attempt_index,
              ...evidence,
            };
          },
        });
        const ledger = makeLedger(
          fixture,
          capability,
          provenanceVerifier({
            eventName: "schedule",
            ref: triggerIdentity.ref,
            shaValue: triggerIdentity.sha,
          }),
          null,
          { candidateInventoryRefreshAdapter: refreshAdapter },
        );
        await ledger.bootstrapCapability();
        const input = {
          workflow_command_handle: await createScheduleWorkflowCommandHandle(),
          trigger_identity: triggerIdentity,
          repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        };
        const before = {
          writes: fixture.writeCalls,
          blobs: fixture.blobs.size,
          trees: fixture.trees.size,
          commits: fixture.commits.size,
          ref: fixture.refTarget,
        };
        await assert.rejects(
          ledger.reconcileCandidateInventoryRefreshAtomically(input),
          (error) => {
            assert.equal(error.code, "CANDIDATE_INVENTORY_CHECKPOINT_REQUIRED");
            assert.equal(
              Reflect.ownKeys(error).some((key) =>
                /handle|claim/u.test(String(key))),
              false,
            );
            return true;
          },
        );
        assert.equal(fullRefreshCalls.length, 2);
        assert.deepEqual({
          writes: fixture.writeCalls,
          blobs: fixture.blobs.size,
          trees: fixture.trees.size,
          commits: fixture.commits.size,
          ref: fixture.refTarget,
        }, before);
      });

    await t.test("current-open v3 publishes at 4094 and rotates at 4095 and 4096",
      async () => {
        const fixture = githubGitFixture();
        const capability = scheduleCapabilityReceipt();
        const triggerIdentity = scheduleTriggerIdentity();
        const verifier = provenanceVerifier({
          eventName: "schedule",
          ref: triggerIdentity.ref,
          shaValue: triggerIdentity.sha,
        });
        const currentOpen = currentOpenSuppressionAdapterFixture({
          initialNumbers: [],
        });
        let legacyFinishCalls = 0;
        const refreshAdapter = Object.freeze({
          schema:
            "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
          schema_version: 2,
          configuration_digest: rawDigest(
            "candidate-current-open-v3-saturated-epoch-configuration-v1",
          ),
          collectCurrentOpenCandidateAuthority:
            currentOpen.collectProductionCandidateAuthority,
          async finishLegacyCandidateInventoryAttempt() {
            legacyFinishCalls += 1;
            throw new Error("capacity boundary used legacy REST");
          },
        });
        const ledger = makeLedger(
          fixture,
          capability,
          verifier,
          null,
          { candidateInventoryRefreshAdapter: refreshAdapter },
        );
        await ledger.bootstrapCapability();
        await padOrdinaryLedgerWithStandaloneAttestations({
          fixture,
          verifier,
          targetCommitCount: MAX_V2_GIT_LEDGER_COMMITS - 2,
        });
        assert.equal(
          fixtureEnvelopeAt(fixture, fixture.refTarget).sequence + 1,
          MAX_V2_GIT_LEDGER_COMMITS - 2,
        );
        assert.equal(
          (await ledger.load()).commit_count,
          MAX_V2_GIT_LEDGER_COMMITS - 2,
        );
        const boundary4094 = fixture.snapshotHistoricalState();
        const input = async () => ({
          workflow_command_handle: await createScheduleWorkflowCommandHandle(),
          trigger_identity: triggerIdentity,
          repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        });
        const snapshot = () => ({
          writes: fixture.writeCalls,
          patches: fixture.calls.filter(({ method }) => method === "PATCH")
            .length,
          blobs: fixture.blobs.size,
          trees: fixture.trees.size,
          commits: fixture.commits.size,
          ref: fixture.refTarget,
        });
        const callsBeforePublish = currentOpen.calls.length;
        const beforePublish = snapshot();
        const published = await ledger
          .reconcileCandidateInventoryRefreshAtomically(await input());
        assert.equal(published.state, "persisted");
        assert.equal(published.publication_result.final_commit_count,
          MAX_V2_GIT_LEDGER_COMMITS);
        assert.equal(published.publication_result
          .published_record_commit_shas.length, 2);
        assert.equal(currentOpen.calls.length - callsBeforePublish, 4);
        assert.equal(fixture.commits.size - beforePublish.commits, 2);
        assert.equal(
          fixture.calls.filter(({ method }) => method === "PATCH").length -
            beforePublish.patches,
          1,
        );

        const assertCheckpointAt = async (targetCommitCount, {
          appendLaterGeneration = false,
        } = {}) => {
          fixture.restoreHistoricalState(boundary4094);
          currentOpen.setListedNumbers([]);
          await padOrdinaryLedgerWithStandaloneAttestations({
            fixture,
            verifier,
            targetCommitCount,
          });
          assert.equal(
            fixtureEnvelopeAt(fixture, fixture.refTarget).sequence + 1,
            targetCommitCount,
          );
          assert.equal((await ledger.load()).commit_count, targetCommitCount);
          const before = snapshot();
          const callsBefore = currentOpen.calls.length;
          let laterGeneration = null;
          if (appendLaterGeneration) {
            fixture.afterNextCandidateInventoryUpdateApplied(async () => {
              currentOpen.setListedNumbers([PR.number]);
              laterGeneration = await makeLedger(
                fixture,
                capability,
                verifier,
                null,
                { candidateInventoryRefreshAdapter: refreshAdapter },
              ).reconcileCandidateInventoryRefreshAtomically(await input());
            });
          }
          const result = await ledger
            .reconcileCandidateInventoryRefreshAtomically(await input());
          assert.equal(result.state, "persisted");
          assert.equal(result.reason, "current-open-generation-persisted");
          assert.equal(result.persistence_mode, "current-open-generation-v3");
          assert.equal(result.suppression_result, null);
          assert.equal(result.publication_result.state, "persisted");
          assert.equal(
            result.publication_result.published_record_commit_shas.length,
            2,
          );
          const expectedFinalCommitCount = appendLaterGeneration ? 5 : 3;
          assert.equal(result.publication_result.append_receipts.length, 0);
          if (appendLaterGeneration) {
            assert.equal(laterGeneration.state, "persisted");
            assert.equal(
              result.publication_result.publication_outcome,
              "recovered-after-apply",
            );
            assert.equal(
              laterGeneration.publication_result.publication_outcome,
              "published",
            );
          }
          assert.equal(
            result.publication_result.final_commit_count,
            expectedFinalCommitCount,
          );
          assert.equal(Object.isFrozen(result), true);
          assertNoCandidateRefreshPrivateAuthority(result);
          assert.equal(
            currentOpen.calls.length - callsBefore,
            appendLaterGeneration ? 8 : 4,
          );
          assert.equal(
            snapshot().patches - before.patches,
            appendLaterGeneration ? 2 : 1,
          );
          assert.equal(snapshot().commits > before.commits, true);
          const loaded = await makeLedger(
            fixture,
            capability,
            verifier,
            null,
            { candidateInventoryRefreshAdapter: refreshAdapter },
          ).load();
          assert.equal(loaded.checkpoint.profile, "mature-quiescent-v1");
          assert.equal(loaded.commit_count, expectedFinalCommitCount);
          assert.equal(
            loaded.authority_projection.candidate_inventory
              .completed_generation.candidate_count,
            appendLaterGeneration ? 1 : 0,
          );
          if (appendLaterGeneration) {
            assert.equal(
              loaded.authority_projection.candidate_dispatch
                .active_reservation.reservation.candidates[0].identity.number,
              PR.number,
            );
          } else {
            assert.equal(
              loaded.authority_projection.candidate_dispatch.current_cycle
                .cycle_complete,
              true,
            );
          }
          const admittedGenerationIndex = loaded.records.findIndex(
            ({ commit_sha: commitSha }) => commitSha ===
              result.publication_result.published_record_commit_shas[0],
          );
          assert.notEqual(admittedGenerationIndex, -1);
          const admittedGenerationEntry =
            loaded.records[admittedGenerationIndex];
          const admittedGenerationManifest =
            admittedGenerationEntry.envelope.payload.attachment_manifest;
          const admittedGenerationEvidence =
            durableCurrentOpenGenerationAttachmentValue(
              fixture,
              admittedGenerationManifest,
            );
          const admittedGenerationAuthority =
            deriveV2GitLedgerCandidateInventoryAuthority(
              loaded.records.slice(0, admittedGenerationIndex + 1),
              REPOSITORY,
            ).completed_generation;
          assert.deepEqual(admittedGenerationAuthority.candidates, []);
          assert.deepEqual(
            admittedGenerationEvidence.lifecycle_candidates,
            [],
          );
          assert.deepEqual(
            admittedGenerationAuthority.attachment_manifest,
            admittedGenerationManifest,
          );
          assert.equal(
            admittedGenerationEvidence.evidence_digest,
            admittedGenerationManifest.evidence_digest,
          );
          const layout = matureCheckpointFixtureLayout(fixture);
          const checkpointState = JSON.parse(
            fixture.blobs.get(layout.state_blob_entry.sha).content,
          );
          assert.equal(checkpointState.schema_version, 3);
          assert.equal(
            checkpointState.next_unit.profile,
            "current-open-generation-v1",
          );
          assert.equal(checkpointState.next_unit.candidate_count, 0);
          assert.equal(checkpointState.next_unit.semantic_record_count, 2);
          assert.equal(
            checkpointState.next_unit.total_commit_budget_required,
            2,
          );
          assert.equal(
            checkpointState.next_unit
              .fresh_epoch_remaining_commit_capacity,
            MAX_V2_GIT_LEDGER_COMMITS - 3,
          );
          assert.equal(
            checkpointState.next_unit
              .current_epoch_remaining_commit_capacity,
            targetCommitCount === MAX_V2_GIT_LEDGER_COMMITS ? -2 : -1,
          );
        };
        await assertCheckpointAt(MAX_V2_GIT_LEDGER_COMMITS - 1, {
          appendLaterGeneration: true,
        });
        await assertCheckpointAt(MAX_V2_GIT_LEDGER_COMMITS);
        assert.equal(legacyFinishCalls, 0);
      });

    await t.test(
      "a saturated terminal nonempty generation suppresses an equivalent scan",
      async () => {
        const attempt =
          await createCurrentOpenV3ScheduledCandidateDispatchAttempt();
        await completeScheduledCandidateDispatchAttempt(attempt);
        attempt.fixture.freezeServerTime();
        await padOrdinaryLedgerWithStandaloneAttestations({
          fixture: attempt.fixture,
          verifier: attempt.verifier,
          targetCommitCount: MAX_V2_GIT_LEDGER_COMMITS,
          templateCommitSha: attempt.capabilityAttestationCommitSha,
        });
        const ledger = makeLedger(
          attempt.fixture,
          attempt.capability,
          attempt.verifier,
          attempt.preflight,
          { candidateInventoryRefreshAdapter: attempt.refreshAdapter },
        );
        const saturated = await ledger.load();
        assert.equal(saturated.commit_count, MAX_V2_GIT_LEDGER_COMMITS);
        assert.equal(
          saturated.authority_projection.candidate_dispatch.current_cycle
            .cycle_complete,
          true,
        );
        assert.equal(
          saturated.authority_projection.candidate_dispatch
            .active_reservation,
          null,
        );
        const before = {
          writes: attempt.fixture.writeCalls,
          patches: attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        };
        const currentOpenCallsBefore = attempt.currentOpen.calls.length;
        attempt.runIdentity.run_id = "9097";
        attempt.runIdentity.run_attempt = 1;

        const result = await ledger.reconcileCandidateInventoryRefreshAtomically({
          ...attempt.input,
          workflow_command_handle: await createScheduleWorkflowCommandHandle({
            runIdentity: attempt.runIdentity,
          }),
        });

        assert.equal(result.state, "suppressed");
        assert.equal(result.reason, "semantic-unchanged-not-due");
        assert.equal(result.persistence_mode, null);
        assert.equal(result.publication_result, null);
        assert.equal(result.suppression_result.state, "suppressed");
        assert.equal(
          result.suppression_result.assurance,
          "authenticated-current-open-terminal-not-due",
        );
        assert.equal(result.suppression_result.writes_performed, false);
        assert.equal(Object.isFrozen(result), true);
        assert.equal(Object.isFrozen(result.suppression_result), true);
        assertNoCandidateRefreshPrivateAuthority(result);
        assert.equal(
          attempt.currentOpen.calls.length - currentOpenCallsBefore,
          4,
        );
        assert.equal(attempt.legacyFinishCalls(), 0);
        assert.deepEqual({
          writes: attempt.fixture.writeCalls,
          patches: attempt.fixture.calls.filter(({ method }) =>
            method === "PATCH").length,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        }, before);
        const after = await ledger.load();
        assert.equal(after.commit_count, MAX_V2_GIT_LEDGER_COMMITS);
        assert.equal(
          after.authority_projection.candidate_inventory.authority_digest,
          saturated.authority_projection.candidate_inventory.authority_digest,
        );
        assert.equal(
          after.authority_projection.candidate_dispatch.authority_digest,
          saturated.authority_projection.candidate_dispatch.authority_digest,
        );
        assert.deepEqual(
          after.authority_projection.candidate_dispatch.current_cycle,
          saturated.authority_projection.candidate_dispatch.current_cycle,
        );
      },
    );

    await t.test("512 candidates exceed a fresh epoch before Git staging",
      async () => {
        const candidateNumbers = Array.from(
          { length: 512 },
          (_, index) => index + 1,
        );
        const attempt = await stableEmptyCandidateRefreshReconciliationFixture({
          currentOpenNumbers: candidateNumbers,
        });
        attempt.fullTransport.setListedNumbers(candidateNumbers);
        const currentOpenCallsBefore = attempt.currentOpen.calls.length;
        const fullRestCallsBefore = attempt.fullTransport.calls.length;
        const before = {
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        };
        await assert.rejects(
          attempt.ledger.reconcileCandidateInventoryRefreshAtomically(
            attempt.input,
          ),
          (error) =>
            error.code === "candidate-inventory-cycle-commit-capacity",
        );
        assert.equal(attempt.fullRefreshCalls.length, 1);
        assert.equal(
          attempt.currentOpen.calls.length - currentOpenCallsBefore,
          14,
        );
        assert.equal(
          attempt.fullTransport.calls.length - fullRestCallsBefore,
          1_048,
        );
        assert.equal("runV2Operation" in attempt.refreshAdapter, false);
        assert.deepEqual({
          writes: attempt.fixture.writeCalls,
          blobs: attempt.fixture.blobs.size,
          trees: attempt.fixture.trees.size,
          commits: attempt.fixture.commits.size,
          ref: attempt.fixture.refTarget,
        }, before);
      });
  });

test("high-level suppression revalidates after classify and claims once",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    });
    const writer = makeLedger(fixture, capability, verifier);
    await writer.bootstrapCapability();
    const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
    const transportFixture = candidateTransportFixture(0);
    const firstCycle = await readCandidateInventoryCycle({
      transportFixture,
    });
    await writer.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...firstCycle,
    });
    await writer.loadOrReserveCandidateDispatch({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    const stable = await writer.load();
    const nextCycle = await readCandidateInventoryCycle({
      transportFixture,
      priorInventory: stable.authority_projection.candidate_inventory
        .completed_cycle.final_inventory,
    });
    let refReadCount = 0;
    let writesAfterInjectedDrift = null;
    const fetchWithPostClassifyDrift = async (url, init) => {
      const parsed = new URL(String(url));
      if (
        init.method === "GET" &&
        parsed.pathname.endsWith(
          "/git/ref/heads/codex-review-gate-ledger-v2",
        )
      ) {
        refReadCount += 1;
        if (refReadCount === 7) {
          await writer.appendCandidateInventoryCycleIfRequired({
            workflow_command_handle: workflowCommandHandle,
            trigger_identity: triggerIdentity,
            repository_endpoint_receipt:
              scheduleRepositoryEndpointReceipt(),
            ...nextCycle,
          });
          writesAfterInjectedDrift = fixture.writeCalls;
        }
      }
      return fixture.fetch(url, init);
    };
    const evidence = currentOpenSuppressionAdapterFixture();
    const ledger = makeLedger(
      { fetch: fetchWithPostClassifyDrift },
      capability,
      verifier,
      null,
      { candidateSuppressionEvidenceAdapter: evidence.adapter },
    );
    const input = {
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    };

    const drifted = await ledger.trySuppressCandidateInventoryRefresh(input);
    assert.equal(drifted.state, "persist-required");
    assert.equal(drifted.reason, "candidate-refresh-authority-stale");
    assert.equal(refReadCount, 8);
    assert.notEqual(writesAfterInjectedDrift, null);
    assert.equal(fixture.writeCalls, writesAfterInjectedDrift);
    assert.equal(evidence.calls.length, 4);

    const writesBeforeConcurrent = fixture.writeCalls;
    const callsBeforeConcurrent = evidence.calls.length;
    const concurrent = await Promise.all([
      ledger.trySuppressCandidateInventoryRefresh(input),
      ledger.trySuppressCandidateInventoryRefresh(input),
    ]);
    assert.deepEqual(
      concurrent.map(({ state }) => state).sort(),
      ["persist-required", "suppressed"],
    );
    assert.equal(
      concurrent.find(({ state }) => state === "persist-required").reason,
      "candidate-refresh-authority-stale",
    );
    assert.equal(evidence.calls.length - callsBeforeConcurrent, 4);
    assert.equal(fixture.writeCalls, writesBeforeConcurrent);
  });

test("read-only refresh classification preserves trigger and durable work gates",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    });
    const evidence = currentOpenSuppressionAdapterFixture();
    const ledger = makeLedger(fixture, capability, verifier, null, {
      candidateSuppressionEvidenceAdapter: evidence.adapter,
    });
    await ledger.bootstrapCapability();
    const transportFixture = candidateTransportFixture(0, {
      exactScopeCandidateNumber: PR.number,
    });
    transportFixture.setListedNumbers([PR.number]);
    const scanCommand = await createScheduleWorkflowCommandHandle();
    const cycle = await readCandidateInventoryCycle({ transportFixture });
    await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: scanCommand,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...cycle,
    });
    const reserved = await ledger.loadOrReserveCandidateDispatch({
      workflow_command_handle: scanCommand,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    assert.notEqual(reserved.plan, null);
    const writesBefore = fixture.writeCalls;
    const objectsBefore = {
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    };
    const unsettled = await ledger
      .trySuppressCandidateInventoryRefresh({
        workflow_command_handle: scanCommand,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      });
    assert.equal(unsettled.state, "persist-required");
    assert.equal(unsettled.reason, "candidate-dispatch-unsettled");
    assert.equal(evidence.calls.length, 0);

    const scopedCommand = await createScheduleWorkflowCommandHandle({
      pullRequestNumber: PR.number,
      dispatchBinding: reserved.plan.items[0],
      runIdentity: {
        run_id: "9002",
        run_attempt: 1,
        actor_id: OWNER.actor_id,
      },
    });
    const scoped = await ledger.trySuppressCandidateInventoryRefresh({
      workflow_command_handle: scopedCommand,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    assert.equal(scoped.state, "persist-required");
    assert.equal(scoped.reason, "candidate-refresh-trigger-in-scope");
    assert.equal("source_handle" in scoped, false);
    assert.equal(evidence.calls.length, 0);

    const unwired = makeLedger(
      fixture,
      capability,
      provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
        jtiPrefix: "candidate-refresh-unwired-jti",
      }),
    );
    const unavailable = await unwired
      .trySuppressCandidateInventoryRefresh({
        workflow_command_handle: scanCommand,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      });
    assert.equal(unavailable.state, "persist-required");
    assert.equal(
      unavailable.reason,
      "candidate-full-evidence-adapter-unavailable",
    );
    assert.equal("source_handle" in unavailable, false);
    assert.equal(fixture.writeCalls, writesBefore);
    assert.deepEqual({
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      ref: fixture.refTarget,
    }, objectsBefore);
  });

test("sampled empty cannot suppress a durable retained candidate closure",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const evidence = currentOpenSuppressionAdapterFixture();
    const ledger = makeLedger(
      fixture,
      capability,
      provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
      }),
      null,
      { candidateSuppressionEvidenceAdapter: evidence.adapter },
    );
    await ledger.bootstrapCapability();
    const transportFixture = candidateTransportFixture(0, {
      exactScopeCandidateNumber: PR.number,
    });
    transportFixture.setListedNumbers([PR.number]);
    transportFixture.setCandidateState(PR.number, "closed");
    const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
    const cycle = await readCandidateInventoryCycle({ transportFixture });
    const cycleReceipt = finalizeV2CandidateInventoryCycle(cycle);
    assert.equal(cycleReceipt.open_pull_requests.length, 0);
    assert.equal(cycle.final_inventory.candidates.length, 1);
    await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...cycle,
    });
    const dispatch = await ledger.loadOrReserveCandidateDispatch({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    assert.equal(dispatch.state, "complete");
    const writesBefore = fixture.writeCalls;
    const classified = await ledger
      .trySuppressCandidateInventoryRefresh({
        workflow_command_handle: workflowCommandHandle,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      });
    assert.equal(classified.state, "persist-required");
    assert.equal(
      classified.reason,
      "candidate-full-evidence-dirty-epoch-required",
    );
    assert.equal("source_handle" in classified, false);
    assert.equal(evidence.calls.length, 0);
    assert.equal(fixture.writeCalls, writesBefore);
  });

test("read-only refresh classification checks due work before nonempty dirtiness",
  async () => {
    const attempt = await createScheduledAutomaticRecoveryBranchAttempt({
      bindArtifactBeforeDispatch: false,
    });
    const completed = await completeScheduledAutomaticArtifactBindingAttempt(
      attempt,
    );
    assert.notEqual(completed.plan.scheduler_plan.due_at, null);
    const evidence = currentOpenSuppressionAdapterFixture();
    const ledger = makeLedger(
      attempt.fixture,
      attempt.capability,
      attempt.verifier,
      attempt.preflight,
      { candidateSuppressionEvidenceAdapter: evidence.adapter },
    );
    Object.assign(attempt.runIdentity, OWNER);
    const input = {
      workflow_command_handle: attempt.scanCommand,
      trigger_identity: attempt.triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    };
    const writesBefore = attempt.fixture.writeCalls;
    const future = await ledger
      .trySuppressCandidateInventoryRefresh(input);
    assert.equal(future.state, "persist-required");
    assert.equal(
      future.reason,
      "candidate-full-evidence-dirty-epoch-required",
    );
    assert.equal(evidence.calls.length, 0);

    attempt.fixture.advanceServerTime(24 * 60 * 60);
    const due = await ledger.trySuppressCandidateInventoryRefresh(
      input,
    );
    assert.equal(due.state, "persist-required");
    assert.equal(due.reason, "candidate-dispatch-due");
    assert.equal("source_handle" in due, false);
    assert.equal(evidence.calls.length, 0);
    assert.equal(attempt.fixture.writeCalls, writesBefore);
  });

test("read-only refresh handles reject durable tip drift after collection starts",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const evidence = currentOpenSuppressionAdapterFixture();
    const ledger = makeLedger(
      fixture,
      capability,
      provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
      }),
      null,
      { candidateSuppressionEvidenceAdapter: evidence.adapter },
    );
    await ledger.bootstrapCapability();
    const transportFixture = candidateTransportFixture(0);
    const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
    const appendNextEmptyCycle = async () => {
      const loaded = await ledger.load();
      const cycle = await readCandidateInventoryCycle({
        transportFixture,
        priorInventory: loaded.authority_projection.candidate_inventory
          .completed_cycle?.final_inventory ?? null,
      });
      return ledger.appendCandidateInventoryCycleIfRequired({
        workflow_command_handle: workflowCommandHandle,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        ...cycle,
      });
    };
    await appendNextEmptyCycle();
    await ledger.loadOrReserveCandidateDispatch({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    const input = {
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    };
    const staleSource = await ledger
      .beginCandidateInventoryRefreshClassification(input);
    await appendNextEmptyCycle();
    const writesAfterDrift = fixture.writeCalls;
    await assert.rejects(
      ledger.collectCandidateInventoryRefreshEvidence({
        source_handle: staleSource.source_handle,
      }),
      (error) => error.code === "STALE_CANDIDATE_REFRESH_SOURCE_HANDLE",
    );
    assert.equal(fixture.writeCalls, writesAfterDrift);

    const currentSource = await ledger
      .beginCandidateInventoryRefreshClassification(input);
    const currentEvidence = await ledger
      .collectCandidateInventoryRefreshEvidence({
        source_handle: currentSource.source_handle,
      });
    await appendNextEmptyCycle();
    const writesAfterEvidenceDrift = fixture.writeCalls;
    await assert.rejects(
      ledger.classifyCandidateInventoryRefresh({
        source_handle: currentSource.source_handle,
        evidence_handle: currentEvidence.evidence_handle,
      }),
      (error) => error.code === "STALE_CANDIDATE_REFRESH_SOURCE_HANDLE",
    );
    assert.equal(fixture.writeCalls, writesAfterEvidenceDrift);
  });

test("a later current-open collection revokes earlier sampled-empty evidence",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const evidence = currentOpenSuppressionAdapterFixture();
    const ledger = makeLedger(
      fixture,
      capability,
      provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
      }),
      null,
      { candidateSuppressionEvidenceAdapter: evidence.adapter },
    );
    await ledger.bootstrapCapability();
    const transportFixture = candidateTransportFixture(0);
    const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
    const cycle = await readCandidateInventoryCycle({ transportFixture });
    await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...cycle,
    });
    await ledger.loadOrReserveCandidateDispatch({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    const source = await ledger.beginCandidateInventoryRefreshClassification({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    const empty = await ledger.collectCandidateInventoryRefreshEvidence({
      source_handle: source.source_handle,
    });
    assert.equal(empty.state, "classification-required");
    evidence.setListedNumbers([PR.number]);
    const later = await ledger.collectCandidateInventoryRefreshEvidence({
      source_handle: source.source_handle,
    });
    assert.equal(later.state, "persist-required");
    const writesBefore = fixture.writeCalls;
    await assert.rejects(
      ledger.classifyCandidateInventoryRefresh({
        source_handle: source.source_handle,
        evidence_handle: empty.evidence_handle,
      }),
      (error) => error.code === "STALE_CANDIDATE_REFRESH_EVIDENCE_HANDLE",
    );
    assert.equal(fixture.writeCalls, writesBefore);
  });

test("candidate inventory publishes one complete cycle with one ref CAS", async (t) => {
    for (const failure of [
      "attempt-object",
      "final-ref-cas",
      "patch-response-loss",
      "stale-ref-reread",
      "post-publish-reread",
    ]) {
      await t.test(failure, async () => {
        const fixture = githubGitFixture();
        const capability = scheduleCapabilityReceipt();
        const triggerIdentity = scheduleTriggerIdentity();
        const verifier = provenanceVerifier({
          eventName: "schedule",
          ref: triggerIdentity.ref,
          shaValue: triggerIdentity.sha,
        });
        const ledger = makeLedger(fixture, capability, verifier);
      await ledger.bootstrapCapability();
      const transportFixture = candidateTransportFixture(0);
      const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
      const initialCycle = await readCandidateInventoryCycle({
        transportFixture,
      });
      await ledger.appendCandidateInventoryCycleIfRequired({
        workflow_command_handle: workflowCommandHandle,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        ...initialCycle,
      });
      await ledger.loadOrReserveCandidateDispatch({
        workflow_command_handle: workflowCommandHandle,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      });
      const before = await ledger.load();
      const priorInventory = before.authority_projection.candidate_inventory
        .completed_cycle.final_inventory;
      transportFixture.setListedNumbers([1]);
      const changedCycle = await readCandidateInventoryCycle({
        transportFixture,
        priorInventory,
      });
      if (failure === "attempt-object") {
        fixture.failNextCandidateInventoryCommit("attempt");
      } else if (failure === "final-ref-cas") {
        fixture.failNextCandidateInventoryUpdate();
      } else if (failure === "patch-response-loss") {
        fixture.failNextCandidateInventoryUpdateAfterApply();
      } else if (failure === "stale-ref-reread") {
        fixture.mismatchNextRefReread();
      } else {
        fixture.failRereadAfterEffectUpdate(null);
      }
      const appendInput = {
          workflow_command_handle: workflowCommandHandle,
          trigger_identity: triggerIdentity,
          repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
          ...changedCycle,
      };
      const publishWasUnknown = new Set([
        "patch-response-loss",
        "stale-ref-reread",
        "post-publish-reread",
      ]).has(failure);
      const recoveredPublication = publishWasUnknown
        ? await ledger.appendCandidateInventoryCycleIfRequired(appendInput)
        : null;
      if (!publishWasUnknown) {
        await assert.rejects(
          ledger.appendCandidateInventoryCycleIfRequired(appendInput),
        );
      }
      const afterFailure = await ledger.load();
      assert.equal(
        afterFailure.commit_count,
        before.commit_count + (publishWasUnknown ? 4 : 0),
      );
      assert.equal(
        afterFailure.tip_commit_sha === before.tip_commit_sha,
        !publishWasUnknown,
      );
      assert.equal(afterFailure.authority_projection.candidate_inventory
        .incomplete_cycle, null,
      "staged inventory is either wholly absent or wholly reachable");
      if (recoveredPublication !== null) {
        assert.equal(recoveredPublication.state, "persisted");
        assert.equal(recoveredPublication.reason, "semantic-change");
        assert.equal(recoveredPublication.append_receipts.length, 0);
        assert.equal(
          recoveredPublication.final_tip_commit_sha,
          afterFailure.tip_commit_sha,
        );
      }

      const retryRunIdentity = {
        run_id: "9002",
        run_attempt: 2,
        actor_id: OWNER.actor_id,
      };
      const retryDelegateVerifier = provenanceVerifier({
            jtiPrefix: "retry-oidc-jti",
            eventName: "schedule",
            ref: triggerIdentity.ref,
            shaValue: triggerIdentity.sha,
        runIdentity: retryRunIdentity,
      });
      const retryVerifier = async (request) => {
        if (request.mode === "mint-and-verify") {
          return retryDelegateVerifier(request);
        }
        const payload = JSON.parse(Buffer.from(
          request.compact_jwt.split(".")[1],
          "base64url",
        ).toString("utf8"));
        return typeof payload.jti === "string" &&
            payload.jti.startsWith("retry-oidc-jti-")
          ? retryDelegateVerifier(request)
          : verifier(request);
      };
      const retryLedger = publishWasUnknown
        ? makeLedger(fixture, capability, retryVerifier)
        : ledger;
      const retryWorkflowCommandHandle = publishWasUnknown
        ? await createScheduleWorkflowCommandHandle({
            runIdentity: retryRunIdentity,
          })
        : workflowCommandHandle;
      if (publishWasUnknown) {
        const mismatchedCommandHandle =
          await createScheduleWorkflowCommandHandle({
            runIdentity: retryRunIdentity,
            event: { changed: true },
          });
        for (const [label, commandHandle, retryTrigger] of [
          ["event payload", mismatchedCommandHandle, triggerIdentity],
          ["trigger sha", retryWorkflowCommandHandle, {
            ...triggerIdentity,
            sha: HEAD,
          }],
        ]) {
          const writesBeforeMismatch = fixture.writeCalls;
          await assert.rejects(
            retryLedger.appendCandidateInventoryCycleIfRequired({
              workflow_command_handle: commandHandle,
              trigger_identity: retryTrigger,
              repository_endpoint_receipt:
                scheduleRepositoryEndpointReceipt(),
              ...changedCycle,
            }),
            (error) => error.code ===
              "candidate-inventory-cycle-retry-authority-mismatch",
            label,
          );
          assert.equal(
            fixture.writeCalls,
            writesBeforeMismatch,
            `${label} retry mismatch wrote a record`,
          );
        }
      }
      const retry = await retryLedger.appendCandidateInventoryCycleIfRequired({
        workflow_command_handle: retryWorkflowCommandHandle,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        ...changedCycle,
      });
      assert.equal(retry.state, "persisted");
      assert.equal(retry.published_record_commit_shas.length, 4);
      assert.equal(retry.append_receipts.length, publishWasUnknown ? 0 : 1);
      assert.equal(
        retry.reason,
        publishWasUnknown
          ? "candidate-inventory-already-current"
          : "semantic-change",
      );
      const afterRetry = await retryLedger.load();
      assert.equal(afterRetry.commit_count, before.commit_count + 4);
      assert.equal(afterRetry.tip_commit_sha, retry.final_tip_commit_sha);
    });
  }
});

test("candidate inventory retry recognizes a superseded compact publication",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    });
    const ledger = makeLedger(fixture, capability, verifier);
    await ledger.bootstrapCapability();
    const transportFixture = candidateTransportFixture(0);
    const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
    const initialCycle = await readCandidateInventoryCycle({
      transportFixture,
    });
    await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...initialCycle,
    });
    await ledger.loadOrReserveCandidateDispatch({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
    const beforeLostPublication = await ledger.load();
    const priorInventory = beforeLostPublication.authority_projection
      .candidate_inventory.completed_cycle.final_inventory;
    transportFixture.setListedNumbers([1]);
    const lostCycle = await readIdentityDriftCandidateInventoryCycle({
      transportFixture,
      priorInventory,
      listedNumbersAfterInitial: [],
      mutateAfterInitial: () => {
        transportFixture.setCandidateState(1, "closed");
      },
      expectedDriftCode: "CANDIDATE_LIFECYCLE_DRIFT",
    });
    transportFixture.setListedNumbers([2]);
    const nextCycle = await readCandidateInventoryCycle({
      transportFixture,
      priorInventory: lostCycle.final_inventory,
    });
    let nextPublication = null;
    fixture.afterNextCandidateInventoryUpdateApplied(async () => {
      nextPublication = await ledger.appendCandidateInventoryCycleIfRequired({
        workflow_command_handle: workflowCommandHandle,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        ...nextCycle,
      });
    });
    const lostPublication = await ledger
      .appendCandidateInventoryCycleIfRequired({
        workflow_command_handle: workflowCommandHandle,
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        ...lostCycle,
      });
    assert.equal(lostPublication.append_receipts.length, 0);
    assert.equal(lostPublication.published_record_commit_shas.length, 5);
    assert.equal(nextPublication?.state, "persisted");
    const afterLostPublication = await ledger.load();
    assert.equal(
      lostPublication.final_tip_commit_sha,
      afterLostPublication.tip_commit_sha,
    );
    assert.notEqual(
      lostPublication.published_record_commit_shas.at(-1),
      lostPublication.final_tip_commit_sha,
    );
    const beforeRetry = await ledger.load();
    const writesBeforeRetry = fixture.writeCalls;

    const retry = await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...lostCycle,
    });
    assert.equal(retry.state, "persisted");
    assert.equal(
      retry.reason,
      "candidate-inventory-already-published-superseded",
    );
    assert.equal(retry.append_receipts.length, 0);
    assert.equal(retry.published_record_commit_shas.length, 5);
    assert.equal(retry.final_tip_commit_sha, beforeRetry.tip_commit_sha);
    assert.equal(fixture.writeCalls, writesBeforeRetry);
  });

test("candidate inventory publishes retained drift attempts in the same ref CAS",
  async (t) => {
    for (const failure of ["final-ref-cas", "post-publish-reread"]) {
      await t.test(failure, async () => {
        const fixture = githubGitFixture();
        const capability = scheduleCapabilityReceipt();
        const triggerIdentity = scheduleTriggerIdentity();
        const ledger = makeLedger(fixture, capability, provenanceVerifier({
          eventName: "schedule",
          ref: triggerIdentity.ref,
          shaValue: triggerIdentity.sha,
        }));
        await ledger.bootstrapCapability();
        const transportFixture = candidateTransportFixture(0);
        const workflowCommandHandle =
          await createScheduleWorkflowCommandHandle();
        const initialCycle = await readCandidateInventoryCycle({
          transportFixture,
        });
        await ledger.appendCandidateInventoryCycleIfRequired({
          workflow_command_handle: workflowCommandHandle,
          trigger_identity: triggerIdentity,
          repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
          ...initialCycle,
        });
        await ledger.loadOrReserveCandidateDispatch({
          workflow_command_handle: workflowCommandHandle,
          trigger_identity: triggerIdentity,
          repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        });
        const before = await ledger.load();
        const driftCycle = await readIdentityDriftCandidateInventoryCycle({
          transportFixture,
          priorInventory: before.authority_projection.candidate_inventory
            .completed_cycle.final_inventory,
          listedNumbersAfterInitial: [1],
        });
        if (failure === "final-ref-cas") {
          fixture.failNextCandidateInventoryUpdate();
        } else {
          fixture.failRereadAfterEffectUpdate(null);
        }
        const appendInput = {
            workflow_command_handle: workflowCommandHandle,
            trigger_identity: triggerIdentity,
            repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
            ...driftCycle,
        };
        const publishWasUnknown = failure === "post-publish-reread";
        const recoveredPublication = publishWasUnknown
          ? await ledger.appendCandidateInventoryCycleIfRequired(appendInput)
          : null;
        if (!publishWasUnknown) {
          await assert.rejects(
            ledger.appendCandidateInventoryCycleIfRequired(appendInput),
          );
        }
        const afterFailure = await ledger.load();
        assert.equal(
          afterFailure.commit_count,
          before.commit_count + (publishWasUnknown ? 5 : 0),
        );
        assert.equal(afterFailure.authority_projection.candidate_inventory
          .incomplete_cycle, null);
        if (recoveredPublication !== null) {
          assert.equal(recoveredPublication.append_receipts.length, 0);
          assert.equal(
            recoveredPublication.final_tip_commit_sha,
            afterFailure.tip_commit_sha,
          );
        }

        const retry = await ledger.appendCandidateInventoryCycleIfRequired({
          workflow_command_handle: workflowCommandHandle,
          trigger_identity: triggerIdentity,
          repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
          ...driftCycle,
        });
        assert.equal(retry.state, "persisted");
        assert.equal(
          retry.reason,
          publishWasUnknown
            ? "candidate-inventory-already-current"
            : "candidate-inventory-drift",
        );
        assert.equal(retry.projection.drift_attempt_count, 1);
        assert.deepEqual(
          retry.projection.drift_codes,
          ["CANDIDATE_INVENTORY_DRIFT"],
        );
        assert.equal(retry.projection.inventory_commit_budget_required, 4);
        assert.equal(retry.published_record_commit_shas.length, 5);
        assert.equal(retry.append_receipts.length, publishWasUnknown ? 0 : 1);
        const after = await ledger.load();
        assert.equal(after.commit_count, before.commit_count + 5);
        assert.equal(after.tip_commit_sha, retry.final_tip_commit_sha);
        assert.deepEqual(
          after.authority_projection.candidate_inventory.source_records
            .slice(-4).map(({ phase }) => phase),
          ["cycle-start", "attempt", "attempt", "cycle-complete"],
        );
      });
    }
  });

test("maximum candidate inventory cycle uses compact attachment manifests",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const delegateVerifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    });
    let mintedProductionRecords = 0;
    const verifier = async (request) => {
      if (request.mode === "mint-and-verify") {
        mintedProductionRecords += 1;
      }
      return delegateVerifier(request);
    };
    const ledger = makeLedger(fixture, capability, verifier);
    await ledger.bootstrapCapability();
    const cycle = maximumClosedCandidateInventoryCycle();
    assert.equal(cycle.initial_inventory.candidates.length, 25_599);
    assert.equal(cycle.initial_inventory.shards.length, 100);
    assert.equal(cycle.final_inventory.shards.length, 100);
    const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
    const mintCountBefore = mintedProductionRecords;
    const refPatchesBefore = fixture.calls.filter((call) =>
      call.method === "PATCH").length;

    const appended = await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...cycle,
    });

    assert.equal(appended.state, "persisted");
    assert.ok(
      appended.published_record_commit_shas.length <= 9,
      "one maximum stable cycle plus its first dispatch must use at most nine records",
    );
    assert.ok(
      mintedProductionRecords - mintCountBefore <= 9,
      "one maximum stable cycle must mint at most nine production record attestations",
    );
    assert.equal(
      fixture.calls.filter((call) => call.method === "PATCH").length -
        refPatchesBefore,
      1,
      "all manifests and attachments become reachable through one ref CAS",
    );
    const reachableBlobs = reachableGitBlobBytes(fixture);
    assert.ok(reachableBlobs.length > 0);
    for (const { path, bytes } of reachableBlobs) {
      assert.ok(
        bytes.byteLength <= MAX_V2_GIT_LEDGER_BLOB_BYTES,
        `${path} exceeds the fixed Git blob cap`,
      );
    }

    const restarted = makeLedger(fixture, capability, provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
      jtiPrefix: "restart-oidc-jti",
    }));
    const reloaded = await restarted.load();
    const durable = reloaded.authority_projection.candidate_inventory
      .completed_cycle.final_inventory;
    assert.equal(durable.candidates.length, 25_599);
    assert.equal(durable.shards.length, 100);
    assert.equal(
      digestCanonical("candidate-inventory-restart-exact", durable),
      digestCanonical(
        "candidate-inventory-restart-exact",
        cycle.final_inventory,
      ),
      "restart must hydrate the complete exact final inventory from attachments",
    );
  });

test("candidate inventory drift attempts are durable and retry-exact",
  async (t) => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    });
    const ledger = makeLedger(fixture, capability, verifier);
    await ledger.bootstrapCapability();
    const transportFixture = candidateTransportFixture(0);
    const cycle = await readIdentityDriftCandidateInventoryCycle({
      transportFixture,
      priorInventory: null,
      listedNumbersAfterInitial: [1],
    });
    const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
    const appended = await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...cycle,
    });
    assert.equal(appended.state, "persisted");

    await t.test("persists final drift evidence and its classification", () => {
      const durableText = reachableGitBlobBytes(fixture)
        .map(({ bytes }) => bytes.toString("utf8"))
        .join("\n");
      const driftFinalShard = cycle.drift_attempts[0]
        .final_shard_receipts[0];
      const durableDriftFinalShard = durableCandidateInventoryAttachmentValue(
        fixture,
        driftFinalShard.receipt_digest,
      );
      assert.deepEqual(
        {
          final_shard_receipt_exact:
            durableDriftFinalShard !== null &&
            candidateDigestCanonical(
              "candidate-inventory-drift-final-shard-exact",
              durableDriftFinalShard,
            ) === candidateDigestCanonical(
              "candidate-inventory-drift-final-shard-exact",
              driftFinalShard,
            ),
          drift_code_reachable:
            durableText.includes("CANDIDATE_INVENTORY_DRIFT"),
        },
        {
          final_shard_receipt_exact: true,
          drift_code_reachable: true,
        },
        "the full final drift shard and classified drift code must be durable",
      );
    });

    await t.test("rejects a retry that omits durable drift attempts", async () => {
      const writesBefore = fixture.writeCalls;
      const refBefore = fixture.refTarget;
      await assert.rejects(
        ledger.appendCandidateInventoryCycleIfRequired({
          workflow_command_handle: workflowCommandHandle,
          trigger_identity: triggerIdentity,
          repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
          ...cycle,
          drift_attempts: [],
        }),
        (error) =>
          error.code === "candidate-inventory-cycle-replay-mismatch",
      );
      assert.equal(fixture.writeCalls, writesBefore);
      assert.equal(fixture.refTarget, refBefore);
    });
  });

test("unchanged candidates with unsettled scheduler actions remain auditable", async () => {
  const attempt = await createScheduledCandidateDispatchAttempt();
  const completed = await completeScheduledCandidateDispatchAttempt(attempt);
  assert.notEqual(completed.plan.scheduler_plan.due_at, null);
  const settled = await attempt.ledger.load();
  const priorInventory = settled.authority_projection.candidate_inventory
    .completed_cycle.final_inventory;
  const settledTip = settled.tip_commit_sha;
  const settledCount = settled.commit_count;
  Object.assign(attempt.runIdentity, OWNER);

  attempt.fixture.advanceServerTime(120);
  attempt.candidateFixture.advanceServerTime(60);
  const futureCycle = await readCandidateInventoryCycle({
    transportFixture: attempt.candidateFixture,
    priorInventory,
  });
  const persisted = await attempt.ledger
    .appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: attempt.scanCommand,
      trigger_identity: attempt.triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...futureCycle,
    });
  assert.equal(persisted.state, "persisted");
  assert.equal(
    persisted.projection.reason,
    "candidate-dispatch-action-unsettled",
  );
  assert.ok(completed.plan.scheduler_plan.actions.some(({ kind }) =>
    kind === "persist_auto_request_intent"));
  assert.equal(persisted.published_record_commit_shas.length, 4);
  assert.equal(persisted.append_receipts.length, 1);
  const after = await attempt.ledger.load();
  assert.notEqual(after.tip_commit_sha, settledTip);
  assert.equal(after.commit_count, settledCount + 4);
});

test("settled unchanged candidates persist without fresh full evidence authority",
  async () => {
    const attempt = await createScheduledAutomaticRecoveryBranchAttempt({
      bindArtifactBeforeDispatch: false,
    });
    const completed = await completeScheduledAutomaticArtifactBindingAttempt(
      attempt,
    );
    const dueAtValue = completed.plan.scheduler_plan.due_at;
    assert.notEqual(dueAtValue, null);
    assert.ok(completed.plan.scheduler_plan.actions.every(({ kind }) =>
      kind === "publish_status" || kind === "record_head_ledger"));
    const settled = await attempt.ledger.load();
    const priorInventory = settled.authority_projection.candidate_inventory
      .completed_cycle.final_inventory;
    const settledCount = settled.commit_count;
    Object.assign(attempt.runIdentity, OWNER);

    attempt.fixture.advanceServerTime(120);
    attempt.candidateFixture.advanceServerTime(60);
    const futureCycle = await readCandidateInventoryCycle({
      transportFixture: attempt.candidateFixture,
      priorInventory,
    });
    const future = await attempt.ledger
      .appendCandidateInventoryCycleIfRequired({
        workflow_command_handle: attempt.scanCommand,
        trigger_identity: attempt.triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        ...futureCycle,
      });
    assert.equal(future.state, "persisted");
    assert.equal(
      future.projection.reason,
      "candidate-full-evidence-authority-required",
    );
    assert.equal(future.published_record_commit_shas.length, 4);
    assert.equal(future.append_receipts.length, 1);
    const afterFuture = await attempt.ledger.load();
    assert.equal(afterFuture.commit_count, settledCount + 4);
    assert.equal(afterFuture.tip_commit_sha, future.final_tip_commit_sha);
  });

test("incomplete candidate inventory fences unrelated production writes", async () => {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt({
    allowedEventNames: ["schedule", "pull_request_target"],
  });
  const triggerIdentity = scheduleTriggerIdentity();
  const ledger = makeLedger(fixture, capability, provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
    deriveTriggerFromRequest: true,
  }));
  await ledger.bootstrapCapability();
  const transportFixture = candidateTransportFixture(0);
  const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
  const initialCycle = await readCandidateInventoryCycle({
    transportFixture,
  });
  await ledger.appendCandidateInventoryCycleIfRequired({
    workflow_command_handle: workflowCommandHandle,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    ...initialCycle,
  });
  await ledger.loadOrReserveCandidateDispatch({
    workflow_command_handle: workflowCommandHandle,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });
  let loaded = await ledger.load();
  const transport = createV2GitHubCandidateInventory({
    fetch: transportFixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: candidateRepository(),
    restBaseUrl: "https://github.example.test",
  });
  const initial = await transport.scan({
    prior_inventory:
      loaded.authority_projection.candidate_inventory.completed_cycle
        .final_inventory,
  });
  await appendCandidateInventoryPhase({
    ledger,
    loaded,
    phase: "cycle-start",
    initial,
  });
  loaded = await ledger.load();
  const tipBeforeLease = loaded.tip_commit_sha;
  const writesBeforeLease = fixture.writeCalls;
  const evaluatedScopeReceipt = await ledger.defaultEvaluatedScopeReceipt();
  await assert.rejects(
    ledger.acquireLease(
      leaseInput(tipBeforeLease),
      { evaluated_scope_receipt: evaluatedScopeReceipt },
    ),
    (error) => error.code === "candidate-inventory-write-window",
  );
  const after = await ledger.load();
  assert.equal(after.tip_commit_sha, tipBeforeLease);
  assert.equal(fixture.writeCalls, writesBeforeLease);
  assert.notEqual(
    after.authority_projection.candidate_inventory.incomplete_cycle,
    null,
  );
});

test("atomic inventory retry recovers a durable incomplete predecessor", async () => {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const triggerIdentity = scheduleTriggerIdentity();
  const ledger = makeLedger(fixture, capability, provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
  }));
  await ledger.bootstrapCapability();
  const transportFixture = candidateTransportFixture(0);
  const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
  const baseline = await readCandidateInventoryCycle({ transportFixture });
  await ledger.appendCandidateInventoryCycleIfRequired({
    workflow_command_handle: workflowCommandHandle,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    ...baseline,
  });
  await ledger.loadOrReserveCandidateDispatch({
    workflow_command_handle: workflowCommandHandle,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });
  let loaded = await ledger.load();
  const transport = createV2GitHubCandidateInventory({
    fetch: transportFixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: candidateRepository(),
    restBaseUrl: "https://github.example.test",
  });
  transportFixture.setListedNumbers([1]);
  const incompleteInitial = await transport.scan({
    prior_inventory: loaded.authority_projection.candidate_inventory
      .completed_cycle.final_inventory,
  });
  await appendCandidateInventoryPhase({
    ledger,
    loaded,
    phase: "cycle-start",
    initial: incompleteInitial,
  });
  loaded = await ledger.load();
  const incompleteTip = loaded.tip_commit_sha;
  const incompleteCount = loaded.commit_count;
  assert.notEqual(
    loaded.authority_projection.candidate_inventory.incomplete_cycle,
    null,
  );
  const stable = await readCandidateInventoryCycle({
    transportFixture,
    priorInventory: incompleteInitial,
  });
  fixture.failRereadAfterEffectUpdate(null);
  const recovered = await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...stable,
  });
  assert.equal(recovered.state, "persisted");
  assert.equal(recovered.append_receipts.length, 0);
  assert.equal(recovered.published_record_commit_shas.length, 4);
  const published = await ledger.load();
  assert.notEqual(published.tip_commit_sha, incompleteTip);
  assert.equal(recovered.final_tip_commit_sha, published.tip_commit_sha);
  assert.equal(published.commit_count, incompleteCount + 4);
  assert.equal(
    published.authority_projection.candidate_inventory.incomplete_cycle,
    null,
  );

  const replay = await ledger.appendCandidateInventoryCycleIfRequired({
    workflow_command_handle: workflowCommandHandle,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    ...stable,
  });
  assert.equal(replay.state, "persisted");
  assert.equal(replay.reason, "candidate-inventory-already-current");
  assert.equal(replay.append_receipts.length, 0);
  assert.equal(replay.published_record_commit_shas.length, 4);
  const final = await ledger.load();
  assert.equal(final.tip_commit_sha, published.tip_commit_sha);
  assert.equal(final.commit_count, published.commit_count);
});

test("candidate inventory rejects 513 open PRs before its completion write", async () => {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const verifier = provenanceVerifier({
    eventName: "schedule",
    ref: "refs/heads/main",
    shaValue: BASE,
  });
  const ledger = makeLedger(fixture, capability, verifier);
  await ledger.bootstrapCapability();
  const transportFixture = candidateTransportFixture(513);
  const transport = createV2GitHubCandidateInventory({
    fetch: transportFixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: candidateRepository(),
    restBaseUrl: "https://github.example.test",
  });
  const initial = await transport.scan();
  let loaded = await ledger.load();
  await appendCandidateInventoryPhase({
    ledger,
    loaded,
    phase: "cycle-start",
    initial,
  });
  loaded = await ledger.load();
  const shards = [];
  for (let index = 0; index < initial.shards.length; index += 1) {
    const shard = await transport.readShard({
      inventory: initial,
      shard_index: index,
    });
    shards.push(shard);
    await appendCandidateInventoryPhase({
      ledger,
      loaded,
      phase: "shard",
      initial,
      shard,
    });
    loaded = await ledger.load();
  }
  const finalInventory = await transport.scan({ prior_inventory: initial });
  const finalShards = await readCandidateShardRound(transport, finalInventory);
  const cycleReceipt = finalizeV2CandidateInventoryCycle({
    initial_inventory: initial,
    shard_receipts: shards,
    final_inventory: finalInventory,
    final_shard_receipts: finalShards,
  });
  const writesBefore = fixture.writeCalls;
  const objectsBefore = {
    blobs: fixture.blobs.size,
    trees: fixture.trees.size,
    commits: fixture.commits.size,
  };
  const tipBefore = fixture.refTarget;
  await assert.rejects(
    appendCandidateInventoryPhase({
      ledger,
      loaded,
      phase: "cycle-complete",
      initial,
      finalInventory,
      cycleReceipt,
    }),
    (error) => error.code === "candidate-inventory-dispatch-cycle-size",
  );
  assert.equal(fixture.writeCalls, writesBefore);
  assert.deepEqual({
    blobs: fixture.blobs.size,
    trees: fixture.trees.size,
    commits: fixture.commits.size,
  }, objectsBefore);
  assert.equal(fixture.refTarget, tipBefore);
});

test("candidate inventory fails closed on missing shards, wrong trigger, and shrink", async () => {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const verifier = provenanceVerifier({
    eventName: "schedule",
    ref: "refs/heads/main",
    shaValue: BASE,
  });
  const ledger = makeLedger(fixture, capability, verifier);
  await ledger.bootstrapCapability();
  const transportFixture = candidateTransportFixture(2);
  const transport = createV2GitHubCandidateInventory({
    fetch: transportFixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: candidateRepository(),
    restBaseUrl: "https://github.example.test",
  });
  const initial = await transport.scan();
  let loaded = await ledger.load();
  const writesBeforeWrongTrigger = fixture.writeCalls;
  await assert.rejects(
    appendCandidateInventoryPhase({
      ledger,
      loaded,
      phase: "cycle-start",
      initial,
      authorityOverride: {
        eventName: "pull_request",
        relation: "scheduled-repository-inventory",
      },
    }),
    /relation differs from its trigger event/u,
  );
  assert.equal(fixture.writeCalls, writesBeforeWrongTrigger);

  await appendCandidateInventoryPhase({
    ledger,
    loaded,
    phase: "cycle-start",
    initial,
  });
  loaded = await ledger.load();
  const shard = await transport.readShard({ inventory: initial, shard_index: 0 });
  const finalInventory = await transport.scan({ prior_inventory: initial });
  const finalShards = await readCandidateShardRound(transport, finalInventory);
  const cycleReceipt = finalizeV2CandidateInventoryCycle({
    initial_inventory: initial,
    shard_receipts: [shard],
    final_inventory: finalInventory,
    final_shard_receipts: finalShards,
  });
  const writesBeforeMissingShard = fixture.writeCalls;
  await assert.rejects(
    appendCandidateInventoryPhase({
      ledger,
      loaded,
      phase: "cycle-complete",
      initial,
      finalInventory,
      cycleReceipt,
    }),
    (error) => error.code === "CANDIDATE_SHARDS_INCOMPLETE",
  );
  assert.equal(fixture.writeCalls, writesBeforeMissingShard);

  await appendCandidateInventoryPhase({
    ledger,
    loaded,
    phase: "shard",
    initial,
    shard,
  });
  loaded = await ledger.load();
  await appendCandidateInventoryPhase({
    ledger,
    loaded,
    phase: "cycle-complete",
    initial,
    finalInventory,
    cycleReceipt,
  });
  loaded = await ledger.load();

  transportFixture.setListedNumbers([2, 3]);
  const expanded = await transport.scan({ prior_inventory: finalInventory });
  assert.deepEqual(expanded.retained_prior_candidate_ids, ["1001"]);
  assert.equal(expanded.candidates.length, 3);
  await appendCandidateInventoryPhase({
    ledger,
    loaded,
    phase: "cycle-start",
    initial: expanded,
  });
  loaded = await ledger.load();
  assert.deepEqual(
    loaded.authority_projection.candidate_inventory.open_pr_discovery
      .candidates.map(({ number }) => number),
    [1, 2, 3],
  );

  const shrunk = structuredClone(expanded);
  shrunk.candidates.shift();
  const authority = loaded.authority_projection.candidate_inventory;
  const badPayload = candidateInventoryPayload({
    phase: "cycle-start",
    initial: shrunk,
    priorAuthorityDigest: authority.authority_digest,
    supersedes: authority.incomplete_cycle.cycle_id,
  });
  assert.throws(
    () => createV2GitLedgerCandidateInventoryRecord({
      predecessor_commit_sha: loaded.tip_commit_sha,
      owner: OWNER,
      server_observed_at: TIME,
      payload: badPayload,
    }),
    /candidate digest|receipt digest|high watermark/u,
  );
});

test("candidate dispatch reserves before projection and restarts the same batch", async () => {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const triggerIdentity = scheduleTriggerIdentity();
  const runIdentity = { ...OWNER };
  const verifier = provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
    runIdentity,
  });
  const ledger = makeLedger(fixture, capability, verifier);
  await ledger.bootstrapCapability();
  const candidateFixture = candidateTransportFixture(0, {
    exactScopeCandidateNumber: PR.number,
  });
  candidateFixture.setListedNumbers([PR.number]);
  await appendCompletedCandidateInventory({
    ledger,
    transportFixture: candidateFixture,
  });
  const scanCommand = await createScheduleWorkflowCommandHandle();
  const beforeReserve = await ledger.load();
  const writesBeforeReserve = fixture.writeCalls;
  const reserved = await ledger.loadOrReserveCandidateDispatch({
    workflow_command_handle: scanCommand,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });
  assert.equal(reserved.state, "dispatch");
  assert.equal(reserved.restarted, false);
  assert.equal(reserved.plan.items.length, 1);
  assert.equal(reserved.plan.items[0].candidate.number, PR.number);
  assert.ok(fixture.writeCalls > writesBeforeReserve);
  const afterReserve = await ledger.load();
  assert.equal(afterReserve.records.length, beforeReserve.records.length + 1);
  assertV2GitLedgerCandidateDispatchHandle(
    reserved.candidate_dispatch_handle,
    { purpose: "scan" },
  );
  assert.deepEqual(
    projectV2GitLedgerCandidateDispatchPlan(
      reserved.candidate_dispatch_handle,
    ),
    reserved.plan,
  );
  assert.throws(
    () => assertV2GitLedgerCandidateDispatchHandle(
      structuredClone(reserved.candidate_dispatch_handle),
    ),
    (error) => error.code === "UNTRUSTED_CANDIDATE_DISPATCH_HANDLE",
  );

  runIdentity.run_id = "9002";
  runIdentity.run_attempt = 2;
  const nextOwner = { ...runIdentity };
  const restartedLedger = makeLedger(
    fixture,
    capability,
    verifier,
  );
  const nextScanCommand = await createScheduleWorkflowCommandHandle({
    runIdentity: nextOwner,
  });
  const writesBeforeRestart = fixture.writeCalls;
  const restarted = await restartedLedger.loadOrReserveCandidateDispatch({
    workflow_command_handle: nextScanCommand,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });
  assert.equal(restarted.state, "dispatch");
  assert.equal(restarted.restarted, true);
  assert.equal(restarted.reservation_receipt.reservation_record_oid,
    reserved.reservation_receipt.reservation_record_oid);
  assert.deepEqual(restarted.plan, reserved.plan);
  assert.equal(fixture.writeCalls, writesBeforeRestart);

  for (const [label, command, trigger] of [
    ["selection policy", await createScheduleWorkflowCommandHandle({
      runIdentity: nextOwner,
      selectionPolicy: "disabled",
    }), triggerIdentity],
    ["event payload", await createScheduleWorkflowCommandHandle({
      runIdentity: nextOwner,
      event: { changed: true },
    }), triggerIdentity],
    ["workflow receipt", await createScheduleWorkflowCommandHandle({
      runIdentity: nextOwner,
      workflowSha: "3".repeat(40),
    }), triggerIdentity],
    ["trigger sha", nextScanCommand, {
      ...triggerIdentity,
      sha: HEAD,
    }],
  ]) {
    const writes = fixture.writeCalls;
    await assert.rejects(
      restartedLedger.loadOrReserveCandidateDispatch({
        workflow_command_handle: command,
        trigger_identity: trigger,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      }),
      /candidate dispatch|schedule authority/u,
      label,
    );
    assert.equal(fixture.writeCalls, writes, `${label} drift wrote a record`);
  }
  const loaded = await restartedLedger.load();
  assert.deepEqual(
    deriveV2GitLedgerCandidateDispatchAuthority(
      loaded.records,
      REPOSITORY,
    ),
    loaded.authority_projection.candidate_dispatch,
  );
});

test("active candidate dispatch rejects semantic capability drift before object writes", async () => {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const triggerIdentity = scheduleTriggerIdentity();
  const verifier = provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
  });
  const ledger = makeLedger(fixture, capability, verifier);
  await ledger.bootstrapCapability();
  const candidateFixture = candidateTransportFixture(0, {
    exactScopeCandidateNumber: PR.number,
  });
  candidateFixture.setListedNumbers([PR.number]);
  await appendCompletedCandidateInventory({
    ledger,
    transportFixture: candidateFixture,
  });
  await ledger.loadOrReserveCandidateDispatch({
    workflow_command_handle: await createScheduleWorkflowCommandHandle(),
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });

  const changedCapability = scheduleCapabilityReceipt({
    allowedEventNames: ["schedule", "workflow_dispatch"],
  });
  assert.notEqual(
    digestV2GitLedgerStableCapabilityAuthorization(capability),
    digestV2GitLedgerStableCapabilityAuthorization(changedCapability),
  );
  const tipBefore = fixture.refTarget;
  const writesBefore = fixture.writeCalls;
  const objectCreatesBefore = fixture.calls.filter((call) =>
    call.method === "POST" && new Set([
      "/git/blobs", "/git/trees", "/git/commits",
    ]).has(call.path)).length;
  const restricted = createV2GitHubGitLedgerBootstrap({
    fetch: fixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: REPOSITORY,
    ledgerRef: V2_GIT_LEDGER_REF,
    restBaseUrl: "https://api.github.test",
    bootstrapCapabilityInput: bootstrapInputForCapability(changedCapability),
    verifyWorkflowProvenance: provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    }),
  });
  await assert.rejects(
    restricted.bootstrapCapability(),
    (error) => error.code === "candidate-dispatch-capability-drift",
  );
  assert.equal(fixture.refTarget, tipBefore);
  assert.equal(fixture.writeCalls, writesBefore);
  assert.equal(fixture.calls.filter((call) =>
    call.method === "POST" && new Set([
      "/git/blobs", "/git/trees", "/git/commits",
    ]).has(call.path)).length, objectCreatesBefore);
});

test("candidate dispatch rejects an oversized public plan before reserve", async () => {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const triggerIdentity = scheduleTriggerIdentity();
  const ledger = makeLedger(fixture, capability, provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
  }));
  await ledger.bootstrapCapability();
  await appendCompletedCandidateInventory({
    ledger,
    transportFixture: candidateTransportFixture(64, {
      longNodeIds: true,
      longIds: true,
    }),
  });
  const before = await ledger.load();
  const writes = fixture.writeCalls;
  await assert.rejects(
    ledger.loadOrReserveCandidateDispatch({
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    }),
    (error) => error.code === "candidate-dispatch-plan-size",
  );
  const after = await ledger.load();
  assert.equal(fixture.writeCalls, writes);
  assert.equal(after.tip_commit_sha, before.tip_commit_sha);
  assert.equal(after.authority_projection.candidate_dispatch.active_reservation,
    null);
});

test("candidate dispatch compacts one regular full batch under its public cap",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const ledger = makeLedger(fixture, capability, provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    }));
    await ledger.bootstrapCapability();
    await appendCompletedCandidateInventory({
      ledger,
      transportFixture: candidateTransportFixture(64),
    });

    const reserved = await ledger.loadOrReserveCandidateDispatch({
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });

    assert.equal(reserved.state, "dispatch");
    assert.equal(reserved.plan.schema_version, 2);
    assert.equal(reserved.plan.items.length, 64);
    assert.deepEqual(
      Object.keys(reserved.plan.items[0]).sort(),
      ["candidate", "candidate_index"],
    );
    const firstBinding = projectV2GitLedgerCandidateDispatchBinding(
      reserved.plan,
      0,
    );
    assert.deepEqual(Object.keys(firstBinding).sort(), [
      "batch_count", "batch_index", "candidate", "cycle_id",
      "dispatch_digest", "generation_id", "inventory_digest",
    ]);
    assert.deepEqual(firstBinding.candidate, reserved.plan.items[0].candidate);
    assert.equal(firstBinding.generation_id, reserved.plan.generation_id);
    assert.equal(firstBinding.cycle_id, reserved.plan.cycle_id);
    assert.equal(firstBinding.inventory_digest, reserved.plan.inventory_digest);
    assert.equal(firstBinding.batch_index, reserved.plan.batch_index);
    assert.equal(firstBinding.batch_count, reserved.plan.batch_count);
    assert.equal(firstBinding.dispatch_digest, reserved.plan.dispatch_digest);
    assert.deepEqual(
      projectV2GitLedgerCandidateDispatchBinding(
        structuredClone(reserved.plan),
        0,
      ),
      firstBinding,
    );
    assert.throws(
      () => projectV2GitLedgerCandidateDispatchBinding(reserved.plan, 64),
      (error) =>
        error.code === "CANDIDATE_DISPATCH_PLAN_ITEM_INDEX_INVALID",
    );
    const forged = structuredClone(reserved.plan);
    forged.plan_digest = `sha256:${"f".repeat(64)}`;
    assert.throws(
      () => projectV2GitLedgerCandidateDispatchBinding(forged, 0),
      (error) => error.code === "CANDIDATE_DISPATCH_PLAN_INVALID",
    );
    assert.ok(
      Buffer.byteLength(canonicalJson(reserved.plan), "utf8") <=
        MAX_V2_CANDIDATE_DISPATCH_PLAN_BYTES,
    );
  });

test("candidate dispatch proves the complete scheduled record budget before reserve", async () => {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const triggerIdentity = scheduleTriggerIdentity();
  const ledger = makeLedger(fixture, capability, provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
  }));
  await ledger.bootstrapCapability();
  await appendCompletedCandidateInventory({
    ledger,
    transportFixture: candidateTransportFixture(293),
  });
  const before = await ledger.load();
  const budget = calculateV2GitLedgerCandidateDispatchCommitBudget({
    candidate_count: 293,
    batch_count: 5,
    reachable_record_count: before.commit_count,
  });
  assert.equal(MAX_V2_SCHEDULED_CANDIDATE_LEDGER_RECORDS, 16);
  assert.equal(budget.dispatch_commit_budget_required, 304);
  assert.equal(
    budget.candidate_execution_commit_budget_required,
    293 * 15,
  );
  assert.equal(budget.total_commit_budget_required, 293 * 16 + 11);
  assert.equal(
    budget.remaining_ledger_commit_capacity_after_dispatch,
    MAX_V2_GIT_LEDGER_COMMITS - before.commit_count -
      budget.total_commit_budget_required,
  );
  const persistedReservationBudget =
    calculateV2GitLedgerCandidateDispatchCommitBudget({
      candidate_count: 293,
      batch_count: 5,
      reachable_record_count: before.commit_count,
      reservation_persisted: true,
    });
  assert.equal(
    persistedReservationBudget.total_commit_budget_required,
    budget.total_commit_budget_required - 1,
  );
  assert.ok(budget.remaining_ledger_commit_capacity_after_dispatch < 0);
  const writes = fixture.writeCalls;
  await assert.rejects(
    ledger.loadOrReserveCandidateDispatch({
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    }),
    (error) => error.code === "candidate-dispatch-commit-capacity",
  );
  const after = await ledger.load();
  assert.equal(fixture.writeCalls, writes);
  assert.equal(after.tip_commit_sha, before.tip_commit_sha);
  assert.equal(after.authority_projection.candidate_dispatch.active_reservation,
    null);
});

test("checkpoint capacity mints only same-factory inert admissions", async () => {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const triggerIdentity = scheduleTriggerIdentity();
  const verifier = provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
  });
  const ledger = makeLedger(fixture, capability, verifier);
  await ledger.bootstrapCapability();
  const transportFixture = candidateTransportFixture(255);
  const cycle = await readIdentityDriftCandidateInventoryCycle({
    transportFixture,
    priorInventory: null,
    listedNumbersAfterInitial: Array.from(
      { length: 255 },
      (_, index) => index + 2,
    ),
    mutateAfterInitial: () => {
      transportFixture.setCandidateState(1, "closed");
    },
  });
  const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
  const writesBefore = fixture.writeCalls;
  const objectsBefore = {
    blobs: fixture.blobs.size,
    trees: fixture.trees.size,
    commits: fixture.commits.size,
  };
  const tipBefore = fixture.refTarget;

  const required = await ledger.appendCandidateInventoryCycleIfRequired({
    workflow_command_handle: workflowCommandHandle,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    ...cycle,
  });
  assert.equal(required.schema, V2_GIT_LEDGER_CHECKPOINT_REQUIRED_RESULT_SCHEMA);
  assert.equal(required.state, "checkpoint-required");
  assert.equal(required.published_record_commit_shas.length, 0);
  assert.equal(required.append_receipts.length, 0);
  assert.equal(
    required.source_load_handle.schema,
    V2_GIT_LEDGER_CHECKPOINT_SOURCE_LOAD_HANDLE_SCHEMA,
  );
  assert.equal(
    required.next_unit_admission_handle.schema,
    V2_GIT_LEDGER_CHECKPOINT_NEXT_UNIT_ADMISSION_HANDLE_SCHEMA,
  );
  assert.equal(
    required.next_unit_admission_handle.total_commit_budget_required,
    4_093,
  );
  assert.equal(
    required.next_unit_admission_handle
      .current_epoch_remaining_commit_capacity,
    0,
  );
  assert.equal(
    required.next_unit_admission_handle
      .fresh_epoch_remaining_commit_capacity,
    2,
  );
  assert.ok(Object.isFrozen(required));
  assert.ok(Object.isFrozen(required.source_load_handle));
  assert.ok(Object.isFrozen(required.next_unit_admission_handle));
  const productionApi = ledger.productionApiForTest();
  assert.equal(
    typeof productionApi.inspectCheckpointTransactionAdmission,
    "function",
  );
  assert.equal(typeof productionApi.prepareCheckpointTransaction, "undefined");
  assert.equal(typeof productionApi.publishCheckpointTransaction, "undefined");

  const inspection = ledger.inspectCheckpointTransactionAdmission({
    source_load_handle: required.source_load_handle,
    next_unit_admission_handle: required.next_unit_admission_handle,
  });
  assert.equal(
    inspection.schema,
    V2_GIT_LEDGER_CHECKPOINT_ADMISSION_INSPECTION_SCHEMA,
  );
  assert.equal(inspection.state, "checkpoint-required");
  assert.equal(inspection.source_tip_commit_sha, tipBefore);
  assert.equal(inspection.source_commit_count, 3);
  assert.equal(inspection.total_commit_budget_required, 4_093);
  assert.equal(inspection.current_epoch_remaining_commit_capacity, 0);
  assert.equal(inspection.fresh_epoch_remaining_commit_capacity, 2);

  const assertNoGitMutation = () => {
    assert.equal(fixture.writeCalls, writesBefore);
    assert.deepEqual({
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
    }, objectsBefore);
    assert.equal(fixture.refTarget, tipBefore);
  };
  assertNoGitMutation();
  assert.throws(
    () => ledger.inspectCheckpointTransactionAdmission({
      source_load_handle: structuredClone(required.source_load_handle),
      next_unit_admission_handle: required.next_unit_admission_handle,
    }),
    (error) => error.code === "UNTRUSTED_CHECKPOINT_SOURCE_LOAD_HANDLE",
  );
  assert.throws(
    () => ledger.inspectCheckpointTransactionAdmission({
      source_load_handle: required.source_load_handle,
      next_unit_admission_handle:
        structuredClone(required.next_unit_admission_handle),
    }),
    (error) =>
      error.code === "UNTRUSTED_CHECKPOINT_NEXT_UNIT_ADMISSION_HANDLE",
  );
  const second = await ledger.appendCandidateInventoryCycleIfRequired({
    workflow_command_handle: workflowCommandHandle,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    ...cycle,
  });
  assert.throws(
    () => ledger.inspectCheckpointTransactionAdmission({
      source_load_handle: required.source_load_handle,
      next_unit_admission_handle: second.next_unit_admission_handle,
    }),
    (error) => error.code === "CHECKPOINT_SOURCE_ADMISSION_MISMATCH",
  );
  const foreign = makeLedger(fixture, capability, verifier);
  assert.throws(
    () => foreign.inspectCheckpointTransactionAdmission({
      source_load_handle: required.source_load_handle,
      next_unit_admission_handle: required.next_unit_admission_handle,
    }),
    (error) => error.code === "UNTRUSTED_CHECKPOINT_SOURCE_LOAD_HANDLE",
  );
  assertNoGitMutation();
});

test("checkpoint admission rejects a next unit larger than a fresh epoch",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const ledger = makeLedger(fixture, capability, provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    }));
    await ledger.bootstrapCapability();
    const cycle = await readCandidateInventoryCycle({
      transportFixture: candidateTransportFixture(256),
    });
    const writesBefore = fixture.writeCalls;
    const objectsBefore = {
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
    };
    const tipBefore = fixture.refTarget;
    await assert.rejects(
      ledger.appendCandidateInventoryCycleIfRequired({
        workflow_command_handle: await createScheduleWorkflowCommandHandle(),
        trigger_identity: triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        ...cycle,
      }),
      (error) => error.code === "candidate-inventory-cycle-commit-capacity",
    );
    assert.equal(fixture.writeCalls, writesBefore);
    assert.deepEqual({
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
    }, objectsBefore);
    assert.equal(fixture.refTarget, tipBefore);
  });

test("checkpoint diagnostic stages one bootstrap-only closed epoch without publishing",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const verifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    });
    const ledger = makeLedger(fixture, capability, verifier);
    await ledger.bootstrapCapability();
    const transportFixture = candidateTransportFixture(255);
    const cycle = await readIdentityDriftCandidateInventoryCycle({
      transportFixture,
      priorInventory: null,
      listedNumbersAfterInitial: Array.from(
        { length: 255 },
        (_, index) => index + 2,
      ),
      mutateAfterInitial: () => {
        transportFixture.setCandidateState(1, "closed");
      },
    });
    const required = await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...cycle,
    });
    const productionApi = ledger.productionApiForTest();
    const diagnostic = createV2GitLedgerCheckpointDiagnosticAdapter(
      productionApi,
    );
    assert.equal(typeof productionApi.prepareCheckpointCandidateTransaction,
      "undefined");
    assert.equal(typeof productionApi.publishCheckpointTransaction,
      "undefined");
    assert.equal(typeof productionApi.recoverCheckpointTransaction,
      "undefined");
    assert.throws(
      () => createV2GitLedgerCheckpointDiagnosticAdapter({
        ...productionApi,
      }),
      (error) => error.code === "UNTRUSTED_CHECKPOINT_DIAGNOSTIC_SOURCE",
    );
    const refBefore = fixture.refTarget;
    const patchCountBefore = fixture.calls.filter((call) =>
      call.method === "PATCH").length;
    const callsBeforePrepare = fixture.calls.length;
    const blobOidsBeforePrepare = new Set(fixture.blobs.keys());

    const prepared = await diagnostic.prepareCheckpointCandidateTransaction({
      source_load_handle: required.source_load_handle,
      next_unit_admission_handle: required.next_unit_admission_handle,
    });

    assert.equal(
      prepared.schema,
      V2_GIT_LEDGER_CHECKPOINT_PREPARATION_DIAGNOSTIC_SCHEMA,
    );
    assert.equal(prepared.state, "prepared");
    assert.equal(prepared.profile, "bootstrap-only-quiescent-v1");
    assert.equal(prepared.source_tip_commit_sha, refBefore);
    assert.equal(prepared.source_commit_count, 3);
    assert.equal(prepared.epoch_commit_count, 6);
    assert.equal(prepared.semantic_record_count, 5);
    assert.equal(prepared.frontier_entry_count, 9);
    assert.equal(prepared.frontier_node_count, 17);
    assert.equal(prepared.first_dispatch_plan_schema_version, 2);
    assert.ok(
      prepared.first_dispatch_plan_bytes <=
        MAX_V2_CANDIDATE_DISPATCH_PLAN_BYTES,
    );
    assert.ok(prepared.planned_write_object_count > 0);
    assert.ok(
      prepared.planned_total_request_count <=
        V2_GIT_LEDGER_HTTP_LIMITS.request_count,
    );
    assert.ok(
      prepared.planned_loader_request_count <
        prepared.planned_total_request_count,
    );
    assert.ok(
      prepared.planned_write_request_body_bytes <= 256 * 1024 * 1024,
    );
    const transactionCalls = fixture.calls.slice(callsBeforePrepare);
    const objectPosts = transactionCalls.filter((call) =>
      call.method === "POST");
    assert.equal(objectPosts.length, prepared.planned_write_object_count);
    assert.equal(
      objectPosts.reduce((total, call) =>
        total + Buffer.byteLength(call.body, "utf8"), 0),
      prepared.planned_write_request_body_bytes,
    );
    assert.equal(objectPosts.every((call) =>
      Buffer.byteLength(call.body, "utf8") <= 8 * 1024 * 1024), true);
    const addedBlobOids = [...fixture.blobs.keys()].filter((objectOid) =>
      !blobOidsBeforePrepare.has(objectOid));
    assert.ok(addedBlobOids.length > 0);
    assert.equal(addedBlobOids.every((objectOid) =>
      fixture.blobs.get(objectOid).bytes.byteLength <=
        MAX_V2_GIT_LEDGER_BLOB_BYTES), true);
    assert.ok(fixture.commits.has(prepared.checkpoint_commit_sha));
    assert.ok(fixture.commits.has(prepared.transaction_tip_commit_sha));
    assert.equal(fixture.refTarget, refBefore);
    assert.equal(fixture.calls.filter((call) =>
      call.method === "PATCH").length, patchCountBefore);
    assert.equal(
      Object.keys(prepared).some((key) =>
        key.includes("handle") || key.includes("receipt")),
      false,
    );

    const objectsAfterPrepare = {
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      calls: fixture.calls.length,
    };
    const repeated = await diagnostic.prepareCheckpointCandidateTransaction({
      source_load_handle: required.source_load_handle,
      next_unit_admission_handle: required.next_unit_admission_handle,
    });
    assert.strictEqual(repeated, prepared);
    assert.deepEqual({
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      calls: fixture.calls.length,
    }, objectsAfterPrepare);

    const readsBeforeClone = fixture.calls.length;
    await assert.rejects(
      diagnostic.loadPreparedCheckpointTransaction(
        structuredClone(prepared),
      ),
      (error) =>
        error.code === "UNTRUSTED_CHECKPOINT_PREPARATION_DIAGNOSTIC",
    );
    assert.equal(fixture.calls.length, readsBeforeClone);
    const foreign = makeLedger(fixture, capability, verifier);
    const foreignDiagnostic = createV2GitLedgerCheckpointDiagnosticAdapter(
      foreign.productionApiForTest(),
    );
    await assert.rejects(
      foreignDiagnostic.loadPreparedCheckpointTransaction(prepared),
      (error) =>
        error.code === "UNTRUSTED_CHECKPOINT_PREPARATION_DIAGNOSTIC",
    );
    assert.equal(fixture.calls.length, readsBeforeClone);

    const writesBeforeValidatedLoad = fixture.writeCalls;
    const validated = await diagnostic.loadPreparedCheckpointTransaction(
      prepared,
    );
    assert.equal(validated.validated, true);
    assert.equal(validated.ref_published, false);
    assert.equal(validated.transaction_tip_commit_sha,
      prepared.transaction_tip_commit_sha);
    assert.equal(validated.epoch_commit_count, prepared.epoch_commit_count);
    assert.equal(validated.semantic_record_count,
      prepared.semantic_record_count);
    assert.equal(validated.frontier_entry_count, 9);
    assert.equal(validated.frontier_node_count, 17);
    assert.equal(fixture.writeCalls, writesBeforeValidatedLoad);
    assert.equal(fixture.refTarget, refBefore);
    assert.equal(fixture.calls.filter((call) =>
      call.method === "PATCH").length, patchCountBefore);

    await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...cycle,
    });
    const readsBeforeStale = fixture.calls.length;
    await assert.rejects(
      diagnostic.loadPreparedCheckpointTransaction(prepared),
      (error) =>
        error.code === "STALE_CHECKPOINT_PREPARATION_DIAGNOSTIC",
    );
    assert.equal(fixture.calls.length, readsBeforeStale);
    assert.equal(fixture.refTarget, refBefore);
    assert.equal(fixture.calls.filter((call) =>
      call.method === "PATCH").length, patchCountBefore);
  });

test("checkpoint diagnostic claims single-flight before its first external call",
  async () => {
    const fixture = githubGitFixture();
    let armed = false;
    let diagnostic = null;
    let pair = null;
    let secondary = null;
    let reentered = false;
    let fetchDepth = 0;
    let maximumFetchDepth = 0;
    const fetch = (url, init) => {
      fetchDepth += 1;
      maximumFetchDepth = Math.max(maximumFetchDepth, fetchDepth);
      try {
        if (armed && !reentered) {
          reentered = true;
          secondary = diagnostic.prepareCheckpointCandidateTransaction(pair);
        }
        return fixture.fetch(url, init);
      } finally {
        fetchDepth -= 1;
      }
    };
    const context = await bootstrapCheckpointAdmissionFixture({
      fixture,
      fetch,
    });
    diagnostic = context.diagnostic;
    pair = {
      source_load_handle: context.required.source_load_handle,
      next_unit_admission_handle:
        context.required.next_unit_admission_handle,
    };
    const refBefore = fixture.refTarget;
    const patchCountBefore = fixture.calls.filter(({ method }) =>
      method === "PATCH").length;
    armed = true;
    const primary = diagnostic.prepareCheckpointCandidateTransaction(pair);
    await Promise.resolve();
    assert.notEqual(secondary, null);
    const [primaryResult, secondaryResult] = await Promise.all([
      primary,
      secondary,
    ]);
    assert.strictEqual(secondaryResult, primaryResult);
    assert.equal(maximumFetchDepth, 1);
    assert.equal(fixture.refTarget, refBefore);
    assert.equal(
      fixture.calls.filter(({ method }) => method === "PATCH").length,
      patchCountBefore,
    );
  });

test("checkpoint seed preserves Q0 future queries across an autonomous ref load",
  async () => {
    const context = await bootstrapCheckpointAdmissionFixture();
    const prepared = await context.diagnostic
      .prepareCheckpointCandidateTransaction({
        source_load_handle: context.required.source_load_handle,
        next_unit_admission_handle:
          context.required.next_unit_admission_handle,
      });
    const validated = await context.diagnostic
      .loadPreparedCheckpointTransaction(prepared);
    const publication = await context.fixture.fetch(
      "https://api.github.test/repos/owner/repo/git/refs/" +
        "heads/codex-review-gate-ledger-v2",
      {
        method: "PATCH",
        headers: {},
        body: JSON.stringify({
          sha: prepared.transaction_tip_commit_sha,
          force: false,
        }),
      },
    );
    assert.equal(publication.status, 200);
    assert.equal(
      context.fixture.refTarget,
      prepared.transaction_tip_commit_sha,
    );

    const restarted = makeLedger(
      context.fixture,
      scheduleCapabilityReceipt(),
      provenanceVerifier({
        eventName: "schedule",
        ref: context.triggerIdentity.ref,
        shaValue: context.triggerIdentity.sha,
        jtiPrefix: "checkpoint-restart-oidc-jti",
      }),
    );
    const writesBeforeLoad = context.fixture.writeCalls;
    const callsBeforeLoad = context.fixture.calls.length;
    const patchCountBeforeLoad = context.fixture.calls.filter((call) =>
      call.method === "PATCH").length;
    const loaded = await restarted.load();
    const autonomousLoadRequestCount =
      context.fixture.calls.length - callsBeforeLoad;
    assert.equal(
      autonomousLoadRequestCount,
      prepared.planned_loader_request_count,
      "autonomous checkpoint load differs from its exact request plan",
    );
    assert.equal(context.fixture.writeCalls, writesBeforeLoad);
    assert.equal(context.fixture.calls.filter((call) =>
      call.method === "PATCH").length, patchCountBeforeLoad);
    assert.equal(context.fixture.refTarget, prepared.transaction_tip_commit_sha);
    assert.equal(loaded.tip_commit_sha, prepared.transaction_tip_commit_sha);
    assert.equal(loaded.epoch_root_commit_sha, prepared.checkpoint_commit_sha);
    assert.equal(loaded.epoch_commit_count, prepared.epoch_commit_count);
    assert.equal(loaded.logical_record_count, 9);
    assert.equal(loaded.commit_count, prepared.epoch_commit_count);
    assert.equal(loaded.records.length, 8);
    assert.equal(loaded.provenance_reverification.length, 9);
    assert.deepEqual(
      loaded.records.map((entry) => entry.envelope.record_type),
      [
        "genesis",
        "capability-canary",
        "capability-attestation",
        "candidate-inventory-observation",
        "candidate-inventory-observation",
        "candidate-inventory-observation",
        "candidate-inventory-observation",
        "candidate-dispatch-observation",
      ],
    );
    assert.equal(loaded.active_lease, null);
    assert.equal(loaded.control_comment_binding, null);
    assert.equal(loaded.effect_intent_count, 0);
    assert.equal(loaded.effect_response_count, 0);
    assert.equal(
      loaded.capability.attestation_commit_sha,
      context.required.source_load_handle
        .capability_attestation_commit_sha,
    );
    assert.equal(
      loaded.authority_projection.candidate_inventory.completed_cycle
        .cycle_receipt.receipt_digest,
      context.required.next_unit_admission_handle
        .stable_cycle_receipt_digest,
    );
    assert.notEqual(
      loaded.authority_projection.candidate_dispatch.active_reservation,
      null,
    );
    assert.deepEqual(
      {
        source_projection_digest:
          loaded.checkpoint.source_projection_digest,
        authority_projection_digest:
          loaded.checkpoint.authority_projection_digest,
        candidate_inventory_authority_digest:
          loaded.checkpoint.candidate_inventory_authority_digest,
        candidate_dispatch_authority_digest:
          loaded.checkpoint.candidate_dispatch_authority_digest,
        first_dispatch_plan_digest:
          loaded.checkpoint.first_dispatch_plan_digest,
      },
      {
        source_projection_digest:
          validated.source_projection_digest,
        authority_projection_digest:
          validated.authority_projection_digest,
        candidate_inventory_authority_digest:
          validated.candidate_inventory_authority_digest,
        candidate_dispatch_authority_digest:
          validated.candidate_dispatch_authority_digest,
        first_dispatch_plan_digest:
          validated.first_dispatch_plan_digest,
      },
    );
    assert.match(validated.future_state_digest, /^sha256:[0-9a-f]{64}$/u);
    assert.equal(
      loaded.checkpoint.future_state_digest,
      validated.future_state_digest,
    );
    assert.match(validated.query_seed_digest, /^sha256:[0-9a-f]{64}$/u);
    assert.equal(
      loaded.checkpoint.query_seed_digest,
      validated.query_seed_digest,
    );
    const queryScope = {
      pull_request: PR,
      head_ref_oid: HEAD,
      base_ref_oid: BASE,
      potential_merge_commit_oid: POTENTIAL,
    };
    const controlAuthority = await restarted.loadControlPlaneAuthority(
      queryScope,
    );
    assert.equal(controlAuthority.load.commit_count,
      prepared.epoch_commit_count);
    assert.equal(controlAuthority.scoped_authority.ordered_records.length, 8);
    assert.equal(
      controlAuthority.scoped_authority.candidate_inventory.authority_digest,
      loaded.authority_projection.candidate_inventory.authority_digest,
    );
    assert.equal(
      controlAuthority.scoped_authority.candidate_dispatch.authority_digest,
      loaded.authority_projection.candidate_dispatch.authority_digest,
    );
    const controlReceipt =
      createV2ControlPlaneReceiptFromGitLedgerAuthority(controlAuthority);
    assert.equal(
      controlReceipt.record_count,
      controlAuthority.scoped_authority.ordered_records.length,
    );
    assert.deepEqual(controlReceipt.derived.generations, []);
    assert.deepEqual(controlReceipt.derived.request_bindings, []);
    assert.deepEqual(controlReceipt.derived.artifact_bindings, []);
    assert.equal(context.fixture.writeCalls, writesBeforeLoad);
    assert.equal(context.fixture.calls.filter((call) =>
      call.method === "PATCH").length, patchCountBeforeLoad);
    const writesBeforeRejectedMutation = context.fixture.writeCalls;
    const objectsBeforeRejectedMutation = {
      blobs: context.fixture.blobs.size,
      trees: context.fixture.trees.size,
      commits: context.fixture.commits.size,
    };
    const callsBeforeRejectedMutation = context.fixture.calls.length;
    await assert.rejects(
      appendCandidateInventoryPhase({
        ledger: restarted,
        loaded,
        phase: "cycle-start",
        initial: context.cycle.final_inventory,
      }),
      (error) => error.code === "CHECKPOINT_STATE_BINDING",
    );
    assert.equal(context.fixture.writeCalls, writesBeforeRejectedMutation);
    assert.deepEqual({
      blobs: context.fixture.blobs.size,
      trees: context.fixture.trees.size,
      commits: context.fixture.commits.size,
    }, objectsBeforeRejectedMutation);
    assert.equal(context.fixture.calls.filter((call) =>
      call.method === "PATCH").length, patchCountBeforeLoad);
    assert.equal(context.fixture.refTarget, prepared.transaction_tip_commit_sha);
    await assert.rejects(
      restarted.appendCandidateInventoryCycleIfRequired({
        workflow_command_handle: await createScheduleWorkflowCommandHandle(),
        trigger_identity: context.triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        ...context.cycle,
      }),
      (error) => error.code === "CHECKPOINT_EPOCH_READ_ONLY",
    );
    assert.equal(context.fixture.writeCalls, writesBeforeRejectedMutation);
    assert.deepEqual({
      blobs: context.fixture.blobs.size,
      trees: context.fixture.trees.size,
      commits: context.fixture.commits.size,
    }, objectsBeforeRejectedMutation);
    assert.equal(context.fixture.calls.filter((call) =>
      call.method === "PATCH").length, patchCountBeforeLoad);
    assert.equal(context.fixture.refTarget, prepared.transaction_tip_commit_sha);
    assert.ok(context.fixture.calls.length > callsBeforeRejectedMutation);
  });

test("autonomous checkpoint source provenance binds its exact request",
  async () => {
    const context = await bootstrapCheckpointAdmissionFixture();
    const prepared = await context.diagnostic
      .prepareCheckpointCandidateTransaction({
        source_load_handle: context.required.source_load_handle,
        next_unit_admission_handle:
          context.required.next_unit_admission_handle,
      });
    const rewritten =
      await resealBootstrapCheckpointSourceProvenanceForFixture({
        fixture: context.fixture,
        prepared,
        mutateReceipt: (receipt) => {
          receipt.claims.repository = "owner/forged-repository";
          receipt.claims.repository_id = "999";
          receipt.claims.workflow_ref =
            "owner/forged/.github/workflows/review.yml@refs/heads/main";
          receipt.claims.workflow_sha = "f".repeat(40);
          receipt.audience = `${V2_GIT_LEDGER_OIDC_AUDIENCE}:forged`;
          receipt.claims.aud = receipt.audience;
          receipt.verified_at_server_time = new Date(
            Date.parse(receipt.verified_at_server_time) + 1000,
          ).toISOString();
        },
      });
    const publication = await context.fixture.fetch(
      "https://api.github.test/repos/owner/repo/git/refs/" +
        "heads/codex-review-gate-ledger-v2",
      {
        method: "PATCH",
        headers: {},
        body: JSON.stringify({
          sha: rewritten.transaction_tip_commit_sha,
          force: false,
        }),
      },
    );
    assert.equal(publication.status, 200);
    const restarted = makeLedger(
      context.fixture,
      scheduleCapabilityReceipt(),
      provenanceVerifier({
        eventName: "schedule",
        ref: context.triggerIdentity.ref,
        shaValue: context.triggerIdentity.sha,
        jtiPrefix: "checkpoint-source-request-oidc-jti",
      }),
    );
    const before = {
      writes: context.fixture.writeCalls,
      patches: context.fixture.calls.filter((call) =>
        call.method === "PATCH").length,
      ref: context.fixture.refTarget,
      blobs: context.fixture.blobs.size,
      trees: context.fixture.trees.size,
      commits: context.fixture.commits.size,
    };
    await assert.rejects(
      restarted.load(),
      (error) => error.code === "CHECKPOINT_SOURCE_PROVENANCE_EVIDENCE",
    );
    assert.deepEqual({
      writes: context.fixture.writeCalls,
      patches: context.fixture.calls.filter((call) =>
        call.method === "PATCH").length,
      ref: context.fixture.refTarget,
      blobs: context.fixture.blobs.size,
      trees: context.fixture.trees.size,
      commits: context.fixture.commits.size,
    }, before);
  });

test("autonomous checkpoint load rejects tampered ref objects without writes",
  async () => {
    const context = await bootstrapCheckpointAdmissionFixture();
    const prepared = await context.diagnostic
      .prepareCheckpointCandidateTransaction({
        source_load_handle: context.required.source_load_handle,
        next_unit_admission_handle:
          context.required.next_unit_admission_handle,
      });
    const publication = await context.fixture.fetch(
      "https://api.github.test/repos/owner/repo/git/refs/" +
        "heads/codex-review-gate-ledger-v2",
      {
        method: "PATCH",
        headers: {},
        body: JSON.stringify({
          sha: prepared.transaction_tip_commit_sha,
          force: false,
        }),
      },
    );
    assert.equal(publication.status, 200);
    const checkpointCommit = context.fixture.commits.get(
      prepared.checkpoint_commit_sha,
    );
    const rootTree = context.fixture.trees.get(checkpointCommit.tree.sha);
    const stateTreeEntry = rootTree.tree.find((entry) =>
      entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_PATH);
    const stateTree = context.fixture.trees.get(stateTreeEntry.sha);
    const stateBlobEntry = stateTree.tree.find((entry) =>
      entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_BLOB_PATH);
    const originalStateBlob = context.fixture.blobs.get(stateBlobEntry.sha);
    const changedState = JSON.parse(originalStateBlob.content);
    changedState.source.rolling_audit_root = rawDigest(
      "autonomous-loader-tamper",
    );
    const changedContent = `${canonicalJson(changedState)}\n`;
    context.fixture.blobs.set(stateBlobEntry.sha, {
      sha: stateBlobEntry.sha,
      content: changedContent,
      bytes: Buffer.from(changedContent, "utf8"),
    });
    const restarted = makeLedger(
      context.fixture,
      scheduleCapabilityReceipt(),
      provenanceVerifier({
        eventName: "schedule",
        ref: context.triggerIdentity.ref,
        shaValue: context.triggerIdentity.sha,
        jtiPrefix: "checkpoint-tamper-oidc-jti",
      }),
    );
    const before = {
      writes: context.fixture.writeCalls,
      patches: context.fixture.calls.filter((call) =>
        call.method === "PATCH").length,
      ref: context.fixture.refTarget,
      blobs: context.fixture.blobs.size,
      trees: context.fixture.trees.size,
      commits: context.fixture.commits.size,
    };
    try {
      await assert.rejects(
        restarted.load(),
        (error) => error.code === "CHECKPOINT_BLOB_IDENTITY",
      );
    } finally {
      context.fixture.blobs.set(stateBlobEntry.sha, originalStateBlob);
    }
    assert.deepEqual({
      writes: context.fixture.writeCalls,
      patches: context.fixture.calls.filter((call) =>
        call.method === "PATCH").length,
      ref: context.fixture.refTarget,
      blobs: context.fixture.blobs.size,
      trees: context.fixture.trees.size,
      commits: context.fixture.commits.size,
    }, before);
  });

test("autonomous checkpoint load enforces one shared post-provenance deadline",
  async () => {
    const context = await bootstrapCheckpointAdmissionFixture();
    const prepared = await context.diagnostic
      .prepareCheckpointCandidateTransaction({
        source_load_handle: context.required.source_load_handle,
        next_unit_admission_handle:
          context.required.next_unit_admission_handle,
      });
    const publication = await context.fixture.fetch(
      "https://api.github.test/repos/owner/repo/git/refs/" +
        "heads/codex-review-gate-ledger-v2",
      {
        method: "PATCH",
        headers: {},
        body: JSON.stringify({
          sha: prepared.transaction_tip_commit_sha,
          force: false,
        }),
      },
    );
    assert.equal(publication.status, 200);
    const performancePrototype = Object.getPrototypeOf(performance);
    const originalNow = Object.getOwnPropertyDescriptor(
      performancePrototype,
      "now",
    );
    let now = 0;
    Object.defineProperty(performancePrototype, "now", {
      ...originalNow,
      value: () => now,
    });
    const baseVerifier = provenanceVerifier({
      eventName: "schedule",
      ref: context.triggerIdentity.ref,
      shaValue: context.triggerIdentity.sha,
      jtiPrefix: "checkpoint-deadline-oidc-jti",
    });
    const verifier = async (...args) => {
      const result = await baseVerifier(...args);
      if (
        args[0].mode === "reverify-stored" &&
        args[0].provenance_request.operation ===
          "candidate-dispatch-observation"
      ) {
        now = 300_001;
      }
      return result;
    };
    const restarted = makeLedger(
      context.fixture,
      scheduleCapabilityReceipt(),
      verifier,
    );
    const before = {
      writes: context.fixture.writeCalls,
      patches: context.fixture.calls.filter((call) =>
        call.method === "PATCH").length,
      ref: context.fixture.refTarget,
    };
    try {
      await assert.rejects(
        restarted.load(),
        (error) => error.code === "checkpoint-transaction-timeout",
      );
    } finally {
      Object.defineProperty(performancePrototype, "now", originalNow);
    }
    assert.deepEqual({
      writes: context.fixture.writeCalls,
      patches: context.fixture.calls.filter((call) =>
        call.method === "PATCH").length,
      ref: context.fixture.refTarget,
    }, before);
  });

test("autonomous checkpoint load rejects a regressed post-ref boundary",
  async () => {
    const fixture = githubGitFixture();
    let regressSecondRefRead = false;
    let refReads = 0;
    const fetch = async (url, init) => {
      const responseValue = await fixture.fetch(url, init);
      const path = new URL(url).pathname;
      if (
        regressSecondRefRead && init.method === "GET" &&
        path.endsWith("/git/ref/heads/codex-review-gate-ledger-v2")
      ) {
        refReads += 1;
        if (refReads === 2) {
          const bytes = await responseValue.arrayBuffer();
          const headers = new Headers(responseValue.headers);
          headers.set("Date", new Date("2020-01-01T00:00:00Z")
            .toUTCString());
          return new Response(bytes, {
            status: responseValue.status,
            headers,
          });
        }
      }
      return responseValue;
    };
    const context = await bootstrapCheckpointAdmissionFixture({
      fixture,
      fetch,
    });
    const prepared = await context.diagnostic
      .prepareCheckpointCandidateTransaction({
        source_load_handle: context.required.source_load_handle,
        next_unit_admission_handle:
          context.required.next_unit_admission_handle,
      });
    const publication = await fixture.fetch(
      "https://api.github.test/repos/owner/repo/git/refs/" +
        "heads/codex-review-gate-ledger-v2",
      {
        method: "PATCH",
        headers: {},
        body: JSON.stringify({
          sha: prepared.transaction_tip_commit_sha,
          force: false,
        }),
      },
    );
    assert.equal(publication.status, 200);
    const restarted = makeLedger(
      { fetch },
      scheduleCapabilityReceipt(),
      provenanceVerifier({
        eventName: "schedule",
        ref: context.triggerIdentity.ref,
        shaValue: context.triggerIdentity.sha,
        jtiPrefix: "checkpoint-ref-time-regression-oidc-jti",
      }),
    );
    const before = {
      writes: fixture.writeCalls,
      patches: fixture.calls.filter((call) =>
        call.method === "PATCH").length,
      ref: fixture.refTarget,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
    };
    regressSecondRefRead = true;
    await assert.rejects(
      restarted.load(),
      (error) => error.code === "unstable-ref-time",
    );
    assert.deepEqual({
      writes: fixture.writeCalls,
      patches: fixture.calls.filter((call) =>
        call.method === "PATCH").length,
      ref: fixture.refTarget,
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
    }, before);
  });

test("checkpoint diagnostic rejects an unrepresentable next unit before object writes",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const ledger = makeLedger(fixture, capability, provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    }));
    await ledger.bootstrapCapability();
    const transportFixture = candidateTransportFixture(255, {
      longNodeIds: true,
      longIds: true,
    });
    const cycle = await readIdentityDriftCandidateInventoryCycle({
      transportFixture,
      priorInventory: null,
      listedNumbersAfterInitial: Array.from(
        { length: 255 },
        (_, index) => index + 2,
      ),
      mutateAfterInitial: () => {
        transportFixture.setCandidateState(1, "closed");
      },
    });
    const required = await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...cycle,
    });
    const diagnostic = createV2GitLedgerCheckpointDiagnosticAdapter(
      ledger.productionApiForTest(),
    );
    const before = {
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      patches: fixture.calls.filter((call) => call.method === "PATCH").length,
      ref: fixture.refTarget,
    };
    await assert.rejects(
      diagnostic.prepareCheckpointCandidateTransaction({
        source_load_handle: required.source_load_handle,
        next_unit_admission_handle: required.next_unit_admission_handle,
      }),
      (error) => error.code ===
        "CHECKPOINT_DISPATCH_PLAN_UNREPRESENTABLE",
    );
    assert.deepEqual({
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      patches: fixture.calls.filter((call) => call.method === "PATCH").length,
      ref: fixture.refTarget,
    }, before);
  });

test("checkpoint diagnostic rejects a mature source before object writes",
  async () => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const ledger = makeLedger(fixture, capability, provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    }));
    await ledger.bootstrapCapability();
    const transportFixture = candidateTransportFixture(255);
    const largeCycle = await readIdentityDriftCandidateInventoryCycle({
      transportFixture,
      priorInventory: null,
      listedNumbersAfterInitial: Array.from(
        { length: 255 },
        (_, index) => index + 2,
      ),
      mutateAfterInitial: () => {
        transportFixture.setCandidateState(1, "closed");
      },
    });
    const required = await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...largeCycle,
    });
    const smallCycle = await readCandidateInventoryCycle({
      transportFixture: candidateTransportFixture(0),
    });
    await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...smallCycle,
    });
    const diagnostic = createV2GitLedgerCheckpointDiagnosticAdapter(
      ledger.productionApiForTest(),
    );
    const before = {
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      patches: fixture.calls.filter((call) => call.method === "PATCH").length,
      ref: fixture.refTarget,
    };
    await assert.rejects(
      diagnostic.prepareCheckpointCandidateTransaction({
        source_load_handle: required.source_load_handle,
        next_unit_admission_handle: required.next_unit_admission_handle,
      }),
      (error) => error.code === "CHECKPOINT_SOURCE_BOUNDARY_UNSUPPORTED",
    );
    assert.deepEqual({
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      patches: fixture.calls.filter((call) => call.method === "PATCH").length,
      ref: fixture.refTarget,
    }, before);
  });

test("checkpoint diagnostic loader rejects authenticated object tampering without publishing",
  async (t) => {
    const fixture = githubGitFixture();
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const baseVerifier = provenanceVerifier({
      eventName: "schedule",
      ref: triggerIdentity.ref,
      shaValue: triggerIdentity.sha,
    });
    let tamperCheckpointReverification = false;
    const verifier = async (...args) => {
      const result = await baseVerifier(...args);
      if (
        tamperCheckpointReverification &&
        args[0].mode === "reverify-stored" &&
        args[0].provenance_request.operation === "checkpoint-rotate"
      ) {
        const changed = structuredClone(result);
        changed.receipt.key_id = "fixture-key-tampered";
        delete changed.receipt.receipt_digest;
        changed.receipt.receipt_digest = digestCanonical(
          "codex-review-gate-v2-git-ledger-provenance",
          changed.receipt,
        );
        return changed;
      }
      return result;
    };
    const ledger = makeLedger(fixture, capability, verifier);
    await ledger.bootstrapCapability();
    const transportFixture = candidateTransportFixture(255);
    const cycle = await readIdentityDriftCandidateInventoryCycle({
      transportFixture,
      priorInventory: null,
      listedNumbersAfterInitial: Array.from(
        { length: 255 },
        (_, index) => index + 2,
      ),
      mutateAfterInitial: () => {
        transportFixture.setCandidateState(1, "closed");
      },
    });
    const required = await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...cycle,
    });
    const diagnostic = createV2GitLedgerCheckpointDiagnosticAdapter(
      ledger.productionApiForTest(),
    );
    const prepared = await diagnostic.prepareCheckpointCandidateTransaction({
      source_load_handle: required.source_load_handle,
      next_unit_admission_handle: required.next_unit_admission_handle,
    });
    const checkpointCommit = fixture.commits.get(
      prepared.checkpoint_commit_sha,
    );
    const rootTree = fixture.trees.get(checkpointCommit.tree.sha);
    const carrierEntry = rootTree.tree.find((entry) =>
      entry.path === V2_GIT_LEDGER_BLOB_PATH);
    const stateTreeEntry = rootTree.tree.find((entry) =>
      entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_PATH);
    const stateTree = fixture.trees.get(stateTreeEntry.sha);
    const stateBlobEntry = stateTree.tree.find((entry) =>
      entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_BLOB_PATH);
    const frontierTreeEntry = stateTree.tree.find((entry) =>
      entry.path === V2_GIT_LEDGER_CHECKPOINT_FRONTIER_NODES_PATH);
    const frontierTree = fixture.trees.get(frontierTreeEntry.sha);
    const frontierBlobOid = frontierTree.tree[0].sha;
    const patchCount = fixture.calls.filter((call) =>
      call.method === "PATCH").length;
    const stableRef = fixture.refTarget;
    const stableObjectCounts = {
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
    };
    const stableWriteCalls = fixture.writeCalls;
    const assertNoPublication = () => {
      assert.equal(fixture.refTarget, stableRef);
      assert.equal(fixture.calls.filter((call) =>
        call.method === "PATCH").length, patchCount);
      assert.deepEqual({
        blobs: fixture.blobs.size,
        trees: fixture.trees.size,
        commits: fixture.commits.size,
      }, stableObjectCounts);
      assert.equal(fixture.writeCalls, stableWriteCalls);
    };
    const replaceJsonBlob = async ({
      objectOid,
      mutate,
      expectedCode = "CHECKPOINT_BLOB_IDENTITY",
    }) => {
      const original = fixture.blobs.get(objectOid);
      const value = JSON.parse(original.content);
      mutate(value);
      const content = `${canonicalJson(value)}\n`;
      fixture.blobs.set(objectOid, {
        sha: objectOid,
        content,
        bytes: Buffer.from(content, "utf8"),
      });
      try {
        await assert.rejects(
          diagnostic.loadPreparedCheckpointTransaction(prepared),
          (error) => error.code === expectedCode,
        );
      } finally {
        fixture.blobs.set(objectOid, original);
      }
      assertNoPublication();
    };

    for (const [label, mutate] of [
      ["manifest tree-entry count", (state) => {
        state.prefix_frontier.tree_entry_count += 1;
      }],
      ["manifest node count and 2N-1 invariant", (state) => {
        state.prefix_frontier.node_count += 2;
      }],
      ["manifest raw node bytes", (state) => {
        state.prefix_frontier.raw_node_bytes += 1;
      }],
      ["manifest OID-set digest", (state) => {
        state.prefix_frontier.oid_set_digest = rawDigest("tampered-oids");
      }],
      ["rolling audit root", (state) => {
        state.source.rolling_audit_root = rawDigest("tampered-rolling-root");
      }],
      ["prefix provenance reverification", (state) => {
        state.source.provenance_reverification_digest =
          rawDigest("tampered-provenance");
      }],
      ["frontier root", (state) => {
        state.prefix_frontier.root.root_digest =
          rawDigest("tampered-frontier-root");
      }],
    ]) {
      await t.test(label, async () => {
        await replaceJsonBlob({ objectOid: stateBlobEntry.sha, mutate });
      });
    }

    await t.test("checkpoint provenance carrier", async () => {
      await replaceJsonBlob({
        objectOid: carrierEntry.sha,
        mutate: (carrier) => {
          carrier.workflow_provenance.receipt_digest =
            rawDigest("tampered-checkpoint-provenance");
        },
      });
    });

    await t.test("checkpoint live provenance reverification", async () => {
      tamperCheckpointReverification = true;
      try {
        await assert.rejects(
          diagnostic.loadPreparedCheckpointTransaction(prepared),
          (error) => error.code === "stored-provenance-mismatch",
        );
      } finally {
        tamperCheckpointReverification = false;
      }
      assertNoPublication();
    });

    await t.test("frontier node content", async () => {
      const original = fixture.blobs.get(frontierBlobOid);
      const bytes = Buffer.from(original.bytes);
      bytes[0] ^= 1;
      fixture.blobs.set(frontierBlobOid, {
        sha: frontierBlobOid,
        content: bytes.toString("utf8"),
        bytes,
      });
      try {
        await assert.rejects(
          diagnostic.loadPreparedCheckpointTransaction(prepared),
          (error) =>
            error.code === "PERSISTENT_FRONTIER_OBJECT_UNREADABLE" &&
            error.cause?.code === "CHECKPOINT_BLOB_IDENTITY",
        );
      } finally {
        fixture.blobs.set(frontierBlobOid, original);
      }
      assertNoPublication();
    });

    await t.test("frontier tree identity and closed entry set", async () => {
      const original = structuredClone(frontierTree);
      frontierTree.tree.push({
        path: `extra-${frontierBlobOid}.json`,
        mode: "100644",
        type: "blob",
        sha: frontierBlobOid,
      });
      try {
        await assert.rejects(
          diagnostic.loadPreparedCheckpointTransaction(prepared),
          (error) => error.code === "CHECKPOINT_TREE_IDENTITY",
        );
      } finally {
        fixture.trees.set(frontierTreeEntry.sha, original);
      }
      assertNoPublication();
    });

    await t.test("frontier OID set is independent of API entry order",
      async () => {
        const currentTree = fixture.trees.get(frontierTreeEntry.sha);
        currentTree.tree.reverse();
        const writesBefore = fixture.writeCalls;
        try {
          const validated = await diagnostic
            .loadPreparedCheckpointTransaction(prepared);
          assert.equal(validated.validated, true);
          assert.equal(validated.ref_published, false);
          assert.equal(validated.frontier_entry_count, 9);
          assert.equal(validated.frontier_node_count, 17);
        } finally {
          currentTree.tree.reverse();
        }
        assert.equal(fixture.writeCalls, writesBefore);
        assertNoPublication();
      });

    await t.test("truncated frontier tree", async () => {
      const original = structuredClone(
        fixture.trees.get(frontierTreeEntry.sha),
      );
      fixture.trees.get(frontierTreeEntry.sha).truncated = true;
      try {
        await assert.rejects(
          diagnostic.loadPreparedCheckpointTransaction(prepared),
          (error) => error.code === "CHECKPOINT_TREE_CAP",
        );
      } finally {
        fixture.trees.set(frontierTreeEntry.sha, original);
      }
      assertNoPublication();
    });

    await t.test("checkpoint commit parent", async () => {
      const original = structuredClone(checkpointCommit);
      fixture.commits.get(prepared.checkpoint_commit_sha).parents = [{
        sha: HEAD,
      }];
      try {
        await assert.rejects(
          diagnostic.loadPreparedCheckpointTransaction(prepared),
          (error) => error.code === "CHECKPOINT_COMMIT_BINDING",
        );
      } finally {
        fixture.commits.set(prepared.checkpoint_commit_sha, original);
      }
      assertNoPublication();
    });
  });

test("checkpoint diagnostic loader enforces its post-read deadline without publishing",
  async () => {
    const fixture = githubGitFixture();
    let delayNextBodyRead = false;
    const timedTransport = {
      fetch: async (...args) => {
        const responseValue = await fixture.fetch(...args);
        if (!delayNextBodyRead) return responseValue;
        const sourceReader = responseValue.body.getReader();
        return {
          status: responseValue.status,
          headers: responseValue.headers,
          body: {
            getReader() {
              return {
                async read() {
                  const result = await sourceReader.read();
                  if (delayNextBodyRead) {
                    delayNextBodyRead = false;
                    const deadline = performance.now() + 1_100;
                    while (performance.now() < deadline) {
                      // Exercise the synchronous post-read deadline fence.
                    }
                  }
                  return result;
                },
                cancel: (reason) => sourceReader.cancel(reason),
                releaseLock: () => sourceReader.releaseLock(),
              };
            },
          },
        };
      },
    };
    const capability = scheduleCapabilityReceipt();
    const triggerIdentity = scheduleTriggerIdentity();
    const ledger = makeLedger(
      timedTransport,
      capability,
      provenanceVerifier({
        eventName: "schedule",
        ref: triggerIdentity.ref,
        shaValue: triggerIdentity.sha,
      }),
      null,
      {
        httpLimits: {
          ...V2_GIT_LEDGER_HTTP_LIMITS,
          timeout_ms: 1_000,
        },
      },
    );
    await ledger.bootstrapCapability();
    const transportFixture = candidateTransportFixture(255);
    const cycle = await readIdentityDriftCandidateInventoryCycle({
      transportFixture,
      priorInventory: null,
      listedNumbersAfterInitial: Array.from(
        { length: 255 },
        (_, index) => index + 2,
      ),
      mutateAfterInitial: () => {
        transportFixture.setCandidateState(1, "closed");
      },
    });
    const required = await ledger.appendCandidateInventoryCycleIfRequired({
      workflow_command_handle: await createScheduleWorkflowCommandHandle(),
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      ...cycle,
    });
    const diagnostic = createV2GitLedgerCheckpointDiagnosticAdapter(
      ledger.productionApiForTest(),
    );
    const prepared = await diagnostic.prepareCheckpointCandidateTransaction({
      source_load_handle: required.source_load_handle,
      next_unit_admission_handle: required.next_unit_admission_handle,
    });
    const refBefore = fixture.refTarget;
    const patchCountBefore = fixture.calls.filter((call) =>
      call.method === "PATCH").length;
    delayNextBodyRead = true;
    await assert.rejects(
      diagnostic.loadPreparedCheckpointTransaction(prepared),
      (error) => error.code === "http-timeout",
    );
    assert.equal(fixture.refTarget, refBefore);
    assert.equal(fixture.calls.filter((call) =>
      call.method === "PATCH").length, patchCountBefore);
  });

test("checkpoint diagnostic rejects server-time regressions before object writes",
  async (t) => {
    const cases = [{
      label: "carrier before source",
      regressedRefRead: 3,
    }, {
      label: "first suffix before carrier",
      regressedRefRead: 4,
    }];
    for (const { label, regressedRefRead } of cases) {
      await t.test(label, async () => {
        const fixture = githubGitFixture();
        let enabled = false;
        let refReads = 0;
        const fetch = async (url, init) => {
          const responseValue = await fixture.fetch(url, init);
          const path = new URL(url).pathname;
          if (
            enabled && init.method === "GET" &&
            path.endsWith("/git/ref/heads/codex-review-gate-ledger-v2")
          ) {
            refReads += 1;
            if (refReads === regressedRefRead) {
              const bytes = await responseValue.arrayBuffer();
              const headers = new Headers(responseValue.headers);
              headers.set("Date", new Date("2020-01-01T00:00:00Z")
                .toUTCString());
              return new Response(bytes, {
                status: responseValue.status,
                headers,
              });
            }
          }
          return responseValue;
        };
        const context = await bootstrapCheckpointAdmissionFixture({
          fixture,
          fetch,
        });
        const before = {
          blobs: fixture.blobs.size,
          trees: fixture.trees.size,
          commits: fixture.commits.size,
          writes: fixture.writeCalls,
          patches: fixture.calls.filter((call) =>
            call.method === "PATCH").length,
          ref: fixture.refTarget,
        };
        enabled = true;
        await assert.rejects(
          context.diagnostic.prepareCheckpointCandidateTransaction({
            source_load_handle: context.required.source_load_handle,
            next_unit_admission_handle:
              context.required.next_unit_admission_handle,
          }),
          (error) => error.code === "CHECKPOINT_TIME_REGRESSION",
        );
        assert.deepEqual({
          blobs: fixture.blobs.size,
          trees: fixture.trees.size,
          commits: fixture.commits.size,
          writes: fixture.writeCalls,
          patches: fixture.calls.filter((call) =>
            call.method === "PATCH").length,
          ref: fixture.refTarget,
        }, before);
      });
    }
  });

test("checkpoint diagnostic revokes an in-flight stale admission before object writes",
  async () => {
    const fixture = githubGitFixture();
    let blockNextRefRead = false;
    let releaseBlockedRead;
    let notifyBlockedRead;
    const blockedRead = new Promise((resolve) => {
      notifyBlockedRead = resolve;
    });
    const releasedRead = new Promise((resolve) => {
      releaseBlockedRead = resolve;
    });
    const fetch = async (url, init) => {
      const path = new URL(url).pathname;
      if (
        blockNextRefRead && init.method === "GET" &&
        path.endsWith("/git/ref/heads/codex-review-gate-ledger-v2")
      ) {
        blockNextRefRead = false;
        notifyBlockedRead();
        await releasedRead;
      }
      return fixture.fetch(url, init);
    };
    const context = await bootstrapCheckpointAdmissionFixture({
      fixture,
      fetch,
    });
    const before = {
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      writes: fixture.writeCalls,
      patches: fixture.calls.filter((call) =>
        call.method === "PATCH").length,
      ref: fixture.refTarget,
    };
    blockNextRefRead = true;
    const stalePreparation = context.diagnostic
      .prepareCheckpointCandidateTransaction({
        source_load_handle: context.required.source_load_handle,
        next_unit_admission_handle:
          context.required.next_unit_admission_handle,
      });
    await blockedRead;
    const replacement = await context.ledger
      .appendCandidateInventoryCycleIfRequired({
        workflow_command_handle: await createScheduleWorkflowCommandHandle(),
        trigger_identity: context.triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        ...context.cycle,
      });
    assert.notStrictEqual(
      replacement.next_unit_admission_handle,
      context.required.next_unit_admission_handle,
    );
    releaseBlockedRead();
    await assert.rejects(
      stalePreparation,
      (error) =>
        error.code === "STALE_CHECKPOINT_NEXT_UNIT_ADMISSION_HANDLE",
    );
    assert.deepEqual({
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      writes: fixture.writeCalls,
      patches: fixture.calls.filter((call) =>
        call.method === "PATCH").length,
      ref: fixture.refTarget,
    }, before);
  });

test("checkpoint diagnostic stops stale suffix planning before another external call",
  async () => {
    const fixture = githubGitFixture();
    let blockNextSuffixRef = false;
    let releaseBlockedRead;
    let notifyBlockedRead;
    const blockedRead = new Promise((resolve) => {
      notifyBlockedRead = resolve;
    });
    const releasedRead = new Promise((resolve) => {
      releaseBlockedRead = resolve;
    });
    const fetch = async (url, init) => {
      const responseValue = await fixture.fetch(url, init);
      const path = new URL(url).pathname;
      if (
        blockNextSuffixRef && init.method === "GET" &&
        path.endsWith("/git/ref/heads/codex-review-gate-ledger-v2")
      ) {
        blockNextSuffixRef = false;
        notifyBlockedRead();
        await releasedRead;
      }
      return responseValue;
    };
    const baseVerifier = provenanceVerifier({
      eventName: "schedule",
      ref: scheduleTriggerIdentity().ref,
      shaValue: scheduleTriggerIdentity().sha,
    });
    const verifierCalls = [];
    const verifier = async (request) => {
      verifierCalls.push({
        mode: request.mode,
        operation: request.provenance_request.operation,
      });
      const result = await baseVerifier(request);
      if (
        request.mode === "mint-and-verify" &&
        request.provenance_request.operation === "checkpoint-rotate"
      ) {
        blockNextSuffixRef = true;
      }
      return result;
    };
    const context = await bootstrapCheckpointAdmissionFixture({
      fixture,
      fetch,
      verifier,
    });
    const before = {
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      writes: fixture.writeCalls,
      patches: fixture.calls.filter((call) =>
        call.method === "PATCH").length,
      ref: fixture.refTarget,
    };
    const stalePreparation = context.diagnostic
      .prepareCheckpointCandidateTransaction({
        source_load_handle: context.required.source_load_handle,
        next_unit_admission_handle:
          context.required.next_unit_admission_handle,
      });
    await blockedRead;
    const replacement = await context.ledger
      .appendCandidateInventoryCycleIfRequired({
        workflow_command_handle: await createScheduleWorkflowCommandHandle(),
        trigger_identity: context.triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        ...context.cycle,
      });
    assert.notStrictEqual(
      replacement.next_unit_admission_handle,
      context.required.next_unit_admission_handle,
    );
    const callsAfterReplacement = fixture.calls.length;
    const verifierCallsAfterReplacement = verifierCalls.length;
    releaseBlockedRead();
    await assert.rejects(
      stalePreparation,
      (error) =>
        error.code === "STALE_CHECKPOINT_NEXT_UNIT_ADMISSION_HANDLE",
    );
    assert.equal(fixture.calls.length, callsAfterReplacement);
    assert.equal(verifierCalls.length, verifierCallsAfterReplacement);
    assert.deepEqual({
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      writes: fixture.writeCalls,
      patches: fixture.calls.filter((call) =>
        call.method === "PATCH").length,
      ref: fixture.refTarget,
    }, before);
  });

test("checkpoint diagnostic loader stops after its prepared result becomes stale",
  async () => {
    const fixture = githubGitFixture();
    let blockedTreeSha = null;
    let releaseBlockedRead;
    let notifyBlockedRead;
    const blockedRead = new Promise((resolve) => {
      notifyBlockedRead = resolve;
    });
    const releasedRead = new Promise((resolve) => {
      releaseBlockedRead = resolve;
    });
    const baseVerifier = provenanceVerifier({
      eventName: "schedule",
      ref: scheduleTriggerIdentity().ref,
      shaValue: scheduleTriggerIdentity().sha,
    });
    const verifierCalls = [];
    const verifier = async (request) => {
      verifierCalls.push({
        mode: request.mode,
        operation: request.provenance_request.operation,
      });
      return baseVerifier(request);
    };
    const fetch = async (url, init) => {
      const responseValue = await fixture.fetch(url, init);
      const path = new URL(url).pathname;
      if (
        blockedTreeSha !== null && init.method === "GET" &&
        path.endsWith(`/git/trees/${blockedTreeSha}`)
      ) {
        blockedTreeSha = null;
        notifyBlockedRead();
        await releasedRead;
      }
      return responseValue;
    };
    const context = await bootstrapCheckpointAdmissionFixture({
      fixture,
      fetch,
      verifier,
    });
    const prepared = await context.diagnostic
      .prepareCheckpointCandidateTransaction({
        source_load_handle: context.required.source_load_handle,
        next_unit_admission_handle:
          context.required.next_unit_admission_handle,
      });
    const checkpointCommit = fixture.commits.get(
      prepared.checkpoint_commit_sha,
    );
    blockedTreeSha = checkpointCommit.tree.sha;
    const before = {
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      writes: fixture.writeCalls,
      patches: fixture.calls.filter((call) =>
        call.method === "PATCH").length,
      ref: fixture.refTarget,
    };
    const staleLoad = context.diagnostic
      .loadPreparedCheckpointTransaction(prepared);
    await blockedRead;
    const replacement = await context.ledger
      .appendCandidateInventoryCycleIfRequired({
        workflow_command_handle: await createScheduleWorkflowCommandHandle(),
        trigger_identity: context.triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        ...context.cycle,
      });
    assert.notStrictEqual(
      replacement.next_unit_admission_handle,
      context.required.next_unit_admission_handle,
    );
    const callsAfterReplacement = fixture.calls.length;
    const verifierCallsAfterReplacement = verifierCalls.length;
    releaseBlockedRead();
    await assert.rejects(
      staleLoad,
      (error) =>
        error.code === "STALE_CHECKPOINT_NEXT_UNIT_ADMISSION_HANDLE",
    );
    assert.equal(fixture.calls.length, callsAfterReplacement);
    assert.equal(verifierCalls.length, verifierCallsAfterReplacement);
    assert.deepEqual({
      blobs: fixture.blobs.size,
      trees: fixture.trees.size,
      commits: fixture.commits.size,
      writes: fixture.writeCalls,
      patches: fixture.calls.filter((call) =>
        call.method === "PATCH").length,
      ref: fixture.refTarget,
    }, before);
  });

test("compact candidate inventory and durable dispatch retain exact record capacity", () => {
  const matureInventory =
    calculateV2GitLedgerCandidateInventoryCycleCommitBudget({
      shard_count: 100,
      candidate_count: 0,
      reachable_record_count: 3,
    });
  assert.equal(matureInventory.inventory_commit_budget_required, 3);
  assert.equal(matureInventory.dispatch_commit_budget_required, 1);
  assert.equal(matureInventory.total_commit_budget_required, 4);
  const retainedDriftInventory =
    calculateV2GitLedgerCandidateInventoryCycleCommitBudget({
      shard_count: 100,
      drift_shard_counts: [0, 1, 100],
      candidate_count: 0,
      reachable_record_count: 3,
    });
  assert.equal(
    retainedDriftInventory.inventory_commit_budget_required,
    6,
  );
  assert.equal(retainedDriftInventory.total_commit_budget_required, 7);

  const exactFit = calculateV2GitLedgerCandidateDispatchCommitBudget({
    candidate_count: 1,
    batch_count: 1,
    reachable_record_count: MAX_V2_GIT_LEDGER_COMMITS - 19,
  });
  assert.equal(exactFit.total_commit_budget_required, 19);
  assert.equal(exactFit.remaining_ledger_commit_capacity_after_dispatch, 0);
  for (let consumed = 1; consumed <= 19; consumed += 1) {
    const durableCrashPrefix =
      calculateV2GitLedgerCandidateDispatchReservedCommitBudget({
        ...exactFit,
        reachable_record_count:
          MAX_V2_GIT_LEDGER_COMMITS - 19 + consumed,
      });
    assert.equal(
      durableCrashPrefix.consumed_reserved_record_count,
      consumed,
    );
    assert.equal(
      durableCrashPrefix.reserved_record_ceiling,
      MAX_V2_GIT_LEDGER_COMMITS,
    );
    assert.equal(
      durableCrashPrefix.remaining_ledger_commit_capacity_after_dispatch,
      0,
      "every reachable crash prefix consumes its original reservation once",
    );
  }
  assert.throws(
    () => calculateV2GitLedgerCandidateDispatchReservedCommitBudget({
      ...exactFit,
      reachable_record_count: MAX_V2_GIT_LEDGER_COMMITS + 1,
    }),
    (error) => error.code === "candidate-dispatch-commit-capacity",
  );
});

test("candidate dispatch raw gate and replay reject request-binding stages that skip scheduler authority",
  async () => {
    const attempt = await createScheduledCandidateDispatchAttempt();
    const loaded = await attempt.ledger.load();
    const lease = attempt.discovery.lease_receipt;
    const scope = attempt.discovery.effect_evaluated_scope_receipt.scope;
    const generation = automaticGeneration(1);
    const action = {
      generation_id: generation.generation_id,
      request_id: "71",
      reservation_record_oid: "a".repeat(40),
      attempt_record_oid: "b".repeat(40),
    };
    const skippedIntent = createV2GitLedgerEffectIntentRecord({
      predecessor_commit_sha: loaded.tip_commit_sha,
      scope,
      kind: "request-binding",
      effect_id: "forged-stage-14",
      idempotency_key: "forged-stage-14",
      server_observed_at: loaded.post_ref.server_time,
      generation,
      action,
      control_comment_binding: loaded.control_comment_binding,
      lease_receipt: lease,
    });
    const skippedResponse = createV2GitLedgerRecord({
      record_type: "effect-response",
      predecessor_commit_sha: loaded.tip_commit_sha,
      pull_request: scope.pull_request,
      head_ref_oid: scope.head_ref_oid,
      base_ref_oid: scope.base_ref_oid,
      potential_merge_commit_oid: scope.potential_merge_commit_oid,
      kind: "request-binding",
      effect_id: "forged-stage-15",
      idempotency_key: "forged-stage-15",
      server_observed_at: loaded.post_ref.server_time,
      payload: effectPayload({
        phase: "response",
        kind: "request-binding",
        predecessor: loaded.tip_commit_sha,
        generation,
        intentCommitSha: "c".repeat(40),
        action,
        receipt: {
          request_id: "71",
          request_node_id: "IC_71",
          request_url:
            "https://github.com/owner/repo/pull/7#issuecomment-71",
          body_sha256: rawDigest("@codex review"),
          created_at: loaded.post_ref.server_time,
          updated_at: loaded.post_ref.server_time,
          raw_body_sha256: `sha256:${"d".repeat(64)}`,
          actor: ACTOR,
          controlled: true,
        },
      }),
      control_comment_binding: loaded.control_comment_binding,
      lease: leaseBinding(lease),
    });
    for (const record of [skippedIntent, skippedResponse]) {
      const writesBefore = attempt.fixture.writeCalls;
      const tipBefore = attempt.fixture.refTarget;
      await assert.rejects(
        attempt.ledger.appendRecord(record, {
          evaluated_scope_receipt:
            attempt.discovery.effect_evaluated_scope_receipt,
        }),
        (error) => error.code === "UNTRUSTED_RAW_EFFECT_APPEND",
      );
      assert.equal(attempt.fixture.writeCalls, writesBefore);
      assert.equal(attempt.fixture.refTarget, tipBefore);
    }
    const historicalTip = attempt.fixture.refTarget;
    await assert.rejects(
      materializeHistoricalProductionRecord({
        ledger: attempt.ledger,
        loaded,
        record: skippedIntent,
        evaluatedScopeReceipt:
          attempt.discovery.effect_evaluated_scope_receipt,
      }),
      (error) => error.code === "candidate-dispatch-attempt-order",
    );
    assert.equal(attempt.fixture.refTarget, historicalTip);
  });

test("candidate public wait stays pending on the same reservation after due_at without dispatch or ACK", async () => {
  const attempt = await createScheduledCandidateDispatchAttempt({
    visibility: "public",
  });
  const fullScopeReceipt = attempt.discovery.effect_evaluated_scope_receipt;
  const controlPlaneAuthority = await attempt.ledger.loadControlPlaneAuthority(
    fullScopeReceipt.scope,
  );
  const initialAuthority = await attempt.ledger.loadInitialRunnerStateAuthority({
    control_plane_authority: controlPlaneAuthority,
    evaluated_scope_receipt: fullScopeReceipt,
    workflow_command_handle: attempt.scheduledCommand,
  });
  const controlPlaneReceipt =
    createV2ControlPlaneReceiptFromGitLedgerAuthority(controlPlaneAuthority);
  const runnerAuthority = createV2ProductionRunnerAuthority({
    preflight_handle: attempt.preflight,
    control_plane_receipt: controlPlaneReceipt,
    initial_runner_state_authority: initialAuthority,
    expected_scope: initialAuthority.scope,
  });
  const plan = schedulerObservationRecord({
    predecessor: attempt.discovery.lease_receipt.acquire_commit_sha,
    lease: attempt.discovery.lease_receipt,
    priorAuthorityDigest: initialAuthority.prior_authority_digest,
    initialAuthority,
  }).payload.action;
  assert.deepEqual(plan.scheduler_plan.actions, []);
  assert.deepEqual(plan.scheduler_plan.required_wait, {
    stage: "public-initial",
    deadline: plan.scheduler_plan.due_at,
  });
  const schedulerAppend = await attempt.ledger
    .appendInitialSchedulerObservation({
      initial_runner_state_authority: initialAuthority,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: plan.status_plan,
    });
  const releaseReceipt = await releaseFixtureDiscoveryLease(
    attempt.ledger,
    attempt.discovery,
  );
  const terminalResult = candidateDispatchTerminalResult({
    decision: plan.scheduler_evaluation.decision,
    schedulerAppend,
    leaseReleaseReceipt: releaseReceipt,
    preflight: attempt.preflight,
    continuityAuthority: attempt.discovery.continuity_authority,
    controlPlaneReceipt,
    initialAuthority,
    runnerAuthority,
  });
  terminalResult.due_at = plan.scheduler_plan.due_at;
  terminalResult.wakeup_hints = "";
  const writesBeforeTerminal = attempt.fixture.writeCalls;
  await assert.rejects(
    attempt.ledger.createCandidateDispatchResultAuthority({
      candidate_dispatch_handle: attempt.scheduled.candidate_dispatch_handle,
      scheduler_append: schedulerAppend,
      production_runner_authority: runnerAuthority,
      lease_release_receipt: releaseReceipt,
      terminal_result: terminalResult,
    }),
    (error) => error.code === "candidate-dispatch-wait-outstanding",
  );
  assert.equal(attempt.fixture.writeCalls, writesBeforeTerminal);

  attempt.fixture.advanceServerTime(3600);
  attempt.runIdentity.run_id = "9003";
  attempt.runIdentity.run_attempt = 1;
  const resumeCommand = await createScheduleWorkflowCommandHandle({
    runIdentity: attempt.runIdentity,
  });
  const writesBeforeResume = attempt.fixture.writeCalls;
  const resumed = await attempt.ledger.loadOrReserveCandidateDispatch({
    workflow_command_handle: resumeCommand,
    trigger_identity: attempt.triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });
  assert.equal(resumed.state, "wait-pending");
  assert.equal(resumed.restarted, true);
  assert.equal(resumed.candidate_dispatch_handle, null);
  assert.equal(resumed.reservation_receipt, null);
  assert.equal(resumed.plan, null);
  assert.equal(resumed.recovery_required, null);
  assert.equal(attempt.fixture.writeCalls, writesBeforeResume);
  const final = await attempt.ledger.load();
  const current = final.authority_projection.candidate_dispatch.current_cycle;
  assert.notEqual(current.active_reservation, null);
  assert.equal(current.completed_batches.length, 0);
  assert.equal(current.cycle_complete, false);
});

test("candidate dispatch binds one released scheduled terminal result before ack", async () => {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt({
    allowedEventNames: ["schedule", "pull_request_target"],
  });
  const triggerIdentity = scheduleTriggerIdentity();
  const runIdentity = { ...OWNER };
  const verifier = provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
    runIdentity,
  });
  const preflight = await loadFixtureProviderPreflight();
  const ledger = makeLedger(fixture, capability, verifier, preflight);
  await ledger.bootstrapCapability();
  const candidateFixture = candidateTransportFixture(0, {
    exactScopeCandidateNumber: PR.number,
  });
  candidateFixture.setListedNumbers([PR.number]);
  await appendCompletedCandidateInventory({
    ledger,
    transportFixture: candidateFixture,
  });
  const scanCommand = await createScheduleWorkflowCommandHandle({ runIdentity });
  const reserved = await ledger.loadOrReserveCandidateDispatch({
    workflow_command_handle: scanCommand,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });
  const unrelatedMinimal = await loadFixtureMinimalScope();
  const unrelatedPreScope = await ledger
    .createPullRequestEventEvaluatedScopeReceipt({
      minimal_scope_handle: unrelatedMinimal,
      trigger_identity: {
        event_name: "pull_request_target",
        ref: "refs/heads/main",
        sha: BASE,
      },
    });
  const beforeUnrelatedAttempt = await ledger.load();
  const writesBeforeUnrelatedAttempt = fixture.writeCalls;
  await assert.rejects(
    ledger.acquireLease({
      ...leaseInput(beforeUnrelatedAttempt.tip_commit_sha),
      lease_id: "lease-unrelated-dispatch-attempt",
    }, {
      evaluated_scope_receipt: unrelatedPreScope,
    }),
    (error) => error.code === "candidate-dispatch-write-window",
  );
  assert.equal(fixture.writeCalls, writesBeforeUnrelatedAttempt);
  assert.equal(
    (await ledger.load()).tip_commit_sha,
    beforeUnrelatedAttempt.tip_commit_sha,
  );
  runIdentity.run_id = "9002";
  runIdentity.run_attempt = 2;
  const scheduledCommand = await createScheduleWorkflowCommandHandle({
    pullRequestNumber: PR.number,
    dispatchBinding: reserved.plan.items[0],
    runIdentity,
  });
  const minimal = await loadFixtureMinimalScope();
  const writesBeforeExpectedCandidateForgery = fixture.writeCalls;
  await assert.rejects(
    ledger.loadCandidateDispatchForScheduledPullRequest({
      workflow_command_handle: scheduledCommand,
      minimal_scope_handle: minimal,
      trigger_identity: triggerIdentity,
      expected_dispatch_binding: {
        ...reserved.plan.items[0],
        candidate: {
          ...reserved.plan.items[0].candidate,
          id: "999999",
        },
      },
    }),
    (error) => error.code === "candidate-dispatch-workflow-command-mismatch",
  );
  assert.equal(fixture.writeCalls, writesBeforeExpectedCandidateForgery);
  const scheduled = await ledger.loadCandidateDispatchForScheduledPullRequest({
    workflow_command_handle: scheduledCommand,
    minimal_scope_handle: minimal,
    trigger_identity: triggerIdentity,
    expected_dispatch_binding: reserved.plan.items[0],
  });
  const staleScheduled =
    await ledger.loadCandidateDispatchForScheduledPullRequest({
      workflow_command_handle: scheduledCommand,
      minimal_scope_handle: minimal,
      trigger_identity: triggerIdentity,
      expected_dispatch_binding: reserved.plan.items[0],
    });
  assertV2GitLedgerCandidateDispatchHandle(
    scheduled.candidate_dispatch_handle,
    { purpose: "scheduled-pull-request" },
  );
  const preScopeReceipt = await ledger
    .loadScheduledPullRequestEvaluatedScopeReceipt({
      candidate_dispatch_handle: scheduled.candidate_dispatch_handle,
      minimal_scope_handle: minimal,
      trigger_identity: triggerIdentity,
    });
  const stalePreScopeReceipt = await ledger
    .loadScheduledPullRequestEvaluatedScopeReceipt({
      candidate_dispatch_handle: staleScheduled.candidate_dispatch_handle,
      minimal_scope_handle: minimal,
      trigger_identity: triggerIdentity,
    });
  const discovery = await acquireFixtureLeasedDiscovery({
    ledger,
    command: scheduledCommand,
    preScopeReceipt,
    minimalPre: minimal,
    leaseTtlSeconds: 900,
  });
  const fullScopeReceipt = discovery.effect_evaluated_scope_receipt;
  const controlPlaneAuthority = await ledger.loadControlPlaneAuthority(
    fullScopeReceipt.scope,
  );
  const initialAuthority = await ledger.loadInitialRunnerStateAuthority({
    control_plane_authority: controlPlaneAuthority,
    evaluated_scope_receipt: fullScopeReceipt,
    workflow_command_handle: scheduledCommand,
  });
  const controlPlaneReceipt =
    createV2ControlPlaneReceiptFromGitLedgerAuthority(controlPlaneAuthority);
  const runnerAuthority = createV2ProductionRunnerAuthority({
    preflight_handle: preflight,
    control_plane_receipt: controlPlaneReceipt,
    initial_runner_state_authority: initialAuthority,
    expected_scope: initialAuthority.scope,
  });
  const plan = schedulerObservationRecord({
    predecessor: discovery.lease_receipt.acquire_commit_sha,
    lease: discovery.lease_receipt,
    priorAuthorityDigest: initialAuthority.prior_authority_digest,
    initialAuthority,
  }).payload.action;
  const schedulerAppend = await ledger.appendInitialSchedulerObservation({
    initial_runner_state_authority: initialAuthority,
    scheduler_evaluation: plan.scheduler_evaluation,
    status_plan: plan.status_plan,
  });
  const statusIntent = await ledger.appendStatusWriteIntent({
    scheduler_append: schedulerAppend,
    status_write_index: 0,
  });
  const statusResponse = await ledger.appendStatusWriteResponse({
    status_intent_handle: statusIntent.status_intent_handle,
    intent_append_receipt: statusIntent.intent_append_receipt,
    receipt: statusWriteResponseReceipt(
      statusIntent.transport,
      statusIntent.intent_append_receipt.ref_reread.server_time,
    ),
  });
  const releaseReceipt = await releaseFixtureDiscoveryLease(ledger, discovery);
  const beforeReplayedAttempt = await ledger.load();
  const writesBeforeReplayedAttempt = fixture.writeCalls;
  await assert.rejects(
    acquireFixtureLeasedDiscovery({
      ledger,
      command: scheduledCommand,
      preScopeReceipt: stalePreScopeReceipt,
      minimalPre: minimal,
      leaseTtlSeconds: 900,
    }),
    (error) => error.code === "candidate-dispatch-attempt-replayed",
  );
  assert.equal(fixture.writeCalls, writesBeforeReplayedAttempt);
  assert.equal(
    (await ledger.load()).tip_commit_sha,
    beforeReplayedAttempt.tip_commit_sha,
  );
  const originalAcquire = fixtureEnvelopeAt(
    fixture,
    discovery.lease_receipt.acquire_commit_sha,
  );
  const duplicateAcquiredAt = beforeReplayedAttempt.post_ref.server_time;
  const duplicateAcquire = {
    record_type: originalAcquire.record_type,
    predecessor_commit_sha: beforeReplayedAttempt.tip_commit_sha,
    pull_request: structuredClone(originalAcquire.pull_request),
    head_ref_oid: originalAcquire.head_ref_oid,
    base_ref_oid: originalAcquire.base_ref_oid,
    potential_merge_commit_oid: originalAcquire.potential_merge_commit_oid,
    kind: originalAcquire.kind,
    effect_id: originalAcquire.effect_id,
    idempotency_key: originalAcquire.idempotency_key,
    server_observed_at: duplicateAcquiredAt,
    payload: {
      ...structuredClone(originalAcquire.payload),
      acquired_at: duplicateAcquiredAt,
      expires_at: new Date(
        Date.parse(duplicateAcquiredAt) +
          originalAcquire.payload.lease_ttl_seconds * 1_000,
      ).toISOString(),
    },
    control_comment_binding:
      structuredClone(originalAcquire.control_comment_binding),
    lease: null,
  };
  const historicalTip = fixture.refTarget;
  await assert.rejects(
    materializeHistoricalProductionRecord({
      ledger,
      loaded: beforeReplayedAttempt,
      record: duplicateAcquire,
      evaluatedScopeReceipt: stalePreScopeReceipt,
    }),
    (error) => error.code === "candidate-dispatch-attempt-replayed",
  );
  assert.equal(fixture.refTarget, historicalTip);
  const terminalResult = candidateDispatchTerminalResult({
    decision: plan.scheduler_evaluation.decision,
    schedulerAppend,
    leaseReleaseReceipt: releaseReceipt,
    preflight,
    continuityAuthority: discovery.continuity_authority,
    controlPlaneReceipt,
    initialAuthority,
    runnerAuthority,
    statusIntent,
    statusResponse,
  });
  const resultAuthority = await ledger.createCandidateDispatchResultAuthority({
    candidate_dispatch_handle: scheduled.candidate_dispatch_handle,
    scheduler_append: schedulerAppend,
    production_runner_authority: runnerAuthority,
    lease_release_receipt: releaseReceipt,
    terminal_result: terminalResult,
  });
  assertV2GitLedgerCandidateDispatchResultHandle(
    resultAuthority.candidate_dispatch_result_handle,
  );
  assert.throws(
    () => assertV2GitLedgerCandidateDispatchResultHandle(
      structuredClone(resultAuthority.candidate_dispatch_result_handle),
    ),
    (error) => error.code === "UNTRUSTED_CANDIDATE_DISPATCH_RESULT_HANDLE",
  );
  const ack = await ledger.ackCandidateDispatch({
    candidate_dispatch_handle: scheduled.candidate_dispatch_handle,
    reservation_receipt: scheduled.reservation_receipt,
    full_scope_receipt: fullScopeReceipt,
    candidate_dispatch_result_handle:
      resultAuthority.candidate_dispatch_result_handle,
  });
  assert.equal(
    resultAuthority.result.decision,
    plan.scheduler_evaluation.decision,
  );
  assert.equal(resultAuthority.result.public_effects_performed, 1);
  assert.equal(ack.result_digest, resultAuthority.result.result_digest);
  assert.notEqual(ack.ack_append_receipt, null);
  assert.notEqual(ack.batch_completion_append_receipt, null);
  assert.notEqual(ack.cycle_completion_append_receipt, null);
  assert.equal(ack.remaining_plan, null);
  const final = await ledger.load();
  assert.equal(final.active_lease, null);
  assert.equal(
    final.authority_projection.candidate_dispatch.current_cycle.cycle_complete,
    true,
  );
  assert.deepEqual(
    deriveV2GitLedgerCandidateDispatchAuthority(final.records, REPOSITORY),
    final.authority_projection.candidate_dispatch,
  );
  const writesBeforeReplay = fixture.writeCalls;
  await assert.rejects(
    ledger.ackCandidateDispatch({
      candidate_dispatch_handle: scheduled.candidate_dispatch_handle,
      reservation_receipt: scheduled.reservation_receipt,
      full_scope_receipt: fullScopeReceipt,
      candidate_dispatch_result_handle:
        resultAuthority.candidate_dispatch_result_handle,
    }),
    (error) => error.code === "candidate-dispatch-handle-replayed",
  );
  assert.equal(fixture.writeCalls, writesBeforeReplay);

  runIdentity.run_id = OWNER.run_id;
  runIdentity.run_attempt = OWNER.run_attempt;
  const priorInventory = (await ledger.load()).authority_projection
    .candidate_inventory.completed_cycle.final_inventory;
  candidateFixture.setListedNumbers([PR.number, PR.number + 1]);
  await appendCompletedCandidateInventory({
    ledger,
    transportFixture: candidateFixture,
    priorInventory,
  });
  runIdentity.run_id = "9003";
  runIdentity.run_attempt = 1;
  const nextScanCommand = await createScheduleWorkflowCommandHandle({
    runIdentity,
  });
  const nextReservation = await ledger.loadOrReserveCandidateDispatch({
    workflow_command_handle: nextScanCommand,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });
  assert.deepEqual(
    nextReservation.plan.items[0].candidate,
    reserved.plan.items[0].candidate,
  );
  assert.notEqual(
    nextReservation.plan.items[0].generation_id,
    reserved.plan.items[0].generation_id,
  );
  runIdentity.run_id = "9004";
  const nextScheduledCommand = await createScheduleWorkflowCommandHandle({
    pullRequestNumber: PR.number,
    dispatchBinding: nextReservation.plan.items[0],
    runIdentity,
  });
  const nextMinimal = await loadFixtureMinimalScope();
  const writesBeforeStaleMatrix = fixture.writeCalls;
  await assert.rejects(
    ledger.loadCandidateDispatchForScheduledPullRequest({
      workflow_command_handle: nextScheduledCommand,
      minimal_scope_handle: nextMinimal,
      trigger_identity: triggerIdentity,
      expected_dispatch_binding: reserved.plan.items[0],
    }),
    (error) => error.code === "candidate-dispatch-workflow-command-mismatch",
  );
  assert.equal(fixture.writeCalls, writesBeforeStaleMatrix);
  const currentScheduled =
    await ledger.loadCandidateDispatchForScheduledPullRequest({
      workflow_command_handle: nextScheduledCommand,
      minimal_scope_handle: nextMinimal,
      trigger_identity: triggerIdentity,
      expected_dispatch_binding: nextReservation.plan.items[0],
    });
  assertV2GitLedgerCandidateDispatchHandle(
    currentScheduled.candidate_dispatch_handle,
    { purpose: "scheduled-pull-request" },
  );
});

for (const recoveryKind of ["artifact-binding", "scheduler-state"]) {
  const title = recoveryKind === "artifact-binding"
    ? "candidate dispatch closes two status writes with aggregate artifact binding before ack"
    : "candidate dispatch reuses one bound two-status transaction before scheduler recovery ack";
  test(title,
    async () => {
      const attempt = await createScheduledAutomaticRecoveryBranchAttempt({
        bindArtifactBeforeDispatch: recoveryKind === "scheduler-state",
      });
      const { recovery } = attempt;
      let recoveryIntent = null;
      let recoveryResponse = null;
      if (recoveryKind === "artifact-binding") {
        assert.equal(recovery.runnerResult.status_plan.writes.length, 2);
        assert.equal(recovery.statusIntents.length, 2);
        assert.equal(recovery.statusResponses.length, 2);
        recoveryIntent = await attempt.ledger
          .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
            scheduler_append: recovery.schedulerAppend,
            artifact_binding_candidate_handle:
              recovery.artifactBindingCandidateHandle,
          });
        const providerArtifactHandles =
          await loadAutomaticRecoveryProviderArtifactHandles({
            ledger: attempt.ledger,
            fixture: attempt.fixture,
            transport: recoveryIntent.transport,
            snapshots: recovery.snapshots,
          });
        recoveryResponse = await attempt.ledger
          .appendAutomaticRecoveryArtifactBindingResponse({
            artifact_binding_intent_handle:
              recoveryIntent.artifact_binding_intent_handle,
            intent_append_receipt: recoveryIntent.intent_append_receipt,
            provider_artifact_handles: providerArtifactHandles,
          });
        assert.equal(
          recoveryResponse.bindings.length,
          recoveryIntent.transport.expected_count,
        );
      } else {
        assert.equal(attempt.priorObservation.statusIntents.length, 2);
        assert.equal(attempt.priorObservation.statusResponses.length, 2);
        assert.equal(recovery.runnerResult.status_plan.writes.length, 2);
        assert.deepEqual(recovery.statusIntents, []);
        assert.deepEqual(recovery.statusResponses, []);
        const statusIntentsBefore = (await attempt.ledger.load()).records
          .filter((entry) => entry.envelope.record_type === "effect-intent" &&
            entry.envelope.kind === "status-write").length;
        const transition = await attempt.ledger
          .appendAutomaticRequestRecoveryTransition({
            scheduler_append: recovery.schedulerAppend,
            recovery_handle: recovery.recoveryHandle,
          });
        assert.equal(transition.prior_generation_id, "automatic:1");
        assert.equal(transition.next_generation_id, "automatic:2");
        assert.equal((await attempt.ledger.load()).records.filter((entry) =>
          entry.envelope.record_type === "effect-intent" &&
          entry.envelope.kind === "status-write").length, statusIntentsBefore);
      }
      const releaseReceipt = await releaseFixtureDiscoveryLease(
        attempt.ledger,
        attempt.discovery,
      );
      const controlPlaneReceipt =
        createV2ControlPlaneReceiptFromGitLedgerAuthority(
          recovery.controlPlaneAuthority,
        );
      const terminalResult = candidateDispatchTerminalResult({
        decision: recovery.runnerResult.scheduler_evaluation.decision,
        schedulerAppend: recovery.schedulerAppend,
        leaseReleaseReceipt: releaseReceipt,
        preflight: attempt.preflight,
        continuityAuthority: attempt.discovery.continuity_authority,
        controlPlaneReceipt,
        initialAuthority: null,
        establishedAuthority: recovery.establishedAuthority,
        runnerAuthority: recovery.productionAuthority,
        statusIntents: recovery.statusIntents,
        statusResponses: recovery.statusResponses,
        statusAlreadyBound: recoveryKind === "scheduler-state",
      });
      const resultAuthority = await attempt.ledger
        .createCandidateDispatchResultAuthority({
          candidate_dispatch_handle:
            attempt.scheduled.candidate_dispatch_handle,
          scheduler_append: recovery.schedulerAppend,
          production_runner_authority: recovery.productionAuthority,
          lease_release_receipt: releaseReceipt,
          terminal_result: terminalResult,
        });
      assert.equal(
        resultAuthority.result.public_effects_performed,
        recoveryKind === "artifact-binding" ? 2 : 0,
      );
      const ack = await attempt.ledger.ackCandidateDispatch({
        candidate_dispatch_handle:
          attempt.scheduled.candidate_dispatch_handle,
        reservation_receipt: attempt.scheduled.reservation_receipt,
        full_scope_receipt:
          attempt.discovery.effect_evaluated_scope_receipt,
        candidate_dispatch_result_handle:
          resultAuthority.candidate_dispatch_result_handle,
      });
      assert.equal(ack.result_digest, resultAuthority.result.result_digest);
      assert.notEqual(ack.ack_append_receipt, null);
      assert.equal((await attempt.ledger.load()).active_lease, null);
  });
}

for (const [priorPhase, expectedCurrentStages] of [
  [null, ["artifact-binding:intent", "artifact-binding-ready:intent",
    "artifact-binding-ready:response", "artifact-binding:response"]],
  ["intent", ["artifact-binding-ready:intent",
    "artifact-binding-ready:response", "artifact-binding:response"]],
  ["ready-intent", ["artifact-binding-ready:response",
    "artifact-binding:response"]],
  ["ready-confirmation", ["artifact-binding:response"]],
]) {
  test(`candidate dispatch closes the ${priorPhase ?? "empty"} ordinary aggregate prefix under the current scheduled lease`,
    async () => {
    const attempt = await createScheduledAutomaticRecoveryBranchAttempt({
      bindArtifactBeforeDispatch: false,
      durableArtifactPrefixPhaseBeforeDispatch: priorPhase,
    });
    const recoveryIntent = await attempt.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: attempt.recovery.schedulerAppend,
        artifact_binding_candidate_handle:
          attempt.recovery.artifactBindingCandidateHandle,
      });
    if (priorPhase !== null) {
      assert.equal(
        recoveryIntent.intent_append_receipt.commit_sha,
        attempt.priorArtifactBindingIntentRecordOid,
      );
    }
    const providerArtifactHandles =
      await loadAutomaticRecoveryProviderArtifactHandles({
        ledger: attempt.ledger,
        fixture: attempt.fixture,
        transport: recoveryIntent.transport,
        snapshots: attempt.recovery.snapshots,
      });
    const recoveryResponse = await attempt.ledger
      .appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          recoveryIntent.artifact_binding_intent_handle,
        intent_append_receipt: recoveryIntent.intent_append_receipt,
        provider_artifact_handles: providerArtifactHandles,
      });
    assert.equal(recoveryResponse.bindings.length,
      recoveryIntent.transport.expected_count);
    const beforeRelease = await attempt.ledger.load();
    const observationIndex = beforeRelease.records.findIndex((entry) =>
      entry.commit_sha === attempt.recovery.schedulerAppend.append_receipt
        .commit_sha);
    assert.notEqual(observationIndex, -1);
    assert.deepEqual(
      beforeRelease.records.slice(observationIndex + 1)
        .filter((entry) => new Set([
          "artifact-binding", "artifact-binding-ready",
        ]).has(entry.envelope.kind))
        .map((entry) =>
          `${entry.envelope.kind}:${entry.envelope.record_type === "effect-intent" ? "intent" : "response"}`),
      expectedCurrentStages,
    );
    const releaseReceipt = await releaseFixtureDiscoveryLease(
      attempt.ledger,
      attempt.discovery,
    );
    const terminalResult = candidateDispatchTerminalResult({
      decision: attempt.recovery.runnerResult.scheduler_evaluation.decision,
      schedulerAppend: attempt.recovery.schedulerAppend,
      leaseReleaseReceipt: releaseReceipt,
      preflight: attempt.preflight,
      continuityAuthority: attempt.discovery.continuity_authority,
      controlPlaneReceipt:
        createV2ControlPlaneReceiptFromGitLedgerAuthority(
          attempt.recovery.controlPlaneAuthority,
        ),
      initialAuthority: null,
      establishedAuthority: attempt.recovery.establishedAuthority,
      runnerAuthority: attempt.recovery.productionAuthority,
      statusIntents: attempt.recovery.statusIntents,
      statusResponses: attempt.recovery.statusResponses,
      statusAlreadyBound: true,
    });
    const resultAuthority = await attempt.ledger
      .createCandidateDispatchResultAuthority({
        candidate_dispatch_handle:
          attempt.scheduled.candidate_dispatch_handle,
        scheduler_append: attempt.recovery.schedulerAppend,
        production_runner_authority: attempt.recovery.productionAuthority,
        lease_release_receipt: releaseReceipt,
        terminal_result: terminalResult,
      });
    const ack = await attempt.ledger.ackCandidateDispatch({
      candidate_dispatch_handle:
        attempt.scheduled.candidate_dispatch_handle,
      reservation_receipt: attempt.scheduled.reservation_receipt,
      full_scope_receipt: attempt.discovery.effect_evaluated_scope_receipt,
      candidate_dispatch_result_handle:
        resultAuthority.candidate_dispatch_result_handle,
    });
    assert.notEqual(ack.ack_append_receipt, null);
    const final = await attempt.ledger.load();
    assert.equal(final.active_lease, null);
    if (priorPhase !== null) {
      const writesBeforeProjection = attempt.fixture.writeCalls;
      const withoutPriorLeaseRelease = final.records.filter((entry) =>
        entry.commit_sha !==
          attempt.priorLeaseReleaseReceipt.commit_sha);
      assert.throws(
        () => deriveV2GitLedgerCandidateDispatchAuthority(
          withoutPriorLeaseRelease,
          REPOSITORY,
        ),
        /closed production chain/u,
      );
      assert.equal(attempt.fixture.writeCalls, writesBeforeProjection);
    }
    });
}

test("candidate dispatch closes a frozen artifact prefix and one durable queued suffix before ack",
  async () => {
    const attempt = await createScheduledAutomaticRecoveryBranchAttempt({
      bindArtifactBeforeDispatch: false,
      durableArtifactPrefixPhaseBeforeDispatch: "ready-confirmation",
      additionalFindingIdsDuringDispatch: ["203"],
    });
    const first = await attempt.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: attempt.recovery.schedulerAppend,
        artifact_binding_candidate_handle:
          attempt.recovery.artifactBindingCandidateHandle,
      });
    assert.deepEqual(
      first.transport.requests.map((request) => request.selector.id),
      ["202"],
    );
    assert.notEqual(
      first.latest_append_receipt.commit_sha,
      first.ready_confirmation_append_receipt.commit_sha,
    );
    const firstHandles = await loadAutomaticRecoveryProviderArtifactHandles({
      ledger: attempt.ledger,
      fixture: attempt.fixture,
      transport: first.transport,
      snapshots: attempt.recovery.snapshots,
    });
    await attempt.ledger.appendAutomaticRecoveryArtifactBindingResponse({
      artifact_binding_intent_handle:
        first.artifact_binding_intent_handle,
      intent_append_receipt: first.intent_append_receipt,
      provider_artifact_handles: firstHandles,
    });
    const second = await attempt.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: attempt.recovery.schedulerAppend,
        artifact_binding_candidate_handle:
          attempt.recovery.artifactBindingCandidateHandle,
      });
    assert.deepEqual(
      second.transport.requests.map((request) => request.selector.id),
      ["203"],
    );
    assert.equal(
      second.latest_append_receipt.commit_sha,
      second.ready_confirmation_append_receipt.commit_sha,
    );
    const secondHandles = await loadAutomaticRecoveryProviderArtifactHandles({
      ledger: attempt.ledger,
      fixture: attempt.fixture,
      transport: second.transport,
      snapshots: attempt.recovery.snapshots,
    });
    await attempt.ledger.appendAutomaticRecoveryArtifactBindingResponse({
      artifact_binding_intent_handle:
        second.artifact_binding_intent_handle,
      intent_append_receipt: second.intent_append_receipt,
      provider_artifact_handles: secondHandles,
    });
    const beforeRelease = await attempt.ledger.load();
    const observationIndex = beforeRelease.records.findIndex((entry) =>
      entry.commit_sha ===
        attempt.recovery.schedulerAppend.append_receipt.commit_sha);
    assert.deepEqual(
      beforeRelease.records.slice(observationIndex + 1)
        .filter((entry) => new Set([
          "artifact-binding", "artifact-binding-ready",
        ]).has(entry.envelope.kind))
        .map((entry) =>
          `${entry.envelope.kind}:${entry.envelope.record_type === "effect-intent" ? "intent" : "response"}`),
      [
        "artifact-binding:intent",
        "artifact-binding:response",
        "artifact-binding-ready:intent",
        "artifact-binding-ready:response",
        "artifact-binding:response",
      ],
    );
    const releaseReceipt = await releaseFixtureDiscoveryLease(
      attempt.ledger,
      attempt.discovery,
    );
    const terminalResult = candidateDispatchTerminalResult({
      decision: attempt.recovery.runnerResult.scheduler_evaluation.decision,
      schedulerAppend: attempt.recovery.schedulerAppend,
      leaseReleaseReceipt: releaseReceipt,
      preflight: attempt.preflight,
      continuityAuthority: attempt.discovery.continuity_authority,
      controlPlaneReceipt:
        createV2ControlPlaneReceiptFromGitLedgerAuthority(
          attempt.recovery.controlPlaneAuthority,
        ),
      initialAuthority: null,
      establishedAuthority: attempt.recovery.establishedAuthority,
      runnerAuthority: attempt.recovery.productionAuthority,
      statusIntents: attempt.recovery.statusIntents,
      statusResponses: attempt.recovery.statusResponses,
      statusAlreadyBound: true,
    });
    const resultAuthority = await attempt.ledger
      .createCandidateDispatchResultAuthority({
        candidate_dispatch_handle:
          attempt.scheduled.candidate_dispatch_handle,
        scheduler_append: attempt.recovery.schedulerAppend,
        production_runner_authority: attempt.recovery.productionAuthority,
        lease_release_receipt: releaseReceipt,
        terminal_result: terminalResult,
      });
    const ack = await attempt.ledger.ackCandidateDispatch({
      candidate_dispatch_handle:
        attempt.scheduled.candidate_dispatch_handle,
      reservation_receipt: attempt.scheduled.reservation_receipt,
      full_scope_receipt: attempt.discovery.effect_evaluated_scope_receipt,
      candidate_dispatch_result_handle:
        resultAuthority.candidate_dispatch_result_handle,
    });
    assert.notEqual(ack.ack_append_receipt, null);
    assert.equal((await attempt.ledger.load()).active_lease, null);
  });

test("candidate dispatch closes a queued-only artifact suffix after its completed durable parent",
  async () => {
    const attempt = await createScheduledAutomaticRecoveryBranchAttempt({
      bindArtifactBeforeDispatch: true,
      additionalFindingIdsDuringDispatch: ["203"],
    });
    assert.notEqual(attempt.priorArtifactBinding, null);
    assert.equal(attempt.recovery.recoveryHandle, null);
    const queued = await attempt.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: attempt.recovery.schedulerAppend,
        artifact_binding_candidate_handle:
          attempt.recovery.artifactBindingCandidateHandle,
      });
    assert.deepEqual(
      queued.transport.requests.map((request) => request.selector.id),
      ["203"],
    );
    const loaded = await attempt.ledger.load();
    const queuedIntent = loaded.records.find((entry) =>
      entry.commit_sha === queued.intent_append_receipt.commit_sha);
    assert.notEqual(queuedIntent, undefined);
    assert.equal(
      queuedIntent.envelope.payload.action
        .queued_after_artifact_binding_intent_record_oid,
      attempt.priorArtifactBindingIntentRecordOid,
    );
    const handles = await loadAutomaticRecoveryProviderArtifactHandles({
      ledger: attempt.ledger,
      fixture: attempt.fixture,
      transport: queued.transport,
      snapshots: attempt.recovery.snapshots,
    });
    await attempt.ledger.appendAutomaticRecoveryArtifactBindingResponse({
      artifact_binding_intent_handle:
        queued.artifact_binding_intent_handle,
      intent_append_receipt: queued.intent_append_receipt,
      provider_artifact_handles: handles,
    });
    const beforeRelease = await attempt.ledger.load();
    const observationIndex = beforeRelease.records.findIndex((entry) =>
      entry.commit_sha ===
        attempt.recovery.schedulerAppend.append_receipt.commit_sha);
    assert.deepEqual(
      beforeRelease.records.slice(observationIndex + 1)
        .filter((entry) => new Set([
          "artifact-binding", "artifact-binding-ready",
        ]).has(entry.envelope.kind))
        .map((entry) =>
          `${entry.envelope.kind}:${entry.envelope.record_type === "effect-intent" ? "intent" : "response"}`),
      [
        "artifact-binding:intent",
        "artifact-binding-ready:intent",
        "artifact-binding-ready:response",
        "artifact-binding:response",
      ],
    );
    const releaseReceipt = await releaseFixtureDiscoveryLease(
      attempt.ledger,
      attempt.discovery,
    );
    const terminalResult = candidateDispatchTerminalResult({
      decision: attempt.recovery.runnerResult.scheduler_evaluation.decision,
      schedulerAppend: attempt.recovery.schedulerAppend,
      leaseReleaseReceipt: releaseReceipt,
      preflight: attempt.preflight,
      continuityAuthority: attempt.discovery.continuity_authority,
      controlPlaneReceipt:
        createV2ControlPlaneReceiptFromGitLedgerAuthority(
          attempt.recovery.controlPlaneAuthority,
        ),
      initialAuthority: null,
      establishedAuthority: attempt.recovery.establishedAuthority,
      runnerAuthority: attempt.recovery.productionAuthority,
      statusIntents: attempt.recovery.statusIntents,
      statusResponses: attempt.recovery.statusResponses,
      statusAlreadyBound: true,
    });
    const resultAuthority = await attempt.ledger
      .createCandidateDispatchResultAuthority({
        candidate_dispatch_handle:
          attempt.scheduled.candidate_dispatch_handle,
        scheduler_append: attempt.recovery.schedulerAppend,
        production_runner_authority: attempt.recovery.productionAuthority,
        lease_release_receipt: releaseReceipt,
        terminal_result: terminalResult,
      });
    const ack = await attempt.ledger.ackCandidateDispatch({
      candidate_dispatch_handle:
        attempt.scheduled.candidate_dispatch_handle,
      reservation_receipt: attempt.scheduled.reservation_receipt,
      full_scope_receipt: attempt.discovery.effect_evaluated_scope_receipt,
      candidate_dispatch_result_handle:
        resultAuthority.candidate_dispatch_result_handle,
    });
    assert.notEqual(ack.ack_append_receipt, null);
    assert.equal((await attempt.ledger.load()).active_lease, null);
  });

test("candidate dispatch terminal rejects a completed prefix with its durable queued suffix incomplete",
  async () => {
    const attempt = await createScheduledAutomaticRecoveryBranchAttempt({
      bindArtifactBeforeDispatch: false,
      durableArtifactPrefixPhaseBeforeDispatch: "ready-confirmation",
      additionalFindingIdsBeforeDispatch: ["203"],
      additionalFindingIdsDuringDispatch: ["203"],
    });
    const first = await attempt.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: attempt.recovery.schedulerAppend,
        artifact_binding_candidate_handle:
          attempt.recovery.artifactBindingCandidateHandle,
      });
    const firstHandles = await loadAutomaticRecoveryProviderArtifactHandles({
      ledger: attempt.ledger,
      fixture: attempt.fixture,
      transport: first.transport,
      snapshots: attempt.recovery.snapshots,
    });
    await attempt.ledger.appendAutomaticRecoveryArtifactBindingResponse({
      artifact_binding_intent_handle:
        first.artifact_binding_intent_handle,
      intent_append_receipt: first.intent_append_receipt,
      provider_artifact_handles: firstHandles,
    });
    const incomplete = await attempt.ledger.load();
    assert.equal(incomplete.records.filter((entry) =>
      entry.envelope.record_type === "effect-intent" &&
      entry.envelope.kind === "artifact-binding" &&
      entry.envelope.payload.action
        .queued_after_artifact_binding_intent_record_oid !== null).length, 1);
    assert.equal(incomplete.records.filter((entry) =>
      entry.envelope.record_type === "effect-response" &&
      entry.envelope.kind === "artifact-binding").length, 1);
    const releaseReceipt = await releaseFixtureDiscoveryLease(
      attempt.ledger,
      attempt.discovery,
    );
    const terminalResult = candidateDispatchTerminalResult({
      decision: attempt.recovery.runnerResult.scheduler_evaluation.decision,
      schedulerAppend: attempt.recovery.schedulerAppend,
      leaseReleaseReceipt: releaseReceipt,
      preflight: attempt.preflight,
      continuityAuthority: attempt.discovery.continuity_authority,
      controlPlaneReceipt:
        createV2ControlPlaneReceiptFromGitLedgerAuthority(
          attempt.recovery.controlPlaneAuthority,
        ),
      initialAuthority: null,
      establishedAuthority: attempt.recovery.establishedAuthority,
      runnerAuthority: attempt.recovery.productionAuthority,
      statusIntents: attempt.recovery.statusIntents,
      statusResponses: attempt.recovery.statusResponses,
      statusAlreadyBound: true,
    });
    const writesBefore = attempt.fixture.writeCalls;
    const tipBefore = attempt.fixture.refTarget;
    await assert.rejects(
      attempt.ledger.createCandidateDispatchResultAuthority({
        candidate_dispatch_handle:
          attempt.scheduled.candidate_dispatch_handle,
        scheduler_append: attempt.recovery.schedulerAppend,
        production_runner_authority: attempt.recovery.productionAuthority,
        lease_release_receipt: releaseReceipt,
        terminal_result: terminalResult,
      }),
      /closed production chain/u,
    );
    assert.equal(attempt.fixture.writeCalls, writesBefore);
    assert.equal(attempt.fixture.refTarget, tipBefore);
  });

test("candidate dispatch terminal rejects an omitted durable aggregate queue",
  async () => {
    const attempt = await createScheduledAutomaticRecoveryBranchAttempt({
      bindArtifactBeforeDispatch: false,
      durableArtifactPrefixPhaseBeforeDispatch: "ready-confirmation",
      additionalFindingIdsBeforeDispatch: ["203"],
      additionalFindingIdsDuringDispatch: ["203"],
    });
    assert.notEqual(
      attempt.recovery.artifactBindingCandidateHandle,
      null,
    );
    const incomplete = await attempt.ledger.load();
    assert.equal(incomplete.records.filter((entry) =>
      entry.envelope.record_type === "effect-intent" &&
      entry.envelope.kind === "artifact-binding").length, 2);
    assert.equal(incomplete.records.filter((entry) =>
      entry.envelope.record_type === "effect-response" &&
      entry.envelope.kind === "artifact-binding").length, 0);
    const releaseReceipt = await releaseFixtureDiscoveryLease(
      attempt.ledger,
      attempt.discovery,
    );
    const terminalResult = candidateDispatchTerminalResult({
      decision: attempt.recovery.runnerResult.scheduler_evaluation.decision,
      schedulerAppend: attempt.recovery.schedulerAppend,
      leaseReleaseReceipt: releaseReceipt,
      preflight: attempt.preflight,
      continuityAuthority: attempt.discovery.continuity_authority,
      controlPlaneReceipt:
        createV2ControlPlaneReceiptFromGitLedgerAuthority(
          attempt.recovery.controlPlaneAuthority,
        ),
      initialAuthority: null,
      establishedAuthority: attempt.recovery.establishedAuthority,
      runnerAuthority: attempt.recovery.productionAuthority,
      statusIntents: attempt.recovery.statusIntents,
      statusResponses: attempt.recovery.statusResponses,
      statusAlreadyBound: true,
    });
    const writesBefore = attempt.fixture.writeCalls;
    const tipBefore = attempt.fixture.refTarget;
    await assert.rejects(
      attempt.ledger.createCandidateDispatchResultAuthority({
        candidate_dispatch_handle:
          attempt.scheduled.candidate_dispatch_handle,
        scheduler_append: attempt.recovery.schedulerAppend,
        production_runner_authority: attempt.recovery.productionAuthority,
        lease_release_receipt: releaseReceipt,
        terminal_result: terminalResult,
      }),
      /closed production chain/u,
    );
    assert.equal(attempt.fixture.writeCalls, writesBefore);
    assert.equal(attempt.fixture.refTarget, tipBefore);
  });

test("aggregate stage9 rejects an unreleased stage8 continuation lease before write",
  async () => {
    const setup = await createAutomaticRecoveryTransitionContext({
      bindArtifact: false,
    });
    setup.fixture.failRereadAfterEffectUpdate("artifact-binding", 1);
    await assert.rejects(
      setup.ledger.loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: setup.schedulerAppend,
        artifact_binding_candidate_handle:
          setup.artifactBindingCandidateHandle,
      }),
      (error) => error.code === "unexpected-http-status",
    );
    await releaseFixtureDiscoveryLease(
      setup.ledger,
      setup.context.discovery,
    );
    setup.fixture.advanceServerTime(120);
    setup.runIdentity.run_id = "9201";
    const second = await acquireEstablishedRunnerContext({
      ledger: setup.ledger,
      runIdentity: setup.runIdentity,
      observationBoundary: "public-post-request-wait-complete",
      transportStartSecond: 10_000,
      minimalServerTime: "2026-08-13T15:00:00.000Z",
    });
    const secondObservation = await appendAutomaticRecoveryObservation({
      ledger: setup.ledger,
      preflight: setup.context.preflight,
      evaluatedScopeReceipt: second.evaluatedScopeReceipt,
      workflowCommandHandle: second.workflowCommandHandle,
      requestCreatedAt: setup.requestCreatedAt,
      findingCreatedAt: setup.findingCreatedAt,
      expectRecoveryHandle: false,
    });
    await setup.ledger.loadOrAppendAutomaticRecoveryArtifactBindingIntent({
      scheduler_append: secondObservation.schedulerAppend,
      artifact_binding_candidate_handle:
        secondObservation.artifactBindingCandidateHandle,
    });
    setup.fixture.advanceServerTime(1000);
    setup.runIdentity.run_id = "9202";
    const third = await acquireEstablishedRunnerContext({
      ledger: setup.ledger,
      runIdentity: setup.runIdentity,
      observationBoundary: "public-post-request-wait-complete",
      transportStartSecond: 12_000,
      minimalServerTime: "2026-08-13T16:00:00.000Z",
    });
    const thirdObservation = await appendAutomaticRecoveryObservation({
      ledger: setup.ledger,
      preflight: setup.context.preflight,
      evaluatedScopeReceipt: third.evaluatedScopeReceipt,
      workflowCommandHandle: third.workflowCommandHandle,
      requestCreatedAt: setup.requestCreatedAt,
      findingCreatedAt: setup.findingCreatedAt,
      expectRecoveryHandle: false,
    });
    const intent = await setup.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: thirdObservation.schedulerAppend,
        artifact_binding_candidate_handle:
          thirdObservation.artifactBindingCandidateHandle,
      });
    const providerArtifactHandles =
      await loadAutomaticRecoveryProviderArtifactHandles({
        ledger: setup.ledger,
        fixture: setup.fixture,
        transport: intent.transport,
        snapshots: thirdObservation.snapshots,
      });
    const before = await setup.ledger.load();
    const writesBefore = setup.fixture.writeCalls;
    await assert.rejects(
      setup.ledger.appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          intent.artifact_binding_intent_handle,
        intent_append_receipt: intent.intent_append_receipt,
        provider_artifact_handles: providerArtifactHandles,
      }),
      (error) => error.code ===
        "automatic-artifact-binding-reachability-lineage",
    );
    const after = await setup.ledger.load();
    assert.equal(setup.fixture.writeCalls, writesBefore);
    assert.equal(after.tip_commit_sha, before.tip_commit_sha);
    assert.equal(after.records.filter((entry) =>
      entry.envelope.record_type === "effect-response" &&
      entry.envelope.kind === "artifact-binding").length, 0);
  });

test("candidate dispatch rejects incomplete internal recovery pairs as terminal",
  async (t) => {
    for (const recovery of [
      { label: "artifact-intent", kind: "artifact-binding", occurrence: 1 },
      {
        label: "artifact-ready-intent",
        kind: "artifact-binding-ready",
        occurrence: 1,
      },
      {
        label: "artifact-ready-confirmation",
        kind: "artifact-binding-ready",
        occurrence: 2,
      },
      { label: "scheduler-state-intent", kind: "scheduler-state", occurrence: 1 },
    ]) {
      await t.test(recovery.label, async () => {
        const attempt = await createScheduledAutomaticRecoveryBranchAttempt({
          bindArtifactBeforeDispatch: recovery.kind === "scheduler-state",
        });
        attempt.fixture.failRereadAfterEffectUpdate(
          recovery.kind,
          recovery.occurrence,
        );
        if (recovery.kind.startsWith("artifact-binding")) {
          await assert.rejects(
            attempt.ledger
              .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
                scheduler_append: attempt.recovery.schedulerAppend,
                artifact_binding_candidate_handle:
                  attempt.recovery.artifactBindingCandidateHandle,
              }),
            (error) => error.code === "unexpected-http-status",
          );
        } else {
          await assert.rejects(
            attempt.ledger.appendAutomaticRequestRecoveryTransition({
              scheduler_append: attempt.recovery.schedulerAppend,
              recovery_handle: attempt.recovery.recoveryHandle,
            }),
            (error) => error.code === "unexpected-http-status",
          );
        }
        const releaseReceipt = await releaseFixtureDiscoveryLease(
          attempt.ledger,
          attempt.discovery,
        );
        const terminalResult = candidateDispatchTerminalResult({
          decision:
            attempt.recovery.runnerResult.scheduler_evaluation.decision,
          schedulerAppend: attempt.recovery.schedulerAppend,
          leaseReleaseReceipt: releaseReceipt,
          preflight: attempt.preflight,
          continuityAuthority: attempt.discovery.continuity_authority,
          controlPlaneReceipt:
            createV2ControlPlaneReceiptFromGitLedgerAuthority(
              attempt.recovery.controlPlaneAuthority,
            ),
          initialAuthority: null,
          establishedAuthority: attempt.recovery.establishedAuthority,
          runnerAuthority: attempt.recovery.productionAuthority,
          statusIntents: attempt.recovery.statusIntents,
          statusResponses: attempt.recovery.statusResponses,
          statusAlreadyBound: recovery.kind === "scheduler-state",
        });
        const before = await attempt.ledger.load();
        const writesBefore = attempt.fixture.writeCalls;
        await assert.rejects(
          attempt.ledger.createCandidateDispatchResultAuthority({
            candidate_dispatch_handle:
              attempt.scheduled.candidate_dispatch_handle,
            scheduler_append: attempt.recovery.schedulerAppend,
            production_runner_authority:
              attempt.recovery.productionAuthority,
            lease_release_receipt: releaseReceipt,
            terminal_result: terminalResult,
          }),
          /closed production chain/u,
        );
        const after = await attempt.ledger.load();
        assert.equal(attempt.fixture.writeCalls, writesBefore);
        assert.equal(after.tip_commit_sha, before.tip_commit_sha);
        assert.equal(after.records.filter((entry) =>
          entry.envelope.record_type === "candidate-dispatch-observation" &&
          entry.envelope.payload.phase === "candidate-ack").length, 0);
      });
    }
  });

for (const durableArtifactPhase of [
  "intent", "ready-intent", "ready-confirmation", "response",
]) {
  test(`candidate dispatch recovers a durable aggregate artifact ${durableArtifactPhase} without a second attempt`,
    async () => {
      const attempt = await createScheduledAutomaticRecoveryBranchAttempt({
        bindArtifactBeforeDispatch: false,
      });
      const partialFailure = durableArtifactPhase === "intent"
        ? { kind: "artifact-binding", occurrence: 1 }
        : durableArtifactPhase === "ready-intent"
          ? { kind: "artifact-binding-ready", occurrence: 1 }
          : durableArtifactPhase === "ready-confirmation"
            ? { kind: "artifact-binding-ready", occurrence: 2 }
            : null;
      if (partialFailure !== null) {
        attempt.fixture.failRereadAfterEffectUpdate(
          partialFailure.kind,
          partialFailure.occurrence,
        );
      }
      let bindingIntent = null;
      const appendBindingIntent = attempt.ledger
        .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
          scheduler_append: attempt.recovery.schedulerAppend,
          artifact_binding_candidate_handle:
            attempt.recovery.artifactBindingCandidateHandle,
        });
      if (partialFailure !== null) {
        await assert.rejects(
          appendBindingIntent,
          (error) => error.code === "unexpected-http-status",
        );
      } else {
        bindingIntent = await appendBindingIntent;
      }
      if (durableArtifactPhase === "response") {
        const providerArtifactHandles =
          await loadAutomaticRecoveryProviderArtifactHandles({
            ledger: attempt.ledger,
            fixture: attempt.fixture,
            transport: bindingIntent.transport,
            snapshots: attempt.recovery.snapshots,
          });
        await attempt.ledger.appendAutomaticRecoveryArtifactBindingResponse({
          artifact_binding_intent_handle:
            bindingIntent.artifact_binding_intent_handle,
          intent_append_receipt: bindingIntent.intent_append_receipt,
          provider_artifact_handles: providerArtifactHandles,
        });
      }
      await releaseFixtureDiscoveryLease(attempt.ledger, attempt.discovery);
      const before = await attempt.ledger.load();
      const artifactIntentsBefore = before.records.filter((entry) =>
        entry.envelope.record_type === "effect-intent" &&
        entry.envelope.kind === "artifact-binding").length;
      const artifactResponsesBefore = before.records.filter((entry) =>
        entry.envelope.record_type === "effect-response" &&
        entry.envelope.kind === "artifact-binding").length;
      const artifactReadyBefore = before.records.filter((entry) =>
        entry.envelope.record_type === "effect-intent" &&
        entry.envelope.kind === "artifact-binding-ready").length;
      const artifactReadyConfirmationsBefore = before.records.filter((entry) =>
        entry.envelope.record_type === "effect-response" &&
        entry.envelope.kind === "artifact-binding-ready").length;
      const recovered = await attempt.ledger.recoverCandidateDispatchFailure({
        workflow_command_handle: attempt.scheduledCommand,
        trigger_identity: attempt.triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        expected_dispatch_binding: attempt.reserved.plan.items[0],
      });
      assert.equal(
        recovered.prefix_phase,
        durableArtifactPhase === "intent"
          ? "automatic-recovery-intent"
          : durableArtifactPhase === "ready-intent"
            ? "automatic-recovery-ready"
            : durableArtifactPhase === "ready-confirmation"
              ? "automatic-recovery-ready-confirmed"
              : "automatic-recovery-response",
      );
      assert.equal(
        recovered.result.failure_code,
        durableArtifactPhase !== "response"
          ? "CANDIDATE_ATTEMPT_RECOVERED_DURING_AUTOMATIC_RECOVERY"
          : "CANDIDATE_ATTEMPT_RECOVERED_AFTER_AUTOMATIC_RECOVERY",
      );
      assert.equal(recovered.result.public_effects_performed, 2);
      assert.equal(recovered.cycle_complete, true);
      const final = await attempt.ledger.load();
      assert.equal(final.records.filter((entry) =>
        entry.envelope.record_type === "lease-acquire" &&
        entry.envelope.workflow_provenance.operation_binding
          .evaluated_scope_receipt.relation === "scheduled-pull-request"
      ).length, 1);
      assert.equal(final.records.filter((entry) =>
        entry.envelope.record_type === "effect-intent" &&
        entry.envelope.kind === "artifact-binding").length,
      artifactIntentsBefore);
      assert.equal(final.records.filter((entry) =>
        entry.envelope.record_type === "effect-response" &&
        entry.envelope.kind === "artifact-binding").length,
      artifactResponsesBefore);
      assert.equal(final.records.filter((entry) =>
        entry.envelope.record_type === "effect-intent" &&
        entry.envelope.kind === "artifact-binding-ready").length,
      artifactReadyBefore);
      assert.equal(final.records.filter((entry) =>
        entry.envelope.record_type === "effect-response" &&
        entry.envelope.kind === "artifact-binding-ready").length,
      artifactReadyConfirmationsBefore);
      assert.equal(final.records.filter((entry) =>
        entry.envelope.record_type === "candidate-dispatch-observation" &&
        entry.envelope.payload.phase === "candidate-ack").length, 1);
  });
}

test("candidate dispatch recovery classifies a durable queued aggregate intent",
  async () => {
    const attempt = await createScheduledAutomaticRecoveryBranchAttempt({
      bindArtifactBeforeDispatch: false,
      durableArtifactPrefixPhaseBeforeDispatch: "ready-confirmation",
      additionalFindingIdsDuringDispatch: ["203"],
    });
    const first = await attempt.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: attempt.recovery.schedulerAppend,
        artifact_binding_candidate_handle:
          attempt.recovery.artifactBindingCandidateHandle,
      });
    assert.notEqual(
      first.latest_append_receipt.commit_sha,
      first.ready_confirmation_append_receipt.commit_sha,
    );
    await releaseFixtureDiscoveryLease(attempt.ledger, attempt.discovery);
    const recovered = await attempt.ledger.recoverCandidateDispatchFailure({
      workflow_command_handle: attempt.scheduledCommand,
      trigger_identity: attempt.triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      expected_dispatch_binding: attempt.reserved.plan.items[0],
    });
    assert.equal(recovered.prefix_phase, "automatic-recovery-intent");
    assert.equal(
      recovered.result.failure_code,
      "CANDIDATE_ATTEMPT_RECOVERED_DURING_AUTOMATIC_RECOVERY",
    );
    assert.deepEqual(attempt.recovery.statusIntents, []);
    assert.equal(recovered.result.public_effects_performed, 0);
    assert.equal(recovered.result.outcome, "failed");
    const final = await attempt.ledger.load();
    assert.equal(final.records.filter((entry) =>
      entry.envelope.record_type === "candidate-dispatch-observation" &&
      entry.envelope.payload.phase === "candidate-ack").length, 1);
  });

test("candidate dispatch recovers one released same-run scheduled attempt", async () => {
  const attempt = await createScheduledCandidateDispatchAttempt();
  const releaseReceipt = await releaseFixtureDiscoveryLease(
    attempt.ledger,
    attempt.discovery,
  );
  const recovered = await attempt.ledger.recoverCandidateDispatchFailure({
    workflow_command_handle: attempt.scheduledCommand,
    trigger_identity: attempt.triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    expected_dispatch_binding: attempt.reserved.plan.items[0],
  });
  assert.equal(recovered.recovery_mode, "released");
  assert.equal(recovered.prefix_phase, "lease-acquired");
  assert.equal(recovered.result.outcome, "failed");
  assert.equal(
    recovered.result.failure_code,
    "CANDIDATE_ATTEMPT_RECOVERED_AFTER_LEASE",
  );
  assert.equal(recovered.result.public_effects_performed, 0);
  assert.notEqual(recovered.ack_append_receipt, null);
  assert.notEqual(recovered.batch_completion_append_receipt, null);
  assert.notEqual(recovered.cycle_completion_append_receipt, null);
  assert.equal(recovered.cycle_complete, true);
  assert.equal(recovered.remaining_plan, null);
  const loaded = await attempt.ledger.load();
  const ack = loaded.records.find((entry) =>
    entry.envelope.record_type === "candidate-dispatch-observation" &&
    entry.envelope.payload.phase === "candidate-ack");
  assert.equal(
    ack.envelope.payload.candidate_ack.terminal_authority
      .lease_release_record_oid,
    releaseReceipt.commit_sha,
  );
  assert.equal(
    ack.envelope.payload.command_authority.pull_request_number,
    PR.number,
  );
});

test("candidate dispatch recovery classifies a durable status ambiguity", async () => {
  const attempt = await createScheduledCandidateDispatchAttempt();
  const fullScopeReceipt = attempt.discovery.effect_evaluated_scope_receipt;
  const controlPlaneAuthority = await attempt.ledger.loadControlPlaneAuthority(
    fullScopeReceipt.scope,
  );
  const initialAuthority = await attempt.ledger.loadInitialRunnerStateAuthority({
    control_plane_authority: controlPlaneAuthority,
    evaluated_scope_receipt: fullScopeReceipt,
    workflow_command_handle: attempt.scheduledCommand,
  });
  const controlPlaneReceipt =
    createV2ControlPlaneReceiptFromGitLedgerAuthority(controlPlaneAuthority);
  const runnerAuthority = createV2ProductionRunnerAuthority({
    preflight_handle: attempt.preflight,
    control_plane_receipt: controlPlaneReceipt,
    initial_runner_state_authority: initialAuthority,
    expected_scope: initialAuthority.scope,
  });
  const plan = schedulerObservationRecord({
    predecessor: attempt.discovery.lease_receipt.acquire_commit_sha,
    lease: attempt.discovery.lease_receipt,
    priorAuthorityDigest: initialAuthority.prior_authority_digest,
    initialAuthority,
  }).payload.action;
  const schedulerAppend = await attempt.ledger
    .appendInitialSchedulerObservation({
      initial_runner_state_authority: initialAuthority,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: plan.status_plan,
    });
  await attempt.ledger.appendStatusWriteIntent({
    scheduler_append: schedulerAppend,
    status_write_index: 0,
  });
  await releaseFixtureDiscoveryLease(attempt.ledger, attempt.discovery);
  const recovered = await attempt.ledger.recoverCandidateDispatchFailure({
    workflow_command_handle: attempt.scheduledCommand,
    trigger_identity: attempt.triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    expected_dispatch_binding: attempt.reserved.plan.items[0],
  });
  assert.equal(recovered.prefix_phase, "status-intent");
  assert.equal(recovered.result.public_effects_performed, 1);
  assert.equal(
    recovered.result.failure_code,
    "CANDIDATE_STATUS_EFFECT_AMBIGUOUS",
  );
  const loaded = await attempt.ledger.load();
  const ack = loaded.records.find((entry) =>
    entry.envelope.record_type === "candidate-dispatch-observation" &&
    entry.envelope.payload.phase === "candidate-ack");
  assert.deepEqual(
    ack.envelope.payload.candidate_ack.terminal_authority.terminal_projection,
    {
      decision: plan.scheduler_evaluation.decision,
      public_effects_performed: 1,
      status_effect_outcome: "ambiguous",
      status_ambiguity_code: "CANDIDATE_STATUS_EFFECT_AMBIGUOUS",
      reservation_status_effect_outcome: "not-required",
      reservation_status_ambiguity_code: null,
      automatic_request_effect_outcome: "not-required",
      automatic_request_ambiguity_code: null,
    },
  );
  const evidence = currentOpenSuppressionAdapterFixture();
  const classificationLedger = makeLedger(
    attempt.fixture,
    attempt.capability,
    attempt.verifier,
    attempt.preflight,
    { candidateSuppressionEvidenceAdapter: evidence.adapter },
  );
  const writesBeforeClassification = attempt.fixture.writeCalls;
  const refBeforeClassification = attempt.fixture.refTarget;
  const classification = await classificationLedger
    .trySuppressCandidateInventoryRefresh({
      workflow_command_handle: attempt.scanCommand,
      trigger_identity: attempt.triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
  assert.equal(classification.state, "persist-required");
  assert.equal(
    classification.reason,
    "candidate-dispatch-terminal-recovery",
  );
  assert.equal(evidence.calls.length, 0);
  assert.equal(attempt.fixture.writeCalls, writesBeforeClassification);
  assert.equal(attempt.fixture.refTarget, refBeforeClassification);
  assert.equal(runnerAuthority.effect_barrier,
    "scheduler-observation-required");
});

test("candidate dispatch recovery classifies every durable protocol prefix", async () => {
  const attempt = await createScheduledCandidateDispatchAttempt();
  attempt.fixture.freezeServerTime();
  const fullScopeReceipt = attempt.discovery.effect_evaluated_scope_receipt;
  const controlPlaneAuthority = await attempt.ledger.loadControlPlaneAuthority(
    fullScopeReceipt.scope,
  );
  const initialAuthority = await attempt.ledger.loadInitialRunnerStateAuthority({
    control_plane_authority: controlPlaneAuthority,
    evaluated_scope_receipt: fullScopeReceipt,
    workflow_command_handle: attempt.scheduledCommand,
  });
  const plan = schedulerObservationRecord({
    predecessor: attempt.discovery.lease_receipt.acquire_commit_sha,
    lease: attempt.discovery.lease_receipt,
    priorAuthorityDigest: initialAuthority.prior_authority_digest,
    initialAuthority,
  }).payload.action;
  const schedulerAppend = await attempt.ledger
    .appendInitialSchedulerObservation({
      initial_runner_state_authority: initialAuthority,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: plan.status_plan,
    });
  const statusIntent = await attempt.ledger.appendStatusWriteIntent({
    scheduler_append: schedulerAppend,
    status_write_index: 0,
  });
  const statusResponse = await attempt.ledger.appendStatusWriteResponse({
    status_intent_handle: statusIntent.status_intent_handle,
    intent_append_receipt: statusIntent.intent_append_receipt,
    receipt: statusWriteResponseReceipt(
      statusIntent.transport,
      statusIntent.intent_append_receipt.ref_reread.server_time,
    ),
  });
  const automatic = await attempt.ledger.appendAutomaticRequestReservation({
    scheduler_append: schedulerAppend,
  });
  const reservationIntent = await attempt.ledger
    .appendReservationStatusWriteIntent({
      automatic_reservation_handle: automatic.automatic_reservation_handle,
      reservation_append_receipt: automatic.reservation_append_receipt,
    });
  const reservationResponse = await attempt.ledger
    .appendReservationStatusWriteResponse({
      reservation_status_intent_handle:
        reservationIntent.reservation_status_intent_handle,
      intent_append_receipt: reservationIntent.intent_append_receipt,
      receipt: reservationStatusWriteResponseReceipt(
        reservationIntent.transport,
        reservationIntent.intent_append_receipt.ref_reread.server_time,
      ),
    });
  const requestIntent = await attempt.ledger
    .appendAutomaticReviewRequestIntent({
      automatic_reservation_handle: automatic.automatic_reservation_handle,
      reservation_append_receipt: automatic.reservation_append_receipt,
      reservation_status_response_append: reservationResponse,
    });
  const requestReceipt = await automaticReviewRequestBindingReceipt({
    action: {
      method: "POST",
      request_body_sha256: rawDigest("@codex review"),
    },
    observedAt: requestIntent.intent_append_receipt.ref_reread.server_time,
  });
  const binding = await attempt.ledger.appendAutomaticReviewRequestBinding({
    automatic_request_intent_handle:
      requestIntent.automatic_request_intent_handle,
    intent_append_receipt: requestIntent.intent_append_receipt,
    receipt: requestReceipt,
  });
  const release = await releaseFixtureDiscoveryLease(
    attempt.ledger,
    attempt.discovery,
  );
  const records = (await attempt.ledger.load()).records;
  const stages = [
    [0, attempt.discovery.lease_receipt.acquire_commit_sha,
      "lease-acquired", "CANDIDATE_ATTEMPT_RECOVERED_AFTER_LEASE", 0,
      "not-required", "not-required", "not-required"],
    [1, schedulerAppend.append_receipt.commit_sha,
      "scheduler-observed", "CANDIDATE_ATTEMPT_RECOVERED_AFTER_SCHEDULER", 0,
      "not-required", "not-required", "not-required"],
    [2, statusIntent.intent_append_receipt.commit_sha,
      "status-intent", "CANDIDATE_STATUS_EFFECT_AMBIGUOUS", 1,
      "ambiguous", "not-required", "not-required"],
    [3, statusResponse.response_append_receipt.commit_sha,
      "status-response", "CANDIDATE_ATTEMPT_RECOVERED_AFTER_STATUS", 1,
      "bound", "not-required", "not-required"],
    [8, automatic.reservation_append_receipt.commit_sha,
      "automatic-reservation",
      "CANDIDATE_ATTEMPT_RECOVERED_AFTER_RESERVATION", 1,
      "bound", "not-required", "not-required"],
    [9, reservationIntent.intent_append_receipt.commit_sha,
      "reservation-status-intent",
      "CANDIDATE_RESERVATION_STATUS_EFFECT_AMBIGUOUS", 2,
      "bound", "ambiguous", "not-required"],
    [10, reservationResponse.response_append_receipt.commit_sha,
      "reservation-status-response",
      "CANDIDATE_ATTEMPT_RECOVERED_AFTER_RESERVATION_STATUS", 2,
      "bound", "bound", "not-required"],
    [11, requestIntent.attempt_append_receipt.commit_sha,
      "request-attempt",
      "CANDIDATE_ATTEMPT_RECOVERED_AFTER_REQUEST_ATTEMPT", 2,
      "bound", "bound", "not-required"],
    [12, requestIntent.intent_append_receipt.commit_sha,
      "review-request-intent", "CANDIDATE_REQUEST_EFFECT_AMBIGUOUS", 3,
      "bound", "bound", "ambiguous"],
    [13, binding.review_response_append_receipt.commit_sha,
      "review-request-response", "CANDIDATE_REQUEST_EFFECT_AMBIGUOUS", 3,
      "bound", "bound", "ambiguous"],
    [14, binding.request_binding_intent_append_receipt.commit_sha,
      "request-binding-intent", "CANDIDATE_REQUEST_EFFECT_AMBIGUOUS", 3,
      "bound", "bound", "ambiguous"],
    [15, binding.request_binding_response_append_receipt.commit_sha,
      "request-binding-response",
      "CANDIDATE_ATTEMPT_RECOVERED_AFTER_BINDING_RESPONSE", 3,
      "bound", "bound", "bound"],
    [16, release.commit_sha, "request-binding-response",
      "CANDIDATE_ATTEMPT_RECOVERED_AFTER_BINDING_RESPONSE", 3,
      "bound", "bound", "bound"],
  ];
  for (const [
    stage,
    commitSha,
    prefixPhase,
    failureCode,
    publicEffects,
    statusOutcome,
    reservationStatusOutcome,
    requestOutcome,
  ] of stages) {
    const recordIndex = records.findIndex((entry) =>
      entry.commit_sha === commitSha);
    assert.notEqual(recordIndex, -1, `stage ${stage} record is reachable`);
    const projection =
      classifyV2GitLedgerCandidateDispatchRecoveryPrefix({
        records: records.slice(0, recordIndex + 1),
        scheduled_scope_receipt: stage === 0
          ? attempt.preScopeReceipt
          : fullScopeReceipt,
      });
    assert.equal(projection.prefix_phase, prefixPhase, `stage ${stage}`);
    assert.equal(projection.failure_code, failureCode, `stage ${stage}`);
    assert.equal(
      projection.terminal_projection.public_effects_performed,
      publicEffects,
      `stage ${stage}`,
    );
    assert.equal(
      projection.terminal_projection.status_effect_outcome,
      statusOutcome,
      `stage ${stage}`,
    );
    assert.equal(
      projection.terminal_projection.reservation_status_effect_outcome,
      reservationStatusOutcome,
      `stage ${stage}`,
    );
    assert.equal(
      projection.terminal_projection.automatic_request_effect_outcome,
      requestOutcome,
      `stage ${stage}`,
    );
    assert.equal(
      projection.lease_release_record_oid,
      stage === 16 ? release.commit_sha : null,
      `stage ${stage}`,
    );
  }

  // Stages 4-9 are the mutually exclusive two-status recovery branch, so
  // they require their own real scheduled attempt rather than synthetic
  // records spliced into the automatic-request branch above.
  const recoveryAttempt =
    await createScheduledAutomaticRecoveryBranchAttempt({
      bindArtifactBeforeDispatch: false,
    });
  const recoveryIntent = await recoveryAttempt.ledger
    .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
      scheduler_append: recoveryAttempt.recovery.schedulerAppend,
      artifact_binding_candidate_handle:
        recoveryAttempt.recovery.artifactBindingCandidateHandle,
    });
  const recoveryHandles = await loadAutomaticRecoveryProviderArtifactHandles({
    ledger: recoveryAttempt.ledger,
    fixture: recoveryAttempt.fixture,
    transport: recoveryIntent.transport,
    snapshots: recoveryAttempt.recovery.snapshots,
  });
  const recoveryResponse = await recoveryAttempt.ledger
    .appendAutomaticRecoveryArtifactBindingResponse({
      artifact_binding_intent_handle:
        recoveryIntent.artifact_binding_intent_handle,
      intent_append_receipt: recoveryIntent.intent_append_receipt,
      provider_artifact_handles: recoveryHandles,
    });
  const recoveryRecords = (await recoveryAttempt.ledger.load()).records;
  const recoveryStages = [
    [4,
      recoveryAttempt.recovery.statusIntents[1]
        .intent_append_receipt.commit_sha,
      "status-intent", "CANDIDATE_STATUS_EFFECT_AMBIGUOUS", "ambiguous"],
    [5,
      recoveryAttempt.recovery.statusResponses[1]
        .response_append_receipt.commit_sha,
      "status-response", "CANDIDATE_ATTEMPT_RECOVERED_AFTER_STATUS", "bound"],
    [6, recoveryIntent.intent_append_receipt.commit_sha,
      "automatic-recovery-intent",
      "CANDIDATE_ATTEMPT_RECOVERED_DURING_AUTOMATIC_RECOVERY", "bound"],
    [7, recoveryIntent.ready_append_receipt.commit_sha,
      "automatic-recovery-ready",
      "CANDIDATE_ATTEMPT_RECOVERED_DURING_AUTOMATIC_RECOVERY", "bound"],
    [8, recoveryIntent.ready_confirmation_append_receipt.commit_sha,
      "automatic-recovery-ready-confirmed",
      "CANDIDATE_ATTEMPT_RECOVERED_DURING_AUTOMATIC_RECOVERY", "bound"],
    [9, recoveryResponse.response_append_receipt.commit_sha,
      "automatic-recovery-response",
      "CANDIDATE_ATTEMPT_RECOVERED_AFTER_AUTOMATIC_RECOVERY", "bound"],
  ];
  for (const [
    stage,
    commitSha,
    prefixPhase,
    failureCode,
    statusOutcome,
  ] of recoveryStages) {
    const recordIndex = recoveryRecords.findIndex((entry) =>
      entry.commit_sha === commitSha);
    assert.notEqual(recordIndex, -1, `stage ${stage} record is reachable`);
    const projection = classifyV2GitLedgerCandidateDispatchRecoveryPrefix({
      records: recoveryRecords.slice(0, recordIndex + 1),
      scheduled_scope_receipt:
        recoveryAttempt.discovery.effect_evaluated_scope_receipt,
    });
    assert.equal(projection.prefix_phase, prefixPhase, `stage ${stage}`);
    assert.equal(projection.failure_code, failureCode, `stage ${stage}`);
    assert.equal(
      projection.terminal_projection.public_effects_performed,
      2,
      `stage ${stage}`,
    );
    assert.equal(
      projection.terminal_projection.status_effect_outcome,
      statusOutcome,
      `stage ${stage}`,
    );
    assert.equal(
      projection.terminal_projection.automatic_request_effect_outcome,
      "not-required",
      `stage ${stage}`,
    );
  }
});

test("candidate dispatch rejects a self-sealed forged ack before write and on restart", async () => {
  const source = await createScheduledCandidateDispatchAttempt();
  await releaseFixtureDiscoveryLease(source.ledger, source.discovery);
  source.fixture.failNextCandidateDispatchUpdate("batch-complete");
  await assert.rejects(
    source.ledger.recoverCandidateDispatchFailure({
      workflow_command_handle: source.scheduledCommand,
      trigger_identity: source.triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      expected_dispatch_binding: source.reserved.plan.items[0],
    }),
    (error) => error.code === "unexpected-http-status",
  );
  const sourceLoaded = await source.ledger.load();
  const sourceAck = sourceLoaded.records.at(-1);
  assert.equal(sourceAck.envelope.record_type,
    "candidate-dispatch-observation");
  assert.equal(sourceAck.envelope.payload.phase, "candidate-ack");
  const forgedPayload = forgeCandidateDispatchAckPayload(
    sourceAck.envelope.payload,
  );

  const target = await createScheduledCandidateDispatchAttempt();
  await releaseFixtureDiscoveryLease(target.ledger, target.discovery);
  const targetLoaded = await target.ledger.load();
  assert.equal(
    targetLoaded.tip_commit_sha,
    sourceAck.envelope.predecessor_commit_sha,
    "the independent target must expose the same deterministic pre-ack history",
  );
  const forgedRecord = createV2GitLedgerCandidateDispatchRecord({
    predecessor_commit_sha: targetLoaded.tip_commit_sha,
    server_observed_at: targetLoaded.post_ref.server_time,
    payload: forgedPayload,
  });
  const forgedEvaluated =
    createV2GitLedgerCandidateDispatchEvaluatedScopeReceipt({
      repository: REPOSITORY,
      payload: forgedPayload,
      trigger_identity: target.triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    });
  const writesBeforeForgedAppend = target.fixture.writeCalls;
  const tipBeforeForgedAppend = target.fixture.refTarget;
  await assert.rejects(
    target.ledger.appendRecord(forgedRecord, {
      evaluated_scope_receipt: forgedEvaluated,
    }),
    /terminal|public effects|reachable history/u,
  );
  assert.equal(target.fixture.writeCalls, writesBeforeForgedAppend);
  assert.equal(target.fixture.refTarget, tipBeforeForgedAppend);

  source.fixture.rewriteTipEnvelope((envelope) => {
    resealFixtureEnvelopePayload(
      envelope,
      forgeCandidateDispatchAckPayload(envelope.payload),
    );
  });
  const restarted = makeLedger(
    source.fixture,
    source.capability,
    source.verifier,
    source.preflight,
  );
  await assert.rejects(
    restarted.load(),
    /terminal|public effects|reachable history/u,
  );
});

test("candidate dispatch rejects a self-sealed intent-only terminal before write and on restart",
  async () => {
    const createIntentOnlyAttempt = async () => {
      const attempt = await createScheduledAutomaticRecoveryBranchAttempt({
        bindArtifactBeforeDispatch: false,
      });
      attempt.fixture.failRereadAfterEffectUpdate("artifact-binding", 1);
      await assert.rejects(
        attempt.ledger
          .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
            scheduler_append: attempt.recovery.schedulerAppend,
            artifact_binding_candidate_handle:
              attempt.recovery.artifactBindingCandidateHandle,
          }),
        (error) => error.code === "unexpected-http-status",
      );
      await releaseFixtureDiscoveryLease(attempt.ledger, attempt.discovery);
      return attempt;
    };

    const source = await createIntentOnlyAttempt();
    source.fixture.failNextCandidateDispatchUpdate("batch-complete");
    await assert.rejects(
      source.ledger.recoverCandidateDispatchFailure({
        workflow_command_handle: source.scheduledCommand,
        trigger_identity: source.triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
        expected_dispatch_binding: source.reserved.plan.items[0],
      }),
      (error) => error.code === "unexpected-http-status",
    );
    const sourceLoaded = await source.ledger.load();
    const sourceAck = sourceLoaded.records.at(-1);
    assert.equal(sourceAck.envelope.payload.phase, "candidate-ack");
    assert.equal(
      sourceAck.envelope.payload.candidate_ack.terminal_authority.kind,
      "durable-prefix-recovery",
    );
    const forgedPayload = forgeCandidateDispatchAckAsControllerTerminal(
      sourceAck.envelope.payload,
    );

    const target = await createIntentOnlyAttempt();
    const targetLoaded = await target.ledger.load();
    assert.equal(
      targetLoaded.tip_commit_sha,
      sourceAck.envelope.predecessor_commit_sha,
      "the target must expose the same deterministic intent-only history",
    );
    const forgedRecord = createV2GitLedgerCandidateDispatchRecord({
      predecessor_commit_sha: targetLoaded.tip_commit_sha,
      server_observed_at: targetLoaded.post_ref.server_time,
      payload: forgedPayload,
    });
    const forgedEvaluated =
      createV2GitLedgerCandidateDispatchEvaluatedScopeReceipt({
        repository: REPOSITORY,
        payload: forgedPayload,
        trigger_identity: target.triggerIdentity,
        repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      });
    const writesBefore = target.fixture.writeCalls;
    const tipBefore = target.fixture.refTarget;
    await assert.rejects(
      target.ledger.appendRecord(forgedRecord, {
        evaluated_scope_receipt: forgedEvaluated,
      }),
      /closed production chain/u,
    );
    assert.equal(target.fixture.writeCalls, writesBefore);
    assert.equal(target.fixture.refTarget, tipBefore);

    source.fixture.rewriteTipEnvelope((envelope) => {
      resealFixtureEnvelopePayload(
        envelope,
        forgeCandidateDispatchAckAsControllerTerminal(envelope.payload),
      );
    });
    const restarted = makeLedger(
      source.fixture,
      source.capability,
      source.verifier,
      source.preflight,
    );
    await assert.rejects(restarted.load(), /closed production chain/u);
  });

test("candidate dispatch restart completes a partial final batch without redispatch", async () => {
  const attempt = await createScheduledCandidateDispatchAttempt();
  await releaseFixtureDiscoveryLease(attempt.ledger, attempt.discovery);
  attempt.fixture.failNextCandidateDispatchUpdate("cycle-complete");
  await assert.rejects(
    attempt.ledger.recoverCandidateDispatchFailure({
      workflow_command_handle: attempt.scheduledCommand,
      trigger_identity: attempt.triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      expected_dispatch_binding: attempt.reserved.plan.items[0],
    }),
    (error) => error.code === "unexpected-http-status",
  );
  const partial = await attempt.ledger.load();
  const partialCycle = partial.authority_projection.candidate_dispatch
    .current_cycle;
  assert.equal(partialCycle.active_reservation, null);
  assert.equal(partialCycle.completed_batches.length, partialCycle.batch_count);
  assert.equal(partialCycle.cycle_complete, false);
  assert.equal(partial.records.filter((entry) =>
    entry.envelope.record_type === "candidate-dispatch-observation" &&
    entry.envelope.payload.phase === "candidate-ack").length, 1);
  assert.equal(partial.records.at(-1).envelope.payload.phase, "batch-complete");

  const unrelatedMinimal = await loadFixtureMinimalScope();
  const unrelatedPreScope = await attempt.ledger
    .createPullRequestEventEvaluatedScopeReceipt({
      minimal_scope_handle: unrelatedMinimal,
      trigger_identity: {
        event_name: "pull_request_target",
        ref: "refs/heads/main",
        sha: BASE,
      },
    });
  const writesBeforeUnrelatedAttempt = attempt.fixture.writeCalls;
  await assert.rejects(
    attempt.ledger.acquireLease({
      ...leaseInput(partial.tip_commit_sha),
      lease_id: "lease-dispatch-completion-gap",
    }, {
      evaluated_scope_receipt: unrelatedPreScope,
    }),
    (error) => error.code === "candidate-dispatch-write-window",
  );
  assert.equal(attempt.fixture.writeCalls, writesBeforeUnrelatedAttempt);
  assert.equal(attempt.fixture.refTarget, partial.tip_commit_sha);

  const changedCapability = scheduleCapabilityReceipt({
    allowedEventNames: ["schedule", "workflow_dispatch"],
  });
  const restricted = createV2GitHubGitLedgerBootstrap({
    fetch: attempt.fixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: REPOSITORY,
    ledgerRef: V2_GIT_LEDGER_REF,
    restBaseUrl: "https://api.github.test",
    bootstrapCapabilityInput: bootstrapInputForCapability(changedCapability),
    verifyWorkflowProvenance: attempt.verifier,
  });
  await assert.rejects(
    restricted.bootstrapCapability(),
    (error) => error.code === "candidate-dispatch-capability-drift",
  );
  assert.equal(attempt.fixture.writeCalls, writesBeforeUnrelatedAttempt);
  assert.equal(attempt.fixture.refTarget, partial.tip_commit_sha);

  attempt.runIdentity.run_id = "9003";
  attempt.runIdentity.run_attempt = 1;
  const restarted = makeLedger(
    attempt.fixture,
    attempt.capability,
    attempt.verifier,
    attempt.preflight,
  );
  const completed = await restarted.loadOrReserveCandidateDispatch({
    workflow_command_handle: await createScheduleWorkflowCommandHandle({
      runIdentity: attempt.runIdentity,
    }),
    trigger_identity: attempt.triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });
  assert.equal(completed.state, "complete");
  assert.equal(completed.plan, null);
  assert.equal(completed.candidate_dispatch_handle, null);
  const final = await restarted.load();
  assert.equal(final.commit_count, partial.commit_count + 1);
  assert.equal(final.records.at(-1).envelope.payload.phase, "cycle-complete");
  assert.equal(final.records.filter((entry) =>
    entry.envelope.record_type === "candidate-dispatch-observation" &&
    entry.envelope.payload.phase === "reserve").length, 1);
  assert.equal(final.records.filter((entry) =>
    entry.envelope.record_type === "candidate-dispatch-observation" &&
    entry.envelope.payload.phase === "candidate-ack").length, 1);
  assert.equal(
    final.authority_projection.candidate_dispatch.current_cycle.cycle_complete,
    true,
  );
});

test("candidate dispatch scan exposes pending then expired recovery without redispatch", async () => {
  const attempt = await createScheduledCandidateDispatchAttempt();
  attempt.runIdentity.run_id = "9003";
  attempt.runIdentity.run_attempt = 1;
  const recoveryScanCommand = await createScheduleWorkflowCommandHandle({
    runIdentity: attempt.runIdentity,
  });
  const writesBeforePending = attempt.fixture.writeCalls;
  const pending = await attempt.ledger.loadOrReserveCandidateDispatch({
    workflow_command_handle: recoveryScanCommand,
    trigger_identity: attempt.triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });
  assert.equal(pending.state, "recovery-required");
  assert.equal(pending.plan, null);
  assert.equal(pending.candidate_dispatch_handle, null);
  assert.equal(pending.recovery_required.ready, false);
  assert.equal(pending.recovery_required.mode, "pending-expiry");
  assert.equal(
    pending.recovery_required.ready_at,
    attempt.discovery.lease_receipt.expires_at,
  );
  assert.deepEqual(
    pending.recovery_required.expected_dispatch_binding,
    attempt.reserved.plan.items[0],
  );
  assert.equal(attempt.fixture.writeCalls, writesBeforePending);
  await assert.rejects(
    attempt.ledger.recoverCandidateDispatchFailure({
      workflow_command_handle: recoveryScanCommand,
      trigger_identity: attempt.triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
      expected_dispatch_binding: attempt.reserved.plan.items[0],
    }),
    (error) => error.code === "candidate-dispatch-recovery-not-ready",
  );
  assert.equal(attempt.fixture.writeCalls, writesBeforePending);

  attempt.fixture.advanceServerTime(1_000);
  const restartedLedger = makeLedger(
    attempt.fixture,
    attempt.capability,
    attempt.verifier,
    attempt.preflight,
  );
  const ready = await restartedLedger.loadOrReserveCandidateDispatch({
    workflow_command_handle: recoveryScanCommand,
    trigger_identity: attempt.triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });
  assert.equal(ready.state, "recovery-required");
  assert.equal(ready.plan, null);
  assert.equal(ready.recovery_required.ready, true);
  assert.equal(ready.recovery_required.mode, "expired");
  assert.equal(
    ready.recovery_required.ready_at,
    attempt.discovery.lease_receipt.expires_at,
  );
  assert.equal(attempt.fixture.writeCalls, writesBeforePending);
  const recovered = await restartedLedger.recoverCandidateDispatchFailure({
    workflow_command_handle: recoveryScanCommand,
    trigger_identity: attempt.triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    expected_dispatch_binding: ready.recovery_required
      .expected_dispatch_binding,
  });
  assert.equal(recovered.recovery_mode, "expired");
  assert.equal(recovered.result.outcome, "failed");
  assert.equal(recovered.result.public_effects_performed, 0);
  assert.equal(recovered.cycle_complete, true);
  const loaded = await restartedLedger.load();
  assert.equal(loaded.active_lease, null);
  const ack = loaded.records.find((entry) =>
    entry.envelope.record_type === "candidate-dispatch-observation" &&
    entry.envelope.payload.phase === "candidate-ack");
  assert.equal(
    ack.envelope.payload.candidate_ack.terminal_authority
      .lease_release_record_oid,
    null,
  );
  assert.equal(
    ack.envelope.payload.command_authority.pull_request_number,
    null,
  );
});

test("discovery continuity receipt closes full and minimal evidence", async () => {
  const fixture = githubGitFixture();
  const ledger = makeLedger(fixture);
  await ledger.bootstrapCapability();
  const preScopeReceipt = await ledger.defaultEvaluatedScopeReceipt();
  const loaded = await ledger.load();
  const lease = await ledger.acquireLease(
    leaseInput(loaded.tip_commit_sha),
    { evaluated_scope_receipt: preScopeReceipt },
  );
  const minimalPre = await loadFixtureMinimalScope();
  const transportFixture = providerTransportFixture();
  const snapshot = await createProviderTransport(transportFixture.fetch)
    .loadSnapshot({
      owner: REPOSITORY.owner,
      repo: REPOSITORY.name,
      pullNumber: PR.number,
      artifactSelectors: [{ kind: "issue_comment", id: "71" }],
      permissionSubjects: [],
    });
  const minimalPost = minimalScopeReceiptAt(
    minimalPre,
    "2026-08-13T12:20:00.000Z",
  );
  const input = {
    repository: REPOSITORY,
    scope: preScopeReceipt.scope,
    pre_scope_receipt_digest: preScopeReceipt.receipt_digest,
    lease_receipt: lease,
    discovery_snapshot: snapshot,
    transport_limits: V2_TRANSPORT_DEFAULT_LIMITS,
    minimal_pre: minimalPre,
    minimal_post: minimalPost,
  };
  const receipt = createV2GitLedgerDiscoveryContinuityReceipt(input);
  assert.equal(
    receipt.schema,
    V2_GIT_LEDGER_DISCOVERY_CONTINUITY_RECEIPT_SCHEMA,
  );
  assert.equal(receipt.full_snapshot.snapshot_schema_version, 2);
  assert.equal(
    receipt.full_snapshot.scope_pre.scope_receipt.endpoint_receipts
      .pull_request.path,
    "/repos/owner/repo/pulls/7",
  );
  assert.equal(
    receipt.minimal_pre.endpoint_receipts.length,
    minimalPre.endpoint_receipts.length,
  );
  assert.deepEqual(
    receipt.full_snapshot.summary.transport_limits,
    V2_TRANSPORT_DEFAULT_LIMITS,
  );
  assert.deepEqual(
    validateV2GitLedgerDiscoveryContinuityReceipt(receipt, {
      repository: REPOSITORY,
      scope: preScopeReceipt.scope,
      pre_scope_receipt_digest: preScopeReceipt.receipt_digest,
      lease_receipt: lease,
    }),
    receipt,
  );

  const narrower = createV2GitLedgerDiscoveryContinuityReceipt({
    ...input,
    transport_limits: {
      ...V2_TRANSPORT_DEFAULT_LIMITS,
      max_pages: V2_TRANSPORT_DEFAULT_LIMITS.max_pages - 1,
    },
  });
  assert.equal(
    narrower.full_snapshot.summary.transport_limits.max_pages,
    V2_TRANSPORT_DEFAULT_LIMITS.max_pages - 1,
  );
  assert.throws(
    () => createV2GitLedgerDiscoveryContinuityReceipt({
      ...input,
      transport_limits: {
        ...V2_TRANSPORT_DEFAULT_LIMITS,
        max_requests: V2_TRANSPORT_DEFAULT_LIMITS.max_requests + 1,
      },
    }),
    /transport limits\.max_requests is relaxed/u,
  );
  assert.throws(
    () => createV2GitLedgerDiscoveryContinuityReceipt({
      ...input,
      minimal_post: minimalScopeReceiptAt(
        minimalPre,
        "2026-08-12T12:00:00.000Z",
      ),
    }),
    /server time regressed/u,
  );
  const tampered = structuredClone(receipt);
  tampered.minimal_pre.endpoint_receipts[0].raw_body_sha256 =
    `sha256:${"f".repeat(64)}`;
  assert.throws(
    () => validateV2GitLedgerDiscoveryContinuityReceipt(tampered),
    /endpoint receipt digest is invalid/u,
  );
});

test("full discovery factory accepts only one branded leased continuity", async () => {
  const fixture = githubGitFixture();
  const preflight = await loadFixtureProviderPreflight();
  const capability = capabilityReceiptForTrigger(
    "pull_request_target",
    "refs/heads/main",
  );
  const ledger = makeLedger(fixture, capability, provenanceVerifier({
    eventName: "pull_request_target",
    ref: "refs/heads/main",
    shaValue: BASE,
  }), preflight);
  await ledger.bootstrapCapability();
  const minimalPre = await loadFixtureMinimalScope();
  const preScopeReceipt = await ledger
    .createPullRequestEventEvaluatedScopeReceipt({
      minimal_scope_handle: minimalPre,
      trigger_identity: {
        event_name: "pull_request_target",
        ref: "refs/heads/main",
        sha: BASE,
      },
    });
  const command = await createPullRequestTargetWorkflowCommandHandle();
  const discovery = await acquireFixtureLeasedDiscovery({
    ledger,
    command,
    preScopeReceipt,
    minimalPre,
  });
  assert.equal(discovery.lease_evaluated_scope_receipt.phase, "pre-scope");
  assert.equal(discovery.full_evaluated_scope_receipt.phase, "full-discovery");
  assert.equal(
    discovery.full_evaluated_scope_receipt.discovery_continuity_receipt
      .continuity_receipt_digest,
    discovery.continuity_authority.continuity_receipt
      .continuity_receipt_digest,
  );

  const writesBeforeForgery = fixture.writeCalls;
  await assert.rejects(
    ledger.createFullDiscoveryEvaluatedScopeReceipt({
      pre_scope_receipt: preScopeReceipt,
      lease_receipt: discovery.lease_receipt,
      continuity_handle: structuredClone(discovery.continuity_authority),
    }),
    (error) => error.code === "UNTRUSTED_LEASED_DISCOVERY_CONTINUITY_HANDLE",
  );
  await assert.rejects(
    ledger.createFullDiscoveryEvaluatedScopeReceipt({
      pre_scope_receipt: structuredClone(preScopeReceipt),
      lease_receipt: discovery.lease_receipt,
      continuity_handle: discovery.continuity_authority,
    }),
    (error) => error.code === "untrusted-pre-scope-receipt",
  );
  await assert.rejects(
    ledger.createFullDiscoveryEvaluatedScopeReceipt({
      pre_scope_receipt: preScopeReceipt,
      lease_receipt: structuredClone(discovery.lease_receipt),
      continuity_handle: discovery.continuity_authority,
    }),
    (error) => error.code === "LEASED_DISCOVERY_CONTINUITY_BINDING_MISMATCH",
  );
  await assert.rejects(
    ledger.createProviderEventFullDiscoveryEvaluatedScopeReceipt({
      full_scope_receipt: discovery.full_evaluated_scope_receipt,
      continuity_handle: discovery.continuity_authority,
    }),
    (error) => error.code === "untrusted-provider-pre-scope-receipt",
  );
  assert.equal(fixture.writeCalls, writesBeforeForgery);
});

test("provider event authority is pre-read, leased, fully rediscovered, and exact-bound", async () => {
  const preflight = await loadFixtureProviderPreflight();
  const fixture = githubGitFixture();
  const capability = capabilityReceiptForTrigger(
    "issue_comment",
    "refs/heads/main",
  );
  const verifier = provenanceVerifier({
    eventName: "issue_comment",
    ref: "refs/heads/main",
    shaValue: HEAD,
  });
  const ledger = makeLedger(fixture, capability, verifier, preflight);
  await ledger.bootstrapCapability();
  const minimalScopeHandle = await loadFixtureMinimalScope();
  const transportFixture = providerTransportFixture();
  const providerArtifactHandle = await loadProviderPreScopeArtifact(
    transportFixture.fetch,
  );
  const triggerIdentity = {
    event_name: "issue_comment",
    ref: "refs/heads/main",
    sha: HEAD,
  };
  const writesBeforeUntrusted = fixture.writeCalls;
  await assert.rejects(
    ledger.createProviderEventPreScopeEvaluatedScopeReceipt({
      minimal_scope_handle: structuredClone(minimalScopeHandle),
      trigger_identity: triggerIdentity,
      provider_artifact_handle: providerArtifactHandle,
    }),
    (error) => error.code === "UNTRUSTED_MINIMAL_LIVE_SCOPE_HANDLE",
  );
  await assert.rejects(
    ledger.createProviderEventPreScopeEvaluatedScopeReceipt({
      minimal_scope_handle: minimalScopeHandle,
      trigger_identity: triggerIdentity,
      provider_artifact_handle: structuredClone(providerArtifactHandle),
    }),
    (error) => error.code === "UNTRUSTED_PROVIDER_PRE_SCOPE_ARTIFACT_HANDLE",
  );
  assert.equal(fixture.writeCalls, writesBeforeUntrusted);

  const preScopeReceipt = await ledger
    .createProviderEventPreScopeEvaluatedScopeReceipt({
      minimal_scope_handle: minimalScopeHandle,
      trigger_identity: triggerIdentity,
      provider_artifact_handle: providerArtifactHandle,
    });
  const loaded = await ledger.load();
  const writesBeforeClone = fixture.writeCalls;
  await assert.rejects(
    ledger.acquireLease(
      leaseInput(loaded.tip_commit_sha),
      { evaluated_scope_receipt: structuredClone(preScopeReceipt) },
    ),
    (error) => error.code === "untrusted-evaluated-scope-receipt",
  );
  assert.equal(fixture.writeCalls, writesBeforeClone);
  const command = await createProviderWorkflowCommandHandle();
  const discovery = await acquireFixtureLeasedDiscovery({
    ledger,
    command,
    preScopeReceipt,
    minimalPre: minimalScopeHandle,
    artifactSelectors: [{ kind: "issue_comment", id: "71" }],
  });
  const lease = discovery.lease_receipt;
  const fullScopeReceipt = discovery.effect_evaluated_scope_receipt;
  assert.equal(
    fullScopeReceipt.discovery_continuity_receipt.continuity_receipt_digest,
    discovery.continuity_authority.continuity_receipt
      .continuity_receipt_digest,
  );
  const effect = effectRecord({
    predecessor: lease.acquire_commit_sha,
    lease,
    effectId: "provider-read-observation",
    idempotencyKey: "provider-read-observation-key",
    at: TIME,
  });
  const writesBeforePreScopeEffect = fixture.writeCalls;
  await assert.rejects(
    ledger.appendRecord(effect, {
      evaluated_scope_receipt: preScopeReceipt,
    }),
    (error) => error.code === "UNTRUSTED_RAW_EFFECT_APPEND",
  );
  assert.equal(fixture.writeCalls, writesBeforePreScopeEffect);
  const controlPlaneAuthority = await ledger.loadControlPlaneAuthority(
    fullScopeReceipt.scope,
  );
  const initialAuthority = await ledger.loadInitialRunnerStateAuthority({
    control_plane_authority: controlPlaneAuthority,
    evaluated_scope_receipt: fullScopeReceipt,
    workflow_command_handle: command,
  });
  const observationPlan = schedulerObservationRecord({
    predecessor: lease.acquire_commit_sha,
    lease,
    priorAuthorityDigest: initialAuthority.prior_authority_digest,
    initialAuthority,
  }).payload.action;
  const schedulerAppend = await ledger.appendInitialSchedulerObservation({
    initial_runner_state_authority: initialAuthority,
    scheduler_evaluation: observationPlan.scheduler_evaluation,
    status_plan: observationPlan.status_plan,
  });
  const effectReceipt = schedulerAppend.append_receipt;
  const release = await ledger.releaseLease({
    predecessor_commit_sha: effectReceipt.commit_sha,
    lease_receipt: lease,
    released_at: TIME,
    control_comment_binding: null,
  }, {
    evaluated_scope_receipt: structuredClone(preScopeReceipt),
  });
  const final = await ledger.load();
  assert.equal(final.tip_commit_sha, release.commit_sha);
  assert.equal(final.active_lease, null);
  assert.equal(final.effect_intent_count, 1);
  assert.equal(
    final.records.at(-2).envelope.workflow_provenance.operation_binding
      .evaluated_scope_receipt.provider_artifact_receipt.phase,
    "full-discovery",
  );
});

test("provider full discovery cannot precede lease or change its native carrier", async (t) => {
  async function setup(transportFixture) {
    const preflight = await loadFixtureProviderPreflight();
    const fixture = githubGitFixture();
    const capability = capabilityReceiptForTrigger(
      "issue_comment",
      "refs/heads/main",
    );
    const verifier = provenanceVerifier({
      eventName: "issue_comment",
      ref: "refs/heads/main",
      shaValue: HEAD,
    });
    const ledger = makeLedger(fixture, capability, verifier, preflight);
    await ledger.bootstrapCapability();
    const minimalScopeHandle = await loadFixtureMinimalScope();
    const artifactHandle = await loadProviderPreScopeArtifact(
      transportFixture.fetch,
    );
    const preScopeReceipt = await ledger
      .createProviderEventPreScopeEvaluatedScopeReceipt({
        minimal_scope_handle: minimalScopeHandle,
        trigger_identity: {
          event_name: "issue_comment",
          ref: "refs/heads/main",
          sha: HEAD,
        },
        provider_artifact_handle: artifactHandle,
      });
    return { fixture, ledger, minimalScopeHandle, preScopeReceipt };
  }

  await t.test("full snapshot before lease", async () => {
    const transportFixture = providerTransportFixture({ startSecond: -180 });
    const { fixture, ledger, minimalScopeHandle, preScopeReceipt } =
      await setup(transportFixture);
    const snapshot = await createProviderTransport(transportFixture.fetch)
      .loadSnapshot({
        owner: REPOSITORY.owner,
        repo: REPOSITORY.name,
        pullNumber: PR.number,
        artifactSelectors: [{ kind: "issue_comment", id: "71" }],
        permissionSubjects: [],
      });
    const loaded = await ledger.load();
    const lease = await ledger.acquireLease(
      leaseInput(loaded.tip_commit_sha),
      { evaluated_scope_receipt: preScopeReceipt },
    );
    const writesBefore = fixture.writeCalls;
    assert.throws(
      () => createV2GitLedgerDiscoveryContinuityReceipt({
        repository: REPOSITORY,
        scope: preScopeReceipt.scope,
        pre_scope_receipt_digest: preScopeReceipt.receipt_digest,
        lease_receipt: lease,
        discovery_snapshot: snapshot,
        transport_limits: V2_TRANSPORT_DEFAULT_LIMITS,
        minimal_pre: minimalScopeHandle,
        minimal_post: minimalScopeReceiptAt(
          minimalScopeHandle,
          "2026-08-13T12:20:00.000Z",
        ),
      }),
      /server time regressed/u,
    );
    assert.equal(fixture.writeCalls, writesBefore);
  });

  await t.test("carrier edited after lease", async () => {
    const transportFixture = providerTransportFixture();
    const { fixture, ledger, minimalScopeHandle, preScopeReceipt } =
      await setup(transportFixture);
    transportFixture.setComment(providerIssueComment({
      body: "changed after lease",
      updated_at: "2026-08-13T12:09:00Z",
    }));
    await assert.rejects(
      acquireFixtureLeasedDiscovery({
        ledger,
        command: await createProviderWorkflowCommandHandle(),
        preScopeReceipt,
        minimalPre: minimalScopeHandle,
        artifactSelectors: [{ kind: "issue_comment", id: "71" }],
        transport: createProviderTransport(transportFixture.fetch),
      }),
      (error) => error.code === "LEASED_DISCOVERY_ABORTED",
    );
    const final = await ledger.load();
    assert.equal(final.active_lease, null);
    assert.equal(final.effect_intent_count, 0, "failed discovery never refunds into a POST");
  });
});

test("provider pre-scope rejects wrong identity, repository, PR, head, deletion, and policy", async (t) => {
  const cases = [
    ["actor", providerIssueComment({ user: { ...providerIssueComment().user, id: 1 } })],
    ["app", providerIssueComment({
      performed_via_github_app: {
        ...providerIssueComment().performed_via_github_app,
        id: 1,
      },
    })],
    ["repository", providerIssueComment({
      issue_url: "https://api.github.test/repos/other/repo/issues/7",
    })],
    ["pull request", providerIssueComment({
      issue_url: "https://api.github.test/repos/owner/repo/issues/8",
    })],
    ["deleted", null],
  ];
  for (const [name, comment] of cases) {
    await t.test(name, async () => {
      const transportFixture = providerTransportFixture({ comment });
      await assert.rejects(
        loadProviderPreScopeArtifact(transportFixture.fetch),
      );
    });
  }

  await t.test("head binding", async () => {
    const preflight = await loadFixtureProviderPreflight();
    const fixture = githubGitFixture();
    const capability = capabilityReceiptForTrigger(
      "issue_comment",
      "refs/heads/main",
    );
    const verifier = provenanceVerifier({
      eventName: "issue_comment",
      ref: "refs/heads/main",
      shaValue: HEAD,
    });
    const ledger = makeLedger(fixture, capability, verifier, preflight);
    await ledger.bootstrapCapability();
    const minimalScopeHandle = await loadFixtureMinimalScope();
    const transportFixture = providerTransportFixture();
    const wrongHeadHandle = await loadV2ProviderPreScopeArtifact({
      fetch: transportFixture.fetch,
      token: SYNTHETIC_BEARER,
      restBaseUrl: "https://api.github.test",
      owner: REPOSITORY.owner,
      repo: REPOSITORY.name,
      pullNumber: PR.number,
      headSha: BASE,
      selector: { kind: "issue_comment", id: "71" },
      expectedActor: CODEX_ACTOR,
      expectedApp: CODEX_APP,
    });
    const writesBefore = fixture.writeCalls;
    await assert.rejects(
      ledger.createProviderEventPreScopeEvaluatedScopeReceipt({
        minimal_scope_handle: minimalScopeHandle,
        trigger_identity: {
          event_name: "issue_comment",
          ref: "refs/heads/main",
          sha: HEAD,
        },
        provider_artifact_handle: wrongHeadHandle,
      }),
      (error) => error.code === "PROVIDER_PRE_SCOPE_HANDLE_BINDING_MISMATCH",
    );
    assert.equal(fixture.writeCalls, writesBefore);
  });

  await t.test("unbranded live authority", async () => {
    const preflight = await loadFixtureProviderPreflight();
    const fixture = githubGitFixture();
    const capability = capabilityReceiptForTrigger(
      "issue_comment",
      "refs/heads/main",
    );
    const verifier = provenanceVerifier({
      eventName: "issue_comment",
      ref: "refs/heads/main",
      shaValue: HEAD,
    });
    const ledger = makeLedger(
      fixture,
      capability,
      verifier,
      structuredClone(preflight),
    );
    await ledger.bootstrapCapability();
    const minimalScopeHandle = await loadFixtureMinimalScope();
    const transportFixture = providerTransportFixture();
    const artifactHandle = await loadProviderPreScopeArtifact(
      transportFixture.fetch,
    );
    const writesBefore = fixture.writeCalls;
    await assert.rejects(
      ledger.createProviderEventPreScopeEvaluatedScopeReceipt({
        minimal_scope_handle: minimalScopeHandle,
        trigger_identity: {
          event_name: "issue_comment",
          ref: "refs/heads/main",
          sha: HEAD,
        },
        provider_artifact_handle: artifactHandle,
      }),
      (error) => error.code === "UNTRUSTED_PREFLIGHT_HANDLE",
    );
    assert.equal(fixture.writeCalls, writesBefore);
  });

  await t.test("catalog policy mismatch", async () => {
    const preflight = await loadFixtureProviderPreflight();
    const base = capabilityBase();
    const wrongPolicy = providerIdentityPolicy();
    wrongPolicy.actor = { ...wrongPolicy.actor, id: "1" };
    delete wrongPolicy.catalog_digest;
    wrongPolicy.catalog_digest = digestCanonical(
      "codex-review-gate-v2-provider-identity-policy",
      wrongPolicy,
    );
    base.provider_identity_policy = wrongPolicy;
    const capability = sealCapability(base);
    const fixture = githubGitFixture();
    const ledger = makeLedger(fixture, capability, provenanceVerifier(), preflight);
    await ledger.bootstrapCapability();
    const minimalScopeHandle = await loadFixtureMinimalScope();
    const transportFixture = providerTransportFixture();
    const artifactHandle = await loadProviderPreScopeArtifact(
      transportFixture.fetch,
    );
    const writesBefore = fixture.writeCalls;
    await assert.rejects(
      ledger.createProviderEventPreScopeEvaluatedScopeReceipt({
        minimal_scope_handle: minimalScopeHandle,
        trigger_identity: {
          event_name: "pull_request",
          ref: "refs/pull/7/merge",
          sha: POTENTIAL,
        },
        provider_artifact_handle: artifactHandle,
      }),
      (error) => error.code === "provider-identity-policy-mismatch",
    );
    assert.equal(fixture.writeCalls, writesBefore);
  });
});

test("manual PR authority comes only from one protected workflow command and stays read-only", async () => {
  const command = await createManualWorkflowCommandHandle();
  const preflight = await loadFixtureProviderPreflight();
  const minimalScopeHandle = await loadFixtureMinimalScope();
  const fixture = githubGitFixture();
  const capability = capabilityReceiptForTrigger(
    "workflow_dispatch",
    "refs/heads/main",
  );
  const verifier = provenanceVerifier({
    eventName: "workflow_dispatch",
    ref: "refs/heads/main",
    shaValue: HEAD,
  });
  const ledger = makeLedger(fixture, capability, verifier, preflight);
  await ledger.bootstrapCapability();
  const triggerIdentity = {
    event_name: "workflow_dispatch",
    ref: "refs/heads/main",
    sha: HEAD,
  };
  const writesBeforeUntrusted = fixture.writeCalls;
  await assert.rejects(
    ledger.createManualPullRequestEvaluatedScopeReceipt({
      minimal_scope_handle: minimalScopeHandle,
      trigger_identity: triggerIdentity,
      workflow_command_handle: structuredClone(command),
    }),
    /protected descriptor-backed command/u,
  );
  await assert.rejects(
    ledger.createManualPullRequestEvaluatedScopeReceipt({
      minimal_scope_handle: minimalScopeHandle,
      trigger_identity: { ...triggerIdentity, event_name: "issue_comment" },
      workflow_command_handle: command,
    }),
    (error) => error.code === "manual-workflow-command-mismatch",
  );
  assert.equal(fixture.writeCalls, writesBeforeUntrusted);

  const manualReceipt = await ledger
    .createManualPullRequestEvaluatedScopeReceipt({
      minimal_scope_handle: minimalScopeHandle,
      trigger_identity: triggerIdentity,
      workflow_command_handle: command,
    });
  assert.equal(manualReceipt.selector.input_name, "pull-request");
  const underscoreSelection = createV2GitLedgerManualSelectionReceipt({
    source: "trusted-reusable-workflow-input",
    input_name: "pull_request_number",
    input_value: String(PR.number),
    command_receipt_digest: manualReceipt.selector.command_receipt_digest,
    scope: manualReceipt.scope,
  });
  assert.throws(
    () => createV2GitLedgerEvaluatedScopeReceipt({
      relation: "manual-pull-request",
      repository: REPOSITORY,
      scope: manualReceipt.scope,
      trigger_identity: triggerIdentity,
      selector: underscoreSelection,
      scope_endpoint_receipt: manualReceipt.scope_endpoint_receipt,
    }),
    /trusted explicit PR selection/u,
  );
  const plainManualReceipt = createV2GitLedgerEvaluatedScopeReceipt({
    relation: "manual-pull-request",
    repository: REPOSITORY,
    scope: manualReceipt.scope,
    trigger_identity: triggerIdentity,
    selector: manualReceipt.selector,
    scope_endpoint_receipt: manualReceipt.scope_endpoint_receipt,
  });
  const loaded = await ledger.load();
  const writesBeforePlain = fixture.writeCalls;
  await assert.rejects(
    ledger.acquireLease(leaseInput(loaded.tip_commit_sha), {
      evaluated_scope_receipt: plainManualReceipt,
    }),
    (error) => error.code === "untrusted-evaluated-scope-receipt",
  );
  assert.equal(fixture.writeCalls, writesBeforePlain);

  await assert.rejects(
    ledger.loadInitialRunnerStateAuthority({
      control_plane_authority: await ledger.loadControlPlaneAuthority(
        manualReceipt.scope,
      ),
      evaluated_scope_receipt: manualReceipt,
      workflow_command_handle: command,
    }),
    (error) => error.code === "full-discovery-receipt-required",
  );
  const discovery = await acquireFixtureLeasedDiscovery({
    ledger,
    command,
    preScopeReceipt: manualReceipt,
    minimalPre: minimalScopeHandle,
  });
  const lease = discovery.lease_receipt;
  const fullManualReceipt = discovery.effect_evaluated_scope_receipt;
  const manualControlPlaneAuthority = await ledger.loadControlPlaneAuthority(
    fullManualReceipt.scope,
  );
  const initialAuthority = await ledger.loadInitialRunnerStateAuthority({
    control_plane_authority: manualControlPlaneAuthority,
    evaluated_scope_receipt: fullManualReceipt,
    workflow_command_handle: command,
  });
  const beforeObservation = await ledger.load();
  const observationPlan = schedulerObservationRecord({
    predecessor: lease.acquire_commit_sha,
    lease,
    priorAuthorityDigest: priorRunnerAuthorityDigest(
      beforeObservation.records,
    ),
    trigger: "manual",
    effectId: "manual-scheduler-observation",
    idempotencyKey: "manual-scheduler-observation-key",
    initialAuthority,
  });
  const schedulerAppend = await ledger.appendInitialSchedulerObservation({
    initial_runner_state_authority: initialAuthority,
    scheduler_evaluation:
      observationPlan.payload.action.scheduler_evaluation,
    status_plan: observationPlan.payload.action.status_plan,
  });
  const observationRecord = schedulerAppend.record;
  const observation = schedulerAppend.append_receipt;
  assert.deepEqual(observationRecord.payload.action.scheduler_plan.actions, []);

  const writesBeforeAttempt = fixture.writeCalls;
  await assert.rejects(
    ledger.appendAutomaticRequestReservation({
      scheduler_append: schedulerAppend,
    }),
    (error) => error.code ===
      "automatic-reservation-scheduler-actions-required",
  );
  assert.equal(fixture.writeCalls, writesBeforeAttempt);

  const release = await ledger.releaseLease({
    predecessor_commit_sha: observation.commit_sha,
    lease_receipt: lease,
    released_at: TIME,
    control_comment_binding: null,
  }, {
    evaluated_scope_receipt: manualReceipt,
  });
  const final = await ledger.load();
  assert.equal(final.tip_commit_sha, release.commit_sha);
  assert.equal(final.active_lease, null);
  assert.equal(final.effect_intent_count, 1);
});

function scheduleCapabilityReceipt({
  allowedEventNames = ["schedule"],
} = {}) {
  const base = capabilityBase();
  base.workflow_provenance_policy.allowed_event_names =
    [...allowedEventNames].sort();
  base.workflow_provenance_policy.allowed_refs = ["refs/heads/main"];
  return sealCapability(base);
}

function candidateRepository() {
  return {
    owner: REPOSITORY.owner,
    name: REPOSITORY.name,
    id: REPOSITORY.id,
    node_id: REPOSITORY.node_id,
  };
}

function scheduleTriggerIdentity() {
  return {
    event_name: "schedule",
    ref: "refs/heads/main",
    sha: BASE,
  };
}

function scheduleRepositoryEndpointReceipt() {
  return {
    method: "GET",
    path: "/repos/owner/repo",
    status: 200,
    server_time: TIME,
    raw_body_sha256: `sha256:${"a".repeat(64)}`,
  };
}

function candidateDispatchTerminalResult({
  decision,
  schedulerAppend,
  leaseReleaseReceipt,
  preflight,
  continuityAuthority,
  controlPlaneReceipt,
  initialAuthority,
  establishedAuthority = null,
  runnerAuthority,
  statusIntent = null,
  statusResponse = null,
  statusIntents = null,
  statusResponses = null,
  statusAlreadyBound = false,
}) {
  const boundStatusIntents = statusIntents ??
    (statusIntent === null ? [] : [statusIntent]);
  const boundStatusResponses = statusResponses ??
    (statusResponse === null ? [] : [statusResponse]);
  const currentStatusBound = boundStatusIntents.length > 0 &&
    boundStatusResponses.length === boundStatusIntents.length;
  const statusBound = currentStatusBound || statusAlreadyBound;
  const finalStatusIntent = boundStatusIntents.at(-1) ?? null;
  const finalStatusResponse = boundStatusResponses.at(-1) ?? null;
  return {
    schema: "codex-review-gate-production-assembly-v2",
    schema_version: 1,
    decision,
    report: { decision },
    due_at: null,
    wakeup_hints: "",
    status_plan: null,
    request_plan: null,
    comment_plan: null,
    writes_performed: currentStatusBound,
    public_effects_performed: currentStatusBound
      ? boundStatusIntents.length
      : 0,
    effect_barrier: statusBound
      ? "STATUS_EFFECT_BOUND"
      : "NO_STATUS_EFFECT_REQUIRED",
    status_effect_outcome: statusBound ? "bound" : "not-required",
    status_ambiguity_code: null,
    status_intent_digest:
      finalStatusIntent?.status_intent_handle.intent_digest ?? null,
    status_response_runner_state_digest:
      finalStatusResponse?.runner_state_digest ?? null,
    reservation_status_effect_outcome: "not-required",
    reservation_status_ambiguity_code: null,
    automatic_reservation_digest: null,
    reservation_status_intent_digest: null,
    reservation_status_response_runner_state_digest: null,
    automatic_request_effect_outcome: "not-required",
    automatic_request_ambiguity_code: null,
    automatic_request_intent_digest: null,
    automatic_request_binding_runner_state_digest: null,
    authoritative_controlled_request: null,
    scheduler_append_receipt: structuredClone(schedulerAppend.append_receipt),
    lease_release_receipt: structuredClone(leaseReleaseReceipt),
    preflight_receipt_digest: preflight.receipt_digest,
    handoff_digest: digestCanonical(
      "candidate-dispatch-test-handoff",
      { runner_authority_digest: runnerAuthority.authority_digest },
    ),
    continuity_receipt_digest:
      continuityAuthority.continuity_receipt.continuity_receipt_digest,
    control_plane_receipt_digest: controlPlaneReceipt.receipt_digest,
    initial_runner_state_authority_digest:
      initialAuthority?.authority_digest ?? null,
    established_runner_state_authority_digest:
      establishedAuthority?.authority_digest ?? null,
    runner_authority_digest: runnerAuthority.authority_digest,
  };
}

function forgeCandidateDispatchAckPayload(payloadValue) {
  const payload = structuredClone(payloadValue);
  const ack = payload.candidate_ack;
  ack.terminal_authority.terminal_projection.public_effects_performed += 1;
  ack.terminal_authority.terminal_projection_digest = digestCanonical(
    "codex-review-gate-v2-candidate-dispatch-terminal-projection",
    ack.terminal_authority.terminal_projection,
  );
  const {
    authority_digest: _terminalDigest,
    ...terminalWithoutDigest
  } = ack.terminal_authority;
  ack.terminal_authority.authority_digest = digestCanonical(
    "codex-review-gate-v2-candidate-dispatch-terminal-authority",
    terminalWithoutDigest,
  );
  ack.result.controller_authority_digest =
    ack.terminal_authority.authority_digest;
  ack.result.controller_result_digest =
    ack.terminal_authority.terminal_projection_digest;
  ack.result.public_effects_performed =
    ack.terminal_authority.terminal_projection.public_effects_performed;
  const { result_digest: _resultDigest, ...resultWithoutDigest } = ack.result;
  ack.result.result_digest = digestCanonical(
    "codex-review-gate-v2-candidate-dispatch-result",
    resultWithoutDigest,
  );
  const { ack_digest: _ackDigest, ...ackWithoutDigest } = ack;
  ack.ack_digest = digestCanonical(
    "codex-review-gate-v2-candidate-dispatch-ack",
    ackWithoutDigest,
  );
  return payload;
}

function forgeCandidateDispatchAckAsControllerTerminal(payloadValue) {
  const payload = structuredClone(payloadValue);
  const ack = payload.candidate_ack;
  const terminal = ack.terminal_authority;
  terminal.kind = "controller-terminal";
  terminal.recovery = null;
  const {
    authority_digest: _terminalDigest,
    ...terminalWithoutDigest
  } = terminal;
  terminal.authority_digest = digestCanonical(
    "codex-review-gate-v2-candidate-dispatch-terminal-authority",
    terminalWithoutDigest,
  );
  const projection = terminal.terminal_projection;
  const ambiguityCode = projection.automatic_request_ambiguity_code ??
    projection.reservation_status_ambiguity_code ??
    projection.status_ambiguity_code;
  const ambiguous = [
    projection.status_effect_outcome,
    projection.reservation_status_effect_outcome,
    projection.automatic_request_effect_outcome,
  ].includes("ambiguous");
  const blocked = new Set([
    "not-selected", "blocked-configuration", "blocked-input",
  ]).has(projection.decision);
  ack.result.controller_authority_digest = terminal.authority_digest;
  ack.result.controller_result_digest = terminal.terminal_projection_digest;
  ack.result.decision = projection.decision;
  ack.result.public_effects_performed =
    projection.public_effects_performed;
  ack.result.outcome = ambiguous ? "failed" : blocked ? "blocked" : "success";
  ack.result.failure_code = ambiguous ? ambiguityCode : null;
  const { result_digest: _resultDigest, ...resultWithoutDigest } = ack.result;
  ack.result.result_digest = digestCanonical(
    "codex-review-gate-v2-candidate-dispatch-result",
    resultWithoutDigest,
  );
  const { ack_digest: _ackDigest, ...ackWithoutDigest } = ack;
  ack.ack_digest = digestCanonical(
    "codex-review-gate-v2-candidate-dispatch-ack",
    ackWithoutDigest,
  );
  return payload;
}

function resealFixtureEnvelopePayload(envelope, payload) {
  envelope.payload = structuredClone(payload);
  envelope.payload_digest = digestV2GitLedgerPayload(envelope.payload);
  const provenance = envelope.workflow_provenance;
  const binding = structuredClone(provenance.operation_binding);
  const priorEvaluated = binding.evaluated_scope_receipt;
  binding.evaluated_scope_receipt =
    createV2GitLedgerCandidateDispatchEvaluatedScopeReceipt({
      repository: envelope.repository,
      payload: envelope.payload,
      trigger_identity: {
        event_name: priorEvaluated.trigger_event_name,
        ref: priorEvaluated.trigger_ref,
        sha: priorEvaluated.trigger_sha,
      },
      repository_endpoint_receipt:
        priorEvaluated.scope_endpoint_receipt,
    });
  binding.record_identity.payload_digest = envelope.payload_digest;
  const {
    request_digest: _requestDigest,
    audience: _audience,
    nonce: _nonce,
    ...requestFields
  } = binding;
  const nonceInput = {
    schema: V2_GIT_LEDGER_PROVENANCE_REQUEST_SCHEMA,
    schema_version: 1,
    ...requestFields,
  };
  binding.nonce = digestCanonical(
    "codex-review-gate-v2-git-ledger-provenance-nonce",
    nonceInput,
  );
  binding.audience = `${V2_GIT_LEDGER_OIDC_AUDIENCE}:` +
    binding.nonce.slice("sha256:".length);
  binding.request_digest = digestCanonical(
    "codex-review-gate-v2-git-ledger-provenance-request",
    {
      ...nonceInput,
      nonce: binding.nonce,
      audience: binding.audience,
    },
  );
  provenance.audience = binding.audience;
  provenance.claims.aud = binding.audience;
  provenance.operation_binding = binding;
  const { receipt_digest: _receiptDigest, ...provenanceWithoutDigest } =
    provenance;
  provenance.receipt_digest = digestCanonical(
    "codex-review-gate-v2-git-ledger-provenance",
    provenanceWithoutDigest,
  );
  const { envelope_digest: _envelopeDigest, ...envelopeWithoutDigest } =
    envelope;
  envelope.envelope_digest = digestCanonical(
    "codex-review-gate-v2-git-ledger-envelope",
    envelopeWithoutDigest,
  );
}

function resealFixtureProductionEnvelopePayload(envelope, payload) {
  envelope.payload = structuredClone(payload);
  envelope.payload_digest = digestV2GitLedgerPayload(envelope.payload);
  envelope.workflow_provenance.operation_binding.record_identity
    .payload_digest = envelope.payload_digest;
  resealProvenance(envelope.workflow_provenance);
  resealEnvelope(envelope);
}

function resealFixtureCandidateInventoryEnvelopePayload(envelope, payload) {
  envelope.payload = structuredClone(payload);
  envelope.payload_digest = digestV2GitLedgerPayload(envelope.payload);
  const binding = envelope.workflow_provenance.operation_binding;
  const evaluated = binding.evaluated_scope_receipt;
  binding.evaluated_scope_receipt =
    createV2GitLedgerCandidateInventoryEvaluatedScopeReceipt({
      repository: envelope.repository,
      payload: envelope.payload,
      trigger_identity: {
        event_name: evaluated.trigger_event_name,
        ref: evaluated.trigger_ref,
        sha: evaluated.trigger_sha,
      },
      repository_endpoint_receipt: evaluated.scope_endpoint_receipt,
    });
  binding.record_identity.payload_digest = envelope.payload_digest;
  resealProvenance(envelope.workflow_provenance);
  resealEnvelope(envelope);
  if (Object.hasOwn(envelope, "public_envelope_digest")) {
    delete envelope.public_envelope_digest;
    envelope.public_envelope_digest = digestCanonical(
      "codex-review-gate-v2-git-ledger-public-envelope",
      envelope,
    );
  }
}

function resealFixtureAutomaticRecoveryCandidateAuthority(
  authorityValue,
  candidatesValue,
) {
  const authority = structuredClone(authorityValue);
  authority.candidates = candidatesValue.map((candidateValue, index) => {
    const candidate = structuredClone(candidateValue);
    candidate.index = index;
    candidate.count = candidatesValue.length;
    delete candidate.candidate_digest;
    candidate.candidate_digest = runnerDigestCanonical(
      "codex-review-gate-v2-automatic-recovery-artifact-binding-candidate",
      candidate,
    );
    return candidate;
  });
  delete authority.authority_digest;
  authority.authority_digest = runnerDigestCanonical(
    "codex-review-gate-v2-automatic-recovery-artifact-binding-candidate-authority",
    authority,
  );
  return authority;
}

function fixtureAutomaticRecoveryCandidateSetDigest(authority) {
  return digestCanonical(
    "codex-review-gate-v2-automatic-recovery-artifact-binding-candidate-set",
    {
      review_epoch_id: authority.review_epoch_id,
      generation_id: authority.generation_id,
      request_id: authority.request_id,
      request_node_id: authority.request_node_id,
      request_bound_at: authority.request_bound_at,
      request_binding_record_oid: authority.request_binding_record_oid,
      request_binding_receipt_digest:
        authority.request_binding_receipt_digest,
      expected_actor: structuredClone(authority.expected_actor),
      expected_app: structuredClone(authority.expected_app),
      scope: structuredClone(authority.scope),
      candidates: authority.candidates.map((candidate) => ({
        selector: structuredClone(candidate.selector),
        artifact_node_id: candidate.artifact_node_id,
        artifact_url: candidate.artifact_url,
        artifact_type: candidate.artifact_type,
        artifact_created_at: candidate.artifact_created_at,
        evidence_raw_body_sha256: candidate.evidence_raw_body_sha256,
      })),
    },
  );
}

async function appendCompletedCandidateInventory({
  ledger,
  transportFixture,
  priorInventory = null,
}) {
  const transport = createV2GitHubCandidateInventory({
    fetch: transportFixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: candidateRepository(),
    restBaseUrl: "https://github.example.test",
  });
  const initial = priorInventory === null
    ? await transport.scan()
    : await transport.scan({ prior_inventory: priorInventory });
  let loaded = await ledger.load();
  await appendCandidateInventoryPhase({
    ledger,
    loaded,
    phase: "cycle-start",
    initial,
  });
  const shards = [];
  for (let index = 0; index < initial.shards.length; index += 1) {
    loaded = await ledger.load();
    const shard = await transport.readShard({
      inventory: initial,
      shard_index: index,
    });
    shards.push(shard);
    await appendCandidateInventoryPhase({
      ledger,
      loaded,
      phase: "shard",
      initial,
      shard,
    });
  }
  loaded = await ledger.load();
  const finalInventory = await transport.scan({ prior_inventory: initial });
  const finalShards = await readCandidateShardRound(transport, finalInventory);
  const cycleReceipt = finalizeV2CandidateInventoryCycle({
    initial_inventory: initial,
    shard_receipts: shards,
    final_inventory: finalInventory,
    final_shard_receipts: finalShards,
  });
  await appendCandidateInventoryPhase({
    ledger,
    loaded,
    phase: "cycle-complete",
    initial,
    finalInventory,
    cycleReceipt,
  });
  return ledger.load();
}

async function readCandidateInventoryCycle({
  transportFixture,
  priorInventory = null,
}) {
  const transport = createV2GitHubCandidateInventory({
    fetch: transportFixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: candidateRepository(),
    restBaseUrl: "https://github.example.test",
  });
  const initialInventory = priorInventory === null
    ? await transport.scan()
    : await transport.scan({ prior_inventory: priorInventory });
  const shardReceipts = await readCandidateShardRound(
    transport,
    initialInventory,
  );
  const finalInventory = await transport.scan({
    prior_inventory: initialInventory,
  });
  const finalShardReceipts = await readCandidateShardRound(
    transport,
    finalInventory,
  );
  return {
    initial_inventory: initialInventory,
    shard_receipts: shardReceipts,
    final_inventory: finalInventory,
    final_shard_receipts: finalShardReceipts,
  };
}

async function readCandidateInventoryAttempt({
  transportFixture,
  priorInventory = null,
  initialInventory = null,
}) {
  const transport = createV2GitHubCandidateInventory({
    fetch: transportFixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: candidateRepository(),
    restBaseUrl: "https://github.example.test",
  });
  const initial = initialInventory ?? (priorInventory === null
    ? await transport.scan()
    : await transport.scan({ prior_inventory: priorInventory }));
  const shardReceipts = await readCandidateShardRound(transport, initial);
  const finalInventory = await transport.scan({ prior_inventory: initial });
  const finalShardReceipts = await readCandidateShardRound(
    transport,
    finalInventory,
  );
  return {
    initial_inventory: initial,
    shard_receipts: shardReceipts,
    final_inventory: finalInventory,
    final_shard_receipts: finalShardReceipts,
  };
}

async function readDriftingCandidateInventoryAttempt({
  request,
  transportFixture,
  finalNumbers,
  mutateBeforeFinal = null,
}) {
  const transport = createV2GitHubCandidateInventory({
    fetch: transportFixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: candidateRepository(),
    restBaseUrl: "https://github.example.test",
  });
  const initialInventory = request.initial_inventory ??
    (request.prior_inventory === null
      ? await transport.scan()
      : await transport.scan({
          prior_inventory: request.prior_inventory,
        }));
  const shardReceipts = await readCandidateShardRound(
    transport,
    initialInventory,
  );
  transportFixture.setListedNumbers(finalNumbers);
  mutateBeforeFinal?.();
  const finalInventory = await transport.scan({
    prior_inventory: initialInventory,
  });
  const finalShardReceipts = await readCandidateShardRound(
    transport,
    finalInventory,
  );
  return {
    initial_inventory: initialInventory,
    shard_receipts: shardReceipts,
    final_inventory: finalInventory,
    final_shard_receipts: finalShardReceipts,
  };
}

function maximumClosedCandidateInventoryCycle() {
  return directClosedCandidateInventoryCycle(25_599);
}

function directClosedCandidateInventoryCycle(candidateCount) {
  const candidates = Array.from({ length: candidateCount }, (_, index) => {
    const number = index + 1;
    return {
      id: String(1_000_000 + number),
      node_id: `PR_closed_${number}`,
      number,
      created_at: new Date(
        Date.parse("2026-08-01T00:00:00.000Z") + index * 1000,
      ).toISOString(),
    };
  });
  const initialInventory = directCandidateInventory({
    candidates,
    priorInventoryDigest: null,
  });
  const finalInventory = directCandidateInventory({
    candidates,
    priorInventoryDigest: initialInventory.receipt_digest,
  });
  return {
    initial_inventory: initialInventory,
    shard_receipts: directClosedCandidateShardReceipts(initialInventory),
    final_inventory: finalInventory,
    final_shard_receipts:
      directClosedCandidateShardReceipts(finalInventory),
  };
}

function directCandidateInventory({ candidates, priorInventoryDigest }) {
  const candidateDigest = candidateDigestCanonical(
    "codex-review-gate-v2-candidate-identities",
    candidates,
  );
  const pages = [];
  for (let offset = 0; offset < candidates.length; offset += 100) {
    const selected = candidates.slice(offset, offset + 100);
    const page = pages.length + 1;
    pages.push({
      page,
      path: "/repos/owner/repo/pulls?" +
        "state=all&sort=created&direction=asc&per_page=100&" +
        `page=${page}`,
      server_time: TIME,
      raw_body_sha256: rawDigest(`closed-candidate-page-${page}`),
      link_sha256: rawDigest(
        offset + selected.length < candidates.length
          ? `closed-candidate-page-${page + 1}`
          : "",
      ),
      item_count: selected.length,
      first_candidate: selected[0],
      last_candidate: selected.at(-1),
    });
  }
  const passes = [1, 2].map((pass) => ({
    pass,
    pages,
    candidates,
    candidate_digest: candidateDigest,
    high_watermark: candidates.at(-1),
    observed_at: TIME,
  }));
  const shards = [];
  for (let offset = 0; offset < candidates.length;
      offset += MAX_V2_CANDIDATES_PER_SHARD) {
    const selected = candidates.slice(
      offset,
      offset + MAX_V2_CANDIDATES_PER_SHARD,
    );
    const index = shards.length;
    shards.push({
      index,
      candidates: selected,
      shard_digest: candidateDigestCanonical(
        "codex-review-gate-v2-candidate-shard-selection",
        { index, candidates: selected },
      ),
      next_shard_index:
        offset + MAX_V2_CANDIDATES_PER_SHARD < candidates.length
          ? index + 1
          : null,
    });
  }
  const withoutDigest = {
    schema: V2_CANDIDATE_INVENTORY_SCHEMA,
    schema_version: 1,
    repository: candidateRepository(),
    query: {
      state: "all",
      sort: "created",
      direction: "asc",
      per_page: 100,
    },
    prior_inventory_digest: priorInventoryDigest,
    candidates,
    candidate_digest: candidateDigest,
    high_watermark: candidates.at(-1),
    retained_prior_candidate_ids: [],
    passes,
    shards,
    observed_at: TIME,
    stable: true,
    completeness: "stable-state-all-candidate-superset",
  };
  return validateV2CandidateInventory({
    ...withoutDigest,
    receipt_digest: candidateDigestCanonical(
      "codex-review-gate-v2-candidate-inventory",
      withoutDigest,
    ),
  });
}

function directClosedCandidateShardReceipts(inventory, receiptSalt = "") {
  const repository = candidateRepository();
  const repoIdentity = {
    id: repository.id,
    node_id: repository.node_id,
    full_name: `${repository.owner}/${repository.name}`,
  };
  return inventory.shards.map((shard) => {
    const observations = shard.candidates.map((candidate) => ({
      ...candidate,
      state: "closed",
      merged: false,
      merged_at: null,
      updated_at: TIME,
      draft: false,
      base: {
        ref: "main",
        sha: BASE,
        repo: repoIdentity,
      },
      head: {
        ref: "closed-candidate",
        sha: HEAD,
        repo: repoIdentity,
      },
      endpoint_receipt: {
        method: "GET",
        path: `/repos/owner/repo/pulls/${candidate.number}`,
        status: 200,
        server_time: TIME,
        raw_body_sha256: rawDigest(
          `closed-candidate-${candidate.number}${receiptSalt}`,
        ),
      },
    }));
    const withoutDigest = {
      schema: V2_CANDIDATE_SHARD_RECEIPT_SCHEMA,
      schema_version: 1,
      repository,
      inventory_receipt_digest: inventory.receipt_digest,
      shard_index: shard.index,
      shard_digest: shard.shard_digest,
      candidates: shard.candidates,
      observations,
      observed_at: TIME,
      stable: true,
    };
    return {
      ...withoutDigest,
      receipt_digest: candidateDigestCanonical(
        "codex-review-gate-v2-candidate-shard",
        withoutDigest,
      ),
    };
  });
}

function directCandidateShardReceiptWithEndpointTimes(receiptValue, times) {
  const receipt = structuredClone(receiptValue);
  assert.equal(receipt.observations.length, times.length);
  receipt.observations = receipt.observations.map((observation, index) => ({
    ...observation,
    endpoint_receipt: {
      ...observation.endpoint_receipt,
      server_time: times[index],
    },
  }));
  receipt.observed_at = times.at(-1);
  delete receipt.receipt_digest;
  return {
    ...receipt,
    receipt_digest: candidateDigestCanonical(
      "codex-review-gate-v2-candidate-shard",
      receipt,
    ),
  };
}

function resealFixtureCandidateInventoryAttemptManifest(
  manifestValue,
  evidence,
) {
  const manifest = structuredClone(manifestValue);
  const values = [
    evidence.initial_inventory,
    ...evidence.shard_receipts,
    evidence.final_inventory,
    ...evidence.final_shard_receipts,
  ];
  assert.equal(manifest.artifacts.length, values.length);
  let totalDecodedBytes = 0;
  let totalEncodedBytes = 0;
  const artifacts = manifest.artifacts.map((artifact, index) => {
    const value = values[index];
    const decoded = Buffer.from(canonicalJson(value), "utf8");
    const encoded = deflateRawSync(decoded, { level: 9 });
    assert.equal(artifact.chunks.length, 1);
    const chunk = {
      path: artifact.chunks[0].path,
      chunk_index: 0,
      byte_length: encoded.byteLength,
      blob_sha: gitObjectSha("blob", encoded),
      sha256: rawDigest(encoded),
    };
    totalDecodedBytes += decoded.byteLength;
    totalEncodedBytes += encoded.byteLength;
    return {
      ...artifact,
      receipt_digest: value.receipt_digest,
      observed_at: value.observed_at,
      decoded_byte_length: decoded.byteLength,
      decoded_sha256: rawDigest(decoded),
      encoded_byte_length: encoded.byteLength,
      encoded_sha256: rawDigest(encoded),
      chunk_count: 1,
      chunks: [chunk],
    };
  });
  const semanticArtifacts = artifacts.map((artifact) => ({
    role: artifact.role,
    shard_index: artifact.shard_index,
    receipt_digest: artifact.receipt_digest,
    observed_at: artifact.observed_at,
    decoded_byte_length: artifact.decoded_byte_length,
    decoded_sha256: artifact.decoded_sha256,
  }));
  const chunks = artifacts.flatMap((artifact) => artifact.chunks);
  const treeBytes = canonicalGitTreeBytes(chunks.map((chunk) => ({
    path: chunk.path,
    mode: "100644",
    type: "blob",
    sha: chunk.blob_sha,
  })));
  const withoutDigest = {
    ...manifest,
    attempt_evidence_digest: digestCanonical(
      "codex-review-gate-v2-candidate-inventory-attempt-evidence",
      {
        repository: manifest.repository,
        attempt_index: manifest.attempt_index,
        attempt_count: manifest.attempt_count,
        outcome: manifest.outcome,
        artifacts: semanticArtifacts,
      },
    ),
    artifact_count: artifacts.length,
    artifacts,
    chunk_count: chunks.length,
    total_encoded_bytes: totalEncodedBytes,
    total_decoded_bytes: totalDecodedBytes,
    attachment_tree_sha: gitObjectSha("tree", treeBytes),
    attachment_tree_sha256: rawDigest(treeBytes),
  };
  delete withoutDigest.manifest_digest;
  return {
    ...withoutDigest,
    manifest_digest: digestCanonical(
      "codex-review-gate-v2-candidate-inventory-attachment-manifest",
      withoutDigest,
    ),
  };
}

async function readIdentityDriftCandidateInventoryCycle({
  transportFixture,
  priorInventory,
  listedNumbersAfterInitial,
  mutateAfterInitial = null,
  expectedDriftCode = "CANDIDATE_INVENTORY_DRIFT",
}) {
  const transport = createV2GitHubCandidateInventory({
    fetch: transportFixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: candidateRepository(),
    restBaseUrl: "https://github.example.test",
  });
  const driftInitial = await transport.scan({
    prior_inventory: priorInventory,
  });
  const driftShards = await readCandidateShardRound(
    transport,
    driftInitial,
  );
  transportFixture.setListedNumbers(listedNumbersAfterInitial);
  mutateAfterInitial?.();
  const driftFinal = await transport.scan({
    prior_inventory: driftInitial,
  });
  const driftFinalShards = await readCandidateShardRound(
    transport,
    driftFinal,
  );
  assert.throws(
    () => finalizeV2CandidateInventoryCycle({
      initial_inventory: driftInitial,
      shard_receipts: driftShards,
      final_inventory: driftFinal,
      final_shard_receipts: driftFinalShards,
    }),
    (error) => error.code === expectedDriftCode,
  );
  const stableInitial = driftFinal;
  const stableShards = await readCandidateShardRound(
    transport,
    stableInitial,
  );
  const stableFinal = await transport.scan({
    prior_inventory: stableInitial,
  });
  const stableFinalShards = await readCandidateShardRound(
    transport,
    stableFinal,
  );
  return {
    drift_attempts: [{
      initial_inventory: driftInitial,
      shard_receipts: driftShards,
      final_inventory: driftFinal,
      final_shard_receipts: driftFinalShards,
    }],
    initial_inventory: stableInitial,
    shard_receipts: stableShards,
    final_inventory: stableFinal,
    final_shard_receipts: stableFinalShards,
  };
}

async function readCandidateShardRound(transport, inventory) {
  const receipts = [];
  for (let index = 0; index < inventory.shards.length; index += 1) {
    receipts.push(await transport.readShard({
      inventory,
      shard_index: index,
    }));
  }
  return receipts;
}

async function createScheduledCandidateDispatchAttempt({
  leaseTtlSeconds = 900,
  visibility = "private",
} = {}) {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const triggerIdentity = scheduleTriggerIdentity();
  const runIdentity = { ...OWNER };
  const verifier = provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
    runIdentity,
  });
  const preflight = await loadFixtureProviderPreflight({ visibility });
  const ledger = makeLedger(fixture, capability, verifier, preflight);
  await ledger.bootstrapCapability();
  const candidateFixture = candidateTransportFixture(0, {
    exactScopeCandidateNumber: PR.number,
  });
  candidateFixture.setListedNumbers([PR.number]);
  await appendCompletedCandidateInventory({
    ledger,
    transportFixture: candidateFixture,
  });
  const scanCommand = await createScheduleWorkflowCommandHandle({
    runIdentity,
  });
  const reserved = await ledger.loadOrReserveCandidateDispatch({
    workflow_command_handle: scanCommand,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });
  runIdentity.run_id = "9002";
  runIdentity.run_attempt = 1;
  const scheduledCommand = await createScheduleWorkflowCommandHandle({
    pullRequestNumber: PR.number,
    dispatchBinding: reserved.plan.items[0],
    runIdentity,
  });
  const minimal = await loadFixtureMinimalScope();
  const scheduled = await ledger.loadCandidateDispatchForScheduledPullRequest({
    workflow_command_handle: scheduledCommand,
    minimal_scope_handle: minimal,
    trigger_identity: triggerIdentity,
    expected_dispatch_binding: reserved.plan.items[0],
  });
  const preScopeReceipt = await ledger
    .loadScheduledPullRequestEvaluatedScopeReceipt({
      candidate_dispatch_handle: scheduled.candidate_dispatch_handle,
      minimal_scope_handle: minimal,
      trigger_identity: triggerIdentity,
    });
  const discovery = await acquireFixtureLeasedDiscovery({
    ledger,
    command: scheduledCommand,
    preScopeReceipt,
    minimalPre: minimal,
    leaseTtlSeconds,
  });
  return {
    fixture,
    ledger,
    triggerIdentity,
    runIdentity,
    capability,
    verifier,
    preflight,
    candidateFixture,
    scanCommand,
    scheduledCommand,
    reserved,
    scheduled,
    preScopeReceipt,
    discovery,
  };
}

async function createCompactScheduledCandidateDispatchAttempt({
  failPublicationAfterApply = false,
  leaseTtlSeconds = 900,
} = {}) {
  const runIdentity = { ...OWNER };
  const preflight = await loadFixtureProviderPreflight();
  const attempt = await stableEmptyCandidateRefreshReconciliationFixture({
    currentOpenNumbers: [PR.number],
    fullTransportOptions: {
      exactScopeCandidateNumber: PR.number,
    },
    preflightHandle: preflight,
    runIdentity,
  });
  attempt.fullTransport.setListedNumbers([PR.number]);
  if (failPublicationAfterApply) {
    attempt.fixture.failNextCandidateInventoryUpdateAfterApply();
  }
  const publication = await attempt.ledger
    .reconcileCandidateInventoryRefreshAtomically(attempt.input);
  assert.equal(publication.state, "persisted");
  const reserved = await attempt.ledger.loadOrReserveCandidateDispatch(
    attempt.input,
  );
  assert.equal(reserved.state, "dispatch");
  runIdentity.run_id = "9002";
  runIdentity.run_attempt = 1;
  const ledger = makeLedger(
    attempt.fixture,
    attempt.capability,
    attempt.verifier,
    preflight,
    { candidateInventoryRefreshAdapter: attempt.refreshAdapter },
  );
  const scheduledCommand = await createScheduleWorkflowCommandHandle({
    pullRequestNumber: PR.number,
    dispatchBinding: reserved.plan.items[0],
    runIdentity,
  });
  const minimal = await loadFixtureMinimalScope();
  const scheduled = await ledger.loadCandidateDispatchForScheduledPullRequest({
    workflow_command_handle: scheduledCommand,
    minimal_scope_handle: minimal,
    trigger_identity: attempt.triggerIdentity,
    expected_dispatch_binding: reserved.plan.items[0],
  });
  const preScopeReceipt = await ledger
    .loadScheduledPullRequestEvaluatedScopeReceipt({
      candidate_dispatch_handle: scheduled.candidate_dispatch_handle,
      minimal_scope_handle: minimal,
      trigger_identity: attempt.triggerIdentity,
    });
  const discovery = await acquireFixtureLeasedDiscovery({
    ledger,
    command: scheduledCommand,
    preScopeReceipt,
    minimalPre: minimal,
    leaseTtlSeconds,
  });
  return {
    ...attempt,
    ledger,
    runIdentity,
    preflight,
    publication,
    reserved,
    scheduledCommand,
    scheduled,
    preScopeReceipt,
    discovery,
  };
}

async function prepareCompactCandidateDispatchItem({
  fixture,
  capability,
  verifier,
  preflight,
  refreshAdapter,
  triggerIdentity,
  runIdentity,
  binding,
  runId,
}) {
  runIdentity.run_id = runId;
  runIdentity.run_attempt = 1;
  const candidate = binding.candidate;
  const number = candidate.number;
  const nodeId = candidate.node_id;
  const headOid = candidate.head_ref_oid;
  const baseOid = candidate.base_ref_oid;
  const headRefName = `candidate-${number}`;
  const ledger = makeLedger(
    fixture,
    capability,
    verifier,
    preflight,
    { candidateInventoryRefreshAdapter: refreshAdapter },
  );
  const scheduledCommand = await createScheduleWorkflowCommandHandle({
    pullRequestNumber: number,
    dispatchBinding: binding,
    runIdentity,
  });
  const minimal = await loadFixtureMinimalScopeForCandidate(
    number,
    headOid,
    headRefName,
    nodeId,
    TIME,
  );
  const scheduled = await ledger.loadCandidateDispatchForScheduledPullRequest({
    workflow_command_handle: scheduledCommand,
    minimal_scope_handle: minimal,
    trigger_identity: triggerIdentity,
    expected_dispatch_binding: binding,
  });
  const preScopeReceipt = await ledger
    .loadScheduledPullRequestEvaluatedScopeReceipt({
      candidate_dispatch_handle: scheduled.candidate_dispatch_handle,
      minimal_scope_handle: minimal,
      trigger_identity: triggerIdentity,
    });
  let discoveryServerTime = Date.parse(TIME);
  const nextDiscoveryServerTime = () => {
    discoveryServerTime = Math.max(
      discoveryServerTime + 1_000,
      Date.parse(fixture.peekServerTime()) + 1_000,
    );
    return new Date(discoveryServerTime).toISOString();
  };
  const discovery = await acquireFixtureLeasedDiscovery({
    ledger,
    command: scheduledCommand,
    preScopeReceipt,
    minimalPre: minimal,
    minimalFetch: (url, init) => minimalScopeFetchForCandidate({
      number,
      nodeId,
      headOid,
      headRefName,
      serverTime: nextDiscoveryServerTime(),
      urlValue: url,
      init,
    }),
    transport: createProviderTransport(providerTransportFixture({
      nextServerTime: nextDiscoveryServerTime,
      comment: null,
      headOid,
      headRefName,
      pullRequestNumber: number,
      pullRequestNodeId: nodeId,
    }).fetch),
  });
  return {
    fixture,
    capability,
    verifier,
    preflight,
    refreshAdapter,
    triggerIdentity,
    runIdentity,
    ledger,
    binding,
    scheduledCommand,
    scheduled,
    preScopeReceipt,
    discovery,
    schedulerScope: {
      pullRequest: { number, node_id: nodeId },
      headOid,
      baseOid,
      potentialMergeCommitOid: POTENTIAL,
      statusIdempotencyKey: `status:mature-checkpoint-${number}`,
      decision: "clean",
    },
  };
}

async function matureQuiescentCheckpointSourceFixture() {
  const runIdentity = { ...OWNER };
  const preflight = await loadFixtureProviderPreflight();
  const checkpointNumbers = Array.from(
    { length: 255 },
    (_, index) => index + 1,
  );
  let checkpointCollection = false;
  const source = await stableEmptyCandidateRefreshReconciliationFixture({
    currentOpenNumbers: [PR.number, PR.number + 1],
    fullTransportOptions: {
      exactScopeCandidateNumber: PR.number,
    },
    preflightHandle: preflight,
    runIdentity,
    collectAttempt: async ({ request, fullTransport, defaultCollectAttempt }) => {
      if (!checkpointCollection) return defaultCollectAttempt(request);
      if (request.attempt_index === 0) {
        return readDriftingCandidateInventoryAttempt({
          request,
          transportFixture: fullTransport,
          finalNumbers: checkpointNumbers,
        });
      }
      return defaultCollectAttempt(request);
    },
  });
  source.fullTransport.setListedNumbers([PR.number, PR.number + 1]);
  const publication = await source.ledger
    .reconcileCandidateInventoryRefreshAtomically(source.input);
  assert.equal(publication.state, "persisted");
  const reserved = await source.ledger.loadOrReserveCandidateDispatch(
    source.input,
  );
  assert.equal(reserved.state, "dispatch");
  assert.equal(reserved.plan.items.length, 2);
  source.fixture.freezeServerTime();
  const originalBindings = reserved.plan.items.map((_, index) =>
    projectV2GitLedgerCandidateDispatchBinding(reserved.plan, index));

  const first = await prepareCompactCandidateDispatchItem({
    ...source,
    preflight,
    runIdentity,
    binding: originalBindings[0],
    runId: "9600",
  });
  const firstCompletion = await completeScheduledCandidateDispatchAttempt(
    first,
    { expectCycleComplete: false },
  );
  assert.deepEqual(
    firstCompletion.ack.remaining_plan.items.map((_, index) =>
      projectV2GitLedgerCandidateDispatchBinding(
        firstCompletion.ack.remaining_plan,
        index,
      )),
    originalBindings.slice(1),
  );

  const second = await prepareCompactCandidateDispatchItem({
    ...source,
    preflight,
    runIdentity,
    binding: originalBindings[1],
    runId: "9600",
  });
  await assert.rejects(
    completeScheduledCandidateDispatchAttempt(second, {
      expectCycleComplete: true,
      beforeAck() {
        source.fixture.failNextCandidateDispatchUpdateAfterApply(
          "candidate-ack",
        );
      },
    }),
    (error) => error.code === "unexpected-http-status",
  );
  const afterLoss = await makeLedger(
    source.fixture,
    source.capability,
    source.verifier,
    preflight,
    { candidateInventoryRefreshAdapter: source.refreshAdapter },
  ).load();
  assert.equal(afterLoss.records.filter((entry) =>
    entry.envelope.record_type === "candidate-dispatch-observation" &&
    entry.envelope.payload.phase === "candidate-ack").length, 2);
  assert.equal(afterLoss.records.some((entry) =>
    entry.envelope.record_type === "candidate-dispatch-observation" &&
    entry.envelope.payload.generation_id ===
      originalBindings[1].generation_id &&
    new Set(["batch-complete", "cycle-complete"]).has(
      entry.envelope.payload.phase,
    )), false);

  runIdentity.run_id = "9603";
  runIdentity.run_attempt = 1;
  const terminalInput = {
    ...source.input,
    workflow_command_handle: await createScheduleWorkflowCommandHandle({
      runIdentity,
    }),
  };
  const terminalLedger = makeLedger(
    source.fixture,
    source.capability,
    source.verifier,
    preflight,
    { candidateInventoryRefreshAdapter: source.refreshAdapter },
  );
  const terminalDispatch = await terminalLedger
    .loadOrReserveCandidateDispatch(terminalInput);
  assert.equal(terminalDispatch.state, "complete");
  const terminal = await terminalLedger.load();
  assert.equal(terminal.active_lease, null);
  assert.equal(
    terminal.authority_projection.candidate_dispatch.current_cycle
      .cycle_complete,
    true,
  );
  assert.equal(
    terminal.authority_projection.candidate_dispatch.active_reservation,
    null,
  );
  assert.equal(terminal.effect_intent_count > 0, true);
  assert.equal(terminal.effect_response_count > 0, true);

  checkpointCollection = true;
  source.currentOpen.setListedNumbers(checkpointNumbers);
  runIdentity.run_id = "9604";
  runIdentity.run_attempt = 1;
  const checkpointInput = {
    ...source.input,
    workflow_command_handle: await createScheduleWorkflowCommandHandle({
      runIdentity,
    }),
  };
  return {
    ...source,
    preflight,
    runIdentity,
    checkpointNumbers,
    checkpointInput,
    terminal,
    originalBindings,
  };
}

async function createCurrentOpenV3ScheduledCandidateDispatchAttempt({
  decision = "clean",
  leaseTtlSeconds = 900,
} = {}) {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const triggerIdentity = scheduleTriggerIdentity();
  const runIdentity = { ...OWNER };
  const preflight = await loadFixtureProviderPreflight();
  const currentOpen = currentOpenSuppressionAdapterFixture({
    initialNumbers: [PR.number],
    exactScopeCandidateNumber: PR.number,
  });
  let legacyFinishCalls = 0;
  const refreshAdapter = Object.freeze({
    schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
    schema_version: 2,
    configuration_digest: rawDigest(
      "candidate-current-open-v3-scheduled-attempt-configuration-v1",
    ),
    collectCurrentOpenCandidateAuthority:
      currentOpen.collectProductionCandidateAuthority,
    async finishLegacyCandidateInventoryAttempt() {
      legacyFinishCalls += 1;
      throw new Error("current-open scheduled attempt used legacy REST");
    },
  });
  const verifier = provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
    runIdentity,
  });
  const ledger = makeLedger(
    fixture,
    capability,
    verifier,
    preflight,
    { candidateInventoryRefreshAdapter: refreshAdapter },
  );
  await ledger.bootstrapCapability();
  const capabilityAttestationCommitSha = fixture.refTarget;
  const scanCommand = await createScheduleWorkflowCommandHandle({
    runIdentity,
  });
  const input = {
    workflow_command_handle: scanCommand,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  };
  const publication = await ledger.reconcileCandidateInventoryRefreshAtomically(
    input,
  );
  assert.equal(publication.state, "persisted");
  const reserved = await ledger.loadOrReserveCandidateDispatch(input);
  assert.equal(reserved.state, "dispatch");
  const binding = projectV2GitLedgerCandidateDispatchBinding(
    reserved.plan,
    0,
  );
  runIdentity.run_id = "9002";
  runIdentity.run_attempt = 1;
  const restarted = makeLedger(
    fixture,
    capability,
    verifier,
    preflight,
    { candidateInventoryRefreshAdapter: refreshAdapter },
  );
  const scheduledCommand = await createScheduleWorkflowCommandHandle({
    pullRequestNumber: PR.number,
    dispatchBinding: binding,
    runIdentity,
  });
  const minimal = await loadFixtureMinimalScope();
  const scheduled = await restarted
    .loadCandidateDispatchForScheduledPullRequest({
      workflow_command_handle: scheduledCommand,
      minimal_scope_handle: minimal,
      trigger_identity: triggerIdentity,
      expected_dispatch_binding: binding,
    });
  const preScopeReceipt = await restarted
    .loadScheduledPullRequestEvaluatedScopeReceipt({
      candidate_dispatch_handle: scheduled.candidate_dispatch_handle,
      minimal_scope_handle: minimal,
      trigger_identity: triggerIdentity,
    });
  const discovery = await acquireFixtureLeasedDiscovery({
    ledger: restarted,
    command: scheduledCommand,
    preScopeReceipt,
    minimalPre: minimal,
    leaseTtlSeconds,
  });
  return {
    fixture,
    ledger: restarted,
    triggerIdentity,
    runIdentity,
    capability,
    verifier,
    preflight,
    currentOpen,
    refreshAdapter,
    legacyFinishCalls: () => legacyFinishCalls,
    capabilityAttestationCommitSha,
    input,
    publication,
    reserved,
    binding,
    scheduledCommand,
    scheduled,
    preScopeReceipt,
    discovery,
    schedulerScope: { decision },
  };
}

async function completeScheduledCandidateDispatchAttempt(
  attempt,
  { expectCycleComplete = true, beforeAck = null } = {},
) {
  const fullScopeReceipt = attempt.discovery.effect_evaluated_scope_receipt;
  const controlPlaneAuthority = await attempt.ledger.loadControlPlaneAuthority(
    fullScopeReceipt.scope,
  );
  const initialAuthority = await attempt.ledger.loadInitialRunnerStateAuthority({
    control_plane_authority: controlPlaneAuthority,
    evaluated_scope_receipt: fullScopeReceipt,
    workflow_command_handle: attempt.scheduledCommand,
  });
  const controlPlaneReceipt =
    createV2ControlPlaneReceiptFromGitLedgerAuthority(controlPlaneAuthority);
  const runnerAuthority = createV2ProductionRunnerAuthority({
    preflight_handle: attempt.preflight,
    control_plane_receipt: controlPlaneReceipt,
    initial_runner_state_authority: initialAuthority,
    expected_scope: initialAuthority.scope,
  });
  const plan = schedulerObservationRecord({
    predecessor: attempt.discovery.lease_receipt.acquire_commit_sha,
    lease: attempt.discovery.lease_receipt,
    priorAuthorityDigest: initialAuthority.prior_authority_digest,
    initialAuthority,
    ...(attempt.schedulerScope ?? {}),
  }).payload.action;
  const schedulerAppend = await attempt.ledger
    .appendInitialSchedulerObservation({
      initial_runner_state_authority: initialAuthority,
      scheduler_evaluation: plan.scheduler_evaluation,
      status_plan: plan.status_plan,
    });
  const statusIntent = await attempt.ledger.appendStatusWriteIntent({
    scheduler_append: schedulerAppend,
    status_write_index: 0,
  });
  const statusResponse = await attempt.ledger.appendStatusWriteResponse({
    status_intent_handle: statusIntent.status_intent_handle,
    intent_append_receipt: statusIntent.intent_append_receipt,
    receipt: statusWriteResponseReceipt(
      statusIntent.transport,
      statusIntent.intent_append_receipt.ref_reread.server_time,
    ),
  });
  const releaseReceipt = await releaseFixtureDiscoveryLease(
    attempt.ledger,
    attempt.discovery,
  );
  const terminalResult = candidateDispatchTerminalResult({
    decision: plan.scheduler_evaluation.decision,
    schedulerAppend,
    leaseReleaseReceipt: releaseReceipt,
    preflight: attempt.preflight,
    continuityAuthority: attempt.discovery.continuity_authority,
    controlPlaneReceipt,
    initialAuthority,
    runnerAuthority,
    statusIntent,
    statusResponse,
  });
  const resultAuthority = await attempt.ledger
    .createCandidateDispatchResultAuthority({
      candidate_dispatch_handle: attempt.scheduled.candidate_dispatch_handle,
      scheduler_append: schedulerAppend,
      production_runner_authority: runnerAuthority,
      lease_release_receipt: releaseReceipt,
      terminal_result: terminalResult,
    });
  if (beforeAck !== null) {
    await beforeAck();
  }
  const ack = await attempt.ledger.ackCandidateDispatch({
    candidate_dispatch_handle: attempt.scheduled.candidate_dispatch_handle,
    reservation_receipt: attempt.scheduled.reservation_receipt,
    full_scope_receipt: fullScopeReceipt,
    candidate_dispatch_result_handle:
      resultAuthority.candidate_dispatch_result_handle,
  });
  if (expectCycleComplete) {
    assert.notEqual(ack.cycle_completion_append_receipt, null);
  } else {
    assert.equal(ack.batch_completion_append_receipt, null);
    assert.equal(ack.cycle_completion_append_receipt, null);
  }
  return {
    plan,
    schedulerAppend,
    statusIntent,
    statusResponse,
    releaseReceipt,
    resultAuthority,
    ack,
  };
}

async function createScheduledAutomaticRecoveryBranchAttempt({
  bindArtifactBeforeDispatch,
  durableArtifactPrefixPhaseBeforeDispatch = null,
  additionalFindingIdsBeforeDispatch = [],
  additionalFindingIdsDuringDispatch = [],
}) {
  const fixture = githubGitFixture();
  const runIdentity = { ...OWNER };
  const capability = scheduleCapabilityReceipt({
    allowedEventNames: ["pull_request_target", "schedule"],
  });
  const verifier = provenanceVerifier({
    eventName: "pull_request_target",
    ref: "refs/heads/main",
    shaValue: BASE,
    runIdentity,
    deriveTriggerFromRequest: true,
  });
  const preflight = await loadFixtureProviderPreflight();
  const ordinary = await createProtectedInitialRunnerContext(fixture, {
    runIdentity,
    capability,
    verifier,
    preflight,
  });
  fixture.freezeServerTime();
  const seed = schedulerObservationRecord({
    predecessor: ordinary.lease.acquire_commit_sha,
    lease: ordinary.lease,
    priorAuthorityDigest: ordinary.initialAuthority.prior_authority_digest,
    initialAuthority: ordinary.initialAuthority,
  }).payload.action;
  const initialSchedulerAppend = await ordinary.ledger
    .appendInitialSchedulerObservation({
      initial_runner_state_authority: ordinary.initialAuthority,
      scheduler_evaluation: seed.scheduler_evaluation,
      status_plan: seed.status_plan,
    });
  const { automatic, response: reservationStatusResponse } =
    await appendAutomaticReservationStatus(
      ordinary.ledger,
      initialSchedulerAppend,
    );
  const requestIntent = await ordinary.ledger
    .appendAutomaticReviewRequestIntent({
      automatic_reservation_handle: automatic.automatic_reservation_handle,
      reservation_append_receipt: automatic.reservation_append_receipt,
      reservation_status_response_append: reservationStatusResponse,
    });
  const requestReceipt = await automaticReviewRequestBindingReceipt({
    action: {
      method: "POST",
      request_body_sha256: rawDigest("@codex review"),
    },
    observedAt: requestIntent.intent_append_receipt.ref_reread.server_time,
  });
  await ordinary.ledger.appendAutomaticReviewRequestBinding({
    automatic_request_intent_handle:
      requestIntent.automatic_request_intent_handle,
    intent_append_receipt: requestIntent.intent_append_receipt,
    receipt: requestReceipt,
  });
  const findingCreatedAt = new Date(
    Date.parse(requestReceipt.created_at) + 1000,
  ).toISOString();
  let priorArtifactBinding = null;
  let priorArtifactBindingIntent = null;
  let priorArtifactBindingIntentRecordOid = null;
  let priorObservation = null;
  if (bindArtifactBeforeDispatch ||
      durableArtifactPrefixPhaseBeforeDispatch !== null) {
    fixture.advanceServerTime(10);
    priorObservation = await appendAutomaticRecoveryObservation({
      ledger: ordinary.ledger,
      preflight,
      evaluatedScopeReceipt: ordinary.evaluatedScopeReceipt,
      workflowCommandHandle: ordinary.workflowCommandHandle,
      requestCreatedAt: requestReceipt.created_at,
      findingCreatedAt,
      expectRecoveryHandle: false,
    });
    assert.equal(priorObservation.statusIntents.length, 2);
    const partialFailure = durableArtifactPrefixPhaseBeforeDispatch === "intent"
      ? { kind: "artifact-binding", occurrence: 1 }
      : durableArtifactPrefixPhaseBeforeDispatch === "ready-intent"
        ? { kind: "artifact-binding-ready", occurrence: 1 }
        : null;
    if (partialFailure !== null) {
      fixture.failRereadAfterEffectUpdate(
        partialFailure.kind,
        partialFailure.occurrence,
      );
    }
    const priorIntentPromise = ordinary.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: priorObservation.schedulerAppend,
        artifact_binding_candidate_handle:
          priorObservation.artifactBindingCandidateHandle,
      });
    if (partialFailure === null) {
      priorArtifactBindingIntent = await priorIntentPromise;
    } else {
      await assert.rejects(
        priorIntentPromise,
        (error) => error.code === "unexpected-http-status",
      );
    }
    const priorLoaded = await ordinary.ledger.load();
    priorArtifactBindingIntentRecordOid = priorLoaded.records.find((entry) =>
      entry.envelope.record_type === "effect-intent" &&
      entry.envelope.kind === "artifact-binding")?.commit_sha ?? null;
    assert.notEqual(priorArtifactBindingIntentRecordOid, null);
    if (bindArtifactBeforeDispatch) {
      const providerArtifactHandles =
        await loadAutomaticRecoveryProviderArtifactHandles({
          ledger: ordinary.ledger,
          fixture,
          transport: priorArtifactBindingIntent.transport,
          snapshots: priorObservation.snapshots,
        });
      priorArtifactBinding = await ordinary.ledger
        .appendAutomaticRecoveryArtifactBindingResponse({
          artifact_binding_intent_handle:
            priorArtifactBindingIntent.artifact_binding_intent_handle,
          intent_append_receipt:
            priorArtifactBindingIntent.intent_append_receipt,
          provider_artifact_handles: providerArtifactHandles,
        });
    }
    if (additionalFindingIdsBeforeDispatch.length !== 0) {
      assert.equal(
        durableArtifactPrefixPhaseBeforeDispatch,
        "ready-confirmation",
      );
      fixture.advanceServerTime(60);
      const expandedPriorObservation =
        await appendAutomaticRecoveryObservation({
          ledger: ordinary.ledger,
          preflight,
          evaluatedScopeReceipt: ordinary.evaluatedScopeReceipt,
          workflowCommandHandle: ordinary.workflowCommandHandle,
          requestCreatedAt: requestReceipt.created_at,
          findingCreatedAt,
          additionalFindingIds: additionalFindingIdsBeforeDispatch,
          expectRecoveryHandle: false,
          requireArtifactBinding: false,
        });
      await ordinary.ledger
        .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
          scheduler_append: expandedPriorObservation.schedulerAppend,
          artifact_binding_candidate_handle:
            expandedPriorObservation.artifactBindingCandidateHandle,
        });
    }
  }
  const priorLeaseReleaseReceipt = await releaseFixtureDiscoveryLease(
    ordinary.ledger,
    ordinary.discovery,
  );
  fixture.advanceServerTime(120);

  const ledger = makeLedger(fixture, capability, verifier, preflight);
  await ledger.bootstrapCapability();
  const candidateFixture = candidateTransportFixture(0, {
    exactScopeCandidateNumber: PR.number,
  });
  candidateFixture.setListedNumbers([PR.number]);
  await appendCompletedCandidateInventory({
    ledger,
    transportFixture: candidateFixture,
  });
  const triggerIdentity = scheduleTriggerIdentity();
  const scanCommand = await createScheduleWorkflowCommandHandle({
    runIdentity,
  });
  const reserved = await ledger.loadOrReserveCandidateDispatch({
    workflow_command_handle: scanCommand,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });
  runIdentity.run_id = "9101";
  runIdentity.run_attempt = 1;
  const scheduledCommand = await createScheduleWorkflowCommandHandle({
    pullRequestNumber: PR.number,
    dispatchBinding: reserved.plan.items[0],
    runIdentity,
  });
  const minimal = await loadFixtureMinimalScope();
  const scheduled = await ledger.loadCandidateDispatchForScheduledPullRequest({
    workflow_command_handle: scheduledCommand,
    minimal_scope_handle: minimal,
    trigger_identity: triggerIdentity,
    expected_dispatch_binding: reserved.plan.items[0],
  });
  const preScopeReceipt = await ledger
    .loadScheduledPullRequestEvaluatedScopeReceipt({
      candidate_dispatch_handle: scheduled.candidate_dispatch_handle,
      minimal_scope_handle: minimal,
      trigger_identity: triggerIdentity,
    });
  const discovery = await acquireFixtureLeasedDiscovery({
    ledger,
    command: scheduledCommand,
    preScopeReceipt,
    minimalPre: minimal,
    leaseTtlSeconds: 900,
  });
  fixture.advanceServerTime(60);
  const recovery = await appendAutomaticRecoveryObservation({
    ledger,
    preflight,
    evaluatedScopeReceipt: discovery.effect_evaluated_scope_receipt,
    workflowCommandHandle: scheduledCommand,
    requestCreatedAt: requestReceipt.created_at,
    findingCreatedAt,
    additionalFindingIds: additionalFindingIdsDuringDispatch,
    expectRecoveryHandle: bindArtifactBeforeDispatch &&
      additionalFindingIdsDuringDispatch.length === 0,
    requireArtifactBinding: bindArtifactBeforeDispatch,
  });
  return {
    fixture,
    ledger,
    capability,
    verifier,
    preflight,
    runIdentity,
    triggerIdentity,
    candidateFixture,
    scanCommand,
    reserved,
    scheduled,
    scheduledCommand,
    minimal,
    preScopeReceipt,
    discovery,
    requestReceipt,
    findingCreatedAt,
    priorArtifactBinding,
    priorArtifactBindingIntent,
    priorArtifactBindingIntentRecordOid,
    priorObservation,
    priorLeaseReleaseReceipt,
    recovery,
  };
}

async function completeScheduledAutomaticArtifactBindingAttempt(attempt) {
  const recoveryIntent = await attempt.ledger
    .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
      scheduler_append: attempt.recovery.schedulerAppend,
      artifact_binding_candidate_handle:
        attempt.recovery.artifactBindingCandidateHandle,
    });
  const providerArtifactHandles =
    await loadAutomaticRecoveryProviderArtifactHandles({
      ledger: attempt.ledger,
      fixture: attempt.fixture,
      transport: recoveryIntent.transport,
      snapshots: attempt.recovery.snapshots,
    });
  await attempt.ledger.appendAutomaticRecoveryArtifactBindingResponse({
    artifact_binding_intent_handle:
      recoveryIntent.artifact_binding_intent_handle,
    intent_append_receipt: recoveryIntent.intent_append_receipt,
    provider_artifact_handles: providerArtifactHandles,
  });
  const releaseReceipt = await releaseFixtureDiscoveryLease(
    attempt.ledger,
    attempt.discovery,
  );
  const controlPlaneReceipt =
    createV2ControlPlaneReceiptFromGitLedgerAuthority(
      attempt.recovery.controlPlaneAuthority,
    );
  const terminalResult = candidateDispatchTerminalResult({
    decision: attempt.recovery.runnerResult.scheduler_evaluation.decision,
    schedulerAppend: attempt.recovery.schedulerAppend,
    leaseReleaseReceipt: releaseReceipt,
    preflight: attempt.preflight,
    continuityAuthority: attempt.discovery.continuity_authority,
    controlPlaneReceipt,
    initialAuthority: null,
    establishedAuthority: attempt.recovery.establishedAuthority,
    runnerAuthority: attempt.recovery.productionAuthority,
    statusIntents: attempt.recovery.statusIntents,
    statusResponses: attempt.recovery.statusResponses,
  });
  const resultAuthority = await attempt.ledger
    .createCandidateDispatchResultAuthority({
      candidate_dispatch_handle: attempt.scheduled.candidate_dispatch_handle,
      scheduler_append: attempt.recovery.schedulerAppend,
      production_runner_authority: attempt.recovery.productionAuthority,
      lease_release_receipt: releaseReceipt,
      terminal_result: terminalResult,
    });
  const ack = await attempt.ledger.ackCandidateDispatch({
    candidate_dispatch_handle: attempt.scheduled.candidate_dispatch_handle,
    reservation_receipt: attempt.scheduled.reservation_receipt,
    full_scope_receipt:
      attempt.discovery.effect_evaluated_scope_receipt,
    candidate_dispatch_result_handle:
      resultAuthority.candidate_dispatch_result_handle,
  });
  assert.notEqual(ack.cycle_completion_append_receipt, null);
  return {
    plan: attempt.recovery.schedulerAppend.record.payload.action,
    recoveryIntent,
    releaseReceipt,
    resultAuthority,
    ack,
  };
}

async function appendCandidateInventoryPhase({
  ledger,
  loaded,
  phase,
  initial,
  shard = null,
  finalInventory = null,
  cycleReceipt = null,
  authorityOverride = null,
}) {
  const authority = loaded.authority_projection.candidate_inventory;
  const payload = candidateInventoryPayload({
    phase,
    initial,
    shard,
    finalInventory,
    cycleReceipt,
    priorAuthorityDigest: authority.authority_digest,
    supersedes: phase === "cycle-start"
      ? authority.incomplete_cycle?.cycle_id ?? null
      : null,
  });
  const historical = HISTORICAL_LEDGER_FIXTURES.get(ledger);
  if (historical === undefined) {
    throw new Error("historical candidate inventory fixture is unavailable");
  }
  const observedAt = loaded.post_ref.server_time;
  const record = createV2GitLedgerCandidateInventoryRecord({
    predecessor_commit_sha: loaded.tip_commit_sha,
    owner: OWNER,
    server_observed_at: observedAt,
    payload,
  });
  const evaluatedScopeReceipt =
    createV2GitLedgerCandidateInventoryEvaluatedScopeReceipt({
      repository: REPOSITORY,
      payload,
      trigger_identity: {
        event_name: authorityOverride?.eventName ?? "schedule",
        ref: "refs/heads/main",
        sha: BASE,
      },
      repository_endpoint_receipt: {
        method: "GET",
        path: "/repos/owner/repo",
        status: 200,
        server_time: TIME,
        raw_body_sha256: `sha256:${"a".repeat(64)}`,
      },
    });
  const sourceWorkflow = historicalSourceWorkflow(
    historical.capability.controller_release,
  );
  const requestInput = {
    schema: V2_GIT_LEDGER_PROVENANCE_REQUEST_SCHEMA,
    schema_version: 1,
    operation: record.record_type,
    repository: REPOSITORY,
    ledger_ref: V2_GIT_LEDGER_REF,
    predecessor_commit_sha: record.predecessor_commit_sha,
    protection_receipt_digest:
      historical.capability.protection.live_ruleset_receipt_digest,
    source_workflow: sourceWorkflow,
    effect_scope: null,
    evaluated_scope_receipt: evaluatedScopeReceipt,
    record_identity: {
      record_type: record.record_type,
      kind: record.kind,
      effect_id: record.effect_id,
      idempotency_key: record.idempotency_key,
      payload_digest: digestV2GitLedgerPayload(record.payload),
    },
    github_server_time: record.server_observed_at,
  };
  const nonce = digestCanonical(
    "codex-review-gate-v2-git-ledger-provenance-nonce",
    requestInput,
  );
  const requestWithoutDigest = {
    ...requestInput,
    nonce,
    audience: `${V2_GIT_LEDGER_OIDC_AUDIENCE}:` +
      nonce.slice("sha256:".length),
  };
  const provenanceRequest = {
    ...requestWithoutDigest,
    request_digest: digestCanonical(
      "codex-review-gate-v2-git-ledger-provenance-request",
      requestWithoutDigest,
    ),
  };
  const provenance = await historical.verifier({
    schema: V2_GIT_LEDGER_PROVENANCE_VERIFIER_REQUEST_SCHEMA,
    schema_version: 1,
    mode: "mint-and-verify",
    provenance_request: provenanceRequest,
    compact_jwt: null,
    stored_receipt: null,
  });
  const envelopeBase = {
    schema: V2_GIT_LEDGER_ENVELOPE_SCHEMA,
    schema_version: 1,
    repository: REPOSITORY,
    ledger_ref: V2_GIT_LEDGER_REF,
    record_type: record.record_type,
    sequence: loaded.records.at(-1).envelope.sequence + 1,
    pull_request: record.pull_request,
    head_ref_oid: record.head_ref_oid,
    base_ref_oid: record.base_ref_oid,
    potential_merge_commit_oid: record.potential_merge_commit_oid,
    kind: record.kind,
    effect_id: record.effect_id,
    idempotency_key: record.idempotency_key,
    predecessor_commit_sha: record.predecessor_commit_sha,
    server_observed_at: record.server_observed_at,
    payload: record.payload,
    control_comment_binding: record.control_comment_binding,
    lease: record.lease,
    source_workflow: sourceWorkflow,
    workflow_provenance: provenance.receipt,
    workflow_provenance_jwt: provenance.compact_jwt,
    payload_digest: digestV2GitLedgerPayload(record.payload),
  };
  const envelope = {
    ...envelopeBase,
    envelope_digest: digestCanonical(
      "codex-review-gate-v2-git-ledger-envelope",
      envelopeBase,
    ),
  };
  const snapshot = historical.fixture.snapshotHistoricalState();
  const objects = historical.fixture.materializeHistoricalEnvelope(envelope);
  try {
    await ledger.load();
  } catch (error) {
    historical.fixture.restoreHistoricalState(snapshot);
    throw error;
  }
  return {
    record_type: envelope.record_type,
    sequence: envelope.sequence,
    predecessor_commit_sha: envelope.predecessor_commit_sha,
    commit_sha: objects.commit_sha,
    tree_sha: objects.tree_sha,
    blob_sha: objects.blob_sha,
    server_observed_at: envelope.server_observed_at,
    payload_digest: envelope.payload_digest,
  };
}

async function materializeHistoricalProductionRecord({
  ledger,
  loaded,
  record,
  evaluatedScopeReceipt,
}) {
  const historical = HISTORICAL_LEDGER_FIXTURES.get(ledger);
  if (historical === undefined) {
    throw new Error("historical production record fixture is unavailable");
  }
  const sourceWorkflow = historicalSourceWorkflow(
    historical.capability.controller_release,
  );
  const effectScope = record.pull_request === null
    ? null
    : {
      pull_request: structuredClone(record.pull_request),
      head_ref_oid: record.head_ref_oid,
      base_ref_oid: record.base_ref_oid,
      potential_merge_commit_oid: record.potential_merge_commit_oid,
    };
  const requestInput = {
    schema: V2_GIT_LEDGER_PROVENANCE_REQUEST_SCHEMA,
    schema_version: 1,
    operation: record.record_type,
    repository: REPOSITORY,
    ledger_ref: V2_GIT_LEDGER_REF,
    predecessor_commit_sha: record.predecessor_commit_sha,
    protection_receipt_digest:
      historical.capability.protection.live_ruleset_receipt_digest,
    source_workflow: sourceWorkflow,
    effect_scope: effectScope,
    evaluated_scope_receipt: evaluatedScopeReceipt,
    record_identity: {
      record_type: record.record_type,
      kind: record.kind,
      effect_id: record.effect_id,
      idempotency_key: record.idempotency_key,
      payload_digest: digestV2GitLedgerPayload(record.payload),
    },
    github_server_time: record.server_observed_at,
  };
  const nonce = digestCanonical(
    "codex-review-gate-v2-git-ledger-provenance-nonce",
    requestInput,
  );
  const requestWithoutDigest = {
    ...requestInput,
    nonce,
    audience: `${V2_GIT_LEDGER_OIDC_AUDIENCE}:` +
      nonce.slice("sha256:".length),
  };
  const provenanceRequest = {
    ...requestWithoutDigest,
    request_digest: digestCanonical(
      "codex-review-gate-v2-git-ledger-provenance-request",
      requestWithoutDigest,
    ),
  };
  const provenance = await historical.verifier({
    schema: V2_GIT_LEDGER_PROVENANCE_VERIFIER_REQUEST_SCHEMA,
    schema_version: 1,
    mode: "mint-and-verify",
    provenance_request: provenanceRequest,
    compact_jwt: null,
    stored_receipt: null,
  });
  const envelopeBase = {
    schema: V2_GIT_LEDGER_ENVELOPE_SCHEMA,
    schema_version: 1,
    repository: REPOSITORY,
    ledger_ref: V2_GIT_LEDGER_REF,
    record_type: record.record_type,
    sequence: loaded.records.at(-1).envelope.sequence + 1,
    pull_request: record.pull_request,
    head_ref_oid: record.head_ref_oid,
    base_ref_oid: record.base_ref_oid,
    potential_merge_commit_oid: record.potential_merge_commit_oid,
    kind: record.kind,
    effect_id: record.effect_id,
    idempotency_key: record.idempotency_key,
    predecessor_commit_sha: record.predecessor_commit_sha,
    server_observed_at: record.server_observed_at,
    payload: record.payload,
    control_comment_binding: record.control_comment_binding,
    lease: record.lease,
    source_workflow: sourceWorkflow,
    workflow_provenance: provenance.receipt,
    workflow_provenance_jwt: provenance.compact_jwt,
    payload_digest: digestV2GitLedgerPayload(record.payload),
  };
  const envelope = {
    ...envelopeBase,
    envelope_digest: digestCanonical(
      "codex-review-gate-v2-git-ledger-envelope",
      envelopeBase,
    ),
  };
  const snapshot = historical.fixture.snapshotHistoricalState();
  historical.fixture.materializeHistoricalEnvelope(envelope);
  try {
    await ledger.load();
  } catch (error) {
    historical.fixture.restoreHistoricalState(snapshot);
    throw error;
  }
  return envelope;
}

async function appendHistoricalEffectRecord(
  ledger,
  record,
  evaluatedScopeReceipt = null,
) {
  const historical = HISTORICAL_LEDGER_FIXTURES.get(ledger);
  if (historical === undefined) {
    throw new Error("historical effect fixture is unavailable");
  }
  const loaded = await ledger.load();
  const boundary = Date.parse(record.server_observed_at) <
      Date.parse(loaded.post_ref.server_time)
    ? loaded.post_ref.server_time
    : record.server_observed_at;
  let authoritativeRecord = record;
  if (boundary !== record.server_observed_at) {
    const plan = recordPlan(record);
    plan.server_observed_at = boundary;
    if (
      plan.record_type === "effect-intent" &&
      plan.kind === "effect-attempt"
    ) {
      const attempt = plan.payload.action.attempt;
      attempt.recorded_at = boundary;
      const { attempt_digest: _attemptDigest, ...withoutDigest } = attempt;
      attempt.attempt_digest = runnerDigestCanonical(
        "codex-review-gate-v2-request-attempt",
        withoutDigest,
      );
    }
    authoritativeRecord = createV2GitLedgerRecord(plan);
  }
  const receipt = evaluatedScopeReceipt ??
    await ledger.defaultEffectEvaluatedScopeReceipt();
  const envelope = await materializeHistoricalProductionRecord({
    ledger,
    loaded,
    record: authoritativeRecord,
    evaluatedScopeReceipt: receipt,
  });
  const commitSha = historical.fixture.refTarget;
  return {
    record_type: envelope.record_type,
    sequence: envelope.sequence,
    predecessor_commit_sha: envelope.predecessor_commit_sha,
    commit_sha: commitSha,
    server_observed_at: envelope.server_observed_at,
    payload_digest: envelope.payload_digest,
    ref_reread: {
      target_commit_sha: commitSha,
      server_time: envelope.server_observed_at,
    },
    stable: true,
  };
}

function historicalSourceWorkflow(release) {
  return {
    repository: structuredClone(release.repository),
    workflow_path: release.workflow_path,
    workflow_ref: release.workflow_ref,
    workflow_sha: release.workflow_sha,
    job_workflow_ref: release.job_workflow_ref,
    job_workflow_sha: release.job_workflow_sha,
    caller_workflow_file_receipt_digest:
      release.caller_workflow_file_receipt_digest,
    job_workflow_file_receipt_digest:
      release.job_workflow_file_receipt_digest,
    release_receipt_digest: release.release_receipt_digest,
  };
}

function candidateInventoryPayload({
  phase,
  initial,
  shard = null,
  finalInventory = null,
  cycleReceipt = null,
  priorAuthorityDigest,
  supersedes = null,
}) {
  return {
    schema: V2_GIT_LEDGER_CANDIDATE_INVENTORY_RECORD_SCHEMA,
    schema_version: 1,
    phase,
    cycle_id: `candidate-cycle:${initial.receipt_digest.slice("sha256:".length)}`,
    owner: OWNER,
    prior_candidate_authority_digest: priorAuthorityDigest,
    supersedes_incomplete_cycle_id: supersedes,
    initial_inventory_receipt_digest: initial.receipt_digest,
    initial_inventory: phase === "cycle-start" ? initial : null,
    shard_receipt: phase === "shard" ? shard : null,
    final_inventory: phase === "cycle-complete" ? finalInventory : null,
    cycle_receipt: phase === "cycle-complete" ? cycleReceipt : null,
  };
}

function candidateTransportFixture(
  initialCount,
  {
    exactScopeCandidateNumber = null,
    longNodeIds = false,
    longIds = false,
  } = {},
) {
  const identities = new Map();
  const candidateStates = new Map();
  const calls = [];
  let responseSecond = 0;
  const ensureCandidate = (number) => {
    if (!identities.has(number)) {
      identities.set(number, {
        id: longIds
          ? `${"9".repeat(240)}${String(1000 + number)}`
          : String(1000 + number),
        node_id: number === exactScopeCandidateNumber
          ? PR.node_id
          : longNodeIds
            ? `PR_${String(number).padStart(4, "0")}_${"n".repeat(240)}`
            : `PR_candidate_${number}`,
        number,
        created_at: new Date(
          Date.parse("2026-08-01T00:00:00.000Z") + number * 1000,
        ).toISOString(),
      });
    }
    return identities.get(number);
  };
  let listedNumbers = Array.from(
    { length: initialCount },
    (_, index) => index + 1,
  );
  listedNumbers.forEach(ensureCandidate);
  return {
    calls,
    advanceServerTime(seconds) {
      responseSecond += seconds;
    },
    setListedNumbers(value) {
      listedNumbers = [...value];
      listedNumbers.forEach(ensureCandidate);
    },
    setCandidateState(number, state) {
      if (!new Set(["open", "closed"]).has(state)) {
        throw new TypeError("candidate fixture state must be open or closed");
      }
      ensureCandidate(number);
      candidateStates.set(number, state);
    },
    async fetch(url, init) {
      calls.push({ method: init.method, url: String(url) });
      assert.equal(init.method, "GET");
      const parsed = new URL(url);
      const listPath = "/repos/owner/repo/pulls";
      if (parsed.pathname === listPath && parsed.search !== "") {
        const page = Number(parsed.searchParams.get("page"));
        const offset = (page - 1) * 100;
        const selected = listedNumbers
          .map(ensureCandidate)
          .sort((left, right) =>
            Date.parse(left.created_at) - Date.parse(right.created_at) ||
            left.number - right.number)
          .slice(offset, offset + 100);
        const body = selected.map((candidate) => ({
          ...candidate,
          url: `https://github.example.test/repos/owner/repo/pulls/${candidate.number}`,
        }));
        const nextPage = offset + selected.length < listedNumbers.length
          ? page + 1
          : null;
        const link = nextPage === null
          ? null
          : `<https://github.example.test/repos/owner/repo/pulls?` +
            `state=all&sort=created&direction=asc&per_page=100&` +
            `page=${nextPage}>; rel="next"`;
        return candidateResponse(body, {
          link,
          serverTime: new Date(
            Date.parse(TIME) + responseSecond * 1000,
          ).toISOString(),
        });
      }
      const match = parsed.pathname.match(/^\/repos\/owner\/repo\/pulls\/(\d+)$/u);
      if (match !== null) {
        const candidate = ensureCandidate(Number(match[1]));
        const state = candidateStates.get(candidate.number) ?? "open";
        return candidateResponse({
          ...candidate,
          url: `https://github.example.test/repos/owner/repo/pulls/${candidate.number}`,
          state,
          merged: false,
          merged_at: null,
          updated_at: "2026-08-13T11:59:00.000Z",
          draft: false,
          base: {
            ref: "main",
            sha: BASE,
            repo: {
              id: REPOSITORY.id,
              node_id: REPOSITORY.node_id,
              full_name: "owner/repo",
            },
          },
          head: {
            ref: `candidate-${candidate.number}`,
            sha: HEAD,
            repo: {
              id: REPOSITORY.id,
              node_id: REPOSITORY.node_id,
              full_name: "owner/repo",
            },
          },
        }, {
          serverTime: new Date(
            Date.parse(TIME) + responseSecond * 1000,
          ).toISOString(),
        });
      }
      throw new Error(`unexpected candidate fixture request ${parsed}`);
    },
  };
}

function candidateResponse(value, { link = null, serverTime = TIME } = {}) {
  const body = JSON.stringify(value);
  const headers = {
    Date: new Date(serverTime).toUTCString(),
    "Content-Type": "application/json",
  };
  if (link !== null) headers.Link = link;
  return new Response(body, { status: 200, headers });
}

function currentOpenSuppressionAdapterFixture({
  initialNumbers = [],
  exactScopeCandidateNumber = null,
} = {}) {
  let listedNumbers = [...initialNumbers];
  const pullRequestNodeIdOverrides = new Map();
  const headRefOidOverrides = new Map();
  const headRefNameOverrides = new Map();
  const baseRefNameOverrides = new Map();
  const headRepositoryOverrides = new Map();
  let activePass = null;
  let responseCount = 0;
  let afterProjection = null;
  let reuseProjection = false;
  let driftNextFinalFence = false;
  let replayedRequestIdsRemaining = 0;
  let lastProjection = null;
  let lastProductionPair = null;
  let reuseProductionPair = false;
  const calls = [];
  const fetch = async (input, init = {}) => {
    const url = new URL(String(input));
    const request = JSON.parse(String(init.body));
    const after = request.variables?.after ?? null;
    if (url.href !== "https://api.github.com/graphql") {
      return new Response(JSON.stringify({ message: "unexpected request" }), {
        status: 400,
        headers: {
          Date: new Date(Date.parse(TIME) + (++responseCount * 1000))
            .toUTCString(),
          "Content-Type": "application/json",
          "X-GitHub-Request-Id": `SUPPRESSION:${responseCount}`,
        },
      });
    }
    let kind;
    let start;
    if (after === null && activePass === null) {
      kind = "page";
      start = 0;
      activePass = {
        numbers: [...listedNumbers],
        next_offset: 0,
        expected_cursor: null,
        awaiting_fence: false,
      };
    } else if (after === null && activePass?.awaiting_fence === true) {
      kind = "fence";
      start = 0;
    } else if (
      typeof after === "string" &&
      activePass?.awaiting_fence === false &&
      after === activePass.expected_cursor
    ) {
      kind = "page";
      start = activePass.next_offset;
    } else {
      return new Response(JSON.stringify({ message: "unexpected cursor" }), {
        status: 400,
        headers: {
          Date: new Date(Date.parse(TIME) + (++responseCount * 1000))
            .toUTCString(),
          "Content-Type": "application/json",
          "X-GitHub-Request-Id": `SUPPRESSION:${responseCount}`,
        },
      });
    }
    const passNumbers = kind === "fence" && driftNextFinalFence
      ? [...activePass.numbers, 999]
      : activePass.numbers;
    const end = Math.min(start + 100, passNumbers.length);
    const pageNumbers = passNumbers.slice(start, end);
    const nodes = pageNumbers.map((number) => ({
      id: pullRequestNodeIdOverrides.get(number) ?? `PR_${number}`,
      fullDatabaseId: String(1000 + number),
      number,
      state: "OPEN",
      isDraft: false,
      createdAt: new Date(Date.UTC(2026, 0, 1, 0, number)).toISOString(),
      updatedAt: "2026-08-13T11:00:00.000Z",
      headRefOid: headRefOidOverrides.get(number) ??
        (number === exactScopeCandidateNumber
          ? HEAD
          : number.toString(16).padStart(40, "0")),
      headRefName: number === exactScopeCandidateNumber
        ? headRefNameOverrides.get(number) ?? "feature"
        : headRefNameOverrides.get(number) ?? `feature-${number}`,
      baseRefOid: BASE,
      baseRefName: baseRefNameOverrides.get(number) ?? "main",
      headRepository: headRepositoryOverrides.get(number) ?? {
        id: REPOSITORY.node_id,
        databaseId: Number(REPOSITORY.id),
        nameWithOwner: `${REPOSITORY.owner}/${REPOSITORY.name}`,
      },
      baseRepository: {
        id: REPOSITORY.node_id,
        databaseId: Number(REPOSITORY.id),
        nameWithOwner: `${REPOSITORY.owner}/${REPOSITORY.name}`,
      },
    }));
    const hasNextPage = end < activePass.numbers.length;
    const endCursor = hasNextPage ? `suppression-cursor:${end}` : null;
    responseCount += 1;
    calls.push({ kind, numbers: [...pageNumbers] });
    const body = {
      data: {
        repository: {
          id: REPOSITORY.node_id,
          databaseId: Number(REPOSITORY.id),
          nameWithOwner: `${REPOSITORY.owner}/${REPOSITORY.name}`,
          pullRequests: {
            nodes,
            pageInfo: { hasNextPage, endCursor },
          },
        },
      },
    };
    if (kind === "fence") {
      driftNextFinalFence = false;
      activePass = null;
    } else if (hasNextPage) {
      activePass.next_offset = end;
      activePass.expected_cursor = endCursor;
    } else {
      activePass.awaiting_fence = true;
    }
    const requestId = replayedRequestIdsRemaining > 0
      ? "SUPPRESSION:REPLAY"
      : `SUPPRESSION:${responseCount}`;
    if (replayedRequestIdsRemaining > 0) {
      replayedRequestIdsRemaining -= 1;
    }
    return new Response(JSON.stringify(body), {
      status: 200,
      headers: {
        Date: new Date(Date.parse(TIME) + (responseCount * 1000))
          .toUTCString(),
        "Content-Type": "application/json",
        "X-GitHub-Request-Id": requestId,
      },
    });
  };
  const currentOpen = createV2GitHubCurrentOpenCandidateInventory({
    fetch,
    token: SYNTHETIC_BEARER,
    repository: {
      owner: REPOSITORY.owner,
      name: REPOSITORY.name,
      id: REPOSITORY.id,
      node_id: REPOSITORY.node_id,
    },
    graphqlUrl: "https://api.github.com/graphql",
  });
  const adapter = Object.freeze({
    schema:
      "codex-review-gate-git-ledger-candidate-suppression-evidence-adapter-v2",
    schema_version: 1,
    configuration_digest: rawDigest(
      "candidate-suppression-test-configuration-v1",
    ),
    async collectCurrentOpenProjection() {
      if (reuseProjection && lastProjection !== null) {
        return lastProjection;
      }
      const receipt = await currentOpen.scan();
      const projection = currentOpen.projectForGitLedger(receipt);
      lastProjection = projection;
      afterProjection?.();
      return projection;
    },
  });
  const collectProductionCandidateAuthority = async () => {
    if (reuseProductionPair && lastProductionPair !== null) {
      return lastProductionPair;
    }
    const receipt = await currentOpen.scan();
    const projection = currentOpen.projectForGitLedger(receipt);
    const productionCandidateAuthority =
      currentOpen.projectProductionCandidateAuthority(projection);
    lastProjection = projection;
    afterProjection?.({
      projection,
      production_candidate_authority: productionCandidateAuthority,
    });
    lastProductionPair = Object.freeze({
      projection,
      production_candidate_authority: productionCandidateAuthority,
    });
    return lastProductionPair;
  };
  return {
    adapter,
    calls,
    collectProductionCandidateAuthority,
    setListedNumbers(numbers) {
      listedNumbers = [...numbers];
      activePass = null;
    },
    setHeadRefOid(number, headRefOid) {
      assert.match(headRefOid, /^[0-9a-f]{40}$/u);
      headRefOidOverrides.set(number, headRefOid);
      activePass = null;
    },
    setPullRequestNodeId(number, nodeId) {
      assert.equal(typeof nodeId, "string");
      assert.ok(nodeId.length > 0 && Buffer.byteLength(nodeId, "utf8") <= 256);
      pullRequestNodeIdOverrides.set(number, nodeId);
      activePass = null;
    },
    setHeadRepository(number, value) {
      assert.equal(typeof value, "object");
      headRepositoryOverrides.set(number, structuredClone(value));
      activePass = null;
    },
    setRefNames(number, { head, base }) {
      assert.equal(typeof head, "string");
      assert.equal(typeof base, "string");
      assert.ok(head.length > 0 && head.length <= 255);
      assert.ok(base.length > 0 && base.length <= 255);
      headRefNameOverrides.set(number, head);
      baseRefNameOverrides.set(number, base);
      activePass = null;
    },
    setAfterProjection(callback) {
      afterProjection = callback;
    },
    reuseLastProjection(value = true) {
      reuseProjection = value;
    },
    reuseLastProductionPair(value = true) {
      reuseProductionPair = value;
    },
    driftNextFence() {
      driftNextFinalFence = true;
    },
    replayNextRequestPair() {
      replayedRequestIdsRemaining = 2;
    },
  };
}

function assertNoCandidateRefreshPrivateAuthority(value) {
  if (value === null || typeof value !== "object") return;
  const forbiddenKeys = new Set([
    "source_handle",
    "evidence_handle",
    "suppression_classification_handle",
    "source_load_handle",
    "next_unit_admission_handle",
    "must_persist_handle",
    "must_persist_claim",
    "request_handle",
    "loaded",
    "continuation",
  ]);
  for (const key of Reflect.ownKeys(value)) {
    assert.equal(
      forbiddenKeys.has(key),
      false,
      `candidate refresh result leaked private authority ${String(key)}`,
    );
    assertNoCandidateRefreshPrivateAuthority(value[key]);
  }
}

async function stableEmptyCandidateRefreshReconciliationFixture({
  currentOpenNumbers = [],
  collectAttempt = null,
  fullTransportOptions = {},
  preflightHandle = null,
  runIdentity = OWNER,
  wrapAttemptResult = null,
} = {}) {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const triggerIdentity = scheduleTriggerIdentity();
  const currentOpen = currentOpenSuppressionAdapterFixture({
    initialNumbers: currentOpenNumbers,
  });
  const fullTransport = candidateTransportFixture(0, fullTransportOptions);
  const fullRefreshCalls = [];
  const defaultCollectAttempt = (request) =>
    readCandidateInventoryAttempt({
      transportFixture: fullTransport,
      priorInventory: request.prior_inventory,
      initialInventory: request.initial_inventory,
    });
  const refreshAdapter = Object.freeze({
    schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
    schema_version: 1,
    configuration_digest: rawDigest(
      "candidate-refresh-reconciliation-fixture-configuration-v1",
    ),
    collectCurrentOpenProjection:
      currentOpen.adapter.collectCurrentOpenProjection,
    async collectCandidateInventoryAttempt(request) {
      fullRefreshCalls.push(request);
      const attempt = collectAttempt === null
        ? await defaultCollectAttempt(request)
        : await collectAttempt({
            request,
            fullTransport,
            defaultCollectAttempt,
          });
      const result = {
        schema:
          "codex-review-gate-git-ledger-candidate-full-refresh-result-v2",
        schema_version: 1,
        request_handle: request.request_handle,
        attempt_index: request.attempt_index,
        ...attempt,
      };
      return wrapAttemptResult === null
        ? result
        : wrapAttemptResult({ request, attempt, result });
    },
  });
  const verifier = provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
    runIdentity,
  });
  const ledger = makeLedger(fixture, capability, verifier, preflightHandle, {
    candidateInventoryRefreshAdapter: refreshAdapter,
  });
  await ledger.bootstrapCapability();
  const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
  const bootstrapCycle = await readCandidateInventoryCycle({
    transportFixture: fullTransport,
  });
  await ledger.appendCandidateInventoryCycleIfRequired({
    workflow_command_handle: workflowCommandHandle,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    ...bootstrapCycle,
  });
  await ledger.loadOrReserveCandidateDispatch({
    workflow_command_handle: workflowCommandHandle,
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
  });
  return {
    fixture,
    capability,
    triggerIdentity,
    currentOpen,
    fullTransport,
    fullRefreshCalls,
    refreshAdapter,
    verifier,
    ledger,
    workflowCommandHandle,
    input: {
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    },
  };
}

async function legacyIncompleteCandidateRefreshFixture({
  candidateCount = 1,
  seedShardCount = 0,
  cycleOverride = null,
  wrapFinishResult = null,
} = {}) {
  const fixture = githubGitFixture();
  const capability = scheduleCapabilityReceipt();
  const triggerIdentity = scheduleTriggerIdentity();
  const transportFixture = candidateTransportFixture(candidateCount);
  const cycle = cycleOverride ??
    await readCandidateInventoryCycle({ transportFixture });
  const finishCalls = [];
  let currentOpenCalls = 0;
  const refreshAdapter = Object.freeze({
    schema: "codex-review-gate-git-ledger-candidate-refresh-adapter-v2",
    schema_version: 2,
    configuration_digest: rawDigest(
      `candidate-refresh-legacy-finish-${candidateCount}-v1`,
    ),
    async collectCurrentOpenCandidateAuthority() {
      currentOpenCalls += 1;
      throw new Error("legacy finish must not collect current-open evidence");
    },
    async finishLegacyCandidateInventoryAttempt(request) {
      finishCalls.push(request);
      const result = {
        schema:
          "codex-review-gate-git-ledger-candidate-inventory-legacy-finish-result-v2",
        schema_version: 1,
        request_handle: request.request_handle,
        cycle_id: request.cycle_id,
        start_record_oid: request.start_record_oid,
        next_shard_index: request.next_shard_index,
        initial_inventory: cycle.initial_inventory,
        shard_receipts: cycle.shard_receipts,
        final_inventory: cycle.final_inventory,
        final_shard_receipts: cycle.final_shard_receipts,
      };
      return wrapFinishResult === null
        ? result
        : wrapFinishResult({ request, result, cycle });
    },
  });
  const verifier = provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
  });
  const ledger = makeLedger(fixture, capability, verifier, null, {
    candidateInventoryRefreshAdapter: refreshAdapter,
  });
  await ledger.bootstrapCapability();
  const workflowCommandHandle = await createScheduleWorkflowCommandHandle();
  let loaded = await ledger.load();
  await appendCandidateInventoryPhase({
    ledger,
    loaded,
    phase: "cycle-start",
    initial: cycle.initial_inventory,
  });
  for (let index = 0; index < seedShardCount; index += 1) {
    loaded = await ledger.load();
    await appendCandidateInventoryPhase({
      ledger,
      loaded,
      phase: "shard",
      initial: cycle.initial_inventory,
      shard: cycle.shard_receipts[index],
    });
  }
  return {
    fixture,
    capability,
    triggerIdentity,
    transportFixture,
    cycle,
    finishCalls,
    get currentOpenCalls() {
      return currentOpenCalls;
    },
    refreshAdapter,
    verifier,
    ledger,
    workflowCommandHandle,
    input: {
      workflow_command_handle: workflowCommandHandle,
      trigger_identity: triggerIdentity,
      repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    },
  };
}

async function bootstrapCheckpointAdmissionFixture({
  fixture = githubGitFixture(),
  fetch = null,
  verifier = null,
  ledgerOptions = {},
} = {}) {
  const capability = scheduleCapabilityReceipt();
  const triggerIdentity = scheduleTriggerIdentity();
  const exactVerifier = verifier ?? provenanceVerifier({
    eventName: "schedule",
    ref: triggerIdentity.ref,
    shaValue: triggerIdentity.sha,
  });
  const ledger = makeLedger(
    { fetch: fetch ?? fixture.fetch },
    capability,
    exactVerifier,
    null,
    ledgerOptions,
  );
  await ledger.bootstrapCapability();
  const transportFixture = candidateTransportFixture(255);
  const cycle = await readIdentityDriftCandidateInventoryCycle({
    transportFixture,
    priorInventory: null,
    listedNumbersAfterInitial: Array.from(
      { length: 255 },
      (_, index) => index + 2,
    ),
    mutateAfterInitial: () => {
      transportFixture.setCandidateState(1, "closed");
    },
  });
  const required = await ledger.appendCandidateInventoryCycleIfRequired({
    workflow_command_handle: await createScheduleWorkflowCommandHandle(),
    trigger_identity: triggerIdentity,
    repository_endpoint_receipt: scheduleRepositoryEndpointReceipt(),
    ...cycle,
  });
  return {
    fixture,
    ledger,
    diagnostic: createV2GitLedgerCheckpointDiagnosticAdapter(
      ledger.productionApiForTest(),
    ),
    required,
    cycle,
    triggerIdentity,
  };
}

function makeLedger(
  fixture,
  capability = capabilityReceipt(),
  verifier = provenanceVerifier(),
  preflightHandle = null,
  {
    httpLimits = V2_GIT_LEDGER_HTTP_LIMITS,
    candidateSuppressionEvidenceAdapter = null,
    candidateInventoryRefreshAdapter = null,
  } = {},
) {
  let production = createV2GitHubGitLedger({
    ...factoryInput(fixture, capability),
    preflightHandle,
    verifyWorkflowProvenance: verifier,
    httpLimits,
    candidateSuppressionEvidenceAdapter,
    candidateInventoryRefreshAdapter,
  });
  let defaultAuthorityReceipt = null;
  let defaultFullAuthorityReceipt = null;
  let minimalScopeHandle = null;
  const defaultAuthority = async () => {
    if (defaultAuthorityReceipt !== null) {
      return { evaluated_scope_receipt: defaultAuthorityReceipt };
    }
    minimalScopeHandle ??= await loadFixtureMinimalScope();
    const defaultEvent = capability.workflow_provenance_policy
      .allowed_event_names[0];
    const triggerIdentity = defaultEvent === "pull_request_target"
      ? {
          event_name: "pull_request_target",
          ref: "refs/heads/main",
          sha: BASE,
        }
      : {
          event_name: "pull_request",
          ref: "refs/pull/7/merge",
          sha: POTENTIAL,
        };
    defaultAuthorityReceipt = await production
      .createPullRequestEventEvaluatedScopeReceipt({
        minimal_scope_handle: minimalScopeHandle,
        trigger_identity: triggerIdentity,
      });
    return { evaluated_scope_receipt: defaultAuthorityReceipt };
  };
  const defaultEffectAuthority = async () => ({
    evaluated_scope_receipt:
      defaultFullAuthorityReceipt ??
      (await defaultAuthority()).evaluated_scope_receipt,
  });
  const ledger = {
    productionApiForTest: () => production,
    async bootstrapCapability() {
      const restricted = createV2GitHubGitLedgerBootstrap({
        fetch: fixture.fetch,
        token: SYNTHETIC_BEARER,
        repository: REPOSITORY,
        ledgerRef: V2_GIT_LEDGER_REF,
        restBaseUrl: "https://api.github.test",
        bootstrapCapabilityInput: bootstrapInputForCapability(capability),
        preflightHandle,
        verifyWorkflowProvenance: verifier,
        httpLimits,
        candidateSuppressionEvidenceAdapter,
        candidateInventoryRefreshAdapter,
      });
      const result = await restricted.bootstrapCapability();
      production = result.ledger;
      defaultAuthorityReceipt = null;
      defaultFullAuthorityReceipt = null;
      return result.bootstrap_receipt;
    },
    load: (...args) => production.load(...args),
    loadControlPlaneAuthority: (...args) =>
      production.loadControlPlaneAuthority(...args),
    loadControlPlaneReceipt: (...args) =>
      production.loadControlPlaneReceipt(...args),
    loadInitialRunnerStateAuthority: (...args) =>
      production.loadInitialRunnerStateAuthority(...args),
    loadEstablishedRunnerStateAuthority: (...args) =>
      production.loadEstablishedRunnerStateAuthority(...args),
    appendInitialSchedulerObservation: (...args) =>
      production.appendInitialSchedulerObservation(...args),
    appendEstablishedSchedulerObservation: (...args) =>
      production.appendEstablishedSchedulerObservation(...args),
    appendStatusWriteIntent: (...args) =>
      production.appendStatusWriteIntent(...args),
    loadNextStatusWriteIntent: (...args) =>
      production.loadNextStatusWriteIntent(...args),
    appendStatusWriteResponse: (...args) =>
      production.appendStatusWriteResponse(...args),
    appendAutomaticRequestReservation: (...args) =>
      production.appendAutomaticRequestReservation(...args),
    appendReservationStatusWriteIntent: (...args) =>
      production.appendReservationStatusWriteIntent(...args),
    appendReservationStatusWriteResponse: (...args) =>
      production.appendReservationStatusWriteResponse(...args),
    appendAutomaticReviewRequestIntent: (...args) =>
      production.appendAutomaticReviewRequestIntent(...args),
    appendRecoveredAutomaticReviewRequestIntent: (...args) =>
      production.appendRecoveredAutomaticReviewRequestIntent(...args),
    loadOrAppendAutomaticRecoveryArtifactBindingIntent: (...args) =>
      production.loadOrAppendAutomaticRecoveryArtifactBindingIntent(...args),
    appendAutomaticRecoveryArtifactBindingResponse: (...args) =>
      production.appendAutomaticRecoveryArtifactBindingResponse(...args),
    appendAutomaticRequestRecoveryTransition: (...args) =>
      production.appendAutomaticRequestRecoveryTransition(...args),
    appendAutomaticReviewRequestBinding: (...args) =>
      production.appendAutomaticReviewRequestBinding(...args),
    append: async (record, authority = null) => production.append(
      record,
      authority ?? await defaultEffectAuthority(),
    ),
    appendRecord: async (record, authority = null) => production.appendRecord(
      record,
      authority ?? await defaultEffectAuthority(),
    ),
    appendEffectIntent: async (input, authority = null) => production.appendEffectIntent(
      input,
      authority ?? await defaultEffectAuthority(),
    ),
    appendEffectResponse: async (input, authority = null) => production.appendEffectResponse(
      input,
      authority ?? await defaultEffectAuthority(),
    ),
    appendCandidateInventory: (...args) =>
      production.appendCandidateInventory(...args),
    appendCandidateInventoryCycleIfRequired: (...args) =>
      production.appendCandidateInventoryCycleIfRequired(...args),
    beginCandidateInventoryRefreshClassification: (...args) =>
      production.beginCandidateInventoryRefreshClassification(...args),
    collectCandidateInventoryRefreshEvidence: (...args) =>
      production.collectCandidateInventoryRefreshEvidence(...args),
    classifyCandidateInventoryRefresh: (...args) =>
      production.classifyCandidateInventoryRefresh(...args),
    trySuppressCandidateInventoryRefresh: (...args) =>
      production.trySuppressCandidateInventoryRefresh(...args),
    reconcileCandidateInventoryRefreshAtomically: (...args) =>
      production.reconcileCandidateInventoryRefreshAtomically(...args),
    inspectCheckpointTransactionAdmission: (...args) =>
      production.inspectCheckpointTransactionAdmission(...args),
    loadOrReserveCandidateDispatch: (...args) =>
      production.loadOrReserveCandidateDispatch(...args),
    loadCandidateDispatchForScheduledPullRequest: (...args) =>
      production.loadCandidateDispatchForScheduledPullRequest(...args),
    loadScheduledPullRequestEvaluatedScopeReceipt: (...args) =>
      production.loadScheduledPullRequestEvaluatedScopeReceipt(...args),
    createCandidateDispatchResultAuthority: (...args) =>
      production.createCandidateDispatchResultAuthority(...args),
    ackCandidateDispatch: (...args) =>
      production.ackCandidateDispatch(...args),
    recoverCandidateDispatchFailure: (...args) =>
      production.recoverCandidateDispatchFailure(...args),
    acquireLease: async (input, authority = null) => production.acquireLease(
      input,
      authority ?? await defaultAuthority(),
    ),
    releaseLease: async (input, authority = null) => production.releaseLease(
      input,
      authority ?? await defaultAuthority(),
    ),
    createPullRequestEventEvaluatedScopeReceipt: (...args) =>
      production.createPullRequestEventEvaluatedScopeReceipt(...args),
    createManualPullRequestEvaluatedScopeReceipt: (...args) =>
      production.createManualPullRequestEvaluatedScopeReceipt(...args),
    createProviderEventPreScopeEvaluatedScopeReceipt: (...args) =>
      production.createProviderEventPreScopeEvaluatedScopeReceipt(...args),
    async createFullDiscoveryEvaluatedScopeReceipt(...args) {
      const receipt = await production
        .createFullDiscoveryEvaluatedScopeReceipt(...args);
      defaultFullAuthorityReceipt = receipt;
      return receipt;
    },
    async createProviderEventFullDiscoveryEvaluatedScopeReceipt(...args) {
      const receipt = await production
        .createProviderEventFullDiscoveryEvaluatedScopeReceipt(...args);
      defaultFullAuthorityReceipt = receipt;
      return receipt;
    },
    defaultEvaluatedScopeReceipt: async () =>
      (await defaultAuthority()).evaluated_scope_receipt,
    defaultEffectEvaluatedScopeReceipt: async () =>
      (await defaultEffectAuthority()).evaluated_scope_receipt,
  };
  HISTORICAL_LEDGER_FIXTURES.set(ledger, { fixture, capability, verifier });
  return ledger;
}

async function createProtectedInitialRunnerContext(
  fixture,
  {
    runIdentity = OWNER,
    capability: capabilityOverride = null,
    verifier: verifierOverride = null,
    preflight: preflightOverride = null,
  } = {},
) {
  const preflight = preflightOverride ?? await loadFixtureProviderPreflight();
  const capability = capabilityOverride ?? capabilityReceiptForTrigger(
    "pull_request_target",
    "refs/heads/main",
  );
  const verifier = verifierOverride ?? provenanceVerifier({
      eventName: "pull_request_target",
      ref: "refs/heads/main",
      shaValue: BASE,
      runIdentity,
    });
  const ledger = makeLedger(fixture, capability, verifier, preflight);
  await ledger.bootstrapCapability();
  const minimalScopeHandle = await loadFixtureMinimalScope();
  const preScopeReceipt = await ledger
    .createPullRequestEventEvaluatedScopeReceipt({
      minimal_scope_handle: minimalScopeHandle,
      trigger_identity: {
        event_name: "pull_request_target",
        ref: "refs/heads/main",
        sha: BASE,
      },
    });
  const workflowCommandHandle =
    await createPullRequestTargetWorkflowCommandHandle({ runIdentity });
  const discovery = await acquireFixtureLeasedDiscovery({
    ledger,
    command: workflowCommandHandle,
    preScopeReceipt,
    minimalPre: minimalScopeHandle,
    leaseTtlSeconds: 900,
  });
  const lease = discovery.lease_receipt;
  const evaluatedScopeReceipt = discovery.effect_evaluated_scope_receipt;
  const controlPlaneAuthority = await ledger.loadControlPlaneAuthority(
    evaluatedScopeReceipt.scope,
  );
  const initialAuthority = await ledger.loadInitialRunnerStateAuthority({
    control_plane_authority: controlPlaneAuthority,
    evaluated_scope_receipt: evaluatedScopeReceipt,
    workflow_command_handle: workflowCommandHandle,
  });
  return {
    ledger,
    lease,
    evaluatedScopeReceipt,
    workflowCommandHandle,
    initialAuthority,
    capability,
    verifier,
    preflight,
    preScopeReceipt,
    controlPlaneAuthority,
    continuityAuthority: discovery.continuity_authority,
    discovery,
  };
}

async function acquireEstablishedRunnerContext({
  ledger,
  runIdentity,
  observationBoundary,
  transportStartSecond,
  minimalServerTime,
}) {
  const minimalScopeHandle = await loadFixtureMinimalScope();
  const preScopeReceipt = await ledger
    .createPullRequestEventEvaluatedScopeReceipt({
      minimal_scope_handle: minimalScopeHandle,
      trigger_identity: {
        event_name: "pull_request_target",
        ref: "refs/heads/main",
        sha: BASE,
      },
    });
  const workflowCommandHandle =
    await createPullRequestTargetWorkflowCommandHandle({
      runIdentity,
      observationBoundary,
    });
  const discovery = await acquireFixtureLeasedDiscovery({
    ledger,
    command: workflowCommandHandle,
    preScopeReceipt,
    minimalPre: minimalScopeHandle,
    transportStartSecond,
    minimalServerTime,
  });
  const evaluatedScopeReceipt = discovery.effect_evaluated_scope_receipt;
  const controlPlaneAuthority = await ledger.loadControlPlaneAuthority(
    evaluatedScopeReceipt.scope,
  );
  return {
    discovery,
    evaluatedScopeReceipt,
    preScopeReceipt,
    workflowCommandHandle,
    controlPlaneAuthority,
  };
}

async function acquireFixtureLeasedDiscovery({
  ledger,
  command,
  preScopeReceipt,
  minimalPre,
  artifactSelectors = [],
  transport: transportOverride = null,
  leaseTtlSeconds = 600,
  transportStartSecond = 600,
  minimalServerTime = "2026-08-13T12:20:00.000Z",
  minimalFetch = null,
}) {
  const transportFixture = providerTransportFixture({
    startSecond: transportStartSecond,
  });
  const transport = transportOverride ??
    createProviderTransport(transportFixture.fetch);
  return acquireV2LeaseThenLoadDiscovery({
    command,
    fetch: minimalFetch ??
      ((url, init) => minimalScopeFetchAt(minimalServerTime, url, init)),
    token: SYNTHETIC_BEARER,
    rest_base_url: "https://api.github.test",
    graphql_url: "https://api.github.test/graphql",
    ledger,
    transport: artifactSelectors.length === 0
      ? transport
      : {
          loadSnapshot: (input) => transport.loadSnapshot({
            ...input,
            artifactSelectors,
          }),
        },
    lease_ttl_seconds: leaseTtlSeconds,
    evaluated_scope_receipt: preScopeReceipt,
    pre_scope: minimalPre,
  });
}

async function releaseFixtureDiscoveryLease(ledger, discovery) {
  const loaded = await ledger.load();
  return ledger.releaseLease({
    predecessor_commit_sha: loaded.tip_commit_sha,
    lease_receipt: discovery.lease_receipt,
    released_at: loaded.post_ref.server_time,
    control_comment_binding: loaded.control_comment_binding,
  }, {
    evaluated_scope_receipt: discovery.lease_evaluated_scope_receipt,
  });
}

function schedulerInputsForEstablishedAuthority(
  authority,
  { snapshotId, decision = "pending" } = {},
) {
  const observedAt = authority.source_authority.post_ref_receipt.server_time;
  const evaluation = {
    epoch_id: authority.scheduling.epoch.id,
    decision,
    complete: true,
    snapshot_id: snapshotId,
    snapshot_fingerprint: `sha256:${"6".repeat(64)}`,
    observed_at: observedAt,
    provider_activity_fingerprint: `sha256:${"7".repeat(64)}`,
    no_start_candidate: structuredClone(
      authority.scheduling.no_start_candidate,
    ),
    run_id: authority.scheduling.run_identity.run_id,
    run_attempt: authority.scheduling.run_identity.run_attempt,
  };
  return {
    scheduler_evaluation: evaluation,
    status_plan: {
      mode: authority.scheduling.status_target_mode,
      decision,
      writes: [],
      terminal_cutover: false,
      freshness_assurance: "point-in-time",
    },
  };
}

async function acquireDefaultDiscovery(
  ledger,
  { leaseTtlSeconds = 600 } = {},
) {
  const minimalPre = await loadFixtureMinimalScope();
  const preScopeReceipt = await ledger.defaultEvaluatedScopeReceipt();
  return acquireFixtureLeasedDiscovery({
    ledger,
    command: await createPullRequestTargetWorkflowCommandHandle(),
    preScopeReceipt,
    minimalPre,
    leaseTtlSeconds,
  });
}

function factoryInput(fixture, capability) {
  return {
    fetch: fixture.fetch,
    token: SYNTHETIC_BEARER,
    repository: REPOSITORY,
    ledgerRef: V2_GIT_LEDGER_REF,
    restBaseUrl: "https://api.github.test",
    capabilityReceipt: capability,
    verifyWorkflowProvenance: provenanceVerifier(),
  };
}

function capabilityReceipt() {
  return sealCapability(capabilityBase());
}

function capabilityReceiptForTrigger(eventName, ref) {
  const base = capabilityBase();
  base.workflow_provenance_policy.allowed_event_names = [eventName];
  base.workflow_provenance_policy.allowed_refs = [ref];
  return sealCapability(base);
}

function bootstrapInput() {
  return bootstrapInputForCapability(capabilityReceipt());
}

function bootstrapInputForCapability(capability) {
  return sealBootstrapInput(bootstrapInputBase(capability));
}

function bootstrapInputBase(capability = capabilityBase()) {
  return {
    schema: V2_GIT_LEDGER_BOOTSTRAP_INPUT_SCHEMA,
    schema_version: 1,
    sealed: false,
    bootstrap_eligible: true,
    current_attestation: false,
    repository: structuredClone(capability.repository),
    repository_endpoint_receipt:
      structuredClone(capability.repository_endpoint_receipt),
    ledger_ref: capability.ledger_ref,
    permissions: {
      contents_write_requested: true,
      metadata_read_observed: true,
      observed_only: true,
      observation_receipt_digest:
        capability.permissions.observation_receipt_digest,
    },
    protection: structuredClone(capability.protection),
    ruleset_receipt: structuredClone(capability.ruleset_receipt),
    protection_receipt: structuredClone(capability.protection_receipt),
    controller_release: structuredClone(capability.controller_release),
    workflow_provenance_policy:
      structuredClone(capability.workflow_provenance_policy),
    provider_identity_policy:
      structuredClone(capability.provider_identity_policy),
    observed_at: capability.observed_at,
  };
}

function sealBootstrapInput(base) {
  return {
    ...structuredClone(base),
    input_digest: digestCanonical(
      "codex-review-gate-v2-git-ledger-bootstrap-input",
      base,
    ),
  };
}

function capabilityBase() {
  return {
    schema: V2_GIT_LEDGER_CAPABILITY_SCHEMA,
    schema_version: 1,
    feature_enabled: true,
    bootstrap_candidate_digest: `sha256:${"b".repeat(64)}`,
    repository: REPOSITORY,
    repository_endpoint_receipt: {
      method: "GET",
      path: "/repos/owner/repo",
      status: 200,
      server_time: TIME,
      raw_body_sha256: `sha256:${"4".repeat(64)}`,
    },
    ledger_ref: V2_GIT_LEDGER_REF,
    permissions: {
      contents_write_observed: true,
      metadata_read_observed: true,
      observed_only: true,
      observation_receipt_digest: `sha256:${"3".repeat(64)}`,
    },
    protection: {
      deletion_blocked: true,
      non_fast_forward_blocked: true,
      force_pushes_blocked: true,
      live_ruleset_receipt_digest: `sha256:${"e".repeat(64)}`,
      accepted_records_restricted_by_oidc_source: true,
      source_workflow_pin: {
        repository: {
          owner: "Joey-Tools",
          name: "codex-review-gate-action",
          id: "4242",
        },
        workflow_path: ".github/workflows/codex-review-gate.yml",
        workflow_ref:
          "owner/repo/.github/workflows/controller.yml@refs/heads/main",
        workflow_sha: "2".repeat(40),
        job_workflow_ref:
          `Joey-Tools/codex-review-gate-action/.github/workflows/codex-review-gate.yml@${"2".repeat(40)}`,
        job_workflow_sha: "2".repeat(40),
        caller_workflow_file_receipt_digest: `sha256:${"a".repeat(64)}`,
        job_workflow_file_receipt_digest: `sha256:${"b".repeat(64)}`,
        release_receipt_digest: `sha256:${"c".repeat(64)}`,
      },
    },
    ruleset_receipt: {
      receipt_digest: `sha256:${"e".repeat(64)}`,
      configuration_digest: `sha256:${"f".repeat(64)}`,
      protection_digest: `sha256:${"e".repeat(64)}`,
      repository_id: REPOSITORY.id,
      ledger_ref: V2_GIT_LEDGER_REF,
      ruleset_ids: ["101"],
      target_includes_exact_ref: true,
      deletion_blocked: true,
      non_fast_forward_blocked: true,
      force_pushes_blocked: true,
      bypass_actors_empty: true,
    },
    protection_receipt: {
      receipt_digest: `sha256:${"0".repeat(64)}`,
      protection_digest: `sha256:${"e".repeat(64)}`,
      ruleset_receipt_digest: `sha256:${"e".repeat(64)}`,
      deletion_blocked: true,
      non_fast_forward_blocked: true,
      source_workflow_pinned: true,
    },
    controller_release: {
      repository: {
        owner: "Joey-Tools",
        name: "codex-review-gate-action",
        id: "4242",
      },
      release_sha: "2".repeat(40),
      workflow_path: ".github/workflows/codex-review-gate.yml",
      workflow_ref:
        "owner/repo/.github/workflows/controller.yml@refs/heads/main",
      workflow_sha: "2".repeat(40),
      job_workflow_ref:
        `Joey-Tools/codex-review-gate-action/.github/workflows/codex-review-gate.yml@${"2".repeat(40)}`,
      job_workflow_sha: "2".repeat(40),
      caller_workflow_file_receipt_digest: `sha256:${"a".repeat(64)}`,
      job_workflow_file_receipt_digest: `sha256:${"b".repeat(64)}`,
      release_receipt_digest: `sha256:${"c".repeat(64)}`,
      current: true,
    },
    workflow_provenance_policy: provenancePolicy(),
    provider_identity_policy: providerIdentityPolicy(),
    observed_at: TIME,
  };
}

function providerIdentityPolicy() {
  const withoutDigest = {
    schema: "codex-review-gate-provider-identity-policy-v2",
    schema_version: 1,
    catalog_version: 1,
    actor: CODEX_ACTOR,
    app: CODEX_APP,
    actor_endpoint_path:
      "/users/chatgpt-codex-connector%5Bbot%5D",
    app_endpoint_path: "/apps/chatgpt-codex-connector",
  };
  return {
    ...withoutDigest,
    catalog_digest: digestCanonical(
      "codex-review-gate-v2-provider-identity-policy",
      withoutDigest,
    ),
  };
}

function sealCapability(base) {
  return {
    ...structuredClone(base),
    receipt_digest: digestCanonical(
      "codex-review-gate-v2-git-ledger-capability",
      base,
    ),
  };
}

function fixtureCapabilityBindingDigest(capability) {
  return digestCanonical(
    "codex-review-gate-v2-git-ledger-capability-binding",
    {
      repository: capability.repository,
      repository_endpoint_receipt: capability.repository_endpoint_receipt,
      ledger_ref: capability.ledger_ref,
      permissions: capability.permissions,
      protection: capability.protection,
      ruleset_receipt: capability.ruleset_receipt,
      protection_receipt: capability.protection_receipt,
      controller_release: capability.controller_release,
      workflow_provenance_policy: capability.workflow_provenance_policy,
      provider_identity_policy: capability.provider_identity_policy,
    },
  );
}

function provenancePolicy() {
  const subject = "repo:owner/repo:pull_request";
  return {
    issuer: "https://token.actions.githubusercontent.com",
    audience: V2_GIT_LEDGER_OIDC_AUDIENCE,
    discovery_url:
      "https://token.actions.githubusercontent.com/.well-known/openid-configuration",
    jwks_uri: "https://token.actions.githubusercontent.com/.well-known/jwks",
    algorithm: "RS256",
    required_claims: [...V2_GIT_LEDGER_OIDC_CLAIMS].sort(),
    claims_supported: [...V2_GIT_LEDGER_OIDC_CLAIMS].sort(),
    repository_owner_id: "88",
    subject_pattern: subject,
    subject_pattern_digest: digestCanonical(
      "codex-review-gate-v2-oidc-subject-pattern",
      subject,
    ),
    subject_policy_receipt_digest: `sha256:${"7".repeat(64)}`,
    execution_policy_receipt_digest: `sha256:${"8".repeat(64)}`,
    replay_registry_policy_receipt_digest: `sha256:${"9".repeat(64)}`,
    fork_pull_requests_api_only: true,
    candidate_code_execution_blocked: true,
    allowed_event_names: ["pull_request"],
    allowed_refs: ["refs/pull/7/merge"],
  };
}

function createFixtureProvenanceMintState(historicalTokenAuthorities) {
  const authoritiesByOrdinal = new Map();
  const tokenDigests = new Set();
  const jtis = new Set();
  let nextOrdinal = 0;
  const mintedAuthorities = [];
  for (const authority of historicalTokenAuthorities) {
    authoritiesByOrdinal.set(authority.ordinal, structuredClone(authority));
    tokenDigests.add(authority.token_sha256);
    if (authority.jti !== null) jtis.add(authority.jti);
    nextOrdinal = Math.max(nextOrdinal, authority.ordinal);
  }
  return Object.freeze({
    get minted_authorities() {
      return mintedAuthorities.map((authority) =>
        structuredClone(authority));
    },
    historical_matches(authorities) {
      return canonicalJson(authorities) ===
        canonicalJson(historicalTokenAuthorities);
    },
    authority_for_ordinal(ordinal) {
      const authority = authoritiesByOrdinal.get(ordinal);
      return authority === undefined ? null : structuredClone(authority);
    },
    mint({
      fixed_jti,
      jti_prefix,
      omit_jti,
      request_digest,
      run_identity,
      trigger,
    }) {
      const ordinal = nextOrdinal + 1;
      const jti = omit_jti
        ? null
        : fixed_jti ?? `${jti_prefix}-${ordinal}`;
      if (jti !== null) {
        assert.equal(jtis.has(jti), false, "duplicate fixture provenance jti");
      }
      const compactJwt = fixtureJwt({ ordinal, jti, request_digest });
      const tokenSha256 = rawDigest(compactJwt);
      assert.equal(
        tokenDigests.has(tokenSha256),
        false,
        "duplicate fixture provenance token",
      );
      const authority = Object.freeze({
        ordinal,
        token_sha256: tokenSha256,
        jti,
        request_digest,
        run_identity: structuredClone(run_identity),
        trigger: structuredClone(trigger),
        compact_jwt: compactJwt,
      });
      nextOrdinal = ordinal;
      tokenDigests.add(tokenSha256);
      if (jti !== null) jtis.add(jti);
      authoritiesByOrdinal.set(ordinal, authority);
      mintedAuthorities.push(authority);
      return structuredClone(authority);
    },
  });
}

function provenanceVerifier({
  fixedJti = null,
  omitJti = false,
  jtiPrefix = "oidc-jti",
  eventName = "pull_request",
  ref = "refs/pull/7/merge",
  shaValue = null,
  claimsSupported = V2_GIT_LEDGER_OIDC_CLAIMS,
  runIdentity = OWNER,
  deriveTriggerFromRequest = false,
  historicalTokenAuthorities = [],
  historicalReverificationLog = null,
  fixtureProvenanceMintState = null,
} = {}) {
  if (historicalReverificationLog !== null) {
    assert.ok(Array.isArray(historicalReverificationLog));
    assert.equal(historicalReverificationLog.length, 0);
  }
  if (fixtureProvenanceMintState !== null) {
    assert.equal(
      fixtureProvenanceMintState.historical_matches(
        historicalTokenAuthorities,
      ),
      true,
    );
  }
  let ordinal = 0;
  const runIdentityByTokenOrdinal = new Map();
  const triggerByTokenOrdinal = new Map();
  const historicalAuthorityByTokenOrdinal = new Map();
  for (const authority of historicalTokenAuthorities) {
    assertExactFixtureKeys(authority, [
      "commit_oid",
      "jti",
      "ordinal",
      "request_digest",
      "run_identity",
      "token_sha256",
      "trigger",
    ], "historical provenance authority");
    assertFixtureOid(authority.commit_oid, "historical provenance commit oid");
    assert.ok(Number.isSafeInteger(authority.ordinal));
    assert.ok(authority.ordinal > 0);
    assert.equal(
      historicalAuthorityByTokenOrdinal.has(authority.ordinal),
      false,
      `duplicate historical provenance ordinal ${authority.ordinal}`,
    );
    assert.match(authority.token_sha256, /^sha256:[0-9a-f]{64}$/u);
    assert.match(authority.request_digest, /^sha256:[0-9a-f]{64}$/u);
    assert.equal(
      authority.jti === null || typeof authority.jti === "string",
      true,
    );
    assertExactFixtureKeys(authority.run_identity, [
      "actor_id",
      "run_attempt",
      "run_id",
    ], "historical provenance run identity");
    assertExactFixtureKeys(authority.trigger, [
      "event_name",
      "ref",
      "sha",
    ], "historical provenance trigger");
    historicalAuthorityByTokenOrdinal.set(
      authority.ordinal,
      structuredClone(authority),
    );
    runIdentityByTokenOrdinal.set(
      authority.ordinal,
      structuredClone(authority.run_identity),
    );
    triggerByTokenOrdinal.set(
      authority.ordinal,
      structuredClone(authority.trigger),
    );
    ordinal = Math.max(ordinal, authority.ordinal);
  }
  return async (verifierRequest) => {
    const request = verifierRequest.provenance_request;
    let tokenOrdinal;
    let jti;
    let compactJwt;
    if (verifierRequest.mode === "mint-and-verify") {
      const evaluated = request.evaluated_scope_receipt;
      const tokenTrigger = {
        event_name: deriveTriggerFromRequest && evaluated !== null &&
            evaluated !== undefined
          ? evaluated.trigger_event_name
          : eventName,
        ref: deriveTriggerFromRequest && evaluated !== null &&
            evaluated !== undefined
          ? evaluated.trigger_ref
          : ref,
        sha: deriveTriggerFromRequest && evaluated !== null &&
            evaluated !== undefined
          ? evaluated.trigger_sha
          : shaValue,
      };
      if (fixtureProvenanceMintState === null) {
        ordinal += 1;
        tokenOrdinal = ordinal;
        jti = omitJti ? null : fixedJti ?? `${jtiPrefix}-${tokenOrdinal}`;
        compactJwt = fixtureJwt({
          ordinal: tokenOrdinal,
          jti,
          request_digest: request.request_digest,
        });
        runIdentityByTokenOrdinal.set(
          tokenOrdinal,
          structuredClone(runIdentity),
        );
        triggerByTokenOrdinal.set(
          tokenOrdinal,
          structuredClone(tokenTrigger),
        );
      } else {
        const mintedAuthority = fixtureProvenanceMintState.mint({
          fixed_jti: fixedJti,
          jti_prefix: jtiPrefix,
          omit_jti: omitJti,
          request_digest: request.request_digest,
          run_identity: runIdentity,
          trigger: tokenTrigger,
        });
        tokenOrdinal = mintedAuthority.ordinal;
        jti = mintedAuthority.jti;
        compactJwt = mintedAuthority.compact_jwt;
        runIdentityByTokenOrdinal.set(
          tokenOrdinal,
          structuredClone(mintedAuthority.run_identity),
        );
        triggerByTokenOrdinal.set(
          tokenOrdinal,
          structuredClone(mintedAuthority.trigger),
        );
      }
    } else {
      compactJwt = verifierRequest.compact_jwt;
      const payload = JSON.parse(Buffer.from(
        compactJwt.split(".")[1],
        "base64url",
      ).toString("utf8"));
      tokenOrdinal = payload.ordinal;
      jti = payload.jti ?? null;
      const historicalAuthority = historicalAuthorityByTokenOrdinal.get(
        tokenOrdinal,
      );
      if (historicalAuthority !== undefined) {
        assert.equal(
          rawDigest(compactJwt),
          historicalAuthority.token_sha256,
        );
        assert.equal(
          payload.request_digest,
          historicalAuthority.request_digest,
        );
        assert.equal(request.request_digest, historicalAuthority.request_digest);
        assert.equal(jti, historicalAuthority.jti);
        historicalReverificationLog?.push(Object.freeze({
          commit_oid: historicalAuthority.commit_oid,
          token_sha256: historicalAuthority.token_sha256,
          jti: historicalAuthority.jti,
          request_digest: historicalAuthority.request_digest,
        }));
      } else if (fixtureProvenanceMintState !== null) {
        const mintedAuthority = fixtureProvenanceMintState
          .authority_for_ordinal(tokenOrdinal);
        assert.notEqual(
          mintedAuthority,
          null,
          `unknown shared fixture provenance ordinal ${tokenOrdinal}`,
        );
        assert.equal(rawDigest(compactJwt), mintedAuthority.token_sha256);
        assert.equal(payload.request_digest, mintedAuthority.request_digest);
        assert.equal(request.request_digest, mintedAuthority.request_digest);
        assert.equal(jti, mintedAuthority.jti);
        runIdentityByTokenOrdinal.set(
          tokenOrdinal,
          structuredClone(mintedAuthority.run_identity),
        );
        triggerByTokenOrdinal.set(
          tokenOrdinal,
          structuredClone(mintedAuthority.trigger),
        );
      }
    }
    const tokenRunIdentity = runIdentityByTokenOrdinal.get(tokenOrdinal) ??
      runIdentity;
    const tokenTrigger = triggerByTokenOrdinal.get(tokenOrdinal) ?? {
      event_name: eventName,
      ref,
      sha: shaValue,
    };
    const epoch = Math.floor(Date.parse(request.github_server_time) / 1000);
    const effectScope = request.effect_scope;
    const claims = {
      aud: request.audience,
      event_name: tokenTrigger.event_name,
      exp: epoch + 300,
      iat: epoch - 1,
      iss: "https://token.actions.githubusercontent.com",
      job_workflow_ref: request.source_workflow.job_workflow_ref,
      job_workflow_sha: request.source_workflow.job_workflow_sha,
      nbf: epoch - 1,
      ref: tokenTrigger.ref,
      repository: `${REPOSITORY.owner}/${REPOSITORY.name}`,
      repository_id: REPOSITORY.id,
      repository_owner_id: "88",
      run_attempt: String(tokenRunIdentity.run_attempt),
      run_id: tokenRunIdentity.run_id,
      sha: tokenTrigger.sha ?? effectScope?.potential_merge_commit_oid ??
        POTENTIAL,
      sub: "repo:owner/repo:pull_request",
      workflow_ref: request.source_workflow.workflow_ref,
      workflow_sha: request.source_workflow.workflow_sha,
    };
    if (jti !== null) claims.jti = jti;
    const operationBinding = {
      operation: request.operation,
      repository: request.repository,
      ledger_ref: request.ledger_ref,
      predecessor_commit_sha: request.predecessor_commit_sha,
      protection_receipt_digest: request.protection_receipt_digest,
      source_workflow: request.source_workflow,
      effect_scope: request.effect_scope,
      evaluated_scope_receipt: request.evaluated_scope_receipt,
      record_identity: request.record_identity,
      github_server_time: request.github_server_time,
      nonce: request.nonce,
      audience: request.audience,
      request_digest: request.request_digest,
    };
    const withoutDigest = {
      schema: V2_GIT_LEDGER_PROVENANCE_RECEIPT_SCHEMA,
      schema_version: 1,
      verified: true,
      signature_verified: true,
      jwks_verified: true,
      live_supported: true,
      issuer: "https://token.actions.githubusercontent.com",
      audience: request.audience,
      algorithm: "RS256",
      key_id: "fixture-key-1",
      claims,
      token_sha256: rawDigest(compactJwt),
      discovery: {
        url: "https://token.actions.githubusercontent.com/.well-known/openid-configuration",
        server_time: TIME,
        raw_body_sha256: `sha256:${"5".repeat(64)}`,
        claims_supported: [...claimsSupported].sort(),
      },
      jwks: {
        url: "https://token.actions.githubusercontent.com/.well-known/jwks",
        server_time: TIME,
        raw_body_sha256: `sha256:${"6".repeat(64)}`,
      },
      verified_at_server_time: request.github_server_time,
      replay_prevention_receipt_digest:
        `sha256:${(tokenOrdinal + 4096).toString(16).padStart(64, "0")}`,
      operation_binding: operationBinding,
    };
    const receipt = {
      ...withoutDigest,
      receipt_digest: digestCanonical(
        "codex-review-gate-v2-git-ledger-provenance",
        withoutDigest,
      ),
    };
    return {
      schema: V2_GIT_LEDGER_PROVENANCE_VERIFIER_RESULT_SCHEMA,
      schema_version: 1,
      mode: verifierRequest.mode,
      compact_jwt:
        verifierRequest.mode === "mint-and-verify" ? compactJwt : null,
      receipt,
    };
  };
}

function fixtureJwt(payload) {
  const header = Buffer.from(JSON.stringify({
    alg: "RS256",
    kid: "fixture-key-1",
    typ: "JWT",
  }), "utf8").toString("base64url");
  const body = Buffer.from(JSON.stringify(payload), "utf8").toString("base64url");
  const signature = Buffer.from(
    `fixture-signature-${payload.ordinal}`,
    "utf8",
  ).toString("base64url");
  return `${header}.${body}.${signature}`;
}

function leaseInput(predecessor) {
  return {
    predecessor_commit_sha: predecessor,
    pull_request: PR,
    head_ref_oid: HEAD,
    base_ref_oid: BASE,
    potential_merge_commit_oid: POTENTIAL,
    lease_id: "lease-run-9001-attempt-1",
    owner: OWNER,
    observed_at: TIME,
    lease_ttl_seconds: 600,
    control_comment_binding: null,
  };
}

function effectIntentInput({ predecessor, lease, effectId, idempotencyKey, at }) {
  return {
    predecessor_commit_sha: predecessor,
    scope: {
      pull_request: PR,
      head_ref_oid: HEAD,
      base_ref_oid: BASE,
      potential_merge_commit_oid: POTENTIAL,
    },
    kind: "thread-resolution-observation",
    effect_id: effectId,
    idempotency_key: idempotencyKey,
    server_observed_at: at,
    action: {
      thread_id: `thread-${effectId}`,
      head_oid: HEAD,
    },
    control_comment_binding: null,
    lease_receipt: lease,
  };
}

function effectRecord({ predecessor, lease, effectId, idempotencyKey, at }) {
  return createV2GitLedgerEffectIntentRecord(effectIntentInput({
    predecessor,
    lease,
    effectId,
    idempotencyKey,
    at,
  }));
}

function automaticReservationRecord({ predecessor, lease, generation }) {
  throw new Error("automaticReservationRecord requires protected scheduler lineage");
}

function runnerEpochId() {
  return `v2-head:${runnerDigestCanonical("codex-review-gate-v2-head-epoch", {
    repository_node_id: REPOSITORY.node_id,
    pull_request_node_id: PR.node_id,
    head_ref_oid: HEAD,
  }).slice("sha256:".length)}`;
}

function emptyHeadLedger(observedAt = TIME) {
  return {
    schema: "codex-review-gate-head-ledger-v2",
    schema_version: 1,
    repository_node_id: REPOSITORY.node_id,
    pull_request_node_id: PR.node_id,
    head_ref_oid: HEAD,
    automatic_request_count: 0,
    exact_sha_context_count: 0,
    latest_status_idempotency_key: null,
    bound_attempt_ids: [],
    observed_at: observedAt,
  };
}

function initialScheduling({
  trigger = "schedule",
  statusTargetMode = "test-merge-with-head-sentinel",
  decision = "pending",
  runId = OWNER.run_id,
  runAttempt = OWNER.run_attempt,
} = {}) {
  const epochId = runnerEpochId();
  return {
    trigger,
    public_wait_supported: false,
    status_target_mode: statusTargetMode,
    run_identity: { run_id: runId, run_attempt: runAttempt },
    epoch: {
      id: epochId,
      started_at: TIME,
      controlled_request: null,
      automatic_request: {
        state: "available",
        generation_index: 1,
        recovery_authority: null,
        intent_id: null,
        intent_persisted_at: null,
        effect_attempted_at: null,
      },
    },
    complete_snapshots: [],
    status: {
      exact_sha_context_count: 0,
      latest_idempotency_key: null,
      head_sentinel_receipt: null,
    },
    applied_action_keys: [],
    no_start_candidate: null,
    wait_completions: [],
    decision,
  };
}

function schedulerObservationRecord({
  predecessor,
  lease,
  priorAuthorityDigest,
  trigger = "schedule",
  effectId = "scheduler-observation-1",
  idempotencyKey = "scheduler-observation-key-1",
  initialAuthority = null,
  decision = "pending",
  pullRequest = PR,
  headOid = HEAD,
  baseOid = BASE,
  potentialMergeCommitOid = POTENTIAL,
  statusIdempotencyKey = "status:fixture-head-pending",
}) {
  const priorScheduling = initialAuthority === null
    ? initialScheduling({ trigger })
    : structuredClone(initialAuthority.scheduling);
  const snapshotTime = initialAuthority === null
    ? TIME
    : initialAuthority.source_authority.post_ref_receipt.server_time;
  const evaluation = {
    epoch_id: priorScheduling.epoch.id,
    decision: initialAuthority === null ? priorScheduling.decision : decision,
    complete: true,
    snapshot_id: "snapshot-1",
    snapshot_fingerprint: `sha256:${"8".repeat(64)}`,
    observed_at: snapshotTime,
    provider_activity_fingerprint: `sha256:${"9".repeat(64)}`,
    no_start_candidate: null,
    run_id: initialAuthority?.scheduling.run_identity.run_id ?? OWNER.run_id,
    run_attempt:
      initialAuthority?.scheduling.run_identity.run_attempt ?? OWNER.run_attempt,
  };
  delete priorScheduling.decision;
  const schedulerPlan = planV2Actions({
    schema: V2_SCHEDULER_SCHEMA,
    schema_version: V2_SCHEDULER_SCHEMA_VERSION,
    trigger: priorScheduling.trigger,
    now: snapshotTime,
    public_wait_supported: priorScheduling.public_wait_supported,
    status_target_mode: priorScheduling.status_target_mode,
    epoch: priorScheduling.epoch,
    evaluation,
    complete_snapshots: priorScheduling.complete_snapshots,
    status: {
      exact_sha_context_count: 0,
      latest_idempotency_key: null,
    },
    applied_action_keys: [],
    wait_completions: priorScheduling.wait_completions,
  });
  const statusPlan = {
    mode: priorScheduling.status_target_mode,
    decision: evaluation.decision,
    writes: trigger === "manual" ||
        !schedulerPlan.actions.some((action) => action.kind === "publish_status")
      ? []
      : [{
      role: "head-sentinel",
      sha: headOid,
      context: "codex/github-review-gate",
      state: "pending",
      reason: "awaiting-terminal-test-merge-decision",
      idempotency_key: statusIdempotencyKey,
    }],
    terminal_cutover: false,
    freshness_assurance: "point-in-time",
  };
  const action = {
    schema: "codex-review-gate-git-ledger-scheduler-observation-v2",
    schema_version: 1,
    prior_authority_digest: initialAuthority === null
      ? priorAuthorityDigest
      : initialAuthority.prior_authority_digest,
    prior_scheduling: priorScheduling,
    prior_head_ledger: initialAuthority === null
      ? emptyHeadLedger()
      : structuredClone(initialAuthority.head_ledger),
    scheduler_evaluation: evaluation,
    scheduler_plan: schedulerPlan,
    scheduler_plan_digest: digestCanonical(
      "codex-review-gate-v2-scheduler-plan",
      schedulerPlan,
    ),
    status_plan: statusPlan,
    status_plan_digest: digestCanonical(
      "codex-review-gate-v2-status-plan",
      statusPlan,
    ),
    evaluated_scope_receipt_digest: initialAuthority === null
      ? `sha256:${"a".repeat(64)}`
      : initialAuthority.evaluated_scope_authority
        .record_evaluated_scope_receipt_digest,
    snapshot_server_time: snapshotTime,
    initial_runner_state_authority: initialAuthority === null
      ? null
      : structuredClone(initialAuthority),
  };
  return createV2GitLedgerRecord({
    record_type: "effect-intent",
    predecessor_commit_sha: predecessor,
    pull_request: pullRequest,
    head_ref_oid: headOid,
    base_ref_oid: baseOid,
    potential_merge_commit_oid: potentialMergeCommitOid,
    kind: "scheduler-observation",
    effect_id: effectId,
    idempotency_key: idempotencyKey,
    server_observed_at: snapshotTime,
    payload: effectPayload({
      phase: "intent",
      kind: "scheduler-observation",
      predecessor,
      action,
      pullRequest,
      headOid,
      baseOid,
      potentialMergeCommitOid,
    }),
    control_comment_binding: null,
    lease: leaseBinding(lease),
  });
}

function requestReservation({
  generation,
  schedulerIntentId,
  createdAt = TIME,
  headLedger = emptyHeadLedger(createdAt),
}) {
  const statusLedgerBinding = {
    head_ref_oid: HEAD,
    automatic_request_count: headLedger.automatic_request_count,
    exact_sha_context_count: headLedger.exact_sha_context_count,
    latest_status_idempotency_key: headLedger.latest_status_idempotency_key,
    bound_attempt_ids: structuredClone(headLedger.bound_attempt_ids),
    observed_at: headLedger.observed_at,
    ledger_digest: runnerDigestCanonical(
      "codex-review-gate-v2-head-ledger",
      headLedger,
    ),
  };
  const preScopeDigest = digestCanonical("codex-review-gate-v2-scope", {
    repository_node_id: REPOSITORY.node_id,
    pull_request_node_id: PR.node_id,
    head_ref_oid: HEAD,
    base_ref_oid: BASE,
    potential_merge_commit_oid: POTENTIAL,
  });
  const intentDigest = runnerDigestCanonical("codex-review-gate-v2-request-intent", {
    repository_node_id: REPOSITORY.node_id,
    pull_request_node_id: PR.node_id,
    head_ref_oid: HEAD,
    ordinal: generation.index,
    generation_id: generation.generation_id,
    scheduler_intent_id: schedulerIntentId,
    body: "@codex review",
    created_at: createdAt,
    pre_scope_digest: preScopeDigest,
    ledger_digest: statusLedgerBinding.ledger_digest,
    recovery_authority: null,
  });
  const intentId = `v2-request:${intentDigest.slice("sha256:".length)}`;
  const withoutDigest = {
    schema: "codex-review-gate-request-reservation-v2",
    schema_version: 1,
    repository: {
      owner: REPOSITORY.owner,
      name: REPOSITORY.name,
      node_id: REPOSITORY.node_id,
    },
    pull_request: PR,
    epoch_head_sha: HEAD,
    ordinal: generation.index,
    generation_id: generation.generation_id,
    generation_kind: generation.kind,
    generation_index: generation.index,
    recovery_authority: null,
    scheduler_intent_id: schedulerIntentId,
    intent_id: intentId,
    intent_digest: intentDigest,
    attempt_id: `v2-attempt:${runnerDigestCanonical(
      "codex-review-gate-v2-request-attempt-id",
      { intent_id: intentId, intent_digest: intentDigest },
    ).slice("sha256:".length)}`,
    body: "@codex review",
    created_at: createdAt,
    automatic: true,
    consumed: true,
    pre_scope_digest: preScopeDigest,
    status_ledger_binding: statusLedgerBinding,
  };
  return {
    ...withoutDigest,
    reservation_digest: runnerDigestCanonical(
      "codex-review-gate-v2-request-reservation",
      withoutDigest,
    ),
  };
}

function protectedAutomaticReservationRecord({
  predecessor,
  lease,
  generation,
  observationRecordOid,
  observation,
  headLedger = emptyHeadLedger(
    observation.payload.action.snapshot_server_time,
  ),
}) {
  const persist = observation.payload.action.scheduler_plan.actions.find((action) =>
    action.kind === "persist_auto_request_intent");
  const post = observation.payload.action.scheduler_plan.actions.find((action) =>
    action.kind === "post_review_request");
  const reservation = requestReservation({
    generation,
    schedulerIntentId: persist.intent_id,
    createdAt: headLedger.observed_at,
    headLedger,
  });
  return createV2GitLedgerRecord({
    record_type: "effect-intent",
    predecessor_commit_sha: predecessor,
    pull_request: PR,
    head_ref_oid: HEAD,
    base_ref_oid: BASE,
    potential_merge_commit_oid: POTENTIAL,
    kind: "automatic-request-reservation",
    effect_id: `reservation-${generation.generation_id}`,
    idempotency_key: `reservation-key-${generation.generation_id}`,
    server_observed_at: headLedger.observed_at,
    payload: effectPayload({
      phase: "intent",
      kind: "automatic-request-reservation",
      predecessor,
      generation,
      action: {
        scheduler_observation_record_oid: observationRecordOid,
        scheduler_action_key: persist.idempotency_key,
        post_scheduler_action_key: post.idempotency_key,
        reservation,
        reservation_digest: reservation.reservation_digest,
        budget_limit: 3,
      },
    }),
    control_comment_binding: null,
    lease: leaseBinding(lease),
  });
}

function priorRunnerAuthorityDigest(records) {
  return digestCanonical("codex-review-gate-v2-runner-prior-authority", {
    scope: {
      pull_request: PR,
      head_ref_oid: HEAD,
      base_ref_oid: BASE,
      potential_merge_commit_oid: POTENTIAL,
    },
    records: records.map((entry) => ({
      record_oid: entry.commit_sha,
      parent_oids: entry.parents,
      tree_oid: entry.tree_sha,
      blob_oid: entry.blob_sha,
      envelope_digest: entry.envelope.envelope_digest,
    })),
  });
}

function reservationStatusIntentRecord({
  predecessor,
  lease,
  generation,
  reservationRecordOid,
  reservationDigest,
}) {
  return createV2GitLedgerRecord({
    record_type: "effect-intent",
    predecessor_commit_sha: predecessor,
    pull_request: PR,
    head_ref_oid: HEAD,
    base_ref_oid: BASE,
    potential_merge_commit_oid: POTENTIAL,
    kind: "reservation-status-write",
    effect_id: `reservation-status-${generation.generation_id}`,
    idempotency_key: `reservation-status-key-${generation.generation_id}`,
    server_observed_at: TIME,
    payload: effectPayload({
      phase: "intent",
      kind: "reservation-status-write",
      predecessor,
      generation,
      action: {
        reservation_record_oid: reservationRecordOid,
        generation_id: generation.generation_id,
        ordinal: generation.index,
        target_sha: HEAD,
        context: `codex/github-review-gate-reservation/${generation.index}`,
        state: "pending",
        description_digest: reservationDigest,
      },
    }),
    control_comment_binding: null,
    lease: leaseBinding(lease),
  });
}

function reservationStatusResponseRecord(intentRecord, intentCommitSha) {
  const action = intentRecord.payload.action;
  const pageBodyDigest = `sha256:${"b".repeat(64)}`;
  return createV2GitLedgerEffectResponseRecord({
    intent_record: intentRecord,
    intent_commit_sha: intentCommitSha,
    server_observed_at: TIME,
    receipt: {
      http_status: 201,
      status_id: "801",
      target_sha: HEAD,
      context: action.context,
      state: "pending",
      description_digest: action.description_digest,
      created_at: TIME,
      updated_at: TIME,
      creator: { login: "github-actions[bot]", type: "Bot" },
      post_server_time: TIME,
      post_raw_body_sha256: `sha256:${"a".repeat(64)}`,
      refetch_server_time: TIME,
      refetch_page_count: 1,
      refetch_item_count: 1,
      refetch_match_count: 1,
      refetch_inventory_digest: digestCanonical(
        "codex-review-gate-v2-reservation-status-refetch-inventory",
        {
          target_sha: HEAD,
          status_id: "801",
          context: action.context,
          pages: [{
            page: 1,
            raw_body_sha256: pageBodyDigest,
            item_count: 1,
          }],
        },
      ),
      refetch_pages: [{
        page: 1,
        http_status: 200,
        server_time: TIME,
        raw_body_sha256: pageBodyDigest,
        item_count: 1,
      }],
    },
  });
}

function reservationStatusWriteResponseReceipt(transport, boundary) {
  const pageBodyDigest = `sha256:${"b".repeat(64)}`;
  return {
    http_status: 201,
    status_id: "801",
    target_sha: transport.target_sha,
    context: transport.context,
    state: transport.state,
    description_digest: transport.description_digest,
    created_at: boundary,
    updated_at: boundary,
    creator: { login: "github-actions[bot]", type: "Bot" },
    post_server_time: boundary,
    post_raw_body_sha256: `sha256:${"a".repeat(64)}`,
    refetch_server_time: boundary,
    refetch_page_count: 1,
    refetch_item_count: 1,
    refetch_match_count: 1,
    refetch_inventory_digest: digestCanonical(
      "codex-review-gate-v2-reservation-status-refetch-inventory",
      {
        target_sha: transport.target_sha,
        status_id: "801",
        context: transport.context,
        pages: [{
          page: 1,
          raw_body_sha256: pageBodyDigest,
          item_count: 1,
        }],
      },
    ),
    refetch_pages: [{
      page: 1,
      http_status: 200,
      server_time: boundary,
      raw_body_sha256: pageBodyDigest,
      item_count: 1,
    }],
  };
}

function statusWriteResponseReceipt(transport, boundary) {
  const pageBodyDigest = `sha256:${"b".repeat(64)}`;
  return {
    http_status: 201,
    status_id: "801",
    target_sha: transport.target_sha,
    role: transport.role,
    context: transport.context,
    state: transport.state,
    description_digest: transport.description_digest,
    created_at: boundary,
    updated_at: boundary,
    creator: { login: "github-actions[bot]", type: "Bot" },
    post_server_time: boundary,
    post_raw_body_sha256: `sha256:${"a".repeat(64)}`,
    refetch_server_time: boundary,
    refetch_page_count: 1,
    refetch_item_count: 1,
    refetch_match_count: 1,
    refetch_inventory_digest: digestCanonical(
      "codex-review-gate-v2-status-refetch-inventory",
      {
        target_sha: transport.target_sha,
        status_id: "801",
        pages: [{
          page: 1,
          raw_body_sha256: pageBodyDigest,
          item_count: 1,
        }],
      },
    ),
    refetch_pages: [{
      page: 1,
      http_status: 200,
      server_time: boundary,
      raw_body_sha256: pageBodyDigest,
      item_count: 1,
    }],
  };
}

async function appendRequiredStatusForScheduler(ledger, schedulerAppend) {
  const intent = await ledger.appendStatusWriteIntent({
    scheduler_append: schedulerAppend,
    status_write_index: 0,
  });
  return ledger.appendStatusWriteResponse({
    status_intent_handle: intent.status_intent_handle,
    intent_append_receipt: intent.intent_append_receipt,
    receipt: statusWriteResponseReceipt(
      intent.transport,
      intent.intent_append_receipt.ref_reread.server_time,
    ),
  });
}

async function appendAutomaticReservationStatus(ledger, schedulerAppend) {
  await appendRequiredStatusForScheduler(ledger, schedulerAppend);
  const automatic = await ledger.appendAutomaticRequestReservation({
    scheduler_append: schedulerAppend,
  });
  const intent = await ledger.appendReservationStatusWriteIntent({
    automatic_reservation_handle: automatic.automatic_reservation_handle,
    reservation_append_receipt: automatic.reservation_append_receipt,
  });
  const response = await ledger.appendReservationStatusWriteResponse({
    reservation_status_intent_handle:
      intent.reservation_status_intent_handle,
    intent_append_receipt: intent.intent_append_receipt,
    receipt: reservationStatusWriteResponseReceipt(
      intent.transport,
      intent.intent_append_receipt.ref_reread.server_time,
    ),
  });
  return { automatic, intent, response };
}

async function createAutomaticRecoveryTransitionContext({
  bindArtifact = true,
  additionalFindingIds = [],
  includeAddresses = true,
  beforeRecoveryObservation = null,
} = {}) {
  const fixture = githubGitFixture();
  const runIdentity = { ...OWNER };
  const context = await createProtectedInitialRunnerContext(fixture, {
    runIdentity,
  });
  fixture.freezeServerTime();
  const seed = schedulerObservationRecord({
    predecessor: context.lease.acquire_commit_sha,
    lease: context.lease,
    priorAuthorityDigest: context.initialAuthority.prior_authority_digest,
    initialAuthority: context.initialAuthority,
  }).payload.action;
  const firstSchedulerAppend = await context.ledger
    .appendInitialSchedulerObservation({
      initial_runner_state_authority: context.initialAuthority,
      scheduler_evaluation: seed.scheduler_evaluation,
      status_plan: seed.status_plan,
    });
  const { automatic, response } = await appendAutomaticReservationStatus(
    context.ledger,
    firstSchedulerAppend,
  );
  const requestIntent = await context.ledger
    .appendAutomaticReviewRequestIntent({
      automatic_reservation_handle: automatic.automatic_reservation_handle,
      reservation_append_receipt: automatic.reservation_append_receipt,
      reservation_status_response_append: response,
    });
  const requestReceipt = await automaticReviewRequestBindingReceipt({
    action: {
      method: "POST",
      request_body_sha256: rawDigest("@codex review"),
    },
    observedAt: requestIntent.intent_append_receipt.ref_reread.server_time,
  });
  const requestBinding = await context.ledger
    .appendAutomaticReviewRequestBinding({
      automatic_request_intent_handle:
        requestIntent.automatic_request_intent_handle,
      intent_append_receipt: requestIntent.intent_append_receipt,
      receipt: requestReceipt,
    });
  const findingCreatedAt = new Date(
    Date.parse(requestReceipt.created_at) + 1000,
  ).toISOString();
  fixture.advanceServerTime(Math.max(
    10,
    additionalFindingIds.length * 2 + 10,
  ));
  if (beforeRecoveryObservation !== null) {
    beforeRecoveryObservation({ fixture, context });
  }
  const candidateObservation = await appendAutomaticRecoveryObservation({
    ledger: context.ledger,
    preflight: context.preflight,
    evaluatedScopeReceipt: context.evaluatedScopeReceipt,
    workflowCommandHandle: context.workflowCommandHandle,
    requestCreatedAt: requestReceipt.created_at,
    findingCreatedAt,
    additionalFindingIds,
    includeAddresses,
    expectRecoveryHandle: false,
  });
  let recovery = candidateObservation;
  if (bindArtifact) {
    const intent = await context.ledger
      .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
        scheduler_append: candidateObservation.schedulerAppend,
        artifact_binding_candidate_handle:
          candidateObservation.artifactBindingCandidateHandle,
      });
    const providerArtifactHandles =
      await loadAutomaticRecoveryProviderArtifactHandles({
        ledger: context.ledger,
        fixture,
        transport: intent.transport,
        snapshots: candidateObservation.snapshots,
      });
    const response = await context.ledger
      .appendAutomaticRecoveryArtifactBindingResponse({
        artifact_binding_intent_handle:
          intent.artifact_binding_intent_handle,
        intent_append_receipt: intent.intent_append_receipt,
        provider_artifact_handles: providerArtifactHandles,
      });
    assert.equal(response.bindings.length, intent.transport.expected_count);
    fixture.advanceServerTime(60);
    recovery = await appendAutomaticRecoveryObservation({
      ledger: context.ledger,
      preflight: context.preflight,
      evaluatedScopeReceipt: context.evaluatedScopeReceipt,
      workflowCommandHandle: context.workflowCommandHandle,
      requestCreatedAt: requestReceipt.created_at,
      findingCreatedAt,
      additionalFindingIds,
      expectRecoveryHandle: true,
    });
  }
  return {
    fixture,
    context,
    ledger: context.ledger,
    requestCreatedAt: requestReceipt.created_at,
    findingCreatedAt,
    runIdentity,
    ...recovery,
  };
}

async function createAutomaticRecoveryQueuedArtifactBindingContext({
  additionalFindingIds = ["203"],
} = {}) {
  const setup = await createAutomaticRecoveryTransitionContext({
    bindArtifact: false,
  });
  const first = await setup.ledger
    .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
      scheduler_append: setup.schedulerAppend,
      artifact_binding_candidate_handle:
        setup.artifactBindingCandidateHandle,
    });
  setup.fixture.advanceServerTime(60);
  const expanded = await appendAutomaticRecoveryObservation({
    ledger: setup.ledger,
    preflight: setup.context.preflight,
    evaluatedScopeReceipt: setup.context.evaluatedScopeReceipt,
    workflowCommandHandle: setup.context.workflowCommandHandle,
    requestCreatedAt: setup.requestCreatedAt,
    findingCreatedAt: setup.findingCreatedAt,
    additionalFindingIds,
    expectRecoveryHandle: false,
    requireArtifactBinding: false,
  });
  const resumed = await setup.ledger
    .loadOrAppendAutomaticRecoveryArtifactBindingIntent({
      scheduler_append: expanded.schedulerAppend,
      artifact_binding_candidate_handle:
        expanded.artifactBindingCandidateHandle,
    });
  const loaded = await setup.ledger.load();
  const parentEntry = loaded.records.find((entry) =>
    entry.commit_sha === first.intent_append_receipt.commit_sha);
  const queuedEntry = loaded.records.find((entry) =>
    entry.commit_sha === resumed.latest_append_receipt.commit_sha);
  assert.notEqual(parentEntry, undefined);
  assert.notEqual(queuedEntry, undefined);
  assert.equal(queuedEntry.envelope.record_type, "effect-intent");
  assert.equal(queuedEntry.envelope.kind, "artifact-binding");
  assert.equal(
    queuedEntry.envelope.payload.action
      .queued_after_artifact_binding_intent_record_oid,
    parentEntry.commit_sha,
  );
  return { setup, first, expanded, resumed, parentEntry, queuedEntry };
}

async function appendAutomaticRecoveryObservation({
  ledger,
  preflight,
  evaluatedScopeReceipt,
  workflowCommandHandle,
  requestCreatedAt,
  findingCreatedAt,
  findingLine = 1,
  addressId = "303",
  precedingFindingIds = [],
  additionalFindingIds = [],
  includePrimaryFinding = true,
  includeAddresses = true,
  expectRecoveryHandle = true,
  expectArtifactBindingCandidateHandle = !expectRecoveryHandle,
  requireArtifactBinding = expectRecoveryHandle,
}) {
  const controlPlaneAuthority = await ledger
    .loadControlPlaneAuthority(evaluatedScopeReceipt.scope);
  const establishedAuthority = await ledger
    .loadEstablishedRunnerStateAuthority({
      control_plane_authority: controlPlaneAuthority,
      evaluated_scope_receipt: evaluatedScopeReceipt,
      workflow_command_handle: workflowCommandHandle,
    });
  const controlPlaneReceipt =
    createV2ControlPlaneReceiptFromGitLedgerAuthority(controlPlaneAuthority);
  const productionAuthority = createV2ProductionRunnerAuthority({
    preflight_handle: preflight,
    control_plane_receipt: controlPlaneReceipt,
    established_runner_state_authority: establishedAuthority,
    expected_scope: establishedAuthority.scope,
  });
  const snapshots = automaticRecoverySnapshotFixture({
    requestCreatedAt,
    findingCreatedAt,
    controller: productionAuthority.projector_controller,
    finalServerTime:
      establishedAuthority.source_authority.post_ref_receipt.server_time,
    addressId,
    precedingFindingIds,
    additionalFindingIds,
    includePrimaryFinding,
    includeAddresses,
    findingLine,
    requireArtifactBinding,
  });
  const runnerResult = await runV2Operation({
    schema: V2_RUNNER_SCHEMA,
    schema_version: 1,
    operation: "prepare-request",
    status_target_mode: productionAuthority.scheduling.status_target_mode,
    status_context: "codex/github-review-gate",
    snapshot_request: {
      owner: REPOSITORY.owner,
      repo: REPOSITORY.name,
      pull_number: PR.number,
    },
    controller: productionAuthority.projector_controller,
    public_report_authority: productionAuthority.public_report_authority,
    scheduling: productionAuthority.scheduling,
    head_ledger: productionAuthority.head_ledger,
    reservation: null,
    post_response: null,
  }, {
    transport: sequentialRecoveryTransport(
      snapshots.discovery,
      snapshots.evidence,
    ),
    reduceSnapshot: reduceV2Snapshot,
  });
  const recoveryHandle = getV2AutomaticRequestRecoveryHandle(runnerResult);
  assert.equal(recoveryHandle === null, !expectRecoveryHandle);
  const artifactBindingCandidateHandle =
    getV2AutomaticRecoveryArtifactBindingCandidateHandle(runnerResult);
  assert.equal(
    artifactBindingCandidateHandle === null,
    !expectArtifactBindingCandidateHandle,
  );
  const schedulerAppend = await ledger
    .appendEstablishedSchedulerObservation({
      established_runner_state_authority: establishedAuthority,
      scheduler_evaluation: runnerResult.scheduler_evaluation,
      status_plan: runnerResult.status_plan,
    });
  const statusIntents = [];
  const statusResponses = [];
  while (true) {
    const statusIntent = await ledger.loadNextStatusWriteIntent({
      scheduler_append: schedulerAppend,
    });
    if (statusIntent === null) break;
    statusIntents.push(statusIntent);
    statusResponses.push(await ledger.appendStatusWriteResponse({
      status_intent_handle: statusIntent.status_intent_handle,
      intent_append_receipt: statusIntent.intent_append_receipt,
      receipt: statusWriteResponseReceipt(
        statusIntent.transport,
        statusIntent.intent_append_receipt.ref_reread.server_time,
      ),
    }));
  }
  return {
    schedulerAppend,
    statusIntents,
    statusResponses,
    recoveryHandle,
    artifactBindingCandidateHandle,
    runnerResult,
    snapshots,
    controlPlaneAuthority,
    establishedAuthority,
    productionAuthority,
  };
}

async function createFreshAutomaticRecoveryContinuation(
  setup,
  {
    addressId = "303",
    findingLine = 1,
    precedingFindingIds = [],
    additionalFindingIds = [],
    includePrimaryFinding = true,
    expectRecoveryHandle = true,
    expectArtifactBindingCandidateHandle = !expectRecoveryHandle,
    requireArtifactBinding = expectRecoveryHandle,
  } = {},
) {
  await releaseFixtureDiscoveryLease(
    setup.ledger,
    setup.context.discovery,
  );
  setup.fixture.advanceServerTime(7200);
  setup.runIdentity.run_id = "9002";
  setup.runIdentity.run_attempt = 1;
  const ledger = createV2GitHubGitLedger({
    ...factoryInput(setup.fixture, setup.context.capability),
    preflightHandle: setup.context.preflight,
    verifyWorkflowProvenance: setup.context.verifier,
  });
  const nextRun = await acquireEstablishedRunnerContext({
    ledger,
    runIdentity: setup.runIdentity,
    observationBoundary: "public-post-request-wait-complete",
    transportStartSecond: 9000,
    minimalServerTime: "2026-08-13T15:00:00.000Z",
  });
  const recovery = await appendAutomaticRecoveryObservation({
    ledger,
    preflight: setup.context.preflight,
    evaluatedScopeReceipt: nextRun.evaluatedScopeReceipt,
    workflowCommandHandle: nextRun.workflowCommandHandle,
    requestCreatedAt: setup.requestCreatedAt,
    findingCreatedAt: setup.findingCreatedAt,
    addressId,
    findingLine,
    precedingFindingIds,
    additionalFindingIds,
    includePrimaryFinding,
    expectRecoveryHandle,
    expectArtifactBindingCandidateHandle,
    requireArtifactBinding,
  });
  return { ledger, nextRun, recovery };
}

function automaticRecoverySnapshotFixture({
  requestCreatedAt,
  findingCreatedAt,
  controller,
  finalServerTime,
  addressId = "303",
  precedingFindingIds = [],
  additionalFindingIds = [],
  includePrimaryFinding = true,
  includeAddresses = true,
  findingLine = 1,
  requireArtifactBinding = true,
}) {
  const addressCreatedAt = new Date(
    Date.parse(findingCreatedAt) + 1000,
  ).toISOString();
  const request = automaticRecoveryIssueComment("71", {
    body: "@codex review",
    author: ACTOR,
    app: GITHUB_ACTIONS_APP,
    created_at: requestCreatedAt,
    updated_at: requestCreatedAt,
  });
  const finding = automaticRecoveryIssueComment("202", {
    body:
      "### 💡 Codex Review\n\n" +
      `- [P1] Fix the current-head issue https://github.com/owner/repo/blob/${HEAD}/src/a.js#L${findingLine}`,
    author: CODEX_ACTOR,
    app: CODEX_APP,
    created_at: findingCreatedAt,
    updated_at: findingCreatedAt,
  });
  const precedingFindings = precedingFindingIds.map((id, index) => {
    const createdAt = new Date(
      Date.parse(findingCreatedAt) -
        ((precedingFindingIds.length - index) * 250),
    ).toISOString();
    return automaticRecoveryIssueComment(String(id), {
      body:
        "### 💡 Codex Review\n\n" +
        `- [P1] Fix an earlier current-head issue https://github.com/owner/repo/blob/${HEAD}/src/pre.js#L${index + 1}`,
      author: CODEX_ACTOR,
      app: CODEX_APP,
      created_at: createdAt,
      updated_at: createdAt,
    });
  });
  const additionalFindings = additionalFindingIds.map((id, index) => {
    const createdAt = new Date(
      Date.parse(findingCreatedAt) + ((index + 1) * 1000),
    ).toISOString();
    return automaticRecoveryIssueComment(String(id), {
      body:
        "### 💡 Codex Review\n\n" +
        `- [P1] Fix another current-head issue https://github.com/owner/repo/blob/${HEAD}/src/b.js#L${index + 2}`,
      author: CODEX_ACTOR,
      app: CODEX_APP,
      created_at: createdAt,
      updated_at: createdAt,
    });
  });
  const findings = [
    ...precedingFindings,
    ...(includePrimaryFinding ? [finding] : []),
    ...additionalFindings,
  ];
  const addresses = includeAddresses ? findings.map((artifact, index) => {
    const createdAt = new Date(
      Date.parse(addressCreatedAt) + (additionalFindingIds.length * 1000) +
        (index * 1000),
    ).toISOString();
    return automaticRecoveryIssueComment(
      index === 0 ? addressId : String(Number(addressId) + index),
      {
        body: `/codex-gate addressed ${artifact.html_url}`,
        author: HUMAN_ACTOR,
        created_at: createdAt,
        updated_at: createdAt,
      },
    );
  }) : [];
  const address = addresses[0] ?? null;
  const issueComments = [request, ...findings, ...addresses];
  const providerArtifacts = issueComments.map((artifact) =>
    providerIssueComment({
      id: Number(artifact.id),
      node_id: artifact.node_id,
      url: artifact.url,
      html_url: artifact.html_url,
      issue_url: artifact.issue_url,
      body: artifact.body,
      created_at: artifact.created_at,
      updated_at: artifact.updated_at,
      user: {
        id: Number(artifact.author.id),
        node_id: artifact.author.node_id,
        login: artifact.author.login,
        type: artifact.author.type,
      },
      performed_via_github_app: artifact.app === null ? null : {
        id: Number(artifact.app.id),
        node_id: artifact.app.node_id,
        slug: artifact.app.slug,
      },
    }));
  const discoveryServerTime = new Date(
    Date.parse(finalServerTime) - 1000,
  ).toISOString();
  const discovery = automaticRecoverySnapshot({
    issueComments,
    serverTime: discoveryServerTime,
  });
  const evidence = automaticRecoverySnapshot({
    issueComments,
    exactArtifacts: issueComments.map((artifact, index) =>
      automaticRecoveryExactArtifact(
        artifact,
        finalServerTime,
        rawDigest(JSON.stringify(providerArtifacts[index])),
      )),
    actorPermissions: addresses.map((item) =>
      automaticRecoveryActorPermission(item, finalServerTime)),
    serverTime: finalServerTime,
  });
  assert.ok(controller.request_bindings.some((binding) =>
    binding.id === request.id && binding.controlled === true));
  if (includePrimaryFinding) {
    assert.equal(
      controller.artifact_bindings.some((binding) =>
        binding.id === finding.id && binding.request_id === request.id),
      requireArtifactBinding,
    );
  }
  return {
    request,
    finding,
    findings,
    address,
    providerArtifacts,
    discovery,
    evidence,
  };
}

function automaticRecoverySnapshot({
  issueComments,
  exactArtifacts = [],
  actorPermissions = [],
  serverTime,
}) {
  const reactions = {
    issue: [],
    issue_comments: issueComments.map((comment) => ({
      subject_id: comment.id,
      reactions: [],
    })),
    reviews: [],
    inline_comments: [],
  };
  const scope = {
    base_ref_name: "main",
    base_ref_tip: BASE,
    head_ref_name: "feature",
    head_ref_oid: HEAD,
    merge_base_sha: BASE,
    potential_merge_oid: POTENTIAL,
    potential_merge_tree: "e".repeat(40),
    ordered_parent_oids: [BASE, HEAD],
    merge_ref_oid: POTENTIAL,
    mergeable: "MERGEABLE",
  };
  const scopeReceipt = {
    repository_owner: REPOSITORY.owner,
    repository_name: REPOSITORY.name,
    repository_node_id: REPOSITORY.node_id,
    pull_request_number: PR.number,
    pull_request_node_id: PR.node_id,
    pull_request_state: "OPEN",
    pull_request_merged: false,
    pull_request_merged_at: null,
    pull_request_is_draft: false,
    ...scope,
    server_time: serverTime,
  };
  const serviceStart = automaticRecoveryServiceStart(serverTime);
  return {
    schema_version: 2,
    repository: {
      owner: REPOSITORY.owner,
      name: REPOSITORY.name,
      node_id: REPOSITORY.node_id,
    },
    pull_request: {
      number: PR.number,
      node_id: PR.node_id,
      state: "OPEN",
      merged: false,
      merged_at: null,
      is_draft: false,
    },
    server_time: serverTime,
    scope,
    pages: {
      issue_comments: structuredClone(issueComments),
      reviews: [],
      inline_comments: [],
      threads: [],
      reactions,
      exact_artifacts: structuredClone(exactArtifacts),
    },
    permissions: {
      transport_capabilities: {
        stable: true,
        pre: automaticRecoveryTransportCapability(serverTime),
        post: automaticRecoveryTransportCapability(serverTime),
      },
      actor_permissions: structuredClone(actorPermissions),
    },
    service_start_observations: serviceStart,
    scope_receipts: {
      pre: automaticRecoveryScopeReceipt({
        ...scopeReceipt,
        server_time: new Date(
          Date.parse(serverTime) - 1000,
        ).toISOString(),
      }),
      post: automaticRecoveryScopeReceipt(scopeReceipt),
    },
    completeness: {
      all_pages_loaded: true,
      issue_comments: true,
      reviews: true,
      inline_comments: true,
      threads: true,
      reactions: true,
      permissions: true,
      exact_artifacts: true,
      service_start_observations: true,
      request_count: 1,
      item_count: issueComments.length + exactArtifacts.length,
      response_bytes: 1,
      server_date_headers: 1,
    },
    stability: {
      scope_stable: true,
      service_start_observations_stable: true,
      server_time_monotonic: true,
    },
  };
}

function automaticRecoveryScopeReceipt(receipt) {
  const repoPath = `/repos/${REPOSITORY.owner}/${REPOSITORY.name}`;
  const endpoint = (method, path, status = 200) => ({
    method,
    path,
    status,
    server_time: receipt.server_time,
    raw_body_sha256: `sha256:${"f".repeat(64)}`,
  });
  return {
    ...structuredClone(receipt),
    endpoint_receipts: {
      pull_request: endpoint("GET", `${repoPath}/pulls/${PR.number}`),
      graphql: endpoint("POST", "/graphql"),
      compare: endpoint("GET", `${repoPath}/compare/${BASE}...${HEAD}`),
      merge_ref: endpoint(
        "GET",
        `${repoPath}/git/ref/pull/${PR.number}/merge`,
      ),
    },
  };
}

function automaticRecoveryTransportCapability(serverTime) {
  return {
    capability_kind: "authenticated-transport-token",
    admin: false,
    maintain: false,
    push: true,
    triage: true,
    pull: true,
    role_name: "write",
    endpoint: "https://api.github.test/repos/owner/repo",
    http_status: 200,
    response_server_time: serverTime,
    raw_body_sha256: `sha256:${"f".repeat(64)}`,
  };
}

function automaticRecoveryServiceStart(serverTime) {
  const receipt = {
    server_time: serverTime,
    page_count: 1,
    total_check_runs: 0,
    matching_app_ids: [],
    check_runs: [],
    page_receipts: [{
      page: 1,
      item_count: 0,
      total_count: 0,
      response_server_time: serverTime,
      raw_body_sha256: `sha256:${"f".repeat(64)}`,
    }],
  };
  return {
    provider_app_slug: CODEX_APP.slug,
    head_sha: HEAD,
    pre: structuredClone(receipt),
    post: structuredClone(receipt),
    stable: true,
  };
}

function automaticRecoveryIssueComment(id, overrides = {}) {
  return {
    id,
    node_id: `IC_${id}`,
    url: `https://api.github.test/repos/owner/repo/issues/comments/${id}`,
    html_url: `https://github.com/owner/repo/pull/7#issuecomment-${id}`,
    issue_url: "https://api.github.test/repos/owner/repo/issues/7",
    author: HUMAN_ACTOR,
    app: null,
    author_association: "MEMBER",
    body: "ordinary comment",
    created_at: TIME,
    updated_at: TIME,
    ...overrides,
  };
}

function automaticRecoveryExactArtifact(
  artifact,
  serverTime,
  rawBodySha256 = `sha256:${"f".repeat(64)}`,
) {
  return {
    selector: { kind: "issue_comment", id: artifact.id },
    artifact: structuredClone(artifact),
    response_server_time: serverTime,
    raw_body_sha256: rawBodySha256,
  };
}

async function loadAutomaticRecoveryProviderArtifactHandle({
  request,
  snapshots,
  server_time: serverTime,
  on_read: onRead = () => {},
}) {
  const rawArtifact = snapshots.providerArtifacts.find((artifact) =>
    String(artifact.id) === request.selector.id);
  assert.notEqual(rawArtifact, undefined);
  return loadV2ProviderPreScopeArtifact({
    fetch: async () => {
      onRead();
      return response(
        200,
        rawArtifact,
        new Date(serverTime).toUTCString(),
      );
    },
    token: SYNTHETIC_BEARER,
    restBaseUrl: "https://api.github.test",
    ...request,
  });
}

async function loadAutomaticRecoveryProviderArtifactHandles({
  ledger,
  fixture,
  transport,
  snapshots,
  server_time: serverTime = null,
  on_read: onRead = () => {},
}) {
  assert.equal(transport.operation, "loadV2ProviderPreScopeArtifacts");
  assert.equal(transport.requests.length, transport.expected_count);
  const readBoundary = (await ledger.load()).post_ref.server_time;
  if (serverTime === null) fixture.advanceServerTime(1);
  const pointReadTime = serverTime ?? new Date(
    Date.parse(readBoundary) + 1000,
  ).toISOString();
  return Promise.all(transport.requests.map((request) =>
    loadAutomaticRecoveryProviderArtifactHandle({
      request,
      snapshots,
      server_time: pointReadTime,
      on_read: onRead,
    })));
}

function automaticRecoveryActorPermission(comment, serverTime) {
  const receipt = {
    subject: { kind: "issue_comment", id: comment.id },
    actor: structuredClone(comment.author),
    effective_permission: "write",
    role_name: "write",
    permissions: {
      admin: false,
      maintain: false,
      push: true,
      triage: true,
      pull: true,
    },
    mapping_source: "user.permissions",
    permission_assurance: "point-in-time-only",
    request_time_permission: "unproven",
    permission_aba_excluded: false,
    endpoint:
      "https://api.github.test/repos/owner/repo/collaborators/reviewer/permission",
    http_status: 200,
    response_server_time: serverTime,
    raw_body_sha256: `sha256:${"f".repeat(64)}`,
  };
  return {
    subject: structuredClone(receipt.subject),
    actor: structuredClone(comment.author),
    assurance: "point-in-time-only",
    request_time_permission: "unproven",
    permission_aba_excluded: false,
    stable: true,
    pre: structuredClone(receipt),
    post: structuredClone(receipt),
  };
}

function sequentialRecoveryTransport(discovery, evidence) {
  const snapshots = [discovery, evidence];
  return {
    async loadSnapshot() {
      const next = snapshots.shift();
      if (next === undefined) throw new Error("unexpected recovery reread");
      return structuredClone(next);
    },
  };
}

function effectAttemptRecord({
  predecessor,
  lease,
  generation,
  observationRecordOid,
  reservationRecordOid,
  reservation,
  schedulerActionKey,
}) {
  const withoutDigest = {
    schema: "codex-review-gate-request-attempt-v2",
    schema_version: 1,
    attempt_id: reservation.attempt_id,
    reservation_digest: reservation.reservation_digest,
    scheduler_intent_id: reservation.scheduler_intent_id,
    recorded_at: TIME,
    recorded_before_effect: true,
    retry_limit: 0,
  };
  const attempt = {
    ...withoutDigest,
    attempt_digest: runnerDigestCanonical(
      "codex-review-gate-v2-request-attempt",
      withoutDigest,
    ),
  };
  return createV2GitLedgerRecord({
    record_type: "effect-intent",
    predecessor_commit_sha: predecessor,
    pull_request: PR,
    head_ref_oid: HEAD,
    base_ref_oid: BASE,
    potential_merge_commit_oid: POTENTIAL,
    kind: "effect-attempt",
    effect_id: `effect-attempt-${generation.generation_id}`,
    idempotency_key: `effect-attempt-key-${generation.generation_id}`,
    server_observed_at: TIME,
    payload: effectPayload({
      phase: "intent",
      kind: "effect-attempt",
      predecessor,
      generation,
      action: {
        scheduler_observation_record_oid: observationRecordOid,
        reservation_record_oid: reservationRecordOid,
        scheduler_action_key: schedulerActionKey,
        attempt,
      },
    }),
    control_comment_binding: null,
    lease: leaseBinding(lease),
  });
}

async function appendGenericRequiredStatus(
  ledger,
  lease,
  observationRecord,
  observationReceipt,
) {
  const observation = observationRecord.payload.action;
  const planned = observation.status_plan.writes[0];
  const publish = observation.scheduler_plan.actions.find((action) =>
    action.kind === "publish_status");
  assert.equal(observation.status_plan.writes.length, 1);
  assert.ok(publish);
  const description = planned.reason;
  const descriptionDigest = digestCanonical(
    "codex-review-gate-v2-status-description",
    { description },
  );
  const intentRecord = createV2GitLedgerEffectIntentRecord({
    predecessor_commit_sha: observationReceipt.commit_sha,
    scope: {
      pull_request: PR,
      head_ref_oid: HEAD,
      base_ref_oid: BASE,
      potential_merge_commit_oid: POTENTIAL,
    },
    kind: "status-write",
    effect_id: `fixture-status:${observationReceipt.commit_sha}`,
    idempotency_key: planned.idempotency_key,
    server_observed_at: observation.snapshot_server_time,
    action: {
      mode: observation.status_plan.mode,
      target_sha: planned.sha,
      role: planned.role,
      context: planned.context,
      state: planned.state,
      description_digest: descriptionDigest,
      scheduler_observation_record_oid: observationReceipt.commit_sha,
      scheduler_action_key: publish.idempotency_key,
      scheduler_plan_digest: observation.scheduler_plan_digest,
      status_plan_digest: observation.status_plan_digest,
      status_write_index: 0,
      status_write_count: 1,
    },
    control_comment_binding: null,
    lease_receipt: lease,
  });
  const intentReceipt = await appendHistoricalEffectRecord(
    ledger,
    intentRecord,
  );
  const responseReceipt = statusWriteResponseReceipt({
    target_sha: planned.sha,
    role: planned.role,
    context: planned.context,
    state: planned.state,
    description_digest: descriptionDigest,
  }, intentReceipt.ref_reread.server_time);
  const responseRecord = createV2GitLedgerEffectResponseRecord({
    intent_record: intentRecord,
    intent_commit_sha: intentReceipt.commit_sha,
    server_observed_at: responseReceipt.refetch_server_time,
    receipt: responseReceipt,
  });
  return appendHistoricalEffectRecord(ledger, responseRecord);
}

async function appendProtectedRequestPrefix(
  ledger,
  lease,
  { initialAuthority, evaluatedScopeReceipt },
) {
  const generation = automaticGeneration(1);
  const beforeObservation = await ledger.load();
  const observationRecord = schedulerObservationRecord({
    predecessor: lease.acquire_commit_sha,
    lease,
    priorAuthorityDigest: priorRunnerAuthorityDigest(beforeObservation.records),
    initialAuthority,
  });
  const observationReceipt = await appendHistoricalEffectRecord(
    ledger,
    observationRecord,
    evaluatedScopeReceipt,
  );
  const requiredStatus = await appendGenericRequiredStatus(
    ledger,
    lease,
    observationRecord,
    observationReceipt,
  );
  const statusControl = await ledger.loadControlPlaneAuthority(
    initialAuthority.scope,
  );
  const reservationRecord = protectedAutomaticReservationRecord({
    predecessor: requiredStatus.commit_sha,
    lease,
    generation,
    observationRecordOid: observationReceipt.commit_sha,
    observation: observationRecord,
    headLedger: statusControl.scoped_authority.runner_state.head_ledger,
  });
  const reservationReceipt = await appendHistoricalEffectRecord(
    ledger,
    reservationRecord,
  );
  const reservationStatusIntent = reservationStatusIntentRecord({
    predecessor: reservationReceipt.commit_sha,
    lease,
    generation,
    reservationRecordOid: reservationReceipt.commit_sha,
    reservationDigest: reservationRecord.payload.action.reservation_digest,
  });
  const reservationStatusIntentReceipt = await appendHistoricalEffectRecord(
    ledger,
    reservationStatusIntent,
  );
  const reservationStatusResponse = reservationStatusResponseRecord(
    reservationStatusIntent,
    reservationStatusIntentReceipt.commit_sha,
  );
  const reservationStatusResponseReceipt = await appendHistoricalEffectRecord(
    ledger,
    reservationStatusResponse,
  );
  const postAction = observationRecord.payload.action.scheduler_plan.actions
    .find((action) => action.kind === "post_review_request");
  const attemptRecord = effectAttemptRecord({
    predecessor: reservationStatusResponseReceipt.commit_sha,
    lease,
    generation,
    observationRecordOid: observationReceipt.commit_sha,
    reservationRecordOid: reservationReceipt.commit_sha,
    reservation: reservationRecord.payload.action.reservation,
    schedulerActionKey: postAction.idempotency_key,
  });
  const attemptReceipt = await appendHistoricalEffectRecord(
    ledger,
    attemptRecord,
  );
  return {
    generation,
    observationRecord,
    observationReceipt,
    reservationRecord,
    reservationReceipt,
    reservationStatusIntent,
    reservationStatusIntentReceipt,
    reservationStatusResponse,
    reservationStatusResponseReceipt,
    attemptRecord,
    attemptReceipt,
    schedulerActionKey: postAction.idempotency_key,
  };
}

async function appendControlledRequestAfterObservation(
  ledger,
  lease,
  { observationRecord, observationReceipt },
) {
  const generation = automaticGeneration(1);
  const requiredStatus = await appendGenericRequiredStatus(
    ledger,
    lease,
    observationRecord,
    observationReceipt,
  );
  const statusControl = await ledger.loadControlPlaneAuthority({
    pull_request: PR,
    head_ref_oid: HEAD,
    base_ref_oid: BASE,
    potential_merge_commit_oid: POTENTIAL,
  });
  const reservationRecord = protectedAutomaticReservationRecord({
    predecessor: requiredStatus.commit_sha,
    lease,
    generation,
    observationRecordOid: observationReceipt.commit_sha,
    observation: observationRecord,
    headLedger: statusControl.scoped_authority.runner_state.head_ledger,
  });
  const reservationReceipt = await appendHistoricalEffectRecord(
    ledger,
    reservationRecord,
  );
  const reservationStatusIntent = reservationStatusIntentRecord({
    predecessor: reservationReceipt.commit_sha,
    lease,
    generation,
    reservationRecordOid: reservationReceipt.commit_sha,
    reservationDigest: reservationRecord.payload.action.reservation_digest,
  });
  const reservationStatusIntentReceipt = await appendHistoricalEffectRecord(
    ledger,
    reservationStatusIntent,
  );
  const reservationStatusResponse = reservationStatusResponseRecord(
    reservationStatusIntent,
    reservationStatusIntentReceipt.commit_sha,
  );
  const reservationStatusResponseReceipt = await appendHistoricalEffectRecord(
    ledger,
    reservationStatusResponse,
  );
  const postAction = observationRecord.payload.action.scheduler_plan.actions
    .find((action) => action.kind === "post_review_request");
  const attemptRecord = effectAttemptRecord({
    predecessor: reservationStatusResponseReceipt.commit_sha,
    lease,
    generation,
    observationRecordOid: observationReceipt.commit_sha,
    reservationRecordOid: reservationReceipt.commit_sha,
    reservation: reservationRecord.payload.action.reservation,
    schedulerActionKey: postAction.idempotency_key,
  });
  const attemptReceipt = await appendHistoricalEffectRecord(
    ledger,
    attemptRecord,
  );
  const requestIntentRecord = reviewRequestRecord({
    predecessor: attemptReceipt.commit_sha,
    lease,
    effectId: "established-review-request",
    idempotencyKey: "established-review-request-key",
    at: observationRecord.payload.action.snapshot_server_time,
    generation,
    schedulerObservationRecordOid: observationReceipt.commit_sha,
    reservationRecordOid: reservationReceipt.commit_sha,
    attemptRecordOid: attemptReceipt.commit_sha,
    schedulerActionKey: postAction.idempotency_key,
  });
  const requestIntent = await appendHistoricalEffectRecord(
    ledger,
    requestIntentRecord,
  );
  const requestResponseReceipt =
    await automaticReviewRequestBindingReceipt({
      action: requestIntentRecord.payload.action,
      observedAt: requestIntent.ref_reread.server_time,
    });
  const requestResponseRecord = createV2GitLedgerRecord({
    ...recordPlan(requestIntentRecord),
    record_type: "effect-response",
    predecessor_commit_sha: requestIntent.commit_sha,
    server_observed_at:
      requestResponseReceipt.request_scope_receipt.post_scope.observed_at,
    payload: effectPayload({
      phase: "response",
      kind: "review-request",
      predecessor: requestIntent.commit_sha,
      generation,
      intentCommitSha: requestIntent.commit_sha,
      action: requestIntentRecord.payload.action,
      receipt: requestResponseReceipt,
    }),
  });
  const requestResponse = await appendHistoricalEffectRecord(
    ledger,
    requestResponseRecord,
  );
  const bindingIntentRecord = createV2GitLedgerRecord({
    ...recordPlan(requestIntentRecord),
    predecessor_commit_sha: requestResponse.commit_sha,
    kind: "request-binding",
    effect_id: "established-request-binding",
    idempotency_key: "established-request-binding-key",
    payload: effectPayload({
      phase: "intent",
      kind: "request-binding",
      predecessor: requestResponse.commit_sha,
      generation,
      action: {
        generation_id: generation.generation_id,
        request_id: "71",
        reservation_record_oid: reservationReceipt.commit_sha,
        attempt_record_oid: attemptReceipt.commit_sha,
      },
    }),
  });
  const bindingIntent = await appendHistoricalEffectRecord(
    ledger,
    bindingIntentRecord,
  );
  const boundAt = requestResponseReceipt.created_at;
  const bindingResponseRecord = createV2GitLedgerRecord({
    ...recordPlan(bindingIntentRecord),
    record_type: "effect-response",
    predecessor_commit_sha: bindingIntent.commit_sha,
    server_observed_at: boundAt,
    payload: effectPayload({
      phase: "response",
      kind: "request-binding",
      predecessor: bindingIntent.commit_sha,
      generation,
      intentCommitSha: bindingIntent.commit_sha,
      action: bindingIntentRecord.payload.action,
      receipt: {
        request_id: "71",
        request_node_id: "IC_71",
        request_url:
          "https://github.com/owner/repo/pull/7#issuecomment-71",
        body_sha256: requestResponseReceipt.body_sha256,
        created_at: boundAt,
        updated_at: boundAt,
        raw_body_sha256: requestResponseReceipt.refetch_raw_body_sha256,
        actor: ACTOR,
        controlled: true,
      },
    }),
  });
  const bindingResponse = await appendHistoricalEffectRecord(
    ledger,
    bindingResponseRecord,
  );
  return { boundAt, bindingResponse };
}

function reviewRequestRecord({
  predecessor,
  lease,
  effectId,
  idempotencyKey,
  at,
  generation = automaticGeneration(1),
  schedulerObservationRecordOid,
  reservationRecordOid,
  attemptRecordOid,
  schedulerActionKey,
}) {
  return createV2GitLedgerRecord({
    record_type: "effect-intent",
    predecessor_commit_sha: predecessor,
    pull_request: PR,
    head_ref_oid: HEAD,
    base_ref_oid: BASE,
    potential_merge_commit_oid: POTENTIAL,
    kind: "review-request",
    effect_id: effectId,
    idempotency_key: idempotencyKey,
    server_observed_at: at,
    payload: effectPayload({
      phase: "intent",
      kind: "review-request",
      predecessor,
      generation,
      action: {
        method: "POST",
        request_body_sha256: rawDigest("@codex review"),
        scheduler_observation_record_oid: schedulerObservationRecordOid,
        reservation_record_oid: reservationRecordOid,
        attempt_record_oid: attemptRecordOid,
        scheduler_action_key: schedulerActionKey,
      },
    }),
    control_comment_binding: null,
    lease: leaseBinding(lease),
  });
}

function artifactBindingIntent({
  predecessor,
  lease,
  generation,
  requestBindingRecordOid,
  requestId = "71",
  requestNodeId = "IC_71",
  artifactSelector = { kind: "pull_request_review", id: "9001" },
  observedAt = TIME,
}) {
  const identity = `${artifactSelector.kind}-${artifactSelector.id}`;
  return createV2GitLedgerRecord({
    record_type: "effect-intent",
    predecessor_commit_sha: predecessor,
    pull_request: PR,
    head_ref_oid: HEAD,
    base_ref_oid: BASE,
    potential_merge_commit_oid: POTENTIAL,
    kind: "artifact-binding",
    effect_id:
      `artifact-binding-${identity}-${requestBindingRecordOid.slice(0, 12)}`,
    idempotency_key:
      `artifact-binding-key-${identity}-${requestBindingRecordOid.slice(0, 12)}`,
    server_observed_at: observedAt,
    payload: effectPayload({
      phase: "intent",
      kind: "artifact-binding",
      predecessor,
      generation,
      action: {
        generation_id: generation.generation_id,
        request_binding_record_oid: requestBindingRecordOid,
        request_id: requestId,
        request_node_id: requestNodeId,
        artifact_selector: structuredClone(artifactSelector),
        expected_actor: CODEX_ACTOR,
        expected_app: CODEX_APP,
        scheduler_observation_record_oid: "8".repeat(40),
        full_scope_receipt_digest: digest("8"),
        candidate_authority: automaticRecoveryArtifactCandidateItemAuthority({
          generation,
          requestBindingRecordOid,
          requestId,
          requestNodeId,
          artifactSelector,
          observedAt,
        }),
      },
    }),
    control_comment_binding: null,
    lease: leaseBinding(lease),
  });
}

function artifactBindingResponse({
  intentRecord,
  intentCommitSha,
  artifactCreatedAt,
  artifactNodeId = "PRR_9001",
  artifactUrl =
    "https://github.com/owner/repo/pull/7#pullrequestreview-9001",
}) {
  const providerServerTime = new Date(
    Date.parse(artifactCreatedAt) + 1000,
  ).toISOString();
  const recordBoundary = new Date(
    Date.parse(artifactCreatedAt) + 2000,
  ).toISOString();
  return createV2GitLedgerRecord({
    ...recordPlan(intentRecord),
    record_type: "effect-response",
    predecessor_commit_sha: intentCommitSha,
    server_observed_at: recordBoundary,
    payload: effectPayload({
      phase: "response",
      kind: "artifact-binding",
      predecessor: intentCommitSha,
      generation: intentRecord.payload.generation,
      intentCommitSha,
      action: intentRecord.payload.action,
      receipt: {
        http_status: 200,
        generation_id: intentRecord.payload.generation.generation_id,
        request_binding_record_oid:
          intentRecord.payload.action.request_binding_record_oid,
        request_id: intentRecord.payload.action.request_id,
        request_node_id: intentRecord.payload.action.request_node_id,
        artifact_selector: structuredClone(
          intentRecord.payload.action.artifact_selector,
        ),
        artifact_node_id: artifactNodeId,
        artifact_url: artifactUrl,
        artifact_type: intentRecord.payload.action.artifact_selector.kind,
        artifact_created_at: artifactCreatedAt,
        server_time: providerServerTime,
        raw_body_sha256: `sha256:${"7".repeat(64)}`,
        actor: CODEX_ACTOR,
        app: CODEX_APP,
      },
    }),
  });
}

function automaticGeneration(index) {
  return {
    generation_id: `automatic:${index}`,
    kind: "automatic",
    index,
    review_epoch_digest: digestCanonical(
      "codex-review-gate-v2-review-epoch",
      {
        pull_request: PR,
        base_ref_oid: BASE,
        head_ref_oid: HEAD,
        potential_merge_commit_oid: POTENTIAL,
      },
    ),
  };
}

function effectPayload({
  phase,
  kind,
  predecessor,
  generation = null,
  action,
  intentCommitSha = null,
  receipt = null,
  pullRequest = PR,
  headOid = HEAD,
  baseOid = BASE,
  potentialMergeCommitOid = POTENTIAL,
}) {
  return {
    schema: "codex-review-gate-git-ledger-effect-payload-v2",
    schema_version: 1,
    phase,
    kind,
    scope: {
      pull_request: pullRequest,
      head_ref_oid: headOid,
      base_ref_oid: baseOid,
      potential_merge_commit_oid: potentialMergeCommitOid,
    },
    generation,
    ordinal: generation?.index ?? 1,
    predecessor_commit_sha: predecessor,
    action,
    intent_commit_sha: intentCommitSha,
    receipt,
  };
}

function evaluatedScopeReceipt({
  eventName = "pull_request",
  ref = "refs/pull/7/merge",
  sha = POTENTIAL,
  relation = "pull-request-event",
  selector = null,
  inventoryReceipt = null,
} = {}) {
  return createV2GitLedgerEvaluatedScopeReceipt({
    relation,
    repository: REPOSITORY,
    scope: {
      pull_request: PR,
      head_ref_oid: HEAD,
      base_ref_oid: BASE,
      potential_merge_commit_oid: POTENTIAL,
    },
    trigger_identity: {
      event_name: eventName,
      ref,
      sha,
    },
    selector,
    inventory_receipt: inventoryReceipt,
    scope_endpoint_receipt: {
      method: "GET",
      path: "/repos/owner/repo/pulls/7",
      status: 200,
      server_time: TIME,
      raw_body_sha256: `sha256:${"a".repeat(64)}`,
      pull_request: PR,
      head_ref_oid: HEAD,
      base_ref_oid: BASE,
      potential_merge_commit_oid: POTENTIAL,
    },
  });
}

async function loadFixtureMinimalScope() {
  return loadV2MinimalLiveScope({
    fetch: minimalScopeFetch,
    token: SYNTHETIC_BEARER,
    repository: { owner: REPOSITORY.owner, name: REPOSITORY.name },
    pull_number: PR.number,
    rest_base_url: "https://api.github.test",
    graphql_url: "https://api.github.test/graphql",
  });
}

async function loadFixtureMinimalScopeForCandidate(
  number,
  headOid = number.toString(16).padStart(40, "0"),
  headRefName = `feature-${number}`,
  nodeId = `PR_${number}`,
  serverTime = TIME,
) {
  return loadV2MinimalLiveScope({
    fetch: (url, init) => minimalScopeFetchForCandidate({
      number,
      nodeId,
      headOid,
      headRefName,
      serverTime,
      urlValue: url,
      init,
    }),
    token: SYNTHETIC_BEARER,
    repository: { owner: REPOSITORY.owner, name: REPOSITORY.name },
    pull_number: number,
    rest_base_url: "https://api.github.test",
    graphql_url: "https://api.github.test/graphql",
  });
}

async function automaticReviewRequestBindingReceipt({
  action,
  observedAt = TIME,
  requestId = "71",
  actor = ACTOR,
  app = APP,
} = {}) {
  const minimal = await loadFixtureMinimalScope();
  const preScope = minimalScopeReceiptAt(minimal, observedAt);
  const postScope = minimalScopeReceiptAt(minimal, observedAt);
  const scopeWithoutDigest = {
    schema: "codex-review-gate-parent-recorded-request-scope-v1",
    schema_version: 1,
    pre_scope: preScope,
    post_scope: postScope,
    stable: true,
  };
  const requestScopeReceipt = {
    ...scopeWithoutDigest,
    receipt_digest: digestCanonical(
      "codex-review-gate-v2-automatic-review-request-scope-receipt",
      scopeWithoutDigest,
    ),
  };
  const carrierSelector = { kind: "issue_comment", id: requestId };
  const identity = {
    carrier_selector: carrierSelector,
    request_id: requestId,
    request_node_id: `IC_${requestId}`,
    api_url:
      `https://api.github.test/repos/owner/repo/issues/comments/${requestId}`,
    request_url:
      `https://github.com/owner/repo/pull/7#issuecomment-${requestId}`,
    issue_url: "https://api.github.test/repos/owner/repo/issues/7",
    body_sha256: action.request_body_sha256,
    created_at: observedAt,
    updated_at: observedAt,
    actor: structuredClone(actor),
    app: structuredClone(app),
  };
  const requestDigest = digestCanonical(
    "codex-review-gate-v2-automatic-review-request-identity",
    identity,
  );
  const withoutDigest = {
    schema:
      "codex-review-gate-git-ledger-automatic-review-request-binding-receipt-v2",
    schema_version: 1,
    http_status: 201,
    ...identity,
    post_server_time: observedAt,
    post_raw_body_sha256: `sha256:${"2".repeat(64)}`,
    request_digest: requestDigest,
    refetch_http_status: 200,
    refetch_server_time: observedAt,
    refetch_raw_body_sha256: `sha256:${"4".repeat(64)}`,
    refetched_request_digest: requestDigest,
    request_scope_receipt: requestScopeReceipt,
  };
  return {
    ...withoutDigest,
    receipt_digest: digestCanonical(
      "codex-review-gate-v2-automatic-review-request-binding-receipt",
      withoutDigest,
    ),
  };
}

function minimalScopeReceiptAt(receipt, observedAt) {
  const projected = structuredClone(receipt);
  projected.endpoint_receipts = projected.endpoint_receipts.map((endpoint) => ({
    ...endpoint,
    server_time: observedAt,
  }));
  projected.observed_at = observedAt;
  delete projected.receipt_digest;
  projected.receipt_digest = runnerDigestCanonical(
    "codex-review-gate-v2-minimal-live-scope",
    projected,
  );
  return projected;
}

async function minimalScopeFetch(urlValue, init = {}) {
  return minimalScopeFetchAt(TIME, urlValue, init);
}

async function minimalScopeFetchAt(serverTime, urlValue, init = {}) {
  const url = new URL(urlValue);
  const date = new Date(serverTime).toUTCString();
  if (url.pathname === "/repos/owner/repo/pulls/7") {
    return response(200, {
      number: PR.number,
      url: "https://api.github.test/repos/owner/repo/pulls/7",
      node_id: PR.node_id,
      state: "open",
      merged: false,
      merged_at: null,
      updated_at: TIME,
    }, date);
  }
  if (url.pathname === "/graphql") {
    assert.equal(init.method, "POST");
    return response(200, {
      data: {
        repository: {
          id: REPOSITORY.node_id,
          name: REPOSITORY.name,
          owner: { login: REPOSITORY.owner },
          pullRequest: {
            id: PR.node_id,
            number: PR.number,
            state: "OPEN",
            merged: false,
            mergedAt: null,
            isDraft: false,
            updatedAt: TIME,
            mergeable: "MERGEABLE",
            baseRefName: "main",
            baseRef: { name: "main", target: { oid: BASE } },
            headRefName: "feature",
            headRefOid: HEAD,
            headRef: { name: "feature", target: { oid: HEAD } },
            potentialMergeCommit: {
              oid: POTENTIAL,
              tree: { oid: "e".repeat(40) },
              parents: {
                totalCount: 2,
                pageInfo: { hasNextPage: false, endCursor: "parent-2" },
                nodes: [{ oid: BASE }, { oid: HEAD }],
              },
            },
          },
        },
      },
    }, date);
  }
  if (url.pathname === `/repos/owner/repo/compare/${BASE}...${HEAD}`) {
    return response(200, {
      base_commit: { sha: BASE },
      merge_base_commit: { sha: BASE },
    }, date);
  }
  if (url.pathname === "/repos/owner/repo/git/ref/pull/7/merge") {
    return response(200, {
      ref: "refs/pull/7/merge",
      url: "https://api.github.test/repos/owner/repo/git/refs/pull/7/merge",
      object: {
        type: "commit",
        sha: POTENTIAL,
        url: `https://api.github.test/repos/owner/repo/git/commits/${POTENTIAL}`,
      },
    }, date);
  }
  throw new Error(`unexpected minimal scope request ${init.method} ${url.href}`);
}

async function minimalScopeFetchForCandidate({
  number,
  nodeId,
  headOid,
  headRefName,
  serverTime,
  urlValue,
  init = {},
}) {
  const url = new URL(urlValue);
  const date = new Date(serverTime).toUTCString();
  const pullPath = `/repos/owner/repo/pulls/${number}`;
  if (url.pathname === pullPath) {
    return response(200, {
      number,
      url: `https://api.github.test${pullPath}`,
      node_id: nodeId,
      state: "open",
      merged: false,
      merged_at: null,
      updated_at: TIME,
    }, date);
  }
  if (url.pathname === "/graphql") {
    assert.equal(init.method, "POST");
    return response(200, {
      data: {
        repository: {
          id: REPOSITORY.node_id,
          name: REPOSITORY.name,
          owner: { login: REPOSITORY.owner },
          pullRequest: {
            id: nodeId,
            number,
            state: "OPEN",
            merged: false,
            mergedAt: null,
            isDraft: false,
            updatedAt: TIME,
            mergeable: "MERGEABLE",
            baseRefName: "main",
            baseRef: { name: "main", target: { oid: BASE } },
            headRefName,
            headRefOid: headOid,
            headRef: { name: headRefName, target: { oid: headOid } },
            potentialMergeCommit: {
              oid: POTENTIAL,
              tree: { oid: "e".repeat(40) },
              parents: {
                totalCount: 2,
                pageInfo: { hasNextPage: false, endCursor: "parent-2" },
                nodes: [{ oid: BASE }, { oid: headOid }],
              },
            },
          },
        },
      },
    }, date);
  }
  if (url.pathname === `/repos/owner/repo/compare/${BASE}...${headOid}`) {
    return response(200, {
      base_commit: { sha: BASE },
      merge_base_commit: { sha: BASE },
    }, date);
  }
  if (url.pathname === `/repos/owner/repo/git/ref/pull/${number}/merge`) {
    return response(200, {
      ref: `refs/pull/${number}/merge`,
      url: `https://api.github.test/repos/owner/repo/git/refs/pull/${number}/merge`,
      object: {
        type: "commit",
        sha: POTENTIAL,
        url: `https://api.github.test/repos/owner/repo/git/commits/${POTENTIAL}`,
      },
    }, date);
  }
  throw new Error(`unexpected candidate scope request ${init.method} ${url.href}`);
}

async function loadFixtureProviderPreflight({
  actor = CODEX_ACTOR,
  app = CODEX_APP,
  visibility = "private",
} = {}) {
  return createV2GitHubWorkflowPreflight({
    fetch: providerPreflightFetch({ actor, app, visibility }),
    token: SYNTHETIC_BEARER,
    repository: `${REPOSITORY.owner}/${REPOSITORY.name}`,
    restBaseUrl: "https://api.github.test",
  }).load({ command: providerPreflightCommand() });
}

function providerPreflightCommand() {
  return {
    schema: "codex-review-gate-workflow-command-v2",
    schema_version: 1,
    command: "run",
    repository: { owner: REPOSITORY.owner, name: REPOSITORY.name },
    pull_request: { number: PR.number },
    dispatch_binding: null,
    selection_policy: "joey-default",
    route: {
      operation: "ordinary",
      trigger: "provider-event",
      observation_boundary: "initial",
    },
    invocation: {
      event_name: "issue_comment",
      event_payload_sha256: `sha256:${"0".repeat(64)}`,
      run_id: OWNER.run_id,
      run_attempt: OWNER.run_attempt,
      actor_id: OWNER.actor_id,
    },
    workflow_receipt: {
      present: true,
      compatible: true,
      source: "trusted-reusable-workflow",
      repository: "Joey-Tools/codex-review-gate-action",
      path: ".github/workflows/codex-review-gate.yml",
      revision: "2".repeat(40),
      checkout_sha: "2".repeat(40),
      caller_repository: "owner/repo",
      caller_workflow_ref:
        "owner/repo/.github/workflows/controller.yml@refs/heads/main",
      caller_workflow_sha: "2".repeat(40),
      status_context: "codex/github-review-gate",
      status_target_mode: "test-merge-with-head-sentinel",
    },
    receipt_policy: {
      server_enforcement: V2_SERVER_ENFORCEMENT_POLICY,
      public_wait: V2_PUBLIC_WAIT_POLICY,
    },
  };
}

function providerPreflightFetch({ actor, app, visibility = "private" }) {
  const ledgerRuleset = {
    id: 101,
    name: "Codex ledger protection",
    target: "branch",
    source_type: "Repository",
    source: "owner/repo",
    enforcement: "active",
    bypass_actors: [],
    conditions: {
      ref_name: { include: [V2_GIT_LEDGER_REF], exclude: [] },
    },
    rules: [{ type: "deletion" }, { type: "non_fast_forward" }],
  };
  const effectiveRule = (type, id, parameters = null) => ({
    type,
    enforcement: "active",
    ruleset_id: id,
    ruleset_name: `Rules ${id}`,
    ruleset_source_type: "Repository",
    ruleset_source: "owner/repo",
    ...(parameters === null ? {} : { parameters }),
  });
  return async (urlValue, init = {}) => {
    const url = new URL(urlValue);
    const path = url.pathname;
    const date = new Date(TIME).toUTCString();
    let body;
    let status = 200;
    if (path === "/repos/owner/repo") {
      body = {
        id: Number(REPOSITORY.id),
        full_name: "owner/repo",
        node_id: REPOSITORY.node_id,
        visibility,
        private: visibility !== "public",
        default_branch: "main",
        permissions: {
          admin: false,
          maintain: false,
          push: true,
          triage: true,
          pull: true,
        },
        owner: { id: Number(REPOSITORY.owner_id), login: "owner" },
      };
    } else if (path === "/repos/owner/repo/environments") {
      const names = [
        "codex-review-gate-public-initial-15m",
        "codex-review-gate-public-post-request-15m",
        "codex-review-gate-public-no-start-15m",
      ];
      body = {
        total_count: names.length,
        environments: names.map((name, index) => ({
          id: 301 + index,
          name,
        })),
      };
    } else if (path.startsWith("/repos/owner/repo/environments/")) {
      const name = decodeURIComponent(path.split("/").at(-1));
      const names = [
        "codex-review-gate-public-initial-15m",
        "codex-review-gate-public-post-request-15m",
        "codex-review-gate-public-no-start-15m",
      ];
      const index = names.indexOf(name);
      if (index === -1) {
        throw new Error(`unexpected public wait environment ${name}`);
      }
      body = {
        id: 301 + index,
        name,
        protection_rules: [{
          id: 401 + index,
          type: "wait_timer",
          wait_timer: 15,
        }],
      };
    } else if (path === "/repos/Joey-Tools/codex-review-gate-action") {
      body = {
        id: 4242,
        node_id: "R_release",
        full_name: "Joey-Tools/codex-review-gate-action",
      };
    } else if (path ===
        "/repos/Joey-Tools/codex-review-gate-action/contents/.github/workflows/codex-review-gate.yml") {
      const yaml = [
        "name: Codex Review Gate v2",
        "permissions:",
        "  contents: write",
        "  id-token: write",
        "  issues: write",
        "  pull-requests: write",
        "  statuses: write",
        "jobs:",
        "  gate:",
        "    runs-on: ubuntu-slim",
        "",
      ].join("\n");
      body = {
        type: "file",
        path: ".github/workflows/codex-review-gate.yml",
        encoding: "base64",
        content: Buffer.from(yaml).toString("base64"),
        sha: "e".repeat(40),
      };
    } else if (path === "/apps/github-actions") {
      body = { id: 15368, node_id: "A_actions", slug: "github-actions" };
    } else if (path === "/apps/chatgpt-codex-connector") {
      body = { id: Number(app.id), node_id: app.node_id, slug: app.slug };
    } else if (path === "/users/chatgpt-codex-connector%5Bbot%5D") {
      body = {
        id: Number(actor.id),
        node_id: actor.node_id,
        login: actor.login,
        type: actor.type,
      };
    } else if (path === "/repos/owner/repo/actions/oidc/customization/sub") {
      body = {
        use_default: false,
        include_claim_keys: ["repo", "job_workflow_ref"],
      };
    } else if (path === "/repos/owner/repo/actions/workflows/controller.yml") {
      body = {
        id: 1,
        node_id: "W_controller",
        path: ".github/workflows/controller.yml",
        state: "active",
      };
    } else if (path ===
        "/repos/owner/repo/contents/.github/workflows/controller.yml") {
      const yaml = [
        "name: controller",
        "jobs:",
        "  gate:",
        "    uses: Joey-Tools/codex-review-gate-action/.github/workflows/" +
          `codex-review-gate.yml@${"2".repeat(40)}`,
        "",
      ].join("\n");
      body = {
        type: "file",
        path: ".github/workflows/controller.yml",
        encoding: "base64",
        content: Buffer.from(yaml).toString("base64"),
        sha: "d".repeat(40),
      };
    } else if (path === "/repos/owner/repo/rules/branches/main") {
      body = url.searchParams.get("page") === "1" ? [
        effectiveRule("required_status_checks", 10, {
          required_status_checks: [{
            context: "codex/github-review-gate",
            integration_id: 15368,
          }],
        }),
        effectiveRule("workflows", 11, {
          workflows: [{
            path: ".github/workflows/codex-review-gate.yml",
            ref: "2".repeat(40),
            repository_id: 4242,
            sha: "2".repeat(40),
          }],
        }),
      ] : [];
    } else if (path ===
        "/repos/owner/repo/branches/codex-review-gate-ledger-v2") {
      body = {
        name: "codex-review-gate-ledger-v2",
        commit: { sha: "f".repeat(40) },
      };
    } else if (path ===
        "/repos/owner/repo/rules/branches/codex-review-gate-ledger-v2") {
      body = url.searchParams.get("page") === "1" ? [
        effectiveRule("deletion", 101),
        effectiveRule("non_fast_forward", 101),
      ] : [];
    } else if (path === "/repos/owner/repo/rulesets") {
      body = url.searchParams.get("page") === "1" ? [ledgerRuleset] : [];
    } else if (path === "/repos/owner/repo/rulesets/101") {
      body = ledgerRuleset;
    } else {
      status = 404;
      body = { message: `unexpected preflight route ${path}` };
    }
    assert.equal(init.method, "GET");
    return response(status, body, date);
  };
}

function providerTransportFixture({
  startSecond = 600,
  nextServerTime = null,
  comment = providerIssueComment(),
  headOid = HEAD,
  headRefName = "feature",
  pullRequestNumber = PR.number,
  pullRequestNodeId = PR.node_id,
} = {}) {
  let second = startSecond;
  let currentComment = structuredClone(comment);
  const calls = [];
  const fixture = {
    calls,
    setComment(value) { currentComment = value === null ? null : structuredClone(value); },
    async fetch(urlValue, init = {}) {
      const url = new URL(urlValue);
      const method = init.method ?? "GET";
      calls.push({ method, path: url.pathname });
      second += 1;
      const date = nextServerTime === null
        ? new Date(Date.UTC(2026, 7, 13, 12, 0, second)).toUTCString()
        : new Date(nextServerTime()).toUTCString();
      const repoPath = "/repos/owner/repo";
      if (url.pathname === "/graphql") {
        const query = String(JSON.parse(init.body).query);
        if (query.includes("CodexReviewGateV2Scope")) {
          return response(200, {
            data: providerScopePayload({
              headOid,
              headRefName,
              pullRequestNumber,
              pullRequestNodeId,
            }),
          }, date);
        }
        if (query.includes("CodexReviewGateV2ReviewThreads")) {
          return response(200, {
            data: {
              repository: {
                pullRequest: {
                  reviewThreads: emptyGraphqlConnection(),
                },
              },
            },
          }, date);
        }
        return response(200, { errors: [{ message: "unexpected query" }] }, date);
      }
      if (url.pathname === `${repoPath}/pulls/${pullRequestNumber}`) {
        return response(200, {
          number: pullRequestNumber,
          node_id: pullRequestNodeId,
          url: `https://api.github.test${repoPath}/pulls/${pullRequestNumber}`,
          state: "open",
          merged: false,
          merged_at: null,
          mergeable: true,
          merge_commit_sha: POTENTIAL,
          base: { ref: "main", sha: BASE },
          head: { ref: headRefName, sha: headOid },
        }, date);
      }
      if (url.pathname === `${repoPath}/compare/${BASE}...${headOid}`) {
        return response(200, {
          base_commit: { sha: BASE },
          merge_base_commit: { sha: BASE },
        }, date);
      }
      if (url.pathname ===
          `${repoPath}/git/ref/pull/${pullRequestNumber}/merge`) {
        return response(200, {
          ref: `refs/pull/${pullRequestNumber}/merge`,
          url: `https://api.github.test${repoPath}/git/refs/pull/${pullRequestNumber}/merge`,
          object: {
            type: "commit",
            sha: POTENTIAL,
            url: `https://api.github.test/repos/owner/repo/git/commits/${POTENTIAL}`,
          },
        }, date);
      }
      if (url.pathname ===
          `${repoPath}/issues/${pullRequestNumber}/comments`) {
        return response(200, currentComment === null ? [] : [currentComment], date);
      }
      if (url.pathname === `${repoPath}/pulls/${pullRequestNumber}/reviews` ||
          url.pathname === `${repoPath}/pulls/${pullRequestNumber}/comments` ||
          url.pathname === `${repoPath}/issues/${pullRequestNumber}/reactions` ||
          url.pathname === `${repoPath}/issues/comments/71/reactions`) {
        return response(200, [], date);
      }
      if (url.pathname === `${repoPath}/commits/${headOid}/check-runs`) {
        return response(200, { total_count: 0, check_runs: [] }, date);
      }
      if (url.pathname === `${repoPath}/issues/comments/71`) {
        return currentComment === null
          ? response(404, { message: "Not Found" }, date)
          : response(200, currentComment, date);
      }
      if (url.pathname === repoPath) {
        return response(200, {
          full_name: "owner/repo",
          url: "https://api.github.test/repos/owner/repo",
          role_name: "admin",
          permissions: {
            admin: true,
            maintain: true,
            push: true,
            triage: true,
            pull: true,
          },
        }, date);
      }
      return response(404, { message: `unexpected transport route ${url.pathname}` }, date);
    },
  };
  return fixture;
}

function providerIssueComment(overrides = {}) {
  return {
    id: 71,
    node_id: "IC_71",
    url: "https://api.github.test/repos/owner/repo/issues/comments/71",
    html_url: "https://github.com/owner/repo/pull/7#issuecomment-71",
    issue_url: "https://api.github.test/repos/owner/repo/issues/7",
    user: {
      id: Number(CODEX_ACTOR.id),
      node_id: CODEX_ACTOR.node_id,
      login: CODEX_ACTOR.login,
      type: CODEX_ACTOR.type,
    },
    performed_via_github_app: {
      id: Number(CODEX_APP.id),
      node_id: CODEX_APP.node_id,
      slug: CODEX_APP.slug,
    },
    author_association: "MEMBER",
    body: "provider result",
    created_at: "2026-08-13T11:00:00Z",
    updated_at: "2026-08-13T11:00:00Z",
    ...overrides,
  };
}

function providerScopePayload({
  headOid = HEAD,
  headRefName = "feature",
  pullRequestNumber = PR.number,
  pullRequestNodeId = PR.node_id,
} = {}) {
  return {
    repository: {
      id: REPOSITORY.node_id,
      name: REPOSITORY.name,
      owner: { login: REPOSITORY.owner },
      pullRequest: {
        id: pullRequestNodeId,
        number: pullRequestNumber,
        state: "OPEN",
        merged: false,
        mergedAt: null,
        isDraft: false,
        mergeable: "MERGEABLE",
        baseRefName: "main",
        baseRefOid: BASE,
        baseRef: { name: "main", target: { oid: BASE } },
        headRefName,
        headRefOid: headOid,
        headRef: { name: headRefName, target: { oid: headOid } },
        potentialMergeCommit: {
          oid: POTENTIAL,
          tree: { oid: "e".repeat(40) },
          parents: {
            totalCount: 2,
            pageInfo: { hasNextPage: false, endCursor: "parent-2" },
            nodes: [{ oid: BASE }, { oid: headOid }],
          },
        },
      },
    },
  };
}

function emptyGraphqlConnection() {
  return {
    totalCount: 0,
    pageInfo: { hasNextPage: false, endCursor: null },
    nodes: [],
  };
}

function createProviderTransport(fetch) {
  return createV2GitHubTransport({
    fetch,
    token: SYNTHETIC_BEARER,
    restBaseUrl: "https://api.github.test",
    graphqlUrl: "https://api.github.test/graphql",
  });
}

async function loadProviderPreScopeArtifact(fetch) {
  return loadV2ProviderPreScopeArtifact({
    fetch,
    token: SYNTHETIC_BEARER,
    restBaseUrl: "https://api.github.test",
    owner: REPOSITORY.owner,
    repo: REPOSITORY.name,
    pullNumber: PR.number,
    headSha: HEAD,
    selector: { kind: "issue_comment", id: "71" },
    expectedActor: CODEX_ACTOR,
    expectedApp: CODEX_APP,
  });
}

async function createManualWorkflowCommandHandle() {
  return createWorkflowCommandHandle({
    eventName: "workflow_dispatch",
    controllerRoute: "evaluate-only",
    event: { inputs: { "pull-request": String(PR.number) } },
  });
}

async function createPullRequestTargetWorkflowCommandHandle({
  runIdentity = OWNER,
  observationBoundary = "initial",
} = {}) {
  return createWorkflowCommandHandle({
    eventName: "pull_request_target",
    controllerRoute: "ordinary",
    event: { pull_request: { number: PR.number } },
    runIdentity,
    observationBoundary,
  });
}

async function createProviderWorkflowCommandHandle() {
  return createWorkflowCommandHandle({
    eventName: "issue_comment",
    controllerRoute: "provider-event-hint",
    event: {
      issue: {
        number: PR.number,
        pull_request: {
          url: "https://api.github.test/repos/owner/repo/pulls/7",
        },
      },
    },
  });
}

async function createScheduleWorkflowCommandHandle({
  pullRequestNumber = null,
  dispatchBinding = null,
  runIdentity = OWNER,
  event = {},
  selectionPolicy = "joey-default",
  workflowSha = "2".repeat(40),
} = {}) {
  return createWorkflowCommandHandle({
    eventName: "schedule",
    controllerRoute: pullRequestNumber === null ? "scan-all-open" : "ordinary",
    event,
    runIdentity,
    pullRequestNumber,
    dispatchBinding,
    selectionPolicy,
    workflowSha,
  });
}

async function createWorkflowCommandHandle({
  eventName,
  controllerRoute,
  event,
  runIdentity = OWNER,
  observationBoundary = "initial",
  pullRequestNumber = PR.number,
  dispatchBinding = null,
  selectionPolicy = "joey-default",
  workflowSha = "2".repeat(40),
}) {
  const directory = await mkdtemp(join(tmpdir(), "v2-git-ledger-command-"));
  const eventPath = join(directory, "event.json");
  const commandPath = join(directory, "command.json");
  await writeFile(eventPath, JSON.stringify(event), { mode: 0o600 });
  const environment = {
    RUNNER_TEMP: directory,
    V2_CONTROLLER_INPUT_PATH: commandPath,
    V2_CONTROLLER_EVENT_PATH: eventPath,
    GITHUB_WORKSPACE: process.cwd(),
    GITHUB_REPOSITORY: "owner/repo",
    V2_CONTROLLER_PULL_REQUEST:
      pullRequestNumber === null ? "" : String(pullRequestNumber),
    V2_CONTROLLER_DISPATCH_BINDING:
      dispatchBinding === null ? "" : canonicalJson(dispatchBinding),
    V2_CONTROLLER_ROUTE: controllerRoute,
    V2_CONTROLLER_OBSERVATION_BOUNDARY: observationBoundary,
    GITHUB_EVENT_NAME: eventName,
    GITHUB_RUN_ID: runIdentity.run_id,
    GITHUB_RUN_ATTEMPT: String(runIdentity.run_attempt),
    GITHUB_ACTOR_ID: runIdentity.actor_id,
    V2_EXPECTED_WORKFLOW_REPOSITORY:
      "Joey-Tools/codex-review-gate-action",
    V2_ACTUAL_WORKFLOW_REPOSITORY:
      "Joey-Tools/codex-review-gate-action",
    V2_EXPECTED_WORKFLOW_SHA: workflowSha,
    V2_CHECKED_OUT_RELEASE_SHA: workflowSha,
    V2_EXPECTED_WORKFLOW_PATH:
      ".github/workflows/codex-review-gate.yml",
    GITHUB_WORKFLOW_REF:
      "owner/repo/.github/workflows/controller.yml@refs/heads/main",
    GITHUB_WORKFLOW_SHA: workflowSha,
    V2_SELECTION_POLICY: selectionPolicy,
    V2_STATUS_CONTEXT: "codex/github-review-gate",
    V2_STATUS_TARGET_MODE: "test-merge-with-head-sentinel",
  };
  try {
    return await prepareV2WorkflowCommand(environment);
  } finally {
    await rm(directory, { recursive: true, force: true });
  }
}

function leaseBinding(lease) {
  return {
    lease_id: lease.lease_id,
    owner: lease.owner,
    acquire_commit_sha: lease.acquire_commit_sha,
    expires_at: lease.expires_at,
  };
}

function recordPlan(record) {
  const {
    schema: _schema,
    schema_version: _version,
    payload_digest: _payloadDigest,
    record_digest: _recordDigest,
    ...plan
  } = record;
  return structuredClone(plan);
}

function recordFromFixtureEnvelope(envelope) {
  return createV2GitLedgerRecord({
    record_type: envelope.record_type,
    predecessor_commit_sha: envelope.predecessor_commit_sha,
    pull_request: structuredClone(envelope.pull_request),
    head_ref_oid: envelope.head_ref_oid,
    base_ref_oid: envelope.base_ref_oid,
    potential_merge_commit_oid: envelope.potential_merge_commit_oid,
    kind: envelope.kind,
    effect_id: envelope.effect_id,
    idempotency_key: envelope.idempotency_key,
    server_observed_at: envelope.server_observed_at,
    payload: structuredClone(envelope.payload),
    control_comment_binding:
      structuredClone(envelope.control_comment_binding),
    lease: structuredClone(envelope.lease),
  });
}

function fixtureAutomaticRecoveryArtifactReachabilityBoundary({
  intentRecord,
  intentRecordOid,
  boundaryTipRecordOid,
  refReceipt: refReceiptValue,
}) {
  const withoutDigest = {
    schema:
      "codex-review-gate-git-ledger-automatic-recovery-artifact-reachability-v2",
    schema_version: 1,
    intent_record_oid: intentRecordOid,
    scheduler_observation_record_oid:
      intentRecord.payload.action.scheduler_observation_record_oid,
    lease_acquire_commit_sha: intentRecord.lease.acquire_commit_sha,
    boundary_tip_record_oid: boundaryTipRecordOid,
    ref_receipt: structuredClone(refReceiptValue),
  };
  return {
    ...withoutDigest,
    boundary_digest: digestCanonical(
      "codex-review-gate-v2-automatic-recovery-artifact-reachability",
      withoutDigest,
    ),
  };
}

function reachableGitBlobBytes(fixture) {
  const commits = new Set();
  const pendingCommits = [fixture.refTarget];
  const rootTrees = [];
  while (pendingCommits.length > 0) {
    const commitSha = pendingCommits.pop();
    if (commitSha === null || commits.has(commitSha)) continue;
    const commit = fixture.commits.get(commitSha);
    assert.notEqual(commit, undefined, `missing reachable commit ${commitSha}`);
    commits.add(commitSha);
    rootTrees.push(commit.tree.sha);
    pendingCommits.push(...commit.parents.map(({ sha }) => sha));
  }

  const trees = new Set();
  const blobs = new Map();
  const pendingTrees = rootTrees.map((sha) => ({ sha, prefix: "" }));
  while (pendingTrees.length > 0) {
    const current = pendingTrees.pop();
    if (trees.has(current.sha)) continue;
    const tree = fixture.trees.get(current.sha);
    assert.notEqual(tree, undefined, `missing reachable tree ${current.sha}`);
    trees.add(current.sha);
    for (const entry of tree.tree) {
      const path = current.prefix === ""
        ? entry.path
        : `${current.prefix}/${entry.path}`;
      if (entry.type === "tree") {
        pendingTrees.push({ sha: entry.sha, prefix: path });
        continue;
      }
      if (entry.type !== "blob" || blobs.has(entry.sha)) continue;
      const blob = fixture.blobs.get(entry.sha);
      assert.notEqual(blob, undefined, `missing reachable blob ${entry.sha}`);
      const bytes = Buffer.isBuffer(blob.bytes)
        ? blob.bytes
        : Buffer.from(blob.content, "utf8");
      blobs.set(entry.sha, { path, bytes });
    }
  }
  return [...blobs.values()];
}

function durableCandidateInventoryAttachmentValue(
  fixture,
  expectedReceiptDigest,
) {
  for (const blob of fixture.blobs.values()) {
    const envelopeBytes = Buffer.isBuffer(blob.bytes)
      ? blob.bytes
      : Buffer.from(blob.content, "utf8");
    let envelope;
    try {
      envelope = JSON.parse(envelopeBytes.toString("utf8"));
    } catch {
      continue;
    }
    const manifest = envelope?.payload?.attachment_manifest;
    if (!Array.isArray(manifest?.artifacts)) continue;
    const artifact = manifest.artifacts.find((candidate) =>
      candidate.receipt_digest === expectedReceiptDigest);
    if (artifact === undefined || artifact.encoding !== "deflate-raw" ||
        !Array.isArray(artifact.chunks)) {
      continue;
    }
    const chunks = artifact.chunks.map(({ blob_sha: blobSha }) => {
      const chunk = fixture.blobs.get(blobSha);
      assert.notEqual(chunk, undefined, `missing attachment blob ${blobSha}`);
      return Buffer.isBuffer(chunk.bytes)
        ? chunk.bytes
        : Buffer.from(chunk.content, "utf8");
    });
    return JSON.parse(inflateRawSync(Buffer.concat(chunks)).toString("utf8"));
  }
  return null;
}

function durableCurrentOpenGenerationAttachmentValue(fixture, manifest) {
  assert.equal(manifest.encoding, "deflate-raw");
  const chunks = manifest.chunks.map(({ blob_sha: blobSha }) => {
    const chunk = fixture.blobs.get(blobSha);
    assert.notEqual(chunk, undefined, `missing generation blob ${blobSha}`);
    return Buffer.isBuffer(chunk.bytes)
      ? chunk.bytes
      : Buffer.from(chunk.content, "utf8");
  });
  return JSON.parse(inflateRawSync(Buffer.concat(chunks)).toString("utf8"));
}

async function padOrdinaryLedgerWithStandaloneAttestations({
  fixture,
  verifier,
  targetCommitCount,
  templateCommitSha = fixture.refTarget,
}) {
  const template = fixtureEnvelopeAt(fixture, templateCommitSha);
  assert.equal(template.record_type, "capability-attestation");
  const sourceBinding = template.workflow_provenance.operation_binding;
  const current = fixtureEnvelopeAt(fixture, fixture.refTarget);
  const paddingServerTime = current.server_observed_at;
  for (
    let sequence = current.sequence + 1;
    sequence < targetCommitCount;
    sequence += 1
  ) {
    const requestInput = {
      schema: V2_GIT_LEDGER_PROVENANCE_REQUEST_SCHEMA,
      schema_version: 1,
      operation: sourceBinding.operation,
      repository: structuredClone(sourceBinding.repository),
      ledger_ref: sourceBinding.ledger_ref,
      predecessor_commit_sha: fixture.refTarget,
      protection_receipt_digest:
        sourceBinding.protection_receipt_digest,
      source_workflow: structuredClone(sourceBinding.source_workflow),
      effect_scope: null,
      evaluated_scope_receipt: null,
      record_identity: null,
      github_server_time: paddingServerTime,
    };
    const nonce = digestCanonical(
      "codex-review-gate-v2-git-ledger-provenance-nonce",
      requestInput,
    );
    const requestWithoutDigest = {
      ...requestInput,
      nonce,
      audience: `${V2_GIT_LEDGER_OIDC_AUDIENCE}:` +
        nonce.slice("sha256:".length),
    };
    const provenanceRequest = {
      ...requestWithoutDigest,
      request_digest: digestCanonical(
        "codex-review-gate-v2-git-ledger-provenance-request",
        requestWithoutDigest,
      ),
    };
    const provenance = await verifier({
      schema: V2_GIT_LEDGER_PROVENANCE_VERIFIER_REQUEST_SCHEMA,
      schema_version: 1,
      mode: "mint-and-verify",
      provenance_request: provenanceRequest,
      compact_jwt: null,
      stored_receipt: null,
    });
    const envelope = structuredClone(template);
    envelope.sequence = sequence;
    envelope.predecessor_commit_sha = fixture.refTarget;
    envelope.server_observed_at = paddingServerTime;
    envelope.workflow_provenance = provenance.receipt;
    envelope.workflow_provenance_jwt = provenance.compact_jwt;
    delete envelope.envelope_digest;
    envelope.envelope_digest = digestCanonical(
      "codex-review-gate-v2-git-ledger-envelope",
      envelope,
    );
    fixture.materializeHistoricalEnvelope(envelope);
  }
}

function fixtureEnvelopeAt(fixture, commitSha) {
  const commit = fixture.commits.get(commitSha);
  assert.notEqual(commit, undefined, `missing fixture commit ${commitSha}`);
  const tree = fixture.trees.get(commit.tree.sha);
  assert.notEqual(tree, undefined, `missing fixture tree ${commit.tree.sha}`);
  const entry = tree.tree.find((candidate) =>
    candidate.path === V2_GIT_LEDGER_BLOB_PATH &&
    candidate.type === "blob");
  assert.notEqual(entry, undefined, "missing fixture envelope blob entry");
  const blob = fixture.blobs.get(entry.sha);
  assert.notEqual(blob, undefined, `missing fixture blob ${entry.sha}`);
  return JSON.parse(blob.content);
}

function matureCheckpointFixtureLayout(fixture) {
  const suffix = [];
  let cursor = fixture.refTarget;
  let checkpointCommit = null;
  let checkpointTree = null;
  while (cursor !== null) {
    const commit = fixture.commits.get(cursor);
    assert.notEqual(commit, undefined, `missing checkpoint commit ${cursor}`);
    const tree = fixture.trees.get(commit.tree.sha);
    assert.notEqual(tree, undefined, `missing checkpoint tree ${commit.tree.sha}`);
    const stateEntry = tree.tree.find((entry) =>
      entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_PATH &&
      entry.type === "tree");
    if (stateEntry !== undefined) {
      checkpointCommit = commit;
      checkpointTree = tree;
      break;
    }
    suffix.unshift({ sha: cursor, commit, tree });
    cursor = commit.parents.length === 0 ? null : commit.parents[0].sha;
  }
  assert.notEqual(checkpointCommit, null, "missing mature checkpoint root");
  assert.notEqual(checkpointTree, null, "missing mature checkpoint root tree");
  const stateTreeEntry = checkpointTree.tree.find((entry) =>
    entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_PATH &&
    entry.type === "tree");
  const stateTree = fixture.trees.get(stateTreeEntry.sha);
  assert.notEqual(stateTree, undefined, "missing mature checkpoint state tree");
  const stateBlobEntry = stateTree.tree.find((entry) =>
    entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_BLOB_PATH &&
    entry.type === "blob");
  const sourceTreeEntry = stateTree.tree.find((entry) =>
    entry.path === "source-query-seed-v1" && entry.type === "tree");
  assert.notEqual(stateBlobEntry, undefined, "missing mature checkpoint state blob");
  assert.notEqual(sourceTreeEntry, undefined, "missing mature checkpoint source seed");
  const sourceTree = fixture.trees.get(sourceTreeEntry.sha);
  assert.notEqual(sourceTree, undefined, "missing mature checkpoint source tree");
  const manifestEntry = sourceTree.tree.find((entry) =>
    entry.path === "manifest-v1.json" && entry.type === "blob");
  const chunksEntry = sourceTree.tree.find((entry) =>
    entry.path === "chunks-v1" && entry.type === "tree");
  assert.notEqual(manifestEntry, undefined, "missing mature checkpoint manifest");
  assert.notEqual(chunksEntry, undefined, "missing mature checkpoint chunks");
  const manifest = JSON.parse(fixture.blobs.get(manifestEntry.sha).content);
  const chunkTree = fixture.trees.get(chunksEntry.sha);
  assert.notEqual(chunkTree, undefined, "missing mature checkpoint chunk tree");
  const encoded = Buffer.concat(manifest.chunks.map(({ blob_sha: blobSha }) =>
    Buffer.from(fixture.blobs.get(blobSha).bytes)));
  const seed = JSON.parse(inflateRawSync(encoded).toString("utf8"));
  return {
    checkpoint_commit: checkpointCommit,
    checkpoint_tree: checkpointTree,
    state_tree: stateTree,
    state_blob_entry: stateBlobEntry,
    source_tree: sourceTree,
    manifest,
    chunk_tree: chunkTree,
    seed,
    suffix,
  };
}

function resealMatureCheckpointClassificationForFixture({
  fixture,
  mutateClassification,
  resealProjectionDigest = true,
}) {
  const layout = matureCheckpointFixtureLayout(fixture);
  const rootEnvelopeEntry = layout.checkpoint_tree.tree.find((entry) =>
    entry.path === V2_GIT_LEDGER_BLOB_PATH && entry.type === "blob");
  assert.notEqual(rootEnvelopeEntry, undefined,
    "missing mature checkpoint carrier");
  const state = JSON.parse(
    fixture.blobs.get(layout.state_blob_entry.sha).content,
  );
  const classification = state.next_unit.classification_projection;
  mutateClassification(classification);
  if (resealProjectionDigest) {
    delete classification.projection_digest;
    classification.projection_digest = digestCanonical(
      "codex-review-gate-v2-candidate-inventory-cycle-classification",
      classification,
    );
  }
  state.next_unit.projection_digest = classification.projection_digest;
  state.next_unit.admission_digest = digestCanonical(
    "codex-review-gate-v2-checkpoint-next-unit-admission",
    {
      schema:
        "codex-review-gate-git-ledger-checkpoint-next-unit-admission-handle-v2",
      schema_version: 1,
      repository: structuredClone(state.repository),
      ledger_ref: state.ledger_ref,
      source_load_digest: state.source.source_load_digest,
      next_unit_kind: state.next_unit.kind,
      cycle_id: state.next_unit.cycle_id,
      stable_cycle_receipt_digest:
        state.next_unit.stable_cycle_receipt_digest,
      stable_command_authority_digest:
        state.next_unit.stable_command_authority_digest,
      trigger_identity_digest: state.next_unit.trigger_identity_digest,
      projection_digest: state.next_unit.projection_digest,
      total_commit_budget_required:
        state.next_unit.total_commit_budget_required,
      current_epoch_remaining_commit_capacity:
        state.next_unit.current_epoch_remaining_commit_capacity,
      fresh_epoch_remaining_commit_capacity:
        state.next_unit.fresh_epoch_remaining_commit_capacity,
    },
  );
  delete state.checkpoint_state_digest;
  state.checkpoint_state_digest = digestCanonical(
    "codex-review-gate-v2-checkpoint-state",
    state,
  );
  const stateContent = `${canonicalJson(state)}\n`;
  const stateBytes = Buffer.from(stateContent, "utf8");
  const stateBlobSha = gitObjectSha("blob", stateBytes);
  fixture.blobs.set(stateBlobSha, {
    sha: stateBlobSha,
    content: stateContent,
    bytes: stateBytes,
  });
  const nextStateTreeEntries = layout.state_tree.tree.map((entry) =>
    entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_BLOB_PATH
      ? { ...entry, sha: stateBlobSha }
      : structuredClone(entry));
  const stateTreeSha = canonicalGitTreeSha(nextStateTreeEntries);
  fixture.trees.set(stateTreeSha, {
    sha: stateTreeSha,
    tree: nextStateTreeEntries,
  });

  const carrier = JSON.parse(
    fixture.blobs.get(rootEnvelopeEntry.sha).content,
  );
  carrier.payload.checkpoint_state_tree_sha = stateTreeSha;
  carrier.payload.checkpoint_state_digest = state.checkpoint_state_digest;
  carrier.payload.next_unit_admission_digest =
    state.next_unit.admission_digest;
  delete carrier.payload.payload_digest;
  carrier.payload.payload_digest = digestCanonical(
    "codex-review-gate-v2-checkpoint-carrier-payload",
    carrier.payload,
  );
  carrier.payload_digest = carrier.payload.payload_digest;
  carrier.workflow_provenance.operation_binding.record_identity
    .payload_digest = carrier.payload_digest;
  resealProvenance(carrier.workflow_provenance);
  delete carrier.carrier_digest;
  carrier.carrier_digest = digestCanonical(
    "codex-review-gate-v2-checkpoint-carrier",
    carrier,
  );
  const carrierContent = `${canonicalJson(carrier)}\n`;
  const carrierBytes = Buffer.from(carrierContent, "utf8");
  const carrierBlobSha = gitObjectSha("blob", carrierBytes);
  fixture.blobs.set(carrierBlobSha, {
    sha: carrierBlobSha,
    content: carrierContent,
    bytes: carrierBytes,
  });
  const rootTreeEntries = layout.checkpoint_tree.tree.map((entry) => {
    if (entry.path === V2_GIT_LEDGER_BLOB_PATH) {
      return { ...entry, sha: carrierBlobSha };
    }
    if (entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_PATH) {
      return { ...entry, sha: stateTreeSha };
    }
    return structuredClone(entry);
  });
  const rootTreeSha = canonicalGitTreeSha(rootTreeEntries);
  fixture.trees.set(rootTreeSha, {
    sha: rootTreeSha,
    tree: rootTreeEntries,
  });
  let predecessor = storeRewrittenFixtureCommit({
    fixture,
    original: layout.checkpoint_commit,
    treeSha: rootTreeSha,
    parents: layout.checkpoint_commit.parents.map(({ sha }) => sha),
  });
  const checkpointCommitSha = predecessor;

  const semanticRecords = layout.seed.records.map((entry) =>
    structuredClone(entry));
  const attemptRecords = [];
  for (const { commit: originalCommit, tree: originalTree } of layout.suffix) {
    const envelopeEntry = originalTree.tree.find((entry) =>
      entry.path === V2_GIT_LEDGER_BLOB_PATH && entry.type === "blob");
    const envelope = JSON.parse(
      fixture.blobs.get(envelopeEntry.sha).content,
    );
    envelope.predecessor_commit_sha = predecessor;
    envelope.workflow_provenance.operation_binding
      .predecessor_commit_sha = predecessor;
    const candidateAuthority =
      deriveV2GitLedgerCandidateInventoryAuthority(
        semanticRecords,
        REPOSITORY,
      );
    if (envelope.record_type === "candidate-inventory-observation") {
      envelope.payload.prior_candidate_authority_digest =
        candidateAuthority.authority_digest;
      if (envelope.payload.phase === "attempt") {
        const priorAttempt = attemptRecords.at(-1) ?? null;
        envelope.payload.prior_attempt_record_oid =
          priorAttempt?.record_oid ?? null;
      } else if (envelope.payload.phase === "cycle-complete") {
        envelope.payload.attempt_records = attemptRecords.map(
          (attempt) => structuredClone(attempt),
        );
      }
    } else if (
      envelope.record_type === "candidate-dispatch-observation"
    ) {
      const candidateAuthorityDigest = candidateAuthority.authority_digest;
      const completedRecordOid =
        candidateAuthority.completed_cycle.complete_record_oid;
      const generationId = `candidate-dispatch:${digestCanonical(
        "codex-review-gate-v2-candidate-dispatch-generation",
        {
          repository: {
            owner: REPOSITORY.owner,
            name: REPOSITORY.name,
            id: REPOSITORY.id,
            node_id: REPOSITORY.node_id,
          },
          cycle_id: envelope.payload.cycle_id,
          completed_cycle_record_oid: completedRecordOid,
          inventory_digest: envelope.payload.inventory_digest,
        },
      ).slice("sha256:".length)}`;
      envelope.payload.generation_id = generationId;
      envelope.payload.candidate_inventory_authority_digest =
        candidateAuthorityDigest;
      envelope.payload.completed_cycle_record_oid = completedRecordOid;
      const reservation = envelope.payload.reservation;
      reservation.generation_id = generationId;
      reservation.candidate_inventory_authority_digest =
        candidateAuthorityDigest;
      reservation.completed_cycle_record_oid = completedRecordOid;
      reservation.source_tip_commit_sha = predecessor;
      reservation.dispatch_digest = digestCanonical(
        "codex-review-gate-v2-candidate-dispatch-selection",
        {
          repository: reservation.repository,
          generation_id: reservation.generation_id,
          cycle_id: reservation.cycle_id,
          candidate_inventory_authority_digest:
            reservation.candidate_inventory_authority_digest,
          inventory_digest: reservation.inventory_digest,
          batch_index: reservation.batch_index,
          batch_count: reservation.batch_count,
          candidate_offset: reservation.candidate_offset,
          candidates: reservation.candidates,
        },
      );
      delete reservation.reservation_digest;
      reservation.reservation_digest = digestCanonical(
        "codex-review-gate-v2-candidate-dispatch-reservation",
        reservation,
      );
    }
    const operationBinding = envelope.workflow_provenance.operation_binding;
    const evaluatedScope = operationBinding.evaluated_scope_receipt;
    const evaluatedScopeInput = {
      repository: evaluatedScope.repository,
      payload: envelope.payload,
      trigger_identity: {
        event_name: evaluatedScope.trigger_event_name,
        ref: evaluatedScope.trigger_ref,
        sha: evaluatedScope.trigger_sha,
      },
      repository_endpoint_receipt:
        evaluatedScope.scope_endpoint_receipt,
    };
    operationBinding.evaluated_scope_receipt =
      envelope.record_type === "candidate-inventory-observation"
        ? createV2GitLedgerCandidateInventoryEvaluatedScopeReceipt(
          evaluatedScopeInput,
        )
        : createV2GitLedgerCandidateDispatchEvaluatedScopeReceipt(
          evaluatedScopeInput,
        );
    envelope.payload_digest = digestV2GitLedgerPayload(envelope.payload);
    operationBinding.record_identity.payload_digest = envelope.payload_digest;
    resealProvenance(envelope.workflow_provenance);
    resealEnvelope(envelope);
    const envelopeContent = `${canonicalJson(envelope)}\n`;
    const envelopeBytes = Buffer.from(envelopeContent, "utf8");
    const envelopeBlobSha = gitObjectSha("blob", envelopeBytes);
    fixture.blobs.set(envelopeBlobSha, {
      sha: envelopeBlobSha,
      content: envelopeContent,
      bytes: envelopeBytes,
    });
    const nextTreeEntries = originalTree.tree.map((entry) =>
      entry.path === V2_GIT_LEDGER_BLOB_PATH
        ? { ...entry, sha: envelopeBlobSha }
        : structuredClone(entry));
    const treeSha = canonicalGitTreeSha(nextTreeEntries);
    fixture.trees.set(treeSha, { sha: treeSha, tree: nextTreeEntries });
    predecessor = storeRewrittenFixtureCommit({
      fixture,
      original: originalCommit,
      treeSha,
      parents: [predecessor],
    });
    let attachmentEvidence = null;
    if (envelope.record_type === "candidate-inventory-observation") {
      if (envelope.payload.phase === "attempt") {
        const values = envelope.payload.attachment_manifest.artifacts.map(
          (artifact) => ({
            artifact,
            value: durableCandidateInventoryAttachmentValue(
              fixture,
              artifact.receipt_digest,
            ),
          }),
        );
        assert.equal(values.every(({ value }) => value !== null), true);
        attachmentEvidence = {
          initial_inventory: values.find(({ artifact }) =>
            artifact.role === "initial-inventory").value,
          shard_receipts: values.filter(({ artifact }) =>
            artifact.role === "initial-shard").map(({ value }) => value),
          final_inventory: values.find(({ artifact }) =>
            artifact.role === "final-inventory").value,
          final_shard_receipts: values.filter(({ artifact }) =>
            artifact.role === "final-shard").map(({ value }) => value),
        };
        attemptRecords.push({
          record_oid: predecessor,
          attempt_evidence_digest:
            envelope.payload.attempt_evidence_digest,
          attachment_manifest_digest:
            envelope.payload.attachment_manifest.manifest_digest,
          outcome: envelope.payload.outcome,
        });
      }
    }
    semanticRecords.push({
      commit_sha: predecessor,
      parents: fixture.commits.get(predecessor).parents.map(({ sha }) => sha),
      tree_sha: treeSha,
      blob_sha: envelopeBlobSha,
      envelope: structuredClone(envelope),
      evidence: {},
      ...(attachmentEvidence === null ? {} : {
        candidate_inventory_attachment_evidence: attachmentEvidence,
      }),
    });
  }
  const rewrittenSnapshot = fixture.snapshotHistoricalState();
  rewrittenSnapshot.ref_target = predecessor;
  fixture.restoreHistoricalState(rewrittenSnapshot);
  return {
    checkpoint_commit_sha: checkpointCommitSha,
    transaction_tip_commit_sha: predecessor,
  };
}

function resealMatureCurrentOpenCheckpointStateForFixture({
  fixture,
  mutateNextUnit,
  resealAdmissionDigest = true,
}) {
  const layout = matureCheckpointFixtureLayout(fixture);
  assert.equal(layout.suffix.length, 2);
  const rootEnvelopeEntry = layout.checkpoint_tree.tree.find((entry) =>
    entry.path === V2_GIT_LEDGER_BLOB_PATH && entry.type === "blob");
  assert.notEqual(rootEnvelopeEntry, undefined,
    "missing mature current-open checkpoint carrier");
  const state = JSON.parse(
    fixture.blobs.get(layout.state_blob_entry.sha).content,
  );
  assert.equal(state.schema_version, 3);
  mutateNextUnit(state.next_unit);
  if (resealAdmissionDigest) {
    state.next_unit.admission_digest = digestCanonical(
      "codex-review-gate-v2-current-open-checkpoint-admission",
      {
        schema:
          "codex-review-gate-git-ledger-current-open-checkpoint-admission-v1",
        schema_version: 1,
        profile: state.next_unit.profile,
        repository: structuredClone(state.repository),
        ledger_ref: state.ledger_ref,
        source_load_digest: state.source.source_load_digest,
        next_unit_kind: state.next_unit.kind,
        generation_id: state.next_unit.generation_id,
        current_open_receipt_digest:
          state.next_unit.current_open_receipt_digest,
        current_open_projection_digest:
          state.next_unit.current_open_projection_digest,
        production_candidate_authority_digest:
          state.next_unit.production_candidate_authority_digest,
        candidate_count: state.next_unit.candidate_count,
        candidate_set_digest: state.next_unit.candidate_set_digest,
        source_current_open_semantic_digest:
          state.next_unit.source_current_open_semantic_digest,
        lifecycle_candidate_set_digest:
          state.next_unit.lifecycle_candidate_set_digest,
        attachment_manifest_digest:
          state.next_unit.attachment_manifest_digest,
        stable_command_authority_digest:
          state.next_unit.stable_command_authority_digest,
        trigger_identity_digest: state.next_unit.trigger_identity_digest,
        repository_endpoint_receipt_digest:
          state.next_unit.repository_endpoint_receipt_digest,
        total_commit_budget_required:
          state.next_unit.total_commit_budget_required,
        current_epoch_remaining_commit_capacity:
          state.next_unit.current_epoch_remaining_commit_capacity,
        fresh_epoch_remaining_commit_capacity:
          state.next_unit.fresh_epoch_remaining_commit_capacity,
      },
    );
  }
  delete state.checkpoint_state_digest;
  state.checkpoint_state_digest = digestCanonical(
    "codex-review-gate-v2-checkpoint-state",
    state,
  );
  const stateContent = `${canonicalJson(state)}\n`;
  const stateBytes = Buffer.from(stateContent, "utf8");
  const stateBlobSha = gitObjectSha("blob", stateBytes);
  fixture.blobs.set(stateBlobSha, {
    sha: stateBlobSha,
    content: stateContent,
    bytes: stateBytes,
  });
  const nextStateTreeEntries = layout.state_tree.tree.map((entry) =>
    entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_BLOB_PATH
      ? { ...entry, sha: stateBlobSha }
      : structuredClone(entry));
  const stateTreeSha = canonicalGitTreeSha(nextStateTreeEntries);
  fixture.trees.set(stateTreeSha, {
    sha: stateTreeSha,
    tree: nextStateTreeEntries,
  });

  const carrier = JSON.parse(
    fixture.blobs.get(rootEnvelopeEntry.sha).content,
  );
  carrier.payload.checkpoint_state_tree_sha = stateTreeSha;
  carrier.payload.checkpoint_state_digest = state.checkpoint_state_digest;
  carrier.payload.next_unit_admission_digest =
    state.next_unit.admission_digest;
  delete carrier.payload.payload_digest;
  carrier.payload.payload_digest = digestCanonical(
    "codex-review-gate-v2-checkpoint-carrier-payload",
    carrier.payload,
  );
  carrier.payload_digest = carrier.payload.payload_digest;
  carrier.workflow_provenance.operation_binding.record_identity
    .payload_digest = carrier.payload_digest;
  resealProvenance(carrier.workflow_provenance);
  delete carrier.carrier_digest;
  carrier.carrier_digest = digestCanonical(
    "codex-review-gate-v2-checkpoint-carrier",
    carrier,
  );
  const carrierContent = `${canonicalJson(carrier)}\n`;
  const carrierBytes = Buffer.from(carrierContent, "utf8");
  const carrierBlobSha = gitObjectSha("blob", carrierBytes);
  fixture.blobs.set(carrierBlobSha, {
    sha: carrierBlobSha,
    content: carrierContent,
    bytes: carrierBytes,
  });
  const rootTreeEntries = layout.checkpoint_tree.tree.map((entry) => {
    if (entry.path === V2_GIT_LEDGER_BLOB_PATH) {
      return { ...entry, sha: carrierBlobSha };
    }
    if (entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_PATH) {
      return { ...entry, sha: stateTreeSha };
    }
    return structuredClone(entry);
  });
  const rootTreeSha = canonicalGitTreeSha(rootTreeEntries);
  fixture.trees.set(rootTreeSha, {
    sha: rootTreeSha,
    tree: rootTreeEntries,
  });
  let predecessor = storeRewrittenFixtureCommit({
    fixture,
    original: layout.checkpoint_commit,
    treeSha: rootTreeSha,
    parents: layout.checkpoint_commit.parents.map(({ sha }) => sha),
  });
  const checkpointCommitSha = predecessor;

  const storeSuffixEnvelope = ({ originalCommit, originalTree, envelope }) => {
    const operationBinding = envelope.workflow_provenance.operation_binding;
    const evaluatedScope = operationBinding.evaluated_scope_receipt;
    const evaluatedScopeInput = {
      repository: evaluatedScope.repository,
      payload: envelope.payload,
      trigger_identity: {
        event_name: evaluatedScope.trigger_event_name,
        ref: evaluatedScope.trigger_ref,
        sha: evaluatedScope.trigger_sha,
      },
      repository_endpoint_receipt:
        evaluatedScope.scope_endpoint_receipt,
    };
    operationBinding.evaluated_scope_receipt =
      envelope.record_type === "candidate-inventory-observation"
        ? createV2GitLedgerCandidateInventoryEvaluatedScopeReceipt(
          evaluatedScopeInput,
        )
        : createV2GitLedgerCandidateDispatchEvaluatedScopeReceipt(
          evaluatedScopeInput,
        );
    envelope.payload_digest = digestV2GitLedgerPayload(envelope.payload);
    operationBinding.record_identity.payload_digest = envelope.payload_digest;
    resealProvenance(envelope.workflow_provenance);
    resealEnvelope(envelope);
    const envelopeContent = `${canonicalJson(envelope)}\n`;
    const envelopeBytes = Buffer.from(envelopeContent, "utf8");
    const envelopeBlobSha = gitObjectSha("blob", envelopeBytes);
    fixture.blobs.set(envelopeBlobSha, {
      sha: envelopeBlobSha,
      content: envelopeContent,
      bytes: envelopeBytes,
    });
    const nextTreeEntries = originalTree.tree.map((entry) =>
      entry.path === V2_GIT_LEDGER_BLOB_PATH
        ? { ...entry, sha: envelopeBlobSha }
        : structuredClone(entry));
    const treeSha = canonicalGitTreeSha(nextTreeEntries);
    fixture.trees.set(treeSha, { sha: treeSha, tree: nextTreeEntries });
    const commitSha = storeRewrittenFixtureCommit({
      fixture,
      original: originalCommit,
      treeSha,
      parents: [predecessor],
    });
    predecessor = commitSha;
    return { commitSha, treeSha, envelopeBlobSha };
  };

  const generationOriginal = layout.suffix[0];
  const generationEnvelopeEntry = generationOriginal.tree.tree.find((entry) =>
    entry.path === V2_GIT_LEDGER_BLOB_PATH && entry.type === "blob");
  const generationEnvelope = JSON.parse(
    fixture.blobs.get(generationEnvelopeEntry.sha).content,
  );
  assert.equal(generationEnvelope.record_type,
    "candidate-inventory-observation");
  assert.equal(generationEnvelope.payload.phase, "current-open-generation");
  generationEnvelope.predecessor_commit_sha = predecessor;
  generationEnvelope.workflow_provenance.operation_binding
    .predecessor_commit_sha = predecessor;
  const generationStored = storeSuffixEnvelope({
    originalCommit: generationOriginal.commit,
    originalTree: generationOriginal.tree,
    envelope: generationEnvelope,
  });
  const attachmentEvidence = durableCurrentOpenGenerationAttachmentValue(
    fixture,
    generationEnvelope.payload.attachment_manifest,
  );
  const semanticRecords = layout.seed.records.map((entry) =>
    structuredClone(entry));
  semanticRecords.push({
    commit_sha: generationStored.commitSha,
    parents: fixture.commits.get(generationStored.commitSha).parents.map(
      ({ sha }) => sha,
    ),
    tree_sha: generationStored.treeSha,
    blob_sha: generationStored.envelopeBlobSha,
    envelope: structuredClone(generationEnvelope),
    evidence: {},
    candidate_inventory_attachment_evidence: attachmentEvidence,
  });
  const candidateAuthority = deriveV2GitLedgerCandidateInventoryAuthority(
    semanticRecords,
    REPOSITORY,
  );
  const generation = candidateAuthority.completed_generation;
  assert.notEqual(generation, null);
  assert.notEqual(generation.candidates, null);
  const sourceBinding = {
    schema: "codex-review-gate-git-ledger-candidate-dispatch-source-v2",
    schema_version: 2,
    source_profile: generation.source_profile,
    source_generation_id: generation.generation_id,
    source_generation_record_oid: generation.generation_record_oid,
    source_generation_digest: generation.generation_digest,
    production_candidate_authority_digest:
      generation.source_authority_digest,
    candidate_set_digest: generation.candidate_set_digest,
    source_current_open_semantic_digest: generation.source_semantic_digest,
    lifecycle_candidate_set_digest:
      generation.lifecycle_candidate_set_digest,
  };
  const dispatchGenerationId = `candidate-dispatch:${digestCanonical(
    "codex-review-gate-v2-current-open-dispatch-generation",
    {
      repository: candidateRepository(),
      generation_record_oid: generation.generation_record_oid,
      generation_digest: generation.generation_digest,
      inventory_digest: generation.inventory_digest,
    },
  ).slice("sha256:".length)}`;
  const cycleId = `candidate-cycle:${digestCanonical(
    "codex-review-gate-v2-current-open-dispatch-cycle",
    {
      repository: candidateRepository(),
      generation_record_oid: generation.generation_record_oid,
      generation_digest: generation.generation_digest,
    },
  ).slice("sha256:".length)}`;
  const selections = generation.candidates.map((candidate) => {
    const withoutDigest = {
      schema:
        "codex-review-gate-git-ledger-candidate-dispatch-selection-v2",
      schema_version: 2,
      source_generation_record_oid: generation.generation_record_oid,
      identity: structuredClone(candidate.identity),
      identity_digest: candidate.identity_digest,
      lifecycle_seed: structuredClone(candidate.lifecycle_seed),
      lifecycle_seed_digest: candidate.lifecycle_seed_digest,
      lifecycle_generation_id: candidate.lifecycle_generation_id,
    };
    return {
      ...withoutDigest,
      selection_digest: digestCanonical(
        "codex-review-gate-v2-current-open-dispatch-selection",
        {
          source_generation_record_oid:
            withoutDigest.source_generation_record_oid,
          identity_digest: withoutDigest.identity_digest,
          lifecycle_seed_digest: withoutDigest.lifecycle_seed_digest,
          lifecycle_generation_id: withoutDigest.lifecycle_generation_id,
        },
      ),
    };
  });

  const dispatchOriginal = layout.suffix[1];
  const dispatchEnvelopeEntry = dispatchOriginal.tree.tree.find((entry) =>
    entry.path === V2_GIT_LEDGER_BLOB_PATH && entry.type === "blob");
  const dispatchEnvelope = JSON.parse(
    fixture.blobs.get(dispatchEnvelopeEntry.sha).content,
  );
  assert.equal(dispatchEnvelope.record_type, "candidate-dispatch-observation");
  assert.equal(dispatchEnvelope.payload.phase, "reserve");
  dispatchEnvelope.predecessor_commit_sha = predecessor;
  dispatchEnvelope.workflow_provenance.operation_binding
    .predecessor_commit_sha = predecessor;
  const originalReservation = dispatchEnvelope.payload.reservation;
  const reservationWithoutDigest = {
    ...structuredClone(originalReservation),
    generation_id: dispatchGenerationId,
    cycle_id: cycleId,
    candidate_inventory_authority_digest:
      candidateAuthority.authority_digest,
    candidate_source: sourceBinding,
    inventory_digest: generation.inventory_digest,
    source_tip_commit_sha: generation.generation_record_oid,
    candidates: selections.slice(
      originalReservation.candidate_offset,
      originalReservation.candidate_offset +
        originalReservation.candidates.length,
    ),
  };
  delete reservationWithoutDigest.reservation_digest;
  reservationWithoutDigest.dispatch_digest = digestCanonical(
    "codex-review-gate-v2-current-open-candidate-dispatch-selection",
    {
      repository: structuredClone(reservationWithoutDigest.repository),
      generation_id: reservationWithoutDigest.generation_id,
      cycle_id: reservationWithoutDigest.cycle_id,
      candidate_source: structuredClone(
        reservationWithoutDigest.candidate_source,
      ),
      candidate_inventory_authority_digest:
        reservationWithoutDigest.candidate_inventory_authority_digest,
      inventory_digest: reservationWithoutDigest.inventory_digest,
      batch_index: reservationWithoutDigest.batch_index,
      batch_count: reservationWithoutDigest.batch_count,
      candidate_offset: reservationWithoutDigest.candidate_offset,
      candidates: structuredClone(reservationWithoutDigest.candidates),
    },
  );
  const reservation = validateV2GitLedgerCandidateDispatchReservation({
    ...reservationWithoutDigest,
    reservation_digest: digestCanonical(
      "codex-review-gate-v2-current-open-candidate-dispatch-reservation",
      reservationWithoutDigest,
    ),
  });
  Object.assign(dispatchEnvelope.payload, {
    generation_id: dispatchGenerationId,
    cycle_id: cycleId,
    candidate_inventory_authority_digest:
      candidateAuthority.authority_digest,
    candidate_source: sourceBinding,
    inventory_digest: generation.inventory_digest,
    reservation,
  });
  const dispatchStored = storeSuffixEnvelope({
    originalCommit: dispatchOriginal.commit,
    originalTree: dispatchOriginal.tree,
    envelope: dispatchEnvelope,
  });
  semanticRecords.push({
    commit_sha: dispatchStored.commitSha,
    parents: fixture.commits.get(dispatchStored.commitSha).parents.map(
      ({ sha }) => sha,
    ),
    tree_sha: dispatchStored.treeSha,
    blob_sha: dispatchStored.envelopeBlobSha,
    envelope: structuredClone(dispatchEnvelope),
    evidence: {},
  });
  deriveV2GitLedgerCandidateDispatchAuthority(semanticRecords, REPOSITORY);

  const rewrittenSnapshot = fixture.snapshotHistoricalState();
  rewrittenSnapshot.ref_target = predecessor;
  fixture.restoreHistoricalState(rewrittenSnapshot);
  return {
    checkpoint_commit_sha: checkpointCommitSha,
    transaction_tip_commit_sha: predecessor,
  };
}

function assertExactFixtureKeys(value, expectedKeys, label) {
  assert.equal(
    value !== null && typeof value === "object" && !Array.isArray(value),
    true,
    `${label} must be an object`,
  );
  assert.deepEqual(
    Object.keys(value).sort(),
    [...expectedKeys].sort(),
    `${label} has unexpected keys`,
  );
}

function assertFixtureOid(value, label) {
  assert.equal(typeof value, "string", `${label} must be a string`);
  assert.match(value, /^[0-9a-f]{40}$/u, `${label} must be a Git OID`);
}

function assertSortedUniqueFixtureOids(records, label) {
  let prior = null;
  for (const record of records) {
    assertFixtureOid(record.sha, `${label} sha`);
    if (prior !== null) {
      assert.ok(prior < record.sha, `${label} OIDs must be sorted and unique`);
    }
    prior = record.sha;
  }
}

function assertC65HistoricalProvenanceReverification(actual, expected) {
  const project = (entry) => canonicalJson({
    commit_oid: entry.commit_oid,
    token_sha256: entry.token_sha256,
    jti: entry.jti,
    request_digest: entry.request_digest,
  });
  assert.equal(actual.length, expected.length);
  assert.equal(new Set(actual.map(project)).size, actual.length);
  assert.deepEqual(actual.map(project).sort(), expected.map(project).sort());
}

async function loadC65Batch0GitFixtureState({
  fixtureUrl = C65_BATCH0_GIT_FIXTURE_URL,
  afterFileIdentityCheck = null,
} = {}) {
  assert.notEqual(
    C65_BATCH0_GIT_FIXTURE_O_CLOEXEC,
    0,
    "C65 fixture loader requires an explicit close-on-exec flag",
  );
  assert.notEqual(
    C65_BATCH0_GIT_FIXTURE_O_NONBLOCK,
    0,
    "C65 fixture loader requires an explicit nonblocking flag",
  );
  const handle = await open(fixtureUrl, C65_BATCH0_GIT_FIXTURE_OPEN_FLAGS);
  let encoded;
  try {
    const before = await handle.stat();
    assert.equal(before.isFile(), true, "C65 fixture must be a regular file");
    assert.ok(before.size <= C65_BATCH0_GIT_FIXTURE_MAX_GZIP_BYTES);
    assert.equal(before.size, C65_BATCH0_GIT_FIXTURE_GZIP_BYTES);
    await afterFileIdentityCheck?.();
    const chunks = [];
    let total = 0;
    while (total <= C65_BATCH0_GIT_FIXTURE_MAX_GZIP_BYTES) {
      const buffer = Buffer.allocUnsafe(Math.min(
        64 * 1024,
        C65_BATCH0_GIT_FIXTURE_MAX_GZIP_BYTES + 1 - total,
      ));
      const { bytesRead } = await handle.read(
        buffer,
        0,
        buffer.byteLength,
        null,
      );
      if (bytesRead === 0) break;
      total += bytesRead;
      assert.ok(total <= C65_BATCH0_GIT_FIXTURE_MAX_GZIP_BYTES);
      chunks.push(buffer.subarray(0, bytesRead));
    }
    encoded = Buffer.concat(chunks, total);
    const after = await handle.stat();
    assert.deepEqual({
      dev: after.dev,
      ino: after.ino,
      type: after.mode & fsConstants.S_IFMT,
      size: after.size,
    }, {
      dev: before.dev,
      ino: before.ino,
      type: before.mode & fsConstants.S_IFMT,
      size: before.size,
    });
    assert.equal(after.isFile(), true);
    assert.equal(after.size, encoded.byteLength);
  } finally {
    await handle.close();
  }
  assert.equal(encoded.byteLength, C65_BATCH0_GIT_FIXTURE_GZIP_BYTES);
  assert.equal(rawDigest(encoded), C65_BATCH0_GIT_FIXTURE_GZIP_SHA256);
  const decoded = gunzipSync(encoded, {
    maxOutputLength: C65_BATCH0_GIT_FIXTURE_MAX_JSON_BYTES,
  });
  assert.equal(decoded.byteLength, C65_BATCH0_GIT_FIXTURE_JSON_BYTES);
  assert.equal(rawDigest(decoded), C65_BATCH0_GIT_FIXTURE_JSON_SHA256);
  const decodedText = decoded.toString("utf8");
  assert.equal(Buffer.byteLength(decodedText, "utf8"), decoded.byteLength);
  const value = JSON.parse(decodedText);
  assert.equal(canonicalJson(value), decodedText);
  assertExactFixtureKeys(value, [
    "blobs",
    "clock_second",
    "commits",
    "ref_target",
    "schema",
    "schema_version",
    "server_time_frozen",
    "trees",
  ], "C65 fixture");
  assert.equal(value.schema, "codex-review-gate-v2-reachable-git-fixture");
  assert.equal(value.schema_version, 1);
  assert.equal(value.ref_target, C65_BATCH0_GIT_FIXTURE_REF);
  assert.equal(value.clock_second, C65_BATCH0_GIT_FIXTURE_CLOCK_SECOND);
  assert.equal(value.server_time_frozen, true);
  assert.equal(value.blobs.length, C65_BATCH0_GIT_FIXTURE_COUNTS.blobs);
  assert.equal(value.trees.length, C65_BATCH0_GIT_FIXTURE_COUNTS.trees);
  assert.equal(value.commits.length, C65_BATCH0_GIT_FIXTURE_COUNTS.commits);
  assertSortedUniqueFixtureOids(value.blobs, "C65 fixture blob");
  assertSortedUniqueFixtureOids(value.trees, "C65 fixture tree");
  assertSortedUniqueFixtureOids(value.commits, "C65 fixture commit");

  const blobs = new Map();
  for (const blob of value.blobs) {
    assertExactFixtureKeys(blob, ["bytes_base64", "sha"], "C65 fixture blob");
    assert.equal(typeof blob.bytes_base64, "string");
    const bytes = Buffer.from(blob.bytes_base64, "base64");
    assert.equal(bytes.toString("base64"), blob.bytes_base64);
    assert.ok(bytes.byteLength <= MAX_V2_GIT_LEDGER_BLOB_BYTES);
    assert.equal(gitObjectSha("blob", bytes), blob.sha);
    blobs.set(blob.sha, bytes);
  }

  const trees = new Map();
  for (const tree of value.trees) {
    assertExactFixtureKeys(tree, ["entries", "sha"], "C65 fixture tree");
    assert.ok(Array.isArray(tree.entries));
    assert.ok(tree.entries.length > 0 && tree.entries.length <= 64);
    const entryPaths = new Set();
    for (const entry of tree.entries) {
      assertExactFixtureKeys(
        entry,
        ["mode", "path", "sha", "type"],
        "C65 fixture tree entry",
      );
      assert.equal(typeof entry.path, "string");
      assert.ok(Buffer.byteLength(entry.path, "utf8") > 0);
      assert.ok(Buffer.byteLength(entry.path, "utf8") <= 1024);
      assert.equal(entry.path.includes("/"), false);
      assert.equal(entry.path.includes("\0"), false);
      assert.notEqual(entry.path, ".");
      assert.notEqual(entry.path, "..");
      assert.equal(entryPaths.has(entry.path), false);
      entryPaths.add(entry.path);
      assertFixtureOid(entry.sha, "C65 fixture tree entry sha");
      assert.equal(
        (entry.type === "blob" && entry.mode === "100644") ||
          (entry.type === "tree" && entry.mode === "040000"),
        true,
      );
    }
    assert.equal(canonicalGitTreeSha(tree.entries), tree.sha);
    trees.set(tree.sha, structuredClone(tree.entries));
  }

  const commits = new Map();
  for (const commit of value.commits) {
    assertExactFixtureKeys(commit, [
      "author",
      "committer",
      "message",
      "parent_shas",
      "sha",
      "tree_sha",
    ], "C65 fixture commit");
    assertFixtureOid(commit.tree_sha, "C65 fixture commit tree sha");
    assert.ok(Array.isArray(commit.parent_shas));
    assert.ok(commit.parent_shas.length <= 1);
    for (const parentSha of commit.parent_shas) {
      assertFixtureOid(parentSha, "C65 fixture commit parent sha");
      assert.notEqual(parentSha, commit.sha);
    }
    for (const [role, identity] of [
      ["author", commit.author],
      ["committer", commit.committer],
    ]) {
      assertExactFixtureKeys(
        identity,
        ["date", "email", "name"],
        `C65 fixture commit ${role}`,
      );
      assert.equal(identity.name, "Codex Review Gate");
      assert.equal(
        identity.email,
        "codex-review-gate@users.noreply.github.com",
      );
      assert.equal(typeof identity.date, "string");
      assert.equal(Number.isFinite(Date.parse(identity.date)), true);
      assert.equal(new Date(identity.date).toISOString(), identity.date);
    }
    assert.equal(typeof commit.message, "string");
    assert.ok(Buffer.byteLength(commit.message, "utf8") <= 1024);
    assert.equal(commit.message.includes("\0"), false);
    const commitBytes = Buffer.from([
      `tree ${commit.tree_sha}`,
      ...commit.parent_shas.map((parent) => `parent ${parent}`),
      `author ${commit.author.name} <${commit.author.email}> ` +
        gitTimestamp(commit.author.date),
      `committer ${commit.committer.name} <${commit.committer.email}> ` +
        gitTimestamp(commit.committer.date),
      "",
      commit.message,
    ].join("\n"), "utf8");
    assert.equal(gitObjectSha("commit", commitBytes), commit.sha);
    commits.set(commit.sha, structuredClone(commit));
  }

  const reachableCommits = [];
  const reachableCommitOids = new Set();
  let cursor = value.ref_target;
  while (cursor !== null) {
    assert.equal(reachableCommitOids.has(cursor), false, "C65 commit cycle");
    const commit = commits.get(cursor);
    assert.notEqual(commit, undefined, `missing C65 commit ${cursor}`);
    reachableCommitOids.add(cursor);
    reachableCommits.push(commit);
    cursor = commit.parent_shas.length === 0 ? null : commit.parent_shas[0];
  }
  assert.equal(reachableCommitOids.size, commits.size);

  const reachableTreeOids = new Set();
  const reachableBlobOids = new Set();
  const pendingTrees = reachableCommits.map((commit) => commit.tree_sha);
  while (pendingTrees.length > 0) {
    const treeSha = pendingTrees.pop();
    if (reachableTreeOids.has(treeSha)) continue;
    const entries = trees.get(treeSha);
    assert.notEqual(entries, undefined, `missing C65 tree ${treeSha}`);
    reachableTreeOids.add(treeSha);
    for (const entry of entries) {
      if (entry.type === "tree") {
        assert.equal(trees.has(entry.sha), true, `missing C65 tree ${entry.sha}`);
        pendingTrees.push(entry.sha);
      } else {
        assert.equal(blobs.has(entry.sha), true, `missing C65 blob ${entry.sha}`);
        reachableBlobOids.add(entry.sha);
      }
    }
  }
  assert.equal(reachableTreeOids.size, trees.size);
  assert.equal(reachableBlobOids.size, blobs.size);

  const historicalTokenAuthorities = [];
  const tokenOrdinals = new Set();
  const tokenDigests = new Set();
  const tokenJtis = new Set();
  let priorOrdinal = 0;
  const fixtureServerTime = new Date(Date.UTC(
    2026,
    7,
    13,
    12,
    0,
    value.clock_second,
  )).toISOString();
  const chronologicalCommits = [...reachableCommits].reverse();
  for (let sequence = 0; sequence < chronologicalCommits.length; sequence += 1) {
    const commit = chronologicalCommits[sequence];
    assert.ok(commit.author.date <= fixtureServerTime);
    assert.ok(commit.committer.date <= fixtureServerTime);
    const entries = trees.get(commit.tree_sha);
    const envelopeEntries = entries.filter((entry) =>
      entry.path === V2_GIT_LEDGER_BLOB_PATH && entry.type === "blob");
    assert.equal(envelopeEntries.length, 1);
    const envelopeBytes = blobs.get(envelopeEntries[0].sha);
    const envelopeText = envelopeBytes.toString("utf8");
    const envelope = JSON.parse(envelopeText);
    assert.equal(`${canonicalJson(envelope)}\n`, envelopeText);
    assert.equal(envelope.schema, V2_GIT_LEDGER_ENVELOPE_SCHEMA);
    assert.equal(envelope.sequence, sequence);
    assert.equal(
      envelope.predecessor_commit_sha,
      sequence === 0 ? null : chronologicalCommits[sequence - 1].sha,
    );
    assert.equal(commit.author.date, envelope.server_observed_at);
    assert.equal(commit.committer.date, envelope.server_observed_at);
    assert.equal(
      commit.message,
      `Codex Review Gate v2: ${envelope.record_type} #${sequence}`,
    );
    assert.ok(envelope.server_observed_at <= fixtureServerTime);

    const jwt = envelope.workflow_provenance_jwt;
    assert.equal(typeof jwt, "string");
    const jwtParts = jwt.split(".");
    assert.equal(jwtParts.length, 3);
    for (const part of jwtParts) {
      assert.equal(Buffer.from(part, "base64url").toString("base64url"), part);
    }
    const jwtHeader = JSON.parse(
      Buffer.from(jwtParts[0], "base64url").toString("utf8"),
    );
    assert.deepEqual(jwtHeader, {
      alg: "RS256",
      kid: "fixture-key-1",
      typ: "JWT",
    });
    const jwtPayload = JSON.parse(
      Buffer.from(jwtParts[1], "base64url").toString("utf8"),
    );
    assertExactFixtureKeys(
      jwtPayload,
      ["jti", "ordinal", "request_digest"],
      "C65 fixture provenance JWT payload",
    );
    assert.ok(Number.isSafeInteger(jwtPayload.ordinal));
    assert.ok(jwtPayload.ordinal > priorOrdinal);
    priorOrdinal = jwtPayload.ordinal;
    assert.equal(fixtureJwt(jwtPayload), jwt);
    assert.equal(tokenOrdinals.has(jwtPayload.ordinal), false);
    tokenOrdinals.add(jwtPayload.ordinal);
    assert.equal(tokenJtis.has(jwtPayload.jti), false);
    tokenJtis.add(jwtPayload.jti);

    const receipt = envelope.workflow_provenance;
    assert.equal(rawDigest(jwt), receipt.token_sha256);
    assert.equal(
      jwtPayload.request_digest,
      receipt.operation_binding.request_digest,
    );
    assert.equal(jwtPayload.jti, receipt.claims.jti);
    assert.equal(tokenDigests.has(receipt.token_sha256), false);
    tokenDigests.add(receipt.token_sha256);
    historicalTokenAuthorities.push(Object.freeze({
      commit_oid: commit.sha,
      ordinal: jwtPayload.ordinal,
      token_sha256: receipt.token_sha256,
      jti: jwtPayload.jti,
      request_digest: jwtPayload.request_digest,
      run_identity: Object.freeze({
        run_id: receipt.claims.run_id,
        run_attempt: Number(receipt.claims.run_attempt),
        actor_id: OWNER.actor_id,
      }),
      trigger: Object.freeze({
        event_name: receipt.claims.event_name,
        ref: receipt.claims.ref,
        sha: receipt.claims.sha,
      }),
    }));
  }
  assert.equal(historicalTokenAuthorities.length, commits.size);
  assert.equal(priorOrdinal, 718);

  return Object.freeze({
    git: Object.freeze({
      ref_target: value.ref_target,
      clock_second: value.clock_second,
      server_time_frozen: value.server_time_frozen,
      blobs: Object.freeze([...blobs].map(([sha, bytes]) =>
        Object.freeze({ sha, bytes: Buffer.from(bytes) }))),
      trees: Object.freeze([...trees].map(([sha, entries]) =>
        Object.freeze({ sha, entries: structuredClone(entries) }))),
      commits: Object.freeze([...commits].map(([sha, commit]) =>
        Object.freeze({ sha, ...structuredClone(commit) }))),
    }),
    historical_token_authorities: Object.freeze(historicalTokenAuthorities),
  });
}

function githubGitFixture(seed = null) {
  const calls = [];
  const blobs = new Map((seed?.blobs ?? []).map(({ sha, bytes }) => [sha, {
    sha,
    content: Buffer.from(bytes).toString("utf8"),
    bytes: Buffer.from(bytes),
  }]));
  const trees = new Map((seed?.trees ?? []).map(({ sha, entries }) => [sha, {
    sha,
    tree: structuredClone(entries),
  }]));
  const commits = new Map((seed?.commits ?? []).map((commit) => [commit.sha, {
    sha: commit.sha,
    message: commit.message,
    tree: { sha: commit.tree_sha },
    parents: commit.parent_shas.map((sha) => ({ sha })),
    author: structuredClone(commit.author),
    committer: structuredClone(commit.committer),
  }]));
  let refTarget = seed?.ref_target ?? null;
  let second = seed?.clock_second ?? 0;
  let mismatchReread = false;
  let refRereadOverride = null;
  let failAttestationUpdate = false;
  let failEffectUpdateKind = null;
  let failRereadAfterEffectUpdate = null;
  let failRefReadOnce = false;
  let failCandidateDispatchPhase = null;
  let failCandidateDispatchPhaseAfterApply = null;
  let failCandidateInventoryCommitPhase = null;
  let failCandidateInventoryUpdate = false;
  let failCandidateInventoryUpdateAfterApply = false;
  let afterCandidateInventoryUpdateApplied = null;
  let moveRefOnCommitRead = false;
  let serverTimeFrozen = seed?.server_time_frozen ?? false;
  let refReadServerTimeOverride = null;

  const fixture = {
    calls,
    blobs,
    trees,
    commits,
    get refTarget() { return refTarget; },
    peekServerTime() {
      return new Date(Date.UTC(2026, 7, 13, 12, 0, second)).toISOString();
    },
    get writeCalls() {
      return calls.filter((call) => new Set(["POST", "PATCH"]).has(call.method)).length;
    },
    snapshotHistoricalState() {
      return {
        ref_target: refTarget,
        blob_keys: new Set(blobs.keys()),
        tree_keys: new Set(trees.keys()),
        commit_keys: new Set(commits.keys()),
      };
    },
    restoreHistoricalState(snapshot) {
      for (const key of blobs.keys()) {
        if (!snapshot.blob_keys.has(key)) blobs.delete(key);
      }
      for (const key of trees.keys()) {
        if (!snapshot.tree_keys.has(key)) trees.delete(key);
      }
      for (const key of commits.keys()) {
        if (!snapshot.commit_keys.has(key)) commits.delete(key);
      }
      refTarget = snapshot.ref_target;
    },
    materializeHistoricalEnvelope(envelope) {
      assert.equal(envelope.predecessor_commit_sha, refTarget);
      const envelopeBytes = Buffer.from(`${canonicalJson(envelope)}\n`, "utf8");
      const blobSha = gitObjectSha("blob", envelopeBytes);
      blobs.set(blobSha, {
        sha: blobSha,
        content: envelopeBytes.toString("utf8"),
        bytes: envelopeBytes,
      });
      const treeEntries = [{
        path: V2_GIT_LEDGER_BLOB_PATH,
        mode: "100644",
        type: "blob",
        sha: blobSha,
      }];
      const treeSha = canonicalGitTreeSha(treeEntries);
      trees.set(treeSha, { sha: treeSha, tree: treeEntries });
      const message =
        `Codex Review Gate v2: ${envelope.record_type} #${envelope.sequence}`;
      const commitBytes = Buffer.from([
        `tree ${treeSha}`,
        `parent ${refTarget}`,
        `author Codex Review Gate <codex-review-gate@users.noreply.github.com> ${gitTimestamp(envelope.server_observed_at)}`,
        `committer Codex Review Gate <codex-review-gate@users.noreply.github.com> ${gitTimestamp(envelope.server_observed_at)}`,
        "",
        message,
      ].join("\n"), "utf8");
      const commitSha = gitObjectSha("commit", commitBytes);
      commits.set(commitSha, {
        sha: commitSha,
        message,
        tree: { sha: treeSha },
        parents: [{ sha: refTarget }],
        author: {
          name: "Codex Review Gate",
          email: "codex-review-gate@users.noreply.github.com",
          date: envelope.server_observed_at,
        },
        committer: {
          name: "Codex Review Gate",
          email: "codex-review-gate@users.noreply.github.com",
          date: envelope.server_observed_at,
        },
      });
      refTarget = commitSha;
      return { blob_sha: blobSha, tree_sha: treeSha, commit_sha: commitSha };
    },
    mismatchNextRefReread() { mismatchReread = true; },
    failNextAttestationUpdate() { failAttestationUpdate = true; },
    failNextEffectUpdate(kind) { failEffectUpdateKind = kind; },
    failRereadAfterEffectUpdate(kind, occurrence = 1) {
      failRereadAfterEffectUpdate = { kind, remaining: occurrence };
    },
    failNextCandidateDispatchUpdate(phase) {
      failCandidateDispatchPhase = phase;
    },
    failNextCandidateDispatchUpdateAfterApply(phase) {
      failCandidateDispatchPhaseAfterApply = phase;
    },
    failNextCandidateInventoryCommit(phase) {
      failCandidateInventoryCommitPhase = phase;
    },
    failNextCandidateInventoryUpdate() {
      failCandidateInventoryUpdate = true;
    },
    failNextCandidateInventoryUpdateAfterApply() {
      failCandidateInventoryUpdateAfterApply = true;
    },
    afterNextCandidateInventoryUpdateApplied(callback) {
      if (typeof callback !== "function") {
        throw new TypeError("candidate inventory update hook must be callable");
      }
      afterCandidateInventoryUpdateApplied = callback;
    },
    advanceServerTime(seconds) { second += seconds; },
    freezeServerTime() { serverTimeFrozen = true; },
    resumeServerTime() { serverTimeFrozen = false; },
    overrideRefReadServerTimeAfter(occurrence, serverTime) {
      assert.ok(Number.isSafeInteger(occurrence) && occurrence > 0);
      assert.equal(new Date(serverTime).toISOString(), serverTime);
      refReadServerTimeOverride = { occurrence, server_time: serverTime };
    },
    overrideNextRefReadTarget(targetCommitSha) {
      assert.match(targetCommitSha, /^[0-9a-f]{40}$/u);
      assert.equal(commits.has(targetCommitSha), true);
      assert.equal(refRereadOverride, null);
      refRereadOverride = targetCommitSha;
    },
    moveRefToParentOnNextCommitRead() { moveRefOnCommitRead = true; },
    tipRecordType() { return recordType(refTarget); },
    rewriteTipText(mutator) {
      const commit = commits.get(refTarget);
      const tree = trees.get(commit.tree.sha);
      const envelopeEntry = tree.tree.find((entry) =>
        entry.path === V2_GIT_LEDGER_BLOB_PATH && entry.type === "blob");
      const blob = blobs.get(envelopeEntry.sha);
      const content = mutator(blob.content);
      const bytes = Buffer.from(content, "utf8");
      const rewrittenSha = gitObjectSha("blob", bytes);
      blobs.set(rewrittenSha, { sha: rewrittenSha, content, bytes });
      const rewrittenEntries = tree.tree.map((entry) =>
        entry.path === V2_GIT_LEDGER_BLOB_PATH
          ? { ...entry, sha: rewrittenSha }
          : structuredClone(entry));
      const rewrittenTreeSha = canonicalGitTreeSha(rewrittenEntries);
      trees.set(rewrittenTreeSha, {
        sha: rewrittenTreeSha,
        tree: rewrittenEntries,
      });
      const commitBytes = Buffer.from([
        `tree ${rewrittenTreeSha}`,
        ...commit.parents.map((parent) => `parent ${parent.sha}`),
        `author ${commit.author.name} <${commit.author.email}> ${gitTimestamp(commit.author.date)}`,
        `committer ${commit.committer.name} <${commit.committer.email}> ${gitTimestamp(commit.committer.date)}`,
        "",
        commit.message,
      ].join("\n"), "utf8");
      const rewrittenCommitSha = gitObjectSha("commit", commitBytes);
      commits.set(rewrittenCommitSha, {
        ...structuredClone(commit),
        sha: rewrittenCommitSha,
        tree: { sha: rewrittenTreeSha },
      });
      refTarget = rewrittenCommitSha;
    },
    rewriteTipEnvelope(mutator) {
      fixture.rewriteTipText((text) => {
        const envelope = JSON.parse(text.trim());
        mutator(envelope);
        return `${canonicalJson(envelope)}\n`;
      });
    },
    async fetch(url, init) {
      const parsed = new URL(url);
      const path = parsed.pathname.replace("/repos/owner/repo", "");
      calls.push({ method: init.method, path, body: init.body ?? null });
      if (!serverTimeFrozen) second += 1;
      const date = new Date(Date.UTC(2026, 7, 13, 12, 0, second)).toUTCString();

      if (init.method === "POST" && path === "/git/blobs") {
        const body = JSON.parse(init.body);
        assert.ok(new Set(["utf-8", "base64"]).has(body.encoding));
        const bytes = body.encoding === "base64"
          ? Buffer.from(body.content, "base64")
          : Buffer.from(body.content, "utf8");
        const sha = gitObjectSha("blob", bytes);
        blobs.set(sha, { sha, content: bytes.toString("utf8"), bytes });
        return response(201, { sha, url: `https://api.github.test/blob/${sha}` }, date);
      }
      if (init.method === "POST" && path === "/git/trees") {
        const body = JSON.parse(init.body);
        const sha = canonicalGitTreeSha(body.tree);
        trees.set(sha, { sha, tree: structuredClone(body.tree) });
        return response(201, { sha, tree: body.tree }, date);
      }
      if (init.method === "POST" && path === "/git/commits") {
        const body = JSON.parse(init.body);
        const stagedTree = trees.get(body.tree);
        const stagedEnvelopeEntry = stagedTree?.tree.find((entry) =>
          entry.path === V2_GIT_LEDGER_BLOB_PATH && entry.type === "blob");
        const stagedBlob = stagedTree === undefined
          ? undefined
          : blobs.get(stagedEnvelopeEntry?.sha);
        const stagedEnvelope = stagedBlob === undefined
          ? null
          : JSON.parse(stagedBlob.content);
        if (
          failCandidateInventoryCommitPhase !== null &&
          stagedEnvelope?.record_type ===
            "candidate-inventory-observation" &&
          stagedEnvelope.payload.phase === failCandidateInventoryCommitPhase
        ) {
          failCandidateInventoryCommitPhase = null;
          return response(500, {
            message: "Internal Server Error",
            documentation_url: "https://docs.github.com/rest",
            status: "500",
          }, date);
        }
        const commitBytes = Buffer.from([
          `tree ${body.tree}`,
          ...body.parents.map((parent) => `parent ${parent}`),
          `author ${body.author.name} <${body.author.email}> ${gitTimestamp(body.author.date)}`,
          `committer ${body.committer.name} <${body.committer.email}> ${gitTimestamp(body.committer.date)}`,
          "",
          body.message,
        ].join("\n"), "utf8");
        const sha = gitObjectSha("commit", commitBytes);
        const commit = {
          sha,
          message: body.message,
          tree: { sha: body.tree },
          parents: body.parents.map((parent) => ({ sha: parent })),
          author: structuredClone(body.author),
          committer: structuredClone(body.committer),
        };
        commits.set(sha, commit);
        return response(201, commit, date);
      }
      if (init.method === "POST" && path === "/git/refs") {
        const body = JSON.parse(init.body);
        assert.equal(refTarget, null);
        refTarget = body.sha;
        return response(201, refBody(refTarget), date);
      }
      if (init.method === "PATCH" && path === "/git/refs/heads/codex-review-gate-ledger-v2") {
        const body = JSON.parse(init.body);
        assert.equal(body.force, false);
        const candidate = commits.get(body.sha);
        if (failAttestationUpdate && recordType(body.sha) === "capability-attestation") {
          failAttestationUpdate = false;
          return response(500, {
            message: "Internal Server Error",
            documentation_url: "https://docs.github.com/rest",
            status: "500",
          }, date);
        }
        if (
          failEffectUpdateKind !== null &&
          recordEnvelope(body.sha).kind === failEffectUpdateKind
        ) {
          failEffectUpdateKind = null;
          return response(500, {
            message: "Internal Server Error",
            documentation_url: "https://docs.github.com/rest",
            status: "500",
          }, date);
        }
        if (
          failCandidateDispatchPhase !== null &&
          recordType(body.sha) === "candidate-dispatch-observation" &&
          recordEnvelope(body.sha).payload.phase ===
            failCandidateDispatchPhase
        ) {
          failCandidateDispatchPhase = null;
          return response(500, {
            message: "Internal Server Error",
            documentation_url: "https://docs.github.com/rest",
            status: "500",
          }, date);
        }
        const candidateInventoryUpdate =
          recordType(body.sha) === "candidate-inventory-observation" ||
          candidate?.parents.some(({ sha: parentSha }) =>
            recordType(parentSha) === "candidate-inventory-observation");
        if (failCandidateInventoryUpdate && candidateInventoryUpdate) {
          failCandidateInventoryUpdate = false;
          return response(500, {
            message: "Internal Server Error",
            documentation_url: "https://docs.github.com/rest",
            status: "500",
          }, date);
        }
        const fastForward = candidate !== undefined &&
          isCommitDescendantOf(body.sha, refTarget);
        if (!fastForward) {
          return response(422, {
            message: "Update is not a fast forward",
            documentation_url: "https://docs.github.com/rest/git/refs#update-a-reference",
            status: "422",
          }, date);
        }
        const old = refTarget;
        refTarget = body.sha;
        const successfulUpdateResponse = response(
          200,
          refBody(refTarget),
          date,
        );
        if (
          failCandidateDispatchPhaseAfterApply !== null &&
          recordType(body.sha) === "candidate-dispatch-observation" &&
          recordEnvelope(body.sha).payload.phase ===
            failCandidateDispatchPhaseAfterApply
        ) {
          failCandidateDispatchPhaseAfterApply = null;
          return response(500, {
            message: "Internal Server Error",
            documentation_url: "https://docs.github.com/rest",
            status: "500",
          }, date);
        }
        if (
          afterCandidateInventoryUpdateApplied !== null &&
          candidateInventoryUpdate
        ) {
          const callback = afterCandidateInventoryUpdateApplied;
          afterCandidateInventoryUpdateApplied = null;
          await callback();
        }
        if (
          failCandidateInventoryUpdateAfterApply &&
          candidateInventoryUpdate
        ) {
          failCandidateInventoryUpdateAfterApply = false;
          return response(500, {
            message: "Internal Server Error",
            documentation_url: "https://docs.github.com/rest",
            status: "500",
          }, date);
        }
        if (
          failRereadAfterEffectUpdate !== null &&
          recordEnvelope(body.sha).kind === failRereadAfterEffectUpdate.kind
        ) {
          failRereadAfterEffectUpdate.remaining -= 1;
          if (failRereadAfterEffectUpdate.remaining === 0) {
            failRereadAfterEffectUpdate = null;
            failRefReadOnce = true;
          }
        }
        if (mismatchReread) {
          refRereadOverride = old;
          mismatchReread = false;
        }
        return successfulUpdateResponse;
      }
      if (init.method === "GET" && path === "/git/ref/heads/codex-review-gate-ledger-v2") {
        let refDate = date;
        if (refReadServerTimeOverride !== null) {
          refReadServerTimeOverride.occurrence -= 1;
          if (refReadServerTimeOverride.occurrence === 0) {
            refDate = new Date(refReadServerTimeOverride.server_time)
              .toUTCString();
            refReadServerTimeOverride = null;
          }
        }
        if (failRefReadOnce) {
          failRefReadOnce = false;
          return response(500, {
            message: "Internal Server Error",
            documentation_url: "https://docs.github.com/rest",
            status: "500",
          }, refDate);
        }
        if (refTarget === null) {
          return response(404, {
            message: "Not Found",
            documentation_url: "https://docs.github.com/rest/git/refs#get-a-reference",
            status: "404",
          }, refDate);
        }
        const target = refRereadOverride ?? refTarget;
        refRereadOverride = null;
        return response(200, refBody(target), refDate);
      }
      const commitMatch = path.match(/^\/git\/commits\/([0-9a-f]{40})$/u);
      if (init.method === "GET" && commitMatch !== null) {
        const commit = commits.get(commitMatch[1]);
        if (moveRefOnCommitRead) {
          moveRefOnCommitRead = false;
          refTarget = commit.parents[0].sha;
        }
        return response(200, commit, date);
      }
      const treeMatch = path.match(/^\/git\/trees\/([0-9a-f]{40})$/u);
      if (init.method === "GET" && treeMatch !== null) {
        return response(200, trees.get(treeMatch[1]), date);
      }
      const blobMatch = path.match(/^\/git\/blobs\/([0-9a-f]{40})$/u);
      if (init.method === "GET" && blobMatch !== null) {
        const blob = blobs.get(blobMatch[1]);
        const bytes = Buffer.isBuffer(blob.bytes)
          ? blob.bytes
          : Buffer.from(blob.content, "utf8");
        return response(200, {
          sha: blob.sha,
          encoding: "base64",
          content: bytes.toString("base64"),
        }, date);
      }
      throw new Error(`unexpected fixture request ${init.method} ${path}`);
    },
  };
  return fixture;

  function refBody(sha) {
    return {
      ref: V2_GIT_LEDGER_REF,
      node_id: "REF_ledger",
      object: { type: "commit", sha },
    };
  }

  function recordType(commitSha) {
    return recordEnvelope(commitSha).record_type;
  }

  function recordEnvelope(commitSha) {
    const commit = commits.get(commitSha);
    const tree = trees.get(commit.tree.sha);
    const envelopeEntry = tree.tree.find((entry) =>
      entry.path === V2_GIT_LEDGER_BLOB_PATH && entry.type === "blob");
    const blob = blobs.get(envelopeEntry.sha);
    return JSON.parse(blob.content);
  }

  function isCommitDescendantOf(candidateSha, ancestorSha) {
    const visited = new Set();
    let cursor = candidateSha;
    while (cursor !== ancestorSha) {
      if (visited.has(cursor)) return false;
      visited.add(cursor);
      const commit = commits.get(cursor);
      if (commit?.parents.length !== 1) return false;
      cursor = commit.parents[0].sha;
    }
    return true;
  }
}

function resealEnvelope(envelope) {
  delete envelope.envelope_digest;
  envelope.envelope_digest = digestCanonical(
    "codex-review-gate-v2-git-ledger-envelope",
    envelope,
  );
}

function resealProvenance(provenance) {
  const binding = provenance.operation_binding;
  const {
    nonce: _nonce,
    audience: _audience,
    request_digest: _requestDigest,
    ...bindingInput
  } = binding;
  const nonce = digestCanonical(
    "codex-review-gate-v2-git-ledger-provenance-nonce",
    {
      schema: "codex-review-gate-git-ledger-provenance-request-v2",
      schema_version: 1,
      ...bindingInput,
    },
  );
  binding.nonce = nonce;
  binding.audience =
    `${V2_GIT_LEDGER_OIDC_AUDIENCE}:${nonce.slice("sha256:".length)}`;
  const { request_digest: _bindingDigest, ...requestWithoutDigest } = binding;
  binding.request_digest = digestCanonical(
    "codex-review-gate-v2-git-ledger-provenance-request",
    {
      schema: "codex-review-gate-git-ledger-provenance-request-v2",
      schema_version: 1,
      ...requestWithoutDigest,
    },
  );
  provenance.audience = binding.audience;
  provenance.claims.aud = binding.audience;
  delete provenance.receipt_digest;
  provenance.receipt_digest = digestCanonical(
    "codex-review-gate-v2-git-ledger-provenance",
    provenance,
  );
}

async function resealBootstrapCheckpointSourceProvenanceForFixture({
  fixture,
  prepared,
  mutateReceipt,
}) {
  const checkpointCommit = fixture.commits.get(
    prepared.checkpoint_commit_sha,
  );
  const rootTree = fixture.trees.get(checkpointCommit.tree.sha);
  const carrierEntry = rootTree.tree.find((entry) =>
    entry.path === V2_GIT_LEDGER_BLOB_PATH);
  const stateTreeEntry = rootTree.tree.find((entry) =>
    entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_PATH);
  const stateTree = fixture.trees.get(stateTreeEntry.sha);
  const stateBlobEntry = stateTree.tree.find((entry) =>
    entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_BLOB_PATH);
  const state = JSON.parse(fixture.blobs.get(stateBlobEntry.sha).content);
  const queryRecord = state.source.query_records[0];
  const provenance = queryRecord.envelope.workflow_provenance;
  mutateReceipt(provenance);
  delete provenance.receipt_digest;
  provenance.receipt_digest = digestCanonical(
    "codex-review-gate-v2-git-ledger-provenance",
    provenance,
  );
  delete queryRecord.envelope.public_envelope_digest;
  queryRecord.envelope.public_envelope_digest = digestCanonical(
    "codex-review-gate-v2-git-ledger-public-envelope",
    queryRecord.envelope,
  );

  const evidence = state.source.provenance_reverification[0];
  evidence.stored_receipt_digest = provenance.receipt_digest;
  evidence.token_sha256 = provenance.token_sha256;
  evidence.key_id = provenance.key_id;
  evidence.verified_at_server_time = provenance.verified_at_server_time;
  const liveReceipt = {
    ...structuredClone(provenance),
    discovery: structuredClone(evidence.discovery),
    jwks: structuredClone(evidence.jwks),
  };
  delete liveReceipt.receipt_digest;
  evidence.live_receipt_digest = digestCanonical(
    "codex-review-gate-v2-git-ledger-provenance",
    liveReceipt,
  );
  delete evidence.evidence_digest;
  evidence.evidence_digest = digestCanonical(
    "codex-review-gate-v2-git-ledger-provenance-reverification",
    evidence,
  );

  const queryManifest = state.source.query_records.map((entry) => ({
    record_oid: entry.commit_sha,
    parent_oid: entry.parents[0] ?? null,
    tree_oid: entry.tree_sha,
    envelope_digest: entry.envelope.envelope_digest,
    record_type: entry.envelope.record_type,
    kind: entry.envelope.kind,
    effect_id: entry.envelope.effect_id,
    idempotency_key: entry.envelope.idempotency_key,
    payload_digest: entry.envelope.payload_digest,
    workflow_provenance_receipt_digest:
      entry.envelope.workflow_provenance.receipt_digest,
    workflow_provenance_jti: fixtureProvenanceReplayIdentity(
      entry.envelope.workflow_provenance,
    ),
    server_observed_at: entry.envelope.server_observed_at,
  }));
  state.source.record_manifest_digest = digestCanonical(
    "codex-review-gate-v2-fully-reachable-record-manifest",
    queryManifest,
  );
  let rollingAuditRoot = digestCanonical(
    "codex-review-gate-v2-checkpoint-audit-root-empty",
    { record_count: 0 },
  );
  for (const [ordinal, entry] of
    state.source.query_records.entries()) {
    rollingAuditRoot = digestCanonical(
      "codex-review-gate-v2-checkpoint-audit-root-step",
      {
        prior_root: rollingAuditRoot,
        ordinal,
        record_oid: entry.commit_sha,
        parent_oid: entry.parents[0] ?? null,
        envelope_digest: entry.envelope.envelope_digest,
        record_type: entry.envelope.record_type,
        server_observed_at: entry.envelope.server_observed_at,
      },
    );
  }
  state.source.rolling_audit_root = rollingAuditRoot;
  state.source.provenance_reverification_digest = digestCanonical(
    "codex-review-gate-v2-git-ledger-provenance-reverification-inventory",
    state.source.provenance_reverification,
  );

  const frontierBytes = new Map();
  const frontier = createV2PersistentMerkleMap({
    domain: "git-ledger-checkpoint-bootstrap-prefix-v1",
    epoch_id: state.epoch_id,
    readBlob: async (objectOid) => {
      const bytes = frontierBytes.get(objectOid);
      return bytes === undefined ? null : new Uint8Array(bytes);
    },
    writeBlob: async (bytes, { expected_object_oid: objectOid }) => {
      frontierBytes.set(objectOid, Buffer.from(bytes));
      return objectOid;
    },
  });
  const updates = [];
  for (const [ordinal, entry] of state.source.query_records.entries()) {
    const receipt = entry.envelope.workflow_provenance;
    const fact = {
      ordinal,
      record_oid: entry.commit_sha,
      parent_oid: entry.parents[0] ?? null,
      tree_oid: entry.tree_sha,
      blob_oid: entry.blob_sha,
      record_type: entry.envelope.record_type,
      sequence: entry.envelope.sequence,
      envelope_digest: entry.envelope.envelope_digest,
      payload_digest: entry.envelope.payload_digest,
      server_observed_at: entry.envelope.server_observed_at,
      provenance_identity: fixtureProvenanceReplayIdentity(receipt),
      provenance_receipt_digest: receipt.receipt_digest,
      provenance_token_sha256: receipt.token_sha256,
    };
    updates.push(
      { key: { kind: "ordinal", ordinal }, value: fact },
      {
        key: { kind: "record-oid", record_oid: entry.commit_sha },
        value: fact,
      },
      {
        key: {
          kind: "provenance-identity",
          identity: fact.provenance_identity,
        },
        value: {
          ordinal,
          record_oid: entry.commit_sha,
          provenance_receipt_digest: fact.provenance_receipt_digest,
          provenance_token_sha256: fact.provenance_token_sha256,
        },
      },
    );
  }
  const applied = await frontier.apply({
    root: frontier.emptyRoot(),
    updates,
    mode: "insert-only",
  });
  const frontierTreeEntries = applied.created_objects.map((descriptor) => {
    const bytes = frontierBytes.get(descriptor.object_oid);
    fixture.blobs.set(descriptor.object_oid, {
      sha: descriptor.object_oid,
      content: bytes.toString("utf8"),
      bytes,
    });
    return {
      path: `n-${descriptor.object_oid}.json`,
      mode: "100644",
      type: "blob",
      sha: descriptor.object_oid,
    };
  }).sort((left, right) => left.path.localeCompare(right.path));
  const frontierTreeSha = canonicalGitTreeSha(frontierTreeEntries);
  fixture.trees.set(frontierTreeSha, {
    sha: frontierTreeSha,
    tree: frontierTreeEntries,
  });
  state.prefix_frontier = {
    domain: "git-ledger-checkpoint-bootstrap-prefix-v1",
    root: applied.root,
    tree_sha: frontierTreeSha,
    tree_entry_count: frontierTreeEntries.length,
    node_count: applied.created_objects.length,
    raw_node_bytes: [...frontierBytes.values()].reduce(
      (total, bytes) => total + bytes.byteLength,
      0,
    ),
    oid_set_digest: digestCanonical(
      "codex-review-gate-v2-checkpoint-frontier-oids",
      frontierTreeEntries.map((entry) => entry.sha).sort(),
    ),
  };
  delete state.checkpoint_state_digest;
  state.checkpoint_state_digest = digestCanonical(
    "codex-review-gate-v2-checkpoint-state",
    state,
  );

  const stateContent = `${canonicalJson(state)}\n`;
  const stateBytes = Buffer.from(stateContent, "utf8");
  const stateBlobSha = gitObjectSha("blob", stateBytes);
  fixture.blobs.set(stateBlobSha, {
    sha: stateBlobSha,
    content: stateContent,
    bytes: stateBytes,
  });
  const nextStateTreeEntries = stateTree.tree.map((entry) => {
    if (entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_BLOB_PATH) {
      return { ...entry, sha: stateBlobSha };
    }
    if (entry.path === V2_GIT_LEDGER_CHECKPOINT_FRONTIER_NODES_PATH) {
      return { ...entry, sha: frontierTreeSha };
    }
    return structuredClone(entry);
  });
  const stateTreeSha = canonicalGitTreeSha(nextStateTreeEntries);
  fixture.trees.set(stateTreeSha, {
    sha: stateTreeSha,
    tree: nextStateTreeEntries,
  });

  const carrier = JSON.parse(fixture.blobs.get(carrierEntry.sha).content);
  carrier.payload.checkpoint_state_tree_sha = stateTreeSha;
  carrier.payload.checkpoint_state_digest = state.checkpoint_state_digest;
  carrier.payload.prefix_frontier_tree_sha = frontierTreeSha;
  delete carrier.payload.payload_digest;
  carrier.payload.payload_digest = digestCanonical(
    "codex-review-gate-v2-checkpoint-carrier-payload",
    carrier.payload,
  );
  carrier.payload_digest = carrier.payload.payload_digest;
  carrier.workflow_provenance.operation_binding.record_identity
    .payload_digest = carrier.payload_digest;
  resealProvenance(carrier.workflow_provenance);
  delete carrier.carrier_digest;
  carrier.carrier_digest = digestCanonical(
    "codex-review-gate-v2-checkpoint-carrier",
    carrier,
  );
  const carrierContent = `${canonicalJson(carrier)}\n`;
  const carrierBytes = Buffer.from(carrierContent, "utf8");
  const carrierBlobSha = gitObjectSha("blob", carrierBytes);
  fixture.blobs.set(carrierBlobSha, {
    sha: carrierBlobSha,
    content: carrierContent,
    bytes: carrierBytes,
  });
  const nextRootEntries = rootTree.tree.map((entry) => {
    if (entry.path === V2_GIT_LEDGER_BLOB_PATH) {
      return { ...entry, sha: carrierBlobSha };
    }
    if (entry.path === V2_GIT_LEDGER_CHECKPOINT_STATE_PATH) {
      return { ...entry, sha: stateTreeSha };
    }
    return structuredClone(entry);
  });
  const rootTreeSha = canonicalGitTreeSha(nextRootEntries);
  fixture.trees.set(rootTreeSha, {
    sha: rootTreeSha,
    tree: nextRootEntries,
  });
  let predecessor = storeRewrittenFixtureCommit({
    fixture,
    original: checkpointCommit,
    treeSha: rootTreeSha,
    parents: checkpointCommit.parents.map((parent) => parent.sha),
  });
  const checkpointCommitSha = predecessor;

  const suffix = [];
  let cursor = prepared.transaction_tip_commit_sha;
  for (let index = 0; index < prepared.semantic_record_count; index += 1) {
    const commit = fixture.commits.get(cursor);
    suffix.unshift(commit);
    cursor = commit.parents[0].sha;
  }
  const candidateState = {
    source_records: [],
    atomic: null,
    completed: null,
  };
  for (const originalCommit of suffix) {
    const originalTree = fixture.trees.get(originalCommit.tree.sha);
    const envelopeEntry = originalTree.tree.find((entry) =>
      entry.path === V2_GIT_LEDGER_BLOB_PATH);
    const envelope = JSON.parse(
      fixture.blobs.get(envelopeEntry.sha).content,
    );
    envelope.predecessor_commit_sha = predecessor;
    envelope.workflow_provenance.operation_binding
      .predecessor_commit_sha = predecessor;
    if (envelope.record_type === "candidate-inventory-observation") {
      envelope.payload.prior_candidate_authority_digest =
        fixtureCandidateInventoryAuthorityDigest(candidateState);
      if (envelope.payload.phase === "attempt") {
        const priorAttempt = candidateState.atomic.attempts.at(-1) ?? null;
        envelope.payload.prior_attempt_record_oid =
          priorAttempt?.record_oid ?? null;
      } else if (envelope.payload.phase === "cycle-complete") {
        envelope.payload.attempt_records = candidateState.atomic.attempts.map(
          (attempt) => structuredClone(attempt),
        );
      }
    } else if (
      envelope.record_type === "candidate-dispatch-observation"
    ) {
      const candidateAuthorityDigest =
        fixtureCandidateInventoryAuthorityDigest(candidateState);
      const completedRecordOid = candidateState.completed.complete_record_oid;
      const generationId = `candidate-dispatch:${digestCanonical(
        "codex-review-gate-v2-candidate-dispatch-generation",
        {
          repository: {
            owner: REPOSITORY.owner,
            name: REPOSITORY.name,
            id: REPOSITORY.id,
            node_id: REPOSITORY.node_id,
          },
          cycle_id: envelope.payload.cycle_id,
          completed_cycle_record_oid: completedRecordOid,
          inventory_digest: envelope.payload.inventory_digest,
        },
      ).slice("sha256:".length)}`;
      envelope.payload.generation_id = generationId;
      envelope.payload.candidate_inventory_authority_digest =
        candidateAuthorityDigest;
      envelope.payload.completed_cycle_record_oid = completedRecordOid;
      const reservation = envelope.payload.reservation;
      reservation.generation_id = generationId;
      reservation.candidate_inventory_authority_digest =
        candidateAuthorityDigest;
      reservation.completed_cycle_record_oid = completedRecordOid;
      reservation.source_tip_commit_sha = predecessor;
      reservation.dispatch_digest = digestCanonical(
        "codex-review-gate-v2-candidate-dispatch-selection",
        {
          repository: reservation.repository,
          generation_id: reservation.generation_id,
          cycle_id: reservation.cycle_id,
          candidate_inventory_authority_digest:
            reservation.candidate_inventory_authority_digest,
          inventory_digest: reservation.inventory_digest,
          batch_index: reservation.batch_index,
          batch_count: reservation.batch_count,
          candidate_offset: reservation.candidate_offset,
          candidates: reservation.candidates,
        },
      );
      delete reservation.reservation_digest;
      reservation.reservation_digest = digestCanonical(
        "codex-review-gate-v2-candidate-dispatch-reservation",
        reservation,
      );
    }
    const operationBinding =
      envelope.workflow_provenance.operation_binding;
    const evaluatedScope = operationBinding.evaluated_scope_receipt;
    const evaluatedScopeInput = {
      repository: evaluatedScope.repository,
      payload: envelope.payload,
      trigger_identity: {
        event_name: evaluatedScope.trigger_event_name,
        ref: evaluatedScope.trigger_ref,
        sha: evaluatedScope.trigger_sha,
      },
      repository_endpoint_receipt:
        evaluatedScope.scope_endpoint_receipt,
    };
    operationBinding.evaluated_scope_receipt =
      envelope.record_type === "candidate-inventory-observation"
        ? createV2GitLedgerCandidateInventoryEvaluatedScopeReceipt(
          evaluatedScopeInput,
        )
        : createV2GitLedgerCandidateDispatchEvaluatedScopeReceipt(
          evaluatedScopeInput,
        );
    envelope.payload_digest = digestV2GitLedgerPayload(envelope.payload);
    operationBinding.record_identity
      .payload_digest = envelope.payload_digest;
    resealProvenance(envelope.workflow_provenance);
    resealEnvelope(envelope);
    const envelopeContent = `${canonicalJson(envelope)}\n`;
    const envelopeBytes = Buffer.from(envelopeContent, "utf8");
    const envelopeBlobSha = gitObjectSha("blob", envelopeBytes);
    fixture.blobs.set(envelopeBlobSha, {
      sha: envelopeBlobSha,
      content: envelopeContent,
      bytes: envelopeBytes,
    });
    const nextTreeEntries = originalTree.tree.map((entry) =>
      entry.path === V2_GIT_LEDGER_BLOB_PATH
        ? { ...entry, sha: envelopeBlobSha }
        : structuredClone(entry));
    const treeSha = canonicalGitTreeSha(nextTreeEntries);
    fixture.trees.set(treeSha, { sha: treeSha, tree: nextTreeEntries });
    predecessor = storeRewrittenFixtureCommit({
      fixture,
      original: originalCommit,
      treeSha,
      parents: [predecessor],
    });
    if (envelope.record_type === "candidate-inventory-observation") {
      const row = fixtureCandidateInventorySourceRecord(
        predecessor,
        fixture.commits.get(predecessor).parents[0].sha,
        envelope,
      );
      candidateState.source_records.push(row);
      if (envelope.payload.phase === "cycle-start") {
        candidateState.atomic = {
          refresh_id: envelope.payload.refresh_id,
          start_record_oid: predecessor,
          attempts: [],
        };
      } else if (envelope.payload.phase === "attempt") {
        candidateState.atomic.attempts.push({
          record_oid: predecessor,
          attempt_evidence_digest:
            envelope.payload.attempt_evidence_digest,
          attachment_manifest_digest:
            envelope.payload.attachment_manifest.manifest_digest,
          outcome: envelope.payload.outcome,
        });
      } else {
        candidateState.completed = {
          complete_record_oid: predecessor,
          cycle_receipt_digest:
            envelope.payload.stable_cycle_receipt_digest,
        };
        candidateState.atomic = null;
      }
    }
  }
  return {
    checkpoint_commit_sha: checkpointCommitSha,
    transaction_tip_commit_sha: predecessor,
  };
}

function fixtureCandidateInventorySourceRecord(
  recordOid,
  parentOid,
  envelope,
) {
  return {
    record_oid: recordOid,
    parent_oid: parentOid,
    sequence: envelope.sequence,
    phase: envelope.payload.phase,
    cycle_id: envelope.payload.cycle_id,
    envelope_digest: envelope.envelope_digest,
    payload_digest: envelope.payload_digest,
    workflow_provenance_receipt_digest:
      envelope.workflow_provenance.receipt_digest,
    workflow_provenance_replay_identity:
      fixtureProvenanceReplayIdentity(envelope.workflow_provenance),
    server_observed_at: envelope.server_observed_at,
  };
}

function fixtureCandidateInventoryAuthorityDigest(state) {
  const sourceRecordDigest = digestCanonical(
    "codex-review-gate-v2-candidate-inventory-source-records",
    state.source_records,
  );
  return digestCanonical(
    "codex-review-gate-v2-candidate-inventory-authority",
    {
      repository: REPOSITORY,
      source_record_digest: sourceRecordDigest,
      completed_cycle_record_oid:
        state.completed?.complete_record_oid ?? null,
      completed_cycle_receipt_digest:
        state.completed?.cycle_receipt_digest ?? null,
      incomplete_cycle_id: null,
      incomplete_start_record_oid: null,
      incomplete_next_shard_index: null,
      atomic_refresh_id: state.atomic?.refresh_id ?? null,
      atomic_start_record_oid: state.atomic?.start_record_oid ?? null,
      atomic_next_attempt_index: state.atomic?.attempts.length ?? null,
    },
  );
}

function fixtureProvenanceReplayIdentity(provenance) {
  return provenance.claims.jti === undefined
    ? `jwt:${provenance.token_sha256}`
    : `jti:${provenance.claims.jti}`;
}

function storeRewrittenFixtureCommit({
  fixture,
  original,
  treeSha,
  parents,
}) {
  const commitBytes = Buffer.from([
    `tree ${treeSha}`,
    ...parents.map((parent) => `parent ${parent}`),
    `author ${original.author.name} <${original.author.email}> ` +
      gitTimestamp(original.author.date),
    `committer ${original.committer.name} <${original.committer.email}> ` +
      gitTimestamp(original.committer.date),
    "",
    original.message,
  ].join("\n"), "utf8");
  const commitSha = gitObjectSha("commit", commitBytes);
  fixture.commits.set(commitSha, {
    ...structuredClone(original),
    sha: commitSha,
    tree: { sha: treeSha },
    parents: parents.map((parent) => ({ sha: parent })),
  });
  return commitSha;
}

function response(status, value, date) {
  const body = JSON.stringify(value);
  const headers = { Date: date, "Content-Type": "application/json" };
  return new Response(body, { status, headers });
}

function rawDigest(value) {
  return `sha256:${createHash("sha256").update(value, "utf8").digest("hex")}`;
}

function gitObjectSha(type, bytes) {
  const header = Buffer.from(`${type} ${bytes.byteLength}\0`, "utf8");
  return createHash("sha1").update(header).update(bytes).digest("hex");
}

function canonicalGitTreeBytes(entriesValue) {
  const entries = [...entriesValue].sort((left, right) => Buffer.compare(
    Buffer.from(`${left.path}${left.type === "tree" ? "/" : ""}`, "utf8"),
    Buffer.from(`${right.path}${right.type === "tree" ? "/" : ""}`, "utf8"),
  ));
  return Buffer.concat(entries.map((entry) => Buffer.concat([
    Buffer.from(`${entry.mode} ${entry.path}\0`, "utf8"),
    Buffer.from(entry.sha, "hex"),
  ])));
}

function canonicalGitTreeSha(entriesValue) {
  return gitObjectSha("tree", canonicalGitTreeBytes(entriesValue));
}

function gitTimestamp(value) {
  return `${Math.floor(Date.parse(value) / 1000)} +0000`;
}

function digestCanonical(domain, value) {
  return rawDigest(`${domain}\0${canonicalJson(value)}`);
}

function candidateDigestCanonical(domain, value) {
  return rawDigest(`${domain}\n${canonicalJson(value)}\n`);
}

function runnerDigestCanonical(domain, value) {
  const domainBytes = Buffer.from(domain, "utf8");
  const valueBytes = Buffer.from(canonicalJson(value), "utf8");
  const hash = createHash("sha256");
  hash.update(`${domainBytes.length}:`);
  hash.update(domainBytes);
  hash.update("\0");
  hash.update(`${valueBytes.length}:`);
  hash.update(valueBytes);
  return `sha256:${hash.digest("hex")}`;
}

function canonicalJson(value) {
  if (Array.isArray(value)) {
    return `[${value.map((item) => canonicalJson(item)).join(",")}]`;
  }
  if (value !== null && typeof value === "object") {
    return `{${Object.keys(value).sort().map((key) =>
      `${JSON.stringify(key)}:${canonicalJson(value[key])}`).join(",")}}`;
  }
  return JSON.stringify(value);
}
