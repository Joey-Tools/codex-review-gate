{
  "name": "codex-review-gate",
  "version": "1.2.1",
  "description": "Deterministic GitHub status gate for Codex pull request reviews.",
  "type": "module",
  "scripts": {
    "check": "node --check src/core.mjs && node --check src/gate.mjs && node --check test/core.test.mjs && node --check test/gate-runner.test.mjs && node --check test/support/fake-github-fetch.mjs",
    "check:state-machine": "node --check src/core.mjs && node --check src/gate.mjs && node --check test/gate-runner.test.mjs && node --check test/support/fake-github-fetch.mjs",
    "test": "node --test",
    "test:state-machine": "node --test test/gate-runner.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JoeyTeng/codex-review-gate.git"
  },
  "license": "Apache-2.0"
}
