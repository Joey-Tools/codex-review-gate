{
  "name": "codex-review-gate-source",
  "version": "1.5.2",
  "private": true,
  "description": "Source workspace for the Codex Review Gate action.",
  "type": "module",
  "scripts": {
    "check": "npm run check:action && npm run check:bootstrap && node --check scripts/generate-action-release-provenance.mjs && node --check test/core.test.mjs && node --check test/evidence-budget.test.mjs && node --check test/gate-runner.test.mjs && node --check test/producer-receipt.test.mjs && node --check test/release-provenance.test.mjs && node --check test/workflow-security-contract.test.mjs && node --check test/support/fake-github-fetch.mjs",
    "check:action": "npm --prefix packages/action run check",
    "check:bootstrap": "node --check src/bootstrap.mjs && node --check scripts/bootstrap-codex-review-gate.mjs && node --check test/bootstrap.test.mjs",
    "check:state-machine": "npm run check:action && node --check test/gate-runner.test.mjs && node --check test/support/fake-github-fetch.mjs",
    "test": "node --test",
    "test:bootstrap": "node --test test/bootstrap.test.mjs",
    "test:receipt": "node --test test/producer-receipt.test.mjs",
    "test:release-provenance": "node --test test/release-provenance.test.mjs",
    "test:state-machine": "node --test test/gate-runner.test.mjs",
    "release:provenance": "node scripts/generate-action-release-provenance.mjs",
    "release:split": "scripts/release-action-subtree.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JoeyTeng/codex-review-gate.git"
  },
  "license": "Apache-2.0"
}
